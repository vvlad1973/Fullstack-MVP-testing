# Три режима ввода текста задания (PRD-57, этап Э13): план

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** текст задания набирается в любом из трёх режимов, и листинг, формула и пропуск
работают во всех трёх; переключение не теряет содержимого молча.

**Architecture:** формат — колонка вопроса, как у обратной связи и у описания теста. Выдача
ветвится в ОДНОМ месте (`promptHtmlOf`); санитайз, типографика по узлам, подсветка и
формулы берутся у существующих модулей.

**Tech Stack:** TypeScript, Drizzle + PostgreSQL, React 19, `@skillum/ui-kit`, Vitest.

**Устройство согласовано:**
[docs/specs/2026-09-20-prd57-e13-input-modes-design.md](../specs/2026-09-20-prd57-e13-input-modes-design.md).
Требования: [PRD-57](../specs/prd-57/open-response-and-formatting.md) §4.3, AC-01b.

**Границы.** Не делается: изображения в тексте задания, таблицы в разметке, свой санитайзер,
внешняя библиотека RTE.

---

## Task 1: Формат как свойство задания

- [ ] **Step 1:** падающие тесты — схема принимает три значения и умолчание; отсутствие
      формата читается как разметка; хеш содержимого от формата не зависит.
- [ ] **Step 2:** колонка `questions.prompt_format`, миграция, схема, приём в POST/PUT.
- [ ] **Step 3:** `npm test -- tests/question-prompt-format.test.ts`

## Task 2: Выдача размеченного текста

- [ ] **Step 1:** падающие тесты — у HTML-задания типографика идёт по текстовым узлам и не
      трогает атрибуты и код; листинг `<pre><code class="language-sql">` подсвечивается;
      формула и пропуск работают в обоих форматах.
- [ ] **Step 2:** ветка формата в `promptHtmlOf` + приведение листинга к внутренней форме.
- [ ] **Step 3:** `npm test -- tests/prompt-html-format.test.ts`

## Task 3: Текст словами и книга

- [ ] **Step 1:** падающие тесты — `stripMarkdown`/`renderPlainText` снимают разметку HTML;
      книга возит формат колонкой; круг «экспорт — импорт» его сохраняет.
- [ ] **Step 2:** проекции текста и колонка «Формат текста».
- [ ] **Step 3:** `npm test -- tests/plain-text-format.test.ts tests/workbook-prompt-format.test.ts`

## Task 4: Отчёт о потерях при переключении

- [ ] **Step 1:** падающие тесты — четыре перехода, числа находок, «без потерь» там, где
      данные не меняются.
- [ ] **Step 2:** `shared/text/mode-switch.ts` — чистая функция отчёта.
- [ ] **Step 3:** `npm test -- tests/mode-switch.test.ts`

## Task 5: Переключатель и режим HTML

- [ ] **Step 1:** падающие тесты — переключатель над полем, режим HTML правится текстом,
      переключение показывает отчёт до согласия автора.
- [ ] **Step 2:** переключатель в ящике + поле HTML + модалка перехода.
- [ ] **Step 3:** `npm test -- client/src/features/questions/__tests__`

## Task 6: Редактор «Форматированный»

- [ ] **Step 1:** падающие тесты — атом не рвётся набором и удаляется целиком; сериализация
      возвращает исходник; панель форматирует выделение.
- [ ] **Step 2:** `rich-prompt-editor.tsx` — contenteditable с атомами и панелью.
- [ ] **Step 3:** `npm test -- client/src/features/questions/__tests__`

## Task 7: Документация

- [ ] **Step 1:** руководство автора и руководство книги — три режима и колонка формата.
- [ ] **Step 2:** `npm run check` и markdownlint по изменённым файлам.

## Task 8: Приёмка

- [ ] **Step 1:** затронутые наборы целиком.
- [ ] **Step 2:** живой прогон: задание с листингом, формулой и пропуском в каждом из трёх
      режимов; переключение с отчётом; один и тот же экран у участника (AC-01b).
