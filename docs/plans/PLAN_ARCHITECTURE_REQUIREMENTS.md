# План приведения проекта test-builder в соответствие требованиям

## Метаданные

- Дата составления: 2026-06-07
- Дата актуализации: 2026-07-05 (статусы задач перепроверены по коду версии 2.7.0-beta)
- Класс системы: BO (по [НФТ])
- Базовый отчёт: [AUDIT_ARCHITECTURE_REQUIREMENTS.md](../reports/AUDIT_ARCHITECTURE_REQUIREMENTS.md)
- Эталон: `ENGINERING_HANDBOOK/handbook/standards/ARCHITECTURE_REQUIREMENTS.md`

## Легенда статусов задач

- Выполнено -- задача закрыта в коде/конфигурации репозитория.
- Частично -- сделан задел или закрыта часть подпунктов.
- Открыто -- не начато.
- Оргмера -- выполняется вне репозитория (развёртывание, провайдеры, согласования); по коду не проверяется.

## Прогресс на 2026-07-05

Закрыто: фаза 0 в части репозитория (утечка секретов). Оставшиеся действия фазы 0 -- организационные
(ротация секретов у провайдеров, ре-шифрование email при смене ключей, переписывание истории git).

Частичные заделы (не закрывают задачи целиком, но приближают):

- 1.7: `forgot-password` не раскрывает существование email, вход выровнен по времени
  (`dummyVerifyPassword`); остаются `check-email` и ответ о деактивации.
- 2.1: `cookie.secure` вынесен в конфиг (`config.server.cookieSecure`); сам TLS в приложении не enforced.
- 4.6: введён слой конфигурации (`config/*.config.jsonc` + `.env`), но горячей перезагрузки нет.
- 7.1: класс BO и целевые параметры зафиксированы в этом плане.

Смежные изменения вне задач плана, влияющие на контекст: пароли переведены с `bcrypt` на `scrypt`
(PRD-9) -- влияет на реализацию 1.2 (сверка истории идёт через `verifyPassword`, а не `bcrypt.compare`);
логирование переведено на Pino с редактированием секретов -- задел под 4.x; ролевая модель заменена
на модель прав (PRD-13, 5 ролей) -- смежно с 3.5. Остальные фазы (1-6, кроме отмеченного) -- открыты.

## Влияние класса BO на объём работ

Класс BO задаёт целевые параметры эксплуатации (раздел 2): режим 16x5, доступность не менее 98%,
RTO не более 24 часов, RPO не более 24 часов. Это смягчает часть требований:

- Среды (раздел 1): обязательны PrePROD/TEST/DEV только для классов MC и BC. Для BO допустим
  упрощённый набор сред; критичность изоляции контуров ниже.
- Доступность (раздел 2): 98% и RTO/RPO до 24 часов достижимы на одном узле с ежедневным
  резервным копированием; кластер высокой доступности не обязателен.
- Горизонтальное масштабирование (req 31) остаётся общим требованием, но для BO это среднеприоритетная
  задача, а не блокер.

Требования информационной безопасности (разделы 5-9) и регистрации событий (раздел 8) от класса
не зависят и применяются в полном объёме. Поэтому контур ИБ -- приоритет плана.

## Принципы планирования

- Сначала устраняем фактическую утечку секретов, затем выстраиваем контур ИБ, затем устойчивость
  и документацию.
- Каждая задача завязана на конкретные файлы репозитория и имеет критерий приёмки.
- Трудоёмкость указана ориентировочно, в человеко-днях (ч-д), без учёта согласований.

## Дорожная карта по фазам

| Фаза | Цель | Разделы эталона | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| 0 | Устранение утечки секретов | 9.3, 9.4 | Критический | Выполнено (репо) / Оргмера |
| 1 | Парольная политика и защита веб-периметра | 7, 9.1 | Высокий | Открыто (частично 1.7) |
| 2 | Транспорт: TLS и каналы | 5.2, 5.3 | Высокий | Открыто (частично 2.1) |
| 3 | Аутентификация, сессии, доступ | 6 | Высокий | Открыто |
| 4 | Аудит, логирование, эксплуатация | 8, 9.5, 3 | Средний | Открыто (частично 4.6) |
| 5 | Доступность и устойчивость (BO) | 2, 11 | Средний | Открыто / Оргмера |
| 6 | Зависимости и проверка кода | 9.2, 9.4 | Средний | Открыто |
| 7 | Документация и фиксация класса | 13, 2 | Средний | Частично (7.1 выполнено) |

## Фаза 0. Устранение утечки секретов

| ID | Задача | Требование | Трудоёмкость | Статус |
| --- | --- | --- | --- | --- |
| 0.1 | Убрать `.env copy` из индекса git, расширить `.gitignore` | 9.3 | 0.25 ч-д | Выполнено |
| 0.2 | Отозвать и сменить все скомпрометированные секреты | 9 | 0.5 ч-д | Оргмера |
| 0.3 | Ре-шифрование email при смене ключей шифрования | 7.1 | 1 ч-д | Инструмент готов / Оргмера |
| 0.4 | Очистить секреты из истории git | 9.3 | 0.5 ч-д | Оргмера |

Реализация:

- 0.1 (Выполнено): в `.gitignore` добавлено правило `.env*` с исключением `!.env.example`;
  из индекса выведены `.env copy` и `docker/build/config/deploy.env`, реальные пароли БД/SMTP
  вычищены из `docker/build/env/.env.example`. В `git ls-files` секретов нет.
- 0.2 (Оргмера): сменить пароль пользователя БД, `SESSION_SECRET`, `SMTP_PASS`, `ENCRYPTION_PASSWORD`,
  `ENCRYPTION_SALT`, а также пароль почтового ящика на стороне провайдера. Регламент -- в
  [secret-rotation.md](../runbooks/secret-rotation.md).
- 0.3 (Инструмент готов / Оргмера): смена `ENCRYPTION_PASSWORD`/`ENCRYPTION_SALT` делает ранее
  зашифрованные email нечитаемыми. Готов одноразовый скрипт `script/reencrypt-emails.ts`
  (`npm run reencrypt-emails`): расшифровать старым ключом, перешифровать новым
  (модуль `server/utils/crypto.ts`). Выполнять в сервисном окне при фактической ротации.
- 0.4 (Оргмера): историю вычистить через `git filter-repo` или BFG, затем форсированно обновить
  удалённые ветки (origin и форк upstream) и оповестить владельца репозитория.

Критерий приёмки: в `git ls-files` нет файлов с секретами (выполнено); все ключи заменены и история
очищена (за оператором); email расшифровывается корректно после ротации.

## Фаза 1. Парольная политика и защита веб-периметра

| ID | Задача | Требование | Трудоёмкость | Статус |
| --- | --- | --- | --- | --- |
| 1.1 | Единый серверный валидатор пароля | 7.2 п.1-4 | 1.5 ч-д | Открыто |
| 1.2 | История паролей (N предыдущих) | 7.2 п.6 | 1 ч-д | Открыто |
| 1.3 | Принудительная периодическая смена пароля | 7.2 п.8 | 1 ч-д | Открыто |
| 1.4 | Блокировка УЗ после N неудач на M минут | 7.4 | 1.5 ч-д | Открыто |
| 1.5 | Запрет копирования из поля пароля | 7.3 | 0.25 ч-д | Открыто |
| 1.6 | Security-заголовки и отключение `X-Powered-By` | 9.1 | 0.5 ч-д | Открыто |
| 1.7 | Устранение раскрытия существования учётной записи | 9.1 | 0.5 ч-д | Частично |

Реализация:

- 1.1 (Открыто): вынести правила в `shared/` (длина не менее 10, минимум 3 из 4 категорий
  A-Z/a-z/0-9/спецсимволы, запрет имени и email в пароле, отклонение пустых и словарных паролей)
  и применить во всех точках: `server/routes/auth.ts` (change-password, reset-password,
  complete-first-login -- сейчас везде порог `< 6`, строки 117/288/344), `server/routes/users.ts`
  (создание и сброс), `script/create-admin.ts`. Серверная проверка -- основная.
- 1.2 (Открыто): таблицы истории нет. Добавить `password_history` (`shared/schema.ts`), хранить хэши
  последних N; при смене сверять через `verifyPassword` (`server/utils/crypto.ts`; примечание: хэши
  теперь scrypt, а не bcrypt).
- 1.3 (Открыто): колонки нет. Добавить `last_password_change_at`, конфигурируемый период (config/env),
  проверка при входе с переводом на принудительную смену.
- 1.4 (Открыто): текущий лимитер только по IP и в памяти (`server/routes/auth.ts:13-28`, теряется при
  рестарте). Добавить счётчик неудач и блокировку на уровне учётной записи, параметры N и M в конфиге;
  при ошибке сообщать число оставшихся попыток, не уточняя, что именно неверно. Перенести состояние в БД.
- 1.5 (Открыто): в `client/src/components/password-input.tsx` добавить запрет `onCopy`.
- 1.6 (Открыто): `helmet` не подключён, `app.disable("x-powered-by")` не вызывается. Подключить `helmet`
  в `server/index.ts` (CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy) и отключить
  `X-Powered-By`.
- 1.7 (Частично): `forgot-password` уже отвечает единообразно (`server/routes/auth.ts:203-208`), вход
  выровнен по времени (`dummyVerifyPassword`, `server/utils/crypto.ts:185-188`). Осталось: убрать или
  закрыть аутентификацией эндпоинт `POST /api/auth/check-email` (`server/routes/auth.ts:177-190`)
  и привести ответ о деактивированной УЗ (`server/routes/auth.ts:53`) к обезличенному виду на входе.

Критерий приёмки: слабый пароль отклоняется сервером во всех сценариях; повтор последних N паролей
запрещён; после N неудач вход блокируется на M минут; в ответах нет `X-Powered-By` и признаков
существования УЗ.

## Фаза 2. Транспорт: TLS и каналы

| ID | Задача | Требование | Трудоёмкость | Статус |
| --- | --- | --- | --- | --- |
| 2.1 | HTTPS, редирект HTTP->HTTPS, HSTS, политика шифров | 5.3 | 1 ч-д | Открыто (частично) |
| 2.2 | SSL-подключение к PostgreSQL | 5.2 | 0.5 ч-д | Открыто |
| 2.3 | Ограничение CORS телеметрии SCORM | 5, 11 | 0.5 ч-д | Открыто |

Реализация:

- 2.1 (Частично): TLS терминируется на обратном прокси (приложение уже работает за прокси,
  `server/index.ts:35`). `cookie.secure` уже конфигурируем (`config.server.cookieSecure`,
  `server/routes.ts:73`). Осталось: добавить в репозиторий эталонный конфиг прокси (nginx) --
  минимальная версия TLS 1.2 (рекомендуется 1.3), запрещённые наборы
  `!EXP:!NULL:!ADH:!LOW:!SSLv2:!SSLv3:!MD5:!RC4`, рекомендуемые
  `EECDH+CHACHA20:EECDH+AESGCM:EDH+AESGCM`, редирект с HTTP и заголовок HSTS; в продуктивной
  конфигурации выставить `COOKIE_SECURE=true`.
- 2.2 (Открыто): в пуле `server/db.ts:35-40` нет параметра `ssl`. Добавить `ssl` и `sslmode=require`
  в `DATABASE_URL` для удалённой БД.
- 2.3 (Открыто): в `server/routes.ts:55-63` по-прежнему `Access-Control-Allow-Origin: *`. Заменить на
  список разрешённых источников LMS либо обосновать необходимость широкого доступа.

Критерий приёмки: веб и API доступны только по HTTPS с TLS не ниже 1.2 и заданными шифрами;
канал к БД зашифрован; CORS телеметрии ограничен.

## Фаза 3. Аутентификация, сессии, доступ

| ID | Задача | Требование | Трудоёмкость | Статус |
| --- | --- | --- | --- | --- |
| 3.1 | Перенос хранилища сессий в PostgreSQL | 6.5, 2 | 1 ч-д | Открыто |
| 3.2 | Принудительное завершение сессий пользователя | 6.5 | 1 ч-д | Открыто |
| 3.3 | Мониторинг активных сессий и уведомления о новом входе | 6.5 | 2 ч-д | Открыто |
| 3.4 | Корпоративная аутентификация либо обоснование локальной | 6.1 | 3-5 ч-д | Открыто |
| 3.5 | Админ-фильтры пользователей по статусу и группам | 6.3 | 1 ч-д | Открыто |
| 3.6 | Технологическая УЗ БД с ограниченными правами | 6.2, 6.4 | 0.5 ч-д | Оргмера |

Реализация:

- 3.1 (Открыто): заменить `memorystore` (`server/routes.ts:68`) на хранилище на PostgreSQL. Это
  устраняет потерю сессий при рестарте и делает приложение stateless по сессиям (см. фазу 5).
- 3.2 (Открыто): после 3.1 реализовать инвалидацию всех сессий пользователя при деактивации
  и по команде админа.
- 3.3 (Открыто): фиксировать устройство/IP сессии, выводить список активных сессий пользователю,
  отправлять уведомление при входе с нового IP или устройства.
- 3.4 (Открыто): целевой вариант -- OIDC через единую корпоративную систему аутентификации. Допустимый
  для BO промежуточный вариант -- задокументированное обоснование локальной аутентификации при условии
  выполненной парольной политики (фаза 1) согласно [ИБ IBv004-IAF-006].
- 3.5 (Открыто): в API списка пользователей (`server/routes/users.ts:24-39` возвращает всех) добавить
  серверные фильтры по статусу (активна/заблокирована) и по группам. Примечание: ролевая модель
  уже переведена на модель прав (PRD-13, `requirePermission`), но это отдельная от req 50 задача.
- 3.6 (Оргмера): создать роль БД без прав администратора СУБД, использовать её в `DATABASE_URL`;
  зафиксировать отсутствие групповых и неперсонифицированных УЗ.

Критерий приёмки: сессии переживают рестарт; деактивация немедленно прекращает все сессии УЗ;
пользователь видит свои сессии и получает уведомление о новом входе; выбран и реализован/обоснован
способ аутентификации; админ-список фильтруется по статусу и группам.

## Фаза 4. Аудит, логирование, эксплуатация

| ID | Задача | Требование | Трудоёмкость | Статус |
| --- | --- | --- | --- | --- |
| 4.1 | Полнота событий и атрибутов аудита | 8.1, 8.2 | 1.5 ч-д | Открыто |
| 4.2 | Экспорт журналов в SIEM | 8.3 | 1.5 ч-д | Открыто |
| 4.3 | Защита журналов от изменений | 8.3 | 1 ч-д | Открыто |
| 4.4 | Просмотр журнала аудита через интерфейс администратора | 8.3 | 1 ч-д | Открыто |
| 4.5 | Настройка уровня логирования без перезапуска | 8.3 | 0.5 ч-д | Открыто |
| 4.6 | Изменение конфигурации без простоя | 9.5 | 1-2 ч-д | Открыто (частично) |
| 4.7 | Исправление пробы healthcheck и режимов работы | 3, 12 | 0.5 ч-д | Открыто |

Реализация:

- 4.1 (Открыто): дополнить `server/logger.ts:296-315` событиями изменения роли/привилегий, изменения
  параметров безопасности, удаления УЗ, отказа в доступе (403). Добавить в запись атрибуты
  (`server/logger.ts:240-256`): идентификатор приложения (название и версия), уровень критичности,
  сервис (протокол и название), IP источника во всех событиях.
- 4.2 (Открыто): добавить конфигурируемый транспорт журналов во внешний приёмник (Syslog или
  Elastic Beats). Логирование уже на Pino (`@vvlad1973/pino-logger-tree`) -- база для транспорта есть.
- 4.3 (Открыто): ограничить права на файлы журналов и/или дублировать в защищённый внешний приёмник;
  опционально цепочка хэшей для контроля целостности. Редактирование секретов в записях уже включено
  (`server/logger.ts:122`).
- 4.4 (Открыто): добавить API и экран автора для просмотра журнала аудита. Сейчас через интерфейс
  доступен только журнал недавних событий приложения (`GET /api/logs`,
  гейт `requirePermission("logs.read")`, `client/src/pages/author/logs.tsx`); файл аудита UI не имеет.
- 4.5 (Открыто): уровни логирования строятся из конфига один раз при инициализации дерева логгеров;
  вынести уровень в параметр, изменяемый без перезапуска.
- 4.6 (Частично): введён слой конфигурации (`server/config.ts` + `config-loader.mjs` +
  `config/*.config.jsonc`), но параметры читаются один раз при старте. Реализовать перечитывание
  параметров, допускающих горячую перезагрузку; для остальных задокументировать сервисное окно.
- 4.7 (Открыто): в `docker/templates/docker-compose.yml:41` заменить пробу `/api/me` на
  `/api/health`; описать штатный/сервисный/аварийный режимы (heartbeat процесса уже есть,
  `server/logger.ts:259-273`). Вторая копия пробы, в `docker/build/config/docker-compose.yml`,
  отпала вместе с удалением мёртвого каталога `docker/build/` (2026-09-21).

Критерий приёмки: фиксируются все обязательные события с полным набором атрибутов; журналы уходят
в SIEM и защищены от изменений; аудит виден в интерфейсе администратора; уровень логирования
меняется без перезапуска; проба healthcheck корректна.

## Фаза 5. Доступность и устойчивость (с учётом класса BO)

| ID | Задача | Требование | Трудоёмкость | Статус |
| --- | --- | --- | --- | --- |
| 5.1 | Резервное копирование БД и процедура восстановления | 2 (RPO/RTO) | 1 ч-д | Оргмера |
| 5.2 | Хранение медиа: фиксация одноузлового варианта | 2, 31 | 0.5 ч-д | Открыто |
| 5.3 | Готовность к горизонтальному масштабированию | 31 | 0.5 ч-д | Открыто |

Реализация:

- 5.1 (Оргмера): ежедневное резервное копирование БД удовлетворяет RPO до 24 часов; задокументировать
  регламент и проверить восстановление в пределах RTO 24 часов.
- 5.2 (Открыто): для BO одноузловое хранение `uploads/media` допустимо; зафиксировать это как ограничение,
  предусмотреть перенос в общее объектное хранилище при росте нагрузки.
- 5.3 (Открыто): зависит от 3.1 -- после переноса сессий в PostgreSQL приложение становится stateless
  по сессиям; зафиксировать оставшееся ограничение по локальным медиа как условие масштабирования.

Критерий приёмки: есть регламент резервного копирования и восстановления в рамках RTO/RPO класса BO;
ограничения масштабирования задокументированы.

## Фаза 6. Зависимости и проверка кода

| ID | Задача | Требование | Трудоёмкость | Статус |
| --- | --- | --- | --- | --- |
| 6.1 | Внутренний реестр npm и сборка без публичной сети | 9.4 | 1 ч-д | Открыто |
| 6.2 | Сканирование уязвимостей зависимостей | 9.2 | 1 ч-д | Открыто |
| 6.3 | Политика хранения исходного кода | 9.3 | оргмера | Оргмера |

Реализация:

- 6.1 (Открыто): `.npmrc` в репозитории нет. Настроить `.npmrc` на внутренний реестр или прокси;
  обеспечить установку зависимостей без прямого доступа в публичную сеть.
- 6.2 (Открыто): в сборке/CI нет проверки уязвимостей. Добавить проверку (например, `npm audit`/SCA)
  в процесс сборки; зафиксировать отсутствие недостатков критического и высокого уровня на момент сдачи.
- 6.3 (Оргмера): согласовать размещение исходного кода на ресурсах организации (вынос с внешнего GitHub
  либо официальное согласование).

Критерий приёмки: зависимости ставятся из внутреннего источника; отчёт сканирования без критических
и высоких уязвимостей; вопрос хранения кода закрыт.

## Фаза 7. Документация и фиксация класса

| ID | Задача | Требование | Трудоёмкость | Статус |
| --- | --- | --- | --- | --- |
| 7.1 | Фиксация класса BO и целевых параметров | 2 | 0.5 ч-д | Выполнено |
| 7.2 | Комплект документов технической архитектуры | 13 | 3-5 ч-д | Частично |

Реализация:

- 7.1 (Выполнено): класс BO и параметры (16x5, доступность не менее 98%, RTO до 24 часов, RPO до
  24 часов) зафиксированы в этом плане; продублировать в ТЗ и описании системы.
- 7.2 (Частично): документация реорганизована по каталогам (`docs/reports/`, `docs/plans/`,
  `docs/runbooks/`). Остаются не подготовленными документы из [НФТ req 40]: ПМИ, регламент действий
  при сбоях и авариях, карточка мониторинга, регламент эксплуатации, документ по масштабированию,
  расчёт доступности, лицензии и подтверждение прав, схема структурная КТС, интеграционная архитектура
  с протоколами и портами.

Критерий приёмки: класс и параметры зафиксированы (выполнено); комплект документов раздела 13
укомплектован.

## Очерёдность и зависимости

1. Фаза 0 выполнена в части репозитория; остаток -- организационный (ротация, история git).
2. Фаза 1 и фаза 2 идут параллельно -- обе блокируют допуск к продуктивной эксплуатации.
3. Фаза 3 зависит от 3.1 (хранилище сессий) как от базы для 3.2 и 3.3; фаза 3 опирается на фазу 1
   (усиленная политика) для варианта обоснования локальной аутентификации.
4. Фаза 4 опирается на стабилизированный контур доступа из фазы 3.
5. Фазы 5-7 завершают приведение к требованиям и сдачу.

## Ориентировочная суммарная трудоёмкость

- Контур ИБ (фазы 0-3): порядка 20-25 ч-д (за вычетом выполненной фазы 0 -- около 19-23 ч-д остатка).
- Эксплуатация и устойчивость (фазы 4-5): порядка 10-12 ч-д.
- Зависимости и документация (фазы 6-7): порядка 8-12 ч-д плюс организационные согласования.

Итого по коду и конфигурации: ориентировочно 38-49 ч-д, без учёта согласований, развёртывания
инфраструктуры (прокси, SIEM, резервное копирование) и подготовки документов вне репозитория.
