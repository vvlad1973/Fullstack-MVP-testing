# План: перестройка настроек редактора теста и подытоги без вердикта

> **Исполнителю:** ОБЯЗАТЕЛЬНЫЙ ПОДНАВЫК — вести работу задача за задачей через
> `superpowers:subagent-driven-development` либо `superpowers:executing-plans`.
> Шаги помечены чекбоксами (`- [ ]`) для отметки готовности.

**Цель:** привести редактор теста к целевому дереву из
[аудита](../reports/AUDIT_TEST_EDITOR_SETTINGS.md) §3.1 и убрать из расчёта подытогов вердикт
подтемы: подтема считается и показывается, но не судится.

**Источники решений:** аудит §3.6 (двадцать решений владельца),
[PRD-50](../specs/prd-50/result-breakdowns.md) §14 (FR-49 - FR-51),
[PRD-29](../specs/prd-29/measurement-results.md) §7.1a (обратная связь темы — замена),
[PRD-19](../specs/prd-19/skip-return-navigation.md) §3.1 (FR-11a - FR-11d).

**Целевое дерево верхнего уровня:** Основное · Состав и сценарий · Правила прохождения ·
Оценка результата · Обратная связь и итоги · Оформление.

## Границы

В плане: расчёт подытогов, обратная связь на подтему, перестройка вкладок и рейлов,
переименования с алиасами книги, свободная навигация внутри раздела, правка поставляемых
шаблонов и контракта, документация, приёмка.

Вне плана: перевод существующих тестов с блоков разделов на подтемы (отдельная работа с
данными заказчика), медиатека, психометрическая аналитика.

## Э0. Эскизы и согласование

- [x] Э0.1. Эскизы шести вкладок целевого дерева: Основное, Состав и сценарий, Правила
      прохождения, Оценка результата, Обратная связь и итоги, Оформление —
      [editor-settings-target.html](../wireframes/editor-settings-target.html), состояния
      `s-basic`, `s-composition`, `s-rules`, `s-scoring`, `s-feedback`, `s-design`.
- [x] Э0.2. Эскизы новых экранов: реестр «По вопросам» деревом «тема → вопросы» (состояние
      `s-during`), реестры «По темам», «По подтемам (тегам)», «По уровням» и вводный текст
      (`s-texts`), «Состав итогов» (`s-feedback`). Тексты правятся существующей модалкой
      `tb-feedback-editor`, поэтому вкладка показывает реестр с переходом в неё, а не второй
      редактор.
- [x] Э0.3. Карта вопросов и экран обзора уже нарисованы в `prd19-learner-real.html`;
      добавлено состояние «свободная навигация» — дельта FR-11a: пиллы раздела кликабельны
      все, статуса «не выдан» нет, «Пропустить» уступает место паре «Назад» / «Далее».
- [x] Э0.4. Пустое состояние «Оформления» — `s-design-empty`: в рейле остаётся только
      «Шаблон», панель показывает `ou-empty` с объяснением, что параметров шаблон не объявил.
- [x] Э0.6. Ревизия эскизов на деградации: три ревьюера сверили рисунок с исходниками
      («Оценка результата», «Обратная связь и итоги», остальные вкладки). Потери закрыты,
      решения владельца записаны в [аудит](../reports/AUDIT_TEST_EDITOR_SETTINGS.md)
      (решения 21-26), разбор — в [отчёте](../reports/REVIEW_E0_WIREFRAMES.md), раздел J.
- [x] Э0.7. Дорисованы состояния, которых не хватало: ступенчатая цена ответа и веса
      опций, числовой показатель и конструктор формулы, палитра блоков документа, выбор
      темы, полный список надписей итогов, блокирующая валидация, пустые панели, список
      изменений черновика, раскрытая строка блока отчёта. Итого 34 состояния; каждое
      проверено в браузере: подвал на месте, разделы в колонке настроек, ничего не уезжает,
      интервалы только 4/16/24.
- [x] Э0.5. Согласование эскизов с владельцем — принято 2026-09-03. Код открыт.

## Э1. Подытоги без вердикта

- [x] Э1.1. Убрать `applyBreakdownGate` из `shared/scoring/aggregate.ts`: вердикт темы считается
      только её правилом; поле `passed` у `BreakdownEntry` снимается вместе с ним.
- [x] Э1.2. `shared/template/result-context.ts`: строка разреза перестаёт нести `passed`,
      `passClass` и `statusLabel`.
- [x] Э1.3. Перестать читать `test_sections.breakdown_rules_json` при расчёте; колонку и данные
      оставить (легаси, чтение только для миграции и переноса).
- [x] Э1.4. Снять предупреждение публикации FR-46 «порог назначен ключу, которого выдача не
      даст» из `shared/breakdown/publish-warnings.ts` и его вызовов.
- [x] Э1.5. Закрепить тестом инвариант: подытог считается по ЭФФЕКТИВНОЙ оценке вопроса в этом
      тесте (цена и схема из `effective-scoring`), частичный ответ даёт долю, измерительный
      вопрос в разрез не попадает.
- [x] Э1.6. Предупреждение при публикации теста, где пороги ключей были заданы: вердикт темы
      может измениться. Текст — «подтема больше не влияет на вердикт темы».
- [x] Э1.7. Обновить тесты: `shared/breakdown/*`, `shared/scoring/aggregate*`, `tests/report*`,
      снимки контекста обоих хостов.

## Э2. Обратная связь: подтема, тема, уровни, реестр вопросов

- [x] Э2.1. Хранение обратной связи подтемы (FR-50): своя колонка раздела
      `test_sections.breakdown_feedback_json` (миграция `0026`), формат — как у темы
      (`format`, `text`, `links`, `assets`, `events`). Правит карточка «По подтемам (тегам)»
      в подразделе «Обратная связь».
- [x] Э2.2. Выдача текста подтемы обоими хостами: веб-хост (экран и отчёт) и рантайм пакета
      (обычный и адаптивный экраны), один отбор — `shared/breakdown/feedback`. Правило
      владельца 2026-09-03: текст выдаётся, когда результат по подтеме НИЖЕ общего
      проходного порога теста, при ЛЮБОМ вердикте. Порог в баллах переводится в долю от
      достижимого; порога нет — условия нет, выдаётся всё написанное.
- [x] Э2.3. Обратная связь темы: ЗАМЕНА вместо сложения (PRD-29 §7.1a). `topicFeedbackTexts`
      разрешает ОДНО значение (текст раздела, иначе текст темы) — одно правило на оба хоста.
      Правка переехала из «Состава» в карточку «По темам» подраздела «Обратная связь»: там
      виден разрешённый текст, подпись источника («Из темы» / «Задано в этом тесте») и
      «Сбросить до установок темы». Материалы (PRD-32) по-прежнему складываются: замена
      объявлена решением для ТЕКСТА, и трогать вложения без отдельного решения нельзя.
- [x] Э2.4. Реестр «По вопросам»: дерево «раздел → вопросы», только чтение, переход в редактор
      вопроса; свёрнуто до разделов, кнопки «Развернуть все» и «Свернуть все». Стоит в
      подразделе «Во время теста»; вопрос открывается ящиком ПОВЕРХ ящика теста — тем же
      швом, что переход из комментария (PRD-52 FR-28).
- [x] Э2.5. Тексты адаптивных уровней переезжают в «Обратная связь и итоги → Обратная связь»,
      карточка «По уровням»: список строится по структуре лестницы, в самой лестнице текстов
      больше нет. Показывается только адаптивному тесту.
- [x] Э2.6. Заморозка новых текстов в снимке публикации; проброс в SCORM-пакет, книгу Excel и
      формат переноса `.tbtest`. Снимок и перенос везут колонку сами — оба копируют строку
      раздела целиком; пакет получил `section.breakdownFeedback`; книга — владельца
      «Подтема» на листах «Обратная связь» и «Рекомендации», со справкой и примером.
      Гард переноса новых ТАБЛИЦ не получил (добавилась колонка, а не таблица), но в нём
      классифицирована `test_review_comments` из соседнего трека.

## Э3. Перестройка ящика

- [x] Э3.1. Семь вкладок и их рейлы по §3.1; якорная навигация по ошибкам валидации
      переписывается под новые адреса. Седьмая вкладка «Комментарии» добавлена решением
      владельца 2026-09-03 поверх принятого дерева из шести: панель комментариев живёт в ящике
      редактора, а не сбоку. Панель подключена (трек согласования теста экспертами); у
      создаваемого теста вкладки нет — комментировать ещё нечего.
- [x] Э3.2. «Основное»: карточки «О тесте» и «Интеграция» (webhook, телеметрия, результат для
      LMS — переезжает из «Ограничений»).
- [x] Э3.3. «Состав и сценарий»: рейл «Состав» (темы и выборка, квоты, варианты), «Адаптивные
      уровни» (только структура лестницы), «Сценарий» (поле «Тип», «Показывать итоги раздела»,
      полотно). Карточка «Блоки итогов» из редактора убирается.
- [x] Э3.4. «Правила прохождения»: навигация, ограничения двумя группами (попытка и повторные
      попытки), правила показа по ходу (прогресс, прогресс-бар, уровень сложности), защита
      контента. Подписи причин у погашенных переключателей с адресом настройки-причины.
- [x] Э3.5. «Оценка результата»: оценка ответа, вердикт (правила оценки тем), шкалы,
      показатели.
- [x] Э3.6. «Обратная связь и итоги»: во время теста, состав итогов (порядок подблоков,
      подытоги, надписи), тексты после теста, отчёт.
- [x] Э3.7. «Оформление»: только выбор шаблона плюс группы и параметры, объявленные манифестом;
      пустые состояния для необъявленных групп.
- [x] Э3.8. Удалить из редактора всё, что переехало: старые пункты «Настроек» и «Оформления»,
      двойной «Предпросмотр отчёта», второй комплект надписей отчёта (наследование от итогов).

## Э4. Свободная навигация внутри раздела

- [x] Э4.1. Колонка теста и миграция; умолчание ВЫКЛ, существующие тесты не меняют поведения.
      `tests.allow_free_section_navigation`, миграция `0027`.
- [x] Э4.2. Правило доступности считает ОДИН общий построитель карты вопросов
      (`shared/template/question-progress-context.ts`) — оба хоста кормят его своим состоянием,
      двух независимых расчётов, как предполагал план, не понадобилось. Заодно экран обзора при
      свободе перечисляет весь охват: иначе он умолчал бы о вопросе, до которого один клик.
- [x] Э4.3. Зависимость от «возврата к неотвеченным» с подписью причины; проброс в снимок,
      пакет, книгу и перенос. Круг книги пройден сквозняком, а не проверкой сериализации.
- [x] Э4.4. Тесты: карта вопросов, экран-обзора, оба хоста, границы раздела и адаптивный режим;
      сторож паритета хостов. Приёмка в браузере —
      [отчёт](../reports/editor-e4-free-navigation-acceptance.md).

## Э5. Именование и совместимость книги

- [x] Э5.1. «Сценарий прохождения» → поле «Тип» в пункте «Сценарий»; старое имя параметра книги
      остаётся алиасом навсегда, шаблон книги печатает новое.
- [x] Э5.2. «Показывать правильные ответы после прохождения» → «после ответа» (Ф-19), тот же
      приём с алиасом; тексты валидации и i18n сводятся к одному имени.
- [x] Э5.3. Словарь: в интерфейсе «подтемы (теги)», «ключ» остаётся в коде и спецификациях.
      Переименовать «Подытоги по ключам», «База подытогов», «Где показывать подытоги».
- [x] Э5.4. «Правила прохождения тем» → «Правила оценки тем»; «Сменить вариант» на полотне →
      «Сменить макет» (слово «вариант» закреплено за формами выдачи PRD-17).
- [x] Э5.5. Единая подпись сценария: список выбора и баннер полотна перестают называть режимы
      по-разному (Ф-18).
- [x] Э5.6. «Место» подытогов: варианты называют место, а не «там» — «В карточках тем»,
      «Отдельным блоком в итогах», «В карточках тем и отдельным блоком».
- [x] Э5.7. Заголовок группы во «Время теста» — «Что видит ученик во время прохождения теста»
      (не «по ходу»).
- [x] Э5.8. Режим обратной связи вопроса в реестре называется как в карточке вопроса:
      «Общая» / «Условная» (`i18n.questions.feedbackMode*`), собственных синонимов не заводим.

## Э6. Шаблоны и контракт

- [x] Э6.1. Эталонный шаблон: строка разреза печатается без метки исхода и без классов
      `is-pass`/`is-fail` — правятся `layouts/results.html`, `layouts/results.adaptive.html`,
      `layouts/report/*`.
- [x] Э6.2. Шаблон «Сертификация»: те же правки в своих раскладках отчёта и итогов
      (`tb-report__breakdown-status` из `layouts/report/topics.html` уходит, CSS чистится).
- [x] Э6.3. «Сертификация»: проверить шапку карточки блока со счётчиком — новые тесты групп не
      заводят, шапка не должна оставлять пустую полосу; деградация без групп отыгрывается на
      растре.
- [x] Э6.4. Гард селекторов и паритета шаблонов обновляется под снятые узлы.
- [x] Э6.5. Контракт формата шаблонов бампится ВТРОЁМ: спецификация, руководство разработчика,
      PDF (`npm run docs:pdf`) — из контекста строки разреза исчезают `passed`, `passClass`,
      `statusLabel`.
- [x] Э6.6. Версия «Сертификации» поднимается, шаблон перезаливается в реестр
      (`PUT /api/admin/templates/certification/update`) — правка файлов без перезалива не видна.

## Э7. Документация и приёмка

- [x] Э7.1. Руководство автора: разделы про подытоги, пороги, блоки итогов и обратную связь
      переписываются под новую модель.
- [x] Э7.2. Руководство по импорту: лист «Настройки» (новые имена и алиасы), лист «Обратная
      связь» (замена вместо сложения, обратная связь подтемы).
- [x] Э7.3. ROADMAP: запись о треке, снятие закрытых пунктов техдолга (Ф-1, Ф-2, Ф-8, Ф-10,
      Ф-13, Ф-21).
- [x] Э7.4. Приёмка в браузере: все вкладки (их СЕМЬ, а не шесть — «Комментарии» добавлены
      решением владельца 2026-09-03), реестр вопросов, подытоги без вердикта на итогах и в
      отчёте, оба шаблона. Свободная навигация в приёмку не входила: Э4 не реализован.
      Отчёт — [editor-settings-restructure-acceptance.md](../reports/editor-settings-restructure-acceptance.md).
      Найден и исправлен один дефект: реестр «По вопросам» печатал «Без формулировки» у
      каждого вопроса (`12b46f93`).
- [x] Э7.5. Приёмка на бумаге: PDF-отчёт стандартного шаблона и «Сертификации», проверка, что
      строка разреза печатается без метки исхода.
- [ ] Э7.6. Полный прогон `npm test` и `npm run test:it`; `npm run test:cov` — по согласованию,
      в одиночку.

## Риски и грабли

- **Вердикты существующих тестов поедут.** Тесты с порогами ключей после перепубликации начнут
  считать тему пройденной там, где раньше её проваливала подтема. Решение владельца: это
  исправление, а не регресс. Сданные попытки не меняются — их результат заморожен снимком.
- **Правка шаблона не видна без перезалива.** Реестр читает копию в `uploads/templates/<id>`;
  файл репозитория приложению не виден.
- **Экспорт опубликованного теста берёт снимок.** После правок пакет остаётся прежним, пока не
  сделана перепубликация.
- **Мок хранилища.** Новый метод `IStorage`, который читает маршрут тестов или попыток, ломает
  разом восемь файлов с моками; точечные прогоны этого не показывают.
- **Книга Excel — контракт.** Переименование параметра без алиаса ломает уже выданные книги.
- **Полный прогон на загруженной машине** не стартует: помогает `TEST_MAX_WORKERS=2 npm test`.

## Порядок

Э0 → Э1 → Э2 → Э6.1-Э6.2 (шаблоны под новый контекст) → Э3 → Э4 → Э5 → Э6.3-Э6.6 → Э7.

Э1 и Э2 меняют данные и контекст рендера, поэтому идут раньше перестройки ящика: иначе новые
экраны придётся переделывать под изменившийся контракт.
