# Хвосты трека (PRD-57, этап Э10): план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** новые типы и новая разметка доезжают туда, где у остальных типов всё работает с
прошлых треков: периметр защиты, книга Excel, импорт выгрузки LMS, аналитика, замер времени.

**Architecture:** нового поведения для участника нет. Каждый хвост — ветка для трёх новых
типов в уже существующем механизме; ни один из них не заводит второй копии правил.

**Tech Stack:** TypeScript, Zod, React 19, Vitest, ExcelJS.

**Устройство согласовано:**
[docs/specs/2026-09-20-prd57-e10-track-tails-design.md](../specs/2026-09-20-prd57-e10-track-tails-design.md).
Требования: [PRD-57](../specs/prd-57/open-response-and-formatting.md) §7.

**Границы этапа.** Не делается: обработка текста моделями, интерфейс проверяющего (PRD-58),
замеры на стенде WebTutor (#51).

---

## Task 1: Периметр защиты пропускает поля ввода

- [ ] **Step 1:** падающие тесты — поле ввода внутри защищённого слота выделяемо, событие
      `copy` в нём не отменяется, текст задания по-прежнему защищён.
- [ ] **Step 2:** исключение в `shared/template/protection/apply.ts`: инлайн
      `user-select: text` полям ввода помеченного региона и пропуск их в `inPerimeter`.
- [ ] **Step 3:** `npm test -- tests/copy-protection.test.ts`

## Task 2: Книга Excel — грамматика правил

- [ ] **Step 1:** падающие тесты — разбор и печать текстового, регулярного и числового
      правила; правила пропусков с именами; ошибки на неизвестном операторе и на имени, которого
      нет в тексте задания.
- [ ] **Step 2:** `server/services/workbook/answer-rules-cell.ts` — разбор и печать ячейки
      правил в обе стороны, без зависимости от Excel.
- [ ] **Step 3:** `npm test -- tests/workbook-answer-rules-cell.test.ts`

## Task 3: Книга Excel — импорт и экспорт новых типов

- [ ] **Step 1:** падающие тесты — круг «экспорт-импорт» для `short`, `blanks`, `long`
      возвращает тот же `correct_json` и `data_json`; листинг и формула в тексте задания
      переживают круг; непустая ячейка правил у `long` — ошибка.
- [ ] **Step 2:** типы в обеих таблицах соответствия, новые колонки, ветки разбора и
      сериализации.
- [ ] **Step 3:** `npm test -- tests/questions-import.test.ts tests/questions-export.test.ts`

## Task 4: Импорт выгрузки отчёта LMS

- [ ] **Step 1:** падающие тесты на парность — текст короткого и развёрнутого ответа,
      значения пропусков по именам; ответ без эталона не роняет разбор.
- [ ] **Step 2:** ветки в `shared/lms-export/response-codec.ts` и порядок пропусков из
      эталона в вызове `lms-export-import.ts`.
- [ ] **Step 3:** `npm test -- shared/lms-export/__tests__ tests/lms-export-import.test.ts`

## Task 5: Аналитика — сводка свободного текста и правила словами

- [ ] **Step 1:** падающие тесты — сводка объёма и длины; тип, содержимое, эталон и ответ
      новых типов печатаются словами, а не JSON.
- [ ] **Step 2:** третий вид в `answer-spread.ts`, переезд `describe-rule.ts` в
      `shared/answer-check/describe.ts`, ветки в помощниках аналитики.
- [ ] **Step 3:** `npm test -- server/services/analytics/__tests__ tests/analytics-helpers.test.ts`

## Task 6: Аналитика — список ответов с выгрузкой

- [ ] **Step 1:** падающие тесты — маршрут отдаёт ответы обоих источников, закрыт областью
      видимости теста, выгрузка несёт те же строки.
- [ ] **Step 2:** маршрут, сервис выборки, колонка и ссылка в строке задания на вкладке
      «Вопросы»; разброс короткого ответа виден и в обычном тесте (долг Э3).
- [ ] **Step 3:** `npm test -- server/routes/analytics/__tests__ client/src/features/analytics`

## Task 7: Время на задании

- [ ] **Step 1:** падающие тесты — событие ответа новых типов несёт `latencyMs` на обоих
      хостах.
- [ ] **Step 2:** починка там, где не доезжает.
- [ ] **Step 3:** `npm test -- tests/question-time.test.ts`

## Task 8: Документация

- [ ] **Step 1:** спецификация формата книги — новые типы, грамматика ячейки правил,
      новые колонки, алиасы заголовков.
- [ ] **Step 2:** руководство по книге и руководство автора — то же человеческим языком.
- [ ] **Step 3:** `npm run check` и markdownlint по изменённым файлам.

## Task 9: Приёмка

- [ ] **Step 1:** затронутые наборы целиком.
- [ ] **Step 2:** живой прогон: защита включена — участник правит свой ответ, а текст
      задания не выделяется; книга с тремя новыми типами уезжает и приезжает; аналитика
      показывает сводку и список ответов.
