# PRD-56, этап Э6. Диаграммы, уборка и разворот среза по темам — план работ

> **Для исполнителя:** задачи выполняются по одной, сверху вниз, в режиме TDD: сначала падающий
> тест, потом минимальная реализация, потом прогон, потом коммит. Шаги отмечены чекбоксами
> (`- [ ]`).

**Цель:** закрыть PRD-56 — снять слой, переставший работать после Э4 и Э5, привести эскиз в
согласие с продуктом и доделать разворот строки среза по темам (FR-06e), незакрытый в Э3.

**Спецификация:** [FR-28, FR-06e](../specs/prd-56/analytics-rework.md).
**Предыдущие этапы:** [Э3 — срезы](2026-09-14-prd56-e3-slices.md),
[Э4 — аналитика теста](2026-09-14-prd56-e4-test-analytics.md),
[Э5 — выдача и шкалы](2026-09-14-prd56-e5-delivery-scales.md).

## Что нужно знать до начала

- **Vitest только через npm:** `npm test -- <путь>`; полный прогон — по разрешению владельца.
- **`npm run check` НЕ видит тесты:** `tsconfig.json` включает только `client/src`, `shared`,
  `server` и вдобавок исключает `**/*.test.ts`. Смена экспортируемой сигнатуры ломает чужие
  тесты молча — это стоило полутора десятков падений в Э5.
- **Дев-сервер не подхватывает серверные правки** — после них перезапуск руками.
- **Приёмка в браузере:** `node scripts/check/acceptance-shot.mjs` и `acceptance-click.mjs`
  (в Git Bash с `MSYS_NO_PATHCONV=1`). Учётка `acceptance@local.test`.

## Состояние на начало работ: аудит против FR-28

FR-28 требует диаграмму только там, где данные суть РАСПРЕДЕЛЕНИЕ или СВЯЗЬ. Проверено по блокам
обоих экранов; правило соблюдено везде, и это не совпадение — форму выбирали поблочно в Э4 и Э5.

| Блок | Что за данные | Чем показан | Вердикт |
| --- | --- | --- | --- |
| Плитки теста | четыре отдельных числа | числа | верно: одно число — это число |
| Распределение результатов | распределение по корзинам | столбцы | верно |
| Темы и подтемы | ряд величин по темам | таблица | верно: читается сравнением строк |
| Динамика сдаваемости | ряд во времени | линия | верно: связь |
| Вопросы теста | список с сортировкой | таблица | верно |
| Варианты, версии | ряды величин | таблицы | верно |
| Профиль экспозиции | распределение по банку | список с полосами | верно (см. задачу 2) |
| Профиль по шкалам | средние по шкалам | полосы | верно |
| Распределение уровней | распределение по полосам | накопительная полоса | верно |
| Реестр прохождений | список | таблица | верно |
| Срезы | ряд величин по категориям | таблица | верно; разворота по темам нет — FR-06e |
| Требует внимания | четыре счётчика и списки | числа и списки | верно |

Отсюда объём Э6 (решение владельца 2026-09-14): диаграмм не добавляем и не убираем — остаются
уборка и FR-06e.

**Мёртвый слой страницы теста.** После Э4 (снятие списка попыток, окна разбора и карточек
заданий) и Э5 в `client/src/pages/author/test-analytics.tsx` остались: импорт `recharts`, который
ничего не рисует (страница целиком на `Charts` дизайн-системы); импорт `QuestionMetrics`, который
не собирается; семнадцать мёртвых объявлений — помощники снятого списка и окна
(`formatDate`, `percentTone`, `percentProgressTone`, `chartTooltipStyle`, `formatIndicatorValue`,
`renderReferenceValue`) и их типы (`AttemptDetail`, `AttemptListItem`, `AnswerRow`). Шапка модуля
обещает то, чего на экране больше нет: список попыток, окно разбора и диаграммы на `recharts`.

**FR-06e.** Разворот строки среза показывает долю верных ЭТОГО среза по темам и объём выборки у
каждой темы; ни с чем не сравнивает — за сравнением ведёт отдельный режим. Расчёт по темам уже
есть (`summariseTopics`, Э4), но собран он внутри маршрута теста вместе с разрешением оценивания:
чтобы им воспользовался и срез, сбор ответов теста нужно вынести в общее место.

## Задачи

### Задача 1: снять мёртвый слой страницы теста

**Файлы:**

- Изменить: `client/src/pages/author/test-analytics.tsx`
- Удалить, если осиротел: `client/src/features/analytics/question-metrics.tsx` и его тест
- Тест: существующие наборы страницы

Уборка без смены поведения: снимается только то, что не собирается. Проверяется не глазами, а
компилятором — `npx tsc --noEmit --noUnusedLocals` по этому файлу обязан замолчать.

- [x] **Шаг 1: снять импорты и мёртвые объявления** — файл похудел с 812 строк до 557.
- [x] **Шаг 2: актуализировать шапку модуля** — она описывает экран, которого нет.
- [x] **Шаг 3: решить судьбу `question-metrics`** — компонент звал только собственный тест;
      удалён вместе с ним, правила показа PRD-55 живут в таблице заданий.
- [x] **Шаг 4: прогон тестов страницы и аналитики** — 109 тестов.
- [x] **Шаг 5: коммит** — `62f6ec5e`

### Задача 2: эскиз против продукта — профиль экспозиции

**Файлы:**

- Изменить: `docs/wireframes/prd56-test-analytics.html` (строка DS-mapping)

Эскиз противоречит сам себе: пояснения требуют СПИСОК («у столбца подпись помещается только
номером, и „1, 2, 3…" не говорит ничего»), а DS-mapping называет профиль экспозиции столбчатой
диаграммой наравне с распределением результатов. Реализован список — по пояснениям. Строка
DS-mapping приводится к факту, иначе следующий читатель эскиза увидит требование, которого никто
не предъявлял.

- [x] **Шаг 1: правка строки DS-mapping**
- [x] **Шаг 2: коммит** — `8ef157ad`

### Задача 3: общий сбор ответов теста

**Файлы:**

- Добавить: `server/services/analytics/test-answer-facts.ts`
- Изменить: `server/routes/analytics/test-details.ts`
- Тест: `server/services/analytics/__tests__/test-answer-facts.test.ts`

Рефактор без смены поведения. Сегодня сбор ответов теста живёт внутри маршрута страницы: там
разрешается эффективное оценивание (`loadTestScoringContext`), там же ответ веб-попытки
превращается в факт, а измерительный вопрос — в `neutral`. Срезу нужно ровно то же самое, и
второй экземпляр этой логики разошёлся бы с первым на первой же правке правил оценивания.

Выносится ОДНА функция, отдающая факты ответов, карту вопросов и правила порогов тем. Маршрут
теста переходит на неё; его тесты — гарантия, что числа не поехали.

- [x] **Шаг 1: тест на вынесенный сбор** — измерительный ответ приходит `neutral`; вопрос не из
      теста не роняет сбор; пороги тем приезжают вместе с фактами.
- [x] **Шаг 2: вынести функцию и перевести маршрут теста**
- [x] **Шаг 3: прогон наборов страницы теста** — 371 тест аналитики, числа прежние.
- [x] **Шаг 4: коммит** — `f777b0aa`

### Задача 4: разворот строки среза по темам (FR-06e)

**Файлы:**

- Изменить: `server/routes/analytics/slices.ts`,
  `client/src/features/analytics/slices/slice-list.tsx`
- Тест: `tests/routes.analytics-slices.test.ts`, тест компонента рядом

Ручка отдаёт темы ОДНОГО среза: тест и период берутся из рамки расчёта, сам срез — осью и ключом
(`axis` + `key`) либо идентификатором сохранённого среза. Ось и ключ, а не условия реестра:
условия покрывают не всякую ось (номер попытки, внешний участник), и срез по ним не восстановить.

Строка показывает долю верных по теме и объём выборки — и ничего больше: сравнение живёт в своём
режиме (FR-06e). Данные грузятся по требованию, при развороте: платить за темы всех срезов при
каждом показе списка незачем.

- [x] **Шаг 1: тесты ручки** — темы считаются по прохождениям ЭТОГО среза, а не всего теста;
      неизвестная ось отвергается; срез без прохождений даёт пустой список; сохранённый срез
      разворачивается своими условиями.
- [x] **Шаг 2: ручка** — `GET /api/analytics/slices/topics`.
- [x] **Шаг 3: разворот строки в `DataGrid`** — сам разворот в ДС был, не было СОБЫТИЯ открытия:
      добавлен необязательный `onRowExpand`, иначе ленивую загрузку вешать некуда.
- [x] **Шаг 4: прогон тестов** — 128 тестов аналитики и ДС, 95 тестов прочих таблиц.
- [x] **Шаг 5: коммит** — `72df0a94`

### Задача 5: приёмка Э6

- [x] **Шаг 1:** развернуть строку среза в браузере, в светлой и тёмной теме — сделано на
      демо-тесте подытогов: строка «Без группы» раскрывается двумя темами (74 % и 98 % верных,
      по 10 прохождений в выборке).
- [~] **Шаг 2 — в браузере НЕ доказан:** на дев-базе у этого теста все прохождения без группы,
      то есть срез совпадает с тестом целиком, и отличить одно от другого снимком нельзя.
      Гарантия — маршрутный тест: две группы по участнику, у одного ответ верный, у другого
      нет; разворот первой группы обязан дать 100 %, а не 50 % теста целиком.
- [x] **Шаг 3:** прибрать за собой на дев-базе — приёмка только читала.
- [ ] **Шаг 4:** полный прогон `npm test` — по разрешению владельца.

## Риски

- **Рефактор сбора ответов трогает числа действующего экрана.** Задача 3 идёт отдельным
  коммитом и проверяется наборами страницы теста ДО того, как на неё обопрётся срез.
- **Разворот строки — новая механика `DataGrid`.** Если компонент её не умеет, правка уезжает в
  дизайн-систему и касается каждой таблицы продукта: держать её узкой, тесты ДС прогонять
  целиком.
- **Уборка выглядит безобидной и потому опаснее прочего.** Снимается только то, что не
  собирается; критерий — молчание компилятора с `--noUnusedLocals`, а не впечатление.
