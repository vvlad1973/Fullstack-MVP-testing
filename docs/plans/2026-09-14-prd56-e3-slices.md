# PRD-56, этап Э3. Срезы и очередь «требует внимания» — план работ

> **Для исполнителя:** задачи выполняются по одной, сверху вниз, в режиме TDD: сначала падающий
> тест, потом минимальная реализация, потом прогон, потом коммит. Шаги отмечены чекбоксами
> (`- [ ]`).

**Цель:** дать оценщику два инструмента поверх реестра — срезы (кого учили и с каким
результатом) и очередь дел, которая говорит «сделай», а не «посмотри». Заодно снять с экрана
величины, которые нельзя интерпретировать (FR-12).

**Спецификация:** [FR-06 - FR-12](../specs/prd-56/analytics-rework.md).
**Эскиз (согласован 2026-09-13):** [раздел «Аналитика»](../wireframes/prd56-analytics-section.html),
состояния `slices`, `slice-gap`, `compare`, `compare-many`, `save-slice`, `attention`.
**Предыдущие этапы:** [Э1 — слой наблюдений](2026-09-13-prd56-analytics-rework.md),
[Э2 — реестр](2026-09-14-prd56-e2-registry.md).

## Что нужно знать до начала

- **Vitest только через npm:** `npm test -- <путь>`; полный прогон — по разрешению владельца.
- **Дев-сервер не подхватывает серверные правки** — после них перезапуск руками.
- **Приёмка в браузере:** `node scripts/check/acceptance-shot.mjs` и `acceptance-click.mjs`
  (в Git Bash с `MSYS_NO_PATHCONV=1`). Учётка `acceptance@local.test`.
- **UI только из ui-kit**; не хватает пропа — правится `vendor/ui-kit`.

## Состояние на начало работ

- Слой наблюдений умеет всё, на чём стоят срезы: фильтры по тесту, группе, источнику, исходу и
  периоду, порции и общее число. Группа уже значит членство человека (`user_groups`) плюс метку
  импорта — FR-09 выполнен на уровне выборки.
- Реестр и его окно условий работают; язык условий готов к переиспользованию (FR-07b требует
  ровно того же набора).
- Сохранённых наборов условий нет: `FilterBar` умеет их показывать (`savedSets`), но в продукте
  ничего не хранит. Срез = сохранённый набор условий (FR-07b), значит нужна таблица.
- Назначения лежат в `test_assignments`, срок — `due_date` (nullable). `storage.getAllAssignments`
  есть. Попытки и их состояния — в общем слое наблюдений.
- Порога наблюдений (FR-06d) в конфигурации НЕТ: раздел `analytics` содержит только
  `lmsImport`. Порог заводится этим этапом.
- «Обзор» по-прежнему кормится `combined-full` и показывает средние по всем тестам — то, что
  FR-12 велит убрать.

## Задачи

### Задача 1: хранилище срезов

**Файлы:**

- Изменить: `shared/schema.ts` (таблица `analytics_slices`), новая миграция drizzle
- Создать: `server/storage/slices-repository.ts`, метод в `IStorage`
- Тест: `tests/it/analytics-slices.it.test.ts` (pglite)

Срез хранит УСЛОВИЯ и имя, а не список прохождений (FR-07d): тест, группа, источник, исход,
период — тем же языком, что фильтр реестра.

Видимость по итогу сделана простой: срез принадлежит тому, кто его завёл, и виден только ему.
Общие срезы не заводились сознательно — «видно всем» в аналитике означает «видно чужие выборки
с чужими людьми», и такое решение принимает владелец продукта, а не автор кода. Понадобится —
добавится отдельным полем.

- [x] **Шаг 1: падающие тесты** — создание, чтение по владельцу, удаление; условия
  возвращаются без потерь; срез без теста допустим (общий по реестру).
- [x] **Шаг 2: схема, миграция, репозиторий**
- [x] **Шаг 3: `npm run test:it -- tests/it/analytics-slices.it.test.ts`**
- [x] **Шаг 4: коммит**

### Задача 2: ручка срезов и их расчёт

**Файлы:**

- Создать: `server/routes/analytics/slices.ts`, `server/services/analytics/slice-stats.ts`
- Тест: `tests/routes.analytics-slices.test.ts`

Разрез «срез × тест» (FR-06): назначено, начато, завершено, сдали, средний результат,
слабейшая тема. Ниже порога наблюдений процент не печатается — вместо него «мало данных» и само
число прохождений (FR-06d); порог берётся из конфигурации инстанса и заводится здесь.

- [x] **Шаг 1: падающие тесты** — величины считаются внутри одного теста; «мало данных» ниже
  порога.
- [x] **Шаг 2: реализация поверх наблюдений** — `slice-stats` и `GET /api/analytics/slices`.
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**
- [ ] **Шаг 5: разворот строки — доли верных по темам этого среза (FR-06e).**

Разворот вынесен в отдельный шаг сознательно: доли по темам у веб-попытки лежат в
`resultJson.topicResults`, а у прохождения из LMS — в `scorm_answers` другой формой. Сведение
этих двух источников по темам — та же работа, что ждёт вкладку «Вопросы» в Э4, и делать её
дважды незачем. Пока разворот не сделан, список срезов остаётся полезен: он отвечает на «кого
учили и с каким результатом», а не на «в какой теме провал».

### Задача 3: ось разбиения

**Файлы:**

- Изменить: `server/services/analytics/slice-stats.ts`, ручка срезов
- Тест: там же

Ось — способ получить сразу все срезы по одному признаку (FR-06a). В объём входят оси, для
которых данные уже есть: группа, период, номер попытки, версия теста, вариант выдачи, источник,
внутренние/внешние. Оргструктуры в продукте нет и в этой работе не появляется (FR-06b).

- [x] **Шаг 1: падающие тесты** — по одной на ось: разбиение даёт ожидаемые срезы и объёмы.
- [x] **Шаг 2: реализация**
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**

### Задача 4: экран срезов и режим сравнения

**Файлы:**

- Создать: `client/src/features/analytics/slices/`
- Изменить: `client/src/pages/author/analytics.tsx`
- Тест: `client/src/features/analytics/slices/__tests__/`

Список срезов показывает только факты — отклонений от невидимой величины в нём нет (FR-06c).
Сравнение — отдельный режим с явно названными срезами в заголовках столбцов (FR-07), «Разница»
появляется только при двух срезах и только у долей. Над срезами — тест и период как рамка
расчёта, без панели фильтра (FR-07i, FR-07j). Предел — четыре среза (FR-07g).

- [ ] **Шаг 1: падающие тесты** — режимы списка и сравнения; «Разница» при двух срезах и её
  отсутствие при трёх; выключение «Добавить срез» на четвёртом; переход в реестр с
  предзаполненным фильтром (FR-08).
- [ ] **Шаг 2: реализация**
- [ ] **Шаг 3: прогон тестов**
- [ ] **Шаг 4: коммит**

### Задача 5: сохранение среза из реестра

**Файлы:**

- Изменить: `client/src/features/analytics/registry/passage-registry.tsx`, `filter-dialog.tsx`
- Тест: `client/src/features/analytics/registry/__tests__/`

«Сохранить как срез» в панели фильтра, при пустом фильтре выключено (FR-07c). В окне —
имя, условия и объём выборки, и прямо сказано, что срез хранит условия, а не состав (FR-07d).
Сохранённые наборы `FilterBar` и срезы — одна сущность, второго языка отбора не заводится.

- [ ] **Шаг 1: падающие тесты** — действие выключено без условий; окно показывает условия и
  объём; сохранённый срез появляется в списке наборов.
- [ ] **Шаг 2: реализация**
- [ ] **Шаг 3: прогон тестов**
- [ ] **Шаг 4: коммит**

### Задача 6: очередь «требует внимания»

**Файлы:**

- Создать: `server/services/analytics/attention.ts`, `server/routes/analytics/attention.ts`
- Тест: `server/services/analytics/__tests__/attention.test.ts`,
  `tests/routes.analytics-attention.test.ts`

Серверная часть сделана; экран очереди собирается вместе с экраном срезов (задача 4), чтобы
вкладка «Требует внимания» появилась разом со своими данными, а не пустой карточкой.

Четыре корзины (FR-10): назначено и не начато к сроку; провалившие; брошенные попытки;
исчерпавшие лимит. Назначения без срока в очередь не попадают (FR-10a) — без даты «просрочено»
определить не из чего. Каждая позиция ведёт к участнику и его прохождению (FR-11).

- [x] **Шаг 1: падающие тесты** — состав каждой корзины; назначение без срока не попадает;
  счётчики совпадают с числом позиций.
- [x] **Шаг 2: реализация**
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**

### Задача 7: снятие неинтерпретируемых величин

**Файлы:**

- Изменить: `client/src/pages/author/analytics.tsx`, `server/routes/analytics/combined.ts`,
  `general.ts`
- Тест: существующие тесты страницы

Уходят плитки «Средний балл» и «Pass Rate» по всем тестам, блок «Проблемные темы» вне контекста
теста и тренды по всем тестам сразу (FR-12). Их место занимают срезы и очередь. Вместе с ними
снимается `combined-full` и мёртвая ручка `GET /api/analytics` (её не зовёт никто).

- [ ] **Шаг 1: тесты на новый состав экрана**
- [ ] **Шаг 2: снятие блоков и ручек**
- [ ] **Шаг 3: прогон тестов**
- [ ] **Шаг 4: коммит**

### Задача 8: приёмка Э3

- [ ] **Шаг 1:** сверить экраны с эскизом в браузере, в светлой и тёмной теме.
- [ ] **Шаг 2:** проверить «мало данных» на группе меньше порога.
- [ ] **Шаг 3:** сравнить два среза и убедиться, что «Разница» есть только у долей.
- [ ] **Шаг 4:** пройти из строки среза в реестр и убедиться, что фильтр предзаполнен.
- [ ] **Шаг 5:** запросить разрешение на полный прогон `npm test`.

## Риски

- **Порог наблюдений — новая настройка.** Он решает, где показывать процент, а где «мало
  данных», то есть влияет на каждое число экрана. Значение по умолчанию нужно согласовать с
  владельцем до реализации задачи 2, а не выбирать в коде.
- **Срез и сохранённый набор условий — одна сущность.** Если развести их «пока что», в продукте
  появится второй язык отбора, и дальше их уже не сведут. Задачи 1 и 5 делаются с этим
  допущением с самого начала.
- **Снятие `combined-full` (задача 7) трогает весь экран.** Делать его последним, когда срезы и
  очередь уже стоят на своих местах: иначе «Обзор» останется пустым.
- **Оси разбиения легко расползаются.** В объём входят только те, для которых данные уже есть
  (FR-06a); оргструктура не появляется ни под каким видом (FR-06b).
