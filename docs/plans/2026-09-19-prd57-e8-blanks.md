# Заполнение пропусков (PRD-57, этап Э8): план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** тип задания «Пропуски»: поля ввода внутри текста, правила на каждый пропуск,
оценка счётчиками и одно взаимодействие в LMS.

**Architecture:** пропуск — это `AnswerRuleSet` с именем, поэтому весь аппарат Э4 — Э7
работает без правок. Новое: разбор разметки, подстановка маркера (одна на все места без
полей) и рендер полей в слот текста задания.

**Tech Stack:** TypeScript, Zod, React 19, `@universityrt/ui-kit`, Vitest.

**Устройство согласовано:**
[docs/specs/2026-09-19-prd57-e8-blanks-design.md](../specs/2026-09-19-prd57-e8-blanks-design.md).
Требования: [PRD-57](../specs/prd-57/open-response-and-formatting.md) §6, FR-24 — FR-26.
Эскиз: `docs/wireframes/approved/prd57-blanks-editor.html`.

**Границы этапа.** Не делается: вес пропуска (его нет по решению владельца), развёрнутый
ответ (Э9), книга Excel (Э10).

---

## Карта файлов

| Файл | Ответственность |
| --- | --- |
| `shared/questions/blanks.ts` | разбор `{{id}}`: имена, положения, экранирование, префиксы |
| `shared/questions/blanks-render.ts` | подстановка маркера: прочерк, ответ, эталон |
| `shared/questions/question-type.ts` | тип `blanks`, признак `hasBlanks` |
| `shared/schema.ts` | `blanksCorrectSchema` — наборы правил по пропускам |
| `shared/scoring/engine.ts` | `exactCorrect` и `countTallies` для пропусков |
| `shared/template/question-interaction.ts` | поля внутри текста задания |
| `shared/template/short-answer-dom.ts` | ввод пропуска отдаёт пару «имя — значение» |
| `server/scorm/template/app/**` | двойник счётчиков, рендер, выдача одним `fill-in` |
| `client/src/features/questions/answer-rules/blanks-block.tsx` | список пропусков в ящике |
| `client/src/features/questions/question-editor-drawer.tsx` | кнопка вставки, предпросмотр |

---

## Task 1: Разбор разметки

- Create: `shared/questions/blanks.ts`; Test: `tests/blanks-parse.test.ts`
- [ ] **Step 1:** падающие тесты — имена по порядку вхождения; повтор виден отдельно;
      `\{{` не пропуск; `{{$x}}` и родня не пропуски и помечены как зарезервированные;
      `{{}}` не пропуск и не ошибка; допустимые знаки имени.
- [ ] **Step 2:** реализация: один проход, возвращает вхождения с положениями.
- [ ] **Step 3:** `npm test -- tests/blanks-parse.test.ts`

## Task 2: Подстановка маркера

- Create: `shared/questions/blanks-render.ts`; Test: `tests/blanks-render.test.ts`
- [ ] **Step 1:** падающие тесты — три режима; эталона нет (выражение, допуск) — прочерк;
      экранированные скобки печатаются без слеша; текст без пропусков не меняется.
- [ ] **Step 2:** реализация.
- [ ] **Step 3:** `npm test -- tests/blanks-render.test.ts`

## Task 3: Тип задания и схема

- Modify: `shared/questions/question-type.ts`, `shared/schema.ts`;
  Test: `tests/blanks-type.test.ts`
- [ ] **Step 1:** падающие тесты — `blanks` в перечне; `hasBlanks` отличает его от
      короткого ответа; схема принимает наборы по пропускам и отвергает повтор имени;
      задание без правил ни у одного пропуска неоцениваемо.
- [ ] **Step 2:** реализация.
- [ ] **Step 3:** `npm test -- tests/blanks-type.test.ts`

## Task 4: Оценка

- Modify: `shared/scoring/engine.ts`, `server/scorm/template/app/scoring/engine.js`;
  Test: `tests/blanks-scoring.test.ts`, `tests/scoring-engine-port.test.ts`
- [ ] **Step 1:** падающие тесты — верно, когда верны все; счётчики `c`/`x`/`total` по
      пропускам; ступень платит за три из четырёх; пропуск без правил вне знаменателя.
- [ ] **Step 2:** реализация и её двойник.
- [ ] **Step 3:** оба набора.

## Task 5: Поля внутри текста

- Modify: `shared/template/question-interaction.ts`, `shared/template/short-answer-dom.ts`,
  `client/src/pages/learner/template-question-screen.tsx`,
  `server/scorm/template/app/render/**`; Test: `tests/blanks-render-host.test.ts`
- [ ] **Step 1:** падающие тесты — поля встают на места маркеров; разметка обрабатывается
      РАНЬШЕ подстановки; ввод отдаёт пару «имя — значение»; блок ответа пуст.
- [ ] **Step 2:** реализация на обоих хостах.
- [ ] **Step 3:** набор плюс `tests/answer-check-render.test.ts`.

## Task 6: Список пропусков в ящике

- Create: `client/src/features/questions/answer-rules/blanks-block.tsx`;
  Modify: `question-editor-drawer.tsx`; Test: `client/src/features/questions/__tests__/`
- [ ] **Step 1:** падающие тесты — список строится по тексту; переименование в тексте
      переносит правила; удаление пропуска с правилами спрашивает подтверждение, отказ
      возвращает пропуск в текст; строка без правил помечена; кнопка вставки ставит `{{}}`
      и оставляет курсор внутри.
- [ ] **Step 2:** реализация.
- [ ] **Step 3:** наборы ящика.

## Task 7: Выдача в LMS и разбор попытки

- Modify: `server/scorm/template/app/render/resultsPage.js`, аналитика и разбор;
  Test: `tests/scorm-interactions.test.ts`, `tests/blanks-review.test.ts`
- [ ] **Step 1:** падающие тесты — одно взаимодействие `fill-in`, ответы через `[,]`,
      эталоны в том же порядке; разбор показывает ответ участника в тексте.
- [ ] **Step 2:** реализация.
- [ ] **Step 3:** оба набора.

## Task 8: Приёмка

- [ ] **Step 1:** `npx tsc --noEmit --incremental false` и затронутые наборы.
- [ ] **Step 2:** живой прогон: задание с двумя пропусками, вставка, переименование,
      удаление с отказом, прохождение, разбор, предпросмотр.
- [ ] **Step 3:** пакет: отладочный плеер и `npm run scorm:template`.
