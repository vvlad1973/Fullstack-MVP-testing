# PRD-50 Э1-доделка: адаптивный режим, полный контракт строки, сохранение записей

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Требования:** [спека PRD-50](../specs/prd-50/result-breakdowns.md), этап «Э1-доделка» —
FR-17, FR-38, FR-39, FR-41 и полный контракт строки разреза (§8.1). Плюс один пункт долга,
найденного по ходу Э1 (пересчёт показателей в аналитике SCORM).

**Ветка:** `prd-50-result-breakdowns` (та же, где лежит Э1; на момент написания HEAD `7e1cf2f8`).

**Goal:** закрыть четыре места, где Э1 обещал разрезы и не дал их: адаптивный режим не считает
записи вовсе, строка контекста уже спеки, записи области теста нигде не хранятся, а сохранённая
попытка внутри пакета SCORM теряет полосы, которые веб на той же попытке печатает.

**Architecture:** ничего нового не заводится. `adaptiveResultAsStandard` получает вторым
аргументом список выданных элементов и прогоняет через ТОТ ЖЕ `computeBreakdowns`, что и
стандартный агрегат; оба хоста собирают элементы из своих данных (веб — из `variant.topics` и
ответов, пакет — из `state.adaptiveState` и `TEST_DATA.adaptiveTopics`). Строка контекста
расширяется в `topicView`, то есть один раз на оба хоста и на отчёт. Записи области теста
кладутся в `attempts.result_json` рядом с `topicResults`; записи области раздела в пакете
доезжают до `suspend_data` тем же путём, каким туда доезжают `topicResults`, — они на них и
лежат.

**Tech Stack:** TypeScript, Vitest (`npm test -- <путь>`), Zod-схемы `shared/schema.ts`,
плоский ES5-рантайм пакета SCORM (`server/scorm/template/app/**`), PostgreSQL (только чтение).

---

## Что проверено чтением кода перед составлением плана

Каждый пункт ниже — факт, снятый с кода на HEAD `7e1cf2f8`, а не предположение.

- **FR-38 уже выполнен конструкцией, кода не требует.** `buildSnapshotContent`
  (`server/services/test-snapshot.ts:104`) кладёт в снимок `test` целиком — результат
  `storage.getTest(testId)`, а тот делает `db.select().from(tests)`
  (`server/storage/tests-repository.ts:165`), то есть ВСЕ колонки. Так же морозятся
  `sections` (`getTestSections` — тоже `db.select()`). Значит `tests.breakdown_display_json`,
  `tests.design_settings_json` (словарь надписей) и будущие `tests.section_groups_json` /
  `test_sections.breakdown_rules_json` едут в снимок без единой правки. Выдача читает их
  оттуда же: `resultsMaterialForAttempt` берёт `deliveredTest = await src.getTest(...)`
  (`server/routes/attempts.ts:230`) и подаёт `breakdownDisplayJson: deliveredTest?.…` (строка 287).
  → В план входит ТЕСТ-СТРАЖ, а не код (задача 6).
- **FR-41 уже выполнен конструкцией, кода не требует.** `Telemetry.finish`
  (`server/scorm/template/app/telemetry/telemetry.js:442`) отправляет закрытый список из восьми
  полей; вызывающий `finishAndClose` (`render/resultsPage.js:248`) тоже собирает объект-белый
  список; серверный маршрут `/api/scorm-telemetry/finish`
  (`server/routes/scorm-telemetry.ts:237`) пишет фиксированный набор колонок, а в
  `scorm_answers` (`shared/schema.ts:1717`) нет колонки под теги. → В план входит ТЕСТ-СТРАЖ
  на оба белых списка (задача 7).
- **FR-39 не выполнен.** Записи области теста собираются в `server/routes/attempts.ts:1533`
  только ради контекста формул и в `result` не попадают; `attemptResultSchema`
  (`shared/schema.ts:1315`) поля под них не имеет. Записи области РАЗДЕЛА сохраняются
  (`topicResults[].breakdown`, строка 1508 + схема 1296).
- **FR-17 не выполнен, но данные под него уже есть.** `adaptiveResultAsStandard`
  (`shared/scoring/aggregate.ts:461`) разрезов не считает; адаптивная ветка веб-хоста
  (`server/routes/attempts.ts:2093`) зовёт `computeAttemptResult` без `breakdowns`;
  `getAdaptiveResultForScorm` (`server/scorm/template/app/adaptive/adaptive.js:563`) — тоже.
  При этом теги адаптивных вопросов уже выпекаются в каждый пакет
  (`server/scorm/builders/test-json.ts:492-494`) и никем не читаются.
- **Пакет НАМЕРЕННО выбрасывает записи раздела перед сохранением.** `calculateResults`
  (`render/resultsPage.js:602-611`) сплющивает обе области в один массив `breakdowns` и не
  кладёт `breakdown` на темы — с комментарием «`saveAttemptResult` persists those verbatim into
  suspend_data, which is a scarce 64K budget». Поэтому `saveAttemptResult`
  (`utils/scorm/suspendAttempts.js:213`, `topicResults: resultData.topicResults`) сохраняет
  темы без разрезов, а `vrTopicBreakdown` (`render/viewResults.js:113`) на сохранённой попытке
  возвращает пустой список — и экран «Мой результат», и скачанный оттуда PDF
  (`utils/pdfExport.js:151`) полос не печатают.
- **Шаблон «Сертификация» полосы УЖЕ поддерживает** (`templates/certification/layouts/results.html:183`
  и `report.html:88`) — пункт 3 из «Решений владельца» плана Э1 закрыт, в этот план не входит.
- **Аналитика SCORM пересчитывает показатели без разрезов.** `server/routes/analytics/scorm.ts:171-184`
  строит `gradedBase` без поля `breakdowns`, поэтому формула с `tag()` даёт там ноль.
  В телеметрии тегов нет и не будет (FR-41), но у каждой строки ответа есть `questionId`
  и `topicId`, а значит записи восстанавливаются из живого банка вопросов.

---

## Файловая карта

| Файл | Ответственность |
| --- | --- |
| `shared/template/context.ts` (правка) | полный состав `CtxBreakdownRow` по §8.1 |
| `shared/template/result-context.ts` (правка) | заполнение новых полей строки в `topicView` |
| `shared/schema.ts` (правка) | `breakdownEntrySchema`, `breakdowns` в результатах обоих режимов |
| `server/routes/attempts.ts` (правка) | сохранение записей области теста; элементы адаптивного разреза |
| `shared/scoring/aggregate.ts` (правка) | `adaptiveResultAsStandard` принимает элементы и считает разрезы |
| `server/scorm/template/app/adaptive/adaptive.js` (правка) | сборка элементов из состояния пакета |
| `server/scorm/template/app/render/resultsPage.js` (правка) | записи раздела остаются на темах |
| `server/scorm/template/app/render/viewResults.js` (правка) | чтение записей с сохранённой попытки |
| `server/scorm/template/app/utils/pdfExport.js` (правка) | тот же читатель для отчёта |
| `server/routes/analytics/scorm.ts` (правка) | разрезы в пересчёте показателей телеметрийной попытки |
| `shared/template/__tests__/result-context-breakdown.test.ts` (правка) | пин полного состава строки |
| `tests/breakdown-adaptive.test.ts` (создать) | FR-17 на обоих хостах |
| `tests/breakdown-persistence.test.ts` (создать) | FR-39 и сохранение в `suspend_data` |
| `tests/breakdown-snapshot-freeze.test.ts` (создать) | FR-38, страж |
| `tests/breakdown-telemetry-silence.test.ts` (создать) | FR-41, страж |
| `tests/breakdown-analytics-scorm.test.ts` (создать) | долг Э1: `tag()` в аналитике |

---

## Task 1: полный контракт строки разреза (§8.1)

Спека §8.1 требует от строки одиннадцати полей; сегодня их четыре. Все недостающие уже
посчитаны и лежат в `BreakdownEntry` — их надо просто отдать шаблону. Поля вердикта
(`passed`, `passClass`, `statusLabel`) в этот этап НЕ входят: наполнить их нечем, пока нет
порога из Э2.

**Files:**

- Modify: `shared/template/context.ts` (`CtxBreakdownRow`, строки 90-99)
- Modify: `shared/template/result-context.ts` (`topicView`, строки 680-694)
- Test: `shared/template/__tests__/result-context-breakdown.test.ts`

- [ ] **Шаг 1. Написать падающий тест**

Дописать в конец `shared/template/__tests__/result-context-breakdown.test.ts`:

```ts
describe("полный состав строки (§8.1)", () => {
  it("строка несёт счётчики, баллы и обе базы, а не только полосу", () => {
    const ctx = buildResultContext(result as never, "Тест", {
      breakdownDisplay: { visibility: "bar_and_value", basis: "units" },
    } as never);
    const rows = (ctx.result.topicResults![0] as never as { breakdown: Array<Record<string, unknown>> }).breakdown;
    // toEqual, а не toMatchObject: контракт строки — это её ПОЛНЫЙ состав. Лишнее поле
    // здесь так же значимо, как недостающее: сторонний шаблон реестра PRD-3 привязывается
    // к именам, и молча появившееся имя потом придётся возить по обоим хостам.
    expect(rows[0]).toEqual({
      key: "ПДн",
      items: 2,
      answered: 2,
      earned: 1,
      possible: 2,
      percent: 40,
      percentUnits: 40,
      percentPoints: 50,
      barPercent: 40,
      showValue: true,
      valueLabel: "40 %",
    });
  });

  it("percent следует ВЫБРАННОЙ базе, а percentPoints остаётся валютой вердикта", () => {
    const ctx = buildResultContext(result as never, "Тест", {
      breakdownDisplay: { visibility: "bar", basis: "points" },
    } as never);
    const rows = (ctx.result.topicResults![0] as never as { breakdown: Array<Record<string, unknown>> }).breakdown;
    expect(rows[0].percent).toBe(50);
    expect(rows[0].percentUnits).toBe(40);
    expect(rows[0].percentPoints).toBe(50);
  });

  it("дробные значения приходят с одним знаком, а не сырым отношением", () => {
    const messy = {
      ...topic,
      breakdown: [
        { scope: "section:law", axis: "tag", key: "ПДн", items: 3, answered: 3,
          earned: 5 / 3, possible: 3, unitEarned: 5 / 9, unitPossible: 3,
          percentPoints: (5 / 9) * 100, percentUnits: (5 / 27) * 100 },
      ],
    };
    const ctx = buildResultContext({ ...result, topicResults: [messy] } as never, "Тест", {
      breakdownDisplay: { visibility: "bar_and_value", basis: "points" },
    } as never);
    const rows = (ctx.result.topicResults![0] as never as { breakdown: Array<Record<string, unknown>> }).breakdown;
    expect(rows[0].earned).toBe(1.7);
    expect(rows[0].percentPoints).toBe(55.6);
    expect(rows[0].barPercent).toBe(56);
  });
});
```

Команда: `npm test -- shared/template/__tests__/result-context-breakdown.test.ts`
Ожидание: FAIL — первый тест на `toEqual` не сходится, полей `items`/`earned`/`percent` нет.

- [ ] **Шаг 2. Расширить контракт**

В `shared/template/context.ts` заменить объявление `CtxBreakdownRow` целиком:

```ts
/**
 * One breakdown row, prepared by the core: the layout only prints it (PRD-50 §8.1).
 *
 * The counts and the points are here from the FIRST release, not from the release that
 * first prints them. A third-party template из реестра PRD-3 cannot show «верно 4 из 7»
 * out of a bar width, and adding a field later is a contract change that has to be
 * carried across both hosts and every shipped package.
 *
 * Three verdict fields the spec also names — `passed`, `passClass`, `statusLabel` —
 * are deliberately ABSENT until Э2: they are filled from the key's threshold, which
 * does not exist yet, and a field that can only ever be `null` teaches a template the
 * wrong shape.
 */
export interface CtxBreakdownRow {
  key: string;
  /** Delivered questions carrying this key (FR-01). */
  items: number;
  /** How many of those the learner answered. */
  answered: number;
  /** Points earned on them, one decimal (as `pointsLabel` rounds). */
  earned: number;
  /** Points they could bring, one decimal. */
  possible: number;
  /**
   * The value of the basis the AUTHOR chose (`units` / `points`), one decimal —
   * the unrounded twin of {@link barPercent}. A layout that wants a number prints
   * this one and does not have to know which basis is in force.
   */
  percent: number;
  /** Normalized share — every question weighs 1 (решение 4), one decimal. */
  percentUnits: number;
  /** Points share, one decimal. The verdict currency (FR-21), whatever is displayed. */
  percentPoints: number;
  /** Bar width in percent, rounded. */
  barPercent: number;
  /** Whether to print the number alongside the bar. */
  showValue: boolean;
  /** Ready-made value label, e.g. "50 %". Empty when showValue is false. */
  valueLabel: string;
}
```

- [ ] **Шаг 3. Заполнить строку**

В `shared/template/result-context.ts`, в `topicView`, заменить тело `map` (строки 686-694):

```ts
    view.breakdown = t.breakdown.map((e): CtxBreakdownRow => {
      const value = display.basis === "points" ? e.percentPoints : e.percentUnits;
      return {
        key: e.key,
        items: e.items,
        answered: e.answered,
        // One decimal everywhere a real number reaches the layout, exactly like
        // `pointsLabel` above: these fields are bound DIRECTLY by templates, and a raw
        // ratio prints as «73.33333333333333». The bar keeps its own integer.
        earned: round1(e.earned),
        possible: round1(e.possible),
        percent: round1(value),
        percentUnits: round1(e.percentUnits),
        percentPoints: round1(e.percentPoints),
        barPercent: Math.round(value),
        showValue,
        valueLabel: showValue ? Math.round(value) + " %" : "",
      };
    });
```

- [ ] **Шаг 4. Прогнать тесты и типы**

```bash
npm test -- shared/template/__tests__/result-context-breakdown.test.ts shared/template/__tests__
npm test -- tests/report-context.test.ts
npm run check
```

Ожидание: PASS. Отчёт строится тем же `buildResultContext`
(`shared/report/report-context.ts:260`), поэтому новые поля доезжают до него без правок —
`tests/report-context.test.ts` не должен сдвинуться.

- [ ] **Шаг 5. Пересобрать предпросмотры шаблонов**

`preview.html` обоих шаблонов встраивает собранный `shared/template/result-context.ts`, а он
изменился. Раскладки НЕ меняются (новые поля никто не печатает), но встроенный код обязан
совпадать с исходником.

```bash
npm run scorm:previews
```

Ожидание: изменились только `server/scorm/templates/default/preview.html` и
`templates/certification/preview.html`.

- [ ] **Шаг 6. Коммит**

```bash
git add shared/template/context.ts shared/template/result-context.ts \
        shared/template/__tests__/result-context-breakdown.test.ts \
        server/scorm/templates/default/preview.html templates/certification/preview.html
git commit -m "feat(prd-50): строка разреза несёт полный состав по §8.1"
```

---

## Task 2: FR-39 — записи области теста сохраняются с попыткой

Сегодня они живут ровно один вызов: собираются в `attempts.ts:1533` для контекста формул и
пропадают. Без них блок `breakdown` из Э4 нечем наполнить, а аналитика обязана пересчитывать
то, что уже было посчитано.

**Files:**

- Modify: `shared/schema.ts` (`topicResultSchema`, `attemptResultSchema`)
- Modify: `server/routes/attempts.ts` (`/finish`, строки 1494-1562)
- Test: `tests/breakdown-persistence.test.ts` (создать)

- [ ] **Шаг 1. Написать падающий тест**

`tests/breakdown-persistence.test.ts`:

```ts
/**
 * @module tests/breakdown-persistence
 * @description PRD-50 FR-39: записи разреза области ТЕСТА сохраняются вместе с попыткой.
 * До этой работы они собирались только ради контекста формул и пропадали, поэтому блок
 * области теста (Э4) и аналитика без пересчёта были невозможны.
 */
import { describe, it, expect } from "vitest";
import { attemptResultSchema, breakdownEntrySchema } from "../shared/schema";

const entry = {
  scope: "test", axis: "tag", key: "ПДн", items: 2, answered: 2, earned: 1, possible: 2,
  unitEarned: 1, unitPossible: 2, percentPoints: 50, percentUnits: 50,
};

const base = {
  totalCorrect: 1, totalQuestions: 2, overallPercent: 50,
  totalEarnedPoints: 1, totalPossiblePoints: 2, overallPassed: false,
  topicResults: [],
};

describe("attemptResultSchema.breakdowns", () => {
  it("принимает записи области теста", () => {
    const parsed = attemptResultSchema.parse({ ...base, breakdowns: [entry] });
    expect(parsed.breakdowns).toEqual([entry]);
  });

  it("результат, сохранённый до этой работы, остаётся валидным", () => {
    expect(attemptResultSchema.parse(base).breakdowns).toBeUndefined();
  });

  it("схема записи одна на все места хранения", () => {
    // Страж от второй копии: три места (тема, тест, адаптивная тема) обязаны разбирать
    // запись ОДНОЙ схемой, иначе поле, добавленное в одном, молча потеряется в двух других.
    expect(breakdownEntrySchema.parse(entry)).toEqual(entry);
  });
});
```

Команда: `npm test -- tests/breakdown-persistence.test.ts`
Ожидание: FAIL — `breakdownEntrySchema` не экспортируется, `breakdowns` не разбирается.

- [ ] **Шаг 2. Вынести схему записи и добавить поле**

В `shared/schema.ts` ПЕРЕД `topicResultSchema` (строка 1262) объявить:

```ts
/**
 * One PRD-50 breakdown record as it is STORED with an attempt — the mirror of
 * `shared/breakdown/types`'s `BreakdownEntry`. Declared once because three places keep
 * the very same record (a topic's own scope, the test scope, an adaptive topic's scope),
 * and three hand-written copies would drift the moment the record gains a field.
 */
export const breakdownEntrySchema = z.object({
  scope: z.string(),
  axis: z.string(),
  key: z.string(),
  items: z.number(),
  answered: z.number(),
  earned: z.number(),
  possible: z.number(),
  unitEarned: z.number(),
  unitPossible: z.number(),
  percentPoints: z.number(),
  percentUnits: z.number(),
});
```

В `topicResultSchema` заменить встроенный `z.array(z.object({ … }))` (строки 1296-1312) на:

```ts
  breakdown: z.array(breakdownEntrySchema).default([]),
```

В `attemptResultSchema` после `topicResults` добавить:

```ts
  // PRD-50 FR-39: записи области ТЕСТА. Секционные лежат на своих темах и здесь НЕ
  // дублируются — одна запись, одно место. `optional()`, а не `.default([])`: отсутствие
  // поля означает «попытка завершена до этой работы», и отличать это от «тест без тегов»
  // нужно аналитике, которая иначе не поймёт, можно ли доверять пустому списку.
  breakdowns: z.array(breakdownEntrySchema).optional(),
```

- [ ] **Шаг 3. Сохранить записи в результат**

В `server/routes/attempts.ts` в обработчике `/finish` заменить сборку массива для формул
(строки 1530-1536) на общее объявление ПЕРЕД блоком `if (scoringConfig…)` (то есть перед
строкой 1513):

```ts
    // PRD-50 FR-35/FR-36: обе области ОДНИМ плоским массивом — движок возвращает записи
    // области теста в `breakdowns`, а секционные кладёт на каждую тему, и `tag()` обязан
    // дотянуться до любой.
    const allBreakdowns = [...agg.breakdowns, ...agg.topicResults.flatMap((t) => t.breakdown)];
```

в вызове `computeAttemptResult` оставить `breakdowns: allBreakdowns,`, а в объект `result`
(строка 1551) добавить последним полем:

```ts
      // PRD-50 FR-39: записи области ТЕСТА едут с попыткой. Секционные уже лежат на темах
      // (`topicResults[].breakdown`), поэтому сюда кладётся ТОЛЬКО область теста — пустой
      // список не кладётся вовсе, чтобы результат теста без тегов не менялся ни на байт.
      ...(agg.breakdowns.length ? { breakdowns: agg.breakdowns } : {}),
```

- [ ] **Шаг 4. Прогнать тесты и типы**

```bash
npm test -- tests/breakdown-persistence.test.ts tests/breakdown-web-host.test.ts
npm test -- tests/routes.attempts-tests.test.ts
npm run check
```

Ожидание: PASS.

- [ ] **Шаг 5. Коммит**

```bash
git add shared/schema.ts server/routes/attempts.ts tests/breakdown-persistence.test.ts
git commit -m "feat(prd-50): записи области теста сохраняются вместе с попыткой"
```

---

## Task 3: FR-17 — адаптивный разрез в движке и на веб-хосте

`adaptiveResultAsStandard` не может посчитать разрез из своего входа: `AdaptiveResult` несёт
только счётчики уровней, а разрез считается по ВЫДАННЫМ вопросам. Поэтому элементы собирает
хост и передаёт вторым аргументом — движок остаётся чистым и единственным.

Цена вопроса в адаптиве равна единице: так же `adaptiveResultAsStandard` уже пересказывает
итоги (`earnedPoints: correct`, `shared/scoring/aggregate.ts:473`), и вердикт «верно» там
двоичный — веб считает `checkAnswer(...) === 1` (`attempts.ts:937`), пакет наращивает
`correctCount` тем же признаком.

**Files:**

- Modify: `shared/scoring/aggregate.ts` (`AdaptiveAsStandard*`, `adaptiveResultAsStandard`)
- Modify: `server/routes/attempts.ts` (`buildAdaptiveResult`, строки 2064-2118)
- Modify: `shared/schema.ts` (`adaptiveTopicResultSchema`, `adaptiveAttemptResultSchema`)
- Test: `tests/breakdown-adaptive.test.ts` (создать)

- [ ] **Шаг 1. Написать падающий тест**

`tests/breakdown-adaptive.test.ts`:

```ts
/**
 * @module tests/breakdown-adaptive
 * @description PRD-50 FR-17: разрезы считаются и в адаптивном режиме. До этой работы
 * `adaptiveResultAsStandard` их не считал, а теги адаптивных вопросов уже выпекались в
 * каждый пакет и никем не читались.
 */
import { describe, it, expect } from "vitest";
import { adaptiveResultAsStandard, type AdaptiveResult } from "../shared/scoring/aggregate";
import { TEST_SCOPE, sectionScope } from "../shared/breakdown/compute";
import type { BreakdownItem } from "../shared/breakdown/types";

const topic = (topicId: string, answered: number, correct: number) => ({
  topicId,
  topicName: topicId,
  achievedLevelIndex: 0,
  achievedLevelName: "Базовый",
  levelPercent: 100,
  totalQuestionsAnswered: answered,
  totalCorrect: correct,
  levelsAttempted: [],
  feedback: null,
  recommendedLinks: [],
});

const result = {
  mode: "adaptive" as const,
  overallPassed: true,
  topicResults: [topic("law", 2, 1), topic("sec", 1, 1)],
} as AdaptiveResult;

const item = (sectionId: string, tags: string[], earned: number): BreakdownItem => ({
  sectionId, axisKeys: { tag: tags }, earned, possible: 1, answered: true,
});

describe("adaptiveResultAsStandard + разрезы", () => {
  it("без элементов даёт пустые списки и прежний пересказ (обратная совместимость)", () => {
    const flat = adaptiveResultAsStandard(result);
    expect(flat.breakdowns).toEqual([]);
    expect(flat.topicResults.map((t) => t.breakdown)).toEqual([[], []]);
    expect(flat.percent).toBe((2 / 3) * 100);
  });

  it("кладёт записи раздела на тему, записи теста — в результат", () => {
    const flat = adaptiveResultAsStandard(result, [
      item("law", ["ПДн"], 1),
      item("law", ["ПДн"], 0),
      item("sec", ["ПДн"], 1),
    ]);
    expect(flat.topicResults[0].breakdown).toEqual([
      expect.objectContaining({ scope: sectionScope("law"), key: "ПДн", items: 2, percentUnits: 50 }),
    ]);
    expect(flat.topicResults[1].breakdown).toEqual([
      expect.objectContaining({ scope: sectionScope("sec"), key: "ПДн", items: 1, percentUnits: 100 }),
    ]);
    expect(flat.breakdowns).toEqual([
      expect.objectContaining({ scope: TEST_SCOPE, key: "ПДн", items: 3, percentUnits: (2 / 3) * 100 }),
    ]);
  });

  it("тема без единого тега получает пустой список, а не отсутствующее поле", () => {
    const flat = adaptiveResultAsStandard(result, [item("law", ["ПДн"], 1)]);
    expect(flat.topicResults[1].breakdown).toEqual([]);
  });
});
```

Команда: `npm test -- tests/breakdown-adaptive.test.ts`
Ожидание: FAIL — второго аргумента нет, `breakdown`/`breakdowns` не существуют.

- [ ] **Шаг 2. Научить движок**

В `shared/scoring/aggregate.ts` в `AdaptiveAsStandardTopic` (строка 415) добавить последним полем:

```ts
  /**
   * PRD-50 FR-17: breakdown records in THIS topic's scope. Empty when the caller passed
   * no items — an adaptive host that knows nothing of the axis keeps its old result shape.
   */
  breakdown: BreakdownEntry[];
```

В `AdaptiveAsStandard` (строка 429) добавить:

```ts
  /** PRD-50 FR-17: breakdown records in the TEST scope (empty without items). */
  breakdowns: BreakdownEntry[];
```

Заменить сигнатуру и тело `adaptiveResultAsStandard` (строка 461):

```ts
export function adaptiveResultAsStandard<E = unknown>(
  result: AdaptiveResult<E>,
  breakdownItems: readonly BreakdownItem[] = [],
): AdaptiveAsStandard {
  let totalQuestions = 0;
  let correct = 0;
  for (const tr of result.topicResults) {
    totalQuestions += tr.totalQuestionsAnswered;
    correct += tr.totalCorrect;
  }

  // PRD-50 FR-17: the SAME engine the standard aggregate calls — the ladder does not get
  // its own arithmetic. The items cannot be derived here: an adaptive result carries only
  // per-level tallies, while a breakdown is computed over DELIVERED questions, so the host
  // that knows which questions were asked assembles them (web: `buildAdaptiveResult`,
  // package: `adaptiveBreakdownItems`). No items = no records, and the result keeps
  // exactly the shape it had before this work (FR-18).
  const entries = computeBreakdowns(breakdownItems);
  const bySection = new Map<string, BreakdownEntry[]>();
  for (const e of entries) {
    const list = bySection.get(e.scope);
    if (list) list.push(e);
    else bySection.set(e.scope, [e]);
  }

  return {
    correct,
    totalQuestions,
    // Each answered question is worth one point in the adaptive mode: the ladder has no
    // per-question price (`test_question_scoring` gates the standard delivery only).
    earnedPoints: correct,
    possiblePoints: totalQuestions,
    percent: totalQuestions > 0 ? (correct / totalQuestions) * 100 : 0,
    passed: result.overallPassed,
    breakdowns: entries.filter((e) => e.scope === TEST_SCOPE),
    topicResults: result.topicResults.map((tr) => ({
      topicId: tr.topicId,
      topicName: tr.topicName,
      correct: tr.totalCorrect,
      total: tr.totalQuestionsAnswered,
      percent: tr.levelPercent,
      earnedPoints: tr.totalCorrect,
      possiblePoints: tr.totalQuestionsAnswered,
      passed: tr.achievedLevelIndex !== null,
      achievedLevelName: tr.achievedLevelName,
      recommendedCourses: tr.recommendedLinks,
      breakdown: bySection.get(sectionScope(tr.topicId)) ?? [],
    })),
  };
}
```

Команда: `npm test -- tests/breakdown-adaptive.test.ts`
Ожидание: PASS, 3 теста.

- [ ] **Шаг 3. Расширить схему адаптивного результата**

В `shared/schema.ts` в `adaptiveTopicResultSchema` (строка 1376) после `feedbackTexts` добавить:

```ts
  // PRD-50 FR-17/FR-39: записи разреза области ЭТОЙ темы, по той же причине и по той же
  // схеме, что у стандартного результата. `.default([])` держит валидными адаптивные
  // попытки, завершённые до этой работы.
  breakdown: z.array(breakdownEntrySchema).default([]),
```

В `adaptiveAttemptResultSchema` (строка 1408) после `resultVariables` добавить:

```ts
  // PRD-50 FR-39: записи области ТЕСТА. Как и у стандартного результата — `optional()`,
  // и секционные здесь не дублируются: они лежат на своих темах.
  breakdowns: z.array(breakdownEntrySchema).optional(),
```

- [ ] **Шаг 4. Собрать элементы на веб-хосте**

В `server/routes/attempts.ts` в `buildAdaptiveResult` (строка 2000), сразу после
`const aggregated = aggregateAdaptiveResult…` (строка 2064-2067), вставить:

```ts
  // PRD-50 FR-17: выданные элементы адаптивного прогона — каждый вопрос, который лестница
  // действительно задала, отнесённый к теме, которая его задала. Читаются БЕЗУСЛОВНО, а не
  // только под шкалы: записи сохраняются вместе с попыткой (FR-39), а экран итогов рисуется
  // из сохранённого результата.
  const answeredIds = Object.keys(answers);
  const answeredQuestions = answeredIds.length > 0 ? await storage.getQuestionsByIds(answeredIds) : [];
  const questionById = new Map<string, any>(answeredQuestions.map((q: any) => [q.id, q]));
  // PRD-15 block D: тем же разрешением цены/ступеней, что и стандартная выдача, — иначе
  // «верно» здесь и «верно» в счётчике уровня разошлись бы на ступенчатом вопросе.
  const adaptiveScoring = await loadTestScoringContext(testId, storage);
  const breakdownItems: BreakdownItem[] = [];
  for (const topic of variant.topics as any[]) {
    for (const level of topic.levelsState as any[]) {
      for (const qId of level.answeredQuestionIds as string[]) {
        const q = questionById.get(qId);
        if (!q || !Array.isArray(q.tags) || q.tags.length === 0) continue;
        // Цена вопроса в адаптиве равна единице — тот же пересказ, что делает
        // `adaptiveResultAsStandard` для итогов (earnedPoints = correct), и тот же
        // двоичный признак, каким лестница наращивает `correctCount`.
        const correct = checkAnswer(q, (answers as Record<string, unknown>)[qId], adaptiveScoring.resolve(q).scoring) === 1;
        breakdownItems.push({
          sectionId: topic.topicId,
          axisKeys: { tag: q.tags },
          earned: correct ? 1 : 0,
          possible: 1,
          answered: true,
        });
      }
    }
  }
  const flat = adaptiveResultAsStandard(aggregated, breakdownItems);
  const breakdownByTopic = new Map(flat.topicResults.map((t) => [t.topicId, t.breakdown]));
```

Добавить импорт типа в шапку файла (рядом с прочими `@shared`-импортами):

```ts
import type { BreakdownItem } from "@shared/breakdown/types";
```

- [ ] **Шаг 5. Подать разрезы формулам и сохранить их**

Внутри блока `if (scoringConfig.scales.length > 0 || …)` удалить строку
`const flat = adaptiveResultAsStandard(aggregated);` (она переехала выше) и дополнить базу
вызова `computeAttemptResult`:

```ts
    const computation = computeAttemptResult(scoringConfig, answers as Record<string, Answer>, questionTypes, {
      percent: flat.percent,
      topicResults: flat.topicResults.map((t) => ({ ...t, code: topicCodeById.get(t.topicId) ?? null })),
      // PRD-50 FR-35/FR-36: обе области одним массивом, как в стандартной ветке.
      breakdowns: [...flat.breakdowns, ...flat.topicResults.flatMap((t) => t.breakdown)],
    });
```

В возвращаемом объекте (строка 2108) добавить записи на темы и на результат:

```ts
  return {
    ...aggregated,
    topicResults: aggregated.topicResults.map(({ extra, ...t }) => ({
      ...t,
      feedbackTexts: extra?.feedbackTexts ?? [],
      recommendedAssets: extra?.recommendedAssets ?? [],
      // PRD-50 FR-17/FR-39: записи области этой темы — тем же путём, каким сюда попадают
      // тексты и вложения: они едут ВМЕСТЕ с попыткой, потому что экран рисуется из
      // сохранённого результата.
      breakdown: breakdownByTopic.get(t.topicId) ?? [],
    })),
    ...(scaleResults ? { scaleResults } : {}),
    ...(resultVariables ? { resultVariables } : {}),
    ...(flat.breakdowns.length ? { breakdowns: flat.breakdowns } : {}),
  };
```

- [ ] **Шаг 6. Дописать тест веб-хоста**

Добавить в `tests/breakdown-adaptive.test.ts` блок, проверяющий схему хранения:

```ts
import { adaptiveAttemptResultSchema } from "../shared/schema";

describe("хранение адаптивного результата", () => {
  const topicRow = {
    topicId: "law", topicName: "Право", achievedLevelIndex: 0, achievedLevelName: "Базовый",
    levelPercent: 100, totalQuestionsAnswered: 2, totalCorrect: 1, levelsAttempted: [],
    feedback: null, recommendedLinks: [],
  };
  const entry = {
    scope: "section:law", axis: "tag", key: "ПДн", items: 2, answered: 2, earned: 1,
    possible: 2, unitEarned: 1, unitPossible: 2, percentPoints: 50, percentUnits: 50,
  };

  it("тема несёт свои записи, результат — записи области теста", () => {
    const parsed = adaptiveAttemptResultSchema.parse({
      mode: "adaptive", overallPassed: true,
      topicResults: [{ ...topicRow, breakdown: [entry] }],
      breakdowns: [{ ...entry, scope: "test" }],
    });
    expect(parsed.topicResults[0].breakdown).toHaveLength(1);
    expect(parsed.breakdowns).toHaveLength(1);
  });

  it("адаптивная попытка, завершённая до этой работы, остаётся валидной", () => {
    const parsed = adaptiveAttemptResultSchema.parse({
      mode: "adaptive", overallPassed: true, topicResults: [topicRow],
    });
    expect(parsed.topicResults[0].breakdown).toEqual([]);
    expect(parsed.breakdowns).toBeUndefined();
  });
});
```

- [ ] **Шаг 7. Прогнать тесты и типы**

```bash
npm test -- tests/breakdown-adaptive.test.ts tests/adaptive.test.ts
npm test -- tests/scoring-aggregate.test.ts tests/result-compute-mbi-parity.test.ts
npm run check
```

Ожидание: PASS. `adaptiveResultAsStandard` вызывается ещё из
`server/scorm/template/app/adaptive/adaptive.js` (правится задачей 4) и из
`shared/template/runtime-entry.ts` (реэкспорт, правки не требует) — второй аргумент
необязателен, поэтому старые вызовы компилируются как есть.

- [ ] **Шаг 8. Коммит**

```bash
git add shared/scoring/aggregate.ts shared/schema.ts server/routes/attempts.ts \
        tests/breakdown-adaptive.test.ts
git commit -m "feat(prd-50): разрезы считаются и в адаптивном режиме на веб-хосте"
```

---

## Task 4: FR-17 — адаптивный разрез в рантайме пакета

**Files:**

- Modify: `server/scorm/template/app/adaptive/adaptive.js` (строки 552-568)
- Test: `tests/breakdown-adaptive.test.ts` (дополнить)

- [ ] **Шаг 1. Написать падающий тест**

Добавить в `tests/breakdown-adaptive.test.ts` блок, поднимающий функцию из плоского
ES5-файла — тем же приёмом, каким `tests/breakdown-formula-context.test.ts` поднимает
`buildResultVarContext`:

```ts
import { readFileSync } from "node:fs";
import { resolve } from "node:path";

const adaptiveSrc = readFileSync(
  resolve(process.cwd(), "server/scorm/template/app/adaptive/adaptive.js"),
  "utf8",
);

/** Lift the package's plain-JS `adaptiveBreakdownItems` and bind it to injected globals. */
function makeAdaptiveBreakdownItems(state: unknown, testData: unknown, checkAnswer: unknown) {
  const match = adaptiveSrc.match(/function adaptiveBreakdownItems\(\)[\s\S]*?\n\}/);
  if (!match) throw new Error("adaptiveBreakdownItems not found in adaptive.js");
  // eslint-disable-next-line @typescript-eslint/no-implied-eval
  return new Function(
    "state", "TEST_DATA", "checkAnswer",
    `${match[0]}\n;return adaptiveBreakdownItems;`,
  )(state, testData, checkAnswer) as () => unknown[];
}

describe("рантайм пакета: сборка элементов адаптивного разреза", () => {
  const state = {
    answers: { q1: 0, q2: 1, q3: 0 },
    adaptiveState: {
      topics: [
        { topicId: "law", levelsState: [{ answeredQuestionIds: ["q1", "q2"] }] },
        { topicId: "sec", levelsState: [{ answeredQuestionIds: ["q3"] }] },
      ],
    },
  };
  const TEST_DATA = {
    adaptiveTopics: [
      { topicId: "law", questions: [
        { id: "q1", tags: ["ПДн"], correct: { correctIndex: 0 } },
        { id: "q2", tags: ["ПДн"], correct: { correctIndex: 0 } },
      ] },
      { topicId: "sec", questions: [{ id: "q3", correct: { correctIndex: 0 } }] },
    ],
  };
  const checkAnswer = (q: any, a: unknown) => (a === q.correct.correctIndex ? 1 : 0);

  it("берёт только вопросы с тегами и даёт им цену в один балл", () => {
    const items = makeAdaptiveBreakdownItems(state, TEST_DATA, checkAnswer)();
    expect(items).toEqual([
      { sectionId: "law", axisKeys: { tag: ["ПДн"] }, earned: 1, possible: 1, answered: true },
      { sectionId: "law", axisKeys: { tag: ["ПДн"] }, earned: 0, possible: 1, answered: true },
    ]);
  });

  it("даёт те же записи, что и веб-хост на тех же данных", () => {
    const items = makeAdaptiveBreakdownItems(state, TEST_DATA, checkAnswer)();
    const flat = adaptiveResultAsStandard(result, items as never);
    expect(flat.topicResults[0].breakdown[0]).toMatchObject({ items: 2, percentUnits: 50 });
  });
});
```

Команда: `npm test -- tests/breakdown-adaptive.test.ts`
Ожидание: FAIL — `adaptiveBreakdownItems not found in adaptive.js`.

- [ ] **Шаг 2. Написать сборщик элементов**

В `server/scorm/template/app/adaptive/adaptive.js` ПЕРЕД `getAdaptiveResultForScorm`
(строка 552) вставить:

```js
/**
 * PRD-50 FR-17: the delivered items of an adaptive run, in the shape the shared breakdown
 * engine takes.
 *
 * The ladder has no per-question price — one asked question is worth one point, the very
 * restatement `adaptiveResultAsStandard` performs for the totals — so `possible` is 1 and
 * `earned` is 1 only on a fully correct answer, the same binary the level's `correctCount`
 * is grown by. Untagged questions are skipped, so a package whose adaptive topics carry no
 * tags produces an empty list and nothing downstream changes (FR-18).
 *
 * `checkAnswer` lives in `render/resultsPage.js` — a sibling in the same flat bundle. The
 * `typeof` guard is not decoration: the concatenation order is not a contract, and a missing
 * grader must cost the breakdown, not the whole results screen.
 *
 * @returns {Array} Breakdown items for `TBTemplate.adaptiveResultAsStandard`.
 */
function adaptiveBreakdownItems() {
  var items = [];
  if (!state.adaptiveState || !state.adaptiveState.topics) return items;
  if (typeof checkAnswer !== 'function') return items;
  state.adaptiveState.topics.forEach(function (topic) {
    var topicData = TEST_DATA.adaptiveTopics.find(function (t) {
      return t.topicId === topic.topicId;
    });
    if (!topicData) return;
    topic.levelsState.forEach(function (level) {
      (level.answeredQuestionIds || []).forEach(function (qId) {
        var question = topicData.questions.find(function (q) { return q.id === qId; });
        if (!question || !question.tags || !question.tags.length) return;
        items.push({
          sectionId: topic.topicId,
          axisKeys: { tag: question.tags },
          earned: checkAnswer(question, state.answers[qId]) === 1 ? 1 : 0,
          possible: 1,
          answered: true
        });
      });
    });
  });
  return items;
}
```

- [ ] **Шаг 3. Подать элементы в пересказ**

Заменить тело `getAdaptiveResultForScorm` (строки 563-568):

```js
function getAdaptiveResultForScorm() {
  if (!state.adaptiveState || !state.adaptiveState.result) {
    return null;
  }
  var flat = window.TBTemplate.adaptiveResultAsStandard(
    state.adaptiveState.result,
    adaptiveBreakdownItems()
  );
  // PRD-50 FR-35/FR-36: ONE flat array for `tag()`, assembled exactly as `calculateResults`
  // assembles it in the standard mode — the shared engine returns the test scope on
  // `breakdowns` and the section scopes on each topic result, and the accessor must reach
  // either. `buildResultVarContext` reads this one field and nothing else.
  var breakdowns = (flat.breakdowns || []).slice();
  for (var i = 0; i < flat.topicResults.length; i++) {
    var secEntries = flat.topicResults[i].breakdown || [];
    for (var j = 0; j < secEntries.length; j++) breakdowns.push(secEntries[j]);
  }
  flat.breakdowns = breakdowns;
  return flat;
}
```

- [ ] **Шаг 4. Прогнать тесты**

```bash
npm test -- tests/breakdown-adaptive.test.ts tests/breakdown-formula-context.test.ts
npm test -- tests/results-feedback-adaptive-scorm.test.ts
```

Ожидание: PASS.

- [ ] **Шаг 5. Проверить на живом пакете**

```bash
npm run scorm:template
npm run scorm:player
```

Пройти АДАПТИВНЫЙ пакет теста с тегами до экрана итогов и в консоли проверить:

```js
getAdaptiveResultForScorm().breakdowns.length  // > 0 у теста с тегами
getAdaptiveResultForScorm().breakdowns.filter(function (e) { return e.scope === 'test'; })
```

Ожидание: непустой массив, в нём есть записи и области теста, и области раздела; у пакета без
тегов массив пуст. Карточка адаптивной темы полос НЕ печатает — см. решение ниже.

- [ ] **Шаг 6. Коммит**

```bash
git add server/scorm/template/app/adaptive/adaptive.js tests/breakdown-adaptive.test.ts
git commit -m "feat(prd-50): рантайм пакета считает разрезы адаптивного прогона"
```

### Решение, зафиксированное задачами 3 и 4: карточка адаптивной темы полос не печатает

FR-17 обоснован в спеке тем, что «`adaptiveResultAsStandard` приводит результат к той же
форме», а у этой функции ровно два потребителя, и оба нарисованы в её собственном JSDoc
(`shared/scoring/aggregate.ts:439-460`): формулы PRD-2 и отчёт LMS PRD-4. Ни один из них не
экран. Контракт §8.1 адресует строку как `result.topicResults[].breakdown`, но в адаптивном
режиме `topicResults` — это `CtxAdaptiveTopicView` (`shared/template/context.ts:102`), другой
тип с другим смыслом: он показывает ПОДТВЕРЖДЁННЫЙ УРОВЕНЬ, а не проценты, и слота под
разрез в нём нет. Раскладка адаптивных итогов тоже отдельная
(`layouts/results.adaptive.html`). Печатать полосы на карточке уровня — это решение о том,
как выглядит адаптивный экран, а его в спеке нет. Поэтому задачи 3 и 4 доводят вычисление,
формулы и хранение, а печать на адаптивной карточке остаётся вопросом к владельцу и в этот
этап не входит.

---

## Task 5: «Мой результат» в пакете SCORM — сохранение записей раздела

### Цена вопроса, посчитанная до правки

Одна запись разреза в JSON — 271 байт для области раздела и 231 для области теста
(измерено: `node -e "JSON.stringify(entry)"` на записи с UUID-раздела и русским ключом
«Корпоративная культура»). Разбор книги сертификационного теста руководителей даёт
одиннадцать тегов, разбиение чистое (вопросов с двумя тегами — 0), значит:

| Форма записи | Что хранится | Байт на попытку |
| --- | --- | --- |
| A. Полная, на темах | 11 записей области раздела | ≈ 3.0 КБ |
| B. Урезанная (без производных) | те же 11 без `percentPoints`/`percentUnits`/`unitPossible` | ≈ 2.0 КБ |
| C. Не хранить | — | 0 |

Против чего это меряется: в `suspend_data` уже пишутся `answers` и `flatQuestions` ЦЕЛИКОМ
(`utils/scorm/suspendAttempts.js:214-215`), а `flatQuestions` — это выданные вопросы со всем
их текстом. Средний выпеченный вопрос демо-пакета `out/sample-default-template.zip` весит
462 байта (измерено декодированием `TEST_DATA` из `app.js`); у сертификационного теста
вопросы длиннее, а выдача — 48 вопросов, то есть `flatQuestions` одной попытки это 30-55 КБ.

**Решение: форма A.** Плюс 3 КБ — это 5-10 % от того, что запись попытки уже весит, и
экономия варианта B (1 КБ) покупается вторым местом, где считаются проценты: `vrTopicBreakdown`
пришлось бы досчитывать то, что ядро уже посчитало, — ровно тот второй источник истины,
против которого написан весь PRD. Вариант C отвергнут: он оставляет расхождение, которое
запрещает пункт 5 приёмки спеки («веб-хост и SCORM-пакет печатают одинаковые разрезы на одной
и той же попытке»).

**Отдельно и НЕ этой правкой:** бюджет `suspend_data` в 64 КБ, судя по цифрам выше, уже
превышен `flatQuestions` на тесте с тремя попытками, и это долг старше PRD-50. Проверяется
одной строкой в консоли живого пакета — см. шаг 5.

**Files:**

- Modify: `server/scorm/template/app/render/resultsPage.js` (`calculateResults`, строки 602-641)
- Modify: `server/scorm/template/app/render/viewResults.js` (`vrTopicBreakdown` + 2 вызова)
- Modify: `server/scorm/template/app/utils/pdfExport.js` (строка 151)
- Test: `tests/breakdown-persistence.test.ts` (дополнить)

- [ ] **Шаг 1. Написать падающий тест**

Дописать в `tests/breakdown-persistence.test.ts`:

```ts
import { readFileSync } from "node:fs";
import { resolve } from "node:path";

const viewSrc = readFileSync(
  resolve(process.cwd(), "server/scorm/template/app/render/viewResults.js"),
  "utf8",
);

/** Lift the package's plain-JS `vrTopicBreakdown` out of the flat ES5 runtime. */
function makeVrTopicBreakdown() {
  const match = viewSrc.match(/function vrTopicBreakdown\([\s\S]*?\n\}/);
  if (!match) throw new Error("vrTopicBreakdown not found in viewResults.js");
  // eslint-disable-next-line @typescript-eslint/no-implied-eval
  return new Function(`${match[0]}\n;return vrTopicBreakdown;`)() as (
    tr: unknown,
    breakdowns: unknown,
  ) => unknown[];
}

describe("пакет SCORM: разрез сохранённой попытки", () => {
  const sectionEntry = { ...entry, scope: "section:law" };
  const vrTopicBreakdown = makeVrTopicBreakdown();

  it("у СОХРАНЁННОЙ попытки берёт записи с самой темы", () => {
    // Запись попытки из suspend_data: плоского списка `breakdowns` в ней нет вовсе.
    const tr = { topicId: "law", breakdown: [sectionEntry] };
    expect(vrTopicBreakdown(tr, undefined)).toEqual([sectionEntry]);
  });

  it("у ТЕКУЩЕЙ попытки берёт свои записи из плоского списка", () => {
    const tr = { topicId: "law" };
    expect(vrTopicBreakdown(tr, [sectionEntry, { ...entry, scope: "section:sec" }]))
      .toEqual([sectionEntry]);
  });

  it("темы без ключей остаются без строк", () => {
    expect(vrTopicBreakdown({ topicId: "sec" }, [sectionEntry])).toEqual([]);
  });
});

describe("пакет SCORM: calculateResults не выбрасывает записи темы", () => {
  const resultsSrc = readFileSync(
    resolve(process.cwd(), "server/scorm/template/app/render/resultsPage.js"),
    "utf8",
  );

  it("тема, уходящая в suspend_data, несёт поле breakdown", () => {
    // Страж, а не проверка вычисления: `saveAttemptResult` пишет `topicResults` дословно
    // (suspendAttempts.js), поэтому поле на теме — единственный путь записей в сохранённую
    // попытку. Пропажа этой строки читается как «полос нет у прошлых попыток» и ничем
    // больше не ловится.
    const mapped = resultsSrc.match(/topicResults: agg\.topicResults\.map\([\s\S]*?\n {4}\}\)/);
    expect(mapped?.[0]).toMatch(/breakdown: t\.breakdown/);
  });
});
```

Команда: `npm test -- tests/breakdown-persistence.test.ts`
Ожидание: FAIL — `vrTopicBreakdown` принимает `topicId`, а не тему; `breakdown: t.breakdown`
в `calculateResults` отсутствует.

- [ ] **Шаг 2. Вернуть записи на темы**

В `server/scorm/template/app/render/resultsPage.js` заменить комментарий и сборку плоского
списка (строки 602-611) на:

```js
  // PRD-50 FR-35/FR-36: обе области ОДНИМ плоским массивом — его и только его читает
  // `buildResultVarContext`, чтобы `tag()` дотянулся до любой области.
  var breakdowns = (agg.breakdowns || []).slice();
  for (var bi = 0; bi < agg.topicResults.length; bi++) {
    var secEntries = agg.topicResults[bi].breakdown || [];
    for (var bj = 0; bj < secEntries.length; bj++) breakdowns.push(secEntries[bj]);
  }
```

и в маппинге тем (строка 620) добавить последним полем:

```js
        // PRD-50: записи разреза ЭТОЙ темы. Остаются на теме сознательно: `saveAttemptResult`
        // пишет `topicResults` дословно, и это ЕДИНСТВЕННЫЙ путь, которым разрез попадает в
        // сохранённую попытку — без него экран «Мой результат» и скачанный оттуда отчёт
        // печатали бы пустоту там, где веб на той же попытке печатает полосы (§14, пункт 5).
        // Цена — около 3 КБ на попытку против 30-55 КБ, которые `flatQuestions` уже весит.
        breakdown: t.breakdown
```

- [ ] **Шаг 3. Научить читателя брать записи с темы**

В `server/scorm/template/app/render/viewResults.js` заменить `vrTopicBreakdown`
(строки 93-120) целиком:

```js
/**
 * PRD-50: this topic's breakdown records, from whichever of the two places has them.
 *
 * A SAVED attempt carries them ON the topic: `saveAttemptResult` persists `topicResults`
 * verbatim, so the records ride along with everything else the topic keeps. The CURRENT
 * attempt additionally has the ONE flat list `calculateResults` returns (`results.breakdowns`
 * — both scopes in a single array, built for `tag()`), and the topic's own field is filled
 * there too; the flat list stays as the fallback for a record written by an older package,
 * which had the list but no per-topic field.
 *
 * The scope string is the shared convention `"section:" + topicId`
 * (`shared/breakdown/compute.ts`'s `sectionScope`), reproduced here rather than imported:
 * this is a flat ES5 runtime file, and the prefix is stable data, not an algorithm.
 *
 * @param {object} tr The topic result being rendered.
 * @param {Array|undefined} breakdowns The flat list, when the caller has one.
 * @returns {Array} This topic's section-scope breakdown records, in order.
 */
function vrTopicBreakdown(tr, breakdowns) {
  if (tr && tr.breakdown && tr.breakdown.length) return tr.breakdown;
  var scope = 'section:' + (tr && tr.topicId);
  var out = [];
  (breakdowns || []).forEach(function (e) {
    if (e && e.scope === scope) out.push(e);
  });
  return out;
}
```

Поправить оба вызова в том же файле (строки 474 и 577):

```js
        breakdown: vrTopicBreakdown(tr, results.breakdowns)
```

и заменить комментарий над вызовом в `renderViewResultsTemplated` (строки 472-473):

```js
        // PRD-50: записи разреза темы — с самой темы у сохранённой попытки, из плоского
        // списка у текущей. См. `vrTopicBreakdown`.
```

- [ ] **Шаг 4. Тот же читатель в отчёте**

В `server/scorm/template/app/utils/pdfExport.js` заменить строки 148-151:

```js
        // PRD-50: записи разреза этой темы — ТЕМ ЖЕ читателем, что и экран итогов
        // (`viewResults.js`): §5.2 запрещает отчёту показывать иное, чем экран, с которого
        // его скачали, а раньше отчёт сохранённой попытки полос не печатал вовсе.
        breakdown: (typeof vrTopicBreakdown === 'function') ? vrTopicBreakdown(tr, results.breakdowns) : []
```

- [ ] **Шаг 5. Приёмка на живом пакете**

```bash
npm run scorm:template
npm run scorm:player
```

Пройти тест с тегами и включённым показом разреза, затем:

1. на экране итогов сразу после завершения — полосы есть;
2. закрыть и открыть заново, зайти в «Мой результат» — полосы те же и в том же порядке;
3. скачать оттуда PDF — полосы те же;
4. измерить бюджет в консоли:

```js
JSON.stringify(readSuspendObj()).length      // весь suspend_data
JSON.stringify(getLastAttempt().topicResults).length  // доля тем в нём
```

Ожидание: пункты 1-3 совпадают между собой и с веб-хостом на той же попытке. Пункт 4 — число
записывается в отчёт о приёмке; если весь `suspend_data` превышает 64000, это фиксируется
отдельной находкой (долг старше PRD-50, см. преамбулу задачи) и выносится владельцу.

- [ ] **Шаг 6. Коммит**

```bash
git add server/scorm/template/app/render/resultsPage.js \
        server/scorm/template/app/render/viewResults.js \
        server/scorm/template/app/utils/pdfExport.js \
        tests/breakdown-persistence.test.ts
git commit -m "feat(prd-50): сохранённая попытка в пакете печатает полосы разреза"
```

---

## Task 6: FR-38 — снимок публикации, тест-страж

Кода задача не требует: снимок морозит строку `tests` и строки `test_sections` ЦЕЛИКОМ (см.
раздел «Что проверено чтением кода»). Но именно поэтому она и нужна: требование выполняется
свойством, которое нигде не записано, и первая же оптимизация «выбирать только нужные
колонки» его молча снимет — а увидят это только через месяц, по отчёту старой попытки.

**Files:**

- Test: `tests/breakdown-snapshot-freeze.test.ts` (создать)

- [ ] **Шаг 1. Написать тест**

`tests/breakdown-snapshot-freeze.test.ts`:

```ts
/**
 * @module tests/breakdown-snapshot-freeze
 * @description PRD-50 FR-38: снимок публикации несёт настройки разреза и словарь надписей.
 *
 * Кода под это требование нет и не нужно: снимок морозит строку `tests` и строки
 * `test_sections` целиком, поэтому и `breakdown_display_json`, и `design_settings_json`,
 * и будущие `section_groups_json` (Э3) и `breakdown_rules_json` (Э2) едут туда сами. Этот
 * файл — страж ровно этого свойства: «выбрать только нужные колонки» выглядит безобидной
 * оптимизацией и снимает требование молча.
 */
import { describe, it, expect, vi } from "vitest";

const testRow = {
  id: "t1",
  title: "Сертификация",
  mode: "standard",
  version: 3,
  breakdownDisplayJson: { visibility: "bar_and_value", basis: "units" },
  designSettingsJson: { templateId: "default", labels: { results: { "topic.verdict.passed": "Зачёт" } } },
  // Колонка, о которой сборщик снимка не знает ничего: стоит здесь как заместитель
  // будущих `section_groups_json` и любых следующих. Она обязана доехать так же.
  someFutureColumnJson: { kept: true },
};

const sectionRow = {
  id: "s1",
  testId: "t1",
  topicId: "tp1",
  drawCount: 1,
  someFutureSectionColumnJson: { kept: true },
};

vi.mock("../server/services/scale-domain", () => ({ materializeScaleDomains: vi.fn() }));
vi.mock("../server/storage", () => ({
  storage: {
    getTest: vi.fn(async () => testRow),
    getTestSections: vi.fn(async () => [sectionRow]),
    getTopics: vi.fn(async () => [{ id: "tp1", name: "Тема" }]),
    getScales: vi.fn(async () => []),
    getQuestionMeasurements: vi.fn(async () => []),
    getResultVariables: vi.fn(async () => []),
    getContentPages: vi.fn(async () => []),
    getTestQuestionScoring: vi.fn(async () => []),
    getQuestionsByTopic: vi.fn(async () => []),
    getTopicCourses: vi.fn(async () => []),
    getTopicEvents: vi.fn(async () => []),
  },
}));

import { buildSnapshotContent, snapshotDataSource } from "../server/services/test-snapshot";
import type { TestSnapshotContent } from "../server/services/test-snapshot";

describe("снимок публикации и настройки разреза (FR-38)", () => {
  it("морозит строку теста ЦЕЛИКОМ, включая колонки, о которых сборщик не знает", async () => {
    const content = (await buildSnapshotContent("t1")) as TestSnapshotContent;
    expect(content.test).toEqual(testRow);
    expect(content.sections[0]).toEqual(sectionRow);
  });

  it("выдача читает настройку показа и словарь надписей ИЗ снимка", async () => {
    const content = (await buildSnapshotContent("t1")) as TestSnapshotContent;
    const frozen = await snapshotDataSource(content).getTest("t1");
    expect((frozen as never as typeof testRow).breakdownDisplayJson).toEqual({
      visibility: "bar_and_value",
      basis: "units",
    });
    expect((frozen as never as typeof testRow).designSettingsJson.labels.results["topic.verdict.passed"])
      .toBe("Зачёт");
  });
});
```

Команда: `npm test -- tests/breakdown-snapshot-freeze.test.ts`
Ожидание: PASS сразу — это страж уже верного поведения, а не TDD-шаг. Если он падает,
значит FR-38 НЕ выполнен и требуется код, которого этот план не содержит: тогда остановиться
и вынести находку владельцу.

- [ ] **Шаг 2. Коммит**

```bash
git add tests/breakdown-snapshot-freeze.test.ts
git commit -m "test(prd-50): страж на заморозку настроек разреза в снимке публикации"
```

---

## Task 7: FR-41 — телеметрия новых полей не получает, тест-страж

Тоже страж без кода. Требование сегодня выполняется тремя белыми списками подряд, и любой из
них можно расширить одной строкой, не заметив, что это нарушение спеки.

**Files:**

- Test: `tests/breakdown-telemetry-silence.test.ts` (создать)

- [ ] **Шаг 1. Написать тест**

`tests/breakdown-telemetry-silence.test.ts`:

```ts
/**
 * @module tests/breakdown-telemetry-silence
 * @description PRD-50 FR-41: телеметрия SCORM новых полей не получает.
 *
 * Требование держится тремя белыми списками подряд — вызывающий (`finishAndClose`), модуль
 * (`Telemetry.finish`) и серверный маршрут. Каждый расширяется одной строкой, и ни один тест
 * этого не заметит: пакет продолжит работать, просто в БД поедет то, чего спека запретила.
 * Поэтому здесь пришпилен ПОЛНЫЙ состав полезной нагрузки, а не отсутствие слова «breakdown».
 */
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { resolve } from "node:path";

const read = (p: string) => readFileSync(resolve(process.cwd(), p), "utf8");

/** Имена полей объектного литерала: `имя:` в начале строки. */
const literalKeys = (block: string) =>
  [...block.matchAll(/^\s*([A-Za-z_][\w]*)\s*:/gm)].map((m) => m[1]);

describe("FR-41: состав телеметрии финиша зафиксирован", () => {
  it("модуль телеметрии шлёт ровно восемь полей", () => {
    const src = read("server/scorm/template/app/telemetry/telemetry.js");
    // Ровно как в исходнике: `finish: function(results) {`, без пробела перед скобкой.
    const body = src.match(/finish: function\(results\) \{[\s\S]*?\n {4}\},/)?.[0];
    expect(body).toBeTruthy();
    expect(literalKeys(body!.slice(body!.indexOf("{", body!.indexOf("send("))))).toEqual([
      "percent", "passed", "earnedPoints", "possiblePoints",
      "totalQuestions", "correctAnswers", "achievedLevels", "failedTopicCourses",
    ]);
  });

  it("вызывающий собирает ровно восемь полей", () => {
    const src = read("server/scorm/template/app/render/resultsPage.js");
    const body = src.match(/Telemetry\.finish\(\{[\s\S]*?\n {2}\}\);/)?.[0];
    expect(body).toBeTruthy();
    expect(literalKeys(body!)).toEqual([
      "percent", "passed", "earnedPoints", "possiblePoints",
      "totalQuestions", "correct", "achievedLevels", "failedTopicCourses",
    ]);
  });

  it("маршрут финиша пишет ровно десять колонок и ни одной под разрез", () => {
    const src = read("server/routes/scorm-telemetry.ts");
    // Якорь по `finishedAt:` обязателен: в файле есть ещё два вызова
    // `updateScormAttempt(attempt.id, { … })`, и без якоря ленивый разбор уезжает в них.
    const body = src.match(/updateScormAttempt\(attempt\.id, \{\r?\n {6}finishedAt:[\s\S]*?\r?\n {4}\}\);/)?.[0];
    expect(body).toBeTruthy();
    expect(literalKeys(body!)).toEqual([
      "finishedAt", "lastActivityAt", "resultPercent", "resultPassed", "totalPoints",
      "maxPoints", "totalQuestions", "correctAnswers", "achievedLevelsJson",
      "failedTopicCoursesJson",
    ]);
  });
});
```

Команда: `npm test -- tests/breakdown-telemetry-silence.test.ts`
Ожидание: PASS сразу. Если регулярное выражение не поймало блок (`toBeTruthy` упал), править
надо ВЫРАЖЕНИЕ, а не список полей: список — это и есть предмет требования.

- [ ] **Шаг 2. Коммит**

```bash
git add tests/breakdown-telemetry-silence.test.ts
git commit -m "test(prd-50): страж на молчание телеметрии о разрезах"
```

---

## Task 8: долг Э1 — разрезы в пересчёте показателей аналитики SCORM

### Почему здесь, а не в Э2

План Э1 предлагал перенести этот пункт «к гейту Э2, когда решится, откуда аналитика берёт
разрезы». Перенос отклоняется по трём причинам.

1. **Это не отсутствующая возможность, а неверное число на экране, и породил его Э1.** До Э1
   `tag()` возвращал ноль ВЕЗДЕ, поэтому расхождения не было. Э1 оживил акцессор на обоих
   хостах — и с этого момента пакет считает показатель по-настоящему, а окно детализации
   телеметрийной попытки показывает по тому же показателю ноль. Оставить это до следующего
   этапа значит сознательно выкатить два разных ответа на один вопрос.
2. **Решать нечего: источник уже определён и уже используется.** Разрезы восстанавливаются из
   строк `scorm_answers` (`questionId`, `topicId`, `points`, `maxPoints`) плюс теги живого
   банка вопросов. Это ровно тот контракт, по которому этот же маршрут уже пересчитывает
   вклады шкал, и он там прямо задокументирован (`analytics/scorm.ts:83-85`: «Recomputed from
   the test's CURRENT config… may drift if the test changed after the attempt»). Никакой новой
   модели данных, никакого решения владельца.
3. **Э2 на это место не влияет.** Э2 приносит пороги ключей и гейт темы; откуда аналитика
   берёт сами записи, он не меняет. Ждать нечего.

Соседний шов `server/routes/analytics/attempts.ts:348` НЕ трогается: он пересчитывает результат
только как фолбэк для попыток, у которых значения не сохранены, то есть завершённых до PRD-50 —
у них разрезов нет ни в каком виде, и подавать туда нечего.

**Files:**

- Modify: `server/routes/analytics/scorm.ts` (строки 89-94 и 170-184)
- Test: `tests/breakdown-analytics-scorm.test.ts` (создать)

- [ ] **Шаг 1. Написать падающий тест**

`tests/breakdown-analytics-scorm.test.ts`:

```ts
/**
 * @module tests/breakdown-analytics-scorm
 * @description Долг Э1: пересчёт показателей для телеметрийной попытки не видел разрезов,
 * поэтому формула с `tag()` давала в аналитике ноль при том, что пакет посчитал настоящее
 * значение. Проверяется чистая часть — сборка элементов из строк телеметрии.
 */
import { describe, it, expect } from "vitest";
import { scormBreakdownItems } from "../server/routes/analytics/scorm";
import { computeBreakdowns } from "../shared/breakdown/compute";

const answer = (questionId: string, topicId: string | null, points: number, maxPoints: number) => ({
  questionId, topicId, points, maxPoints,
});

describe("scormBreakdownItems", () => {
  it("берёт теги живого банка и цену из строки телеметрии", () => {
    const items = scormBreakdownItems(
      [answer("q1", "law", 2, 3)],
      new Map([["q1", ["ПДн"]]]),
    );
    expect(items).toEqual([
      { sectionId: "law", axisKeys: { tag: ["ПДн"] }, earned: 2, possible: 3, answered: true },
    ]);
    expect(computeBreakdowns(items)[0]).toMatchObject({ key: "ПДн", percentPoints: (2 / 3) * 100 });
  });

  it("пропускает ответы без тега и без раздела", () => {
    expect(scormBreakdownItems([answer("q1", "law", 1, 1)], new Map())).toEqual([]);
    expect(scormBreakdownItems([answer("q1", null, 1, 1)], new Map([["q1", ["ПДн"]]]))).toEqual([]);
  });

  it("пропускает вопрос без возможных баллов (FR-02 отработает в движке)", () => {
    const items = scormBreakdownItems([answer("q1", "law", 0, 0)], new Map([["q1", ["ПДн"]]]));
    expect(computeBreakdowns(items)).toEqual([]);
  });
});
```

Команда: `npm test -- tests/breakdown-analytics-scorm.test.ts`
Ожидание: FAIL — `scormBreakdownItems` не экспортируется.

- [ ] **Шаг 2. Написать сборщик и подать разрезы в пересчёт**

В `server/routes/analytics/scorm.ts` добавить импорты:

```ts
import { computeBreakdowns } from "@shared/breakdown/compute";
import type { BreakdownItem } from "@shared/breakdown/types";
```

Перед объявлением роутов добавить чистую функцию:

```ts
/**
 * PRD-50: breakdown items of ONE telemetry attempt.
 *
 * Telemetry carries no tags and never will (FR-41), so the axis keys come from the
 * question bank as it stands TODAY, while the points come from the answer row as it was
 * DELIVERED. That split is not new: this route already recomputes scale contributions
 * against today's configuration and says so. Without it a `tag()` indicator read zero on
 * the analytics screen while the package had reported a real value for the same attempt.
 *
 * Exported for its own test: the route around it needs a DB, this does not.
 *
 * @param answers Telemetry answer rows of the attempt.
 * @param tagsByQuestion Live tags per question id.
 * @returns Items for {@link computeBreakdowns}; questions without a topic or a tag are skipped.
 */
export function scormBreakdownItems(
  answers: ReadonlyArray<{ questionId: string; topicId: string | null; points: number; maxPoints: number }>,
  tagsByQuestion: ReadonlyMap<string, string[] | null | undefined>,
): BreakdownItem[] {
  const items: BreakdownItem[] = [];
  for (const a of answers) {
    const tags = tagsByQuestion.get(a.questionId);
    if (!a.topicId || !Array.isArray(tags) || tags.length === 0) continue;
    items.push({
      sectionId: a.topicId,
      axisKeys: { tag: tags },
      earned: a.points,
      possible: a.maxPoints,
      // A telemetry row exists BECAUSE the learner answered: there is no «delivered but
      // untouched» row to tell apart here.
      answered: true,
    });
  }
  return items;
}
```

В обработчике `/scorm-attempts/:attemptId` после цикла, заполняющего `rawAnswers`
(строка 94), добавить:

```ts
    // PRD-50: живые теги выданных вопросов — единственный источник ключей для
    // телеметрийной попытки (см. `scormBreakdownItems`). Один запрос; пустой для
    // удалённых вопросов, и тогда разрезов просто нет.
    const answeredQuestions = answers.length
      ? await storage.getQuestionsByIds(answers.map((a) => a.questionId))
      : [];
    const tagsByQuestion = new Map(answeredQuestions.map((q) => [q.id, q.tags]));
    const breakdowns = computeBreakdowns(scormBreakdownItems(answers, tagsByQuestion));
```

и в `gradedBase` (строка 171) добавить последним полем:

```ts
      // PRD-50: без этого `tag()` в показателе давал бы здесь ноль, тогда как пакет на той
      // же попытке посчитал настоящее значение.
      breakdowns,
```

- [ ] **Шаг 3. Прогнать тесты и типы**

```bash
npm test -- tests/breakdown-analytics-scorm.test.ts
npm test -- tests/routes.analytics-scorm.coverage.test.ts tests/routes.scorm-telemetry-analytics.test.ts
npm run check
```

Ожидание: PASS. Соседние наборы аналитики не должны сдвинуться: в ответ маршрута новых полей
не добавляется, меняются только значения показателей у теста с формулой на `tag()`.

- [ ] **Шаг 4. Коммит**

```bash
git add server/routes/analytics/scorm.ts tests/breakdown-analytics-scorm.test.ts
git commit -m "fix(prd-50): аналитика SCORM пересчитывает показатели с разрезами"
```

---

## Риск выката: оживший `tag()` меняет числа и вердикты на живых тестах

Это не задача с кодом, а шаг процесса. Он обязателен ДО деплоя ветки и относится к Э1 в той же
мере, что и к этой доделке: `tag()` и `sectionById()` до Э1 подавались пустыми картами и молча
возвращали нули, а теперь возвращают настоящие значения. Значит:

- у показателя типа `number` меняется ЧИСЛО;
- у показателя с «Управляет статусом = success» меняется ВЕРДИКТ попытки: `controls_status`
  перезаписывает `overallPassed` на веб-хосте (`server/routes/attempts.ts:1546`) и
  `cmi.success_status` в пакете (`render/resultsPage.js:296-301`);
- прошлые попытки НЕ пересчитываются (значения сохранены в `result_json`), меняются только
  новые;
- уже выгруженные SCORM-пакеты продолжают считать по-старому, пока их не пересоберут, — то
  есть после деплоя веб и LMS какое-то время будут расходиться на одном и том же тесте.

### Шаг 1. Найти затронутые тесты

Три запроса; выполнять на ЧТЕНИЕ, на проде и на dev-БД (dev — копия прода):

```sql
-- 1. Рабочие версии тестов: где формула показателя вообще обращается к разрезу или разделу.
SELECT t.id AS test_id, t.title, t.status, rv.name, rv.label,
       rv.controls_status, rv.learner_visibility, rv.formula
  FROM result_variables rv
  JOIN tests t ON t.id = rv.test_id
 WHERE rv.formula ~ '(^|[^A-Za-z0-9_])(tag|sectionById)[[:space:]]*\('
 ORDER BY (rv.controls_status = 'success') DESC, t.title, rv.name;

-- 2. Опубликованные версии: выдача читает показатели из СНИМКА, а не из строк выше.
SELECT s.test_id, s.version, s.published_at,
       rv->>'name' AS name, rv->>'controlsStatus' AS controls_status, rv->>'formula' AS formula
  FROM test_snapshots s,
       LATERAL jsonb_array_elements(coalesce(s.content_json->'resultVariables', '[]'::jsonb)) rv
 WHERE rv->>'formula' ~ '(^|[^A-Za-z0-9_])(tag|sectionById)[[:space:]]*\('
 ORDER BY s.test_id, s.version DESC;

-- 3. Масштаб: сколько завершённых попыток уже накоплено на этих тестах.
SELECT t.id AS test_id, t.title,
       count(a.id) FILTER (WHERE a.finished_at IS NOT NULL) AS finished_attempts,
       bool_or(rv.controls_status = 'success') AS drives_verdict
  FROM tests t
  JOIN result_variables rv ON rv.test_id = t.id
  LEFT JOIN attempts a ON a.test_id = t.id
 WHERE rv.formula ~ '(^|[^A-Za-z0-9_])(tag|sectionById)[[:space:]]*\('
 GROUP BY t.id, t.title
 ORDER BY drives_verdict DESC, finished_attempts DESC;
```

### Шаг 2. Что делать с найденным

- **Пусто во всех трёх** — риска нет, деплой обычный. Результат приложить к отчёту о приёмке:
  «запросы выполнены, 0 строк» — это и есть доказательство.
- **Есть строки, но `drives_verdict = false`** — меняются только числа показателей.
  Владельцу отправляется список «тест — показатель — формула»; деплой возможен после его
  подтверждения, что новые значения и есть ожидаемые.
- **Есть строка с `controls_status = 'success'`** — деплой этого теста БЛОКИРУЕТСЯ до решения
  владельца. Такой показатель переписывает вердикт, и оживший акцессор может перевернуть его
  у всех будущих попыток. Варианты решения (выбирает владелец, не исполнитель): снять
  «Управляет статусом», переписать формулу под настоящие значения или отложить деплой теста.
- **Есть строки во 2-м запросе** — вдобавок к решению выше тест придётся ПЕРЕОПУБЛИКОВАТЬ:
  выдача читает показатели из снимка, и правка рабочей версии на уже опубликованный тест не
  подействует.
- **Тест выгружен в LMS** — после решения пересобрать и перезалить пакет, иначе веб и LMS
  будут расходиться. Список пакетов: `SELECT id, test_id, test_title FROM scorm_packages
  WHERE is_active AND test_id IN (…)`.

---

## Что этот этап сознательно НЕ закрывает

- **Полосы на карточке адаптивной темы** — обоснование в решении после задачи 4.
- **Поля вердикта строки (`passed`, `passClass`, `statusLabel`)** — наполнять нечем до Э2
  (спека §8.1, отметка «с Э2»).
- **Предупреждение о незнакомом ключе в валидаторе:** `ValidationRefs.tagKeys` по-прежнему
  никто не подаёт (долг Э1). Это UX редактора, а не выдача; относится к таблице «раздел ×
  ключ» из Э2, где ключи собираются в любом случае.
- **`tests/it/schema.sql` без колонки `breakdown_display_json`** — долг Э1, фикстура pglite
  протухла шире PRD-50 и чинится своим порядком.
- **Бюджет `suspend_data`** — измеряется в задаче 5 шаг 5, но не чинится: долг старше PRD-50.

## Что этот этап даёт следующим

- Э2 получает записи обеих областей на обоих хостах и в хранении — гейт по ключу считать не с
  чего было бы иначе.
- Э4 получает `attempts.result_json.breakdowns`: блок области теста печатается из них, без
  пересчёта.

---

## Проверка этапа

- [ ] `npm run check` — чисто
- [ ] целевые прогоны — PASS:

```bash
npm test -- shared/template/__tests__/result-context-breakdown.test.ts tests/report-context.test.ts
npm test -- tests/breakdown-persistence.test.ts tests/breakdown-adaptive.test.ts
npm test -- tests/breakdown-snapshot-freeze.test.ts tests/breakdown-telemetry-silence.test.ts
npm test -- tests/breakdown-analytics-scorm.test.ts tests/breakdown-formula-context.test.ts
npm test -- shared/breakdown tests/breakdown-aggregate.test.ts tests/breakdown-web-host.test.ts
```

- [ ] `npm run lint:md` — чисто
- [ ] `npm run scorm:previews` выполнен, изменились только два `preview.html`
- [ ] приёмка в браузере и на пакете проведена (задача 4 шаг 5, задача 5 шаг 5), снимки и
  измеренный размер `suspend_data` приложены к отчёту о приёмке
- [ ] запросы «риска выката» выполнены, их вывод приложен к отчёту, решение владельца по
  показателям с «Управляет статусом» получено
- [ ] полный прогон `npm test` — ТОЛЬКО по явному разрешению владельца
