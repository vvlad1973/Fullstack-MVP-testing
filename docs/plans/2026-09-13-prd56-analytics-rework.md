# PRD-56. Переработка аналитики — план работ

> **Для исполнителя:** задачи выполняются по одной, сверху вниз, в режиме TDD: сначала падающий
> тест, потом минимальная реализация, потом прогон, потом коммит. Шаги отмечены чекбоксами
> (`- [ ]`).

**Цель:** сделать из двух похожих экранов два инструмента — рабочее место оценщика (раздел
«Аналитика») и рабочее место автора (аналитика теста), — считающих по одному источнику
наблюдений.

**Спецификация:** [docs/specs/prd-56/analytics-rework.md](../specs/prd-56/analytics-rework.md).
Номера `FR-NN` ниже ссылаются на неё.

**Эскизы (согласованы 2026-09-13):**
[раздел «Аналитика»](../wireframes/prd56-analytics-section.html),
[аналитика теста](../wireframes/prd56-test-analytics.html).

**Технологии:** TypeScript, Express, Drizzle ORM, PostgreSQL, React 19, `@skillum/ui-kit`, Vitest.

## Что нужно знать до начала

- **Vitest запускается ТОЛЬКО через npm:** `npm test -- <путь>`. Прямой `npx vitest run` в этом
  проекте падает.
- **Полный прогон (`npm test` без пути) не запускать** без явного разрешения владельца: он
  занимает около восьми минут и забирает машину.
- **Приёмка в браузере обязательна** на обоих экранах, в светлой и тёмной теме, и сверяется с
  эскизами выше. Учётка для приёмки — `acceptance@local.test`.
- **UI собирается только из компонентов ui-kit.** Не хватает компонента или пропа — правится
  `vendor/ui-kit`, а не обходится локально.
- **Дев-БД общая для всех worktree.** Разрушительные миграции применяются руками и осознанно.

## Состояние кода на начало работ

Проверено по `main` 2026-09-13.

- Единого слоя наблюдений нет. Каждая ручка сама вызывает `storage.getAllAttempts()` либо
  `storage.getAllScormAttempts()` и нормализует записи по-своему:
  [general.ts:20](../../server/routes/analytics/general.ts#L20),
  [combined.ts:24](../../server/routes/analytics/combined.ts#L24),
  [attempts.ts:54](../../server/routes/analytics/attempts.ts#L54),
  [export.ts:52](../../server/routes/analytics/export.ts#L52),
  [test-details.ts:25](../../server/routes/analytics/test-details.ts#L25).
- Дефект раздела 1.1 спеки живёт в
  [test-details.ts:25](../../server/routes/analytics/test-details.ts#L25): страница теста читает
  ТОЛЬКО веб-попытки, поэтому на тесте, который проходят в LMS, её числа расходятся с разделом
  «Аналитика».
- Все ручки грузят таблицу целиком и фильтруют в памяти. На сегодняшних объёмах это работает, но
  ленивая подгрузка реестра (FR-01c) требует выборки с лимитом и смещением на стороне БД.
- `DataGrid` умеет страницы (`page` / `pageSize` / `onPageChange`), но не умеет догрузку при
  прокрутке — её предстоит добавить в ui-kit (FR-01c).

## Этапы

Порядок из раздела 9 спеки. Каждый этап — отдельный план с задачами; этот файл детализирует Э1,
планы Э2 - Э6 пишутся перед их началом, чтобы опираться на уже сделанное.

| Этап | Состав | Состояние |
| --- | --- | --- |
| Э1 | Единый источник наблюдений на обеих страницах (FR-33, FR-25) | детализирован ниже |
| Э2 | Реестр прохождений: `FilterBar`, состояние в адресе, экспорт по фильтру (FR-01 - FR-05) | план пишется перед началом |
| Э3 | Срезы, сводка по группам и очередь «требует внимания» (FR-06 - FR-12) | план пишется перед началом |
| Э4 | Аналитика теста: обзор, вопросы, переходы (FR-13 - FR-17b, FR-23) | план пишется перед началом |
| Э5 | Выдача и шкалы (FR-18 - FR-22, FR-19a, FR-21a, FR-21b) | план пишется перед началом |
| Э6 | Диаграммы там, где они уместны после всего этого (FR-28) | план пишется перед началом |

Две работы выходят за пределы экранов аналитики и планируются отдельно, поскольку трогают сборку
пакета и модель данных:

- **FR-19a, версия публикации в LMS.** Номер снимка запекается в пакет, сообщается телеметрией и
  полем, попадающим в отчёт LMS, и читается парсером выгрузок (PRD-54). Без этого разрез по
  версиям публикации слеп к тем прохождениям, ради которых пакет и собирали. Делается в Э5, но
  затрагивает `server/scorm/` и `lms-export-import.ts`, поэтому оценивается отдельно.
- **FR-17a, исключение вопроса из выдачи.** Новое поле в `test_question_scoring` и миграция;
  учитывается выдачей (`shared/draw`), проверкой выполнимости (`draw-feasibility.ts`) и
  публикацией снимка. Делается в Э4.

## Э1. Единый источник наблюдений

**Что делаем:** заводим один серверный слой, который отвечает на вопрос «какие прохождения есть по
этим условиям», и переводим на него ВСЕ ручки аналитики. Интерфейс при этом не меняется — меняются
числа на странице теста, потому что она начинает видеть прохождения из LMS.

**Архитектура:** новый модуль `server/services/analytics/observations.ts`. Тип `Observation` —
одна запись прохождения независимо от источника: участник (пользователь либо псевдоним PRD-54),
тест, версия публикации, вариант выдачи, дата, длительность, результат, исход, источник
(`web` / `telemetry` / `import`), группы участника. Функция `loadObservations(filter, scope)`
читает веб-попытки и строки телеметрии с импортом, приводит их к `Observation`, применяет область
видимости и возвращает отсортированный список с общим числом. Ручки перестают знать, откуда
берутся данные.

**Почему первым:** пока источники разные, любая новая величина считается по неполным данным, а
FR-25 объявляет расхождение чисел между экранами дефектом.

### Задача 1: тип наблюдения и нормализация

**Файлы:**

- Создать: `server/services/analytics/observations.ts`
- Создать: `server/services/analytics/__tests__/observations.test.ts`
- Читать: `server/routes/analytics/helpers.ts` (`attemptTestId`, `attemptParticipant`,
  `analyticsScope` — логика переносится, а не дублируется)

- [x] **Шаг 1: падающие тесты на нормализацию**

Проверяется: веб-попытка и строка телеметрии с одинаковым содержанием дают одинаковый
`Observation`; импортированная запись несёт `origin: "import"` и псевдоним вместо имени;
незавершённая попытка получает исход «не завершено», а не «не сдал»; у измерительного теста
результат в процентах отсутствует (`null`), а не равен нулю (FR-22); длительность берётся из
`latency_ms`, когда она есть, и `null`, когда не измерялась.

- [x] **Шаг 2: реализация `toObservation` для обоих источников**
- [x] **Шаг 3: `npm test -- server/services/analytics`**
- [x] **Шаг 4: коммит**

### Задача 2: выборка с условиями и областью видимости

**Файлы:**

- Изменить: `server/services/analytics/observations.ts`
- Изменить: `server/storage/attempts-repository.ts`, `server/storage/scorm-repository.ts`
  (выборка по условиям вместо чтения таблицы целиком)
- Тест: `tests/it/analytics-observations.it.test.ts` (pglite, `npm run test:it`)

- [x] **Шаг 1: падающие тесты на фильтры**

Проверяется: фильтры теста, группы, периода, источника и исхода; область видимости отсекает тесты
вне грантов (FR-35); лимит и смещение возвращают ту же выборку, что и целиком прочитанная,
страницами; общее число не зависит от лимита.

- [x] **Шаг 2: реализация выборки на стороне БД**
- [x] **Шаг 3: `npm run test:it`**
- [x] **Шаг 4: коммит**

### Задача 3: перевод ручек раздела «Аналитика»

> Порядок задач 3 и 4 поменян местами при исполнении: задача 4 даёт красный тест на FR-25 и
> страхует перевод остальных ручек. Выяснилось также, что слой обязан ходить через `IStorage`,
> а не в базу напрямую: маршрутные тесты мокают DAL, и прямой доступ к `db` ронял их в 500.
> Выборка живёт в `server/storage/analytics-repository.ts`.

**Файлы:**

- Изменить: `server/routes/analytics/combined.ts`, `general.ts`, `attempts.ts`
- Тест: `server/routes/analytics/__tests__/`

- [x] **Шаг 1: тесты на неизменность ответа**

Ответы ручек не меняются по форме — меняется только то, откуда взяты строки. Тесты фиксируют
текущий контракт до правки, чтобы перевод на общий слой не сдвинул поля.

- [x] **Шаг 2: перевод `/summary` на `loadObservations`**

`combined` и `combined-full` собирают ещё и СПИСОК прохождений со своей формой строки; их
перевод сливается с задачей Э2 (реестр), где список пересобирается целиком, и делается там,
чтобы не переписывать одно и то же дважды. Сводка переведена сейчас: именно она даёт плитки,
которые сравниваются со страницей теста (FR-25).

- [x] **Шаг 3: `npm test -- server/routes/analytics`**
- [ ] **Шаг 4: коммит**

### Задача 4: страница теста начинает видеть LMS

**Файлы:**

- Изменить: `server/routes/analytics/test-details.ts`
- Тест: `server/routes/analytics/__tests__/test-details-sources.test.ts`

- [x] **Шаг 1: падающий тест на дефект 1.1**

Тест на тесте, у которого есть и веб-попытка, и телеметрия, и импортированная запись: число
прохождений, доля сдавших и средний результат совпадают с тем, что отдаёт раздел «Аналитика» по
тому же тесту (FR-25).

- [x] **Шаг 2: перевод на `loadObservations`**
- [x] **Шаг 3: `npm test -- server/routes/analytics`**
- [x] **Шаг 4: коммит**

### Задача 5: экспорт на тех же данных

**Файлы:**

- Изменить: `server/routes/analytics/export.ts`
- Тест: `server/routes/analytics/__tests__/export-*.test.ts`

- [ ] **Шаг 1: падающий тест: выгрузка содержит строки из всех трёх источников**
- [ ] **Шаг 2: перевод на `loadObservations`**
- [ ] **Шаг 3: `npm test -- server/routes/analytics`**
- [ ] **Шаг 4: коммит**

### Задача 6: приёмка Э1

- [ ] **Шаг 1:** на dev-инсталляции завести тест с веб-попыткой, телеметрией и импортированной
  выгрузкой; убедиться, что обе страницы показывают одни и те же числа (FR-25).
- [ ] **Шаг 2:** убедиться, что у измерительного теста нигде не появилось «0 % верных» (FR-22).
- [ ] **Шаг 3:** убедиться, что пользователь без гранта на тест по-прежнему не видит его в
  агрегатах (FR-35).
- [ ] **Шаг 4:** запросить у владельца разрешение на полный прогон `npm test` перед закрытием
  этапа.

## Риски

- **Числа на странице теста изменятся** сразу после Э1 — это и есть починка дефекта, но владельца
  стоит предупредить до выката: у тестов, которые проходят в LMS, вырастут и объём выборки, и
  средние.
- **Выборка на стороне БД** меняет поведение при больших объёмах; интеграционные тесты на pglite
  обязаны покрывать лимит, смещение и сортировку, иначе ленивая подгрузка Э2 встанет на неверном
  фундаменте.
- **`analyticsScope`** сейчас строит множество доступных тестов в памяти; при переносе фильтрации
  в БД область видимости должна попасть в условие запроса, а не остаться постфильтром — иначе
  лимит будет отсчитываться до отсечения недоступного.
