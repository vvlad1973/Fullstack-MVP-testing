# Регулярное выражение (PRD-57, этап Э7): план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** дать текстовому правилу второй режим — регулярное выражение — вместе с бюджетом
времени, без которого его выпускать нельзя.

**Architecture:** сравнение выражением живёт в `shared/answer-check/regex.ts`; бюджет знает
общий модуль `budget.ts`, а убивает прогон каждый хост своим исполнителем. Движок оценки
остаётся СИНХРОННЫМ: сервер считает вердикты выражений предпроходом и передаёт их готовыми
в `checkRuleSet`.

**Tech Stack:** TypeScript, Node `worker_threads`, Web Worker, React 19, Vitest.

**Устройство согласовано:**
[docs/specs/2026-09-19-prd57-e7-regex-design.md](../specs/2026-09-19-prd57-e7-regex-design.md).
Требования: [PRD-57](../specs/prd-57/open-response-and-formatting.md) §6.2, §6.4.

**Порядок работ внутри этапа:** сначала бюджет и рантайм, потом переключатель в ящике.
Режим, выпущенный без бюджета, — это открытая дверь в обслуживание всех пользователей.

**Границы этапа.** Не делается: нечёткое сравнение, проба у пропусков (Э8), числовое
правило выражением (не делается никогда — FR-28f).

---

## Карта файлов

| Файл | Ответственность |
| --- | --- |
| `shared/answer-check/regex.ts` | компиляция и применение выражения |
| `shared/answer-check/budget.ts` | бюджет и замер одного прогона, общий для трёх хостов |
| `shared/answer-check/simplify.ts` | перевод выражения в обычное сравнение, когда он точен |
| `shared/answer-check/rules.ts` | третий довод `checkRuleSet` — готовые вердикты |
| `shared/scoring/aggregate.ts` | ответ с превышенным бюджетом — нейтральный, вне знаменателя |
| `server/services/regex-runner.ts` | долгоживущий рабочий поток со сторожем |
| `server/routes/attempts.ts` | предпроход перед оценкой |
| `server/scorm/template/app/scoring/*` | тот же предпроход в пакете, через `Worker` браузера |
| `client/src/features/questions/answer-rules/regex-bar.tsx` | панель вставки |
| `client/src/features/questions/answer-rules/answer-rules-block.tsx` | режим, замер, предложение замены |
| `config/*.config.jsonc`, `server/config.ts` | `answerCheckBudgetMs`, `answerCheckWarnMs` |

---

## Task 1: Сравнение выражением

- Create: `shared/answer-check/regex.ts`; Test: `tests/answer-check-regex.test.ts`
- [ ] **Step 1:** падающие тесты — флаг `i` работает, `g` не ставится (два вызова подряд
      дают один результат), невалидное выражение не совпадает и помечается, краевые пробелы
      ответа снимаются, нормализация §6.1 НЕ применяется.
- [ ] **Step 2:** реализация с кэшом скомпилированных выражений по строке.
- [ ] **Step 3:** `npm test -- tests/answer-check-regex.test.ts`

## Task 2: Готовые вердикты в наборе правил

- Modify: `shared/answer-check/rules.ts`; Test: `tests/answer-check-rules.test.ts`
- [ ] **Step 1:** падающие тесты — `checkRuleSet` с третьим доводом берёт готовый вердикт и
      НЕ исполняет выражение; `"budget"` в вердикте означает «не проверено», а не «неверно»;
      без довода правило-выражение считается на месте.
- [ ] **Step 2:** реализация: `RuleVerdicts = Array<boolean | "budget">`, `pending` в исходе.
- [ ] **Step 3:** тот же набор плюс `tests/answer-check-scoring.test.ts`.

## Task 3: Бюджет и его нарушение в агрегате

- Modify: `shared/answer-check/budget.ts` (новый), `shared/scoring/aggregate.ts`,
  `shared/scoring/engine.ts`; Test: `tests/answer-check-budget.test.ts`
- [ ] **Step 1:** падающие тесты — ответ, у которого правило вышло за бюджет, даёт
      нейтральный исход, не идёт ни в числитель, ни в знаменатель и не отнимает балла.
- [ ] **Step 2:** реализация.
- [ ] **Step 3:** `npm test -- tests/answer-check-budget.test.ts tests/answer-outcomes.test.ts`

## Task 4: Рабочий поток на сервере

- Create: `server/services/regex-runner.ts`; Modify: `server/routes/attempts.ts`;
  Test: `tests/regex-runner.test.ts`
- [ ] **Step 1:** падающие тесты — долгое выражение убивается по таймеру и отдаёт
      `"budget"`; следующий запрос обслуживается (поток поднялся заново); быстрый ответ
      возвращает вердикт; отсутствие правил-выражений не поднимает поток вовсе.
- [ ] **Step 2:** реализация: один поток на процесс, сторож с таймером, `terminate()`.
- [ ] **Step 3:** `npm test -- tests/regex-runner.test.ts`

## Task 5: Бюджет в пакете

- Modify: `server/scorm/template/app/scoring/*`, `server/scorm/builders/test-json.ts`;
  Test: `tests/scoring-engine-port.test.ts`, `tests/scorm-regex-budget.test.ts`
- [ ] **Step 1:** падающие тесты — бюджет запечён в `TEST_DATA`; при недоступном `Worker`
      сравнение идёт в основном потоке и не падает; при доступном — превышение даёт
      нейтральный исход.
- [ ] **Step 2:** реализация.
- [ ] **Step 3:** оба набора.

## Task 6: Панель вставки и режим в ящике

- Create: `client/src/features/questions/answer-rules/regex-bar.tsx`;
  Modify: `answer-rules-block.tsx`; Test: `client/src/features/questions/__tests__/`
- [ ] **Step 1:** падающие тесты — переключатель режима не заперт; тринадцать кнопок в
      порядке эскиза с подсказками; кнопка вставляет кусок В ПОЗИЦИЮ КУРСОРА, а не в конец;
      проба показывает замер; долгое выражение поднимает предупреждение.
- [ ] **Step 2:** реализация, разметка из эскиза дословно.
- [ ] **Step 3:** наборы ящика.

## Task 7: Предложение упростить

- Create: `shared/answer-check/simplify.ts`; Test: `tests/answer-check-simplify.test.ts`
- [ ] **Step 1:** падающие тесты — `^Федеральная служба по .* надзору$` переводится в
      `Федеральная служба по * надзору`; `.+`, `|`, `[абв]`, `{2,4}`, `\1` — отказ;
      экранированные знаки остаются буквальными.
- [ ] **Step 2:** реализация плюс кнопка замены в ящике.
- [ ] **Step 3:** `npm test -- tests/answer-check-simplify.test.ts`

## Task 8: Приёмка

- [ ] **Step 1:** `npx tsc --noEmit --incremental false` и затронутые наборы.
- [ ] **Step 2:** живой прогон: правило-выражение, панель вставки, замер на пробе, долгое
      выражение с заменой и без неё, прохождение теста, превышение бюджета на живом ответе.
- [ ] **Step 3:** пакет: `npm run scorm:template` и локальный плеер.
