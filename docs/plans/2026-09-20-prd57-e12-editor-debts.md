# Долги трека (PRD-57): панель вставки и предпросмотр — план

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** автор вставляет листинг и формулу кнопкой, а перед сохранением видит задание
глазами участника.

**Architecture:** предпросмотр рисуется ТЕМ ЖЕ компонентом, что экран прохождения; разметка
задания приходит с сервера, потому что подсветка и формула живут только там.

**Tech Stack:** TypeScript, React 19, `@skillum/ui-kit`, Vitest.

**Устройство согласовано:**
[docs/specs/2026-09-20-prd57-e12-editor-debts-design.md](../specs/2026-09-20-prd57-e12-editor-debts-design.md).
Требования: FR-09a, FR-24g.

**Границы.** Три режима ввода текста задания (§4.3) не делаются: их не делал ни один этап
трека, и это отдельная работа, а не долг.

---

## Task 1: Вставка разметки

- [x] **Step 1:** падающие тесты — вставка в позицию курсора, обёртывание выделенного,
      положение курсора после вставки, язык листинга в открывающей строке.
- [x] **Step 2:** `client/src/features/questions/insert-markup.ts` — чистые функции над
      текстом и положением курсора, без DOM.
- [x] **Step 3:** `npm test -- tests/insert-markup.test.ts`

## Task 2: Панель в ящике

- [x] **Step 1:** падающие тесты — три кнопки, меню языка, «Пропуск» только у своего типа.
- [x] **Step 2:** панель над полем текста задания в ящике вопроса.
- [x] **Step 3:** `npm test -- client/src/features/questions/__tests__`

## Task 3: Маршрут предпросмотра

- [x] **Step 1:** падающие тесты — отдаёт подсвеченный листинг и формулу картинкой,
      ничего не пишет, закрыт правом.
- [x] **Step 2:** `POST /api/questions/preview` поверх существующего `promptHtmlOf`.
- [x] **Step 3:** `npm test -- tests/routes.question-preview.test.ts`

## Task 4: Окно предпросмотра

- [x] **Step 1:** падающие тесты — окно открывается кнопкой подвала, рисует экран вопроса,
      ввод ничего не сохраняет.
- [x] **Step 2:** `question-preview-modal.tsx` на `TemplateQuestionScreen` + кнопка в подвале
      ящика.
- [x] **Step 3:** `npm test -- client/src/features/questions/__tests__`

## Task 5: Приёмка

- [x] **Step 1:** затронутые наборы целиком.
- [x] **Step 2:** живой прогон: кнопки вставляют разметку, предпросмотр показывает
      подсвеченный листинг, формулу и поля пропусков.

Приёмка на живом приложении, 9 из 9 (`e12-accept.mjs`): маршрут отдаёт подсвеченный листинг
и формулу картинкой, кнопки панели ставят разметку в позицию курсора, а окно предпросмотра
рисует задание сценой участника — с подсветкой и формулой на своих местах.
