# PRD-51, этап Э5: контракт формата шаблонов 2.0.0

> **Исполнителю:** ОБЯЗАТЕЛЬНЫЙ ПОДНАВЫК: вести работу по этому плану задача за задачей
> через `superpowers:subagent-driven-development` или `superpowers:executing-plans`.
> Шаги помечены чекбоксами (`- [ ]`) для отметки готовности.

**Цель:** внешний разработчик шаблона, читая только спецификацию и руководство, собирает
шаблон с документом отчёта из блоков — и понимает, почему его прежний шаблон продолжает
печататься по-старому.

**Подход:** контракт бампится ВТРОЁМ (спецификация, руководство, PDF в `docs/dist/`).
Версия — **2.0.0**: впервые за трек изменение НЕ обратно совместимо по СМЫСЛУ — вид
`report` перестал быть «страницей отчёта» и стал ОБОЛОЧКОЙ документа. Прежние шаблоны
работают (§5.4 спецификации PRD-51), но разработчик, читавший контракт 1.x, понимает
`kind: "report"` иначе, чем понимает его сегодняшнее ядро.

**Технологии:** markdown + markdownlint, `npm run docs:pdf`.

**Спецификация продукта:** `docs/specs/prd-51/report-document-blocks.md` (§3, §5, §8, FR-24).
**Что уже сделано:** Э1-Э4 — движок, эталон, редактор, «Сертификация». Э5 не меняет ни
строчки кода: это работа с документами.

---

## Почему MAJOR, а не MINOR

Обоснование пойдёт в раздел «История версий» спецификации, поэтому фиксируется здесь.

Формально старые шаблоны валидны и печатаются (§5.4): вид без объявленного состава остаётся
на своей цельной раскладке. Но три вещи изменились так, что читатель контракта 1.x сделает
неверный вывод:

1. **`kind: "report"` сменил смысл.** Был макет всей страницы отчёта; стал корень документа,
   в который движок вкладывает блоки прямыми детьми. Шаблон, завернувший блоки в свой
   контейнер, сломает постраничную раскладку — и об этом в контракте 1.x нет ни слова.
2. **Появился вид, которого не было.** `report.block` с обязательным ключом блока из
   ЗАКРЫТОГО реестра продукта. Закрытый перечень — это ограничение, а не расширение:
   одиннадцатый вид данных шаблон придумать не вправе.
3. **Запрет `placeholders[]` больше не общий.** У оболочки он остался, у блока `page` —
   снят. Правило «в отчёте нет содержимого, которое читают как страницу» перестало быть
   верным: авторская страница документа — ровно такое содержимое.

---

## Структура файлов

**Изменяются:**

| Файл | Что меняется |
| --- | --- |
| `docs/specs/spec-template-platform.md` | §8.4 переписан под документ из блоков; новые §8.4.7-8.4.9; §8.3.2 (сканирование надписей), §17.1 (валидация); версия 2.0.0 и запись в истории |
| `docs/guides/template-development.md` | §7.3a дополнен документом; новый раздел «Документ отчёта из блоков» с рецептом; версия 2.0.0 |
| `docs/dist/spec-template-platform.pdf` | Пересобран |
| `docs/dist/template-development.pdf` | Пересобран |

**Не трогается:** код, шаблоны, тесты. Э5 — документы.

---

## Task 1: Спецификация — вид `report.block`

**Files:**

- Modify: `docs/specs/spec-template-platform.md:1894-1936` (§8.4.1)

- [ ] **Шаг 1: Переписать врезку §8.4 про предмет раздела**

Заменить абзац «Отчёт — PDF-файл…» (строки 1885-1892) на:

```markdown
Отчёт — PDF-файл с итогами попытки, который обучающийся скачивает действием
`download-report` (флаг `result.nav.showReport` на экране итогов, `state.canDownloadReport`
на стартовом экране в кулдауне). Это не снимок экрана результатов, а самостоятельный
ДОКУМЕНТ со своей вёрсткой, и вёрстка эта принадлежит ШАБЛОНУ, а не ядру.

Документ собирается из БЛОКОВ: вид `report` (и `report.adaptive`) объявляет ОБОЛОЧКУ —
корень документа, — а разделы объявляются отдельными вариантами вида `report.block`.
Порядок и состав блоков в конкретном тесте задаёт автор теста; шаблон объявляет документ
по умолчанию (§8.4.7). Шаблон, блоков не объявивший, печатает свою цельную раскладку ровно
как раньше (§8.4.9).

Страницу рисует тот же браузерный рендерер, что и ученические экраны; конвейер экспорта
растеризует её и укладывает в A4. Ядро отвечает за данные, за сборку документа и за файл,
шаблон — за облик оболочки и каждого блока. Данные, из которых документ собирается, —
публичный контекст отчёта (§10.2).
```

- [ ] **Шаг 2: Дописать в §8.4.1 объявление оболочки**

После списка полей варианта (строки 1918-1924) вставить:

````markdown
С версии 2.0.0 `layoutFile` вида `report` — это макет ОБОЛОЧКИ, а не всей страницы.
Оболочка обязана быть ОДНИМ корневым узлом и не содержать разделов документа: движок
вкладывает блоки её ПРЯМЫМИ детьми, и всякий контейнер между корнем и блоком ломает
постраничную раскладку — она измеряет именно прямых детей корня.

Типичная оболочка — корень с классом состояния и полями подложки и логотипа:

```html
<div class="tb-report {{ report.verdictClass }}" style="{{#if report.values.backgroundImage}}background-image: url({{ report.values.backgroundImage }});{{/if}}">
{{#if report.values.logoImage}}<div class="tb-report__brand"><img src="{{ report.values.logoImage }}" alt=""></div>{{/if}}
</div>
```
````

- [ ] **Шаг 3: Добавить §8.4.7 — варианты блоков**

Вставить перед `#### 8.4.6` новый раздел (нумерация: 8.4.6 остаётся за разрывом листа,
новые идут после него — вставлять ПОСЛЕ §8.4.6, перед `## 9`):

```markdown
#### 8.4.7 Блоки документа: вид `report.block`

Раздел документа объявляется записью `contentTemplates[]` вида `report.block`:

| Поле | Обязательность | Смысл |
| --- | --- | --- |
| `block` | обязательно | Ключ блока из закрытого перечня ниже |
| `kinds[]` | необязательно | Виды отчёта, которым служит вариант; без него — оба |
| `layoutFile` | обязательно | Макет раздела |
| `isDefault` | ровно один на пару «блок + вид» | Умолчание блока |
| `placeholders[]` | только у блока `page` | Поля авторской страницы |
| `settings[]` | необязательно | Свойства раздела |

Перечень блоков ЗАКРЫТ продуктом: это то, что документ умеет рассказать о попытке. Шаблон
вправе решать, КАК блок выглядит, но не вправе придумать одиннадцатый вид данных.

| Ключ | Что печатает |
| --- | --- |
| `header` | Название документа, вердикт, слушатель, дата |
| `intro` | Вводный текст автора теста |
| `summary` | Сводка баллов |
| `topics` | Результаты по темам |
| `breakdown` | Сводный разрез по тесту |
| `scales` | Шкалы |
| `indicators` | Показатели |
| `recommendations` | Рекомендации |
| `courses` | Курсы |
| `events` | Мероприятия |
| `page` | АВТОРСКАЯ страница: заголовок и текст, которые пишет автор теста |
| `page-break` | Разрыв листа. Раскладки не имеет: это инструкция документу, а не раздел |

Блок `page` — единственный, которому разрешён `placeholders[]`: его содержимое пишет автор
теста, и типы полей те же, что у контентных страниц (§8.2.1). У оболочки `placeholders[]`
по-прежнему запрещён (§8.4.1): оболочка не несёт содержимого, она его вмещает.

Шаблон вправе объявить несколько вариантов одного блока — «темы карточками» и «темы
таблицей», «страница в одну колонку» и «в три». Автор теста выбирает вариант строкой
документа; вариант, которого шаблон больше не объявляет, деградирует к умолчанию СВОЕГО
блока, а не роняет блок целиком.

`kinds[]` нужен там, где блок печатается по-разному у обычного и адаптивного отчёта:
карточка темы говорит то долей верных ответов, то подтверждённым уровнем. Вариант без
`kinds[]` служит обоим видам — так объявляются блоки, у которых различий нет.
```

- [ ] **Шаг 4: Добавить §8.4.8 — документ по умолчанию**

````markdown
#### 8.4.8 Документ по умолчанию: `reportDocument`

Корневой ключ манифеста объявляет, из чего документ состоит, пока автор теста его не
трогал:

```json
"reportDocument": {
  "report": ["header", "intro", "page-break", "topics", "summary", "recommendations"],
  "report.adaptive": ["header", "intro", "topics", "recommendations"]
}
```

Правила:

- список — ключи блоков в порядке печати;
- блок с данными в списке НЕ повторяется: второй «Сводный разрез» напечатал бы одни и те же
  числа дважды;
- `page-break` повторяется свободно: документ из трёх листов ставит два разрыва;
- вид, для которого состав не объявлен, печатается цельной раскладкой (§8.4.9) — даже если
  соседний вид на блоки уже переведён;
- блок, объявленный в списке, но не имеющий ни одного варианта, — ошибка валидации (§17.1).

Что происходит со СТАРЫМ тестом при смене шаблона, описано в спецификации продукта
(PRD-51 §5.1) и шаблона не касается: строки теста не удаляются, неизвестные блоки
пропускаются, новые дописываются в конец выключенными.
````

- [ ] **Шаг 5: Добавить §8.4.9 — деградация**

```markdown
#### 8.4.9 Шаблон без блоков: цельная раскладка

Шаблон, не объявивший ни одного варианта `report.block` и ни одного состава в
`reportDocument`, печатает свою раскладку вида `report` целиком — ровно как до версии
2.0.0. Ничего делать не нужно: это не режим совместимости, который включают, а естественное
следствие правила «нет объявленного состава — нет документа из блоков».

Проверять надо именно ОБЪЯВЛЕНИЕ СОСТАВА, а не наличие вариантов блоков: шаблон,
переведённый наполовину, у непереведённого вида обязан печатать свой прежний макет, а не
пустой документ.
```

- [ ] **Шаг 6: Прогнать markdownlint**

```bash
npx markdownlint-cli2 "docs/specs/spec-template-platform.md"
```

Ожидание: `0 issues`.

- [ ] **Шаг 7: Коммит**

```bash
git add docs/specs/spec-template-platform.md
git commit -m "docs(prd-51): спецификация формата — документ отчёта из блоков"
```

---

## Task 2: Спецификация — надписи, валидация, версия

**Files:**

- Modify: `docs/specs/spec-template-platform.md` (§8.3.2, §17.1, шапка, история версий)

- [ ] **Шаг 1: Поправить правило сканирования надписей**

Найти в §8.3.2 фразу про сканирование макетов вариантов отчёта и заменить на:

```markdown
Перечень надписей, которые редактор предлагает для ОТЧЁТА, считается сканированием макетов
отчёта — И ОБОЛОЧКИ, И БЛОКОВ. С разбором отчёта на блоки надписи переехали в них:
зонтичный заголовок итогов живёт теперь в макете блока сводки, а не в оболочке. Сканер,
читающий одну оболочку, предлагал бы автору половину словаря.
```

- [ ] **Шаг 2: Дописать проверки в §17.1**

В перечень статических проверок объявления варианта добавить:

```markdown
- **`report.block` без `block`** — ошибка: ключ блока обязателен, иначе движку неизвестно,
  какой раздел печатает вариант.
- **Неизвестный ключ блока** — ошибка: перечень закрыт продуктом (§8.4.7).
- **Два умолчания на пару «блок + вид»** или НИ ОДНОГО — ошибка: движку не из чего выбрать
  раскладку, когда автор варианта не выбирал.
- **`placeholders[]` у блока, отличного от `page`** — ошибка: содержимое пишет автор только
  на авторской странице.
- **`reportDocument` с неизвестным блоком** — ошибка. Повтор `page-break` ошибкой НЕ
  является (§8.4.8).
- **Блок в `reportDocument` без единого варианта** — ошибка: документ по умолчанию не
  вправе ссылаться на раздел, которого шаблон не умеет напечатать.
```

- [ ] **Шаг 2а: Дописать §5 — корневые ключи манифеста**

В таблицу корневых ключей манифеста (§5) добавить строку:

```markdown
| `reportDocument` | нет | Документ отчёта по умолчанию, по видам (§8.4.8) |
```

- [ ] **Шаг 3: Поднять версию в шапке**

Заменить строки версии и даты:

```markdown
**Версия:** 2.0.0 (см. [«История версий»](#история-версий))  
**Дата актуализации:** 2026-08-16  
```

И дописать в перечень предмета спецификации (первый абзац после «Статус») упоминание
документа отчёта: «…и документ отчёта из блоков (`kind: "report.block"`, `reportDocument`,
§8.4.7-8.4.9)».

- [ ] **Шаг 4: Добавить запись в историю версий**

Первой строкой таблицы:

```markdown
| 2.0.0 | 2026-08-16 | Отчёт стал ДОКУМЕНТОМ ИЗ БЛОКОВ (PRD-51). **Оболочка:** вид `report` / `report.adaptive` объявляет корень документа, а не всю страницу; блоки движок вкладывает её прямыми детьми, и контейнер между корнем и блоком ломает постраничную раскладку (§8.4.1). **Блоки:** новый вид `report.block` с обязательным ключом блока из ЗАКРЫТОГО перечня продукта, необязательным `kinds[]` и умолчанием на пару «блок + вид» (§8.4.7). **Документ по умолчанию:** корневой ключ `reportDocument` по видам; `page-break` в нём вправе повторяться (§8.4.8). **Поля содержимого:** запрет `placeholders[]` снят с блока `page` — авторская страница документа несёт содержимое, которое пишет автор теста; у оболочки запрет остаётся. **Надписи:** перечень для отчёта сканируется и по макетам блоков (§8.3.2). Мажорная версия — при том, что прежние шаблоны печатаются как раньше (§8.4.9): смысл вида `report` изменился, и разработчик, читавший контракт 1.x, сделает о нём неверный вывод. |
```

- [ ] **Шаг 5: Прогнать markdownlint и закоммитить**

```bash
npx markdownlint-cli2 "docs/specs/spec-template-platform.md"
git add docs/specs/spec-template-platform.md
git commit -m "docs(prd-51): спецификация формата шаблонов 2.0.0"
```

---

## Task 3: Руководство разработчика

**Files:**

- Modify: `docs/guides/template-development.md:27` (версия), `:1440-1625` (§7.3a),
  новый раздел после §7.3a

- [ ] **Шаг 1: Поднять версию**

Строка 27:

```markdown
**Версия руководства:** 2.0.0 · **соответствует спецификации формата:** 2.0.0 ·
```

И в строке 37 заменить «версия 1.7.0» на «версия 2.0.0».

- [ ] **Шаг 2: Добавить раздел «Документ отчёта из блоков»**

Сразу после §7.3a (перед §7.4) вставить:

````markdown
### 7.3b Документ отчёта из блоков

Отчёт собирается не одним макетом, а списком БЛОКОВ. Вы объявляете оболочку и разделы,
автор теста собирает из них документ.

**Шаг 1. Оболочка.** Вид `report` теперь объявляет корень документа:

```html
<!-- layouts/report/shell.html -->
<div class="tb-report {{ report.verdictClass }}">
{{#if report.values.logoImage}}<div class="tb-report__brand"><img src="{{ report.values.logoImage }}" alt=""></div>{{/if}}
</div>
```

Внутрь ничего не кладите: блоки движок вложит сюда сам, ПРЯМЫМИ детьми. Свой контейнер
между корнем и блоками сломает разбивку на листы — она измеряет прямых детей корня.

**Шаг 2. Блоки.** На каждый раздел — своя запись и свой макет:

```json
{
  "key": "report.block.topics",
  "label": "Темы: карточки",
  "kind": "report.block",
  "block": "topics",
  "kinds": ["report"],
  "isDefault": true,
  "layoutFile": "layouts/report/topics.html"
}
```

Ключей блоков двенадцать и перечень закрыт (спецификация §8.4.7). Вариантов одного блока
может быть сколько угодно — автор выберет.

**Шаг 3. Авторская страница.** Блок `page` — единственный, у которого есть
`placeholders[]`: его текст пишет автор теста.

```json
{
  "key": "report.block.page.text",
  "label": "Страница: заголовок и текст",
  "kind": "report.block",
  "block": "page",
  "isDefault": true,
  "layoutFile": "layouts/report/page-text.html",
  "placeholders": [
    { "key": "title", "type": "text", "label": "Заголовок" },
    { "key": "body", "type": "richText", "label": "Текст" }
  ]
}
```

```html
<!-- layouts/report/page-text.html -->
<section class="tb-report__card">
  <div class="tb-report__page-title" data-placeholder="title"></div>
  <div class="tb-report__text" data-placeholder="body"></div>
</section>
```

**Шаг 4. Документ по умолчанию.** Корневым ключом манифеста:

```json
"reportDocument": {
  "report": ["header", "intro", "page-break", "topics", "summary"],
  "report.adaptive": ["header", "topics"]
}
```

`page-break` ставьте столько раз, сколько листов хотите открыть; блок с данными в списке
повторять нельзя.

**Частые ошибки**

| Симптом | Причина |
| --- | --- |
| Документ печатается одним куском, разрывы игнорируются | Оболочка завернула блоки в свой контейнер — раскладка измеряет ПРЯМЫХ детей корня |
| Шаблон не проходит загрузку: «блок указан дважды» | В `reportDocument` повторён блок с данными; повторять вправе только `page-break` |
| Блок печатается, но пустым | Макет блока читает путь, которого в контексте нет — сверьтесь с §7.3a |
| Автор не видит своего текста в отчёте | `placeholders[]` объявлен у блока, отличного от `page`: там он запрещён |
| Отчёт печатается по-старому, целиком | Не объявлен `reportDocument` для этого вида — так и работает деградация |
````

- [ ] **Шаг 3: Прогнать markdownlint и закоммитить**

```bash
npx markdownlint-cli2 "docs/guides/template-development.md"
git add docs/guides/template-development.md
git commit -m "docs(prd-51): руководство разработчика 2.0.0 — документ отчёта"
```

---

## Task 4: Пересборка PDF

**Files:**

- Modify: `docs/dist/spec-template-platform.pdf`, `docs/dist/template-development.pdf`

- [ ] **Шаг 1: Пересобрать**

```bash
npm run docs:pdf
```

- [ ] **Шаг 2: Проверить, что новые разделы попали в PDF**

```bash
python - <<'PY'
import re
for f in ['docs/dist/spec-template-platform.pdf', 'docs/dist/template-development.pdf']:
    d = open(f, 'rb').read()
    print(f, len(d), 'байт, страниц:', len(re.findall(rb'/Type\s*/Page[^s]', d)))
PY
```

Ожидание: оба файла ВЫРОСЛИ относительно предыдущей версии (`git diff --stat`), число
страниц увеличилось.

- [ ] **Шаг 3: Коммит**

```bash
git add docs/dist/
git commit -m "docs(prd-51): PDF контракта и руководства пересобраны под 2.0.0"
```

---

## Task 5: Сверка и закрытие трека

- [ ] **Шаг 1: Проверить, что версии сошлись втроём**

```bash
grep -n "Версия:\*\* 2.0.0" docs/specs/spec-template-platform.md
grep -n "Версия руководства:\*\* 2.0.0" docs/guides/template-development.md
ls -la docs/dist/*.pdf
```

Все три обязаны говорить 2.0.0. Разошедшаяся тройка — это контракт, о котором читатель
узнаёт разное в зависимости от того, какой файл открыл.

- [ ] **Шаг 2: Прогнать тесты документации, если они есть**

```bash
npm test -- tests/docs
```

Красный тест здесь означает, что документ ссылается на несуществующий раздел или пример
разошёлся с кодом.

- [ ] **Шаг 3: Записать отчёт приёмки**

`docs/reports/prd51-e5-acceptance.md`: какие разделы добавлены, какие версии подняты,
сколько страниц в PDF. Отдельным разделом — что из PRD-51 осталось незакрытым по треку в
целом (хвосты приёмок Э3 и Э4, issue #39).

- [ ] **Шаг 4: Коммит**

```bash
git add docs/reports/prd51-e5-acceptance.md
git commit -m "docs(prd-51): приёмка этапа Э5"
```
