# PRD-56, этап Э4. Аналитика теста: обзор, темы, вопросы — план работ

> **Для исполнителя:** задачи выполняются по одной, сверху вниз, в режиме TDD: сначала падающий
> тест, потом минимальная реализация, потом прогон, потом коммит. Шаги отмечены чекбоксами
> (`- [ ]`).

**Цель:** сделать страницу теста местом, где отвечают на вопрос «что чинить в этом тесте»:
распределение результатов, разрезы по темам, таблица заданий с признаками ревизии и состоянием
«исключён из выдачи». Список попыток с неё уходит — он уже есть в реестре.

**Спецификация:** [FR-13 - FR-17b, FR-23](../specs/prd-56/analytics-rework.md).
**Эскиз (согласован 2026-09-13):** [аналитика теста](../wireframes/prd56-test-analytics.html),
состояния `overview`, `overview-inside`, `items`, `items-review`, `items-exclude`,
`items-measurement`.
**Предыдущие этапы:** [Э1 — слой наблюдений](2026-09-13-prd56-analytics-rework.md),
[Э2 — реестр](2026-09-14-prd56-e2-registry.md), [Э3 — срезы](2026-09-14-prd56-e3-slices.md).

## Что нужно знать до начала

- **Vitest только через npm:** `npm test -- <путь>`; полный прогон — по разрешению владельца.
- **Дев-сервер не подхватывает серверные правки** — после них перезапуск руками.
- **Приёмка в браузере:** `node scripts/check/acceptance-shot.mjs` и `acceptance-click.mjs`
  (в Git Bash с `MSYS_NO_PATHCONV=1`). Учётка `acceptance@local.test`.
- **UI только из ui-kit**; не хватает пропа — правится `vendor/ui-kit`.
- **Миграция в Э4 разрушительной не является**, но dev-база общая: применяется вручную
  (`npm run db:migrate`), а `tests/it/schema.sql` пересобирается через `drizzle-kit export`.

## Состояние на начало работ

- Сводка теста (`GET /api/analytics/tests/:testId`) уже считается по слою наблюдений — то есть
  видит веб, телеметрию и импорт (Э1). А вот **темы и вопросы на той же ручке считаются только по
  `attempts`**: разбор ответов берётся из веб-попыток, и на тесте, который проходят в LMS, вкладки
  «Обзор» и «Вопросы» отвечают о другой популяции, чем плитки над ними. Это прямое нарушение
  FR-25, и оно чинится первой задачей.
- `scorm_answers` содержат всё нужное для сведения: вопрос, результат (`correct` / `incorrect` /
  `neutral` — PRD-54), баллы и `latency_ms` (PRD-55). Читаются они сегодня только поштучно
  (`getScormAnswersByAttempt`) — нужна выборка по тесту.
- Экспозиция и медиана времени по вопросу уже считаются (`getDeliveryCountsForTest`,
  `getLatencyStats`, PRD-55) и отдаются ручкой — их остаётся показать в новой таблице.
- Порог темы и исход «Пройдено / Не пройдено» считает движок PRD-50 (`shared/breakdown`):
  своего правила аналитика не заводит, иначе исход в отчёте участника и исход в аналитике
  разойдутся.
- Признака «вопрос исключён из выдачи» нет нигде: ни у `questions`, ни в
  `test_question_scoring`. Заводится этим этапом (FR-17a).
- Страница `client/src/pages/author/test-analytics.tsx` (1175 строк) несёт вкладки «Обзор»,
  «Попытки», «Вопросы», «Уровни» и модальное окно разбора; диаграммы — на recharts.

## Задачи

### Задача 1: ответы из LMS в статистике тем и вопросов

**Файлы:**

- Изменить: `server/storage/analytics-repository.ts` (выборка ответов по тесту),
  `server/routes/analytics/test-details.ts`
- Тест: `tests/it/analytics-answers.it.test.ts` (новый), `tests/routes.analytics-test-details.*`

Одна выборка ответов на прохождения теста — по обоим источникам сразу, как это сделано для самих
прохождений. Веб даёт ответы из `attempts.answersJson` с разбором по вопросам, LMS —
из `scorm_answers`; третье состояние `neutral` (измерительный ответ) не считается ни верным, ни
неверным, иначе опросник получает долю верных, которой у него нет.

- [x] **Шаг 1: тест на сведение** — вопрос, отвеченный в вебе и в LMS, даёт ОДНУ строку
      статистики с суммой наблюдений.
- [x] **Шаг 2: выборка и расчёт**
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**

### Задача 2: распределение результатов корзинами

**Файлы:**

- Изменить: `server/services/analytics/test-summary.ts` (или новый `score-buckets.ts`),
  `server/routes/analytics/test-details.ts`
- Тест: `server/services/analytics/__tests__/score-buckets.test.ts`

Корзины одной ширины 10 п.п., нижняя граница включается, верхняя — нет, последняя берёт 100
(FR-13a). Цвет задаёт проходной балл: ниже — красный, корзина с порогом внутри — жёлтая целиком,
выше — зелёный; порог рисуется вертикалью внутри своей корзины. У теста без оценки порога нет —
столбцы одноцветные. Сумма зелёных корзин обязана совпасть с долей сдавших: это проверяется
тестом, а не глазами.

- [x] **Шаг 1: тесты на корзины и их цвета**
- [x] **Шаг 2: расчёт**
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**

### Задача 3: разрезы по темам и подтемам

**Файлы:**

- Изменить: `server/routes/analytics/test-details.ts`, `shared/breakdown` (только чтение)
- Тест: `server/services/analytics/__tests__/topic-stats.test.ts`

Колонки называют единицу счёта (FR-14a): «Прошли тему» — доля ПРОХОЖДЕНИЙ, где участник взял
порог темы (исход движка PRD-50, посчитанный поштучно); «Доля верных ответов» — доля ОТВЕТОВ;
«Порог темы» — что требовалось; «В выборке» — сколько прохождений вообще содержали вопросы темы.
Колонки «вердикт» нет и не будет: среднее с порогом не сравнивают, потому что порог судит одну
попытку.

- [x] **Шаг 1: тест на две разные доли** (в теме из четырёх вопросов с порогом «три из четырёх»
      доля верных 50 % при нуле прошедших)
- [x] **Шаг 2: расчёт по темам и подтемам**
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**

### Задача 4: обзор теста на экране

**Файлы:**

- Изменить: `client/src/pages/author/test-analytics.tsx`
- Добавить: `client/src/features/analytics/test/score-distribution.tsx`,
  `topic-breakdown.tsx`, `pass-trend.tsx`
- Тест: рядом с компонентами

Плитки (прохождений, сдали, средний результат, время — медиана), распределение результатов,
таблица тем и подтем, динамика сдаваемости по месяцам. Диаграммы — компонентом `Charts` ui-kit,
цвет серии атрибутами (`stroke` / `fill`), как это уже выяснено на эскизе.

- [x] **Шаг 1: тесты на состав экрана**
- [x] **Шаг 2: компоненты и вкладка**
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**

Попутно в ui-kit добавлены два свойства `BarChart`: цвет отдельного столбика (`colors`) и
вертикаль-маркер (`xMarker`). Первого не хватало цветам корзин, второго — порогу, который не
кратен ширине корзины и потому проходит по столбику, а не по границе между ними.

### Задача 5: таблица заданий и вид «требуют ревизии»

**Файлы:**

- Добавить: `client/src/features/analytics/test/question-table.tsx`
- Изменить: `client/src/pages/author/test-analytics.tsx`, ручка теста
- Тест: рядом с компонентом + маршрутный

Колонки: вопрос (пиктограмма типа `QuestionTypeIcon`, тема · подтема), доля верных, доля
пропусков, экспозиция, медиана времени, авторская трудность. Сортировка по любому столбцу.
Вид «требуют ревизии» (FR-16) — предустановленный отбор по сошедшимся признакам: высокая
экспозиция при низкой доле верных либо аномально быстрые ответы. Признаки называются на экране,
иначе вид читается как приговор без основания. Из строки — переход в реестр к прохождениям, где
на вопросе ошиблись (FR-17).

- [x] **Шаг 1: тесты таблицы и вида**
- [x] **Шаг 2: реализация**
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**

Найденный долг ДС (не блокирует Э4): заголовок сортируемой колонки `DataGrid` — кликабельный
`div`, а не кнопка, поэтому сортировка недоступна с клавиатуры и не объявляется скринридером.

### Задача 6: состояние «исключён из выдачи»

**Файлы:**

- Изменить: `shared/schema.ts`, `drizzle/00NN_prd56_question_excluded.sql` (новая миграция),
  `server/storage/tests-repository.ts`, `shared/draw/`, `server/services/draw-feasibility.ts`,
  `server/services/test-snapshot.ts`
- Тест: `shared/draw/__tests__`, `server/__tests__/draw-feasibility*`, интеграционный на DAL

Признак живёт в `test_question_scoring`: негодный ЗДЕСЬ вопрос может быть годен в другом тесте,
а отключение в банке — операция владельца темы, а не читателя аналитики (FR-17a). Выдача
(`shared/draw`) исключённый вопрос не берёт, проверка выполнимости учитывает его при подсчёте
квот, снимок публикации фиксирует состояние на момент публикации. Собранные ответы и статистика
сохраняются — из аналитики вопрос не пропадает.

- [x] **Шаг 1: тесты выдачи и выполнимости**
- [x] **Шаг 2: миграция, схема, DAL** — `drizzle/0033_prd56_question_excluded.sql`, аддитивная
- [x] **Шаг 3: учёт в выдаче, выполнимости и снимке**
- [x] **Шаг 4: прогон тестов** — включая полный прогон `test:it` (152 теста)
- [x] **Шаг 5: коммит**

ГОЧА, найденная здесь: `npx drizzle-kit export --sql` пишет в начало вывода строку
`DATABASE_URL: …`. Попав в `tests/it/schema.sql`, она роняет ВСЕ интеграционные тесты
синтаксической ошибкой — при пересборке схемы первую строку надо убирать.

### Задача 7: исключение и возврат из аналитики

**Файлы:**

- Изменить: `server/routes/analytics/test-details.ts` (или новый `question-delivery.ts`),
  `client/src/features/analytics/test/question-table.tsx`
- Добавить: окно подтверждения
- Тест: маршрутный + компонентный

Окно называет последствия (FR-17b): сколько вопросов останется в теме при её квоте выдачи, и что
опубликованная версия не меняется — до повторной публикации и веб, и выгруженный пакет продолжают
выдавать вопрос по снимку (PRD-15). Если выдача становится невыполнимой, действие ЗАПРЕЩЕНО, а не
«выполнено с предупреждением». Возврат в выдачу подтверждения не требует: он ничего не отнимает.
Состояние метится перечёркнутым кругом с подсказкой перед названием, отдельным видом списка со
счётчиком и обратным действием — тегом не метится.

- [x] **Шаг 1: тесты подтверждения и запрета**
- [x] **Шаг 2: ручка и окно**
- [x] **Шаг 3: прогон тестов**
- [x] **Шаг 4: коммит**

ГОЧА: миграцию, написанную руками, `drizzle-kit migrate` не применяет — она не попадает в
`meta/_journal.json`, и команда рапортует об успехе. Создавать только через
`drizzle-kit generate`.

### Задача 8: снятие списка попыток

**Файлы:**

- Изменить: `client/src/pages/author/test-analytics.tsx`, ручка `.../attempts`
- Тест: существующие тесты страницы

Вкладка «Попытки» уходит, вместо неё — ссылка в реестр с фильтром по тесту (FR-23): один список
на продукт, а не два. Ручка `GET /api/analytics/tests/:testId/attempts` снимается, если её больше
никто не зовёт (проверить экспорт и отладочный плеер).

**Перед снятием — перенести ТРАЕКТОРИЮ адаптивного прохождения** в окно разбора реестра
(решение владельца 2026-09-14). Сегодня она показывается только в окне со страницы теста, и
вместе со вкладкой функция исчезла бы молча: в реестре её нет.

- [ ] **Шаг 1: тесты на новый состав экрана**
- [ ] **Шаг 2: снятие вкладки и ручки**
- [ ] **Шаг 3: прогон тестов**
- [ ] **Шаг 4: коммит**

### Задача 9: приёмка Э4

- [ ] **Шаг 1:** сверить экраны с эскизом в браузере, в светлой и тёмной теме.
- [ ] **Шаг 2:** проверить распределение на тесте, где порог не кратен ширине корзины.
- [ ] **Шаг 3:** проверить, что доля верных и «прошли тему» — разные числа на реальных данных.
- [ ] **Шаг 4:** исключить вопрос и убедиться, что выдача его не берёт, а снимок — берёт.
- [ ] **Шаг 5:** запросить разрешение на полный прогон `npm test`.

## Риски

- **Сведение ответов меняет числа вкладок** так же, как Э1 изменил числа плиток: у тестов,
  которые проходят в LMS, доля верных и разрезы по темам поедут. Это починка, но владельца стоит
  предупредить до выката.
- **Исключение вопроса трогает выдачу.** Ошибка здесь ломает не экран, а прохождение: участник
  получит меньше вопросов, чем задумано, либо выдача встанет. Поэтому задача 6 идёт ПЕРЕД
  экранным действием (задача 7) и закрывается тестами выдачи и выполнимости.
- **Опубликованная версия не меняется** — это не дефект, а свойство снимков (PRD-15). Если
  окно об этом не скажет, автор решит, что исключение не сработало, и полезет чинить.

## Отложено за пределы Э4

- **FR-22** (вкладка «Вопросы» измерительного теста: разброс ответов вместо доли верных) делается
  вместе с задачей 5, потому что это та же таблица; остальная часть раздела 4.4 — в Э5.
- **FR-06e** (разворот строки среза по темам на вкладке «Срезы») — после задачи 3: ей нужен тот
  же расчёт по темам, и делать его дважды незачем.
