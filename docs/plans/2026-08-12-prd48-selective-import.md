# PRD-48. Выборочный импорт теста: план реализации

> Для исполнителя: задачи идут по порядку, каждая заканчивается зелёными тестами и коммитом.
> Шаги помечены чек-боксами. Спека — `docs/specs/prd-48/test-transfer.md`, эскиз формы —
> `.playwright-mcp/wf-transfer-import.html`.

**Цель:** автор выбирает, какие части переносимого теста принять, и видит перечень удаляемого
до того, как что-либо запишется.

**Подход:** три шага (`inspect` -> `plan` -> `apply`) поверх готового формата `.tbtest`.
Вся логика решений — в ЧИСТОЙ функции `diff`, поэтому проверяется таблицей случаев без базы.
Сопоставление сущностей — по идентификаторам, что требует сначала перевести импорт на
сохранение исходных id.

**Стек:** TypeScript, Express, Drizzle, vitest (модульные) + pglite (`npm run test:it`),
дизайн-система `@skillum/ui-kit`.

---

## Что уже готово (не переделывать)

- Формат `.tbtest` и экспорт: `server/services/test-transfer/{package,export}.ts`,
  `GET /api/tests/:id/transfer`.
- Импорт с созданием НОВОГО теста: `import.ts`, `plan.ts` (перенумерация),
  `storage/test-transfer-repository.ts`, `POST /api/tests/transfer`.
- Гард полноты: `server/__tests__/test-transfer-completeness.test.ts`.
- Приёмка в чистую базу: `tests/it/test-transfer.it.test.ts`.

## Файловая структура

| Файл | Ответственность |
| --- | --- |
| `server/services/test-transfer/identity.ts` | СОЗДАТЬ. Решение «взять исходный id или выдать новый» |
| `server/services/test-transfer/inspect.ts` | СОЗДАТЬ. Разбор пакета + инвентаризация совпадений |
| `server/services/test-transfer/session-store.ts` | СОЗДАТЬ. Пакет между шагами: токен, TTL, LRU, владелец |
| `server/services/test-transfer/diff.ts` | СОЗДАТЬ. ЧИСТАЯ функция: пакет + снимок + опции -> операции |
| `server/services/test-transfer/apply.ts` | СОЗДАТЬ. Исполнение операций в транзакции |
| `server/services/test-transfer/import.ts` | ИЗМЕНИТЬ. Разделить: создание копии остаётся, появляется путь через diff |
| `server/storage/test-transfer-repository.ts` | ИЗМЕНИТЬ. Добавить update/delete к существующей записи |
| `server/routes/test-transfer.ts` | ИЗМЕНИТЬ. Три новых роута |
| `client/src/features/tests/transfer/` | СОЗДАТЬ. Форма импорта по эскизу |
| `server/utils/workbook-sheets.ts` | ИЗМЕНИТЬ. Лист «Исходы показателей» |

---

## Задача 1. Импорт сохраняет исходные идентификаторы — ВЫПОЛНЕНА 2026-08-12

Сделано: `services/test-transfer/identity.ts` (`makeIdentityResolver`, `buildIdMap`),
`plan.ts` принимает готовую `idMap` и экспортирует `collectSourceIds`, `import.ts` получил
`takenIds`. Тесты: `test-transfer-identity.test.ts` (6) + два сквозных случая в
`test-transfer-roundtrip.test.ts`. Всего по переносу 30 модульных тестов, зелёные.

Отступление от буквы плана: занятые идентификаторы приходят в импорт ДАННЫМИ
(`takenIds: Set<string>`), а не портом-функцией. Вызывающий, который знает приёмник, всё
равно читает его состояние для `diff` — второй, построчный опрос базы был бы и медленнее, и
вторым источником правды. Пока `takenIds` не передан, импорт работает как прежде (режим
копии), поэтому существующее поведение не сломано.

Ниже — исходный текст задачи.

Без неё «тот же вопрос» опознать нечем: сейчас `plan.ts` перенумеровывает всегда.

**Файлы:** создать `server/services/test-transfer/identity.ts`, изменить `plan.ts`,
`import.ts`; тест — `server/__tests__/test-transfer-identity.test.ts`.

- [ ] **Шаг 1.** Написать падающий тест: пакет с `id: "src-q"`, в приёмнике такого id нет —
  запись создаётся С ТЕМ ЖЕ id; тот же id занят ЧУЖОЙ сущностью — выдаётся новый и попадает
  в отчёт как `remapped`.

```ts
it("keeps the source id when it is free", async () => {
  const resolve = makeIdentityResolver({ taken: new Set() });
  expect(await resolve("src-q")).toEqual({ id: "src-q", remapped: false });
});

it("issues a new id when the source one belongs to something else", async () => {
  const resolve = makeIdentityResolver({ taken: new Set(["src-q"]) });
  const r = await resolve("src-q");
  expect(r.id).not.toBe("src-q");
  expect(r.remapped).toBe(true);
});
```

- [ ] **Шаг 2.** Прогнать: `npm test -- server/__tests__/test-transfer-identity.test.ts`.
  Ожидание: FAIL, модуля нет.
- [ ] **Шаг 3.** Реализовать `identity.ts`: `makeIdentityResolver({ taken })` возвращает
  функцию `(sourceId) => { id, remapped }`. Занятость проверяется по множеству, которое
  собирает вызывающий (одним запросом на таблицу, не по одной записи).
- [ ] **Шаг 4.** В `import.ts` завести режим: `mode: "copy"` (текущее поведение через
  `planImport`) и `mode: "preserve"` (новый, через `identity.ts`). Умолчание — `preserve`.
- [ ] **Шаг 5.** Прогнать `npm test -- server/__tests__/test-transfer-*.test.ts` и
  `npm run test:it -- tests/it/test-transfer.it.test.ts`. Обе группы зелёные.
- [ ] **Шаг 6.** Коммит: `feat(transfer): импорт сохраняет исходные идентификаторы`.

## Задача 2. Разбор пакета без записи (`inspect`) — ВЫПОЛНЕНА 2026-08-12

Сделано: `inspect.ts` (`readTransferPackage`, `inspectPackage`, `inspectParsedPackage`),
`session-store.ts` (токен, TTL 30 минут, LRU 50, владелец). Тест —
`test-transfer-inspect.test.ts` (14 случаев). Коммит `359e6d28`.

Отступление: чтение и проверка пакета переехали из `import.ts` в `inspect.ts`
(`readTransferPackage`), а `InvalidPackageError` теперь объявлен там же и реэкспортируется
из `import.ts`. Два ответа на вопрос «читается ли этот файл» разъехались бы; проверка версии
формата тоже сместилась раньше — до регистрации медиа, а не внутрь `planImport`.

Ниже — исходный текст задачи.

**Файлы:** создать `inspect.ts`, `session-store.ts`; тест — `test-transfer-inspect.test.ts`.
Образец хранилища — `server/scorm/debug-player/session-store.ts` (TTL 30 мин, LRU 50,
владелец `userId`); повторить его форму, не изобретая свою.

- [ ] **Шаг 1.** Тест: `inspectPackage(buffer)` возвращает заголовок теста, счётчики по пяти
  частям и `missingMedia`; для темы пакета сообщает состояние `new | existing | foreign`
  (последнее — когда тема есть, но прав управления нет).
- [ ] **Шаг 2.** Прогнать, убедиться в падении.
- [ ] **Шаг 3.** Реализовать `inspect.ts`. Права темы брать из
  `server/services/topic-access.ts`, НЕ выдумывать проверку заново.
- [ ] **Шаг 4.** Реализовать `session-store.ts` по образцу отладочного плеера.
- [ ] **Шаг 5.** Тесты зелёные, коммит: `feat(transfer): разбор пакета без записи`.

## Задача 3. Ядро решений (`diff`) — ВЫПОЛНЕНА 2026-08-12

Сделано: `diff.ts` (`diffTransfer`, `TargetSnapshot`, `TransferOptions`,
`TEST_FIELDS_BY_PART`), тест `test-transfer-diff.test.ts` (11 случаев), правило свободного
имени темы вынесено в `shared/topics/naming.freeTopicName`. Коммит `5d6de83b`.

Четыре решения приняты сверх буквы спеки. РАЗОБРАНЫ С ВЛАДЕЛЬЦЕМ 2026-08-12: пункт 2
изменён, остальные подтверждены.

1. **Строка теста пишется частями.** В `tests` пишут три части сразу, поэтому у операции
   появилось поле `omitFields`: снятая часть «Оценивание» удерживает
   `overallPassRuleJson`/`defaultQuestionPoints`, снятые «Итоги и оформление» —
   `introJson`/`designSettingsJson`/`reportSettingsJson`. Всё не перечисленное едет по
   умолчанию, поэтому новая колонка не теряется.
2. **Лишний раздел приёмника удаляется только при политике темы «полная замена»** (решение
   владельца 2026-08-12: «та же логика — upsert/replace»). Первоначально раздел удалялся при
   любой взятой части «Структура и вопросы», то есть без явно выбранной замены. Теперь
   действует общее правило: upsert не удаляет ничего, а единственная «замена», которой
   подчинён раздел, — политика ЕГО темы (своего переключателя у части быть не должно, §4
   спеки). Раздел темы, которой в пакете нет вовсе, сохраняется: молчание пакета не
   указание удалять.
3. **Запасное сопоставление по ручке автора.** По идентификатору — первично; если его нет
   (тест собран руками), шкала ищется по `key`, показатель по `name`, страница по `kind`
   (и только когда вид на приёмнике один — `sort_order` идентичностью не является), вклад по
   паре «вопрос+шкала», переопределение оценки по вопросу. Вопрос — только по
   идентификатору.
4. **Политика «создать новую тему» выдаёт новые идентификаторы и вопросам** этой темы:
   исходные заняты темой, которую автор решил не трогать.

У операции появилось поле `sourceId` (строка пакета, из которой берётся содержимое) — при
сопоставлении по ручке идентификаторы источника и приёмника разные, и `apply` без этого не
знает, что куда писать.

Ниже — исходный текст задачи.

**Файлы:** создать `diff.ts`; тест — `test-transfer-diff.test.ts`.

- [ ] **Шаг 1.** Тест-таблица. Обязательные случаи:
  - часть выключена -> операций по ней НЕТ вовсе;
  - `upsert` шкал: совпавшая по `key` обновляется, отсутствующая создаётся, лишняя НЕ
    трогается;
  - `replace` шкал: лишняя попадает в `delete`;
  - тема «обновить и долить»: вопрос, совпавший ПО ИДЕНТИФИКАТОРУ, обновляется; которого нет
    — создаётся; лишний в теме остаётся;
  - тема «полная замена»: лишний вопрос темы попадает в `delete`;
  - тема «создать новую»: операций по существующей теме НЕТ, создаётся новая с другим
    названием;
  - «Итоги и оформление» и «Медиа» не порождают `delete` НИКОГДА.
- [ ] **Шаг 2.** Прогнать, убедиться в падении.
- [ ] **Шаг 3.** Реализовать сигнатуру:

```ts
export type TransferOperation =
  | { kind: "create"; entity: EntityKind; id: string; title: string }
  | { kind: "update"; entity: EntityKind; id: string; title: string }
  | { kind: "delete"; entity: EntityKind; id: string; title: string; usedInAttempts: boolean };

export function diffTransfer(
  pkg: TestTransferPackage,
  target: TargetSnapshot,
  options: TransferOptions,
): TransferOperation[];
```

`title` заполняется человекочитаемо (имя шкалы, начало формулировки вопроса) — он идёт
прямо в перечень удаляемого на экране.

- [ ] **Шаг 4.** Тесты зелёные, коммит: `feat(transfer): ядро решений по частям и режимам`.

## Задача 4. Исполнение (`apply`) — ВЫПОЛНЕНА 2026-08-12

Сделано: `apply.ts` (`applyTransfer`, `TransferForbiddenError`), `target.ts`
(`buildTargetSnapshot`), `applyTransferBatch` в репозитории переноса, порт
`getAnsweredQuestionIds` в репозитории попыток и фасаде, медиа-проход вынесен из `import.ts`
в общую `registerPackageMedia`. Приёмка — `tests/it/test-transfer-apply.it.test.ts`
(5 случаев на pglite, включая откат и отказ по правам). Коммит `a7175f08`.

Отступления: план ПЕРЕСЧИТЫВАЕТСЯ внутри `applyTransfer`, а не принимается от клиента —
увиденное автором есть основание согласия, а не список записей; порт `canManageTopic`
обязателен, чтобы роут не мог забыть проверку; обновление снимает со строки `ownerId`,
`folderId` и `createdBy` — владелец, папка и аудит принадлежат приёмнику; снимок приёмника
живёт отдельным модулем, чтобы решения остались чистыми.

Ниже — исходный текст задачи.

**Файлы:** создать `apply.ts`, изменить `storage/test-transfer-repository.ts` (добавить
`updateImportedRows` и `deleteImportedRows` рядом с существующей записью); тест —
`tests/it/test-transfer-apply.it.test.ts`.

- [ ] **Шаг 1.** Интеграционный тест на pglite: применение списка операций даёт ожидаемое
  состояние; ошибка в середине откатывает ВСЁ (проверять по пустоте таблиц).
- [ ] **Шаг 2.** Тест прав: политика «долить» для темы без прав управления даёт отказ, а не
  молчаливое изменение чужой темы.
- [ ] **Шаг 3.** Прогнать, убедиться в падении.
- [ ] **Шаг 4.** Реализовать. Всё внутри ОДНОЙ `db.transaction`. Строки писать целиком, как
  в существующем `writeImportedTest`, — перечень колонок не заводить.
- [ ] **Шаг 5.** `npm run test:it -- tests/it/test-transfer-apply.it.test.ts` зелёный,
  коммит: `feat(transfer): применение плана одной транзакцией`.

## Задача 5. Роуты — ВЫПОЛНЕНА 2026-08-12

Сделано: `POST /api/tests/transfer/{inspect,plan,apply}` в `server/routes/test-transfer.ts`.
Коды: `422` нечитаемый пакет и чужая версия, `403` политика темы без прав, `404` неизвестный
токен, `410` истёкший (разница важна: во втором случае нужно просто загрузить файл заново).
Коммит `dcc44ead`.

Живая проверка на dev (порт 8097, тест-копия `f70eb383-…`): разбор дал 14 вопросов, 4 шкалы,
56 вкладов, тема — `existing`; план на самого себя — 86 обновлений и НОЛЬ удалений; политика
«создать новую тему» дала создание темы и 14 вопросов; применение прошло, токен после него
недействителен (`404`), повторная выгрузка совпала с исходной байт в байт (сверка манифестов
без `exportedAt`/`updatedAt`).

Ниже — исходный текст задачи.

**Файлы:** изменить `server/routes/test-transfer.ts`.

- [ ] **Шаг 1.** `POST /api/tests/transfer/inspect` (`tests.create`, `transferUploadSingle`)
  -> `{ token, summary }`.
- [ ] **Шаг 2.** `POST /api/tests/transfer/plan` (`tests.create`) -> операции по токену и
  опциям; `404` на неизвестный или истёкший токен.
- [ ] **Шаг 3.** `POST /api/tests/transfer/apply` (`tests.create`) -> отчёт; `403` при
  политике темы без прав; `422` на нечитаемый пакет и чужой формат.
- [ ] **Шаг 4.** Проверить живым HTTP: поднять `PORT=8097 npm run dev`, войти
  `acceptance@local.test`, прогнать три вызова curl подряд, убедиться в `201` и в отчёте.
  ВАЖНО: после проверки убить процесс — `TaskStop` оставляет дочерний node живым и порт
  занятым.
- [ ] **Шаг 5.** Коммит: `feat(transfer): роуты трёхшагового импорта`.

## Задача 6. Форма импорта — ВЫПОЛНЕНА 2026-08-12

Сделано: `client/src/features/tests/transfer/` (`transfer-api.ts`, `use-transfer-import.ts`,
`import-dialog.tsx`), тест `__tests__/import-dialog.test.tsx` (7 случаев), точки входа в
`tests-list.tsx` — кнопка «Импорт из пакета» в панели и пункт меню теста «Экспорт пакета
(.tbtest)». Коммит `ff8f0337`.

Приёмка в браузере на dev (порт 8097, учётка приёмки): снимки трёх шагов в
`.playwright-mcp/transfer-step{1,2-choose,2-bottom,3-done}.png`, эскиз для сверки —
`transfer-wireframe.png`. План на самого себя дал 0 создано / 86 обновлено / 0 удалено,
применение прошло, отчёт показан.

Два отступления от эскиза, оба ради компонентов вместо ручных классов:

1. В дизайн-систему добавлены `WizardSteps navOnly` (только полоса шагов — панель и кнопки
   мастера живут снаружи, в модальном окне) и `variant` у `CardKpi`. Эскиз рисовал это
   руками, компоненты так не умели.
2. Порядок «значение / подпись» в KPI-плитке взят из компонента (подпись сверху), а не из
   эскиза, где разметка была написана вручную в обратном порядке.

Ниже — исходный текст задачи.

**Файлы:** создать `client/src/features/tests/transfer/` (`import-dialog.tsx`,
`use-transfer-import.ts`, `transfer-api.ts`); пункт меню — в
`client/src/features/tests/list/tests-list.tsx` рядом с экспортом книги.

- [ ] **Шаг 1.** Свериться с эскизом `.playwright-mcp/wf-transfer-import.html`. Разметку
  брать ИЗ КОМПОНЕНТОВ ui-kit (`WizardSteps`, `Checkbox`, `SegmentedControl`, `Table`,
  `Chip`, `Banner`, `Card`, `Modal`), а не переписывать классы руками.
- [ ] **Шаг 2.** Шаг «Файл» на `FileUploader`, шаг «Что импортировать» по эскизу, шаг
  «Готово» — отчёт.
- [ ] **Шаг 3.** Пересчёт плана при КАЖДОМ изменении опции; пока идёт пересчёт — кнопка
  «Импортировать» недоступна, иначе применится план, которого автор не видел.
- [ ] **Шаг 4.** Подобрать варианты `Chip` осознанно: в тёмной теме нейтральный чип почти
  сливается с фоном, а `solid` кричит. Состояния «есть», «новая», «чужая» должны читаться
  с одного взгляда.
- [ ] **Шаг 5.** Приёмка в браузере: настоящий вход в dev, настоящая загрузка пакета,
  скриншот каждого шага, сверка с эскизом по каждой детали.
- [ ] **Шаг 6.** Коммит: `feat(transfer): форма выборочного импорта`.

## Задача 7. Лист «Исходы показателей» в книге — ВЫПОЛНЕНА 2026-08-12

Сделано: `OUTCOME_HEADERS`/`parseOutcomeRow`/`serializeOutcomeRows`/`mergeOutcomes` в
`workbook-sheets.ts`, проход импорта в `workbook-import.ts`, лист в выгрузке
(`routes/tests-workbook.ts`) и в шаблоне книги (`workbook-template.ts`: ролевой лист,
выпадающий список тональности, справка по колонкам). Тест `tests/workbook-outcomes.test.ts`
(6 случаев). Коммит `3e2698f7`.

Гоча: различить «колонки нет» и «ячейка пуста» по объекту строки НЕЛЬЗЯ — `sheetToObjects`
выбрасывает пустые ячейки. Наличие колонки читается из заголовков (`sheetHeaders`), как это
уже делает импорт вопросов.

Ниже — исходный текст задачи.

**Файлы:** изменить `server/utils/workbook-sheets.ts`, `server/routes/tests-workbook.ts`,
`server/services/workbook-import.ts`; тест — `tests/workbook-outcomes.test.ts`.

- [ ] **Шаг 1.** Тест на круг: показатель с исходами переживает выгрузку и загрузку без
  потерь; отсутствующий лист НЕ трогает сохранённые исходы; пустая ячейка «Текст» ОЧИЩАЕТ
  текст исхода.
- [ ] **Шаг 2.** Прогнать, убедиться в падении.
- [ ] **Шаг 3.** Заголовки: `["Показатель", "Код", "Метка", "Текст", "Тональность"]`.
  Правило то же, что у «Шкал»: колонка есть — книга задаёт поле целиком, колонки нет — не
  трогает (`mergeScaleConfig` как образец). Обратная связь исхода (вложения, ссылки) в книгу
  НЕ идёт — её несёт пакет.
- [ ] **Шаг 4.** Тесты зелёные, коммит: `feat(workbook): лист «Исходы показателей»`.

## Задача 8. Закрытие долгов — ВЫПОЛНЕНА 2026-08-12

Сделано целиком: скрипт расшивки удалён; спека и оба плана приведены в соответствие с кодом;
полный `npm run test:it` прогнан по разрешению владельца — 16 файлов, 72 теста, зелёные;
тест-копия `f70eb383-…` и тема «… (импорт 2)» с 14 вопросами удалены из dev.

Гоча уборки: API тестов в этот момент отдавал `500` — другая сессия добавила в
`shared/schema.ts` колонку `pass_decision_policy`, которой в dev-базе ещё нет. Удаление
выполнено SQL напрямую, повторяя порядок из `TestsRepository.deleteTest`; зависимости темы
проверены заранее (ни разделов, ни страниц, ни грантов, ни вкладов извне).

Ниже — исходный текст задачи.

- [ ] **Шаг 1.** Удалить `scripts/db/transfer-test-settings.ts` — его работу выполняет
  штатный импорт.
- [ ] **Шаг 2.** Прогнать полный `npm run test:it` (ТОЛЬКО с разрешения владельца:
  `tests/it/schema.sql` был регенерирован, затронуты все интеграционные тесты).
- [ ] **Шаг 3.** Удалить из dev тест-копию `f70eb383-28fe-4f51-abff-4bc4eb2013ac` и тему
  «… (импорт 2)», оставшиеся от приёмки среза B.
- [ ] **Шаг 4.** Обновить `docs/specs/prd-48/test-transfer.md` (раздел 9) и
  `docs/plans/2026-08-11-test-transfer-without-losses.md`.
- [ ] **Шаг 5.** Коммит: `chore(transfer): закрытие долгов PRD-48`.

---

## Риски

1. **Сопоставление по id ломается, если тест на приёмнике собран вручную, а не импортом.**
   Тогда идентификаторы не совпадут ни с чем и всё уедет в «создать». Форма обязана показать
   это честно: 32 создания вместо 32 обновлений — видимый сигнал, что выбран не тот тест.
2. **Удаление вопроса с историей попыток.** `diff` помечает такие операции
   (`usedInAttempts`), форма показывает отдельно. Запрета нет — решение за автором.
3. **Пересчёт плана на каждый чих.** Пакет уже разобран и лежит в памяти по токену, так что
   пересчёт — чистая функция над снимком; в базу на этом шаге не ходить.
4. **Права темы проверять на сервере.** Спрятать вариант в интерфейсе недостаточно: `apply`
   обязан отвечать `403` независимо от того, что прислал клиент.
