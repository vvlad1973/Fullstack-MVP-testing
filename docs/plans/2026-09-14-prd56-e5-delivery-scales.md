# PRD-56, этап Э5. Выдача и шкалы — план работ

> **Для исполнителя:** задачи выполняются по одной, сверху вниз, в режиме TDD: сначала падающий
> тест, потом минимальная реализация, потом прогон, потом коммит. Шаги отмечены чекбоксами
> (`- [ ]`).

**Цель:** ответить на экране теста на два вопроса, на которые он сегодня не отвечает: одинаково ли
оценивают разные варианты выдачи и не смешаны ли в одном среднем прохождения до и после правки
теста, — и показать измерительному тесту профиль по шкалам. Для этого версия публикации, вариант
выдачи и значения шкал впервые проносятся из пакета SCORM в LMS и обратно.

**Спецификация:** [FR-18 - FR-22, FR-19a](../specs/prd-56/analytics-rework.md).
**Эскиз (согласован 2026-09-13):** [аналитика теста](../wireframes/prd56-test-analytics.html),
состояния `delivery` и `scales`.
**Предыдущие этапы:** [Э1 — слой наблюдений](2026-09-13-prd56-analytics-rework.md),
[Э2 — реестр](2026-09-14-prd56-e2-registry.md), [Э3 — срезы](2026-09-14-prd56-e3-slices.md),
[Э4 — аналитика теста](2026-09-14-prd56-e4-test-analytics.md).

## Что нужно знать до начала

- **Vitest только через npm:** `npm test -- <путь>`; полный прогон — по разрешению владельца.
- **Дев-сервер не подхватывает серверные правки** — после них перезапуск руками.
- **Приёмка в браузере:** `node scripts/check/acceptance-shot.mjs` и `acceptance-click.mjs`
  (в Git Bash с `MSYS_NO_PATHCONV=1`). Учётка `acceptance@local.test`.
- **UI только из ui-kit**; не хватает пропа — правится `vendor/ui-kit`.
- **Миграции создаются ТОЛЬКО `drizzle-kit generate`.** Рукописный файл `migrate` не применяет:
  он идёт по `drizzle/meta/_journal.json` и при этом рапортует об успехе. Миграции Э5 аддитивные,
  но дев-база общая — применяются вручную (`npm run db:migrate`), после чего `tests/it/schema.sql`
  пересобирается через `drizzle-kit export` С УДАЛЕНИЕМ первой строки `DATABASE_URL: …`.
- **Правки рантайма пакета проверяются собранным пакетом,** а не чтением кода: `npm run
  scorm:sample` / `scorm:template` и локальный плеер `npm run scorm:player`. Живой стенд WebTutor
  доступен с 2026-08-06 — механики LMS принимаются на цели.
- **ГОЧА рантайма:** дубли функций в `server/scorm/assets/app.js` перекрывают модули
  `template/app/*`. Добавив функцию в `resultsPage.js`, убедитесь, что в собранном пакете
  вызывается именно она.

## Состояние на начало работ

- **Версия публикации есть только у веб-попытки** (`attempts.snapshot_id`). В пакет она не
  бакается, телеметрией не сообщается, в выгрузке LMS её нет. Разрез по версиям (FR-19) поэтому
  слеп ровно к тем прохождениям, ради которых пакет и собирали. Он же временно снят с продукта
  вместе с ручкой списка попыток (Э4, T-20) и восстанавливается здесь.
- **Пин варианта в аналитике не работает вовсе.** `Observation.formId` читает
  `variantJson.formId` — поля ВЕРХНЕГО уровня, которого в `testVariantSchema` нет: пин лежит
  ПОСЕКЦИОННО (`variantJson.sections[].formId`, PRD-17 BR-12). Значит и ось «вариант» среза из Э3
  всегда отвечает «Без варианта». Это чинится задачей 5, до неё таблица вариантов строиться не
  может.
- **Живая телеметрия шкал не сообщает.** `Telemetry.finish` шлёт проценты, баллы и уровни;
  `scorm_attempts.scales_json` / `variables_json` заполняет ТОЛЬКО импорт выгрузки (PRD-54).
  Решение владельца 2026-09-14: тем же каналом, что и версию, пронести и вариант, и шкалы —
  иначе профиль по шкалам видит опросник, пройденный в вебе и загруженный книгой, но не тот,
  что прошли в LMS.
- **Служебный блок выгрузки уже существует и работает:** `meta_response_format`
  (`shared/lms-export/response-codec.ts` + `buildResponseFormatInteraction` в `resultsPage.js`),
  а парсер пропускает любые `meta_*` мимо «неопознанных колонок». Новые блоки делаются по этому
  образцу — изобретать канал не нужно.
- **Экспозиция и время уже считаются** (`getDeliveryCountsForTest`, `getDeliveryCounts`,
  `getLatencyStats`, PRD-55) и отдаются ручкой теста. Профилю банка (FR-20) нужен ещё состав
  банка темы и знаменатель — попытки за окно, он уже отдаётся как `exposureAttempts`.
- **Значения шкал прохождения лежат в двух разных формах:** веб — `attempts.result_json
  .scaleResults` (`{ключ: {raw, label, level}}`), LMS — `scorm_attempts.scales_json`
  (`{ключ: число}`). Уровень по значению считается из полос самой шкалы
  (`parseScaleInterpretation` + `findBand`), а не берётся из записи: импорт подпись уровня не
  хранит вовсе.
- **Страница теста** (`client/src/pages/author/test-analytics.tsx`, 722 строки) несёт вкладки
  «Обзор», «Вопросы» и — у адаптивного теста — «Уровни». По эскизу вкладок четыре: «Обзор»,
  «Вопросы», «Выдача», «Шкалы», а «Уровни» переезжают внутрь «Выдачи».
- **FR-22 уже закрыт в Э4:** у измерительного теста вкладка «Вопросы» печатает разброс ответов, а
  не долю верных — это та же таблица, и делать её дважды было незачем. В Э5 из раздела 4.4
  спецификации остаются FR-21, FR-21a и FR-21b.
- **`ProgressStacked` с легендой в ui-kit уже есть** (`vendor/ui-kit/src/components/
  ProgressBar.tsx`) — ровно то, чем нарисовано распределение уровней в эскизе. Правок ДС здесь
  не ожидается.

## Задачи

### Задача 1: версия публикации бакается в пакет

**Файлы:**

- Изменить: `server/services/test-snapshot.ts` (`exportSourceForTest`),
  `server/scorm/build-export-data.ts`, `server/scorm/builders/test-json.ts`
- Тест: `tests/scorm-build-export-data.test.ts`, `tests/scorm-builders.test.ts`

Первое звено FR-19a. `exportSourceForTest` уже выбирает снимок публикации, но возвращает только
источник данных — номер снимка при этом теряется. Он начинает возвращать и сам снимок, ассемблер
кладёт его номер в `ExportData.publicationVersion`, а `buildTestJson` печатает его в `TEST_DATA`.

Бакается НОМЕР версии, а не идентификатор снимка: номер уникален внутри теста
(`test_snapshots_test_version_idx`), читается человеком в отчёте LMS и не раскрывает внутренних
ключей. Черновик и отладочный прогон (`source: "debug"`) версии не имеют — ключ тогда НЕ
добавляется вовсе, и пакет такого теста остаётся байт в байт прежним.

- [x] **Шаг 1: тест бейка** — у опубликованного теста `TEST_DATA` несёт `publicationVersion`
      номером активного снимка; у черновика и у отладочной сборки ключа нет.
- [x] **Шаг 2: проброс снимка и печать ключа**
- [x] **Шаг 3: прогон тестов** — `npm test -- tests/scorm-build-export-data.test.ts
      tests/scorm-builders.test.ts` (100 тестов), плюс `scorm-export` и `scorm-debug-player-assets`
      (25) и `npm run check`.
- [x] **Шаг 4: коммит** — `16b83c5f`

### Задача 2: пакет сообщает версию, вариант и шкалы

**Файлы:**

- Добавить: `shared/lms-export/meta.ts` (идентификаторы служебных блоков и кодирование варианта)
- Изменить: `server/scorm/template/app/telemetry/telemetry.js`,
  `server/scorm/template/app/render/resultsPage.js`
- Тест: `shared/lms-export/__tests__/meta.test.ts`, `tests/scorm-meta-blocks.test.ts` (новый,
  зеркало литералов рантайма и констант ядра — по образцу `tests/scorm-latency`)

Второе звено FR-19a, и самое рискованное: ошибка здесь ломает не экран, а прохождение в LMS.

В LMS уезжают ДВА новых служебных взаимодействия рядом с уже существующим
`meta_response_format`: `meta_test_version` (номер версии публикации строкой) и `meta_variant`
(идентификаторы выданных вариантов, склеенные `;`). Оба `result: 'neutral'` — правильного ответа
у служебного блока нет. Если `TEST_DATA.publicationVersion` в пакете нет (пакет собран раньше или
это черновик), блок НЕ добавляется: пустая ячейка и отсутствующая колонка одинаково означают
«версия не сообщена», и это честнее выдуманного номера.

Тем же движением телеметрия начинает сообщать то, что и так знает: `start` — `publicationVersion`
и `deliveredForms` (карта «тема → вариант» из `state.variant.sections`, рядом с уже уезжающим
`deliveredQuestionIds`), `finish` — `scales` и `variables` из `results.scaleComputation.values` и
`results.resultComputation`. Формы значений выбраны так, чтобы совпасть с тем, что уже пишет
импорт: шкалы — `{ключ: число}`, показатели — `{имя: строка}`. Подпись уровня не шлётся: её
считает сервер по полосам шкалы, иначе два источника дадут разные уровни на одном значении.

Идентификаторы блоков и кодирование варианта живут в `shared/lms-export/meta.ts`, потому что их
читает парсер выгрузки (задача 4). Рантайм пакета — обычный ES5 без импортов, поэтому в нём
остаются литералы, а совпадение литерала и константы стережёт зеркальный тест — так же, как
устроено измерение времени.

- [x] **Шаг 1: тесты ядра и зеркала** — `encodeVariant`/`decodeVariant` (пустой список, один
      вариант, несколько), совпадение литералов `resultsPage.js` с константами.
- [x] **Шаг 2: `shared/lms-export/meta.ts`**
- [x] **Шаг 3: служебные блоки в обоих путях `resultsPage.js`** — адаптивном
      (`finishScormAdaptive`) и обычном (`finishScormLmsOnly`).
- [x] **Шаг 4: новые поля телеметрии** в `start` и `finish`.
- [x] **Шаг 5: сборка пакета и проверка** — `npm run scorm:sample`: в собранном `app.js` есть
      определение `buildRunMetaInteractions` и оба вызова, двойника в `assets/app.js` нет.
      Телеметрия в образец не попадает (собирается с `telemetry-disabled.js`), поэтому её
      нагрузка проверена функционально — модуль исполняется поверх поддельных LMS, сети и
      подписи, и проверяется ОТПРАВЛЕННОЕ тело.
- [x] **Шаг 6: прогон тестов** — новые (13) плюс соседние наборы телеметрии, экспозиции и
      экспорта (96) и `npm run check`. Гард состава телеметрии PRD-50 FR-41 обновлён: он
      стережёт МОЛЧАЛИВОЕ расширение, а это санкционировано решением владельца.
- [x] **Шаг 7: коммит** — `2fed621a`

### Задача 3: сервер принимает версию, вариант и шкалы телеметрии

**Файлы:**

- Изменить: `shared/schema.ts` (`scormAttempts`), `server/routes/scorm-telemetry.ts`,
  `server/storage.ts` + `server/storage/scorm-repository.ts`,
  `server/storage/tests-repository.ts` (`getSnapshotByVersion`)
- Добавить: `drizzle/0034_prd56_lms_version_variant.sql` (через `drizzle-kit generate`)
- Тест: `tests/routes.scorm-telemetry-analytics.test.ts`, `tests/it/lms-version-variant.it.test.ts`
  (новый)

Строка прохождения из LMS получает две колонки: `snapshot_id` (версия публикации, как у
веб-попытки) и `forms_json` (карта «тема → вариант»). Обе необязательные — у прохождений,
собранных пакетами прошлых сборок, их не будет никогда.

Номер версии превращается в снимок запросом по паре (тест, версия): именно поэтому в пакет
бакается номер, а не идентификатор. Снимка с таким номером может не оказаться — тест удалён и
заведён заново, снимок подчищен `pruneSnapshots`. Тогда `snapshot_id` остаётся пустым, а
прохождение попадает в строку «Версия не указана»; приписывать его текущей версии запрещено —
именно так разрез и начинает врать.

Шкалы и показатели пишутся в уже существующие `scales_json` / `variables_json` на `finish`: своих
колонок для телеметрии не заводится, иначе у одного смысла окажется два хранилища.

- [x] **Шаг 1: тесты маршрута** — `start` с версией пишет снимок; `start` с неизвестной версией
      пишет `null` и не падает; `finish` со шкалами заполняет `scales_json`; запрос без новых
      полей ведёт себя ровно как раньше.
- [x] **Шаг 2: миграция и схема** — `drizzle/0034_prd56_lms_version_variant.sql`, аддитивная,
      применена на dev.
- [x] **Шаг 3: DAL** — `getSnapshotByVersion` в репозитории тестов и в фасаде `IStorage`.
- [x] **Шаг 4: маршрут телеметрии**
- [x] **Шаг 5: пересборка `tests/it/schema.sql`** — разница ровно в двух колонках.
- [x] **Шаг 6: прогон тестов** — 10 новых маршрутных + 5 интеграционных, полный
      интеграционный набор 161 тест, `npm run check` чист. Гард состава телеметрии обновлён и
      на маршруте (двенадцать колонок).
- [x] **Шаг 7: коммит** — `5b88f93d`

### Задача 4: парсер выгрузки читает версию и вариант

**Файлы:**

- Изменить: `shared/lms-export/parse.ts`, `server/services/lms-export-import.ts`
- Тест: `shared/lms-export/__tests__/parse.test.ts`,
  `server/services/__tests__/lms-export-import.test.ts`, `tests/it/lms-import.it.test.ts`

Третье звено FR-19a: то, чего нет в выгрузке, для импорта не существует — теперь оно там есть.
`LmsExportRow` получает `testVersion: number | null` и `formIds: string[]`, читаемые из тех же
служебных блоков, что пишет пакет. Пустая ячейка — не ноль и не текущая версия, а `null`.

Импорт превращает номер в снимок ОДНИМ разрешением на партию (карта «версия → снимок»), а не
запросом на строку: в файле тысячи прохождений и три-четыре версии. Идентификаторы вариантов
разворачиваются в карту «тема → вариант» по `formSetJson` разделов теста — так `forms_json`
у импортированного прохождения оказывается той же формы, что у телеметрии и у веба. Вариант,
которого в тесте больше нет (раздел переведён на случайную выдачу), в карту не попадает и
уходит в предупреждения партии — молча терять его нельзя, но и ронять из-за него загрузку не за
что.

- [x] **Шаг 1: тесты разбора и импорта** — строка с версией и вариантами; строка без них; версия,
      которой у теста нет; вариант, которого в разделах нет; плюс проверка, что версия
      спрашивается один раз на партию, а не на строку.
- [x] **Шаг 2: разбор служебных блоков**
- [x] **Шаг 3: разрешение версий и вариантов при записи партии**
- [x] **Шаг 4: прогон тестов** — разбор (56), импорт (26), интеграционный импорт (13),
      маршруты аналитики (15), `npm run check` чист.
- [x] **Шаг 5: коммит** — `0bd3d2dd`

### Задача 5: наблюдение несёт версию и вариант обоих источников

**Файлы:**

- Изменить: `server/services/analytics/observations.ts`,
  `server/storage/analytics-repository.ts` (новые колонки в выборке),
  `server/services/analytics/slice-axis.ts`
- Тест: `server/services/analytics/__tests__/observations.test.ts`,
  `server/services/analytics/__tests__/slice-axis.test.ts`,
  `tests/it/analytics-observations.it.test.ts`

`Observation.formId` (строка) заменяется на `forms: Record<string, string>` — «тема → вариант».
Так устроен пин и в вебе, и в пакете, а одна строка была догадкой: у теста из двух разделов с
вариантами прохождению принадлежат ДВА варианта, и склеивать их в один ключ нечем.

Заодно чинится само чтение: `variantJson.sections[].formId` вместо несуществующего поля верхнего
уровня. Прохождения из LMS отдают `snapshotId` и `forms` из колонок, заведённых задачей 3.

Ось «вариант» среза (Э3) переводится на новую форму: прохождение попадает в СТРОКУ КАЖДОГО своего
варианта — так же, как оно попадает в несколько групп участника, — а подписью служит `label`
варианта из `formSetJson` («Форма A»), а не сырой `formId`: идентификатор это uuid, и в срезе он
не читается.

- [x] **Шаг 1: тесты наблюдений и оси** — веб-попытка с двумя разделами-вариантами даёт две
      записи в `forms`; прохождение из LMS отдаёт версию и вариант; срез подписывает вариант
      названием. Попутно исправлена фикстура, повторявшая ту же выдумку о поле верхнего уровня.
- [x] **Шаг 2: выборка и приведение** — сама выборка правки не потребовала: строки читаются
      целиком, новые колонки пришли с ними.
- [x] **Шаг 3: ось «вариант»** — плюс `formLabels` в контексте оси и его сборка в маршруте.
- [x] **Шаг 4: прогон тестов** — аналитика (125), интеграционные наблюдения и срезы (16 + 5,
      с новой проверкой колонок на реальной базе), `npm run check` чист.
- [x] **Шаг 5: коммит** — `a3c31b51`

### Задача 6: расчёт вкладки «Выдача»

**Файлы:**

- Добавить: `server/services/analytics/delivery-stats.ts`,
  `server/routes/analytics/delivery.ts`
- Изменить: `server/routes/analytics/index.ts`
- Тест: `server/services/analytics/__tests__/delivery-stats.test.ts`,
  `server/routes/analytics/__tests__/delivery.test.ts`

Три расчёта, один слой наблюдений (FR-25) и одно правило про малую выборку: ниже порога
наблюдений (`config.analytics.minObservations`, сегодня 10) печатается «мало данных», а не
процент (FR-27).

**Варианты выдачи (FR-18).** Таблица группируется ПО РАЗДЕЛАМ теста: вариант — свойство раздела,
и у теста с двумя наборами форм плоский список смешал бы формы разных тем. Строка варианта:
прохождений, доля сдавших, средний результат, расхождение. Расхождение считается к доле сдавших
ПО ТЕСТУ и помечается тегом при отклонении не меньше десяти пунктов (`VARIANT_DELTA_POINTS` —
названная константа сервиса, а не число в коде): меньший разброс на выборке в сотню прохождений
ничего не утверждает. У варианта ниже порога наблюдений расхождение не считается вовсе — иначе
восьми прохождениям формы D назначается вердикт.

**Версии публикации (FR-19).** Строка на снимок теста плюс строка «Версия не указана» для
прохождений без пина. «Действовала» — от даты публикации снимка до публикации следующего; у
текущей версии конца нет. Версии, по которым прохождений нет, в таблице остаются: пустая строка
здесь и есть ответ («после правки ещё никто не проходил»).

**Профиль экспозиции банка (FR-20).** Строится по банку ОДНОЙ темы — у разных тем разные квоты
выдачи, и вместе они несопоставимы. Строка: вопрос, доля прохождений, где он был выдан
(`getDeliveryCountsForTest` / попытки за окно), абсолютное число выдач. Невыданные вопросы
сворачиваются в одну строку «Ещё N вопросов не выдавались ни разу»: перечислять их поштучно
незачем.

Ручка: `GET /api/analytics/tests/:testId/delivery` с необязательным `topicId` для профиля
(по умолчанию — первая тема теста), права `analytics.read` + `requireTestScope`, как у остальных
ручек теста.

- [x] **Шаг 1: тесты расчёта** — вариант ниже порога без вердикта; расхождение считается к тесту,
      а не к соседнему варианту; прохождения без версии не приписываются текущей; хвост банка
      сворачивается в строку.
- [x] **Шаг 2: сервис** — `delivery-stats.ts`, 15 тестов.
- [x] **Шаг 3: ручка и монтирование** — `GET /api/analytics/tests/:testId/delivery`, 6 тестов.
- [x] **Шаг 4: прогон тестов** — `npm run check` чист.
- [x] **Шаг 5: коммит** — `a242ba58`

### Задача 7: вкладка «Выдача» на экране

**Файлы:**

- Добавить: `client/src/features/analytics/test/variant-table.tsx`,
  `version-table.tsx`, `exposure-profile.tsx`
- Изменить: `client/src/pages/author/test-analytics.tsx`
- Тест: рядом с компонентами

Три карточки в порядке эскиза: «Варианты выдачи», «Версии публикации», «Профиль экспозиции банка».
Таблицы — `DataGrid`, полоса доли — `ProgressBar`, признак расхождения — `Tag --error`. Тема
профиля выбирается селектором в шапке карточки: карточка называет тему и её квоту выдачи, иначе
доли не с чем соотнести.

Сюда же переезжает вкладка «Уровни» адаптивного теста: она о том, как тест выдавался, и рядом с
вариантами и версиями стоит на своём месте. Отдельной вкладки у неё больше нет, но сама таблица
не меняется — переносится как есть.

- [x] **Шаг 1: тесты состава вкладки** — включая «мало данных» вместо процента и наличие блока
      уровней только у адаптивного теста.
- [x] **Шаг 2: компоненты и вкладка**
- [x] **Шаг 3: прогон тестов** — 12 новых компонентных, 102 по аналитике и ДС, `npm run check`.
- [x] **Шаг 4: коммит** — `aab3e8a0`

Найденный и исправленный долг ДС: `Select` уводил `aria-label` на обёртку-`div`, и кнопка-триггер
оставалась без доступного имени — поле без видимой подписи было безымянным для скринридера.

### Задача 8: расчёт профиля по шкалам

**Файлы:**

- Добавить: `server/services/analytics/scale-profile.ts`
- Изменить: `server/storage/analytics-repository.ts` (`selectScaleValuesForTest`),
  `server/storage.ts`, `shared/template/level-ramp.ts` (`rampFromParams`),
  `server/services/result-context.ts` (переходит на общую функцию),
  `server/routes/analytics/delivery.ts` или отдельная ручка `scales.ts`
- Тест: `server/services/analytics/__tests__/scale-profile.test.ts`,
  `shared/template/__tests__/level-ramp.test.ts`

Значения шкал прохождений берутся ОДНОЙ выборкой по обоим источникам — по образцу
`selectAnswersForTest`: веб отдаёт `result_json -> scaleResults -> raw`, LMS —
`scales_json`. Считаются две величины: среднее по шкале с объёмом выборки и распределение по
полосам толкования.

Уровень прохождения определяется по значению полосами САМОЙ шкалы (`parseScaleInterpretation` +
`findBand`), а не берётся из записи: импорт подписи уровня не хранит, и считать её по-разному для
двух источников значило бы получить два разных распределения на одних данных.

**Цвет полосы (FR-21a) не изобретается.** Порядок ровно один: тон уровня, заданный автором
(`band.tone` → success / info / warning / error), а где тона нет — рампа уровней теста
(`zoneColors(ramp, count, valence)`) с учётом валентности шкалы. Рампа собирается из параметров
оформления теста; функция её сборки сегодня спрятана в `result-context.ts` — она переезжает в
`shared/template/level-ramp.ts` как `rampFromParams`, и оба места начинают звать одну. Раздавать
цвет по порядку полос запрещено: иначе «Риск» окажется красным в итогах участника и другого цвета
в аналитике.

**Где уровня нет, оценки нет (FR-21b):** шкала без полос толкования в распределение не попадает
вовсе (строка «полосы толкования не заданы»), а её среднее в профиле рисуется АКЦЕНТОМ, а не
тонами «успех/предупреждение»: эталона у опросника не существует.

- [x] **Шаг 1: тесты расчёта** — среднее и объём по обоим источникам; уровень считается из полос,
      а не из записи; авторский тон перебивает рампу; валентность меняет конец рампы; шкала без
      полос даёт среднее и не даёт распределения.
- [x] **Шаг 2: общая `rampFromParams` и переход `result-context.ts` на неё**
- [x] **Шаг 3: выборка значений и сервис** — `selectScaleValuesForTest` + `scale-profile.ts`.
- [x] **Шаг 4: ручка** — `GET /api/analytics/tests/:testId/scales`.
- [x] **Шаг 5: прогон тестов** — 24 теста расчёта и рампы, 153 по аналитике.
- [x] **Шаг 6: коммит** — `40466e46`

### Задача 9: вкладка «Шкалы» на экране

**Файлы:**

- Добавить: `client/src/features/analytics/test/scale-profile.tsx`
- Изменить: `client/src/pages/author/test-analytics.tsx`
- Тест: рядом с компонентом

Две карточки: «Профиль по шкалам» (`ProgressBar` акцентом, подпись «среднее N из M» и объём
выборки) и «Распределение уровней» (`ProgressStacked` с легендой, по одной полосе на шкалу —
общей легенды у шкал быть не может, полосы задаются в самой шкале). Цвета приходят готовыми
с сервера: экран их не выбирает.

Вкладка показывается только у теста со шкалами: у оцениваемого теста без шкал ей нечего сказать,
и пустая вкладка читается как поломка.

- [x] **Шаг 1: тесты состава вкладки** — шкала без полос печатает «полосы толкования не заданы»;
      вкладки нет у теста без шкал.
- [x] **Шаг 2: компонент и вкладка** — признак `hasScales` добавлен в сводку теста.
- [x] **Шаг 3: прогон тестов** — 226 тестов маршрутов аналитики, `npm run check` чист. Попутно
      дополнены тестовые двойники хранилища: маршруты обзавелись новыми зависимостями.
- [x] **Шаг 4: коммит** — `6dab6836`

### Задача 10: приёмка Э5

- [x] **Шаг 1:** сверить обе вкладки с эскизом в браузере, в светлой и тёмной теме — снято на
      дев-сервере (:8099) учёткой приёмки: «Выдача» на тесте с 20 формами в трёх разделах,
      «Шкалы» на опроснике Маслач, обе темы. НАЙДЕНО И ИСПРАВЛЕНО: карточка распределения
      объявляла шкалу беспорожной там, где полосы заданы, а распределять было некого
      (коммит `e9bf9c05`).
- [x] **Шаг 2:** собрать пакет опубликованного теста и убедиться, что `TEST_DATA` несёт версию,
      а собранный `app.js` шлёт служебные блоки. Сделано на РЕАЛЬНОМ тесте дев-базы
      (`608932a1…`, третья версия публикации): в раскодированном `TEST_DATA` собранного пакета
      `publicationVersion: 3`, в рантайме — сборщик служебных блоков и оба новых поля
      телеметрии, двойника в `assets/app.js` нет.
- [ ] **Шаг 3 (на стенде, нужен доступ владельца):** выгрузить пакет ОПУБЛИКОВАННОГО теста из
      интерфейса (кнопка экспорта SCORM — версия бакается сама), загрузить его в WebTutor,
      пройти. Проверить: в отчёте LMS появились колонки `meta_test_version` и `meta_variant`;
      загрузка этого отчёта импортом ставит прохождению версию и вариант, а вкладка «Выдача»
      считает его в строке своей версии, а не в «Версия не указана».
- [ ] **Шаг 4:** править тест, опубликовать заново и убедиться, что старые прохождения остались
      в своей версии, а не переехали в новую.
- [ ] **Шаг 5:** на опроснике проверить, что уровень в аналитике совпадает с уровнем в итогах
      участника — и подписью, и цветом.
- [ ] **Шаг 6:** прибрать за собой на дев-базе (загруженные партии, тестовые прохождения).
- [x] **Шаг 7:** полный прогон `npm test` (разрешение владельца 2026-09-14). ПЕРВЫЙ прогон:
      10 114 тестов, 18 падений в десяти файлах — из них пятнадцать свои и вскрылись только
      полным прогоном (контракт `exportSourceForTest` в двух наборах SCORM, гард цветов ДС на
      `hsl(${…})` в компоненте, вкладка «Уровни» в тесте адаптивной страницы и семь чужих
      тестов, сломанных правкой `Select`). Все закрыты коммитом `33b474d2`. ВТОРОЙ прогон:
      10 111 из 10 114, три падения — известные чужие (`template-scene-mobile-css` ×2,
      `template-manifest-prd29`), они же стоят в реестре долга §0.0.4 п. 9 и к PRD-56
      отношения не имеют.

## Риски

- **Правки рантайма пакета ломают прохождение, а не экран.** Служебное взаимодействие, отправленное
  не тем типом, или обращение к несуществующему `TEST_DATA.publicationVersion` в LMS без консоли
  выглядит как «тест не открывается». Поэтому задача 2 идёт раньше серверных и заканчивается
  сборкой пакета и проверкой в плеере, а не прогоном тестов.
- **Пакеты в поле версии не знают и знать не будут.** Их прохождения обязаны попадать в строку
  «Версия не указана». Любая попытка «догадаться» по дате публикации сделает разрез по версиям
  бесполезным ровно там, где он и нужен.
- **Смена `Observation.formId` на карту трогает Э3.** Ось «вариант» среза сегодня отвечает «Без
  варианта» на всё; после починки числа среза изменятся — это и есть починка, но владельцу стоит
  сказать до выката.
- **Дев-база общая для всех рабочих копий.** Загруженные для приёмки партии и прохождения видны
  всем сессиям — убирать за собой обязательно.
- **Выгрузка WebTutor — четыре подколонки на взаимодействие.** Два новых служебных блока это восемь
  колонок в отчёте; в `cmi.suspend_data` они не пишутся и бюджет PRD-36 не трогают.

## Отложено за пределы Э5

- **Э6 «Диаграммы»** (FR-28) — там, где после Э5 они окажутся уместны.
- **FR-06e, разворот строки среза по темам** — незакрытый шаг задачи 3 плана Э3. Расчёт по темам
  появился в Э4, так что работа стала дешевле; к Э5 отношения не имеет.
- **Лист «Ответы» в книге экспорта** — отложен вместе со сведением `scorm_answers`.
- **Долг ДС:** заголовок сортируемой колонки `DataGrid` — кликабельный `div`, а не кнопка:
  сортировка недоступна с клавиатуры и не объявляется скринридером. Найден в Э4, к Э5 не
  привязан.
