# Roadmap реализации SCORM-расширений

**Версия:** 1.27  
**Статус:** Утверждено  
**Источник:** [BRD](./specs/brd-scorm-enhancements.md), PRD-1...PRD-52  
**Последняя актуализация:** 2026-09-03 — проход после этапов Э0-Э3 трека «Перестройка настроек
редактора теста и подытоги без вердикта». Трека в документе не было вовсе: ящик редактора
перестроен с пяти вкладок на семь, у подтемы снят вердикт, заведена её обратная связь, а §0
об этом молчал. Заведены: строка трека в [§0](#0-текущий-статус-на-2026-09-01), строка остатка
(Э4 и приёмка Э7.4-Э7.6) в [§0.0.1](#001-треки-в-работе--что-осталось) и запись о
закрытии всех шести намеченных находок аудита в
[§0.0.4](#004-технический-долг--открытые-пункты) — в реестре долга они не значились, счёт им
вёл сам аудит. Пять (Ф-1, Ф-8, Ф-10, Ф-13, Ф-21) сняты перестройкой ящика; шестая, Ф-2, была
заведена долгом этим же проходом и закрыта в тот же день правкой `b530d6d3` — сверка
руководства с интерфейсом показала, что поле переехало на вкладку обратной связи вместе с
подсказкой, а сама подсказка по-прежнему обещала экран, который разрез не печатает. Тем же
проходом руководства автора и импорта приведены к новому ящику и к новой модели подтемы
([§0.0.5](#005-документационный-долг)).
Предыдущая — 2026-09-01 — проход после трека PRD-51 «Отчёт как документ из
блоков». Трек реализован, принят и влит в `main` 2026-08-16, но в этом документе не значился
ВОВСЕ: §0 и §0.0 обрывались на PRD-50, а §14 спецификации PRD-51 ссылалась на запись техдолга,
которой здесь не было — ссылка вела в пустоту. Заведены: строка трека в
[§0](#0-текущий-статус-на-2026-09-01), строка остатка в
[§0.0.1](#001-треки-в-работе--что-осталось) (одиночный `npm run test:cov` на коде трека не
прогонялся), пункт долга «экран итогов остаётся на `resultsBlockOrder`, а документ отчёта
собирается блоками» в [§0.0.4](#004-технический-долг--открытые-пункты) и
[§0.3](#03-технический-долг), запись
[#39](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/39) в
[§0.0.6](#006-открытые-задачи-в-трекере). Тем же проходом отмечены выполненными четыре плана
трека — 178 пунктов стояли неотмеченными, и планы читались как неначатые, — и приведена к факту
шапка спецификации PRD-51: она стояла «НА СОГЛАСОВАНИИ» при реализованном, принятом и влитом коде.
Предыдущая — 2026-08-15 (второй проход за день) — сверка статуса, документации и
руководств с кодом на HEAD. Приведены к факту четыре расхождения, все по треку PRD-36: ветка
`prd-36-run-state-compactness` ВЛИТА в `main` (merge `78ba8d77`), а документ и шапка спеки
говорили «в ветке»; версия спеки — 1.3, а не 1.1; трек стоял в
[§0.0.2](#002-треки-не-начатые-вовсе) «не начатые вовсе» при реализованном и влитом коде —
перенесён в [§0.0.1](#001-треки-в-работе--что-осталось), где ему и место (осталась приёмка на
стенде WebTutor); в [§0.0.6](#006-открытые-задачи-в-трекере) числились открытыми
[#36](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/36) и
[#37](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/37) — обе закрыты в трекере.
Тем же проходом закрыт документационный долг, накопленный этапами Э3-Э5 PRD-50 и треком PRD-36:
контракт формата шаблонов поднят до 1.7.0 (блоки разделов, сводный разрез, адаптивные итоги),
руководство разработчика шаблонов догнало контракт, руководство автора получило разделы о блоках
итогов, ключах разреза и результате для LMS, а `CLAUDE.md` и `README.md` — счёт таблиц (31),
миграции 0016-0023 и проверку схемы dev-БД на `npm run dev`. Подробности —
в [§0.0.5](#005-документационный-долг).
Ранее — 2026-08-15 — проход после слияния PRD-50. Трек приведён к факту
везде, где значился незакрытым: ветка `prd-50-result-breakdowns` влита в `main`, закрыты все
пять этапов (Э1, «Э1-доделка», Э2, Э3, Э4, Э5), приёмки в браузере пройдены по всем; в §0 и
[§0.0.1](#001-треки-в-работе--что-осталось) не осталось ни одного хвоста: тем же числом закрыты
все три — сквозной прогон адаптивного теста (проведён на заведённой фикстуре стенда), разрыв
страницы отчёта внутри длинного блока (отыгран, дефекта нет) и долг Э2 (явное «не проверять» у
ключа теперь доезжает до базы). Из [§0.0.4](#004-технический-долг--открытые-пункты) ушли три
пункта: паритет «Сертификации» снят слиянием — его условие было записано в нём самом, — а два
заведённых сегодня закрыты в тот же день. Тремя строками пополнился трекер:
[#36](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/36),
[#37](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/37),
[#38](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/38) — два наблюдения приёмок,
державшиеся только протоколами, и запись в трекере для трека PRD-36. Документационный долг
[§0.0.5](#005-документационный-долг) закрыт целиком: шапки PRD-44, PRD-48 и PRD-50 приведены
к факту.
Предыдущая — 2026-08-14 — проход после пятидневного разрыва. В документ добавлены
три трека, которых в нём не было вовсе (PRD-48 перенос теста, PRD-49 заголовки блоков итогов,
PRD-50 разрезы результата) и две работы, PRD не заводившие (настройка «Тест пройден, если» и
регистрация прогона измерительного теста в аналитике). Приведены к факту три строки: PRD-28
значился «НЕ НАЧАТ» при влитом 2026-08-13 и принятом в браузере треке; PRD-44 — «идея, требования
не собирались» при согласованной 2026-08-06 спеке и семи закрытых фазах плана; из хвостов PRD-10
снят закрытый issue #31. Сводка открытых задач трекера [§0.0.6](#006-открытые-задачи-в-трекере)
пересобрана: закрыты #12, #31, #33, #34, заведён #35 (композиция шкал и пересчёт-формула —
единственный остаток закрытого #12). Заново открыт документационный долг
[§0.0.5](#005-документационный-долг): шапки спек PRD-44 и PRD-48 отстали от собственного текста.
Предыдущая — 2026-08-09 — закрыт PRD-47 (измерения и диаграмма в отчёте,
настройки отчёта на вкладке «Оформление»); строка PRD-35 сужена ещё раз — предпросмотр отчёта
теперь показывает измерения; тем же числом закрыт дефект PRD-46, найденный приёмкой PRD-47:
умолчание манифеста молча отменяло легаси-переключатель радара в отчёте.
Предыдущая — 2026-08-08 — в документ добавлены три трека, которых в нём не было
вовсе (PRD-45, PRD-46, PRD-47); строка PRD-35 сужена по факту (попытки по методике в dev-БД
есть — восемь завершённых от 2026-08-06, проверено запросом); из §0.3 сняты обе половины
пункта об импорте книги. Предыдущая актуализация — 2026-08-06 — открытые работы собраны в одну сводку
[§0.0 «Что не закрыто»](#00-что-не-закрыто-сводка-на-2026-09-01); попутно приведены к факту
четыре устаревшие строки §0 (PRD-23, PRD-29, PRD-31, PRD-34 — последний значился «не начат»
при влитом в `main` треке), заголовки спек PRD-23 и PRD-34 и снятые пометки о «неактуальных
спеках» PRD-39/40/42, которые к этому моменту уже были актуальны. Тем же числом закрыты PRD-31
(остаток приёмки — барьер B при разведённых часах — подтверждён владельцем продукта) и PRD-20
(принята политика выката 5.15). Полный статус по всем трекам —
в [§0 «Текущий статус»](#0-текущий-статус-на-2026-09-01); ценность и зависимости треков —
в [§0.2](#02-post-mvp-backlog); история релизов — в [CHANGELOG](../CHANGELOG.md).

---

## 0.0 Что не закрыто (сводка на 2026-09-01)

Раздел отвечает на ОДИН вопрос: что ещё предстоит сделать. Он ничего не добавляет к остальному
документу — только собирает в одно место то, что иначе разнесено по [§0](#0-текущий-статус-на-2026-09-01)
(статус трека), [§0.2](#02-post-mvp-backlog) (повод и зависимости) и
[§0.3](#03-технический-долг) (долг). Нормативным источником статуса остаётся §0: если строка
здесь разошлась с §0, верна §0.

Всё, чего в этой сводке нет, закрыто.

### 0.0.1 Треки в работе — что осталось

| PRD | Осталось | Чем держится |
| --- | --- | --- |
| [PRD-25](./specs/prd-25/home-page.md) | [issue #27](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/27) (нет экрана отчёта о дубликатах тем), [issue #28](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/28) (тег «Доступ выдан» не различает грант, роль и общий пул) | Ничем |
| [PRD-34](./specs/prd-34/copy-protection.md) | Приёмка частичная ([отчёт](./reports/prd34-copy-protection-acceptance.md) §2): полный круг настроек AC-08, экран обзора на веб-хосте, AC-04 живьём в отладочном плеере, мобильное поведение FR-27, печать вживую, PDF-отчёт при включённой защите AC-02, внешний шаблон через реестр AC-07 | Фикстуры (у тестовой нет системных узлов PRD-19), iOS/Safari, отчёт в демо-пакете |
| [PRD-35](./specs/prd-35/competency-radar.md) | Приёмка частичная: не проверена финальная растеризация PDF. Живое прохождение и радар в пакете закрыты 2026-08-08 в ходе PRD-46; предпросмотр отчёта показывает измерения с 2026-08-09 (PRD-47) | Отсутствие данных на стенде |
| [PRD-36](./specs/prd-36/run-state-compactness.md) | Приёмка на стенде WebTutor (§11 пункт 13 спеки) и сверка отчёта LMS с отчётом ДО правки (§11 пункт 4). Реализация влита в `main` 2026-08-15 (merge `78ba8d77` + четыре правки после слияния), приёмка в браузере пройдена — 23 проверки на локальном плеере | Ничем — нужен доступ к стенду; трек держит вход в PRD-37 |
| [PRD-46](./specs/prd-46/scales-chart-choice.md) | Приёмка по §9 спеки: контрольная выкладка ЧИЛ и Маслача, ипсативная модель со скрытым баллом, паритет веб ↔ пакет, различимость палитры в двух темах. Реализация влита 2026-08-08 целиком; дефект совместимости (умолчание манифеста отменяло легаси-переключатель радара в отчёте) закрыт 2026-08-09, см. [отчёт о правке](./reports/prd46-legacy-chart-kind-fix.md) | Ничем — приёмка не проводилась |
| [PRD-44](./specs/prd-44/scale-allocation-question-type.md) | Фаза 7 (смежное) — телеметрия FR-54, аналитика FR-51, инспектор FR-52, снапшоты FR-53, демо-вопрос шаблона FR-55 — «почти готова»; приёмка A-08 (порог покрытия 80%) требует одиночного `npm run test:cov`. Фазы 0-6 закрыты 2026-08-06, приёмки A-02 - A-05 закрыты | Ничем; A-08 ждёт свободной машины под одиночный прогон |
| [PRD-49](./specs/prd-49/results-headings.md) | Приёмка: реализация выполнена и влита 2026-08-12/13, приёмки в браузере НЕ было ни на экране итогов, ни на скачанном PDF | Ничем — приёмка не проводилась |
| [PRD-50](./specs/prd-50/result-breakdowns.md) | Реализация закрыта целиком (Э1, «Э1-доделка», Э2, Э3, Э4, Э5), ветка `prd-50-result-breakdowns` ВЛИТА в `main`, приёмки в браузере пройдены по всем этапам ([Э2](./reports/prd50-e2-acceptance.md), [Э3 и Э4](./reports/prd50-e3-e4-acceptance.md), [Э5](./reports/prd50-e5-acceptance.md)). Все три хвоста закрыты 2026-08-15: сквозной прогон адаптивного теста проведён на заведённой фикстуре стенда ([issue #36](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/36)), разрыв страницы отчёта внутри длинного блока отыгран на отчёте из 18 разделов и дефекта не показал ([issue #37](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/37)), долг Э2 снят: наблюдаемого следа он не давал — строка ключа без правила рисуется как «Не проверять», а движок считает отсутствие структуры информационным, — но ловушка сработала бы при появлении редактора умолчания, и решение автора теперь доезжает до базы | Ничем |
| [PRD-51](./specs/prd-51/report-document-blocks.md) | Одиночный `npm run test:cov`: порог покрытия 80 % на коде трека не проверен — прогон чистит `./coverage` целиком, а машину делят параллельные сессии. Реализация закрыта целиком (Э1-Э5), влита в `main` 2026-08-16, приёмки в браузере пройдены по всем этапам плюс отдельная [приёмка пяти хвостов](./reports/prd51-tails-acceptance.md); полный прогон на коммите `e272cb2f` — 585 файлов, 9089 тестов, 0 падений | Ничем — прогон ждёт свободной машины |
| [Перестройка настроек редактора теста](./plans/2026-09-02-editor-settings-restructure.md) (вне PRD) | Остался ОДИН пункт: Э7.6 — полный `npm test` и `npm run test:it` на общей верхушке; владелец 2026-09-03 ответил «пока нет», прогон ждёт его слова. Всё остальное закрыто и влито в `main` 2026-09-03: Э0-Э3, Э5, Э6, Э7.1-Э7.3, приёмка [Э7.4-Э7.5](./reports/editor-settings-restructure-acceptance.md) и Э4 «Свободная навигация внутри раздела» с [собственной приёмкой](./reports/editor-e4-free-navigation-acceptance.md) на обоих хостах | Ничем — работы были разделены на четыре параллельные сессии, порядок вливания в [карте разделения](./handoff/HANDOFF-2026-09-03-split-map.md) §5 |

### 0.0.2 Треки, не начатые вовсе

| PRD | Готовность требований | Блокер старта |
| --- | --- | --- |
| [PRD-21](./specs/prd-21/eligibility-plugin-registry.md) — реестр eligibility-плагинов | Требования есть (выделен из PRD-6) | Блокер СНЯТ 2026-08-06 (стенд WebTutor доступен) — можно начинать |
| [PRD-33](./specs/prd-33/code-listing.md) — листинг кода в задании | Только повод; требования не собирались, эскизов нет | Сбор требований |
| [PRD-37](./specs/prd-37/scorm12-export-profile.md) — профиль SCORM 1.2 | Разбор потерь есть, продуктового решения о запуске нет | **PRD-36 (жёстко)** + заказчик с LMS, принимающей только 1.2 |
| [PRD-41](./specs/prd-41/moodle-cooldown-plugin.md) — Moodle cooldown plugin | Предложен, требует исследования | PRD-21 (архитектура реестра) |
| PRD-N — flat adaptive `(adaptive, linear_flat)` | Отложено из PRD-4 v1.1 (§3.1.2) | Общая шкала сложности на тест без привязки к теме |
| PRD-N — медиатека и импорт медиа | Вынесено из PRD-14 2026-06-10; частично закрыто этапом Э4 в PRD-32 | Формализация процесса медиа |
| PRD-N (будущий scales-PRD) — глобальная библиотека шкал, композитные шкалы, пересчёт-формула | Отложено из PRD-5 2026-06-03; состав держит [issue #35](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/35) (заведён 2026-08-14 вместо закрытого #12): вклад «другая шкала» описан в [scoring-model](./specs/scoring-model.md) §2.2, но в схеме источник вклада только вопрос; нормализация `custom` объявлена в типе и движком не обрабатывается | Не приоритезировано |
| [BRD психометрической аналитики](./specs/brd-psychometric-analytics.md) (v0.4) | Требования на согласовании | Согласование BRD |

### 0.0.3 Хвосты закрытых треков

| PRD | Хвост | Почему не блокирует |
| --- | --- | --- |
| [PRD-1](./specs/prd-1/templates-content-pages.md) | Text-overflow preview/diagnostics в контентных страницах (§1.10) | Deferred post-MVP, объём существенный |
| [PRD-5](./specs/prd-5/scales-competency-measurements.md) | Гард переупорядочивания опций с измерениями | Точечный пункт, вырожденный случай |
| PRD-7 (контракты — [test-editor-contracts.md](./architecture/test-editor-contracts.md)) | S13.5b (G22 mapping-flow при смене шаблона), S13.8b (G12 wf-basic-warning), раздел «Архив» с восстановлением в списке тестов | Deferred при закрытии 2026-05-28 |
| [PRD-9](./specs/prd-9/crypto-password-hashing.md) | Этап 3 — удаление `bcryptjs` | Ждёт дренажа метрики `auth.legacy_bcrypt_rehash` |
| [PRD-12](./specs/prd-12/web-runtime-parity.md) | Буквальный ренейм namespace `test`/`page` (Вариант 2) | Решено НЕ делать |
| [PRD-14](./specs/prd-14/questions-import-export.md) | Импорт медиа по дескриптору / companion-ZIP | Вынесен в отдельный будущий PRD |

### 0.0.4 Технический долг — открытые пункты

Полные формулировки, риск и объём — в [§0.3](#03-технический-долг).

1. Согласие на обработку данных и срок хранения — ПЕРЕОТКРЫТО как бизнес-требование BR-23
   (внеочередной приоритет).
2. Пересоздание сессии при смене уровня доступа — привязано к BR-22-06 (единый вход).
3. Генератор превью шаблонов зависит от окончаний строк рабочей копии.
4. В SCORM-пакете нельзя завершить попытку с экрана «Обзор теста» при неотвеченном вопросе.
5. Классификатор типов вопроса: ветвление по `questions.type` рассыпано примерно по сорока
   местам и почти нигде не исчерпывающее — новый тип работает неправильно МОЛЧА.
6. Копия `tb-components.css` для эскизов отстала от приложения на 250 селекторов (замер
   2026-08-14); часть эскизов подключает копию, часть — источник.
7. Устаревшие подмены классов ДС в `docs/wireframes/ds/wf-utils.css`.
8. Экран итогов остаётся на `resultsBlockOrder`, а документ отчёта собирается блоками:
   один и тот же результат показывается двумя непохожими механиками (PRD-51 §14).

Закрыты 2026-09-03 треком перестройки настроек редактора все шесть намеченных находок аудита:
Ф-1 (настройка показа подытогов стояла в правилах прохождения), Ф-8 («Итоги раздела» были
размазаны по трём вкладкам), Ф-10 (результат для LMS стоял среди ограничений), Ф-13 (у
погашенных переключателей не было подписи причины с её адресом) и Ф-21 (подтема могла
провалить тему, пройденную по общему счёту) — этапами Э1-Э3; Ф-2 (подсказка обещала экран,
который разрез не печатает) — отдельной правкой `b530d6d3` в тот же день: поле переехало на
вкладку обратной связи вместе с подсказкой, а сам текст остался прежним, и это заметила
сверка руководства с интерфейсом. В реестре долга эти пункты не значились — счёт находкам вёл
сам [аудит](./reports/AUDIT_TEST_EDITOR_SETTINGS.md), — и записаны здесь, чтобы закрытие не
потерялось вместе с ним. То, что экран «Итоги раздела» разрез НЕ печатает, долгом не
признано: это свойство продукта — поля нет в контракте `CtxSectionResult`, блока нет ни в
одной раскладке обоих поставляемых шаблонов, — и теперь об этом прямо сказано и в подсказке,
и в руководстве автора.

Закрыты 2026-08-15, в день заведения: «Разрыв страницы PDF-отчёта внутри длинного блока ни разу
не отыгран» — ветка отыграна в браузере на отчёте из 18 разделов, разрез прошёл по границе ряда
карточек, дефекта нет ([issue #37](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/37));
«Ни один адаптивный тест dev-стенда не доигрывается до итогов» — оба дефекта прогона исправлены,
на стенде заведена проходимая фикстура со своим скриптом засева
([issue #36](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/36)).

Закрыт 2026-08-15: «Паритет "Сертификации" с эталоном держится вручную, а гард сверял только
разметку» — условие снятия, записанное в самом пункте, выполнено: ветка
`prd-50-result-breakdowns` влита в `main`. Подробности — в [§0.3](#03-технический-долг).

Закрыт 2026-08-06: «В PDF-отчёте тема с неопределённым вердиктом печатается с меткой
"Не пройден"» — подробности в [§0.3](#03-технический-долг).

### 0.0.5 Документационный долг

Открытых пунктов нет.

Проход 2026-09-03 закрыл долг, заведённый этапами Э1-Э3 трека перестройки настроек редактора:
оба руководства описывали ящик из пяти вкладок, которого больше нет, и модель, в которой
подтема выносила вердикт. Сделано:

- [руководство автора](./guides/test-authoring-guide.md) — главы 6-13 переписаны под семь
  вкладок нового ящика (Основное · Состав и сценарий · Правила прохождения · Оценка
  результата · Обратная связь и итоги · Оформление · Комментарии), заведена таблица
  «где искать привычное поле теперь»; раздел о порогах подтем переписан под модель без
  вердикта, раздел о блоках итогов заменён объяснением, почему их больше не заводят; главы
  об обратной связи собраны в одну — замена текста темы, правило выдачи текста подтемы,
  реестр «По вопросам», тексты уровней; таблица предупреждений публикации приведена к коду
  (снято FR-46, добавлено предупреждение о порогах подтем);
- [руководство по импорту](./guides/import-workbook-guide.md) — листы «Обратная связь» и
  «Рекомендации» получили колонку «Подтема» и владельца «Подтема» с адресом «Раздел» +
  «Подтема», описаны замена текста темы вместо сложения, правило выдачи текста подтемы и
  правило «раздел, чьи подтемы книга назвала, забирает набор целиком»; примеры листов
  дополнены строками подтемы; ссылки на несуществующую вкладку «Настройки» сняты.

Тем же числом, вторым заходом, оба руководства догнали Э5, влитый в тот же день: в
руководстве по импорту заведён раздел о прежних именах параметров (`Тип сценария`,
`Показывать правильные ответы после ответа` — прежние имена принимаются навсегда, выгрузка
пишет нынешнее, имя сравнивается без учёта регистра и лишних пробелов, и книжное имя
`Тип сценария` намеренно расходится с подписью поля «Сценарий прохождения» в редакторе);
в руководстве автора обновлены подписи, которые Э5 переименовал — «Подытоги по подтемам
(тегам)» с заголовком группы, «Правила оценки тем», «Показывать правильные ответы после
ответа», имена мест подытогов, — и пересняты три снимка (вкладка «Обратная связь и итоги»,
подраздел «Состав итогов», полотно сценария с бейджем «Доступно макетов»).

Проход 2026-08-15 (второй за день) закрыл долг, которого реестр не видел: контракт формата
шаблонов и оба руководства отстали от кода на три этапа PRD-50 и на трек PRD-36. Сделано:

- [спецификация формата шаблонов](./specs/spec-template-platform.md) поднята до **1.7.0** —
  вердикт ключа в строке разреза (`passed` / `passClass` / `statusLabel`, §10.6), блоки разделов
  `topicGroups[]` / `ungroupedTopics[]` (§10.7) и сводный разрез по тесту `result.breakdown[]`
  пятым подблоком итогов (§8.3.3, §10.5, §10.8). До правки контракт описывал только подытоги в
  карточке темы, хотя оба поставляемых шаблона печатали и остальное — сторонний шаблон из реестра
  PRD-3 не мог узнать о них ниоткуда;
- [руководство разработчика шаблонов](./guides/template-development.md) — версия 1.7.0, те же
  три темы плюс ловушка «блоки ИЛИ плоский список, но не оба» и два пункта в чек-лист;
- [руководство автора](./guides/test-authoring-guide.md) — новые разделы: блоки итогов (7.1a),
  пороги по подтемам (7.3a), три поля показа разреза в «Правилах прохождения», настройка
  «Результат для LMS при нескольких попытках» в «Ограничениях», таблица предупреждений
  публикации в §14;
- `CLAUDE.md` и [README](../README.md) — счёт таблиц (31, а не 29), шестой тип вопроса
  (`allocation`), колонки миграций `0014`-`0023`, движок `shared/breakdown/`, сервис
  `breakdown-warnings.ts`, состояние прогона в `cmi.suspend_data` и проверка схемы dev-БД
  на `npm run dev`.

Проход 2026-08-14 закрыт 2026-08-15 — приведены к факту три шапки:
[PRD-44](./specs/prd-44/scale-allocation-question-type.md) (было «СПРОЕКТИРОВАН 2026-08-06,
реализация не начата» при закрытых фазах 0-6, почти закрытой фазе 7 и пройденных приёмках
A-02 - A-05), [PRD-48](./specs/prd-48/test-transfer.md) (было «реализация частично выполнена»
при собственном же §9 «РЕАЛИЗОВАНО ПОЛНОСТЬЮ» и снятых 2026-08-12 долгах) и
[PRD-50](./specs/prd-50/result-breakdowns.md) (было «Э1 реализован, ветка не влита» при закрытых
Э1 — Э5, пройденных приёмках и влитой ветке).

Проход 2026-08-06 закрыт. Заголовки [PRD-23](./specs/prd-23/template-themes.md) (было
«В РАБОТЕ, закрыты Э0-Э1» при закрытых Э0-Э9) и [PRD-34](./specs/prd-34/copy-protection.md)
(было «код не начат, эскизов нет» при влитом в `main` треке и пройденной браузерной приёмке)
приведены к факту; заголовки
[PRD-39](./specs/prd-39/matching-connector-merge.md),
[PRD-40](./specs/prd-40/cooldown-by-outcome.md) и
[PRD-42](./specs/prd-42/feedback-assets-links-only.md) на момент проверки уже были актуальны —
устаревшими оказались не они, а пометки о них в §0, которые тем же проходом сняты.

### 0.0.6 Открытые задачи в трекере

Открыты — семь задач (сверено с трекером `gh issue list` 2026-09-01):
[#39](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/39) перенос теста между
инсталляциями не забирает документ отчёта — заведена 2026-08-16 хвостом PRD-51, чужой трек
(PRD-48); долг зафиксирован списком `NOT_CARRIED` в гарде
`server/__tests__/test-transfer-completeness.test.ts`, поэтому молча не потеряется;
[#38](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/38) переполнение
`cmi.suspend_data` обнуляет состояние прогона молча — запись в трекере для трека PRD-36,
нормативный источник по-прежнему спецификация; сам трек реализован и влит, задача держится
приёмкой на стенде;
[#35](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/35) композиция шкал (вклад = другая
шкала) и пересчёт-формула `a*raw+b` с отсечением — отложено в будущий scales-PRD;
[#32](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/32) сертификат при успешной попытке;
[#28](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/28) и
[#27](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/27) хвосты PRD-25;
[#26](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/26) эпик «расчёт шкал и показателей
внутри SCORM».

Закрыты 2026-08-15: [#37](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/37) (разрыв
страницы PDF-отчёта внутри длинного блока — разобрана в день заведения: ветка отыграна на отчёте
из 18 разделов, дефекта нет) и [#36](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/36)
(адаптивные тесты стенда — все четыре пункта закрыты в день заведения: две правки прогона, две —
предупреждений о невыполнимой выдаче, плюс фикстура стенда со своим скриптом засева). Обе
оставались открытыми до решения владельца и им же закрыты.

Закрыты 2026-08-14: [#12](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/12)
(spec-extension по градуированной оценке — весь состав доставлен PRD-10 и PRD-11, единственный
остаток вынесен в #35), [#31](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/31)
(хвосты PRD-10: предпросмотр балла влит коммитом `2d8c7499`),
[#33](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/33) (шкалы и показатели на
адаптивном экране итогов) и [#34](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/34)
(условная обратная связь вопроса на веб-хосте).

---

## 0. Текущий статус (на 2026-09-01)

Таблица покрывает ВСЕ заведённые треки (PRD-1...PRD-51) и является нормативным источником
статуса: при расхождении с формулировками в [§0.2](#02-post-mvp-backlog) и в самих PRD верна
она. В §0.2 фиксируются ценность трека, его повод и зависимости. Только НЕЗАКРЫТОЕ — в сводке
[§0.0](#00-что-не-закрыто-сводка-на-2026-09-01).

| Шаг | PRD | Фаза | Статус | Блокер для старта следующей фазы |
| --- | --- | --- | --- | --- |
| 1 | PRD-7 | S0 — контракты, decisions, baseline | Закрыта | — |
| 1 | PRD-7 | S1 — wireframes 153/153 + согласование | Закрыта 2026-05-21 | — |
| 1 | PRD-7 | S2 — backend, миграции, API | Закрыта (миграции, TestSettingsService, endpoints; regression проверяется в S9) | — |
| 1 | PRD-7 | S3 — frontend mappers + validation | Закрыта (unit-тесты 46 шт.) | — |
| 1 | PRD-7 | S4-S8 — UI Drawer + секции | Закрыта (2026-05-25; FR-20c якорная навигация → S9) | — |
| 1 | PRD-7 | S9 — component + API тесты, regression | Закрыта 2026-05-27 (тесты + FR-20c; полный suite 1375 зелёных, `npm run check` 0 ошибок) | — |
| 1 | PRD-7 | S10 — удаление legacy UI | Закрыта 2026-05-27 (inline wizard удалён в S5-S8; `tests-list` монтирует `TestEditor`; `ContentPagesDialog` выведен; чтение `start_page_content` удалено из SCORM-export + runtime, контент играется как intro content-page миграции 003 §4.2; golden-guard добавлен) | — |
| 1 | PRD-7 | S11 — acceptance pass §10 | **Закрыта 2026-05-28** — финальный acceptance выполнен после закрытия S12 + S13; кодовый closeout подтверждён: `npm run check` 0 ошибок, `vitest run` 1373/1373 зелёные. Live-browser acceptance (Playwright + axe) выполняется отдельно от кодового closeout. | — |
| 1 | PRD-7 | S12 — Design tab closeout | **Закрыта 2026-05-28** — G1 sub-rail params + G2 iframe preview + G3 gallery + G4 все param-типы (image/asset/file/downloadLink/url/multiselect/number) + G5 orphan dialog + G6 incompatible-banner. Контракты — [test-editor-contracts.md](./architecture/test-editor-contracts.md) | — |
| 1 | PRD-7 | S13 — Editor parity | **Закрыта 2026-05-28** — все 8 sub-фаз закрыты (S13.1 quick wins / S13.2 feedback / S13.3 per-topic limits / S13.4 row-actions / S13.5 router-mode / S13.6 variant-replace / S13.7 drawer chrome / S13.8 cleanup+acceptance). Deferred: S13.5b (G22 mapping-flow cross-tab coupling) + S13.8b (G12 wf-basic-warning UX notification). | — |
| 2 | PRD-4 | Runtime `flowPolicy`, `section.*` | **Закрыта 2026-05-29** — все 6 фаз (1: validation L2/L3, 2: mapper L4, 3: UI L1, 4a: export, 4b: section results, 4c: router runtime + completionPolicy + unlockRules, 4d: adaptive integration linear+router, 4e: per-section timers, 4f: recovery, 5: golden tests). Все 5 валидных `(mode×flowMode)` комбинаций имеют runtime support; `(adaptive, linear_flat)` blocked в Phase 1, deferred в будущий PRD «Flat adaptive». См. [PRD-4 spec v1.1](./specs/prd-4/course-flow-sections.md). | — |
| 4 | PRD-8 | Router-flow runtime + UI «Структура» в router-режиме | **Закрыта 2026-05-29** — реализована cross-PRD: UI (Структура + Настройки→Сценарий) в PRD-7 G45/router-by-topics; runtime (router state machine, completionPolicy, sectionUnlockRules, recovery) в PRD-4 phases 4c/4f; PRD-8-specific delta — FR-18 router lifecycle events. См. [PRD-8 spec](./specs/prd-8/section-router-flow.md). | — |
| 5 | PRD-2 | `result.*` показатели результата | **Закрыта 2026-06-03** — Этапы A-C (DSL+`controls_status`, CRUD+вкладка «Показатели», рантайм `result.*`, golden MBI). См. [PRD-2 spec](./specs/prd-2/result-variables.md). | PRD-4 |
| 6 | PRD-5 | Шкалы и многомерные измерения | **Закрыта 2026-06-03** — Этапы A-C (схема+движок+API, рантайм `scale.*`+экспорт, вкладка «Шкалы»+матрица «Вклады», golden MBI). Отложено: гард опций, композитные шкалы, глобальная библиотека шкал (будущий PRD). См. [PRD-5 spec](./specs/prd-5/scales-competency-measurements.md). | PRD-2 (DSL), PRD-4 |
| 3 | PRD-6 | Retake gate, eligibility plugins | **ЗАКРЫТ 2026-07-05 (Phase 1 — 2026-06-04)** — ядро (схема `retakePolicy`, eligibility-движок/плагины/реестр с TS↔JS-парити), рантайм-гейт до SCORM `Initialize` (NFR-01/02), блок-экран из системной страницы шаблона `system.blocked`, боевой источник `webtutor_cooldown` через ClientBridge `get_metadata` (вскрыт на живом портале RT), авторский пейн «Повторное прохождение». Администрируемый реестр eligibility-плагинов (endpoint'ы/фильтры/конфигурации, выбор из >= 2 в UI, тестовая проверка) вынесен в [PRD-21](./specs/prd-21/eligibility-plugin-registry.md). См. [PRD-6 spec](./specs/prd-6/retake-cooldown-gate.md). | PRD-4 |
| 7 | PRD-3 | Жизненный цикл шаблонов (СУЖЕН 2026-06-05: только админ-реестр внешних ZIP; платформенная часть → PRD-12) | **Закрыта 2026-06-07** — Фаза 1 (схема +6 колонок + миграция 011, ZIP-сервис с zip-slip guard, структурный валидатор, admin API `/api/admin/templates`, реестр), Фаза 2 (браузерный движок проверки работоспособности `shared/template/smoke-runner` поверх общего рендерера PRD-12 + серверный гейт активации NFR-01, эндпоинты `smoke-bundle`/`preview-image`), Фаза 3 (UI `/author/templates`: список, загрузка, предпросмотр+проверка, жизненный цикл). tsc 0, vitest 2093, Playwright-сверка с эскизом. См. [PRD-3 spec v2.1](./specs/prd-3/external-templates.md). | PRD-1 closeout; PRD-12 |
| 9 | PRD-12 | Единый шаблонный рантайм рендера (L2): один DSL-рендерер + публичный контекст для SCORM и веб; серверный расчёт `@shared`; поглощает платформу PRD-3 | **Закрыта 2026-06-06** — Фаза 0 (DSL-движок + публичный контекст + `renderScreenInto`), Фаза 2 (серверный расчёт `@shared`: PRD-10/5/2 + retake-гейт PRD-6), Фаза 1 (все ученические экраны SCORM на общих layouts) и веб-хост (React-обёртка + Shadow DOM) закрыты; единый DnD-движок (`shared/template/dnd`) + CSS-унификация (один компонентный источник на оба хоста). Открыт только опц. буквальный ренейм namespace `test/page` (Вариант 2 — не делаем). См. [PRD-12 spec](./specs/prd-12/web-runtime-parity.md). | PRD-2/4/5/6/10/11; spec-template-platform |
| 8 | PRD-9 | Миграция bcryptjs → `@vvlad1973/crypto` (scrypt) | **ЭТАПЫ 1-2 РЕАЛИЗОВАНЫ 2026-07-05** — новые пароли хешируются scrypt (`@vvlad1973/crypto`, профиль OWASP) через единый шов `server/utils/crypto.ts`; ленивый rehash bcrypt→scrypt при первом успешном логине; `bcryptjs` — временная динамически импортируемая legacy-зависимость только для старых `$2a$…`-хешей (Этап 3 = удаление по дренажу метрики `auth.legacy_bcrypt_rehash`). БД-миграция не нужна (`passwordHash` — `text`). См. [PRD-9](./specs/prd-9/crypto-password-hashing.md). | — |
| — | PRD-1 | Шаблоны и контентные страницы | **Закрыта 2026-05-28** — Backend 100% / Runtime 100% / Frontend 100% (MVP-scope). Закрытие PRD-1 произошло одновременно с PRD-7: остаточные task'и (предпросмотр, галерея, all-param-types, manifest validation, `kind` во встроенных шаблонах) переехали в PRD-7 S12 G2/G3/G4/G6/G48. См. [PRD-1 spec](./specs/prd-1/templates-content-pages.md). | Deferred post-MVP: text-overflow preview/diagnostics в content-pages (§1.10) — substantial, не блокирует MVP. |
| — | PRD-16 | Раздел «Темы и вопросы» и модель вопроса (ось контента: единое дерево; сложность число/nullable; медиа только-загрузка; валидация конструкторов; порядок и перемешивание вариантов) | **РЕАЛИЗОВАН (сверка кода 2026-08-06), кроме отложенного FR-90** — блоки A-F закрыты: единый раздел «Темы и вопросы» с деревом, фасет-фильтром и слоем действий (`7f465df`, `c3cc1ac`, `b9af11f`, `ca1984e`, 2026-06-16), групповые операции с папками/темами (`8fb8705`, 2026-07-01), ретайр старых страниц «Темы»/«Вопросы» с редиректами (`a570e77`, 2026-07-24); сложность `questions.difficulty` nullable (`0c03a2f`, миграция 029) с «Не задано» в редакторе (`180537a`) и фасетом-интервалом (`31be025`); медиа только загрузкой через DS `FileUploader`, тип по MIME; валидация конструкторов баннером с блокировкой сохранения и сохранение вариантов при смене типа (`08ab89a`, 2026-06-16); ручной порядок вариантов перетаскиванием + «Случайный порядок вариантов» на обоих хостах (`client/src/pages/learner/answer-gate.ts`, `server/scorm/template/app/utils/shuffle.js`), обязательное перемешивание Ранжирования (`54d22ac`, 2026-07-15). Незакрыт только FR-90 (переопределение выдачи вариантов на уровне теста) — сознательно отложен до подтверждения гипотезы. См. [PRD-16](./specs/prd-16/content-axis-and-question-model.md) §6.1, [BRD](./specs/brd-content-axis.md), [план](./plans/PLAN_content_axis_implementation.md). | PRD-7, PRD-15 |
| — | PRD-17 | Фиксированные варианты выдачи (формы) — выдача варианта целиком в случайном порядке, ротация | **ЗАКРЫТ 2026-06-25** — фазы 0-5: `test_sections.form_set_json` (миграция 030), `shared/draw/forms.ts`, SCORM-паритет (деградация до случайного выбора, NFR-17), лист «Варианты» в книге Excel, UI редактора вариантов. См. [PRD-17](./specs/prd-17/question-set-variants.md). | PRD-7, PRD-11 |
| — | PRD-18 | Встроенный плеер тестирования и отладки (BR-13/Этап 14) | **ЗАКРЫТ 2026-06-27** — фазы 0-4: общий `buildScormExportData`, роутер `debug-player` (пакет из живого состояния, telemetry off, одноразовый токен, гейт edit-scope), окно `/author/tests/:id/debug` (статус-панель + iframe + инспектор 7 вкладок + тоггл «Эталон»), общий `window.TBInspector`. См. [PRD-18](./specs/prd-18/debug-test-player.md). | PRD-12, PRD-15 |
| — | PRD-19 | Навигация прохождения (пропуск/возврат), поэтапное завершение, контракт экранов | **ЗАКРЫТ 2026-06-29** — блоки A-G: настройки `allowReturnToUnanswered`/`allowAnswerChange`/`showSectionResults` (миграция 031), прогресс-пиллы, служебный экран-обзор, поэтапное завершение по разделам, неотвеченный = 0 баллов в едином движке, cooldown-старт на стартовой странице обоих хостов; системные узлы границ раздела `intro`/`review`/`section-results` (миграции 034/035). Версия 1.1 спеки (2026-09-02) добавила настройку «Свободная навигация внутри раздела» (FR-11a - FR-11d) — **реализована и влита 2026-09-03** этапом Э4 трека перестройки настроек редактора: колонка `tests.allow_free_section_navigation` (миграция `0027`), умолчание ВЫКЛ, правило доступности в общем построителе карты вопросов на оба хоста, [приёмка](./reports/editor-e4-free-navigation-acceptance.md). См. [PRD-19](./specs/prd-19/skip-return-navigation.md), [контракт структуры](./specs/prd-19/test-structure-contract.md). | PRD-4, PRD-8, PRD-12 |
| — | PRD-20 | Устойчивость таймера и возобновление timed-тестов (BR-14/Этап 15) | **ЗАКРЫТ 2026-08-06** (Фазы 1-2 реализованы 2026-07-02, приняты на живом WebTutor 2026-08-06) — Фаза 1: монотонный `performance.now()` (нет дрейфа в фоне и выигрыша от перевода часов в сессии), единая точка `stopTestTimer`, секционный таймер PRD-4 на том же примитиве. Фаза 2: кросс-reload на модели активного времени — anchor `{ limitMinutes, baselineTotalSec, sig }` в `suspend_data.timer` + `cmi.total_time`, снят технический guard `saveCurrentSession` (timed-тесты резюмятся; adaptive — исключение), периодический `Commit(session_time)` 10 с (kill-устойчивость), tamper-evidence keyed djb2-checksum на anchor (правка `suspend_data` → потеря resume, не «фри тайм»), единообразное возобновление по режимам BR-06 (linear — точный вопрос, router — внутри незавершённой темы). Watchdog отката часов снят в пользу флага аномалии, счётчики reload отложены. Деградация к прежнему поведению при любом сбое персистентности. **Фаза 3 закрыта целиком:** 5.12/5.14 реализованы, 5.13 закрыта моделью, 5.15 (выкат) закрыта решением владельца продукта 2026-08-06 — новые тесты уходят с новым рантаймом сразу, действующие переиздаются на границе цикла; разовой миграции и уведомления учащихся не требуется. Ограничение принятой политики: технического гарда под ней нет, ре-экспорт доступен автору в любой момент, соблюдение границы цикла организационное. **Техдолг ЗАКРЫТ 2026-08-06:** LMS-валидация Фазы 2 выполнена на живом WebTutor — reload посередине попытки и kill вкладки отыграны, семантика `cmi.total_time` подтверждена, гейт выката пройден. Попутно из приёмки убраны два устаревших пункта, ссылавшихся на отменённые механизмы (watchdog отката часов 5.8 и счётчики reload/single-active-session 5.9); остаточный вопрос параллельных окон переквалифицирован — он не таймерный, а про затирание `suspend_data` (к PRD-36 и анти-абьюз-треку). Не отыграна правка `suspend_data` вручную, но она проверяема локально и гейт не держит. См. [PRD-20](./specs/prd-20/timer-integrity-resume.md), [план](./plans/PLAN_prd20_timer_phase2.md). | PRD-4, PRD-6, PRD-15 |
| — | PRD-10 | Градуированная оценка ответа («Цена ответа») | **ЗАКРЫТ 2026-06-04** — веса опций (`single`) и ступенчатая таблица «условие → балл» (`multiple`/`matching`/`ranking`), golden против эталона РТК. См. [PRD-10](./specs/prd-10/graded-answer-scoring.md). | BR-09 |
| — | PRD-11 | Квоты выдачи по тегам | **ЗАКРЫТ 2026-06-04** — стратифицированная выдача `strata:[{tag,count,mode}]`, тегирование вопросов, верификация в плеере. См. [PRD-11](./specs/prd-11/tag-draw-quotas.md). | BR-10 |
| — | PRD-13 | Ролевая модель и разграничение доступа | **ЗАКРЫТ 2026-06-08** (миграция 017: `user_roles`, мультироль, владелец + гранты). **Дополнен 2026-07-30:** заведена шестая роль «Разработчик» (drizzle-миграция `0005`) — генерация SCORM закреплена за ней (`tests.export.scorm`), а отладочный прогон остался у автора отдельным правом `tests.debug.play`. См. [PRD-13](./specs/prd-13/role-based-access-control.md), [role-model](./specs/access-control/role-model.md). | — |
| — | PRD-14 | Импорт и экспорт банка вопросов (Excel) | **Фазы 0-2 + мультилист реализованы 2026-06-10/11**; книга далее росла листами «Оценка» (PRD-15), «Пороги вариантов» (PRD-24) и «Вклады вопросов» (PRD-26). Импорт медиа — отдельный будущий PRD. См. [PRD-14](./specs/prd-14/questions-import-export.md). | BR-11 |
| — | PRD-15 | Владение контентом, целостность и публикация | **ЗАВЕРШЁН ПОЛНОСТЬЮ 2026-06-13** — блоки A-D + фаза 5 приёмки (миграции 019-028). См. [PRD-15](./specs/prd-15/content-ownership.md), [RUNBOOK](./runbooks/prd15-content-ownership.md). | PRD-13, PRD-14, PRD-10 |
| — | PRD-21 | Администрируемый реестр eligibility-плагинов | **НЕ НАЧАТ, но БЛОКЕР СНЯТ 2026-08-06** (выделен из PRD-6 2026-07-05). Прежний блокер — нет доступа к живому WebTutor для проверки контракта плагина; стенд стал доступен, cooldown и контракт `webtutor_cooldown` подтверждены на цели, поэтому трек можно начинать. Закрыт OQ-01: режим проверки на мок-данных как временная замена не нужен. См. [PRD-21](./specs/prd-21/eligibility-plugin-registry.md). | PRD-6, PRD-13 |
| — | PRD-22 | Поля контентной страницы и последовательности | **ЗАКРЫТ 2026-07-23** — этапы Э0-Э7 плюс [план доработок предпросмотра и вариантов](./plans/PLAN_PRD22_PREVIEW_AND_VARIANTS.md) (Э1-Э10) закрыты и закоммичены; состав полей страницы задаёт манифест (`placeholders[]` + `settings[]`), хранение — `content_pages.settings_json` (drizzle-миграция `0001`). См. [PRD-22](./specs/prd-22/page-fields-and-sequences.md). | PRD-1, PRD-3, PRD-12, PRD-19 |
| — | PRD-23 | Темы шаблона и раздел «Цвета» | **ЗАКРЫТ** — закрыты Э0-Э9: контракт `themes[]`, хранение, отрисовка на обоих хостах через `data-theme` на корне сцены, раздел «Цвета», предпросмотр, «Сертификация» 1.3.0, документы, приёмка пакетом на локальном плеере (`data-theme="dark"` на `<html>`, блок `tb-theme-vars`, вычисленный `--primary` равен переопределению тёмной палитры). Приёмка вскрыла два дефекта (форма `:host(...)`, утечка defaulted-параметров) — оба починены. См. [PRD-23](./specs/prd-23/template-themes.md), [план](./plans/PLAN_PRD23_TEMPLATE_THEMES.md). | PRD-3, PRD-7, PRD-12, PRD-22 |
| — | PRD-24 | Пороги прохождения по вариантам | **ЗАКРЫТ 2026-07-24** — Task 1-9: движок `resolveTopicRule`, веб-оценка, SCORM-рантайм (пиннинг выпавшего `formId`), манифест, редактор, лист книги «Пороги вариантов», отладчик, приёмка. Миграции нет — хранение в существующем `topic_pass_rule_json`. См. [PRD-24](./specs/prd-24/variant-pass-thresholds.md). | PRD-17, PRD-10, PRD-15, PRD-14 |
| — | PRD-25 | Домашняя страница | **РЕАЛИЗОВАНА**, приёмка в реальном браузере пройдена 2026-07-30 по четырём профилям прав. `/` стал страницей-рабочим столом (секции по capability, признаки внимания, агрегат `GET /api/home`, `topics.updated_at` — drizzle-миграция `0002`) вместо редиректа по правам. Открытый долг: issue #27 (нет экрана отчёта о дубликатах тем), #28 (тег «Доступ выдан» не различает грант, роль и общий пул). См. [PRD-25](./specs/prd-25/home-page.md). | PRD-13, PRD-15, PRD-16, PRD-18 |
| — | PRD-26 | Тип вопроса «шкала» | **ЗАКРЫТ 2026-07-30** — пятый тип вопроса `scale` (упорядоченные градации, измерительный и проверяемый режимы), режим выбора у DS-компонента `Stepper`, рендер на обоих хостах, книга Excel (листы «Вопросы»/«Оценка»/«Вклады вопросов»), редактор, аналитика и отладчик. Миграция БД не потребовалась (перечисления — `text` без `CHECK`). Приёмка — [отчёт](./reports/prd26-scale-acceptance.md). См. [PRD-26](./specs/prd-26/scale-question-type.md). | PRD-5, PRD-2, PRD-10, PRD-12, PRD-14 |
| — | PRD-27 | Шаблон отчёта о результатах | **РЕАЛИЗОВАН И ЗАКОММИЧЕН 2026-07-31** — Фазы 1-5 + FR-05: отчёт выведен из кода в модель шаблона (типы `report` / `report.adaptive` в `manifest.contentTemplates[]`, формат шаблонов 1.1.0), вариант разрешается одной функцией `resolveReportBake` на всех хостах, запекание в `TEST_DATA.designSettings.report`, предпросмотр в настройках теста, отчёт по попытке со снапшотом собирается вариантом ИЗ ВЫДАЧИ, деградация макета И стиля на вложенный «Стандартный», проверка работоспособности рендерит каждый объявленный вид. Хранение — `tests.report_settings_json` (drizzle-миграция `0004`). См. [PRD-27](./specs/prd-27/report-template.md). | PRD-12, PRD-22, PRD-3, PRD-15 |
| — | PRD-28 | Внешний участник | **РЕАЛИЗОВАН, ПРИНЯТ и влит в main 2026-08-13** (ветка `feat/bulk-invite`) — открытые вопросы спеки закрыты 2026-08-12, тогда же в трек добавлено заведение СПИСКОМ. Внешний участник — признак учётной записи, а не роль: учётка заводится без пароля, паролем не пускает, восстановление пароля и обычное приглашение ей отказаны; перевод в штатные и обратно, видимость в списке пользователей и аналитике, вкладка приглашения списком (разбор книги со схлопыванием повторов, классификация строк предпросмотра, прогон с отчётом по каждому адресу, выгрузка ссылок книгой, аудит прогона), потолок строк и почасовой предел писем в настройках. Приёмка в браузере пройдена по всем десяти критериям §11, кроме части пункта 7 (недоставку письма на dev-контуре воспроизвести нечем). Известные долги — в §14 спеки. См. [PRD-28](./specs/prd-28/external-participant.md), [план](./plans/2026-08-12-external-participant.md). | PRD-13 |
| — | PRD-29 | Итоги измерительного теста | **ЗАКРЫТ и ПРИНЯТ 2026-08-01** (Task 1-16, [приёмочная матрица](./specs/prd-29/acceptance.md), коммит `8e0a0e9`; дефект приёмки D-4 добит 2026-08-06 коммитом `6637acd`) — закрыты типы толкования и поиск исхода, достижимый диапазон шкалы, рампа цветов уровней, сборка строки измерения, хранение (`scales.config_json` + новая `result_variables.config_json`, трёхпозиционная видимость вместо булева флага; drizzle-миграция `0006` с переносом данных), сбор рекомендаций из трёх источников, контекст итогов и проброс измерений на веб-хосте, разметка и стили экрана итогов, параметры варианта дизайна, SCORM-пакет, редактор шкалы и толкований, сверка литералов формулы с перечнем исходов, приёмка на обоих хостах (5 дефектов вскрыты и закрыты). Трек вскрыл [PRD-32](./specs/prd-32/feedback-assets-delivery.md). См. [PRD-29](./specs/prd-29/measurement-results.md), [план](./plans/PLAN_PRD29_MEASUREMENT_RESULTS.md). | PRD-2, PRD-5, PRD-12, PRD-22, PRD-23, PRD-27 |
| — | PRD-30 | Порядок выдачи вопросов в теме | **РЕАЛИЗОВАН 2026-08-01** — этапы Э0-Э7: эскиз, миграция `0007` (`questions.order_index` + `test_sections.question_order`), движок `shared/draw/order-questions.ts`, веб-сборка попытки и адаптивная ветка, JS-двойник пакета с golden-паритетом, редактор (свитч темы, поле «Индекс в теме», колонка банка, предупреждение для `linear_flat`), книга Excel. Приёмка пройдена на всех трёх путях: веб-прохождение в браузере, экспортированный ZIP в локальном плеере и отладочный плеер выдают 10 → 20 → 30 → 40 → без индекса. См. [PRD-30](./specs/prd-30/question-order.md). | PRD-11, PRD-12, PRD-15, PRD-16, PRD-17, PRD-18 |
| — | PRD-31 | Интервал между попытками и общая модель допуска | **ЗАКРЫТ 2026-08-06** (реализован 2026-08-01, Фазы 1-6) — общий движок допуска (барьер A «общий кулдаун» между назначениями + барьер B «интервал между попытками» внутри назначения), `attempts.assignment_id` с backfill, перенос кулдауна и счётчика попыток на назначение в вебе, проверка барьера B после `Initialize` в пакете по `suspend_data.attempts[].completedAt`, доверенное «сейчас» через общий `trusted-now.js`, авторский интерфейс. Приёмка веба и SCORM-пакета пройдена (§12.1 спеки): повторный вход в СВОЁ назначение кулдауном не блокируется, очередная попытка ждёт интервал и видит момент открытия. Остаток приёмки — барьер B при разведённых часах — закрыт 2026-08-06 подтверждением владельца продукта (§12.2 спеки): вердикт принят по доверенным часам портала, момент открытия напечатан с датой и временем. См. [PRD-31](./specs/prd-31/attempt-interval.md). | PRD-6, PRD-12, PRD-19, PRD-15 |
| — | PRD-32 | Доставка вложений обратной связи | **РЕАЛИЗОВАН И ПРИНЯТ 2026-08-02** — вложение стало обычным активом медиатеки: канонический адрес `url` в дескрипторе, немедленная загрузка из редактора со строгой проверкой на сервере (только PDF до 5 МБ), метка «Файл не загружен» и замена файла у дескрипторов, сохранённых до работы, индекс использования и права ученика для ПЯТИ мест хранения обратной связи (тест, раздел, тема, шкала, показатель), показ вложений в блоке «Материалы» на обоих хостах. Заодно закрыт этап Э4 медиатеки: упаковщик SCORM резолвит `/api/media/<id>` через реестр — до этого медиа, загруженное после медиатеки, В ПАКЕТ НЕ ПОПАДАЛО вовсе. Приёмка вскрыла три дефекта, все закрыты. См. [спеку](./superpowers/specs/2026-08-02-feedback-assets-delivery-design.md), [план](./superpowers/plans/2026-08-02-feedback-assets-delivery.md), [приёмку](./reports/prd32-feedback-assets-acceptance.md). Продолжение — консолидация ТЕКСТОВ обратной связи (принята 2026-08-03): тексты теста, темы и раздела теста показываются одним блоком без повторов на обоих хостах и в обоих режимах и печатаются в PDF-отчёте; правило видимости сведено к одному (молчим только при явном успехе, неопределённость — в пользу показа); попутно починена выгрузка PDF в вебе у тестов со шкалами и снят per-topic слот, который был пуст всегда. См. [план](./superpowers/plans/2026-08-02-feedback-texts-consolidation.md), [приёмку](./reports/feedback-texts-consolidation-acceptance.md). | PRD-29, PRD-15, медиатека |
| — | PRD-33 | Листинг кода в тексте задания | **НЕ НАЧАТ** — необходимость подтверждена владельцем продукта 2026-08-01, требования не собирались. Повод вскрыт конкурентным анализом. См. [PRD-33](./specs/prd-33/code-listing.md). | PRD-12, PRD-15, PRD-14, PRD-27 |
| — | PRD-34 | Защита текста задания от копирования | **РЕАЛИЗОВАН и влит в main 2026-08-02** (merge `029a292`, правки до 2026-08-05), **приёмка ЧАСТИЧНАЯ** — этапы Э0-Э10: чистое ядро периметра `shared/template/protection/` (построитель без DOM + применение инлайн-свойствами, слушатели `copy`/`cut`/`contextmenu`/`dragstart` на корне сцены, впрыснутое правило `@media print` — поэтому внешний шаблон PRD-3 получает защиту, ничего о ней не зная), три настройки теста (миграция `038`), блок «Защита» в редакторе, оба хоста, отключение в отладочном прогоне PRD-18, водяной знак с идентификацией участника, скрытие задания при потере фокуса. Требования согласованы 2026-08-02 (переформулировка раздела 2: запрет недостижим, речь о цене бытового копирования и об атрибуции утечки, плюс девять решений раздела 10). Не проверено ([отчёт](./reports/prd34-copy-protection-acceptance.md) §2): полный круг настроек AC-08, экран обзора на веб-хосте, AC-04 живьём в отладочном плеере, мобильное поведение FR-27 (нужны iOS/Safari), печать вживую, PDF-отчёт при включённой защите AC-02, внешний шаблон через реестр AC-07. Пакет принимался локальным плеером — стенд WebTutor тогда был недоступен, с 2026-08-06 доступен. См. [PRD-34](./specs/prd-34/copy-protection.md). | PRD-12, PRD-19, PRD-27, PRD-18, PRD-3 |
| — | PRD-35 | Радар компетенций | **РЕАЛИЗОВАН 2026-08-01, приёмка частичная** — закрыты все девять задач плана: эскиз, чистое ядро `shared/template/radar-view.ts`, контракт `result.scalesChart` + `scalesBlockClass`, экран итогов веб-хоста (переключатель `showCompetencyRadar` в `settings[]` варианта, разметка, стили), SCORM-паритет, материализация домена шкалы, показ описания свойства в редакторе, радар и шкалы в PDF-отчёте со СВОИМ переключателем. 34 модульных теста, `tsc` чистый. Браузерная сверка нашла и закрыла два дефекта подписей. НЕ проверено: живое прохождение (в dev-БД нет попыток по методике), радар в SCORM-плеере (демо-пакет без шкал), финальная растеризация PDF, предпросмотр отчёта (собирается без измерений). См. [PRD-35](./specs/prd-35/competency-radar.md), [план](./plans/PLAN_PRD35_COMPETENCY_RADAR.md), [отчёт о приёмке](./reports/prd35-radar-acceptance.md). | PRD-29, PRD-5, PRD-2, PRD-12, PRD-22, PRD-27 |
| — | — | Результат для LMS при нескольких попытках | **РЕАЛИЗОВАН 2026-08-15** — настройка теста `lmsAttemptResult` («Ограничения» редактора, миграция 0022): что SCORM-пакет отдаёт в LMS, когда попыток несколько, — лучшую по проценту или только что завершённую. Повод: пакет всегда подменял результат на лучший, тогда как стандарт этого не решает (SCORM не требует от LMS ничего о хранении истории), а платформы — Moodle, Blackboard, Teachbase — держат выбор «лучшая / последняя» у себя и ждут от содержимого данные ТЕКУЩЕЙ попытки; при подмене настройка LMS перебивается, а в отчёте попытки лежат ответы ЧУЖОЙ попытки. Умолчание для НОВЫХ тестов — `last` (миграция 0023): содержимое отчитывается за текущую попытку, а какую засчитать, решает LMS. Тесты, заведённые до колонки, остались на `best` (проставлено миграцией 0022) и поведения не меняют; в пакет выпекается только значение `last`, поэтому пакеты без настройки байт-в-байт прежние. При `last` повопросная детализация в `suspend_data` не хранится вовсе (её единственный потребитель — сборка interactions по лучшей). В вебе настройка не применяется: внешней системы там нет. Проверено в браузере на собранном пакете: при `best` в LMS уходит сохранённые 95 %, при `last` — текущие 25 % с вердиктом «не пройден». Вопрос был вынесен PRD-36 §12 как продуктовый. **Поправка к обоснованию в коммите `828a6040`:** там записано, будто на WebTutor «лучшей» не существует, потому что состояние не переживает границу попытки. Это неверно — граница обнуления проходит по НАЗНАЧЕНИЮ, а не по попытке (см. строку «Границы состояния прогона» ниже), так что настройка осмысленна и там. Само решение об умолчании принято владельцем продукта и не пересматривается. | PRD-36 |
| — | — | Границы состояния прогона в LMS (справка) | Состояние `cmi.suspend_data` живёт ВНУТРИ назначения (регистрации) и обнуляется при новом назначении, а не при новой попытке. Отсюда две непересекающиеся механики: интервал между попытками (барьер B PRD-31, ЧАСЫ) считается по `suspend_data.attempts[].completedAt` — приёмка на живом WebTutor пройдена 2026-08-06 (§12.1 спеки PRD-31); общий кулдаун между назначениями (барьер A PRD-6) считается по записям обучения портала — точность СУТКИ, матч по имени курса, разные назначения одного курса неразличимы (`docs/references/webtutor-learning-records-api.md`, вариант с `object_id` отклонён зондами 2026-07-16). Внутри назначения работают счётчик попыток, «лучшая попытка», якорь таймера PRD-20 и секционные бюджеты PRD-4. Записано потому, что старый замер 2026-06-04 («suspend очищается на новой попытке») читается шире, чем следует, и уже привёл к ошибочному выводу, будто барьер B и лимит попыток на WebTutor не работают. | PRD-6, PRD-31, PRD-36 |
| — | PRD-36 | Компактность состояния прогона в SCORM-пакете | **РЕАЛИЗОВАН и ВЛИТ в `main` 2026-08-15** (merge `78ba8d77`, задачи 1-10 [плана](./plans/2026-08-15-prd36-run-state-compactness.md)). После слияния — четыре правки: бюджет состояния берётся по профилю выгрузки (`3e11ca51`: 4096 — предел SCORM 1.2 и проектная цель, но резать по ней в профиле 2004, где LMS принимает 64000, значит выбрасывать данные, которые LMS готова хранить; спека поднята до 1.2), приведение формата 1 к формату 2 сохраняет остаток времени по разделам PRD-4 (`ef5672e3`: поле пишет таймер, а не учёт попыток, поэтому его не было ни в одном требовании), отпечаток пакета учитывает состав банка вопросов, а не только количества (`9164a760`: замена вопроса в банке не меняла количеств, и восстановленные позиции указывали на другие вопросы; заодно заведён страж на новые поля состояния), и настройка результата для LMS (строка выше). Спека — версия 1.3. Приёмка В БРАУЗЕРЕ пройдена 2026-08-15: пакет проигран headless-хромом в локальном SCORM-плеере, 23 проверки (формат состояния, бюджет, отсутствие текстов, детализация лучшей, interactions и балл в LMS, продолжение прерванного прогона с тем же порядком вариантов, приведение состояния старого пакета). Ею же найден и починен дефект кодека: у вопроса на сопоставление перемешиваются ДВЕ колонки независимо, и карта-пара терялась — карточки после перерыва вставали в авторском порядке. На стенде WebTutor приёмка НЕ проведена. Замер после правки: контрольный состав §11.3 (60 вопросов, 10 тем, 5 шкал, 5 показателей, 11 ключей) занимает 3651 символ из 4096 вместе с прогоном в работе; состояние перестало зависеть от числа попыток; на составе крупнее (20 тем, 20 ключей — 5484) срабатывает объявленный порядок жертв, оставляя целыми прогон, счётчик, таймер и обе даты барьеров. Требования согласованы 2026-08-03, актуализированы по коду 2026-08-15 (версия 1.1, затем 1.2 — два бюджета состояния, и 1.3) (анализ трекерной записи [#38](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/38)). Повод вскрыт разбором профиля SCORM 1.2: в `cmi.suspend_data` складывается полное содержимое выданных вопросов, размер растёт линейно с числом попыток и молча пробивает предел (64000 в 2004, 4096 в 1.2), после чего разом теряются счётчик попыток, история, таймер и оба барьера; замер 2026-08-14 — тест на 48 вопросов пробивает предел на ТРЕТЬЕЙ попытке. Целевая модель: содержимого вопросов в состоянии нет, вопрос адресуется позицией в `TEST_DATA`, массив попыток заменяется тремя полями (счётчик, лучшая, последняя). Правки версии 1.1: дубль охватывает не только вопросы, но и названия тем, рекомендованные материалы, правила порогов и тексты ключей разреза (FR-01/FR-02 расширены); в модель введены записи разрезов PRD-50 и идентификатор выданного варианта PRD-17/PRD-24; принята плотная кодировка рядов (§4.6) — без неё контрольный тест не влезает в 4096 примерно в полтора раза, перемешивание хранится картой-строкой (зерно ГПСЧ отклонено); в риски внесено отсутствие автотестов рантайма (харнесс до правки формата, NFR-05). Заодно снимает ТРИ дефекта: статусы вопросов не сохраняются с попыткой, перемешивание вариантов не переживает продолжение прогона, порог `by_variant` PRD-24 после восстановления молча деградирует до общего. См. [PRD-36](./specs/prd-36/run-state-compactness.md). | PRD-19, PRD-20, PRD-31, PRD-6, PRD-18, PRD-17/PRD-24, PRD-50 |
| — | PRD-37 | Профиль экспорта SCORM 1.2 | **НЕ НАЧАТ, решения о запуске нет** — трек заведён 2026-08-03 по итогам технического разбора: объём и перечень потерь описаны, продуктового решения нет, нужен заказчик с LMS, принимающей только 1.2. Профиль деградированный: нет `score.scaled`, нет раздельных вердикта и завершения, нет типа взаимодействия `other` (им уезжают шкалы PRD-5 и показатели PRD-2), нет упорядочивания в манифесте (пороги по темам и по вариантам PRD-24 наружу не уходят), нет скрытия навигации LMS. Условие входа — PRD-36. См. [PRD-37](./specs/prd-37/scorm12-export-profile.md). | **PRD-36 (жёсткое)**, PRD-18, PRD-12, PRD-24, PRD-34 |
| — | PRD-38 | Медиа вопроса как часть вопроса и вид аудио-вопроса | **РЕАЛИЗОВАН и влит в main 2026-08-04** (ветка `prd-38-question-media`, приёмка ПРИНЯТА С ЗАМЕЧАНИЯМИ — расхождений с эскизом нет, замечания только к оснастке приёмки). Слот медиа переехал внутрь блока вопроса на обоих хостах через общий рендерер PRD-12, добавлен полноэкранный просмотр и плашка аудио-плеера, карточки ответов растянуты на всю ширину освободившейся колонки. См. [PRD-38](./specs/prd-38/question-media-placement.md), [отчёт приёмки](./reports/prd38-question-media-acceptance.md). | PRD-12, PRD-26, PRD-34 |
| — | PRD-39 | Слияние карточек сопоставления вместо стрелки | **РЕАЛИЗОВАН и влит в main 2026-08-04** — `renderMatching` печатает режим слияния (единая панель вместо пары карточек со стрелкой-коннектором) в общем рендерере PRD-12, зазор и вердикт слитой панели доведены в обоих поставляемых шаблонах («Стандартный», «Сертификация»). См. [PRD-39](./specs/prd-39/matching-connector-merge.md). | PRD-12 |
| — | PRD-40 | Период охлаждения по исходу попытки | **ЗАКРЫТ и влит в main 2026-08-05** — барьер A (PRD-6/PRD-31) получил раздельные периоды кулдауна на успех/провал (`cooldownByOutcome` + split-поля в `retakePolicySchema`), резолвер `resolveCooldownDays` читает исход из `AttemptFact` на обоих хостах, `webtutor_cooldown` резолвит период по `passedStateIn`; легаси-плагин `suspend_data_cooldown` снят вместе с его best-effort баннером. См. [PRD-40](./specs/prd-40/cooldown-by-outcome.md). | PRD-6, PRD-31 |
| — | PRD-41 | Moodle cooldown plugin | **ПРЕДЛОЖЕН, требует исследования — не начат** — заведён 2026-08-04 как переиспользование контракта `EligibilityPlugin` (PRD-6) и архитектуры реестра плагинов (PRD-21) для интеграции с Moodle. См. [PRD-41](./specs/prd-41/moodle-cooldown-plugin.md). | PRD-6, PRD-21 |
| — | PRD-42 | Материалы обратной связи — только ссылки, без загрузки файла | **РЕАЛИЗОВАН и влит в main 2026-08-05** (ветка `prd-42-feedback-links`) — секция «Материалы» отдаёт ссылку вместо загрузки PDF в медиатеку (`purpose=feedback-asset` снят с `/api/media/upload`), поле переименовано «Документы» → «Материалы» в редакторе и превью; следом закрыт защитный стиль копирования в PDF-отчёте (`205fe2b`). См. [PRD-42](./specs/prd-42/feedback-assets-links-only.md). | PRD-32, PRD-27 |
| — | PRD-43 | Быстрый переход к следующему вопросу — отдельный переключатель | **ЗАКРЫТ и влит в main 2026-08-05** — `quickAdvance` вынесен из-под `allowReturnToUnanswered` (PRD-19) в независимый переключатель настроек теста, проведён через редактор, `apiToEditorModel`, сервис настроек, SCORM-экспорт и `buildQuestionNav` рантайма обоих хостов. См. [PRD-43](./specs/prd-43/quick-advance-toggle.md). | PRD-19 |
| — | PRD-44 | Тип вопроса «Распределение баллов между шкалами» | **ТРЕБОВАНИЯ СОГЛАСОВАНЫ 2026-08-06, ФАЗЫ 0-6 ЗАКРЫТЫ, фаза 7 почти закрыта** (идея зафиксирована 2026-08-05) — constant-sum/ipsative-распределение фиксированного бюджета баллов между шкалами (референс — форс-чойс методики вроде Шварца): в отличие от PRD-26 один ответ измеряет НЕСКОЛЬКО шкал с зависимыми весами. Закрыты ядро (тип `allocation`, вклад в шкалы с доменом, паритет JS-двойника), согласованный эскиз, DS-компонент `BudgetAllocation`, экран учащегося на обоих хостах, редактор с проверкой выполнимости, книга Excel, ведущая и слабая шкала (`topScale`/`bottomScale`). Остаток — фаза 7 (телеметрия, аналитика, инспектор, снапшоты, демо-вопрос шаблона) и приёмка A-08 (порог покрытия). Приёмки A-02 - A-05 закрыты, прогон на WebTutor снят решением владельца. Трек попутно снял три дефекта старше себя: измерительный вопрос уезжал в LMS как `incorrect`, недоступный хвост ползунка сливался с дорожкой в светлой теме, обложка измерительного теста обещала проходной балл. См. [PRD-44](./specs/prd-44/scale-allocation-question-type.md), [план](./specs/prd-44/implementation-plan.md). | PRD-5, PRD-26 |
| — | PRD-45 | Редактор уровней шкалы — пороги вместо пар границ | **РЕАЛИЗОВАН и ПРИНЯТ в браузере 2026-08-07**, влит в `main` слиянием `1fb9b283`. Автор задаёт ряд «Начало — пороги — Конец», поэтому перекрытия и разрывы уровней стали непредставимы по построению; лента покрытия показывает тон, выведенный по направлению шкалы. Тем же треком добавлены два гарда (`npm run check:guards`): числа автора разбираются только через `parseAuthorNumber`, контраст измеренных цветовых пар не ниже порога. См. [PRD-45](./specs/prd-45/scale-levels-editor.md). | PRD-29, PRD-5, PRD-2 |
| — | PRD-46 | Выбор диаграммы профиля: радар или роза | **РЕАЛИЗОВАН 2026-08-08, приёмка НЕ проводилась** — влит в `main` целиком (девять коммитов, `e228257d`…`c215844e`): роза для ипсативных методик и правило выбора вида, предел оси радара (§6), цвет и пиктограмма шкалы, задаваемые автором (§7). Паритет веб ↔ SCORM-пакет держится по построению: карта облика применяется в `buildScalesChart`, одной точкой на оба хоста. PDF-отчёт в трек не входит — выделен в PRD-47. Приёмка — по §9 спеки. Дефект совместимости, найденный приёмкой PRD-47, закрыт 2026-08-09: умолчание манифеста (`scalesChartKind: "none"`) молча отменяло легаси-переключатель `showCompetencyRadar` в отчёте, и тест, настроенный до PRD-46, переставал печатать диаграмму без единой правки автора; заодно редактор перестал отбрасывать ветку настроек отчёта без ключа варианта. См. [PRD-46](./specs/prd-46/scales-chart-choice.md) §4.1, [отчёт о правке](./reports/prd46-legacy-chart-kind-fix.md). | PRD-35, PRD-29, PRD-5, PRD-12 |
| — | PRD-47 | Диаграмма профиля в PDF-отчёте | **РЕАЛИЗОВАН и ПРИНЯТ в браузере 2026-08-09** — отчёт печатает блок измерений и диаграмму одинаково в вебе и в пакете (до этого пакет не получал измерений ВОВСЕ), облик шкал приходит с экрана итогов, «авто» в отчёте получает признак ипсативности, предпросмотр и смоук показывают измерения из демо-набора шаблона, а настройки отчёта переехали на вкладку «Оформление» в свой пункт рейла. Печать вне рамок трека (решение владельца). См. [PRD-47](./specs/prd-47/report-scales-chart.md), [план](./plans/2026-08-08-prd47-report-measures.md), [отчёт о приёмке](./reports/prd47-report-measures-acceptance.md). | PRD-46, PRD-27 |
| — | PRD-48 | Перенос теста между инсталляциями без потерь | **ЗАКРЫТ 2026-08-12** — двумя документами. (1) [Полный обратимый перенос книгой Excel](./specs/prd-48/test-workbook-full-transfer.md): этапы Э1-Э6 реализованы 2026-08-10/11, приёмка Э3-Э6 пройдена на dev-стенде — листы «Настройки», «Обратная связь», «Рекомендации», «Страницы», «Поля страниц», «Адаптивные уровни», «Оформление»; побочный итог — заработала книга адаптивного теста. (2) [Перенос формата `.tbtest`](./specs/prd-48/test-transfer.md): пакет сериализует строки таблиц ЦЕЛИКОМ (основа — `TestSnapshotContent`), а не перечень полей, поэтому новая колонка едет сама; перечень ТАБЛИЦ защищён гардом `test-transfer-completeness.test.ts`; идентификаторы сохраняются; удаление показывается до применения; форма выборочного импорта принята в браузере на dev. Критерий приёмки — перенос в ЧИСТУЮ базу: круг на одной базе проходит, даже если пакет не несёт ничего. Долгов не осталось, шесть решений владельца закрыты 2026-08-12. Книга Excel форматом переноса не станет и остаётся форматом СОДЕРЖАНИЯ. | PRD-14, PRD-15, PRD-27, PRD-29 |
| — | PRD-49 | Настраиваемые заголовки блоков итогов | **РЕАЛИЗОВАН и влит 2026-08-12, приёмки в браузере НЕ было** — заголовки блоков перестали быть зашитыми в макет: надписи разрешаются по экранам на обоих хостах и доезжают до адаптивных итогов, итогов раздела, отчёта и шаблона «Сертификация»; у отчёта свой слой переопределений, зонтик «Ваш результат» печатают все четыре макета отчёта. Слоты карточки показателя (название, значение, уровень, пояснение) выключаются независимо и гаснут в том числе в PDF. Перечень надписей отчёта СЧИТАЕТСЯ по макетам (`readReportLabelKeys`, поле `reportLabelKeys` в `GET /api/templates/:id`), а не объявляется вторым списком в манифесте. См. [PRD-49](./specs/prd-49/results-headings.md), [план](./plans/2026-08-12-results-headings.md). | PRD-27, PRD-29, PRD-12, PRD-23 |
| — | PRD-50 | Разрезы результата: подытоги по ключам, блоки разделов, свои подписи вердикта | **РЕАЛИЗОВАН ЦЕЛИКОМ, ПРИНЯТ и ВЛИТ в `main` 2026-08-15** (спецификация согласована 2026-08-13) — повод вскрыт разбором отчётов и книги сертификационного теста руководителей. Закрыты Э1 (движок разрезов, агрегат по разделам и тесту, подача ключей обоими хостами, полосы разреза в карточке темы на итогах и в PDF, заморозка разреза в снимке), «Э1-доделка» (адаптивный режим на обоих хостах, сохранение записей области теста, полный контракт строки §8.1, аналитика SCORM), Э2 (пороги ключей у раздела, вердикт темы с учётом ключей, таблица «раздел — ключ», предупреждения публикации), Э3 и Э4 (группы разделов, надписи вердикта, блок области теста) и Э5 (сводный разрез на адаптивных итогах и в отчёте — записи для адаптивной попытки считались с «Э1-доделки», но обратно их до этого этапа никто не читал). Приёмки в браузере пройдены по всем этапам: [Э2](./reports/prd50-e2-acceptance.md), [Э3 и Э4](./reports/prd50-e3-e4-acceptance.md), [Э5](./reports/prd50-e5-acceptance.md); последняя перепроверила Э3 и Э4 на коде с Э5. Открытые хвосты, трек не держащие: [#36](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/36) (сквозной прогон адаптивного теста не на чем провести — все адаптивные тесты стенда непроходимы; проводка Э5 закрыта модульными проверками на каждом шве), [#37](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/37) (разрыв страницы отчёта внутри длинного блока не отыгран) Долг Э2 (явное «не проверять» у ключа не переживало сохранения) снят 2026-08-15: наблюдаемого следа он не давал, но ловушка сработала бы при появлении редактора умолчания. См. [PRD-50](./specs/prd-50/result-breakdowns.md). | PRD-49, PRD-29, PRD-24, PRD-17 |
| — | PRD-51 | Отчёт как документ из блоков | **РЕАЛИЗОВАН ЦЕЛИКОМ, ПРИНЯТ и ВЛИТ в `main` 2026-08-16** (56 коммитов, 143 файла, база трека `302f24d1`, последний коммит `e272cb2f`) — повод тот же, что у PRD-50: разбор двух референсных отчётов сертификационного теста руководителей. Здесь закрыта вторая половина разбора: третий лист референса контрактом не выражался, и владелец решил править платформу, а не обходить ограничение. Отчёт перестал быть одной раскладкой `report.html` — документ собирается из упорядоченного списка блоков (закрытый реестр десяти системных блоков плюс `page` и `page-break`, `shared/report/report-blocks.ts`), ОДНА функция разрешает документ на все три выдачи (веб-PDF, SCORM-пакет, предпросмотр), блоки хранятся своей таблицей `report_blocks` (миграция `0024`) по ветви на режим теста и сохраняются в ОДНОЙ транзакции с остальным ящиком настроек. Закрыты Э1-Э2 (движок документа, хранение, эталон разобран на 21 раскладку — 12 обычных и 9 адаптивных), Э3 (редактор документа в карточке «Отчёт о результатах»: список блоков, палитра вставки, перетаскивание — тем же способом, что структура теста), Э4 («Сертификация» получила СВОЙ светлый документ отчёта; из-под правила паритета шаблонов выведено всё семейство `report`/`report.adaptive`/`report.block`, а не только блоки) и Э5 (контракт формата шаблонов и руководство разработчика подняты до 2.0.0, оба PDF пересобраны: вид `report` перестал быть страницей отчёта и стал ОБОЛОЧКОЙ документа — впервые за трек изменение не обратно совместимо по смыслу, хотя прежние шаблоны продолжают печататься). Приёмки в браузере пройдены по всем этапам: [Э1-Э2](./reports/prd51-e1-e2-acceptance.md), [Э3](./reports/prd51-e3-acceptance.md), [Э4](./reports/prd51-e4-acceptance.md), [Э5](./reports/prd51-e5-acceptance.md), плюс [приёмка пяти хвостов](./reports/prd51-tails-acceptance.md), вскрывшая два настоящих дефекта — оба исправлены: веб-выдача PDF печатала лист с ОДНИМ логотипом (сервер разрешал только оболочку и остался на цельной раскладке, когда эталон уже переехал на блоки; расхождение двух выдач не видел ни один тест) и блоки разделов PRD-50 не перекладывались во вход результата, из-за чего документ показывал иное, чем экран, с которого его скачали. Осталось: одиночный `npm run test:cov` ([§0.0.1](#001-треки-в-работе--что-осталось)). Вне границ трека — [#39](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/39): перенос теста между инсталляциями документ отчёта не забирает. Расхождение механик экрана итогов и документа принято сознательно и записано долгом ([§0.3](#03-технический-долг)). После слияния — правка 2026-09-01 (шаблон «Сертификация» 1.11.0): титул занимает ЛИСТ ЦЕЛИКОМ — белая плита росла по содержимому, и первый лист оставался пустым больше чем наполовину (382 px при 816 px полезной высоты), тогда как референс печатает титул страницей; приёмка Э4 этого не поймала, потому что сверяла состав узлов и цвета, а не заполнение листа. Тем же заходом вид отчёта переименован в «Отчёт: сертификация»: прежнее «Отчёт: стандартный», унаследованное от эталона, читалось как признак стандартного ШАБЛОНА. См. [PRD-51](./specs/prd-51/report-document-blocks.md). | PRD-50, PRD-49, PRD-27, PRD-22, PRD-19, PRD-1 |
| — | PRD-52 | Рецензирование теста экспертами | **В РАБОТЕ, этапы Э0-Э6 закрыты** (ветка `feat/prd-52-test-review`, 28 коммитов). Повод: автор не мог показать тест предметникам до публикации — единственный способ пройти его заранее, отладчик PRD-18, заперт за edit-доступом, то есть открыт только тем, кто и так может тест править, а замечания жили в переписке и терялись. Решение собрано из уже существующих механик: доступ даёт третий уровень гранта теста (`review`, роль НЕ заводится — у внешнего рецензента она `learner`), ссылка — существующий магик-токен с назначением `purpose`, окно рецензента — тот же движок прогона, что у отладчика, с урезанным до трёх вкладок инспектором, полная выдача банка темы — второй hash-флаг рядом с пином варианта PRD-17/18 (в проде инертен). Комментарий цепляется к СУЩНОСТИ (вопрос, страница, тема, экран), несёт снимок контекста строкой и пин содержимого, по которому автор видит «изменено после комментария»; ветка в один уровень, исход «учтено»/«отклонено» ставит только тот, кто правит тест, при отклонении ответ обязателен, чужой комментарий не удаляет никто. Миграция `0025` (`test_review_comments`, `purpose` и обнуляемый `assignment_id` в токенах). ОТЛОЖЕНО до перестройки ящика редактора из соседнего трека: вкладка «Комментарии» в ящике и разбор ссылки `?review=<id>`. Впереди — приёмка в браузере. См. [PRD-52](./specs/prd-52/test-review.md). | PRD-28, PRD-18, PRD-15, PRD-13 |
| — | вне PRD | Настройка «Тест пройден, если» | **РЕАЛИЗОВАНА и влита 2026-08-12** — правило вердикта не хранилось ВОВСЕ: политика зашита в код. Заведена колонка `tests.pass_decision_policy` с засевом по существующим тестам, вердикт считается выбранной политикой в общем движке, пакет судит попытку той же политикой, что и веб, настройка сохраняется, применяется и переносится. Контракт — в [test-editor-contracts.md](./architecture/test-editor-contracts.md). ВНИМАНИЕ: вердикт существующих тестов мог измениться — засев ставит явную политику там, где раньше действовало неявное правило. | PRD-10, PRD-24, PRD-29 |
| — | вне PRD | Аналитика измерительных прогонов | **РЕАЛИЗОВАНА и влита 2026-08-13** — прохождение измерительного теста регистрируется целиком (до этого измерительный прогон в аналитику не попадал), окно «Детализация попытки» показывает ответ, шкалы и показатели; эскиз согласован. Попутно правило «есть ли что оценивать» вынесено в `pass-rule`. | PRD-5, PRD-2, PRD-26, PRD-44 |
| — | вне PRD | Перестройка настроек редактора теста и подытоги без вердикта | **В РАБОТЕ, закрыты Э0-Э3, Э5, Э6 и Э7.1-Э7.3** — повод: [аудит настроек редактора](./reports/AUDIT_TEST_EDITOR_SETTINGS.md), 21 находка и целевое дерево (§3.1); владелец принял 26 решений по составу и именованию и 34 эскиза (2026-09-03). Сделано: ящик перестроен с пяти вкладок на СЕМЬ (Основное · Состав и сценарий · Правила прохождения · Оценка результата · Обратная связь и итоги · Оформление · Комментарии), у каждой свой рейл; у подтемы (тега вопросов) СНЯТ вердикт — она считается и показывается, но не судится (`applyBreakdownGate` убран из `shared/scoring/aggregate.ts`, колонка `test_sections.breakdown_rules_json` осталась легаси, а при публикации теста с заданными порогами выводится предупреждение «подтема больше не влияет на вердикт темы»); заведена обратная связь ПОДТЕМЫ (`test_sections.breakdown_feedback_json`, drizzle-миграция `0026`) с единым для обоих хостов правилом выдачи «результат по подтеме ниже общего проходного порога теста — при любом вердикте» (`shared/breakdown/feedback.ts`); обратная связь ТЕМЫ стала ЗАМЕНОЙ вместо сложения ([PRD-29](./specs/prd-29/measurement-results.md) §7.1a); тексты адаптивных уровней уехали из лестницы в карточку «По уровням», заведён реестр «По вопросам» (только чтение); карточка «Блоки итогов» из редактора убрана — группировку выполняет подтема-тег, данные уже собранных тестов сохраняются и печатаются. Э5 («Именование и книга») дал листу «Настройки» механизм алиасов (`SettingParam.aliases`: выгрузка пишет нынешнее имя, импорт принимает и прежние — навсегда) и переименовал два параметра — `Тип сценария` и `Показывать правильные ответы после ответа`, — а в интерфейсе свёл к одному слову подписи подытогов, правил оценки тем и макетов сценария. Э6 («Шаблоны и контракт») убрал вердикт подтемы из раскладок обоих поставляемых шаблонов, перевернул гард и поднял контракт формата до 3.0.0, «Сертификацию» — до 1.12.0. Закрыты все шесть намеченных находок аудита — Ф-1, Ф-2, Ф-8, Ф-10, Ф-13, Ф-21 ([§0.0.4](#004-технический-долг--открытые-пункты)). Остаток — в [§0.0.1](#001-треки-в-работе--что-осталось). [План](./plans/2026-09-02-editor-settings-restructure.md), [карта разделения работ](./handoff/HANDOFF-2026-09-03-split-map.md). | — |

**PRD-7 полностью закрыт 2026-05-28**: единый редактор тестов (фазы S0-S13).
Текущая модель и контракты редактора — в
[architecture/test-settings-parameter-structure.md](./architecture/test-settings-parameter-structure.md)
и [architecture/test-editor-contracts.md](./architecture/test-editor-contracts.md).
История реализации фаз — в git. Deferred (не блокируют MVP): S13.5b (G22
mapping-flow при смене design template) и S13.8b (G12 wf-basic-warning UX-notification).

---

## 0.1 MVP-срез: Storyline-MVP (критический путь)

**Граница MVP зафиксирована 2026-05-27.** Цель — максимально быстро довести продукт до
shippable-состояния с главной бизнес-ценностью BRD (Storyline-подобный сценарий через
страницу-маршрутизатор), отложив тяжёлые и несрочные тректы в post-MVP (§0.2).

**В MVP входят:**

- **PRD-7** — единый редактор: закрытие S10 (удаление legacy), S11 (acceptance) и
  две новые фазы **S12 (Design closeout)** + **S13 (Editor parity)** — открыты 2026-05-28
  по результатам аудита соответствия wireframes.
- **PRD-1 closeout** — редактор content-pages в «Структуре» (см. [PRD-1 spec](./specs/prd-1/templates-content-pages.md)).
- **PRD-4** — гибкий поток и разделы (`flowPolicy`, `section.*`, `sort_order`).
- **PRD-8** — сценарий через страницу-маршрутизатор (Storyline-сценарий).

**Критический путь MVP (порядок исполнения):**

| # | Шаг | Зона | Зависит от | Статус |
| --- | --- | --- | --- | --- |
| 1 | PRD-7 S10 (удаление legacy) **совмещённо с** closeout PRD-1 шаг 1 | frontend `tests.tsx` + секция «Структура» | S9 (закрыта) | **Выполнен 2026-05-27** |
| 2 | PRD-7 S11 — acceptance pass §10 | acceptance | шаг 1 | **Закрыта 2026-05-28** после успешного closeout S12 + S13 |
| 2a | **PRD-7 S12 — Design closeout** (FR-30/31/31a/33, удаление orphan `DesignSettingsDialog`) | frontend `design-section.tsx` + manifest schema | S11 (формально), wireframe `prd7-design-tab.html` (согласован 2026-05-21) | **Закрыта 2026-05-28** |
| 2b | **PRD-7 S13 — Editor parity** (8 sub-фаз: quick wins, feedback, per-topic limits, row-actions, router-mode, variant-replace, drawer chrome, cleanup) | frontend test-editor.tsx, basic-settings-section.tsx, start-pages-section.tsx, tests-list.tsx, feedback-editor-modal.tsx | wireframes согласованы 2026-05-21 | **Закрыта 2026-05-28** |
| 3 | PRD-1 closeout — остаток (manifest validation, приёмка), отметить PRD-1 closed | backend + docs | шаги 2a + 2b | Ожидает |
| 4 | PRD-4 — runtime потока + flow-настройки в редакторе («Сценарий») | backend + frontend | PRD-7 закрыт (S12 + S13) | **Закрыта 2026-05-29** — см. [PRD-4 spec v1.1](./specs/prd-4/course-flow-sections.md) |
| 5 | PRD-8 — router-runtime + вкладка «Структура» в router-режиме | backend + frontend | PRD-4, PRD-1 | **Закрыта 2026-05-29** — cross-PRD реализация |

После шага 5 — **Storyline-MVP shippable.**

**STORYLINE-MVP SHIPPABLE 2026-05-29** — все 5 шагов критического пути
закрыты. Сводка коммитов — в git-истории. Live-browser acceptance
(Playwright + axe + LMS smoke) — отдельный gate, не блокирует кодовый closeout.

**Ускоритель:** шаги 1 и 3 (PRD-7 S10 + closeout PRD-1) делались в одной
сессии/зоне — экономил двойной проход по `tests.tsx` и вкладке «Структура».

## 0.2 Post-MVP backlog

**Переприоритизирован 2026-05-29:** по бизнес-запросу пользователя пара PRD-2 +
PRD-5 поднята в верх — целевой сценарий «шкалы и итоговая категория передаются в
LMS без ручной постобработки Excel-выгрузки» (см. внешний `report_build`
postprocessor, который должен быть заменён SCORM-пакетом). PRD-2 и PRD-5 ведутся
**парным треком**, потому что DSL `result_variables` (PRD-2) обязан содержать
источники `scaleById(...)` и `countScales(...)` до момента сдачи PRD-5; иначе
формулы итоговых категорий невозможно реализовать без выкатки миграции (см. §3.4
и [example-mbi.md](./specs/prd-5/example-mbi.md)).

Граница раздела (сверено 2026-08-15): таблица ниже доведена до PRD-37 и дальше не продолжалась —
для PRD-38 и старше повод, решение и зависимости живут в самих спеках, а статус, как и для
остальных, в [§0](#0-текущий-статус-на-2026-09-01). Отсутствие строки здесь НЕ означает, что трек
не заведён.

Включается после Storyline-MVP, в порядке ценности/зависимостей. Ниже фиксируются повод,
принятое решение и зависимости трека; **актуальный статус — в [§0](#0-текущий-статус-на-2026-09-01)**,
пометки статуса в строках даются справочно.

| Приоритет | PRD | Что даёт | Зависит от |
| --- | --- | --- | --- |
| 1 | **PRD-2 + PRD-5 (парный трек)** — ЗАКРЫТ 2026-06-03 | `result.*` показатели + `scale.*` шкалы + передача итогов в LMS через pseudo-interactions; закрывает бизнес-запрос «без постобработки» | PRD-4 |
| 2 | **[PRD-10 (Цена ответа)](./specs/prd-10/graded-answer-scoring.md)** — ЗАКРЫТ 2026-06-04 | Градуированный балл: веса опций (`single`) + ступенчатая таблица «условие→балл» (`multiple`/`matching`/`ranking`); `Σ s` на теме/тесте, порог на `Σ s`, сертификация. Интернализует 2-й внешний постпроцессор (РТК), golden против `key_NEW`. Стадии 0-6. Серверный градуированный `check-answer.ts` и `scoreRatio`/«Частично правильно» в SCORM CMI доставлены позже, попутно с PRD-15 блоком D. Отложено (не-ядро, актуализация 2026-08-05, [issue #31](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/31)): preview-модалка балла; «Частично правильно» в мгновенном фидбеке веб-хоста для стандартного (не адаптивного) режима | BR-09; перед релизом РТК |
| 3 | **[PRD-11 (Квоты выдачи по тегам)](./specs/prd-11/tag-draw-quotas.md)** — ЗАКРЫТ 2026-06-04 | Опц. квота выдачи `[тег→N×режим]` на тему (стратификация по подтемам, режим `exact`/`min` на тег); тегирование вопросов (chip-инпут) как пререквизит; верифицировано в рантайм-плеере. Доставка, ортогональна скорингу | BR-10; перед релизом РТК |
| 4 | **PRD-6** — ЗАКРЫТ 2026-07-05 (Phase 1) | Retake gate / cooldown (compliance для корпоративных курсов) | PRD-4 |
| — | **[PRD-12 (Единый шаблонный рантайм рендера)](./specs/prd-12/web-runtime-parity.md)** — ЗАКРЫТ 2026-06-06, охват L2 | Один DSL-рендерер + публичный контекст для SCORM и веб; серверный расчёт `@shared`; убрал хардкод-рендер в обеих средах + единый DnD-движок + единый CSS-источник; поглощает платформенную часть PRD-3 | PRD-2/4/5/6/10/11; spec-template-platform |
| 5 | PRD-3 (сужен) | ТОЛЬКО админ-реестр и жизненный цикл ВНЕШНИХ шаблонных ZIP; платформа (DSL, registry, layout-контракты) перенесена в PRD-12 | PRD-1, PRD-12 |
| 6 | **PRD-N (Flat adaptive)** | Адаптивная выдача из общего pool без секционных границ — комбинация `(mode=adaptive, flowMode=linear_flat)`, отложена из PRD-4 v1.1 | PRD-4 |
| 7 | **[PRD-14 (Импорт/экспорт вопросов)](./specs/prd-14/questions-import-export.md)** — Фазы 0-2 реализованы 2026-06-10; мультилист (FR-15) — бэкенд 2026-06-10, авторский UI «Импорт» 2026-06-11 | Round-trip matching/ranking + сохранение нулей; теги, условная ОС, «Цена ответа», правило «пустая ячейка vs нет колонки»; шаблон Excel + предпросмотр dry-run + подсказки (FR-01..FR-14). Мультилист (FR-15.1..10): книга = один тест, лист `Вопросы`+`Шкалы`+`Показатели`+`Измерения`, ссылка по ID/локальному «Ключ строки», симметричный экспорт, round-trip (эндпоинты + парсеры + 30 тестов). Авторский UI: отдельный пункт «Импорт» в боковой навигации → страница на DS AppShell (инлайн), автоопределение листов, выбор/создание целевого теста, dry-run-предпросмотр (эскиз в `approved/`, +7 тестов). Импорт медиа — отдельный PRD | BR-11; `questions` + `storage` + `scales`/`result_variables` |
| 8 | **PRD-N (Медиатека и импорт медиа)** | Отдельный будущий PRD (вынесен из PRD-14 2026-06-10): реестр `media_assets`, медиатека (UI), человекочитаемый дескриптор, дедуп по checksum, защита раздачи `/uploads`, затем импорт медиа по дескриптору/companion-ZIP | формализация процесса медиа |
| 9 | **[PRD-15 (Владение контентом, целостность и публикация)](./specs/prd-15/content-ownership.md)** — ЗАВЕРШЁН ПОЛНОСТЬЮ 2026-06-13, миграции 019-028; блок D (оценка — свойство теста) готов целиком: `test_question_scoring` + умолчания (026), резолвер во всех потребителях, backfill (027), удаление колонок `questions.points`/`scoring_json` (T-40, миграция 028), лист «Оценка» книги Excel, клиентская вкладка «Оценка» с переносом конструктора градуировки; фаза 5 закрытия (приёмка E-1..E-13, [матрица](./specs/prd-15/acceptance-matrix.md), [RUNBOOK](./runbooks/prd15-content-ownership.md)) | Целостность (защита зависимых тестов + dry-run), публикация через снапшоты (неизменность опубликованного, доигрывание попыток на своей версии, экстренная переопубликация), владение темами (видимость, гранты user-only TD-01, двухрежимный отзыв, одноимённость), богатая обратная связь темы `feedback_json` (TD-02), оценка как свойство теста (переопределения, умолчания, эффективная сложность в адаптиве; вопрос — только контент) | PRD-13 (модель прав), PRD-14 (книга Excel), PRD-10 (цена ответа) |
| — | **PRD-9** — ЭТАПЫ 1-2 РЕАЛИЗОВАНЫ 2026-07-05 | Миграция bcrypt → `@vvlad1973/crypto` scrypt (единый шов, ленивый rehash при логине; `bcryptjs` — legacy-only до Этапа 3) | — |
| — | **PRD-21 (Администрируемый реестр eligibility-плагинов)** — выделен из PRD-6 2026-07-05, не начат | Админ редактирует endpoint'ы/фильтры/конфигурации плагинов допуска без изменения Core, выбор из >= 2 конфигураций в UI, тестовая проверка плагина | PRD-6, PRD-13, паттерн PRD-3 |
| — | **[PRD-22 (Поля контентной страницы и последовательности)](./specs/prd-22/page-fields-and-sequences.md)** — РЕАЛИЗОВАН и закоммичен 2026-07-23; этапы Э0-Э7 закрыты, приёмка галереи на локальном плеере пройдена | Повод: вариант «Галерея: карточка» недоступен автору. Отдельного вида страницы «галерея» нет — это вариант `info` со своим макетом; состав полей страницы задаёт ИСКЛЮЧИТЕЛЬНО манифест (`placeholders[]` — содержимое, новый `settings[]` — свойства); закрытый перечень типов с валидацией при загрузке И активации; последовательности страниц (индикатор и позиция вычисляются ядром, автор задаёт только идентификатор); хранение настроек в `content_pages.settings_json`. Структура теста и раскладка формы НЕ меняются. Продолжение — [план доработок предпросмотра и вариантов](./plans/PLAN_PRD22_PREVIEW_AND_VARIANTS.md) (Э1-Э10 закрыты): рельс предпросмотра от вариантов, текст предупреждений, сетка из 12 раскладок контентной страницы в обоих поставляемых шаблонах, нетихая подмена недоступного варианта вариантом по умолчанию + подсветка проблемных тестов в списке, `ColorPicker` на общий `Popover`, названия и порядок цветов по фактическому применению | PRD-1, PRD-3, PRD-12, PRD-19 |
| — | **[PRD-23 (Темы шаблона и раздел «Цвета»)](./specs/prd-23/template-themes.md)** — ЗАКРЫТ: этапы Э0-Э9 (контракт, хранение, отрисовка на обоих хостах, раздел «Цвета», предпросмотр, «Сертификация» 1.3.0, документы, приёмка SCORM-пакетом на локальном плеере); эскиз в `approved/`, этапы в [плане](./plans/PLAN_PRD23_TEMPLATE_THEMES.md) | Повод: `certification` поставляет светлую и тёмную палитры, а платформа о них не знает — автор не может ни зафиксировать вид теста, ни задать цвет на каждую палитру, переопределения печатаются инлайном и медиа-запросом не ограничиваются. Шаблон объявляет `themes[]` (закрытый перечень `light`/`dark`, валидация при загрузке И активации, сверка с токенами стилей); тест хранит вид (`light`/`dark`/`auto`) и цвета по темам; отрисовка ставит `data-theme` на корень сцены и печатает переопределения `<style>`-блоком последним в каскаде; цвета выносятся в отдельный раздел рельсы по ТИПУ параметра, пустые разделы скрываются. Шаблон без тем работает как сегодня, переиздание не требуется | PRD-3, PRD-7, PRD-12, PRD-22 |
| — | **[PRD-24 (Пороги прохождения по вариантам)](./specs/prd-24/variant-pass-thresholds.md)** — РЕАЛИЗОВАН 2026-07-24 (Task 1-9 плана: движок, веб-оценка, SCORM-рантайм, манифест, редактор, книга Excel, отладчик, приёмка); этапы в [плане](./plans/PLAN_PRD24_VARIANT_PASS_THRESHOLDS.md) | Повод: правило прохождения темы резолвится без знания выпавшего варианта, порог на тему один на все варианты — при разном достижимом максимуме баллов абсолютный порог даёт неравные шансы (риски PRD-17 R-8/R-9, частичный возврат к D-6). Правило прохождения темы получает четвёртый источник «по вариантам»: порог (`Процент` / `Сумма баллов`) на каждый вариант, применяется к ВЫДАННОМУ; ключ — стабильный `formId`, хранение в существующем `topic_pass_rule_json` (миграции нет); вся новая логика заперта в `resolveTopicRule` — оба хоста читают её единым движком; SCORM-рантайм начинает пиннить выпавший `formId`; обмен через новый лист книги `Пороги вариантов`. Калибровочные коэффициенты по-прежнему вне охвата | PRD-17, PRD-7, PRD-10, PRD-15, PRD-14 |
| — | **[PRD-25 (Домашняя страница)](./specs/prd-25/home-page.md)** — РЕАЛИЗОВАНА, приёмка в браузере 2026-07-30 | Повод: маршрут `/` был не страницей, а редиректом по наиболее привилегированной доступной области, поэтому у сервиса не было точки входа «продолжить работу»: автор попадал в общий список тестов, где его собственные незавершённые правки ничем не выделены, учащийся — в список назначений без приоритета незавершённой попытки, а мультиролевой пользователь обходил три раздела подряд; вдобавок редирект породил защиту от петли (`homePath()` может вернуть `null` → экран «нет доступа»). Решено: `/` — рабочий стол из секций, каждая со своим правом на просмотр, своим источником данных и своим пустым состоянием; состав секций определяется профилем прав (capability), а не ролью; признаки внимания (незавершённая попытка, дрейф пула, черновик без вопросов) вычисляются одним агрегатом `GET /api/home`; страница рендерится в авторской или учебной оболочке по профилю. Появление реальной страницы на `/` снимает класс случаев с редирект-петлёй. Вне охвата: SCORM-пакет (у пакета нет точки входа приложения) | PRD-13, PRD-15, PRD-16, PRD-18 |
| — | **[PRD-26 (Тип вопроса «шкала»)](./specs/prd-26/scale-question-type.md)** — ЗАКРЫТ 2026-07-30 | Повод: психодиагностические опросники (референс — опросник выгорания Маслач) состоят из утверждений с одинаковой упорядоченной шкалой ответа, но импортировались как `multiple` с шестью вариантами и рисовались шестью вертикальными карточками на каждом из 22 экранов. Механика ответа при этом не «выбор одного из равноправных вариантов», а «отметка на шкале»: порядок градаций содержателен, перемешивать их нельзя, а правильного ответа может не быть вовсе. Решено: пятый тип вопроса `scale` с двумя режимами — измерительный (правильного ответа нет: вопрос не проверяется, не приносит баллов и не влияет на процент, его результат — только вклад в шкалы и показатели) и проверяемый (ведёт себя как одиночный выбор, включая градуированную цену ответа); рисуется новым режимом выбора существующего DS-компонента `Stepper`; числовое значение градации учащемуся не показывается. Миграция БД не потребовалась. Вне охвата: матричная раскладка (модель «один вопрос — один экран» не меняется), автопереход по выбору градации | PRD-5, PRD-2, PRD-10, PRD-12, PRD-14 |
| — | **[PRD-27 (Шаблон отчёта о результатах)](./specs/prd-27/report-template.md)** — РЕАЛИЗОВАН и закоммичен 2026-07-31 | Повод: отчёт вынесен в `shared/report/` и заработал на обоих хостах, но ВЁРСТКА страницы осталась в КОДЕ — `report-html.ts` собирал её инлайн-стилями с зашитыми цветами и подложками из ассетов ядра. Отчёт оставался единственным ученическим артефактом вне модели шаблона: разработчик шаблона не мог ни изменить структуру, ни объявить настраиваемые поля, автор теста не мог выбрать вид отчёта, и для сертификационного теста PDF уходил с чужим брендингом. Решено: отчёт — часть шаблона; в `manifest.contentTemplates[]` заводятся ДВА типа (`report` для обычного режима и `report.adaptive` для адаптивного — разные типы, а не варианты одного), каждый с несколькими вариантами (`key`/`label`/`layoutFile`/`settings[]`); выбор варианта разрешает ОДНА функция `resolveReportBake` на всех хостах, иначе отчёт в LMS расходился бы с предпросмотром; вариант запекается в `TEST_DATA.designSettings.report`, а CSS — в `styles.css` пакета, потому что к моменту растеризации читать файлы рантайма поздно; отчёт по попытке со снапшотом собирается вариантом ИЗ ВЫДАЧИ, чтобы смена вида не переписывала задним числом уже выданные документы; деградация тянет и макет, и стиль. Формат шаблонов поднят до 1.1.0 | PRD-12, PRD-22, PRD-3, PRD-15 |
| — | **[PRD-28 (Внешний участник)](./specs/prd-28/external-participant.md)** — форма решения согласована 2026-07-30, реализация не начата | Повод: тест назначают людям извне, приходящим разово; ради одного прохождения заводится учётная запись сотрудника с паролем, восстановлением доступа и кабинетом, то есть у внешнего человека остаётся возможность войти в обход выданной ссылки. Решено: это ПРИЗНАК учётной записи, а не роль (роль остаётся `learner`, матрица прав не меняется) — иначе роль закодировала бы внутри себя конкретный тест и перестала быть набором прав, а повторный приход того же человека упёрся бы в тупик. Добавить нужно только признак «внешний», отказ входа по паролю и отсутствие пароля с восстановлением; область, назначения и попытки уже работают. Открытые вопросы (заведение, видимость в списках и аналитике, перевод из штатных и обратно) — в спеке | PRD-13, область ссылки-приглашения |
| — | **[PRD-29 (Итоги измерительного теста)](./specs/prd-29/measurement-results.md)** — РЕАЛИЗОВАН и ПРИНЯТ 2026-08-01 на обоих хостах; [план](./plans/PLAN_PRD29_MEASUREMENT_RESULTS.md), [приёмка](./specs/prd-29/acceptance.md) | Повод: шкалы (PRD-5) и показатели (PRD-2) считались, но не показывались — тогл у шкал был намеренно скрыт константой с пометкой «отдельный PRD», у показателей контрола не было вовсе, и измерительная методика выдавала ученику бессмысленное «верно 22 из 22». Сделано: общая модель толкования (интервалы для чисел, перечень исходов с кодами для строк — формула возвращает код, валидатор сверяет литералы), хранимый домен с предзаполнением по вкладам, благоприятное направление ОТДЕЛЬНО от `direction`, трёхпозиционная видимость, рампа цветов уровней по образцу условного форматирования, виды рендера с откатом по выполнимости (кольцо и градиент недостижимы автозаменой — процент при `normalization: none` не определён), ОДИН макет `results` с настройками `auto`/`show`/`hide`, рекомендации из трёх источников в единый блок, редакторы интервалов и исходов, три значения в книге Excel, миграции 036 + `drizzle/0006` с переносом данных. Подтверждено: домен по вкладам дал 45/25/40 — верхние границы интервалов методолога; третья шкала окрашена ЗЕРКАЛЬНО при одной схеме; вход построителя в пакете побайтно совпал с веб-хостом. Приёмка вскрыла 5 дефектов, все закрыты (главный — у показателя не было контрола видимости, то есть профиль нельзя было показать вовсе; и сводка с зелёным «Пройден» у измерительного теста с порогом по умолчанию). Ограничения: вложения обратной связи не доезжают до выдачи (см. PRD-32), своя схема цветов требует закреплённой темы, книга новые поля не переносит | PRD-2, PRD-5, PRD-12, PRD-15, PRD-22, PRD-23 |
| — | **[PRD-30 (Порядок выдачи вопросов в теме)](./specs/prd-30/question-order.md)** — РЕАЛИЗОВАН 2026-08-01, этапы Э0-Э7 закрыты ([эскиз](./wireframes/approved/prd30-question-order.html), миграция 0007, движок упорядочивания, оба хоста, редактор, книга Excel, приёмка) | Повод: порядок вопросов внутри темы всегда случайный и не отключается — перемешивание зашито в `drawSection` (`shared/draw/blueprint.ts`) и в `selectForm` (`shared/draw/forms.ts`, где ломается даже авторский список варианта PRD-17); авторского порядка в банке физически нет: у `questions` нет колонки порядка, а `getQuestionsByTopic` читает без `ORDER BY`. Решено: nullable `questions.order_index` («Индекс в теме», по образцу nullable `difficulty`) плюс `test_sections.question_order` = `random`/`fixed` (перечисление, а не булево — под будущее «по возрастанию сложности»); сортировка по возрастанию, NULL в конец, равные значения перемешиваются («эшелоны» вместо ложно-строгой очереди); поле влияет ТОЛЬКО на последовательность, отбор и квоты PRD-11 не трогаются; в режиме вариантов порядок берётся из списка варианта; `order_index` НЕ входит в `content_hash`, иначе перестановка ложно протухала бы `pinned_content_hash` PRD-15; в адаптиве сортировка внутри уровня. 2026-08-03: настройка действует и в `linear_flat` — рантайм пакета перемешивал плоский список вторым проходом и стирал уже выстроенный порядок (в вебе такого прохода не было), проход снят, предупреждение в редакторе убрано. Вынесено: перетаскивание с перенумерацией, разный порядок в разных тестах | PRD-11, PRD-12, PRD-15, PRD-16, PRD-17, PRD-18 |
| — | **[PRD-31 (Интервал между попытками и общая модель допуска)](./specs/prd-31/attempt-interval.md)** — ЗАКРЫТ 2026-08-06; реализован 2026-08-01, фазы 1-6, приёмка веба и SCORM-пакета пройдена (§12.1 спеки), остаток по разведённым часам закрыт (§12.2) | Повод: один переключатель PRD-6 означает на хостах разное. В SCORM гейт решает по записям обучения LMS, то есть кулдаун стоит на границе НАЗНАЧЕНИЯ, а внутри запуска пакет не ограничен ничем; в вебе тот же переключатель проверяется на старте ПОПЫТКИ и считает от `attempts.finished_at`, потому что связи попытки с назначением не существует. Тем же расхождением страдает `maxAttempts` (в SCORM — внутри регистрации, в вебе — за всю историю). Решено: назначение объявляется единицей допуска, барьер A «общий кулдаун» (дни) действует между назначениями, новый барьер B «интервал между попытками» (часы, 1–8760, независимый переключатель) — внутри назначения; барьеры действуют в непересекающиеся моменты, решение принимает общий движок, хосты поставляют только факты. Веб получает `attempts.assignment_id` (миграция, backfill NULL = легаси-бакет, резолв текущего назначения по позднейшему `assigned_at`), туда же переезжают кулдаун и счётчик попыток — смена поведения без компенсирующей миграции. В пакете барьер B проверяется ПОСЛЕ `Initialize` по `suspend_data.attempts[].completedAt` (NFR-01/02 барьера A не затронуты), «сейчас» берётся доверенным временем портала — `resolveToday` выносится из гейта в общий `trusted-now.js` и отдаёт полный timestamp. Блокировка показывается на штатных экранах, новых экранов нет. Приёмка подтвердила ключевое: повторный вход в СВОЁ назначение кулдауном больше не блокируется, а очередная попытка ждёт интервал и видит момент открытия с датой и временем. Попутно починен рассинхрон журнала миграций dev-БД (0006-0008 не были отмечены в ledger). Вне охвата: книга Excel, отдельный экран блокировки, ограничение возобновления прерванной попытки | PRD-6, PRD-12, PRD-19, PRD-15 |
| — | **[PRD-32 (Доставка вложений обратной связи)](./specs/prd-32/feedback-assets-delivery.md)** — РЕАЛИЗОВАН и ПРИНЯТ 2026-08-02 на обоих хостах; [спека](./superpowers/specs/2026-08-02-feedback-assets-delivery-design.md), [план](./superpowers/plans/2026-08-02-feedback-assets-delivery.md), [приёмка](./reports/prd32-feedback-assets-acceptance.md) | Повод двойной. Первый: автор прикладывал PDF к обратной связи, редактор показывал файл и давал сохранить, но загрузка на сервер НЕ была реализована с самого начала — файл выбрасывался, в модель уходил только дескриптор, и подсекция «Материалы» была пуста на обоих хостах. Второй, крупнее: после включения медиатеки загрузка отдаёт канонический адрес `/api/media/<id>`, а упаковщик SCORM знал только `data:` и легаси-префикс `/uploads/` — медиа вопросов, страниц и оформления В ПАКЕТ НЕ ПОПАДАЛО, пакет терял автономность, и дефект маскировался отладочным плеером PRD-18 (он играет same-origin с живой сессией). Сделано: адрес вложения живёт обычной строкой `url`, поэтому обходчик медиа обслуживает его без специальных веток; загрузка идёт в момент выбора файла с проверкой `purpose=feedback-asset` на сервере; упаковщик стал чистым обходом строк с портом-резолвером и распознаёт адрес через ОДИН общий распознаватель `media-refs`, который заодно отвечает за границу совпадения (иначе абсолютный URL на чужой хост портился); нерезолвленный адрес вычищается и попадает в лог — молчаливая потеря и была причиной, по которой дефект прожил незамеченным; индекс использования получил писателей на всех путях сохранения, включая каскады удаления темы и папки и дублирование темы, и покрыт полной пересборкой. Приёмка вскрыла три дефекта, все закрыты: вложение РАЗДЕЛА теста не индексировалось (поле `test_sections.feedback_json` оказалось write-only), вложение ТЕМЫ не показывалось на итогах, обратная связь ТЕСТА не доходила до «Материалов» без шкал и показателей. Ограничения: WebTutor недоступен — пакет принят локальным плеером; вложения по-прежнему только PDF до 5 МБ | PRD-29 (потребитель), PRD-15 (снимки), медиатека (Э4/Э6) |
| — | **[PRD-33 (Листинг кода в тексте задания)](./specs/prd-33/code-listing.md)** — необходимость подтверждена 2026-08-01, требования не собирались | Повод: автор не может показать участнику фрагмент исходного кода, а вопрос «что выведет этот фрагмент» — базовая механика ИТ-аттестации. Разметка авторского текста в `shared/text/` есть, но это подмножество markdown для ФОРМАТИРОВАНИЯ (жирный, курсив, ссылка, автоссылка, переносы) — кода в грамматике нет вовсе, поэтому листинг приезжает участнику пропорциональным шрифтом, с потерянными ведущими пробелами и подменённой пунктуацией. Вскрыто конкурентным анализом: у StartExam «вставка кода» заявлена как средство оформления содержимого задания. Проектирование обязано пройти пять препятствий, проверенных по коду: типографический проход подменяет кавычки и дефисы (код должен миновать его целиком, механизм маскировки в модуле уже есть для ссылок); эмфаза применяется после экранирования и съест `*ptr`; текст вопроса рендерится в `<h2>`, поэтому `<pre>` в нём невалиден — развилка «инлайн-код в вопросе против блочного контейнера в макетах обоих шаблонов»; автоподбор кегля считает длину в символах и на моноширинном тексте неверен; изменение грамматики двигает `content_hash` и ложно зажигает индикатор устаревших переопределений оценки (прецедент PRD-30 с `order_index`). Вне охвата: формулы LaTeX (соседний пробел, отдельная задача), автопроверка исполняемого кода, открытые задания | PRD-12, PRD-15, PRD-14, PRD-27, PRD-19, PRD-3 |
| — | **[PRD-34 (Защита текста задания от копирования)](./specs/prd-34/copy-protection.md)** — РЕАЛИЗОВАН и влит в main 2026-08-02, приёмка частичная (актуальный статус — [§0](#0-текущий-статус-на-2026-09-01)) | Повод: участник выделяет текст задания и вариантов и уносит его в поиск или в переписку — утекает не вопрос, а банк; ученический экран ведёт себя как обычная веб-страница, обработчиков `user-select`/`selectstart`/`contextmenu`/`copy` нет НИГДЕ, а нативное перетаскивание текста подавлено только на карточках сопоставления и ранжирования (`pointer-dnd.ts:224`). Вскрыто конкурентным анализом: строка «Защита от копирования текста» у нас «-» при «+» у Тестографа, а раздел «Контроль честности» матрицы StartExam мы проигрываем вчистую. Главное, что обязано попасть в постановку: «запретить копирование» невозможно (клиент обязан получить текст, чтобы его нарисовать: веб отдаёт весь выданный набор на старте попытки, пакет запекает банк ВМЕСТЕ С ключами в `TEST_DATA`), поэтому задача — поднять цену бытового копирования и сделать утечку атрибутируемой, а не «сделать текст некопируемым»; запрет снимка экрана в браузере недостижим в принципе (нет аналога системного флага исключения окна из захвата, PrintScreen до страницы не доходит, пакет играется в рамке на чужой странице LMS), и вместо него обсуждаемы водяной знак с идентификацией участника, скрытие задания при потере фокуса и отметка в протоколе. Препятствия, проверенные по коду: мера обязана быть одной на оба хоста; периметр нельзя ставить на корень сцены (те же тексты — на экране обзора PRD-19, в разборе ответов и в PDF-отчёте PRD-27, который растеризуется по тому же дереву документа); отладочный прогон PRD-18 обязан остаться копируемым для автора; на мобильном долгое нажатие — одновременно выделение и жест; инлайн-стили рантайма перебивают `theme.css`; настройка теста тянет миграцию, книгу Excel и снимок публикации; внешние шаблоны PRD-3 о новом классе не знают; фиксация попытки копирования — новый канал и персональные данные. Вне охвата: прокторинг (покупается интеграцией), антиплагиат, обфускация `TEST_DATA`, запрет копирования пароля (архитектурное требование 1.5, решается отдельно) | PRD-12, PRD-19, PRD-27, PRD-18, PRD-3, PRD-15, PRD-14 |
| — | **[PRD-35 (Радар компетенций)](./specs/prd-35/competency-radar.md)** — РЕАЛИЗОВАН 2026-08-01, приёмка частичная (актуальный статус — [§0](#0-текущий-статус-на-2026-09-01)) | Повод: шкалы показываются по одной — все виды рендера PRD-29 односкальны, поэтому методика из трёх шкал выглядит как три независимые карточки и профиль не читается; на референсной методике Маслача, где благоприятное направление третьей шкалы противоположно двум другим, список карточек скрывает именно то соотношение, ради которого методика и считается. Заведён решением по позиции BR-21-08 в порядке BR-21-12 (BR-24, Этап 29 BRD). Решено: лучами служат видимые шкалы PRD-5, но рендерер принимает ГОТОВЫЕ оси и об их происхождении не знает (темы как второй источник — следующий этап, адаптер сейчас не пишется); радиус — доля значения от домена БЕЗ инверсии по `valence`, потому что инверсия рассинхронизировала бы радар с `band_ruler` той же шкалы на том же экране и оторвала бы точку от подписи оси; оценку несёт тон вершины и метка уровня, из чего следует, что площадь фигуры не является метрикой «общей компетентности» при смешанных направлениях — такой суммы у методики и нет; при видимости `level` луч квантуется по середине зоны (продолжение дискретности PRD-29 §6.3), засечка точной позиции не рисуется; диаграмма стоит СБОКУ от карточек и не заменяет их, на узком экране схлопывается над ними; показ включается явным булевым переключателем, отдельно на `results` и отдельно на видах отчёта, по умолчанию выключенным — режима `auto` нет; домен МАТЕРИАЛИЗУЕТСЯ (запись при сохранении шкалы и при публикации снапшота), поэтому смешанный случай «часть шкал без домена» как класс не наступает, а правило допустимости сводится к «не меньше трёх видимых шкал»; геометрию и цвет считает чистое ядро `shared/template/radar.ts`, разметка получает готовые координаты (DSL — подмножество mustache, арифметики в нём нет), SVG статичный, без скриптов и библиотек. Вне охвата первого этапа: «Сертификация» (отстаёт от ревизии «Стандартного»), темы как источник осей, групповая аналитика и сквозной профиль по нескольким тестам | PRD-29 (домен, `valence`, тона, видимость), PRD-5, PRD-2, PRD-12, PRD-22, PRD-27 |
| — | **[PRD-36 (Компактность состояния прогона в SCORM-пакете)](./specs/prd-36/run-state-compactness.md)** — требования согласованы 2026-08-03, РЕАЛИЗОВАН и влит в `main` 2026-08-15, приёмка на стенде WebTutor впереди | Повод вскрыт разбором профиля SCORM 1.2 и оказался самостоятельным дефектом профиля 2004: `cmi.suspend_data` хранит полное содержимое выданных вопросов (`prompt`, варианты, эталон, обратная связь) по копии на каждую завершённую попытку плюс копию для прерванного прогона, потолка у массива попыток нет, и одна попытка теста на 25 вопросов занимает 35-75 тысяч символов при пределе 64000 — то есть предел пробивается на первой же попытке длинного теста, причём молча: LMS обрезает строку, разбор падает, и разом обнуляются счётчик попыток, история, якорь таймера PRD-20 и момент завершения для барьера PRD-31. Инвентаризация потребителей показала, что массив попыток не нужен ни одному из них (из него достают только максимум, хвост или длину), содержимое вопросов читает единственное место — сборка interactions по лучшей попытке, а сам `TEST_DATA` уже несёт все вопросы целиком, то есть хранится второй экземпляр рядом с первым. Решено: содержимого вопросов в состоянии нет, вопрос адресуется позицией в `TEST_DATA` (UUID в 36 символов съедал бы четверть бюджета на служебные строки), массив заменяется тремя полями — счётчик, лучшая, последняя, — детализация хранится только у лучшей, бюджет 4096 символов проверяется фактически с объявленным порядком жертв, формат версионируется и старое состояние приводится к новому. Правило «в LMS уезжает лучшая попытка» НЕ меняется: требования на него нет ни в одном PRD и веб-хост его не разделяет, но менять правило оценки заодно с оптимизацией хранения нельзя. Побочно закрываются два дефекта: статусы вопросов не сохраняются с попыткой (interactions прошлой попытки фильтруются статусами текущей), перемешивание вариантов не переживает продолжение прогона | PRD-19 (статусы), PRD-20 (якорь), PRD-31 и PRD-6 (даты барьеров), PRD-18 (отладочный плеер) |
| — | **[PRD-37 (Профиль экспорта SCORM 1.2)](./specs/prd-37/scorm12-export-profile.md)** — трек заведён 2026-08-03, продуктового решения о запуске нет | Повод: заявлен единственный профиль SCORM 2004 4th Ed., тогда как 1.2 остаётся самым распространённым в корпоративных LMS, и конкурентный анализ относит это к пробелам (iSpring и WebTutor принимают 1.2, 2004, AICC, xAPI, cmi5). Разбор 2026-08-03 показал, что профиль будет ДЕГРАДИРОВАННЫМ, и перечень потерь подлежит согласованию как часть требований, а не считается дефектом: нет `score.scaled` (баллы вырождаются в проценты), вердикт и завершение схлопываются в один статус, нет типа взаимодействия `other` — а им уезжают шкалы PRD-5, показатели PRD-2 и рекомендации тем, — нет `description` у взаимодействия (текст вопроса в отчёте LMS), результат называется `wrong`, а не `incorrect` (без перекладки ответы молча перестанут помечаться неверными), время в другом формате (таймер PRD-20 молча выродится в посессионный), в манифесте нет упорядочивания и навигации — пороги по темам и по вариантам PRD-24 наружу не уходят, а LMS покажет свою кнопку выхода посреди попытки. Жёсткое условие входа — PRD-36: предел `suspend_data` в 1.2 равен 4096 при нынешних 35-75 тысячах, разрыв на порядок, сжатие его не закрывает. Отдельные риски: кириллица (1.2 не обязывает UTF-8), отсутствие живого WebTutor для приёмки при более широком разбросе реализаций 1.2, и требование не ветвить ничего глубже адаптера среды выполнения и построителя манифеста. Открытые вопросы: задавать ли `adlcp:masteryscore` (задать — LMS переопределит вердикт и разойдётся с собственными правилами прохождения) | **PRD-36 (жёсткое)**, PRD-18, PRD-12, PRD-24, PRD-34 |

**Будущие BRD (требования на согласовании, реализация не начата):**
[«Аппарат психометрической аналитики тестов»](./specs/brd-psychometric-analytics.md) (v0.4) —
CTT, надёжность и анализ дистракторов поверх SCORM-телеметрии, импорта обезличенных выгрузок
LMS и веб-прохождений.

Нормативная сквозная модель расчёта — [scoring-model.md](./specs/scoring-model.md).

Парный трек PRD-2 + PRD-5 — внутренний порядок:

| Этап | Состав | Обоснование |
| --- | --- | --- |
| Этап A | Расширение DSL PRD-2: `scaleById(...)`, `countScales(...)`, поле `controls_status`; миграция `result_variables` | DSL должен знать `scale.*` до того, как Core начнёт публиковать значения шкал |
| Этап B | Реализация PRD-5: таблицы `scales` + `question_measurements`, runtime расчёта, авторская UI «Шкалы» и блок «Измерения» в карточке вопроса | Опирается на готовый DSL этапа A |
| Этап C | E2E acceptance по [example-mbi.md](./specs/prd-5/example-mbi.md); golden-тест замены `process_burnout_export.py` | Подтверждает закрытие бизнес-запроса |

**Все три этапа выполнены 2026-06-03** (коммиты `a6e3e32`…`ce7467e` A, `29282ab`…`a9bbc9a` B,
golden `tests/mbi-golden.test.ts` C). Шкалы test-scoped; **глобальная библиотека шкал** —
будущий PRD. Композитные шкалы (источник «Другие шкалы») и гард переупорядочивания опций
с измерениями — отложенные точечные пункты.

**Трек «Цена ответа» (PRD-10) + «Квоты выдачи» (PRD-11) — ЗАКРЫТ 2026-06-04.** Нормативная модель —
[scoring-model.md](./specs/scoring-model.md) §11 (v1.9: «Цена ответа» — веса опций / ступенчатая таблица);
спеки — [PRD-10](./specs/prd-10/graded-answer-scoring.md) (балл за ответ, Стадии 0-6) и
[PRD-11](./specs/prd-11/tag-draw-quotas.md) (квоты выдачи + тегирование вопросов). Драйвер — РТК-сертификация.
**PRD-10:** градуированный балл реализован end-to-end (UI редактора «Цены ответа» → экспорт `scoring` →
рантайм `Σ s` → порог раздела на `Σ s` → сертификация), подтверждено golden против эталона РТК
(`key_NEW_15-08-25.xlsx` + pandas, 63/63 балла, 0 расхождений). **PRD-11:** стратифицированная выдача
(модель `strata:[{tag,count,mode}]`, режим на тег) + тегирование вопросов (chip-инпут с правилами
именования) + UI квот в строке темы; рантайм-фикс экспорта `q.tags` в `TEST_DATA` + верификация в
scorm-player (100/100 выдач соблюдают квоты).

Отложенные точечные пункты: `showSectionResult` (промежуточные результаты по темам),
text-overflow diagnostics в PRD-1, порог coverage 50%, раздел «Архив» с восстановлением
в списке тестов, **flat adaptive mode** (`(adaptive, linear_flat)` пара — требует
общей шкалы сложности на тест без topicId-привязки; см. PRD-4 §3.1.2).

## 0.3 Технический долг

Пункты, признанные долгом осознанно: они не блокируют текущие работы, но и не должны потеряться.

**Закрыто и снято из таблицы (2026-08-15):** «Разрыв страницы PDF-отчёта внутри длинного блока
ни разу не отыгран». Ветка отыграна в браузере на отчёте из 18 разделов: блок «По темам» оказался
выше листа, разложился на три страницы A4, и разрез прошёл ПО ГРАНИЦЕ РЯДА карточек — первый лист
кончается после целого ряда, оставляя внизу поле, второй начинается целыми рядами (проверено и
снимками, и запросом по DOM: в верхних 20 px листа нет ни одного нижнего края карточки). Дефекта
нет, поведение соответствует замыслу. Регрессом не закрепляется сознательно: `getBoundingClientRect`
в jsdom отдаёт нули, и такой тест проверял бы собственную фикстуру, а не пагинацию, — пороговая же
часть давно закреплена модульными тестами на чистой функции `sliceBySafeLines`.

**Закрыто и снято из таблицы (2026-08-15):** «Ни один адаптивный тест dev-стенда не доигрывается
до итогов». Оба наблюдения разведены и закрыты: первое было данными (уровень с диапазоном
сложности, под который в теме нет вопросов) и породило три правки — предупреждение редактора при
сохранении, замечание в отладочном прогоне и подтверждение, что публикация такой тест и раньше
не пропускала; второе оказалось дефектом кода (истечение общего времени не завершало адаптивную
попытку на сервере) и исправлено вместе с невидимым обратным отсчётом. На стенде заведена
проходимая фикстура и скрипт её засева `scripts/dev/seed-adaptive-fixture.ts`.

**Закрыто и снято из таблицы (2026-08-15):** «Паритет внешнего шаблона с эталоном держится
вручную, а гард сверяет только разметку». Условие снятия выполнено слиянием ветки
`prd-50-result-breakdowns` в `main`: «Сертификация» догнала эталон по шкальному вопросу PRD-26,
настройкам итогов PRD-29 и полосам разреза, а `tests/template-layout-parity.test.ts` расширен на
ключи `settings[]`, порядок блоков итогов, покрытие селекторов стилей и демо-набор — то есть гард
теперь видит и оформление, и авторскую настройку, а не только разметку. Оговорка на будущее:
структурный выход из класса ошибок — `manifest.extends`, при котором внешний шаблон возит только
дельту вместо побайтовых копий 19 раскладок, — НЕ сделан; долгом он не признан, но при следующем
расхождении вернуться придётся именно к нему.

**Закрыто и снято из таблицы (2026-08-05):** «Настройка "Сводка баллов" игнорируется у теста без
шкал и показателей» — исправлено коммитом `a9b1628` (issue #30): построчный «Баллов»/«Правильно»
темы теперь тоже гейтится настройкой «Сводка по баллам», PDF-отчёт сознательно исключён отдельным
флагом `topicPointsIgnoreScoreSummary`. «Текст обратной связи ТЕМЫ не показывается ни одним
хостом» — исправлено коммитом `85426a0`: пакет и веб читают текст единым `feedbackTexts` из
`feedback_json.text` с откатом на легаси-колонку.

**Закрыто и снято из таблицы (2026-08-06):** «В PDF-отчёте тема с неопределённым вердиктом
печатается с меткой "Не пройден"». Вердикт темы в отчёте был булевым (`src.passed === true`), тогда
как экран печатает его тремя состояниями (`topicView`: да / нет / метки нет вовсе), — и тема без
правила прохождения объявлялась проваленной. Правка в `shared/report/report-context.ts` вернула
третье состояние, а макет `report.html` обоих поставляемых шаблонов перестал печатать узел плашки
при пустой метке: плашка несёт фон и отступы, поэтому пустой строки было мало — остался бы красный
прямоугольник. Той же логике подчинены два цвета в `report.css`: полоса карточки и заливка
прогресса были красными по умолчанию и красили тему без вердикта в провал мимо метки — теперь база
нейтральная, а красное даёт класс `is-fail`, приходящий тем же `passClass`, что и `is-pass`.
Счётчики и баллы у такой темы остаются на месте: снято утверждение о прохождении, а не данные.

**Закрыто и снято из таблицы (2026-08-06):** «Тест с порогом, но без оцениваемых вопросов
показывает шапку "Пройден" и одновременно печатает обратную связь». Продуктового решения пункт
не требовал: правило задано PRD-29 §6.7 («вердикт опирается на ДВА признака сразу: порог задан
И есть что оценивать») и в коде уже жило — но внутри ветки измерений, поэтому доставалось только
тесту со шкалами или показателями. Контрольный тест мимо этой ветки проходит, и его вердикт
оставался булевым, тогда как гейт обратной связи по тому же признаку работал всегда — отсюда
экран, который сам себе противоречит. Гашение вынесено в общий путь `buildResultContext`;
неизвестный признак порога по-прежнему НЕ гасит вердикт (хост, не научившийся его слать, иначе
потерял бы шапку у каждого оцениваемого теста). Тем же исправлением закрыт дефект приёмки
PRD-29 D-4.

**Закрыто и снято из таблицы (2026-08-08):** «Импорт книги Excel пишет шкалы и показатели в
обход маршрутов». Индекс использования медиа переиндексируют маршруты шкал и показателей, а
импорт обращался к хранилищу напрямую и мимо них — поэтому после загрузки книги строки
`media_usages` расходились с содержимым. Пункт был безобиден, пока импорт сносил конфигурацию
целиком, и стал заметен ровно наоборот: теперь книга удаляет ОТДЕЛЬНЫЙ уровень, вложение уходит
вместе с ним, а строка индекса остаётся и продолжает выдавать ученику файл, которого в тесте уже
нет (индекс не запрещает, а разрешает — в этом направлении расхождение и опасно). Оба помощника
переехали из маршрутов в `server/services/media/usage-index.ts` и вызываются всеми, кто пишет
эти сущности: копия на писателя — это и есть механизм, которым один из них оказывается забыт.
Переиндексация набора самоисцеляющая, поэтому удаление уровня, шкалы или показателя лечится тем
же вызовом. Предпросмотр книги индекс не трогает.

**Закрыто и снято из таблицы (2026-08-08):** «Импорт книги Excel перезаписывает `configJson`
шкалы». Загрузка листа «Шкалы» писала конфигурацию значением `{ bands }` целиком, и круг
«выгрузил → загрузил» без единой правки сносил границы шкалы, благоприятное направление, предел
показа PRD-46 и тексты, тональности и вложения уровней PRD-29/PRD-32. Показатель в том же импорте
вёл себя правильно и служил образцом: `parseResultVariableRow` не кладёт `configJson` вовсе.
Принято правило «книга задаёт целиком те поля, у которых есть колонка, и не трогает те, у которых
её нет»: слияние живёт в Pass 2 импорта, где сохранённое значение уже в руках, а уровни сшиваются
по КОДУ уровня — границы и подпись книга выражает, текст и обратную связь нет. Пустая ячейка
существующей колонки по-прежнему означает «удалить», иначе автор лишился бы единственного способа
снять уровни; наличие колонки определяется по строке заголовков, потому что `sheetToObjects`
выбрасывает пустые ячейки и по объекту строки «колонки нет» неотличимо от «автор очистил». Той же
работой книга стала полной: у границ шкалы, предела показа и благоприятного направления появились
свои колонки, так что поля больше не живут только в редакторе. Приёмка — круг на трёх реальных
тестах (методика ЧИЛ и два Маслача, десять шкал), потерь нет.

| Пункт | Суть | Риск и почему не сделано сразу | Объём |
| --- | --- | --- | --- |
| Согласие на обработку данных и срок хранения | Экрана согласия не существует: `client/src/pages/first-login.tsx` отправляет `gdprConsent: true` жёстко зашитым, а строки `gdprConsent`/`gdprConsentText`/`gdprRequired` в `client/src/lib/i18n.ts` не используются. Срок хранения данных не определён | У сотрудника основание обработки — трудовые отношения, у внешнего участника (PRD-28) единственное основание — согласие, которого фактически никто не даёт; вдобавок он никогда не увидит экран первого входа, потому что в систему не входит, а его имя и почта остаются бессрочно. Отложено 2026-07-30 по решению владельца продукта: ясности по срокам и по форме согласия пока нет. **ПЕРЕОТКРЫТО 2026-08-01:** поднято из техдолга в бизнес-требование BR-23 (Этап 28 BRD) с ВНЕОЧЕРЕДНЫМ приоритетом — конкурентный анализ отнёс формальное соответствие к отсекающим критериям закупки | Средний: экран согласия на старте теста плюс политика хранения (удаление, обезличивание с сохранением результата или бессрочно) |
| Пересоздание сессии при смене уровня доступа | Ни `POST /api/auth/login`, ни погашение ссылки-приглашения `/access/:token` не вызывают `req.session.regenerate()` — идентификатор сессии остаётся тем, что пришёл в cookie | Фиксация сессии: злоумышленник, сумевший навязать жертве известный ему идентификатор, после её входа окажется в её аккаунте. Через саму систему навязать идентификатор нельзя (нужна XSS или контроль домена либо поддомена), а `express-session` отбрасывает неизвестный идентификатор, поэтому это защита в глубину, а не эксплуатируемая дыра. Отложено 2026-07-30 сознательно: пробел системный, чинить надо ОБА пути входа сразу, а правка логина требует собственных тестов и приёмки и не должна ехать хвостом чужой задачи. **2026-08-01 привязано к BR-22-06 (Этап 27 BRD, единый вход):** SSO всё равно правит вход, поэтому обе точки чинятся в его составе, а не отдельным заходом | Небольшой: две точки плюс перенос данных сессии, но нужны свои тесты входа |
| Генератор превью шаблонов зависит от окончаний строк рабочей копии | `scripts/generate-prd1-template-previews.mjs` вшивает содержимое макетов в `preview.html` как строковые литералы JS через `JSON.stringify`, не приводя переводы строк к LF. На Windows-копии (`core.autocrlf=true`) файлы читаются с CRLF, поэтому в литералы попадает `\r\n`, тогда как в закоммиченных превью — `\n` | Перегенерация превью на Windows даёт дифф на тысячи невидимых глазу отличий поверх содержательной правки: ревью не отличает реальное изменение макета от смены окончаний строк, а два разработчика на разных системах будут бесконечно переписывать файл друг за другом. Не эксплуатируемо и на рендер не влияет — превью читает те же строки обратно. Отложено 2026-08-02 сознательно: всплыло хвостом задачи о паритете макетов «Сертификации», а правка тулинга требует собственной приёмки и пересечётся с незакоммиченной правкой DSL в том же скрипте | Небольшой: нормализовать читаемый текст к LF в одном месте (`readText`), затем перегенерировать превью один раз |
| В SCORM-пакете нельзя завершить попытку с экрана «Обзор теста», если текущий вопрос не отвечен | Подтверждение в модальном окне вызывает отправку без принудительного режима, и проверка «сначала ответьте на вопрос» возвращает ученика назад — выхода с экрана нет. Веб-хост тот же сценарий завершает штатно | Вскрыто приёмкой консолидации текстов (2026-08-03). Природа — PRD-19 (навигация); дефект объясняет, почему пакет с пропущенными вопросами не удавалось довести до итогов | Малый: отправка с принудительным режимом, как в остальных таких путях |
| Компактность `suspend_data` | В `cmi.suspend_data` складывается ВСЁ состояние прогона: `attempts[]`, где каждая запись несёт `answers` и полный `flatQuestions` (объекты выданных вопросов с текстами и вариантами), якорь таймера PRD-20, `completedAt` для интервала PRD-31, `retake.lastCompletedDate` для cooldown PRD-6, `attemptsUsed` для `maxAttempts`. См. `server/scorm/template/app/utils/scorm/suspendAttempts.js` (`saveAttemptResult`, `readTimerAnchor`) и `server/scorm/template/app/eligibility/gate.js`. Обрезки нет: `saveAttemptResult` только добавляет запись, объём растёт с каждой попыткой без потолка | Предел `cmi.suspend_data` в SCORM 2004 — 64000 символов, и на длинном тесте с несколькими попытками мы к нему подходим; переполнение проявится не ошибкой, а МОЛЧА — LMS обрежет строку, `JSON.parse` упадёт, `readSuspendObj` вернёт пустой объект, и разом обнулятся счётчик попыток, история, «лучшая попытка», таймер и интервал между попытками. Отдельно это условие входа для профиля SCORM 1.2 (анализ 2026-08-03): там предел 4096 символов, то есть текущая схема не влезает на порядок, и сжатие не спасает — нужен выигрыш в 10-30 раз, а не в 2-3. Не сделано сразу: правка меняет формат состояния, который читают уже выпущенные пакеты, поэтому нужна миграция чтения и своя приёмка на обоих барьерах PRD-6/PRD-31. **ПЕРЕВЕДЕНО В ТРЕК 2026-08-03:** поднято из техдолга в отдельный [PRD-36](./specs/prd-36/run-state-compactness.md) — инвентаризация потребителей показала, что массив попыток не нужен ни одному из них, а хранимое содержимое вопросов дублирует запечённый `TEST_DATA`; статус трека ведётся в [§0](#0-текущий-статус-на-2026-09-01), настоящая строка сохранена как след происхождения. Заведено в трекере 2026-08-15: [#38](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/38) — чтобы риск был виден не только в реестре долга. **ЗАКРЫТО КОДОМ 2026-08-15:** описание выше — состояние ДО правки; трек реализован и влит в `main`, содержимого вопросов в состоянии больше нет, объём перестал расти с числом попыток. Осталась только приёмка на стенде WebTutor ([§0.0.1](#001-треки-в-работе--что-осталось)) | Средний: содержимого вопросов в состоянии нет, адресация позицией в `TEST_DATA`, массив попыток заменяется на счётчик + лучшая + последняя, контроль размера при записи |
| Шкала едет в SCORM-пакет вторым представлением, показатель — первым | У показателя `config_json` уезжает в `TEST_DATA` сырым (`test-json.ts`: `configJson: rv.configJson ?? {}`), и рантайм разбирает его общим парсером. У шкалы то же `config_json` разбирается НА СБОРКЕ и укладывается ПЛОСКО — `domainMin`/`domainMax`/`displayMax`/`valence`/`bands` полями рядом с ключом, — а рантайм перечитывает плоский объект тем же парсером, будто это конфигурация. Форма родом из PRD-5 (`fbb496a8`), где пакету нужен был единственный ключ `bands` для движка расчёта; в PRD-29 (`1281e0ff`) её дописали руками под карточки шкал, и в том же коммите показатель получил другое решение | Пока представления два, поле интерпретации, не доехавшее до пакета, не падает — пакет молча рисует другую фигуру, чем веб, при одинаковых настройках теста (PRD-29 §9 считает это дефектом). Так и случилось в PRD-46 с `displayMax`: поймано только приёмкой в живом пакете. Острота снята 2026-08-08 (`546db613`, разворот всей интерпретации вместо перечисления полей + зеркальный тест в `server/scorm/__tests__/test-json-prd29.test.ts`), поэтому пункт не блокирует и молча разойтись больше не может. Не выровнено сразу: плоскую шкалу читают ДВОЕ — `viewResults.js` через общий парсер и движок `scales/engine.js`, который лезет прямо в `scale.bands`, — значит переход на сырой `config_json` правит и рантайм, и движок, и требует своей приёмки расчёта шкал в пакете, а не едет хвостом чужой задачи | Средний: возить `config_json` шкалы сырым, как у показателя; научить `scales/engine.js` брать уровни через общий парсер; снять зеркальный тест за ненадобностью |
| Классификатор типов вопроса | Ветвление по `questions.type` рассыпано примерно по сорока местам (сервер, веб-хост, рантайм пакета, редактор, книга импорта и экспорта, аналитика, инспектор PRD-18, снапшоты) и почти нигде не является исчерпывающим `switch`: это цепочки `if (type === "...")`, замыкаемые `default` или `return false`. Единый источник признаков `shared/questions/question-type.ts` заведён в PRD-26, но переведены на него не все точки — часть по-прежнему сравнивает тип с литералом | Новый тип вопроса компилируется, проходит существующие тесты и при этом работает неправильно МОЛЧА: `default` в `hasAnswer` (`client/src/pages/learner/answer-gate.ts`) засчитает частичный ответ как готовый, а `return false` в `isActive` (`shared/scales/engine.ts`) обнулит все вклады в шкалы при полностью заполненном опроснике. Дефект проявляется расхождением цифр в отчёте, а не падением, поэтому ищется дольше всего. Не сделано сразу: перевод всех точек — сквозная правка через оба хоста и оба JS-двойника, её нельзя проверить иначе как отдельной приёмкой на всех типах сразу. Зафиксировано как долг 2026-08-06 при проектировании [PRD-44](./specs/prd-44/scale-allocation-question-type.md), где риск вскрылся в третий раз подряд (PRD-26 R-1) | Средний: инвентаризация точек ветвления, перевод сравнений с литералом на признаки, тест-матрица «тип x признак», правило ревью «новых сравнений с литералом не появляется» |
| Копия `tb-components.css` для эскизов отстала от приложения | Компонентный слой существует в ДВУХ файлах: `client/src/styles/tb-components.css` (источник, 395 корневых селекторов) и `docs/wireframes/tb-components.css` (копия для эскизов, 146). Замер 2026-08-14: в копии НЕТ 250 селекторов источника — в том числе всей таблицы цветов вкладки «Оформление» (`.design-colors-table`, `.design-color-field`). Эскизы разошлись и по тому, что подключают: 33 файла берут копию, 13 — источник приложения напрямую | Тот же класс ошибки, что и у `wf-utils.css`, и та же цена: эскиз — обязательный гейт перед React, а показывает он не то, что получится на экране. Правило «правишь компонентный слой — правь обе копии» на практике мертво: расхождение в 250 селекторов накопилось молча, ничем не сторожится и обнаруживается только глазами на конкретном эскизе. Не сделано сразу: перевод всех эскизов на один источник затрагивает 46 файлов и требует сверки каждого в браузере | Малый на замер, средний на исправление: перевести эскизы на ЕДИНСТВЕННЫЙ источник (`client/src/styles/tb-components.css`), копию удалить, а не догонять; проверка «копии нет» — в `npm run check:wireframes:ds` |
| Устаревшие подмены классов ДС в `docs/wireframes/ds/wf-utils.css` | Файл переопределяет НАСТОЯЩИЕ классы дизайн-системы — `.ou-tag` со всеми тонами, `.ou-banner`, `.ou-input`, `.ou-select`, `.ou-table`, `.ou-button` — и опирается на токены, которых в ДС больше нет: у `.ou-tag--success` это `--ou-success-bg`/`--ou-success-fg`, из-за чего тег получает прозрачный фон вместо тона. Подключается последним, как во всех эскизах, поэтому гасит настоящие стили | Эскиз перестаёт показывать то, что получится на экране, а эскиз у нас — обязательный гейт перед React: расхождение уезжает в интерфейс. Вскрыто 2026-08-13 на эскизах PRD-28; там обойдено порядком подключения (`prd7-shared.css` -> `wf-utils.css` -> `university-rt.css` -> `tb-components.css`), а не устранено, и следующий эскиз наступит на те же грабли. Отдельно: общий прогон `npm run check:wireframes:ds` красный по унаследованным файлам (2968 нарушений, крупнейший источник — `prohozhdenie/ui-kit/university-rt.css`), поэтому новая поломка в нём и не видна | Малый: вычистить из `wf-utils.css` переопределения существующих классов ДС, оставив только утилиты эскиза, и проверить соседние эскизы на регресс. Отдельно и крупнее — привести общий прогон проверки к зелёному, иначе она ничего не сторожит |
| Экран итогов и документ отчёта показывают один результат двумя механиками | Документ отчёта PRD-51 собирается из блоков: состав, порядок и авторские страницы задаёт автор строками `report_blocks` поверх закрытого реестра `shared/report/report-blocks.ts`. Экран итогов остался на `resultsBlockOrder` и трёхпозиционных настройках показа PRD-29/PRD-49 — переставить подблок, выключить лишний или вставить свой методический текст там нельзя | Автор настраивает ОДНО И ТО ЖЕ — что слушатель видит после теста — в двух непохожих местах и получает разный набор возможностей; каждая новая настройка показа результата теперь требует решения, в какой из двух механик её заводить, и рискует разойтись между экраном и документом (PRD-51 §5.2 требует обратного: документ не показывает иное, чем экран, с которого его скачали, и приёмка хвостов уже нашла нарушение этого правила по блокам разделов). Не сделано сразу — сознательное решение [PRD-51](./specs/prd-51/report-document-blocks.md) §14: документ никто не проходит, сломать выдачу им нечем, а экран итогов видит КАЖДЫЙ ученик, поэтому его перевод на блоки требует своей приёмки на обоих хостах | Крупный: реестр блоков экрана итогов, перенос настроек `resultsBlockOrder` в строки, редактор, оба хоста, снапшоты и книга переноса |

---

## 1. Контекст

Разделы 1-6 фиксируют исходный порядок реализации PRD-1...PRD-14 и обоснование зависимостей
между ними. Треки, заведённые позже (PRD-15...PRD-51), встраиваются в этот порядок как
расширения соответствующих контрактов; их статус — в [§0](#0-текущий-статус-на-2026-09-01),
повод и зависимости — в [§0.2](#02-post-mvp-backlog).

Документ фиксирует порядок реализации PRD-1...PRD-8. Порядок отличается от приоритизации
этапов, перечисленной в [BRD §7](./specs/brd-scorm-enhancements.md), потому что учитывает фактические
зависимости контрактов, риск двойного рефакторинга UI и стоимость переделок при добавлении
новых настроек теста.

PRD-1 закрыт 2026-05-28 (см. [specs/prd-1/templates-content-pages.md](./specs/prd-1/templates-content-pages.md))
и считается фундаментом для остальных PRD.

---

## 2. Порядок реализации

> Таблица ниже документирует **зависимости и обоснование** порядка PRD. Фактический
> порядок **исполнения** — MVP-first (см. §0.1): PRD-7 → closeout PRD-1 → PRD-4 → PRD-8,
> затем post-MVP по §0.2: **PRD-2 + PRD-5 (парный трек)** → PRD-6 → PRD-3. То есть для
> MVP PRD-8 поднят выше PRD-6, а в post-MVP пара PRD-2+PRD-5 поднята выше PRD-6 по
> бизнес-запросу 2026-05-29 (шкалы + LMS-передача без постобработки).

| Шаг | PRD | Этап BRD | Основная причина выбранного порядка |
| --- | --- | --- | --- |
| 1 | PRD-7 Рефакторинг редактора параметров теста ([контракты](./architecture/test-editor-contracts.md)) | Этап 9 | Контракт `TestEditorModel`, DTO, `passDecisionPolicy` и `flowMode` нужны как поверхность для всех последующих PRD. Если сначала добавлять flow/retake/scales в текущий god-component `TestsPage`, потребуется повторный переписать UI. |
| 2 | [PRD-4](./specs/prd-4/course-flow-sections.md) Гибкий поток прохождения и разделы | Этап 4 | Базовый runtime: `flowPolicy`, расчёт `section.*`, граница «внутреннего старта попытки», `sort_order` для `test_sections`. Без него PRD-6, PRD-8 и часть формул PRD-2/PRD-5 нечем питать. |
| 3 | [PRD-6](./specs/prd-6/retake-cooldown-gate.md) Ограничение повторного прохождения и retake gate | Этап 5 | Использует только границу старта попытки из PRD-4 и системную страницу из PRD-1. Лёгкая интеграция, даёт compliance-ценность для корпоративных курсов. |
| 4 | [PRD-8](./specs/prd-8/section-router-flow.md) Сценарий через страницу-маршрутизатор | Этап 4 расширение | Зависит от PRD-4 (`flowPolicy`, section results, completion policy), PRD-7 (вкладка «Структура» в новом редакторе) и PRD-1 (системная router-страница). Закрывает Storyline-сценарий полностью. |
| 5 | [PRD-2](./specs/prd-2/result-variables.md) Пользовательские показатели результата | Этап 6 часть | Депендит на runtime PRD-1, реальную пользу даёт после PRD-4: формулы могут ссылаться на `section.*`. Нужен как фундамент для PRD-5 (`result.*` использует `scale.*`). |
| 6 | [PRD-5](./specs/prd-5/scales-competency-measurements.md) Шкалы, компетенции и многомерные измерения | Этап 6 | Явно депендит на PRD-2 и PRD-4. Самая тяжёлая авторская UI (матрица вкладов на каждый вопрос); делается после стабилизации редактора и flow. |
| 7 | [PRD-3](./specs/prd-3/external-templates.md) Администрирование жизненного цикла шаблонов | Этап 7 | Не блокирует runtime-возможности. Включается, когда команда готова открывать платформу подрядчикам или партнёрам. |
| 8 | [PRD-9](./specs/prd-9/crypto-password-hashing.md) Миграция bcryptjs → `@vvlad1973/crypto` | Tech debt | Снижение количества криптографических SDK, упрощение audit security и build externals. Не блокирует runtime функциональности, может выполняться параллельно с PRD-3 после стабилизации PRD-7. |
| 9 | [PRD-14](./specs/prd-14/questions-import-export.md) Импорт и экспорт банка вопросов | Этап 12 | Авторский tooling вне runtime; зависит только от модели `questions` и DAL `storage`. Отдельный трек: обратимость экспорта/импорта и паритет полей вопроса; импорт медиа — отдельный будущий PRD; шкалы/показатели — мультилистом на следующем этапе. |

---

## 3. Связки контрактов

### 3.1 PRD-7 и PRD-4

Доменная модель `TestEditorModel` из PRD-7 уже описывает `flowMode`, `passDecisionPolicy`,
`sections[].required` — это поверхность PRD-4. Доменные контракты обоих PRD проектируются одной
итерацией, даже если PRD-7 уходит в реализацию первым. Иначе при выкатке PRD-4 модель редактора
придётся менять повторно.

### 3.2 PRD-4 и PRD-1

PRD-4 §8.2 содержит скрытое требование к PRD-1: `test_sections.sort_order` нужно выкатить
раньше, чем UI PRD-1 для перестановки тем станет рабочим. Колонка `sort_order` входит в скоуп
PRD-4, но её добавление и заполнение должны быть выполнены до соответствующего UI PRD-1.

### 3.3 PRD-6 и PRD-4

PRD-6 опирается на `start_course_button` и отложенный SCORM `Initialize` из PRD-4 §4.1.1. До
выпуска PRD-4 retake gate можно реализовывать только в режиме flag-only без реального
gate-runtime.

### 3.4 PRD-2, PRD-4, PRD-5

PRD-2 публикует `result.*`. PRD-5 публикует `scale.*` до `result:calculated` и используется
формулами `result_variables`. PRD-4 публикует `section.*`. Все три пространства имён должны быть
согласованы до начала PRD-2, чтобы избежать миграции формул при выкатке PRD-5.

**PRD-2 + PRD-5 идут парным треком (см. §0.2).** Расширение DSL PRD-2 источниками
`scaleById(...).{raw,normalized,percent,level,label,hasValue}` и helper'ом
`countScales([...], level)` — обязательное условие старта PRD-5: без них формулы
итоговых категорий по шкалам (типовой кейс — burnout category в [MBI](./specs/prd-5/example-mbi.md))
неразрешимы. Также PRD-2 v2.1 добавляет поле `controls_status` для управления
`cmi.success_status` / `cmi.completion_status` boolean-показателем, что нужно для
сценариев, где «pass» определяется категорией шкал, а не `passing_score`.

### 3.5 PRD-9 (изолирован)

PRD-9 затрагивает только authentication path (`server/storage.ts`, `script/create-admin.ts`) и
не пересекается с runtime/контрактами других PRD. БД-миграция не требуется (колонка
`passwordHash` имеет тип `text`, формат хеша вмещает оба варианта). Поддержка legacy bcrypt-хешей
обеспечивается lazy rehash при логине.

---

## 4. Возможные параллельные ветки

| Ветка | Условия |
| --- | --- |
| PRD-3 параллельно с PRD-4...PRD-6 | PRD-3 имеет собственную таблицу `templates`, собственный admin API и не пересекается с runtime потока. Может вестись отдельной парой разработчиков. |
| PRD-6 параллельно с PRD-8 | После завершения PRD-4 две ветки независимы: PRD-6 трогает bootstrap и плагин допуска, PRD-8 трогает router-layout и completion policy. Конфликт возможен только в системных страницах PRD-1. |
| PRD-9 параллельно с PRD-3...PRD-8 | PRD-9 изолирован в authentication path. Может выполняться в любое время после стабилизации PRD-7 S9. |

---

## 5. Перестановки порядка

| Сценарий | Изменение |
| --- | --- |
| Бизнес-приоритет — Storyline-router сценарий | Поменять местами PRD-6 и PRD-8 (router раньше cooldown). |
| Бизнес-приоритет — компетентностная оценка | Поменять местами PRD-6 и PRD-2 (показатели и шкалы раньше cooldown), PRD-8 уйдёт в конец. |
| Появляется внешний партнёр-разработчик шаблонов | PRD-3 поднимается в любой момент после PRD-1, минимум — на 3 шаг. |

PRD-7 как первый шаг и PRD-4 как второй шаг не двигаются, потому что от них зависит почти всё
остальное.

---

## 6. Точки контроля

Перед стартом каждого шага должны быть выполнены:

- wireframes согласованы (см. [BRD §2.6](./specs/brd-scorm-enhancements.md));
- доменные контракты соседних PRD зафиксированы;
- backward compatibility со старыми тестами проверена golden/smoke-тестами;
- регрессионный набор тестов покрывает поведение предыдущих шагов.
