# PRD-51, этап Э5: приёмка контракта формата шаблонов 2.0.0

Дата: 2026-08-16. Этап документарный: кода не менялось ни строчки.

## 1. Что сделано

### 1.1. Спецификация формата — 2.0.0

`docs/specs/spec-template-platform.md`, версия **1.7.0 → 2.0.0**, дата актуализации
2026-08-16.

| Раздел | Правка |
| --- | --- |
| Шапка | Предмет спецификации назван документом отчёта: оболочка плюс блоки |
| §5.2 | В опциональные ключи манифеста добавлены варианты `report.block` и `reportDocument` |
| §8.3.2 | Перечень надписей отчёта сканируется по макетам ОБОЛОЧКИ И БЛОКОВ |
| §8.4 (врезка) | Отчёт назван документом из блоков; названа деградация |
| §8.4.1 | `layoutFile` вида `report` — макет ОБОЛОЧКИ; требование одного корневого узла; запрет `placeholders[]` сужен до оболочки; пример оболочки |
| §8.4.7 (новый) | Вид `report.block`: поля объявления, закрытый перечень двенадцати ключей, правила вариантов и `kinds[]` |
| §8.4.8 (новый) | `reportDocument`: документ по умолчанию по видам, правила повторов |
| §8.4.9 (новый) | Шаблон без блоков печатает цельную раскладку; проверяется объявление состава, а не наличие вариантов |
| §17.1 | Шесть новых блокирующих проверок объявлений блоков и `reportDocument` |
| История версий | Запись 2.0.0 с обоснованием мажорной версии |

### 1.2. Руководство разработчика — 2.0.0

`docs/guides/template-development.md`, версия **1.7.0 → 2.0.0**. Добавлен раздел **§7.3b
«Документ отчёта из блоков»**: рецепт из четырёх шагов (оболочка, блоки, авторская
страница, документ по умолчанию) с примерами манифеста и макетов, плюс таблица из шести
частых ошибок с их причинами.

### 1.3. PDF пересобраны

| Файл | Было | Стало | Страниц |
| --- | --- | --- | --- |
| `docs/dist/spec-template-platform.pdf` | 1 784 517 | 1 870 839 | 81 |
| `docs/dist/template-development.pdf` | 1 631 186 | 1 662 251 | 66 |

Два других PDF каталога пересобирались тем же прогоном и возвращены в прежнее состояние:
к документу отчёта они отношения не имеют.

## 2. Почему версия мажорная

Формально прежние шаблоны валидны и печатаются (§8.4.9). Мажорная версия поставлена
потому, что изменился СМЫСЛ, а не только состав:

1. `kind: "report"` был макетом всей страницы, стал корнем документа. Разработчик,
   читавший контракт 1.x, завернёт блоки в свой контейнер — и потеряет разбивку на листы,
   ничего об этом не узнав.
2. Появился вид `report.block` с ЗАКРЫТЫМ перечнем ключей. Закрытый перечень — ограничение,
   а не расширение.
3. Запрет `placeholders[]` перестал быть общим: у оболочки остался, с блока `page` снят.
   Правило «в отчёте нет содержимого, которое читают как страницу» больше не верно.

## 3. Сверка

Тройка сошлась: спецификация 2.0.0, руководство 2.0.0 (и его ссылка на спецификацию —
тоже 2.0.0), оба PDF пересобраны. `markdownlint` на обоих документах — 0 замечаний. Тесты
документации (`tests/deploy-image-docs`, `tests/routes.docs`) — 13 зелёных.

## 4. Что осталось незакрытым по треку PRD-51 в целом

- **Растр адаптивного отчёта** (хвост Э1-Э2, повторно назван в приёмках Э3 и Э4): фикстура
  стенда до конца не проходится — лимит 3 минуты истекает, а адаптивная выдача возвращает
  вопрос по кругу. Нужна адаптивная фикстура без лимита времени.
- **Веб-выдача PDF живьём**: в обеих приёмках отчёт скачивался из ПАКЕТА. Веб-хост печатает
  его той же функцией через тот же конвейер, совпадение закреплено тестами паритета, но
  живого скачивания из веба не было.
- **Шапка карточки блока со счётчиком** на живых данных: проверять нужно тестом, у которого
  настроены блоки разделов PRD-50 (см. §3 приёмки Э4).
- **Иллюстрация титула «Сертификации»**: поле объявлено и печатается, файла не подставляли —
  чужих товарных знаков в репозитории нет.
- **[Issue #39](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/39)**: перенос
  теста между инсталляциями документ отчёта не переносит. Чужой трек (PRD-48), в границы
  PRD-51 не входил.
