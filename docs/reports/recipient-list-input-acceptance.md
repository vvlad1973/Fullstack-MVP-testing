# Приёмка: набранный вручную список получателей

Дата: 2026-09-10. Контур: dev на порту 8097 (второй экземпляр, общая dev-база).
Спеки: `docs/specs/prd-28/external-participant.md` раздел 16, `docs/specs/prd-52/test-review.md` раздел 14,
`docs/specs/access-control/role-model.md` версия 1.5. План: `docs/plans/2026-09-10-recipient-list-input.md`.

Ветка `main`, коммиты `f2090011`, `15e4c84b`, `5cddb905`, `d2a1edb6`, эскизы `1cdd7ee9`, документы `ab6f812f`.

## 1. Что проверялось и чем

Фикстуры, заведённые на dev ради приёмки (их можно удалять):

- `prd52.acceptance.author@example.com` — роль ТОЛЬКО «Автор», владелец теста приёмки.
- `prd52.acceptance.manager@example.com` — роль ТОЛЬКО «Методист».
- Тест «Приёмка PRD-52: список рецензентов» (`43b49a7b-1a36-452b-a24a-67af5be4f24d`).
- Внешние рецензенты `prd52.expert.*@example.com`, `prd52.book.*@example.com`, заведённые прогоном.

Скриншоты: `.playwright-mcp/dev-review-*.png`, эскизы — `.playwright-mcp/wf28-*.png`, `.playwright-mcp/wf52-*.png`.

## 2. Критерии PRD-28, раздел 16.4

| Критерий | Итог | Чем подтверждён |
| --- | --- | --- |
| 1. Вкладка называется «Списком», обе половины видны | Пройден | `dev-review-bulk-empty.png` |
| 2. Две записи доходят до предпросмотра: имя и прочерк | Пройден | `dev-review-preview.png`: `prd52.expert.named` → «Ирина Петрова», `prd52.expert.plain` → «—» |
| 3. Заполненная половина гасит вторую, симметрично | Пройден | `dev-review-bulk-typed.png` (набран список → книга погашена), `dev-review-bulk-file.png` (выбран файл → поле погашено) |
| 4. Вставка с запятой внутри кавычек даёт ДВЕ строки | Пройден | `dev-review-preview.png`: `"Петров, Иван" <…quoted>; Сидорова <…semi>` → две строки, имя «Петров, Иван» цело |
| 5. Запись без собаки — строка «Некорректный адрес», не выбирается | Пройден | `dev-review-preview.png` и `dev-review-report.png`: `ivanov.example.com` → «Пропущен», «Адрес не похож на почтовый» |
| 6. Подпись комментария: имя, если задано; адрес, если нет | Пройден | `dev-review-comments-both.png`: «Ирина Петрова» и `prd52.expert.plain@example.com` |
| 7. Прогон заводит внешние учётки, выпускает ссылки, отдаёт отчёт с выгрузкой | Пройден | `dev-review-report.png`: создано 4, писем 4, пропущено 1, выгрузка ссылок отдала книгу |

Счётчик предпросмотра говорит об источнике: «5 записей списка» у набранного и «2 строк файла» у книги
(`dev-review-preview.png`, `dev-review-preview-file.png`).

## 3. Критерии PRD-52, раздел 14.2

| Критерий | Итог | Коды ответов |
| --- | --- | --- |
| 1. Автор-владелец проходит весь путь без отказов | Пройден | `review/preview` 200, `review/invite` 200, `review/links-exported` 204, `review/template` 200 |
| 2. Методисту закрыто | Пройден | все три маршрута 403 |
| 3. Автору на ЧУЖОМ тесте закрыто | Пройден | `preview`/`invite`/`template` 403 |
| 4. Администратор проходит на любом тесте | Пройден | `review/preview` 200 на тесте другого владельца |

До правки этот путь у автора был закрыт целиком: вкладка ходила в маршруты участников, требующие
`assignments.manage` и `users.create`, которых у роли «Автор» нет.

## 4. Сверка с эскизами

Состояния `docs/wireframes/approved/prd52-invite-dialog.html` (кнопки «списком» / «списком: набран») и
`docs/wireframes/prd28-bulk-invite-upload.html` (три состояния) сопоставлены с реализацией поэлементно:
подпись поля «Адреса почты», placeholder в две записи, подсказка дословно, жёлоб «или», погашенная зона
книги без CTA, погашенная ссылка на шаблон, порядок блоков. Расхождений в этой части нет.

## 5. Что найдено попутно и НЕ входило в охват

1. **Диалог рецензирования показывает поля назначения.** «Срок выполнения» и «Создать группу из списка»
   видны на вкладке «Списком», но прогон рецензирования их не отправляет вовсе (`review/invite` знает только
   срок жизни ссылки). Автор заполняет поля, которые ничего не делают. Расхождение существует с первой
   редакции общей вкладки. ИСПРАВЛЕНО 2026-09-11 — см. раздел 7.
2. **Утверждённый эскиз `prd52-invite-dialog.html` беднее реальной вкладки:** в его состоянии «списком» нет
   ни сроков, ни имени группы, ни кнопки «Проверить список» — они нарисованы только в эскизе PRD-28.
   ЗАКРЫТО 2026-09-11 приведением эскиза к реальному виду — см. раздел 7.
3. **Кнопки `Dark`/`Compact` в эскизе PRD-52 не работали** — `toggleTheme is not defined`. Починено в этой
   же ветке: без этого тёмную тему на эскизе было не посмотреть.
4. **Статус «уже назначен» в предпросмотре рецензирования** считается по назначениям теста, а не по грантам
   рецензирования (PRD-52 раздел 14.3). На приёмке не всплыл: все адреса были новыми.

## 6. Прогоны тестов

Целевые, полный прогон не запускался (правило проекта):

- `shared/recipients` — 8 тестов;
- `tests/routes.participants.test.ts` — 21;
- `tests/access/permissions.test.ts` — 19;
- `tests/review-routes.test.ts` — 39, `tests/review-invite.test.ts` и `tests/access` — 137 суммарно;
- `client/src/features/tests/assign` + `client/src/components/__tests__/assign-test-dialog.test.tsx` — 60
  суммарно с роут-тестами участников;
- `tests/routes.home.test.ts`, `tests/routes.access.test.ts`, `tests/magic-scope-rules.test.ts`,
  `tests/prd52-full-draw.test.ts`, `tests/services.participants-invite.test.ts` — 80.

`npm run check` (tsc) чист. Покрытие (`npm run test:cov`) не запускалось.

## 7. Доделка 2026-09-11 по итогам разбора находок

Владелец решил: находку 1 (поля назначения в диалоге рецензирования) — исправить, находку 2 (эскиз беднее
реальности) — закрыть приведением эскиза к реальному виду.

### 7.1 Что исправлено в диалоге

Убраны из режима рецензирования — на вкладках «Пользователи», «Группы» и «Списком»:

- «Срок выполнения» — приглашение рецензента этого поля не отправляет;
- «Создать группу из списка» — группа создаётся вместе с назначением, которого здесь нет.

Переписаны подписи режима: «Приглашено» вместо «Назначено», «К приглашённым» вместо «К назначениям»,
«Все пользователи уже приглашены» вместо «…уже назначены», «Доступ выдан, ссылка выпущена…» вместо
«Назначение создано, …». Поле «Ссылка активна до» осталось: оно работает в обоих режимах.

Спека: PRD-52 раздел 14.4 (FR-36, FR-37). Тесты: три новых в
`client/src/features/tests/assign/__tests__/bulk-invite-tab.test.tsx` и два в
`client/src/components/__tests__/assign-test-dialog.test.tsx`; прогон — 43 теста трёх файлов зелёные.
Проверено в браузере: `real-review-bulk-fixed.png`, `real-review-report.png`.

### 7.2 Как пересобран эскиз

`docs/wireframes/approved/prd52-invite-dialog.html` собран из СНИМКОВ реального DOM работающего диалога:
шесть состояний (приглашены, пользователи, списком, списком с набранным списком, предпросмотр, отчёт).
Данные прогона заменены демонстрационными, идентификаторы разведены по состояниям, значение поля адресов
возвращено содержимым тега, а правила, которые ДС вставляет на лету (ширины колонок таблицы), внесены
отдельным блоком — иначе снимок потерял бы их вместе с рантаймом.

Скрипт сборки — `scripts/docs/build-prd52-wireframe.mjs`, порядок пересборки описан в его шапке: снять
снимки состояний (`document.querySelector('.ou-modal').outerHTML`) и правила динамического листа со
селекторами вида `.ou-tbl-cell-*`, затем запустить скрипт из корня репозитория.

Эскиз подключает таблицу стилей ДС оттуда же, откуда её берёт приложение
(`/client/src/styles/vendor/skillum-ds.css`). Копия в `docs/wireframes/ds/` отстала на ~42 КБ — в ней нет
раскладочных правил (`ou-stack--gap-*`, `ou-text--*`), и на ней снимок реального DOM разъезжается по
отступам и выравниванию. По этой же причине `npm run check:wireframes:ds` показывает на файле «unknown DS
class» для классов, которые в боевой таблице есть: словарь линтера собран по отставшей копии. Обновление
общей копии в охват не входило — её читают полсотни эскизов.

### 7.3 Что осталось нетронутым

- **Вкладка «Группы» в режиме рецензирования не работает** (PRD-52 раздел 14.5): приглашение идёт поимённо,
  выбор группы уходит на сервер пустым списком и получает отказ. Решение продуктовое — либо раскрывать
  группу в участников, либо убирать вкладку из режима (тогда правится и раздел 3.3 спеки).
- Статус «уже назначен» в предпросмотре считается по назначениям, а не по грантам (раздел 14.3).
