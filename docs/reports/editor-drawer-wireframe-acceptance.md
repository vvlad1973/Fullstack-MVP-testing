# Приёмка UI ящика редактора теста по утверждённым эскизам

Дата приёмки: 2026-09-04. Ветка `main`, версия приложения 2.17.0 (`a3c54b56-dirty`),
в рабочем дереве есть незакоммиченные правки в `basic-settings-section.tsx`,
`editor-tabs.tsx`, `scales-section.tsx` — приёмка сделана по рабочему дереву, как есть.

## Что было эталоном

Ориентир по составу и облику ящика — целевой эскиз
[editor-settings-target.html](../wireframes/editor-settings-target.html): 34 состояния,
принят владельцем 2026-09-03 (этап Э0.5 плана
[перестройки настроек редактора](../plans/2026-09-02-editor-settings-restructure.md)).
Сквозной хром ящика дополнительно сверялся по утверждённым эскизам PRD-7
([ящик](../wireframes/approved/prd7-editor-drawer.html),
[индикаторы](../wireframes/approved/prd7-editor-status-indicators.html),
[диалог закрытия](../wireframes/approved/prd7-editor-close-confirm.html),
[конфликт версий](../wireframes/approved/prd7-editor-conflict.html)), вкладка
«Комментарии» — по [эскизу PRD-52](../wireframes/approved/prd52-review-panel.html),
поскольку в целевом эскизе её содержимое не нарисовано ни разу.

## Как проверялось

- Эскиз отрисован в браузере со статического сервера из корня репозитория, сняты все
  34 состояния.
- Ящик редактора открыт на живом dev-сервере под учётной записью приёмки; сняты все
  21 сочетание «вкладка — раздел рельса» на тесте «Сертификация руководителей
  (демо: подытоги по подтемам)» (стандартный, опубликован, две темы с подтемами,
  отчёт включён, шаблон «Отчёт: сертификация»).
- Дополнительно сняты: адаптивный тест («Стенд: адаптивный проходимый») — рельс
  «Состав и сценарий» с пунктом «Адаптивные уровни»; тест со шкалами и показателями
  («Определение ведущего стиля человекоцентричного лидерства») — наполненные разделы
  «Шкалы» и «Показатели»; модалки «Оценка вопроса в тесте», «Добавить блок»,
  «Добавить тему»; состояние с несохранёнными изменениями, поповер списка изменений и
  диалог закрытия (правки после снятия отменены, ничего не сохранялось).
- Отступы и фиксированные размеры измерены `getComputedStyle` одним и тем же скриптом
  на обеих страницах — сравнивались вычисленные значения, а не намерение в коде.
- Поведение полосы вкладок и рельса замерено при ширинах окна 1600, 1366, 1280, 1024
  и 820 px.

Оговорки метода, влияющие на полноту:

- Живой ящик снят в тёмной теме, эскиз — в светлой; цветовые различия отдельных токенов
  не разбирались.
- Состояния, которых нет в данных снятых тестов (пустые состояния тем, блокирующая
  валидация, конфликт версий, оверлей сохранения, раскрытые карточки шкал и уровней,
  ступенчатая цена ответа, веса опций, конструктор формулы, вкладка «Комментарии»
  с комментариями), проверены по коду и CSS, а не на экране. Полный перечень — в разделе
  «Что осталось непроверенным».
- Каждая находка опирается либо на строку эскиза, либо на строку кода, либо на видимое
  на снимке. Спорное вынесено в «Непроверено», а не в находки.

## Итог

Семь вкладок, их подписи и порядок совпадают с эскизом. Рельсы шести вкладок из семи
совпадают по составу и порядку, включая появление «Адаптивных уровней» в адаптивном
тесте. Расхождений — 219.

| Участок | Блокирующих | Существенных | Косметических | Всего |
| --- | --- | --- | --- | --- |
| A. Хром ящика и вкладка «Основное» | 1 | 10 | 17 | 28 |
| B. Состав и сценарий | 1 | 19 | 32 | 52 |
| C. Правила прохождения | 0 | 6 | 10 | 16 |
| D. Оценка результата | 0 | 8 | 50 | 58 |
| E. Обратная связь и итоги | 0 | 19 | 17 | 36 |
| F. Оформление и Комментарии | 0 | 8 | 16 | 24 |
| G. Сетка и оболочка (замеры) | 0 | 1 | 4 | 5 |
| Итого | 2 | 71 | 146 | 219 |

Участок G содержит только те замеры, что не дублируют находки A-6 и A-7.

## Два блокирующих расхождения

1. **Поповер списка изменений схлопывается.** Ширина задана
   `width: var(--wf-size-400); max-height: var(--wf-size-420)`
   (`tb-components.css:1055`), но ни одного
   объявления этих токенов в проекте нет. Проверено в живом рантайме:
   `getComputedStyle(...).getPropertyValue('--wf-size-400')` возвращает пустую строку,
   тогда как соседние `--wf-size-150` и `--wf-size-180` дают `150px` и `180px`. Правило
   невалидно, поповер сжимается по содержимому — вместо 420 px получается около 170 px,
   значения переносятся по два-три слова. Эскиз требует 420 px и ограничение высоты
   320 px у ТЕЛА поповера, а не у всего поповера.

2. **Блок квот по подтемам собран не тем компонентом.** Эскиз рисует карточки
   `ou-card--outlined ou-card--sm tb-level-card`: заголовок-подтема, подзаголовок-сводка
   «ровно 4 · доступно 11 · в вариантах A: 2 · B: 2», сворачивание и тело в две и три
   колонки. Реализация — плоская таблица `tb-table`
   (`topics-structure-section.tsx:733`).
   Вместе с этим теряются подписи полей карточки, панель «Развернуть все / Свернуть все»
   и сводка по вариантам.

## Что расходится системно

Пять узоров повторяются по всем вкладкам и дают большую часть существенных находок.

1. **Разделы формы потеряли заголовки.** Эскиз строит панели из
   `ou-formsection--stacked` с `h3.ou-formsection__title`. Реализация во многих местах
   рендерит поля голым потоком: нет заголовков «О тесте» и «Интеграция» (A-3, A-4),
   «Навигация» и «Защита контента» (C-1, C-15), «Сценарий» (B-36), «Показ правильных
   ответов» (E-1); «Темы теста» (B-1) и «Показатели» (D-40) понижены до мелкой подписи
   `tb-section-label`, а «Тест пройден, если» (D-6) и шесть разделов вкладки «Обратная
   связь и итоги» (E-25), наоборот, обёрнуты в карточки, которых в эскизе на этом
   участке нет ни одной.

2. **Правило модульной сетки 4/16/24 перенесено не полностью.** Эскиз объявляет
   22 перебивки ui-kit; в приложении отсутствуют шесть (раздел G), плюс точечные
   нарушения внутри панелей (B-19, B-20, B-21, D-15, D-28, E-13, F-3, F-4, F-8).
   Отдельный случай — числовые поля «Ограничений» (C-8): проектное правило фиксированной
   ширины 280 px адресовано прямому потомку `.tb-settings-content` и до полей внутри
   `ou-formsection__body` не достаёт, поэтому поля растянуты во всю колонку.

3. **DS-компонент подменён самодельной или headless-вёрсткой.** Таблица весов опций
   собрана из `div` с инлайновыми стилями (D-37), реестр обратной связи вопросов лишился
   таблицы (E-7), переключатель исхода комментария заменён двумя кнопками (F-22), ячейка
   вклада в шкалу стала простым `Input` без степперов (D-32). Находка G-7 (аккордеон
   `ou-acc` подменён `Collapsible`) при разборе снята как ложная: в названных ею местах
   эскиз рисует свои свёртки `tb-fold-sec`, а не аккордеон.

4. **Пары «Развернуть все / Свернуть все» и поиск потеряны.** Их нет в списке тем, в
   блоке квот, в адаптивных темах (B-6, B-14, B-30), в разделах «По подтемам» и «По
   уровням» обратной связи (E-19); поля «Поиск темы» в шапке раздела «Состав» нет
   вовсе (B-2).

5. **Тексты разошлись с эскизом в обе стороны.** Реализация добавляет пояснения там,
   где эскиз их не рисует (C-3, C-14, C-16, E-4, E-11, E-27 и другие), и переписывает
   те, что эскиз задаёт дословно (C-9, C-11, E-10, E-15, E-34). Отдельно: сообщения
   валидации полей «Название» и «Webhook URL» выводятся по-английски (A-5), а в тексте
   пустого состояния тем печатается код требования «(FR-12)» (B-22).

## Что оказалось не дефектом

- **Пункт рельса «Макет» во вкладке «Оформление».** Раздел реализован
  (`design-section.tsx:115`,
  панель — строки 324-331) и скрыт правилом PRD-23 «пункт без параметров не показываем».
  Манифест «Сертификация (РТК)» v1.12.0 объявляет 18 параметров, ни одного с
  `"section": "layout"`. Требует решения владельца: показывать пункт всегда, как в
  эскизе, или оставить правило.
- **Отсутствие группы «Удалённые из документа» в палитре блоков отчёта.** Группа
  собирается из системных блоков, которых нет в документе; в документе этого теста стоят
  все десять блоков реестра, пустая группа не рисуется.
- **Адрес переключателя «Показывать правильные ответы» в баннерах навигации.** Эскиз
  ссылается на раздел «Ограничения», реализация — на «Обратная связь и итоги» → «Во время
  теста». Правильна реализация; править надо эскиз.
- **Мета-тег вводного блока отчёта.** Эскиз ссылается на несуществующую вкладку
  «Настройки», реализация называет верное место — подраздел «Обратная связь», карточка
  «Вводный текст».
- **Вариант кнопки «Отменить» в подвале.** Эскиз противоречит сам себе (secondary в
  `s-dirty`, ghost в `s-changes`); реализация делает secondary, то есть следует более
  подробному состоянию.

## Расхождения внутри самого эскиза, требующие решения владельца

1. Рельс вкладки «Основное»: в `s-basic` его нет, в `s-changes` он есть с пунктами
   «Название и назначение» / «Режим теста» / «Выдача в LMS». Реализация следует `s-basic`.
2. Нумерация тем: в `s-composition` темы подписаны «1. О компании», в `s-scenario` —
   без номера. Реализация делает наоборот.
3. Главная кнопка модалки оценки вопроса: «Применить» в `s-qscoring` против «Сохранить»
   в двух других состояниях. Реализация делает «Применить».
4. Переключатель способа формулы показателя: «Конструктор» / «DSL» размера s против
   «Конструктор» / «Выражение» размера m. Реализация смешала: подписи из первого
   варианта, размер из второго.
5. Подписи надписей итогов: в `s-feedback` — «Заголовок блока по темам» и «по шкалам»,
   в `s-labels` — «Подзаголовок тем» и «шкал». Реализация следует `s-labels`.
6. Заголовок ящика открыт как `h2`, а закрыт как `</h1>`. Реализация даёт `h1`.
7. Ширина ящика: FR-43 требует `min(1120px, 100vw - var(--ou-space-9))`, но сам эскиз
   берёт `ou-drawer--xl` без переопределения (1040 px). Реализация повторяет эскиз.
8. Прокрутка полосы вкладок: целевой эскиз её разрешает, утверждённый эскиз ящика
   требует стрелок. Замерено: до ширины окна 1024 px полоса не переполняется вовсе,
   при 820 px переполняется и прокручивается горизонтально, стрелок нет.
9. Счётчик открытых комментариев на вкладке: эскиз PRD-52 его рисует, целевой эскиз —
   нет. Разрешено в пользу PRD-52: содержимое вкладки «Комментарии» целевой эскиз не
   рисует ни разу, поэтому о счётчике он ничего и не утверждает. Счётчик реализован.
10. Значение селекта «Порядок выдачи вопросов в тесте»: в эскизе стоит «Как в тесте»,
    что для правила уровня теста бессмысленно — это значение уровня ТЕМЫ; список опций
    эскиз не раскрывает. Гейт несёт это расхождение записью `wf:test-order-value`: пока
    владелец не решит, требовать от реализации значение, которого у неё быть не может,
    нельзя.
11. `.ou-acc__body { gap }`: правило эскиза задаёт `var(--ou-space-4)`, то есть 16 px, а
    комментарий рядом обещает 4 px («ui-kit: 2 -> 4»). Найдено не приёмкой, а сверщиком
    отступов при разборе эскиза 2026-09-04; в браузере эскиз показывает 16 px, то есть
    работает токен, а не комментарий. До решения владельца это правило исключено из
    проверяемого контракта: гейт не может требовать значение, в котором эскиз не уверен.

## Нарушения дизайн-системы в самих эскизах

Найдено не приёмкой, а линтером эскизов после того, как в него добавили пятую категорию —
`ou-*`-классы, которых не существует ни в таблице стилей дизайн-системы, ни в проектном слое,
ни в разметке, выпускаемой компонентами ui-kit (`npm run check:wireframes:ds`, категория
`unknown DS class`). Эскиз, нарисованный несуществующим классом, нельзя реализовать дословно:
исполнитель либо сочиняет разметку, либо молча подставляет другой компонент — и то и другое
есть отступление от утверждённого рисунка.

По репозиторию таких классов 142 в 44 файлах. Ниже — только те 12, что относятся к эскизам
этого трека. Колонка «Эскиз» здесь называет выдуманный класс, колонка «Реализация» — класс
дизайн-системы, который стоит на его месте; так таблица читается тем же разбором, что и
остальные.

| № | Место (файл эскиза) | Эскиз (выдуманный класс) | Реализация (класс ДС) | Тип | Важность | Ссылка на код |
| --- | --- | --- | --- | --- | --- | --- |
| W-1 | editor-settings-target.html | `ou-drawer-root--right`, использований 28 | Модификатора нет: в ДС объявлен только `ou-drawer-root`, сторона задаётся на панели (`ou-drawer--right`) | выдуманный класс ДС | существенное | `docs/wireframes/editor-settings-target.html:493` |
| W-2 | editor-settings-target.html | `ou-formfield__hint`, использований 19 | `ou-formfield__desc` | выдуманный класс ДС | существенное | `docs/wireframes/editor-settings-target.html:1261` |
| W-3 | editor-settings-target.html | `ou-field__hint`, использований 2 | `ou-formfield__desc`; семейства `ou-field__*` для подсказки в ДС нет | выдуманный класс ДС | существенное | `docs/wireframes/editor-settings-target.html:1215` |
| W-4 | editor-settings-target.html | `ou-seg--m`, использований 4 | Модификатора нет: в ДС объявлены `ou-seg--s` и `ou-seg--l`, размер m — умолчание без класса | выдуманный класс ДС | косметическое | `docs/wireframes/editor-settings-target.html:744` |
| W-5 | editor-settings-target.html | `ou-textarea--m`, использований 2 | Модификатора нет: объявлены `ou-textarea--s` и `ou-textarea--l`, размер m — умолчание | выдуманный класс ДС | косметическое | `docs/wireframes/editor-settings-target.html:3554` |
| W-6 | editor-settings-target.html | `ou-tabs__ico` | В ДС не объявлен; иконка вкладки в ui-kit своего класса не имеет | выдуманный класс ДС | косметическое | `docs/wireframes/editor-settings-target.html:2050` |
| W-7 | approved/prd7-editor-conflict.html | `ou-table` | `ou-tbl` (11 объявлений в ДС) | выдуманный класс ДС | существенное | `docs/wireframes/approved/prd7-editor-conflict.html:133` |
| W-8 | approved/prd7-editor-conflict.html | `ou-table-wrap` | Обёртки с таким именем в ДС нет | выдуманный класс ДС | существенное | `docs/wireframes/approved/prd7-editor-conflict.html:132` |
| W-9 | approved/prd52-review-panel.html | `ou-textarea--m`, использований 3 | Модификатора нет, размер m — умолчание | выдуманный класс ДС | косметическое | `docs/wireframes/approved/prd52-review-panel.html:290` |
| W-10 | approved/prd52-review-player.html | `ou-textarea--m` | Модификатора нет, размер m — умолчание | выдуманный класс ДС | косметическое | `docs/wireframes/approved/prd52-review-player.html:300` |
| W-11 | approved/prd7-editor-settings-tab.html | `ou-textarea--m`, использований 8 | Модификатора нет, размер m — умолчание | выдуманный класс ДС | косметическое | `docs/wireframes/approved/prd7-editor-settings-tab.html:666` |
| W-12 | approved/prd7-editor-settings-tab.html | `ou-skel--w-1/4`, использований 3 | В ДС не объявлен; имя с дробью — остаток утилитарного класса Tailwind | выдуманный класс ДС | косметическое | `docs/wireframes/approved/prd7-editor-settings-tab.html:952` |

Две находки основного списка опирались на эти выдуманные классы и потому недействительны в
той части, где они требуют класс:

- **A-16** требовала модификатор `ou-drawer-root--right` на корне ящика. Модификатора в
  дизайн-системе нет (W-1), значит права реализация, а не эскиз. Находка снимается и
  переходит в решения владельца вместе с W-1.
- **A-25** требовала таблицу `ou-table`. Такого класса в дизайн-системе нет (W-7), настоящий
  — `ou-tbl`. Часть находки про `aria-label` («Различия версий» против «Сравнение версий»)
  остаётся в силе, часть про класс — снимается.

Остальные 130 нарушений живут в эскизах других треков и в этот трек не входят.

### Чего гейт отступов пока не покрывает

Находку G-9 нашёл не гейт, а соседняя сессия, вручную. Причина в устройстве разбора: контракт
собирается только из правил эскиза, у которых есть комментарий-перебивка вида
`/* ui-kit: 8 -> 4 */`. Таких во встроенном блоке 21, и гейт проверяет 19 из них (два правила
пакуют по два объявления в строку и пропускаются).

Кроме них во встроенном блоке эскиза есть ещё **100 правил**, которые задают отступы токенами
`--ou-space-*` без комментария. Большая часть — собственная обвязка эскиза (`.wf-order`,
`.variant-picker`, скелетоны `.tb-sheet`), и требовать их от продукта неверно. Но часть,
как `.tb-fold-actions`, — настоящий контракт, просто записанный без комментария.

Надёжный признак различения известен и проверяем: правило эскиза есть контракт тогда, когда
тот же селектор объявлен и в `client/src/styles/tb-components.css`, а значения расходятся.
Это следующий шаг для гейта; до него дыра остаётся, и находки такого рода ловятся руками.

## Полный список расхождений

Нумерация сквозная внутри участка. Столбец «Ссылка на код» указывает на файл и строку
рабочего дерева на момент приёмки.

### A. Хром ящика и вкладка «Основное»

| № | Место (вкладка/раздел) | Эскиз (что должно быть) | Реализация (что есть) | Тип | Важность | Ссылка на код |
| --- | --- | --- | --- | --- | --- | --- |
| A-1 | Поповер изменений (`s-changes`) | Ширина 420 px (`--wf-w-popover`), эскиз строка 273 | `width: var(--wf-size-400)` — токен `--wf-size-400` НИГДЕ не объявлен (ни в `client/src/styles`, ни в `vendor/ui-kit/css`), правило невалидно, ширина схлопывается до содержимого (~170 px на снимке `impl-changes.png`), значения переносятся по 2-3 слова | нарушение фиксированного размера (раздел 7.7) | блокирующее | `client/src/styles/tb-components.css:1055` |
| A-2 | Поповер изменений | Ограничение высоты 320 px у ТЕЛА поповера (`.tb-changes-popover__body { max-height: var(--wf-h-changes) }`) | `max-height: var(--wf-size-420)` навешено на весь поповер, токен также не объявлен; у `__body` ограничения нет | нарушение фиксированного размера | существенное | `client/src/styles/tb-components.css:1055`, `:1070` |
| A-3 | Вкладка «Основное», раздел «О тесте» | Раздел `div.ou-formsection.ou-formsection--stacked` с заголовком `h3.ou-formsection__title` «О тесте», внутри — Название, Описание, Режим теста | Разделов нет: три поля идут подряд во фрагменте, заголовка «О тесте» нет ни в разметке, ни на снимке | отсутствующий элемент | существенное | `client/src/features/tests/editor/sections/basic-settings-section.tsx:111`; `client/src/features/tests/editor/sections/editor-tabs.tsx:67` |
| A-4 | Вкладка «Основное», раздел «Интеграция» | Раздел `ou-formsection--stacked` с заголовком `h3` «Интеграция», внутри — Webhook URL, телеметрия, результат для LMS | Разделов нет, заголовка «Интеграция» нет; поля продолжают тот же поток, что и «О тесте» | отсутствующий элемент | существенное | `client/src/features/tests/editor/sections/basic-settings-section.tsx:929` |
| A-5 | Вкладка «Основное», поле «Название» | Сообщение об ошибке `ou-field__msg--error`: «Название обязательно.» (эскиз `s-errors`) | Сообщение приходит из валидатора на английском: «Test title is required.»; у поля «Webhook URL» — «Webhook URL must be a valid HTTP or HTTPS URL.» | расхождение подписи | существенное | `client/src/features/tests/editor/test-editor.validation.ts:117`, `:175` |
| A-6 | Риббон вкладок | `.ou-tabs__list` — `padding-inline: 24` (раздел 7.2), полоса выровнена с содержимым тела | Переопределения нет: у DS `.ou-tabs__list` отступа нет (`vendor/ui-kit/css/university-rt.css:6520`), в `tb`-слое задана только прокрутка. На снимке вкладки прижаты к левому краю ящика, поля тела начинаются правее | нарушение сетки 24 px | существенное | `client/src/styles/tb-components.css:89` |
| A-7 | Шапка ящика | `.ou-drawer__head` — padding 24 (раздел 7.2, «ui-kit: 20») | Переопределения в проектном слое нет — действует значение ui-kit `padding: var(--ou-space-5)` = 20 px (в `tb`-слое переопределён только подвал) | нарушение сетки 24 px | существенное | `client/src/styles/tb-components.css:228` (правило только для `__foot`); `vendor/ui-kit/css/university-rt.css:2850` |
| A-8 | Шапка ящика, тег статуса | Тег «Изменено» (`ou-tag--warning ou-tag--outline`), когда есть несохранённые изменения (3.2, 5.26) | Тег считается только по `editor.isDirty` (черновик настроек теста), тогда как подвал и блокировка «Сохранить» — по `combinedDirty` (настройки + оформление + страницы содержания). Правка только оформления или структуры даёт «грязный» подвал, но тег в шапке остаётся прежним | потерянное состояние | существенное | `client/src/features/tests/editor/test-editor.tsx:1487`; сравнение с `:306` |
| A-9 | Подвал, состояния `s-errors` и `s-changes` | В подвале слева стоит тег `ou-tag--warning` «Изменено» (3.5, 5.27, 5.28) | Тега в подвале нет ни в одном состоянии: только кнопки | отсутствующий элемент | существенное | `client/src/features/tests/editor/test-editor.tsx:738`-`:812` |
| A-10 | Точка состояния во вкладке | `span.tb-status-dot` с модификаторами `--warn` (жёлтая, «несохранённые изменения») и `--err` (красная), диаметр 12 px (`.tb-status-dot`) | `span.status-dot` с классами `dirty` / `warn` / `error`, диаметр 8 px; «изменено» окрашено акцентным (фиолетовым) `--ou-accent-default`, а не жёлтым `--ou-warning-default`. На снимке `impl-dirty.png` точка фиолетовая | не тот класс и цвет индикатора | существенное | `client/src/features/tests/editor/test-editor.tsx:853`-`:859`; `client/src/styles/tb-components.css:663`-`:674` |
| A-11 | Точка состояния во вкладке, `aria-label` | «Есть несохранённые изменения» и «Есть ошибки» — раздел 6.3 прямо предписывает брать формулировку целевого эскиза | «есть изменения», «есть предупреждения», «есть блокирующие ошибки» (со строчной буквы) | расхождение подписи | существенное | `client/src/features/tests/editor/test-editor.tsx:861`-`:865` |
| A-12 | Вкладка «Основное», между «Описание» и «Режим теста» | Разделителя нет: поля лежат в одном `ou-formsection__body` | Горизонтальный разделитель `<hr className="wf-sep" />` (видимая линия, `border-top`) | лишний элемент | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:154`; стиль `client/src/styles/tb-components.css:446` |
| A-13 | Вкладка «Основное», «Webhook URL» | Placeholder `https://some-lms.example.ru/hooks/scorm` | Placeholder `https://example.com/webhook` | расхождение подписи | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:939` |
| A-14 | Вкладка «Основное», «Webhook URL» | Пояснения под полем нет | `div.ou-formfield__desc`: «Оставьте пустым, если webhook не нужен.» | лишний элемент | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:950` |
| A-15 | Вкладка «Основное», «Результат для LMS при нескольких попытках» | Подсказки под полем нет | Длинный hint: «Действует только в SCORM-пакете. Многие LMS сами решают…» | лишний элемент | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:974` |
| A-16 | Оболочка ящика | Корень `div.ou-drawer-root.ou-drawer-root--right` (3.1) | Корень `div.ou-drawer-root` без модификатора `--right` (модификатор есть только на самой панели `aside`) | отсутствующий модификатор | косметическое | `client/src/features/tests/editor/test-editor.tsx:506` |
| A-17 | Шапка ящика, тег статуса при ошибках | В целевом эскизе `s-errors` шапка держит нейтральный тег «Черновик»: об ошибках говорят баннер и точки, а «Изменено» стоит в подвале | При любой ошибке валидации тег шапки подменяется на «Есть ошибки» (тон `error`) и вытесняет статус публикации | лишний элемент / расхождение подписи | косметическое | `client/src/features/tests/editor/test-editor.tsx:1480`-`:1485` |
| A-18 | Риббон вкладок | Вкладка «Комментарии» присутствует во всех 27 состояниях-ящиках (3.3) | Вкладка скрыта в режиме создания теста (`editor.mode !== "create"`) | отсутствующий элемент (условно) | косметическое | `client/src/features/tests/editor/test-editor.tsx:466` |
| A-19 | Подвал, «Сохранить» при ошибках | `disabled` + `aria-describedby` с причиной «исправьте ошибки в выделенных секциях» (6.3) | `disabled` + `aria-disabled`, `aria-describedby` нет — причина блокировки не озвучена скринридеру | отсутствующий элемент | косметическое | `client/src/features/tests/editor/test-editor.tsx:780`-`:781`, `:803`-`:804` |
| A-20 | Поповер изменений, строка изменения | Старое значение — приглушённое зачёркнутое (`color: fg-muted`), новое — полужирное; фонов и моноширинного шрифта нет | Старое — красная плашка `error-soft` моноширинным, новое — зелёная плашка `success-soft` моноширинным | расхождение оформления | косметическое | `client/src/styles/tb-components.css:1087`-`:1098` |
| A-21 | Поповер изменений, строка изменения | Стрелка — иконка `i-arrow-right` 12x12 | Текстовый символ «→» | не тот тип элемента | косметическое | `client/src/features/tests/editor/test-editor.tsx:982` |
| A-22 | Поповер изменений, строки группы | Разделителей между строками нет (только интервал 4 px) | Между строками пунктирная линия `border-top: … dashed` | лишний элемент | косметическое | `client/src/styles/tb-components.css:1080`-`:1085` |
| A-23 | Диалог закрытия редактора | `ou-modal--m`, ширина 480 px (6.4) | `ModalDialog size="l"` (`ou-modal--l`, max-width 720 px) — в коде оговорено, что три кнопки не влезают в `m` | не тот размер | косметическое | `client/src/features/tests/editor/test-editor.tsx:1138` |
| A-24 | Диалог конфликта версий | `ou-modal--m`, ширина 520 px (6.5) | `ModalDialog size="l"` (max-width 720 px) | не тот размер | косметическое | `client/src/features/tests/editor/test-editor.tsx:1241` |
| A-25 | Диалог конфликта, таблица различий | Таблица `ou-table` с `aria-label="Различия версий"` | Собственная таблица `table.tb-conflict-diff` с `aria-label="Сравнение версий"` | расхождение подписи | косметическое | `client/src/features/tests/editor/test-editor.tsx:1358`-`:1362` |
| A-26 | Диалог конфликта, подсказки кнопок | «Записать ваши изменения поверх серверной версии. Чужие правки будут перезаписаны.» и «Загрузить серверную версию. Ваши несохранённые изменения будут потеряны.» | Обе подсказки усечены до первого предложения: «Записать ваши изменения поверх серверной версии.» и «Загрузить серверную версию.» | расхождение подписи | косметическое | `client/src/features/tests/editor/test-editor.tsx:1259`, `:1271` |
| A-27 | Открытие ящика, фокус | NFR-19: фокус при открытии — тело ящика (`div.ou-drawer__body[tabindex="0"]`) | Фокус ставится на первую вкладку риббона; тело `tabindex={0}` есть, но фокус в него не уходит | расхождение поведения | косметическое | `client/src/features/tests/editor/test-editor.tsx:267`-`:272` |
| A-28 | Баннер ошибок, «Перейти к ошибкам» | Поле, на которое ведёт переход, получает класс `is-focused` и кольцо фокуса 2 px (`--wf-ring`, 5.27) | Переход прокручивает к якорю `data-field` и вызывает нативный `focus()`; класс `is-focused` не ставится, отдельного кольца нет | отсутствующий элемент | косметическое | `client/src/features/tests/editor/test-editor.tsx:351`-`:371` |

### B. Вкладка «Состав и сценарий»

| № | Место (раздел/секция) | Эскиз | Реализация | Тип | Важность | Ссылка на код |
| --- | --- | --- | --- | --- | --- | --- |
| B-1 | Состав, заголовок раздела | `ou-formsection--stacked` с `h3.ou-formsection__title` «Темы теста» (эскиз 618-621) | сырой `div.tb-section-label` «Темы и выборка вопросов» — мелкий приглушённый текст, не заголовок раздела | подпись + тип элемента | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:242` |
| B-2 | Состав, шапка раздела | поле «Поиск темы» — Input `ou-field--m` с иконкой `i-search` (эскиз 623-626) | поля нет | отсутствующий элемент | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:242-260` |
| B-3 | Состав, порядок выдачи | подпись «Порядок выдачи вопросов в тесте» | «Порядок выдачи вопросов» (в `aria-label` при этом полная формулировка) | подпись | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:250` |
| B-4 | Состав, порядок выдачи | Select `ou-field--m ou-field--full` с меткой НАД полем, на всю ширину (эскиз 627-633) | строка «метка слева + Select `size="s"` + хвост-подсказка» | тип/размер контрола | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:249-260` |
| B-5 | Состав, панель действий | `tb-fold-toolbar` НАД списком тем: «Добавить тему» (secondary, s, `i-plus`) слева (эскиз 634-640) | кнопка стоит ПОД списком тем, `variant="secondary" size="m" fullWidth` | место + размер кнопки | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:311-321` |
| B-6 | Состав, панель действий | пара «Развернуть все» / «Свернуть все» (ghost, s, `i-chevrons-up-down` / `i-chevrons-down-up`) справа (эскиз 636-639) | кнопок нет | отсутствующее действие | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:242-321` |
| B-7 | Состав, список тем | аккордеон `ou-acc ou-acc--separated`, строка-шапка `tb-acc-head` с шевроном `ou-acc__chev`; по умолчанию все темы СВЁРНУТЫ (комментарий эскиза, строка 643) | плоские карточки `tb-topic-row`, всегда раскрытые: ни шеврона, ни свёрнутого состояния | потерянное состояние | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:408-437` |
| B-8 | Состав, шапка темы | подзаголовок под названием: «41 вопрос в банке · выдаётся 16» | счётчик «9 вопросов» справа в шапке; сколько выдаётся — не показано | подпись + место | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:416-418` |
| B-9 | Состав, шапка темы | название с номером: «1. О компании» | номера нет, выводится только имя темы | подпись | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:410` |
| B-10 | Состав, шапка темы | кнопка «Убрать тему «…»» с иконкой `i-trash` 14x14 (эскиз 653) | та же кнопка с иконкой `X` | не та иконка | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:428-435` |
| B-11 | Состав, тело темы | Switch «Обязательная» — ПЕРВЫЙ пункт тела карточки (эскиз 657-662) | Switch перенесён в шапку карточки, между счётчиком и крестиком | порядок/место | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:419-427` |
| B-12 | Состав, блок квот | карточки квот `ou-card--outlined ou-card--sm tb-level-card`: `h5` с именем подтемы, подзаголовок «ровно 4 · доступно 11 · в вариантах A: 2 · B: 2», действия «Удалить квоту «…»» и «Свернуть квоту «…»», тело — группа в 2 колонки и группа в 3 колонки (эскиз 710-792) | таблица `tb-table` со строками-квотами | не тот тип контрола | блокирующее | `client/src/features/tests/editor/sections/topics-structure-section.tsx:733-879` |
| B-13 | Состав, блок квот | подписи полей карточки: «Подтема (тег вопроса)», «Сколько вопросов», «Режим квоты», «Порог по подтеме», «Значение порога, %» | заголовки колонок: «Подтема (тег вопроса)», «Сколько», «Режим», «Доступно», «Порог», «В вариантах»; у поля процента подписи нет | подписи + состав колонок | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:735-743` |
| B-14 | Состав, блок квот | панель «Развернуть все» / «Свернуть все» (ghost, s), прижата вправо (эскиз 704-709) | панели нет | отсутствующее действие | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:703-733` |
| B-15 | Состав, блок квот | пояснение «Порог сравнивается с долей баллов по подтеме. Подтема, не попавшая в выдачу, вердикт темы не роняет.» | «Порог сравнивается с долей БАЛЛОВ по подтеме, независимо от того, что выбрано для показа. Подтема, не попавшая в выдачу, вердикт темы не роняет.» | подпись | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:913-918` |
| B-16 | Состав, переключатели квот/порогов/вариантов | подписи без иконок: «Квоты по подтемам (тегам)», «Пороги по подтемам (тегам)», «Варианты теста» | перед каждой подписью иконка (`Layers`, `Layers`, `List`) | лишний элемент | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:667-670`, `client/src/features/tests/editor/sections/variants-editor.tsx:110-113` |
| B-17 | Состав, блок вариантов | строка итога одна: «В вариантах задействовано 32 из 41 вопроса темы · 9 не используются» + тег | добавлена вторая строка баланса: «{N} вариантов по {X} · при старте темы выпадает один, выдаётся целиком; на повторе — другой» + тег «сбалансировано» / «неравные варианты» | лишний элемент | косметическое | `client/src/features/tests/editor/sections/variants-editor.tsx:176-193` |
| B-18 | Состав, блок вариантов | метки вариантов «Вариант A» / «Вариант B» (и в подзаголовке квоты — «в вариантах A: 2 · B: 2») | метки нумеруются цифрами: «Вариант 1» / «Вариант 2» | подпись | косметическое | `client/src/features/tests/editor/sections/variants-editor.tsx:263`, `variants-editor.tsx:302` |
| B-19 | Состав, интервалы | между разными настройками — 4x (16 px), метка и её контрол — 1x (4 px) (раздел 7.3-7.4 описи, заметка эскиза 141-144) | строки тела карточки отбиты на `--ou-space-2` (8 px), внутренний зазор «метка — контрол» тоже 8 px | нарушение сетки | косметическое | `client/src/styles/tb-components.css:800-838` |
| B-20 | Состав, интервалы | тело раскрытой темы — `padding: 0 24 24` (раздел 7.2 описи) | `.tb-topic-row__body { padding: 12px 16px }` | нарушение сетки | косметическое | `client/src/styles/tb-components.css:798` |
| B-21 | Состав, интервалы | однородные карточки тем — 1x (4 px) между собой (заметка эскиза 141-144) | `.tb-topic-row { margin-bottom: 8px }` | нарушение сетки | косметическое | `client/src/styles/tb-components.css:781` |
| B-22 | Пустое состояние «нет тем» | заголовок «В тесте нет тем», описание «Тема даёт тесту вопросы: из неё идёт выборка, по ней считается вердикт и печатается разбор. Добавьте первую тему.» | заголовок «Пока нет ни одной темы», описание «Добавьте темы, из которых будут отбираться вопросы. Минимум одна тема обязательна для сохранения теста (FR-12).» — с кодом требования в тексте интерфейса | подпись | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:262-270` |
| B-23 | Пустое состояние «нет тем» | под пустым состоянием — баннер `ou-banner--info`, `role="status"`: «Пока тем нет, правила оценки тем, квоты по подтемам и адаптивная лестница недоступны.» | баннера нет | отсутствующий элемент | существенное | `client/src/features/tests/editor/sections/topics-structure-section.tsx:262-270` |
| B-24 | Пустое состояние «нет тем» | `ou-empty--page ou-empty--well` с иллюстрацией-иконкой `i-info`; «Добавить тему» вынесена в шапку раздела (secondary, s) | `EmptyState layout="inline" well` без иллюстрации; кнопка «Добавить тему» — под пустым состоянием, `size="m" fullWidth` | вариант компонента + место кнопки | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:263-321` |
| B-25 | Модалка «Добавить тему» | описание шапки «Темы, доступные вам и ещё не добавленные в тест» | «Выберите тему, вопросы из которой попадут в тест.» | подпись | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:957` |
| B-26 | Модалка «Добавить тему» | поле поиска с видимой подписью «Поиск темы» и иконкой `i-search` в поле | подписи и иконки нет — только `aria-label` | отсутствующий элемент | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:970-980` |
| B-27 | Модалка «Добавить тему» | placeholder «Название темы» | placeholder «Поиск по названию...» (с тремя точками вместо многоточия) | подпись | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:973` |
| B-28 | Модалка «Добавить тему» | пометка темы без вопросов — «нет вопросов» | всегда числовая пометка, для пустой темы «0 вопросов» | подпись | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:998-1000` |
| B-29 | Модалка «Добавить тему» | «Отмена» — ghost, размер m | ghost, размер s | не тот размер кнопки | косметическое | `client/src/features/tests/editor/sections/topics-structure-section.tsx:959-966` |
| B-30 | Адаптивные уровни, шапка раздела | `ou-formsection` + `h3` в строке `tb-section-head`, справа пара «Развернуть все» / «Свернуть все» (ghost, s) (эскиз 2323-2332) | сырой `h3.tb-topics-title` «Адаптивность по темам», кнопок нет | отсутствующее действие + тип элемента | существенное | `client/src/features/tests/editor/sections/basic-settings-section.tsx:1886` |
| B-31 | Адаптивные уровни, строка темы | точка состояния `tb-status-dot--ok` / `--off` с `aria-label="Состояние темы"` (NFR-21: точка обязательно парная с `aria-label`) | модификаторы `--ok` / `--warn`, точка помечена `aria-hidden="true"` — подписи нет | потерянное состояние + нарушение NFR-21 | существенное | `client/src/features/tests/editor/sections/basic-settings-section.tsx:1998-2001` |
| B-32 | Адаптивные уровни, строка темы | подзаголовок «16 вопросов · 2 уровня», у темы без уровней — «10 вопросов · уровней нет» | «12 вопросов · 3 уровня»; у темы без уровней — «0 уровней»; слово «вопросов» не склоняется | подпись | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:1976-1978` |
| B-33 | Адаптивные уровни, блок «Уровни сложности» | кнопка «Добавить уровень» (secondary, s) с иконкой `i-plus` | та же кнопка без иконки, плюс набран текстом: «+ Добавить уровень» | не тот контрол/подпись | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:2046-2053` |
| B-34 | Адаптивные уровни, карточка уровня | шапка: точка состояния, `h4` с названием, кнопки удаления и сворачивания; подзаголовка НЕТ | добавлен подзаголовок «0–100 · 8 вопросов · 70 % · валидно» | лишний элемент | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:2122-2127` |
| B-35 | Адаптивные уровни, карточка уровня | `section.ou-card.tb-level-card` (базовая карточка); кнопка удаления — `ou-iconbtn`, `aria-label` «Удалить уровень «Базовый»» с кавычками | `Card variant="outlined" size="sm"`; удаление — `Button variant="ghost"` с иконкой, `aria-label` «Удалить уровень Базовый» без кавычек | вариант компонента + подпись | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:2106-2143` |
| B-36 | Сценарий, заголовок раздела | `ou-formsection--stacked` + `h3` «Сценарий» (эскиз 2527-2530) | заголовка раздела нет вовсе, подраздел начинается сразу с поля | отсутствующий элемент | существенное | `client/src/features/tests/editor/sections/basic-settings-section.tsx:214-216` |
| B-37 | Сценарий, «Показывать итоги раздела» | Switch без описания | у Switch добавлено описание «После завершения раздела показывается экран с его результатом. Только для секционных тестов.» | лишний элемент | косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:252-267` |
| B-38 | Сценарий, полотно структуры | сразу после настроек идёт зона «ДО ТЕСТА» | между настройками и зоной вставлена полоса `FlowModeBar`: «Режим: Линейный по темам — задаётся выше, полем «Сценарий прохождения»» | лишний элемент | косметическое | `client/src/features/tests/editor/sections/start-pages-section.tsx:489`, `start-pages-section.tsx:574-585` |
| B-39 | Сценарий, полотно структуры | зона `section.inside-test` с меткой «ВНУТРИ ТЕСТА» и ветками `tree-branches` / `tree-branch` вокруг тем (эскиз 2581-2721) | в сценарии «Линейный по темам» зоны и метки «ВНУТРИ ТЕСТА» нет: блоки тем идут прямо за зоной «До теста»; метка есть только в маршрутизаторном сценарии | отсутствующий элемент | существенное | `client/src/features/tests/editor/sections/start-pages-section.tsx:899-917` |
| B-40 | Сценарий, заголовки зон | `div.zone-header` — только текст «ДО ТЕСТА» / «ПОСЛЕ ТЕСТА» | перед текстом добавлен шеврон `ChevronRight` | лишний элемент | косметическое | `client/src/features/tests/editor/sections/start-pages-section.tsx:952-961` |
| B-41 | Сценарий, строка страницы | бейдж `page-variant-badge` — вид узла: «Старт», «Введение», «Вопросы», «Обзор», «Итоги», «Страница» | бейдж — название МАКЕТА: «Старт: стандартный», «Введение раздела: стандартное», «Стандартный макет вопроса», «Итоги раздела: стандартные» | подпись | существенное | `client/src/features/tests/editor/sections/start-pages-section.tsx:1276`, `start-pages-section.tsx:1342` |
| B-42 | Сценарий, зона «До теста» | заголовок системной строки — «Стартовая страница» | «Старт» (значение `KIND_LABEL`) | подпись | косметическое | `client/src/features/tests/editor/sections/start-pages-section.tsx:181-192`, `start-pages-section.tsx:247-255` |
| B-43 | Сценарий, узел «Обзор» | заголовок «Обзор раздела» | «Обзор раздела — навигация по вопросам и «Завершить раздел»» | подпись | косметическое | `client/src/features/tests/editor/sections/start-pages-section.tsx:1106-1108` |
| B-44 | Сценарий, строка страницы | в строке только бейдж, заголовок и два действия | добавлены иконка типа строки (`page-icon`), кнопка разворота `page-expand-toggle` («Развернуть»), маркер «шаблон» и теги `page-row__meta` («Доступно макетов: 2») | лишние элементы | косметическое | `client/src/features/tests/editor/sections/start-pages-section.tsx:1329-1353`, `start-pages-section.tsx:1396-1425` |
| B-45 | Сценарий, действия строки | «Предпросмотр страницы» — `i-eye` 14x14; «Действия для страницы» — `i-more-vertical` 13x13 | `Eye` 12x12; меню — `MoreHorizontal` (горизонтальные точки) 12x12 | не та иконка + размеры из раздела 7 | косметическое | `client/src/features/tests/editor/sections/start-pages-section.tsx:1357-1377` |
| B-46 | Сценарий, действия строки | `aria-label` «Предпросмотр страницы» / «Действия для страницы» | «Предпросмотр системной страницы «{бейдж}»» / «Действия для системной страницы «{бейдж}»» | подпись | косметическое | `client/src/features/tests/editor/sections/start-pages-section.tsx:1360`, `start-pages-section.tsx:1372` |
| B-47 | Сценарий, зона «После теста» | порядок: вставка, страница «Что дальше», вставка, системная «Итоги теста» — после «Итогов теста» вставки НЕТ | после строки «Итоги теста» стоит ещё одна вставка «Добавить страницу» | порядок элементов | косметическое | `client/src/features/tests/editor/sections/start-pages-section.tsx:1521-1532` |
| B-48 | Модалка «Варианты теста», строка вопроса | мета строки — «подтема · N балл(а)» («Продукты компании · 1 балл») | мета — тип вопроса («Один вариант ответа»); подтема и балл не показаны | подпись/состав данных | существенное | `client/src/features/tests/editor/sections/variants-editor.tsx:268-289` |
| B-49 | Модалка «Варианты теста», подвал | «2 варианта · в вариантах задействовано 32 из 41 вопроса · 9 не используются» | «2 вариант(а/ов) · в вариантах задействовано 32 из 41» — склонение не сделано, шаблон печатается как есть | подпись | существенное | `client/src/features/tests/editor/sections/variants-editor.tsx:316-320` |
| B-50 | Модалка «Варианты теста», панели переноса | строка выбора «Выбрать все»; две кнопки переноса — «Назначить выбранных», «Снять выбранных» | «Выбрать всех (N)»; четыре кнопки — добавлены «Назначить всех» и «Снять всех» (поведение компонента ДС) | подпись + лишние элементы | косметическое | `vendor/ui-kit/src/components/TransferList.tsx:249`, `TransferList.tsx:272-290` |
| B-51 | Модалка «Варианты теста», вкладки | `ou-tabs__list` с `aria-label="Варианты темы"` | `aria-label` не задан | потерянная подпись | косметическое | `client/src/features/tests/editor/sections/variants-editor.tsx:329-336` |
| B-52 | Модалка «Варианты теста», строка вопроса | строка — чекбокс, аватар, название, мета | добавлены иконка типа вопроса и теги-номера «также в вариантах» | лишние элементы | косметическое | `client/src/features/tests/editor/sections/variants-editor.tsx:273-288` |

### C. Вкладка «Правила прохождения»

| № | Место (раздел/секция) | Эскиз | Реализация | Тип | Важность | Ссылка на код |
| --- | --- | --- | --- | --- | --- | --- |
| C-1 | Навигация | Раздел обёрнут в `ou-formsection` с заголовком `h3` «Навигация» (эскиз, строки 900-903) | Заголовка раздела нет: панель рендерится без `FormSection`, первым идёт переключатель | Отсутствующий элемент | Существенное | `client/src/features/tests/editor/sections/editor-tabs.tsx:194`; `client/src/features/tests/editor/sections/basic-settings-section.tsx:1013`-`:1015` |
| C-2 | Навигация | Порядок переключателей: возврат, свободная навигация, изменение ответа, «Переходить к следующему вопросу сразу после ответа», «Не показывать обзор, если отвечены все вопросы» (`wf-spec.md:367`-`371`) | Четвёртым идёт «Не показывать обзор, если отвечены все вопросы», пятым — «Переходить к следующему вопросу сразу после ответа» | Порядок элементов | Существенное | `client/src/features/tests/editor/sections/basic-settings-section.tsx:1090`-`:1127` |
| C-3 | Навигация | У всех пяти переключателей только `ou-switch-field__label`, пояснений нет (эскиз, строки 904-940) | У каждого переключателя есть `description` (5 текстов, от «Ученик может пропускать вопросы…» до «Без отдельного нажатия «Далее»…») | Лишний элемент (текст) | Косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:1018`, `:1043`, `:1066`, `:1093`, `:1108` |
| C-4 | Навигация, баннеры блокировки | «Недоступно при включённом показе правильных ответов (раздел «Ограничения»): …» (два баннера) | «Недоступно при включённом показе правильных ответов («Обратная связь и итоги» → «Во время теста»): …» | Расхождение текста подсказки | Косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:1085`, `:1124` |
| C-5 | Навигация, баннеры блокировки | `ou-banner ou-banner--subtle ou-banner--warning ou-banner--sm` с одним `ou-banner__desc`, без иконки | DS-компонент `Banner` всегда печатает `span.ou-banner__ico` с иконкой тона — на скриншоте у обоих баннеров треугольник слева | Лишний элемент | Косметическое | `vendor/ui-kit/src/components/Banner.tsx:107`; вывод `client/src/features/tests/editor/sections/basic-settings-section.tsx:1078`-`:1088` |
| C-6 | Навигация, «Свободная навигация внутри раздела» | Переключатель активен, никакой подчинённости не показано | Переключатель гаснет при выключенном возврате к неотвеченным и получает баннер «Доступно только при включённом возврате к неотвеченным.» | Лишняя зависимость и баннер | Косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:1008`, `:1045`, `:1055`-`:1061` |
| C-7 | Во время прохождения | «Режим прогресса» и «Показывать прогресс-бар» — без подсказок (эскиз, строки 3628-3644) | Под селектом подсказка «Что считает индикатор в шапке: выданные вопросы, страницы или ничего.», под переключателем — «Полоса заполнения под счётчиком в шапке.» | Лишний элемент (текст) | Косметическое | Приходят из манифеста шаблона через `SectionPane`: `client/src/features/tests/editor/sections/editor-tabs.tsx:202`-`:210` |
| C-8 | Ограничения, числовые поля | «Лимит времени теста» и «Максимум попыток» имеют фиксированную ширину `--tb-input-w-md` (280 px): правило `.tb-settings-content .ou-formsection__body > .ou-number` эскиза, строки 161-164 | Поля растянуты на всю ширину колонки (видно на скриншоте): проектное правило адресовано только прямому потомку `.tb-settings-content`, до полей внутри `.ou-formsection__body` не достаёт, а `.ou-number` в flex-колонке растягивается | Нарушение фиксированных размеров (раздел 7.7) | Существенное | `client/src/styles/tb-components.css:174` (правило есть только для `> .ou-formfield > .ou-number`); поля — `client/src/features/tests/editor/sections/basic-settings-section.tsx:456`-`:464`, `:478`-`:485` |
| C-9 | Ограничения повторных попыток | Подписи полей периода охлаждения: «После неуспешной попытки, дней», «После успешной попытки, дней» (`wf-spec.md:409`-`410`) | «При успешном прохождении, дней», «При неуспешном прохождении, дней» | Расхождение подписи (два поля) | Существенное | `client/src/features/tests/editor/sections/basic-settings-section.tsx:787`, `:802` |
| C-10 | Ограничения повторных попыток | Сначала поле неуспешного исхода (7), затем успешного (180) | Сначала «При успешном прохождении, дней», затем «При неуспешном прохождении, дней» | Порядок элементов | Косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:783`-`:812` |
| C-11 | Ограничения, баннер WebTutor | «Проверка через WebTutor находит прошлые попытки по названию курса. Ограничение сработает, только если название курса в WebTutor точно совпадает с названием этого теста.» | «…по НАЗВАНИЮ курса. Ограничение сработает только если название курса (модуля) в WebTutor точно совпадает с названием этого теста. Если при загрузке в LMS курс назван иначе — период охлаждения применяться не будет.» | Расхождение текста подсказки | Существенное | `client/src/features/tests/editor/sections/basic-settings-section.tsx:833` |
| C-12 | Ограничения, баннеры | Баннер WebTutor и итоговый info-баннер — размера по умолчанию (`ou-banner--subtle ou-banner--warning` / `--info`, без `--sm`) | Оба баннера с `size="sm"` | Не тот вариант компонента | Косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:832`, `:912` |
| C-13 | Ограничения повторных попыток | Разделительной линии перед «Ограничение между попытками» нет; проектный слой прямо говорит «Разделительных ЛИНИЙ в панели нет — их работу делает интервал» | Перед переключателем стоит `div.ou-card__divider` | Лишний элемент | Косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:863`; правило — `client/src/styles/tb-components.css:181` |
| C-14 | Ограничения, переключатели | «Индивидуальные лимиты для тем» и «Ограничить повторное прохождение» — без пояснений (эскиз, строки 3697-3701 и 3753-3757) | У обоих есть пояснение: «Если выключено, действует только общий лимит теста (см. выше).» и «Выключено — учащийся может перезапускать курс без ограничений (как сейчас).» | Лишний элемент (текст) | Косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:567`, `:717`-`:721` |
| C-15 | Защита контента | Раздел обёрнут в `ou-formsection` с заголовком `h3` «Защита контента» (эскиз, строки 3929-3932) | Заголовка раздела нет: панель рендерится без `FormSection` | Отсутствующий элемент | Существенное | `client/src/features/tests/editor/sections/editor-tabs.tsx:217`; `client/src/features/tests/editor/sections/basic-settings-section.tsx:1181`-`:1186` |
| C-16 | Защита контента | У трёх переключателей только подписи, пояснений нет | У каждого переключателя есть `description` (три текста) | Лишний элемент (текст) | Косметическое | `client/src/features/tests/editor/sections/basic-settings-section.tsx:1189`, `:1204`, `:1219` |

#### Как разрешено расхождение из раздела 9 эскиза

Раздел 9 эскиза (пункт 5, `wf-spec.md:1543`-`1545`) фиксирует, что баннеры `s-rules`
ссылаются на раздел «Ограничения», хотя переключатель «Показывать правильные ответы»
живёт во вкладке «Обратная связь и итоги» → «Во время теста».

Фактическое положение в реализации: адрес исправлен. Оба баннера называют
«(«Обратная связь и итоги» → «Во время теста»)» — это подтверждается и кодом
(`basic-settings-section.tsx:1085`, `:1124`), и живым снимком
(`SP\impl-live.txt`, строки 294 и 299), и скриншотом раздела «Навигация».
Переключатель «Показывать правильные ответы после ответа» действительно живёт
в `DuringTestPane` (`basic-settings-section.tsx:284`-`:285`), то есть реализация
указывает на настоящее место, а не на «Ограничения».

Побочно: подпись самого переключателя в реализации — «Показывать правильные ответы
после ответа», а баннеры и раздел 9 эскиза называют его «Показывать правильные ответы
после прохождения». Это соседний участок (вкладка «Обратная связь и итоги»),
поэтому в таблицу находок он не вынесен.

Вывод по пункту: расхождение эскиза снято реализацией в пользу верного адреса;
править надо эскиз, а не код. В таблице оно значится находкой № 4 только как
формальное текстовое расхождение с утверждённым эскизом.

### D. Вкладка «Оценка результата»

| № | Место (раздел/секция) | Эскиз | Реализация | Тип | Важность | Ссылка на код |
| --- | --- | --- | --- | --- | --- | --- |
| D-1 | Оценка ответа, строка умолчания | Input `ou-field--m` (эскиз, строка 2171) | `Input size="s"` | не тот размер контрола | косметическое | `scoring-section.tsx:184` |
| D-2 | Оценка ответа, колонка «Состояние» | у переопределённой строки тег `ou-tag--warning` «задано в тесте» (эскиз, строка 2243) | тег выводится ТОЛЬКО при устаревшей настройке («Настройка устарела»); у обычного переопределения колонка пуста | отсутствующий элемент | существенное | `scoring-section.tsx:330-339` |
| D-3 | Оценка ответа, таблица вопросов | `aria-label="Оценка вопросов темы «О компании»"` (строка 2193) | таблица без `aria-label` | потерянный атрибут | косметическое | `scoring-section.tsx:256` |
| D-4 | Оценка ответа, свёртка темы | классы `tb-fold-sec` / `tb-fold-sec-head` / `tb-fold-sec-name` (строки 2181-2185) | собственный дубликат `tb-qscoring__sec` / `__sec-head` / `__sec-name` с отдельными правилами CSS | расхождение разметки | косметическое | `scoring-section.tsx:221-234`, `tb-components.css:2365-2374` |
| D-5 | Оценка ответа, балл секции | `aria-label="Балл по умолчанию секции «О компании»"` (кавычки, строка 2190) | «Балл по умолчанию секции Демо: управленческие компетенции» — без кавычек | расхождение подписи | косметическое | `scoring-section.tsx:247` |
| D-6 | Вердикт, «Тест пройден, если» | `ou-formsection--stacked` + `h3.ou-formsection__title` (строки 990-993) | обведённая карточка `Card variant="outlined" size="sm"` + `CardHeader` | не тот контейнер раздела | существенное | `basic-settings-section.tsx:1369-1376` |
| D-7 | Вердикт, заголовок | «Тест пройден, если» | «Тест пройден, если:» (двоеточие) | расхождение подписи | косметическое | `basic-settings-section.tsx:1376` |
| D-8 | Вердикт, вариант 2 | «достигнут порог и пройдены все обязательные темы» (строка 1011) | «достигнут общий проходной порог и пройдены все обязательные темы» | расхождение подписи | косметическое | `basic-settings-section.tsx:1352` |
| D-9 | Вердикт, тело карточки | разделителя между радиогруппой и селектом нет | `hr.wf-sep` | лишний элемент | косметическое | `basic-settings-section.tsx:1390` |
| D-10 | Вердикт, общее правило | «Тип общего правила» и счётчик «Порог» — два поля во всю ширину друг под другом (строки 1034-1049) | оба поля в одной строке двумя колонками (`tb-pass-overall`) | расхождение раскладки | существенное | `basic-settings-section.tsx:1392-1445` |
| D-11 | Вердикт, порог | подпись «Порог», значение с суффиксом «%» (строки 1042-1046) | «Порог (%)» / «Порог (баллы)» | расхождение подписи | косметическое | `basic-settings-section.tsx:1422-1425` |
| D-12 | Вердикт, «Правила оценки тем» | `ou-formsection--stacked` + `h3.ou-formsection__title` (строки 1053-1056) | голый `h3.tb-topics-title` без обёртки раздела | не тот контейнер раздела | косметическое | `basic-settings-section.tsx:1451` |
| D-13 | Шкалы, подзаголовок карточки | «сумма · проценты · 17 вопросов» (строка 2812) | добавлен член «N уровней»: «сумма · без пересчёта · 3 уровня · 14 вопросов» | лишний элемент | косметическое | `scales-section.tsx:527-536` |
| D-14 | Шкалы, поле «Источник» | только Seg «Вопросы» / «Другие шкалы» (disabled), описания нет (строки 2838-2844) | под сегментом `p.ou-formfield__desc` «„Другие шкалы“ пока недоступны…» | лишний элемент | косметическое | `scales-section.tsx:675-678` |
| D-15 | Шкалы, верхняя сетка формы | три группы `ou-formgroup--two`, gap 16 px | один `Grid cols={2} gap={3}` — 12 px | нарушение сетки 4/16/24 | косметическое | `scales-section.tsx:629`, `vendor/ui-kit/css/university-rt.css:3354` |
| D-16 | Шкалы, «Уровни шкалы» и «Границы шкалы и показ результата» | подписи ПОЛЯ (`ou-formfield__lbl` + `ou-formfield__desc`) внутри тела карточки (строки 2862-2864, 2982-2983) | `hr.wf-sep` + `div.tb-section-label` + `p.tb-card-desc` — линейка-разделитель, которой в эскизе нет | лишний элемент, не тот контрол | существенное | `scales-section.tsx:702-712`, `:724-725` |
| D-17 | Шкалы, «Задать предел показа на диаграмме» | «Читается, только когда у экрана итогов выбран предел оси „заданный автором“.» (строка 3019) | добавлено второе предложение «Нужен, когда домен недостижимо велик: фигура иначе жмётся к центру и различия шкал пропадают.» | расхождение текста подсказки | косметическое | `scales-section.tsx:1008` |
| D-18 | Шкалы, «Слоты карточки» | «Выключается показ слота — на экране итогов и в отчёте…» (строка 3048) | «Выключается ПОКАЗ слота — …» (заглавными) | расхождение текста подсказки | косметическое | `scales-section.tsx:957` |
| D-19 | Конструктор уровней, поля покрытия | `ou-field--m`, `aria-label` «Начало шкалы» / «Конец шкалы» (строки 2869, 2881) | `Input size="s"`, `aria-label` «Начало» / «Конец» | не тот размер, расхождение подписи | косметическое | `levels-editor.tsx:161-170`, `:214-223` |
| D-20 | Конструктор уровней, шапка карточки | заголовок — КОД уровня (`basic`, `confident`, `advanced`; строки 2887, 2921, 2955) | заголовок — название уровня («Базовый»), код только в поле | расхождение подписи | косметическое | `levels-editor.tsx:73-75`, `:269` |
| D-21 | Конструктор уровней, удаление | `aria-label="Удалить уровень «basic»"` (строка 2890) | «Удалить уровень 1» | расхождение подписи | косметическое | `levels-editor.tsx:277` |
| D-22 | Конструктор уровней, сетка карточки | Input `ou-field--m`, без подсказки и placeholder (строки 2893-2900) | `Input size="s"`; у «Кода уровня» добавлены hint «Машинное имя для формул показателей» и `placeholder="напр. high"` | не тот размер, лишний элемент | косметическое | `levels-editor.tsx:286-306` |
| D-23 | Конструктор уровней, «Как трактовать» | Seg с `aria-label="Трактовка уровня"`, подсказки справа нет (строка 2904) | `aria-label="Оценка уровня N"`, добавлена подпись «Авто — цвет по направлению шкалы» | расхождение подписи, лишний элемент | косметическое | `levels-editor.tsx:309-321` |
| D-24 | Конструктор уровней, «Добавить уровень» | ghost, размер **m**, центрирована в `div.tb-levels__add` (строки 2977-2979) | ghost, размер **s**, без обёртки, слева | не тот размер, расхождение раскладки | косметическое | `levels-editor.tsx:373-383` |
| D-25 | Конструктор уровней, карточка уровня | тело заканчивается строкой «Как трактовать» | добавлены складка «Толкование для обучающегося» и строка «Рекомендации» с бейджем | лишний элемент | косметическое | `levels-editor.tsx:326-368` |
| D-26 | Конструктор уровней, низ | баннера нет | постоянный `Banner tone="info"` «„Код уровня“ — машинное имя…» | лишний элемент | косметическое | `levels-editor.tsx:415-421` |
| D-27 | Конструктор уровней, порог | `aria-label="Порог между уровнями «Базовый» и «Уверенный»"` (строка 2911) | «Порог между уровнями 1 и 2» | расхождение подписи | косметическое | `levels-editor.tsx:245` |
| D-28 | Конструктор уровней и вклады, интервалы | §7.4: `.tb-levels`, `.tb-levels__cover`, `.tb-levels__grid`, `.tb-levels__tone`, `.tb-fold-sec__body` — 16 px | 8 px (`.tb-levels`, column-gap `.tb-levels__cover`) и 12 px (`__grid`, `__tone`, `.tb-fold-sec__body`) | нарушение сетки 4/16/24 | косметическое | `tb-components.css:2403`, `:2542`, `:2546`, `:2622`, `:2631` |
| D-29 | Вклады вопросов, карточка вопроса | заголовок = пиктограмма типа + «1. текст» (строка 3108) | только «1. текст», пиктограммы нет | отсутствующий элемент | косметическое | `scales-section.tsx:1480`, `:1491` |
| D-30 | Вклады вопросов, подзаголовок | только теги шкал, у непривязанного — «· не привязан» (строки 3085, 3109) | подзаголовок начинается с типа словами («распределение баллов», «один выбор») | лишний элемент | косметическое | `scales-section.tsx:1492-1493` |
| D-31 | Вклады вопросов, матрица | `aria-label="Вклады вариантов ответа в шкалы"` (строка 3117) | таблица без `aria-label` | потерянный атрибут | косметическое | `scales-section.tsx:1539-1543` |
| D-32 | Вклады вопросов, ячейка вклада | счётчик `ou-number--s ou-number--split` со степперами «Меньше»/«Больше», `aria-label="Вклад"` (строки 3134-3140) | `Input size="s"` без степперов, `aria-label` «Вклад «…» в шкалу …» | не тот тип контрола | существенное | `scales-section.tsx:1601-1629` |
| D-33 | Вклады вопросов, шапка свёртки | один тег `ou-tag--neutral ou-tag--outline` «41 вопрос» (строка 3077) | добавлен тег `ou-tag--warning` «{N} не привязано» перед нейтральным | лишний элемент | косметическое | `scales-section.tsx:1416-1424` |
| D-34 | Модалка оценки вопроса, подвал | «Предпросмотр балла» — secondary (`s-qscoring`, `s-qscoring-tiered`, `s-qscoring-weighted`) | `Button variant="ghost"` | не тот вариант кнопки | косметическое | `question-scoring-modal.tsx:144-152` |
| D-35 | Модалка оценки вопроса, подвал и шапка | раздел 9 п. 3: «Применить» (`s-qscoring`) против «Сохранить» (два других состояния); «Секция «…»» против «Тема «…»» | фактически: главная кнопка — «Применить», описание шапки — «Секция «Демо: управленческие компетенции»»; то есть выбран вариант `s-qscoring` | разрешённое расхождение эскиза | косметическое | `question-scoring-modal.tsx:126-127`, `:159` |
| D-36 | Модалка, строка «Цена ответа» | `div.tb-qscoring__price` + тег `ou-tag--neutral ou-tag--outline` (строки эскиза `s-qscoring`, п. 5.30.3) | `Cluster justify="between"` + `Tag variant="outline"` без тона `neutral`, класса `tb-qscoring__price` нет | расхождение разметки | косметическое | `scoring-builder.tsx:143-148` |
| D-37 | Модалка, режим «Веса опций» | `table.tb-table.tb-weights` с колонками «Вариант ответа» и «Балл» (п. 5.32) | не таблица: `Grid template="label-control"` из `div`, заголовки — `div` с инлайновым `font-size:12px; font-weight:500` | не тот тип контрола, обход ДС | существенное | `scoring-builder.tsx:168-189` |
| D-38 | Модалка, `s-qscoring-weighted` | полей «Балл за вопрос» и «Сложность» на этом экране НЕТ (§8 п. 9) | поля показываются всегда, для любого типа вопроса | лишний элемент | косметическое | `question-scoring-modal.tsx:184-209` |
| D-39 | Модалка, ступени | фиксированные ширины §7.7: счётчик 152 px, оператор 88 px, балл ступени 88 px | `tb-fw-lg` = 144 px, `tb-fw-sm` = 80 px (и оператор, и балл ступени) | нарушение фиксированных размеров | косметическое | `scoring-builder.tsx:207`, `:218`, `:256`; `tb-components.css:2440-2442` |
| D-40 | Показатели, заголовок раздела | `ou-formsection--stacked` + `h3.ou-formsection__title` «Показатели» (строки 3262-3265) | мелкая подпись `div.tb-section-label` «Показатели результата[ · порядок вычисления]», раздела нет | не тот уровень заголовка, расхождение подписи | существенное | `result-variables-section.tsx:271-274` |
| D-41 | Показатели, «Добавить показатель» | отдельная панель `ou-formactions` под заголовком, кнопка слева (строки 3267-3272) | кнопка в одной строке с подписью, прижата вправо | расхождение раскладки | косметическое | `result-variables-section.tsx:271-286` |
| D-42 | Показатели, тело карточки | между «Имя/Метка» и «Формула» разделителя нет | `hr.wf-sep` | лишний элемент | косметическое | `result-variables-section.tsx:530` |
| D-43 | Показатели, поле имени | «Имя показателя» (`s-metrics-number` строка 4517, `s-metrics-builder` строка 4780) | «Имя» (совпадает с `s-metrics`) | расхождение подписи | косметическое | `result-variables-section.tsx:508` |
| D-44 | Показатели, выбор способа формулы | раздел 9 п. 4: «Формула» + «Конструктор»/«DSL», `ou-seg--s` (`s-metrics`) против «Как задать формулу» + «Конструктор»/«Выражение», `ou-seg--m`, `aria-label="Способ задания формулы"` (`s-metrics-builder`) | фактически смешано: подпись «Формула», элементы «Конструктор»/«DSL», `aria-label="Режим редактора формулы"`, но размер **m** | разрешённое расхождение эскиза | косметическое | `result-variables-section.tsx:533-541` |
| D-45 | Показатели, поле DSL | Textarea `rows="4"` (строка 3308) | `rows={5}` | не тот размер контрола | косметическое | `result-variables-section.tsx:562` |
| D-46 | Показатели, «Толкование результата» и «Вывод» | подписи ПОЛЯ (`ou-formfield__lbl`, строки 3317, 3339) | `hr.wf-sep` + `div.tb-section-label` | лишний элемент, не тот контрол | косметическое | `result-variables-section.tsx:580-585`, `:658-660` |
| D-47 | Показатели, таблица исходов | `aria-label="Исходы показателя"`, четвёртая колонка озаглавлена видимым «Действия» (строки 3318-3326) | `aria-label` нет; четвёртый заголовок — `span.sr-only` «Действия» | потерянный атрибут, скрытая подпись | косметическое | `outcomes-editor.tsx:90`, `:102` |
| D-48 | Показатели, блок «Вывод» | «Показывать обучающемуся» и «Управление статусом курса» парой, «Передавать в LMS» — отдельным селектом во всю ширину ниже (строки 3341-3360) | все три селекта в одном `Grid cols={2}` | расхождение раскладки | косметическое | `result-variables-section.tsx:661-700` |
| D-49 | Показатели, «Толкование результата» | постоянного баннера нет | постоянный `Banner tone="info"` с пояснением по типу | лишний элемент | косметическое | `result-variables-section.tsx:648-656` |
| D-50 | Числовой показатель, границы | подпись группы «Границы показателя» (строка 4656) | подписи группы нет — сразу переключатель | отсутствующий элемент | косметическое | `result-variables-section.tsx:605-617` |
| D-51 | Числовой показатель, переключатель границ | «Выключено — границы берутся из охвата уровней.» (строка 4660) | добавлено «Ноль — законная граница, а не признак „не задано“.» | расхождение текста подсказки | косметическое | `result-variables-section.tsx:613` |
| D-52 | Числовой показатель, порядок полей | «Благоприятное направление» и «Показывать обучающемуся» стоят парой в одной группе (строки 4686-4700) | разнесены: направление — в «Толковании результата», видимость — в «Выводе» | расхождение порядка | косметическое | `result-variables-section.tsx:618-629`, `:668-677` |
| D-53 | Числовой показатель, лента покрытия | подпись «Покрытие» (строка 4544; раздел 9 п. 7) | всегда «Покрытие шкалы» — редактор общий со шкалами | расхождение подписи | косметическое | `levels-editor.tsx:158` |
| D-54 | Конструктор формулы | блок «Что получится»: `tb-formula-preview` с собранным выражением + описание «Выражение собирается конструктором…» (строки 4818-4822) | блока нет; вместо него баннер валидации «Синтаксис корректен…» | отсутствующий элемент | существенное | `result-variables-section.tsx:576-578`, `:780-978` |
| D-55 | Конструктор формулы, «Сертификация / вердикт» | подпись поля «Условия» (строка 4801) | «Все условия должны выполняться (И)» | расхождение подписи | косметическое | `result-variables-section.tsx:807` |
| D-56 | Конструктор формулы, строка условия | «Тест целиком», «Тема «Информационная безопасность»», свойство «Процент» (строки 4803-4816) | «Общий результат», тема — просто название темы, свойство «процент» строчными | расхождение подписи | косметическое | `result-variables-builder.ts:69-79`, `:119-124` |
| D-57 | Конструктор формулы, строка условия | четыре колонки, кнопки удаления условия нет | добавлен `IconButton` «Удалить условие» при более чем одном условии | лишний элемент | косметическое | `result-variables-section.tsx:1049` |
| D-58 | Конструктор формулы, статус курса | «Управление статусом курса» = «Ставит „Пройден“, когда истина» + описание «Доступно только для показателей типа „да/нет“.» (строки 4823-4830) | опции «Нет» / «Успех (success_status)» / «Завершение (completion_status)», описания у поля нет | расхождение подписи, отсутствующий элемент | косметическое | `result-variables-section.tsx:95-99`, `:678-689` |

### E. Вкладка «Обратная связь и итоги»

| № | Место (раздел/секция) | Эскиз | Реализация | Тип | Важность | Ссылка на код |
| --- | --- | --- | --- | --- | --- | --- |
| E-1 | Во время теста | Раздел «Показ правильных ответов» — `ou-formsection` с заголовком `h3` (эскиз, строки 1385-1397) | Заголовка раздела нет: переключатель стоит голым `div.ou-formfield` | Отсутствующий элемент | Существенное | `basic-settings-section.tsx:281-302` |
| E-2 | Во время теста | Подпись «Показывать правильные ответы после прохождения» (5.15) | «Показывать правильные ответы после ответа» | Расхождение подписи | Существенное | `basic-settings-section.tsx:285` |
| E-3 | Во время теста | Заголовок раздела «Обратная связь вопросов» (5.15, строка 1401) | «По вопросам» | Расхождение подписи | Существенное | `question-feedback-registry.tsx:96` |
| E-4 | Во время теста | У раздела подзаголовка нет | Добавлен подзаголовок «Только чтение: обратная связь принадлежит вопросу и правится в его редакторе — тот же вопрос стоит и в других тестах.» | Лишний элемент | Косметическое | `question-feedback-registry.tsx:97` |
| E-5 | Во время теста | «Развернуть все» / «Свернуть все» с иконками `i-chevrons-up-down` и `i-chevrons-down-up` (строки 1404-1405) | Кнопки без иконок | Отсутствующий элемент | Косметическое | `question-feedback-registry.tsx:100-115` |
| E-6 | Во время теста | Подзаголовок темы «16 вопросов · у 12 задана обратная связь» (5.15) | «вопросов 9 · с обратной связью 0» — иной порядок слов и формулировка | Расхождение подписи | Косметическое | `question-feedback-registry.tsx:140` |
| E-7 | Во время теста | Таблица с колонками «Вопрос», «Режим», «Текст», безымянная колонка действия (5.15) | Таблицы нет: строки `div.tb-qfeedback__row`, шапки и колонок нет, режим — тег, тексты — строки с подписями | Не тот тип контрола | Существенное | `question-feedback-registry.tsx:148-181` |
| E-8 | Во время теста | Кнопка строки «К вопросу», ghost, s, иконка `i-arrow-right` (5.15) | «Открыть вопрос», иконка `Pencil` | Расхождение подписи и иконки | Существенное | `question-feedback-registry.tsx:171-179` |
| E-9 | Состав итогов | Порядок разделов: «Порядок подблоков», затем «Подытоги по подтемам», затем надписи (5.16, строки 1162, 1206, 1243) | «Подытоги по подтемам», затем семь групп надписей, «Порядок подблоков» — последним | Расхождение порядка | Существенное | `editor-tabs.tsx:344-362`, `results-labels-pane.tsx:182-191` |
| E-10 | Состав итогов | Подсказка: «Полосы по подтемам (тегам вопросов) на карточке темы: итоги раздела, итоги теста и отчёт. «Не показывать» убирает подытоги совсем и прячет два поля ниже.» (5.16) | «Полосы по подтемам (тегам вопросов): на экране итогов теста и в отчёте. Экран «Итоги раздела» подытогов не печатает. «Не показывать» убирает подытоги совсем и прячет два поля ниже.» | Расхождение текста подсказки | Косметическое | `basic-settings-section.tsx:1257` |
| E-11 | Состав итогов → Порядок подблоков | Только заголовок `h3`, подзаголовка нет (строки 1160-1164) | Добавлен подзаголовок «Блоки печатаются под общим заголовком итогов в этом порядке. Выключенный заголовок блок не убирает — за видимость отвечают настройки самих блоков.» | Лишний элемент | Косметическое | `results-labels-pane.tsx:310` |
| E-12 | Состав итогов → Порядок подблоков | Строка `wf-order-row`: рамка `--ou-border-soft`, радиус `s`, padding 8/12 (эскиз, строки 425-433) | Плоская строка без рамки и подложки: `Stack` + `Text`; CSS для строк порядка в проектном слое нет | Потерянное оформление | Косметическое | `results-labels-pane.tsx:313-322`; в `client/src/styles/` селекторов нет |
| E-13 | Состав итогов | Интервалы: между строками порядка 1x = 4 px (`.wf-order > * + *`, раздел 7.3), между подписью и полем внутри строки надписи — 4 px (`.ou-formfield` gap 4) | `Stack gap={2}` = `--ou-space-2` = 8 px в обоих местах | Нарушение сетки 4/16/24 | Косметическое | `results-labels-pane.tsx:223`, `results-labels-pane.tsx:312`; токен — `client/src/styles/vendor/university-rt.css:389` |
| E-14 | Состав итогов → надписи | У каждой строки с пустым значением — подсказка «Пусто — печатается формулировка шаблона.» (5.18, строки 785-787) | Такой подсказки нет ни у одной строки: hint выводится только когда пуст сам подставляемый текст | Отсутствующий элемент | Существенное | `results-labels-pane.tsx:218-221` |
| E-15 | Состав итогов → «Вердикт: нет порога» | Подсказка «Шаблон не печатает эту надпись — задайте свою, если она нужна.» (5.18, строка 808) | «Эта надпись не печатается на экране итогов.» | Расхождение текста подсказки | Косметическое | `results-labels-pane.tsx:220` |
| E-16 | Обратная связь | Порядок: «Вводный текст» первым, «Общая обратная связь теста» вторым (5.17, строки 1489 и 1513) | «Общая обратная связь теста» первой, «Вводный текст» второй | Расхождение порядка | Существенное | `basic-settings-section.tsx:316-387` |
| E-17 | Обратная связь | Разделы «По уровням шкал» и «По уровням показателей» (5.17, строки 1714 и 1792) | Разделов нет; в клиентском коде таких компонентов нет вовсе (поиск по строкам даёт только `docs/`) | Отсутствующий элемент | Существенное | `editor-tabs.tsx:373-388` |
| E-18 | Обратная связь | Раздел «По уровням» показан у СТАНДАРТНОГО теста эскиза, вторая тема помечена тегом «адаптивность выключена» (5.17) | Раздел рисуется только при `model.mode === "adaptive"` | Потерянное состояние | Существенное | `editor-tabs.tsx:384-386`, `level-feedback-card.tsx:37` |
| E-19 | Обратная связь → «По подтемам (тегам)», «По уровням» | Заголовок в `tb-section-head` с «Развернуть все» / «Свернуть все»; темы — свёртки `tb-fold-sec` с тегом «3 подтемы» / «2 уровня» (5.17, строки 1584-1600) | Панели свёртки нет, свёрток нет, тега со счётчиком нет: плоский список разделов формы | Отсутствующие элементы | Существенное | `breakdown-feedback-card.tsx:92-101`, `level-feedback-card.tsx:62-77` |
| E-20 | Обратная связь → «Общая обратная связь теста» | Превью без подписи поля (строки 1513-1527) | Появилась подпись поля «Обратная связь после прохождения» | Лишний элемент | Косметическое | `basic-settings-section.tsx:316-337` |
| E-21 | Обратная связь → «По темам» | Переопределённый текст помечается ТОЛЬКО жёлтой полосой слева, без подписи (решение эскиза 13, раздел 6.1) | Кроме полосы добавлен тег «Задано в этом тесте» / «Из темы» | Лишний элемент | Существенное | `topic-feedback-card.tsx:139-143` |
| E-22 | Обратная связь → «По темам» | Название темы — подпись поля `label.ou-formfield__lbl` внутри раздела «По темам» (строки 1536-1539) | Каждая тема — отдельный `FormSection` с заголовком `h3` | Не тот тип контрола | Существенное | `topic-feedback-card.tsx:138` |
| E-23 | Обратная связь | Пустое превью подписано «Без текста» (5.17) | «Не задано — ни в теме, ни в этом тесте» (темы) и «Не задано — нажмите для редактирования» (подтемы) | Расхождение подписи | Косметическое | `topic-feedback-card.tsx:171`, `feedback-preview.tsx:83` |
| E-24 | Вся вкладка | Название темы с порядковым номером: «1. О компании», «2. Корпоративные финансы» (5.15, 5.17) | Номера нет: «Демо: управленческие компетенции» | Расхождение подписи | Косметическое | `question-feedback-registry.tsx:138`, `topic-feedback-card.tsx:138` |
| E-25 | Вся вкладка | Разделы — `ou-formsection ou-formsection--stacked` с заголовком `h3`, карточек нет ни в одном состоянии участка | «По вопросам», «Общая обратная связь теста», «Вводный текст», «По темам», «По подтемам (тегам)», «Отчёт о результатах» обёрнуты в `Card variant="outlined"` | Не тот тип контрола | Существенное | `question-feedback-registry.tsx:94`, `basic-settings-section.tsx:316`, `basic-settings-section.tsx:342`, `topic-feedback-card.tsx:89`, `breakdown-feedback-card.tsx:92`, `report-settings-card.tsx:238` |
| E-26 | Отчёт | «Что показывать в отчёте» — один `Select` со значением «Итоги и разбор по темам» (5.19, строки 3450-3457) | Это подпись ГРУППЫ полей вида: сам `Select` вида живёт только на стороне «Оформления», а под подписью рисуется сетка полей шаблона в две колонки | Не тот тип контрола | Существенное | `report-settings-card.tsx:305-321`, `report-settings-card.tsx:275` |
| E-27 | Отчёт | Переключатель «Выдавать отчёт обучающемуся» без описания (строки 3446-3451) | Добавлено описание «Кнопка «Скачать отчёт» на экране результатов. Выключите, если документ по этому тесту не выдаётся.» | Лишний элемент | Косметическое | `report-settings-card.tsx:246-251` |
| E-28 | Отчёт | «Предпросмотр отчёта» — secondary, размер `s`, иконка `i-eye` (5.19, строка 3572) | secondary, размер `m`, иконки нет | Не тот вариант кнопки | Косметическое | `report-settings-card.tsx:368-378` |
| E-29 | Отчёт → строка блока | Меню действий — иконка `i-more-vertical` (5.19) | `MoreHorizontal` (горизонтальное многоточие) | Не та иконка | Косметическое | `report-document-list.tsx:328-341` |
| E-30 | Отчёт → «Разрыв листа» | Название обычным регистром, без линий (5.19, скриншот `wf-report.png`) | «РАЗРЫВ ЛИСТА» капслоком и с пунктирными линиями по бокам | Расхождение оформления | Косметическое | `report-document-list.tsx:254`; видно на `impl-t5-...-r4-отч-т.png` |
| E-31 | Отчёт | После «Предпросмотра отчёта» подраздел заканчивается (5.19, строка 3576) | Добавлена карточка «Заголовки и подписи отчёта» со вторым слоем надписей | Лишний элемент | Существенное | `editor-tabs.tsx:397-404`, `design-section.tsx:571-602` |
| E-32 | Палитра блоков | `div.ou-modal.ou-modal--xl` (5.33, строка 4366) | `ModalDialog size="m"` | Не тот размер | Существенное | `report-block-palette.tsx:157` |
| E-33 | Палитра блоков | Тело — сетка `variant-picker`: слева список 320 px (мин. 220 px), справа живое превью `tb-sheet` шириной до 320 px (5.33, разделы 7.7) | Правой колонки нет вовсе: только поиск и список; ни `variant-picker`, ни `variant-picker__preview`, ни `tb-sheet` не рисуются | Отсутствующий элемент | Существенное | `report-block-palette.tsx:176-199` |
| E-34 | Палитра блоков → «Страницы» | У каждого пункта своё описание: «Свободный текст: заголовок и абзацы», «Тексты по темам и подтемам из обратной связи», «Вопросы, ответы участника и верные ответы» (5.33) | Всем страницам подставляется одна жёстко зашитая строка «Авторская страница документа.» | Расхождение текста | Существенное | `report-block-palette.tsx:71` |
| E-35 | Палитра блоков → «Служебное» | «Разрыв листа» — «Следующий блок печатается с новой страницы» (5.33) | «Следующий блок начнётся с нового листа документа.» | Расхождение текста | Косметическое | `report-block-palette.tsx:80-81` |
| E-36 | Палитра блоков → «Удалённые из документа» | Описание пункта — «Убран из документа — вернётся на прежнее место» (5.33) | «Вернуть системный блок в документ.» | Расхождение текста | Косметическое | `report-block-palette.tsx:91` |

#### Где фактически живёт карточка отчёта

Замечание эскиза (раздел 9, пункт 6) о ссылке на несуществующую вкладку
«Настройки» в реализации СНЯТО.

- Карточка отчёта живёт во вкладке «Обратная связь и итоги», подраздел «Отчёт»
  (четвёртый пункт рельса): `editor-tabs.tsx:389-405` монтирует `ReportContentPane`,
  та — `ReportSettingsCard scope="content"` (`basic-settings-section.tsx:399-401`,
  `report-settings-card.tsx:152`). Вкладки «Настройки» в риббоне из семи вкладок нет.
- Мета-тег блока «Вводный блок» говорит: «Текст задаётся в подразделе «Обратная
  связь», карточка «Вводный текст»» (`report-document-list.tsx:376-380`). Это верно:
  карточка «Вводный текст» действительно стоит в подразделе «Обратная связь»
  (`basic-settings-section.tsx:342-343`). Формулировка эскиза («во вкладке
  «Настройки», блок «Вводный текст»») исправлена и дефектом не считается.

### F. Вкладки «Оформление» и «Комментарии»

| № | Место (раздел/секция) | Эскиз | Реализация | Тип | Важность | Ссылка на код |
| --- | --- | --- | --- | --- | --- | --- |
| F-1 | Оформление, рельс в состоянии «шаблон без параметров» | Рельс из ОДНОГО пункта «Шаблон» (`editor-settings-target.html:1903-1905`, спека 5.22) | «Облик отчёта» показывается всегда, поэтому рельс не может схлопнуться до одного пункта — минимум два | Лишний элемент | Косметическое | `client/src/features/tests/editor/sections/design-section.tsx:189-197` |
| F-2 | Оформление → Шаблон, кнопка «Заменить шаблон» | Кнопка secondary s БЕЗ иконки (`editor-settings-target.html:1338`) | Кнопка с ведущей иконкой `Layout` 12 px | Лишний элемент | Косметическое | `client/src/features/tests/editor/sections/design-section.tsx:538-546` |
| F-3 | Оформление → Шаблон, блок сведений `tpl-info` | `gap: 16`, у `tpl-info__head`, `tpl-desc`, `tpl-actions` — `margin: 0` (`editor-settings-target.html:370-372`; спека 7.4 прямо называет `.tpl-info`) | `gap: 8`, у `.tpl-actions` дополнительный `margin-top: 4` | Нарушение сетки 4/16/24 | Косметическое | `client/src/styles/tb-components.css:2146-2161` |
| F-4 | Оформление, ВСЕ панели («Шаблон», «Брендирование», «Цвета», «Вид диаграмм») | Блоки панели — прямые дети `.tb-settings-content`, между ними 16 px (спека 7.4; `tb-components.css:183-184`) | Каждая панель обёрнута в собственный `div[data-testid]`, правило `.tb-settings-content > *` до её блоков не достаёт: строки параметров, таблица цветов и баннеры идут без интервала (видно на снимках «Брендирование» и «Вид диаграмм» — подсказка одного поля и подпись следующего слиты) | Нарушение сетки 4/16/24 | Существенное | `design-section.tsx:450`, `:661`, `:673`, `:725`, `:781`; `client/src/styles/tb-components.css:183-184` |
| F-5 | Оформление → Шаблон, баннер «Шаблон обновлён» | Баннер — прямой ребёнок панели, поэтому полноширинный и липкий, без скруглений и боковых полей (`editor-settings-target.html:1319-1327`; правило `tb-components.css:148-158`) | Баннер лежит внутри обёртки панели, под правило не попадает — печатается обычным баннером в отступах панели | Не то место / не тот облик | Существенное | `design-section.tsx:450-472` |
| F-6 | Оформление → Цвета, переключатель «Тема теста» | `ou-seg ou-seg--s` — размер s (`editor-settings-target.html:4001`) | `SegmentedControl` без `size`, то есть размер m (`ou-seg` без модификатора) | Не тот размер контрола | Косметическое | `design-section.tsx:787-793`; `vendor/ui-kit/src/components/SegmentedControl.tsx:33` |
| F-7 | Оформление → Цвета, кнопка «Предпросмотр» | `design-theme-head` = `flex; gap: 16` без выравнивания по краям: кнопка стоит сразу за переключателем (`editor-settings-target.html:178`) | `justify-content: space-between; gap: 12` — кнопка прижата к правому краю панели (снимок `impl-t6-оформление--r3-цвета.png`) | Не то место + шаг 12 px вне сетки | Косметическое | `client/src/styles/tb-components.css:2178-2185` |
| F-8 | Оформление → Цвета, ячейка цвета | `.design-color-field { gap: 4 }` (`editor-settings-target.html:179`) | `gap: 8` | Нарушение сетки 4/16/24 | Косметическое | `client/src/styles/tb-components.css:2165-2169` |
| F-9 | Оформление → Цвета, подсказка параметра в таблице | Шрифт `--ou-text-body-xs` = 400 11/15 (`editor-settings-target.html:180`) | Шрифт `--ou-text-caption` = 500 11/16 — другое начертание и интерлиньяж | Расхождение облика текста | Косметическое | `client/src/styles/tb-components.css:2196-2199` |
| F-10 | Комментарии, панель управления | Три элемента: «Добавить комментарий» (primary s), фильтр, «Следующий комментарий» (secondary s) — `prd52-review-panel.html:159` | Кнопки «Следующий комментарий» нет | Отсутствующий элемент | Существенное | `client/src/features/tests/review/review-panel.tsx:93-113` |
| F-11 | Комментарии, фильтр «Только открытые» | Порядок: Switch, затем подпись (`prd52-review-panel.html:156-157`) | Порядок обратный: подпись `span.rvp__toggle-lbl`, затем Switch | Расхождение порядка элементов | Косметическое | `review-panel.tsx:104-112` |
| F-12 | Комментарии, шапка ветки и шапка ответа | Перед именем автора — аватар `ou-avatar ou-avatar--xs` с инициалами (`prd52-review-panel.html:167`, `:187`, `:198`, `:220`, `:231`) | Аватара нет: сразу имя автора | Отсутствующий элемент | Существенное | `review-panel.tsx:164-169`, `:177-182` |
| F-13 | Комментарии, кнопка перехода в строке действий | «Перейти к вопросу» (`prd52-review-panel.html:176`, `:207`, `:447`) | «Перейти к месту» | Расхождение подписи | Косметическое | `review-panel.tsx:259` |
| F-14 | Комментарии, итоговая строка | «Всего 7 комментариев · открытых 3 · от двух экспертов» (`prd52-review-panel.html:246`) | «Всего {N} · открытых {M}» — потеряно слово «комментариев» и сведение о числе экспертов | Расхождение текста, потеря сведения | Существенное | `review-panel.tsx:294-298` |
| F-15 | Комментарии, заголовок группы | `div.tb-section-label` — body-s, 600, приглушённый; DS-mapping эскиза прямо называет этот класс (`prd52-review-panel.html:162`, `:215`, `:561`) | Собственный `.rvp__group-title` — caption, ПРОПИСНЫЕ, разрядка 0.04em | Не тот компонент / облик | Косметическое | `client/src/features/tests/review/review-panel.css:9-14` |
| F-16 | Комментарии, вкладка в риббоне | На вкладке тег-счётчик открытых `ou-tag--warning ou-tag--s` со значением «3» (`prd52-review-panel.html:147`, `:262`, `:318`, `:387`, `:430`; DS-mapping `:560`) | Счётчика нет — только подпись и общая точка состояния. Примечание: целевой эскиз счётчика тоже не рисует, эскизы расходятся | Отсутствующий элемент | Косметическое | `client/src/features/tests/editor/test-editor.tsx:479-487` |
| F-17 | Комментарии, форма нового комментария | Форма лежит в карточке `ou-card ou-card--outlined ou-card--sm` с заголовком «Новый комментарий» (`prd52-review-panel.html:391-393`) | Форма — голый `Stack.rvp__form`: ни карточки, ни заголовка | Отсутствующий элемент | Существенное | `client/src/features/tests/review/review-comment-form.tsx:57-58` |
| F-18 | Комментарии, форма нового комментария — выбор места | Два Select в `ou-formgroup--two`: «Место» и «Вопрос или страница» (выключен, значение «Выберите раздел», подсказка «Доступно после выбора раздела.») — `prd52-review-panel.html:395-404`, DS-mapping `:565` | Один Select «Место» во всю ширину; второго поля нет — из ящика нельзя поставить якорь на конкретный вопрос или страницу | Отсутствующий элемент | Существенное | `review-comment-form.tsx:42-55`, `:69-77` |
| F-19 | Комментарии, поле «Место» | Подсказка `ou-formfield__desc` «По умолчанию — тест в целом.» (`prd52-review-panel.html:399`) | Подсказки нет | Отсутствующий текст подсказки | Косметическое | `review-comment-form.tsx:69-77` |
| F-20 | Комментарии, поле «Место» | Select размера s — `ou-select ou-field--s` (`prd52-review-panel.html:398`) | `Select` без `size`, то есть размер m | Не тот размер контрола | Косметическое | `review-comment-form.tsx:70-76` |
| F-21 | Комментарии, поле текста комментария | `ou-textarea--m ou-textarea--full` БЕЗ подписи, только заполнитель (`prd52-review-panel.html:405`) | `Textarea` с подписью «Комментарий» (в ответе — «Ответ») | Лишний элемент | Косметическое | `review-comment-form.tsx:79-86` |
| F-22 | Комментарии, форма исхода — выбор исхода | Поле с подписью «Исход» и переключателем `ou-seg ou-seg--s ou-seg--accent` (`aria-label="Исход комментария"`) из пунктов «Учтено» / «Отклонено» (`prd52-review-panel.html:277-285`; DS-mapping `:563`) | Две отдельные кнопки «Учтено» и «Отклонено» (выбранная primary, вторая secondary), подписи поля нет | Не тот тип контрола + отсутствующая подпись | Существенное | `review-panel.tsx:201-218` |
| F-23 | Комментарии, форма исхода — поле «Ответ автора» | Заполнитель «Почему комментарий отклонён» (`prd52-review-panel.html:291`) | Заполнителя нет | Отсутствующий текст | Косметическое | `review-panel.tsx:219-228` |
| F-24 | Комментарии, форма исхода — подсказка поля | «При отклонении ответ обязателен: эксперт должен увидеть причину.» (`prd52-review-panel.html:293`) | «При отклонении ответ обязателен: рецензент должен увидеть причину.» | Расхождение текста в одном слове | Косметическое | `review-panel.tsx:222` |

### G. Сетка и оболочка: замеры

ВАЖНО О БАЗЕ ЗАМЕРА. Приёмка снималась с dev-сервера, а он отдаёт РАБОЧЕЕ ДЕРЕВО. В нём на тот
момент лежала незакоммиченная работа сессии `c0d7eb0e` — проектный слой сетки ящика по этому же
эскизу. Поэтому одиннадцать пунктов контракта отступов показали совпадение благодаря ей, а не
коду в `main`: на чистом коммите расходились все семнадцать. Работа принята владельцем и влита
в трек отдельным коммитом `cb9407c9`, так что таблица ниже верна для кода начиная с него.
Строка G-8 добавлена после того, как разбор эскиза нашёл две перебивки, пропущенные при ручном
замере (`.page-row` и `.tb-level-grid`; вторая на экране не встретилась).

Замеры сняты ведущей сессией одним и тем же скриптом на двух страницах: эскиз
`docs/wireframes/editor-settings-target.html` (статический сервер, порт 8123) и живой ящик
редактора на dev (`localhost:8081`, тест «Сертификация руководителей (демо: подытоги по
подтемам)»). Сравнивается вычисленное значение `getComputedStyle`, а не намерение в коде.

Эскиз объявляет 22 перебивки ui-kit ради правила модульной сетки 4/16/24 — они собраны в его
собственном блоке `<style>` (строки 50-359 файла эскиза, каждая с комментарием вида
`/* ui-kit: 20 -> 24 */`). Проверено, какие из них есть в приложении.

#### Расхождения сетки

| № | Селектор | Свойство | Эскиз | Реализация | Где найдено в реализации | Важность |
| --- | --- | --- | --- | --- | --- | --- |
| G-1 | `.ou-drawer__head` | `padding` | 24 px | 20 px | шапка ящика, все вкладки | существенное |
| G-2 | `.ou-tabs__list` | `padding-inline` | 24 px | 0 px | риббон вкладок, все вкладки | существенное |
| G-3 | `.ou-select` | `gap` | 4 px | СНЯТА: внутри ящика 4 px, замер был сделан вне его | ложное срабатывание | снято |
| G-4 | `.ou-btn` | `gap` | 4 px | 8 px | все вкладки | косметическое |
| G-5 | `.ou-radio-group__items` | `gap` | 4 px | 8 px | «Оценка результата» / «Вердикт» | косметическое |
| G-6 | `.ou-banner__body` | `gap` | 4 px | 2 px | «Правила прохождения» / «Навигация» | косметическое |
| G-8 | `.page-row` | `gap` | 4 px | 8 px | «Состав и сценарий» / «Сценарий» | косметическое |
| G-9 | `.tb-fold-actions` | `gap` | 4 px | 8 px | пары «Развернуть все / Свернуть все» на всех вкладках | косметическое |

Находка G-3 снята гейтом `npm run check:editor-ui`: замер брал ПЕРВЫЙ `.ou-select` в
документе, а это селект сортировки в списке тестов за подложкой ящика. Внутри ящика значение
равно требуемым 4 px. Урок записан в гейт: элементы ищутся только внутри `.ou-drawer`, иначе
страница за подложкой выдаётся за ящик — она носит те же классы дизайн-системы.

Совпадает: `.ou-drawer__foot` padding 24 px и gap 4 px, `.ou-drawer__rail` padding 24 px и
gap 4 px, `.tb-settings-content` padding 24 px, `.ou-number` gap 4 px, `.ou-textarea` gap
4 px, `.ou-switch-field` и `.ou-switch-field__text` gap 4 px, `.ou-formsection__body` gap
16 px.

Причина G-1 и G-2 установлена: правило ui-kit одинаково во всех трёх копиях CSS
(`vendor/ui-kit/css/university-rt.css:2850`, `client/src/styles/vendor/university-rt.css:2850`,
`docs/wireframes/ds/university-rt.css:3070` — везде `padding: var(--ou-space-5)`, то есть
20 px). Эскиз перебивает это в своём проектном слое, приложение — нет: в
`client/src/styles/tb-components.css` селектора `.ou-drawer__head` нет вовсе.

#### Подмена компонента

| № | Место | Эскиз | Реализация | Важность |
| --- | --- | --- | --- | --- |
| G-7 | Сворачиваемые карточки тем: «Оценка результата» / «Оценка ответа», «Оценка результата» / «Шкалы» (вклады вопросов), «Обратная связь и итоги» / «Во время теста», уровни шкал | СНЯТА: в этих местах эскиз рисует не аккордеон, а свои свёртки `tb-fold-sec` | ложное срабатывание | снято |

Находка снята при разборе партии «подмены компонентов». Класс `ou-acc ou-acc--separated`
встречается в целевом эскизе ровно трижды — список тем в «Составе» (строка 644), «Обратная
связь вопросов» (1410) и адаптивные темы (2336). Все четыре места, названные находкой,
нарисованы в эскизе разметкой `tb-fold-sec` / `tb-fold-sec-head` / `tb-fold-trigger` —
ровно той, которую реализация и выпускает (12 вхождений `tb-fold-sec` в эскизе). Вывод
«эскиз требует DS-аккордеона» был сделан по одному лишь отсутствию класса `ou-acc` в живой
разметке, без сверки с тем, что нарисовано на этом самом месте.

Верно в находке было одно: `Collapsible` — headless-примитив без CSS-слоя
(`.ou-collapsible` в `university-rt.css` не встречается ни разу). Но это его назначение:
оформление даёт разметка `tb-fold-*`, и `CollapsibleTrigger` проставляет кнопке
`aria-expanded`, которого требует эскиз. Подмены компонента здесь нет.

Из трёх мест, где эскиз действительно требует аккордеона, «Обратная связь вопросов»
переведена на `Accordion` ДС при разборе партии «разделы», список тем в «Составе» ждёт
находки B-7, а у адаптивных тем при разборе этой партии обнаружилось и исправлено
настоящее расхождение: корню списка недоставало классов `ou-acc ou-acc--separated`, из-за
чего вложенные `ou-acc__item` оставались без рамок и отступов.

#### Поведение полосы вкладок и рельса при сужении окна

Замерено на живом ящике при пяти ширинах окна.

| Ширина окна | Ширина ящика | Переполнение полосы вкладок | Ширина рельса | Ширина панели |
| --- | --- | --- | --- | --- |
| 1600 | 1040 | нет | 240 | 800 |
| 1366 | 1040 | нет | 240 | 800 |
| 1280 | 1040 | нет | 240 | 800 |
| 1024 | 992 | нет | 240 | 752 |
| 820 | 788 | да (1030 против 778) | 240 | 548 |

При ширине окна 820 px полоса вкладок переполняется и прокручивается горизонтально
(`overflow-x: auto`), стрелок прокрутки нет. Рельс разделов при этом не сворачивается — его
ширина остаётся 240 px на всех проверенных ширинах. Горизонтальной прокрутки самой страницы не
возникает ни на одной ширине.

#### Оболочка ящика: фактические значения

- Ширина ящика 1040 px, класс `ou-drawer ou-drawer--xl ou-drawer--right`, подложка
  `ou-drawer__backdrop`.
- Шапка: заголовок теста, тег статуса (`Опубликован` / `Изменено`), тег версии (`v16`),
  кнопка закрытия `ou-iconbtn ou-iconbtn--ghost ou-iconbtn--s`.
- Подвал в чистом состоянии: «Закрыть» (`ou-btn--ghost`, `data-testid="test-editor-cancel"`),
  «Сохранить» (`ou-btn--primary`, `disabled`). Проверено по разметке подвала.
- Заголовок ящика в реализации — `h1#tb-test-editor-title.ou-drawer__title`.
- Подвал в состоянии с изменениями: «Показать изменения» (`ou-btn--ghost`), «Отменить»
  (`ou-btn--secondary`), «Сохранить» (`ou-btn--primary`).
- Точка изменений на вкладке: `<span class="status-dot dirty" aria-label="есть изменения">`
  внутри `ou-tabs__label`.

## Что совпадает с эскизом

### A. Хром ящика и вкладка «Основное»

- Оболочка: `role="dialog"`, `aria-modal="true"`, подложка `ou-drawer__backdrop` с закрытием
  по клику, панель `aside.ou-drawer.ou-drawer--xl.ou-drawer--right`.
- Шапка: порядок «название теста — тег статуса — тег версии `v{N}` — крестик»;
  крестик `ou-drawer__close` с `aria-label="Закрыть редактор"` и иконкой X 16 px,
  заблокирован во время сохранения.
- Риббон: `Tabs variant="underline" size="m"`, `aria-label="Разделы редактора"`,
  семь вкладок ровно в порядке эскиза: Основное, Состав и сценарий, Правила прохождения,
  Оценка результата, Обратная связь и итоги, Оформление, Комментарии.
- Тело: `tabindex="0"`, модификатор `ou-drawer__body--flush` ставится всем вкладкам,
  КРОМЕ «Основного» и «Комментариев» — то есть рельса у «Основного» нет, как в `s-basic`.
- Вкладка «Основное»: состав и порядок полей совпадают — Название (обязательное, со звёздочкой),
  Описание (Textarea, rows=3, на всю ширину), Режим теста (`SegmentedControl` с сырой подписью
  `label.ou-formfield__lbl` и `aria-label="Режим теста"`, опции «Стандартный» / «Адаптивный»),
  Webhook URL (`type="url"`), «Отправлять телеметрию о прохождении» (Switch),
  «Результат для LMS при нескольких попытках» (Select, «Лучшая попытка» / «Последняя попытка»).
- Баннер блокирующей валидации: тон error, заголовок «Поля с ошибками: N», описание
  «Исправьте отмеченные поля — сохранение недоступно, пока есть ошибки.», действие
  «Перейти к ошибкам»; стоит первым элементом тела.
- Подвал в чистом состоянии: «Закрыть» (ghost, m) + «Сохранить» (primary, m);
  в грязном — «Показать изменения» (ghost, m, в обёртке `.tb-changes-anchor`, с `aria-expanded`),
  «Отменить» (secondary, m), «Сохранить» (primary, m); у подвала `data-state="dirty"`.
- Разрешение внутреннего расхождения эскиза № 1 (раздел 9): реализация делает «Отменить»
  **secondary**, то есть следует `s-dirty`, а не `s-changes`. Дефектом не считаю — вариант
  из более подробного состояния; расхождение эскиза закрывается в пользу secondary.
- Правило «Сохранить» блокируется при отсутствии изменений, при ошибках и во время
  сохранения (`saveDisabled`) — совпадает с 6.3; предупреждения не блокируют.
- «Отменить» сбрасывает черновик и закрывает ящик БЕЗ диалога, диалог показывается только
  по крестику и по клику в подложку — совпадает с решением 6.2 (переутверждено 2026-05-27).
- Оверлей сохранения: подложка `opacity: 0.88`, `inert` на содержимом, шапка и подвал видимы,
  крестик заблокирован, спиннер и текст «Сохранение…», кнопка подвала «Сохранение…» с `loading`.
- Поповер изменений: `role="dialog"`, заголовок «Изменения в черновике» + счётчик в скобках,
  крестик `aria-label="Закрыть список изменений"` (иконка 14 px), группы по вкладкам с
  заголовком прописными и склонением «изменение / изменения / изменений», строка
  «Структурные изменения (раскрыть детали невозможно).» для группы без детализации;
  всплывает над подвалом; текст кнопки «Показать изменения» при открытом поповере не меняется.
- Диалог закрытия: иконка `AlertTriangle` (warning) / `XCircle` (danger) при ошибках,
  заголовки «Есть несохранённые изменения» и «Есть несохранённые изменения и ошибки»,
  оба текста абзаца дословно как в эскизе, баннер ошибок с действием «Перейти к первой ошибке»,
  чипы изменённых разделов (`tb-close-confirm-chips`), кнопки в порядке
  «Продолжить редактирование» (ghost) — «Выйти без сохранения» (secondary) — «Сохранить»
  (primary, `disabled` + `title="Исправьте ошибки перед сохранением"`).
  Кнопки «Сбросить всё» нет ни в подвале, ни в диалоге (FR-05b).
- Диалог конфликта: заголовок «Конфликт версий», описание и абзац дословно как в эскизе,
  колонки таблицы в порядке «Поле» / «На сервере» / «Ваши изменения», кнопки
  «Отмена» (ghost) — «Сохранить поверх» (destructive) — «Обновить данные» (primary, `autoFocus`);
  номера версий пользователю не показываются.
- Сетка: `.tb-settings-content` — padding 24 и `margin-top: 16` между элементами,
  `.ou-formfield` — gap 4 и `margin-bottom: 0`, `.ou-drawer__foot` — padding 24 и gap 4,
  `.ou-drawer__rail` — padding 24 и gap 4, `.status-dot` — `margin-left: 4`.

### B. Вкладка «Состав и сценарий»

- Рельс вкладки: «Состав», «Сценарий» в стандартном тесте и «Состав», «Адаптивные уровни», «Сценарий»
  в адаптивном — состав, порядок и `aria-label` «Подразделы состава и сценария» совпадают с разделом 4
  описи (`editor-tabs.tsx:109-141`).
- Порядок настроек в теле карточки темы после «Обязательной»: «Все вопросы темы» → «Вопросов в тест»
  (счётчик + суффикс «из N») → «Порядок вопросов» (Select s, сброс `RotateCcw` с `title="Как в тесте"`,
  хвост-подсказка) → «Квоты по подтемам (тегам)» → «Пороги по подтемам (тегам)» → блок квот →
  «Варианты теста» → блок вариантов.
- Кнопка сброса порядка показана только при переопределении у темы; `aria-label` «Вернуть порядок как
  в тесте: {тема}» и `title` совпадают с эскизом.
- Строка итога квот: «Σ квот: {N} из {M} · остаток {K}» + тег «в пределах выборки» (success) —
  формулировка и тон тега совпадают.
- Блок вариантов: таблица с колонками «Вариант», «Вопросов в варианте» в том же порядке; кнопка
  «Настроить варианты…» (secondary, s, иконка списка) в тулбаре под таблицей; строка покрытия
  «В вариантах задействовано {N} из {M} вопросов темы · {K} не используются» + предупреждающий тег.
- Модалка вариантов: размер xl, заголовок «Варианты теста — тема «{название}»» и описание совпадают
  дословно; вкладки вариантов с бейджем количества и завершающей вкладкой-действием «Вариант» с плюсом;
  «Удалить вариант» (ghost, s, корзина) справа; двухпанельный перенос с заголовком левой панели
  «Банк темы», placeholder поиска «Поиск по тексту»; главная кнопка «Готово» (primary).
- Модалка «Добавить тему»: размер m, заголовок «Добавить тему», плоский список строк-кнопок
  «название + пометка», выбор темы по нажатию на строку, единственная кнопка подвала «Отмена».
  Ни в эскизе, ни в реализации нет группировки по папкам, счётчика выбранного и множественного выбора —
  расхождения тут нет. Поле поиска в реализации ЕСТЬ (в текстовый дамп не попал только placeholder),
  расходятся лишь подпись, иконка и текст placeholder — находки 26 и 27.
- Панель настроек сценария: Select «Сценарий прохождения» (m, на всю ширину) со значением «Линейный
  по темам» и Switch «Показывать итоги раздела» — в том же порядке.
- Дерево страниц: зоны «ДО ТЕСТА» / «ПОСЛЕ ТЕСТА», порядок узлов внутри темы (введение → вставка →
  авторские страницы → вопросы → вставка → обзор → итоги раздела), отсутствие вставки между «Обзором»
  и «Итогами раздела», кнопка вставки `button.insert-btn` с иконкой плюса 12x12 и подписью
  «Добавить страницу» между двумя линиями, подпись строки вопросов «{N} вопросов темы «{тема}»»,
  «ручка» перетаскивания у авторских строк и в шапке темы, счётчик «{N} вопросов» в шапке темы.
- Адаптивные уровни: строка темы «точка → название и подзаголовок → переключатель с подписью
  «Включено» / «Выключено» → шеврон», заголовок `h4` «Уровни сложности» с кнопкой справа, состав и
  порядок шести контролов карточки уровня («Название», «Сложность от», «до», «Вопросов», «Тип порога»
  со значениями «Процент» / «Сумма баллов», «Порог» с суффиксом «%» / «б.»), точки `--ok` / `--err`
  с `aria-label` «Состояние уровня: валидно» / «невалидно».

### C. Вкладка «Правила прохождения»

- Рельс вкладки: четыре пункта в порядке «Навигация», «Во время прохождения»,
  «Ограничения», «Защита контента», `aria-label="Подразделы правил прохождения"`
  (`editor-tabs.tsx:168`-`:173`, `:191`) — как в разделе 4 эскиза.
- Навигация: набор из пяти переключателей и все пять подписей — дословно как в эскизе;
  тип контрола (Switch) совпадает.
- Навигация: погашены те же два переключателя, что и в эскизе («Позволить изменять ответ
  до завершения», «Переходить к следующему вопросу сразу после ответа»), и у каждого
  показан warning-баннер `role="status"` без заголовка.
- Во время прохождения: заголовок раздела «Что видит ученик во время прохождения теста»,
  Select «Режим прогресса» со значением «По вопросам» и Switch «Показывать прогресс-бар»
  (включён) — состав, порядок и подписи совпадают.
- Ограничения: две секции с заголовками «Ограничения попытки» и «Ограничения повторных
  попыток», внутри — «Лимит времени теста» (счётчик, суффикс «минут», подсказка «Оставьте 0,
  чтобы не ограничивать.»), Switch «Индивидуальные лимиты для тем», «Максимум попыток»
  (подсказка «Оставьте 0 для неограниченного числа попыток.»), Switch «Ограничить повторное
  прохождение», Switch «Ограничение между попытками» — подписи и порядок совпадают.
- Ограничения: скрытая часть блока повторов по коду повторяет эскиз по составу и порядку —
  «Разделять период по результату попытки» с тем же пояснением, два поля периода, Select
  «Способ проверки (плагин)», warning-баннер, Seg «При ошибке проверки допуска» с опциями
  «Разрешить старт» / «Заблокировать» и тем же пояснением, «Интервал, часов» с подсказкой
  «От 1 до 8760 часов (до года).», info-баннер о разнице барьеров.
- Ограничения: таблица индивидуальных лимитов объявлена с
  `aria-label="Индивидуальные лимиты времени по темам"` и колонками «Тема», «Лимит»,
  поле строки — счётчик `size="s"` с суффиксом «минут» и placeholder «Без ограничения»
  (`basic-settings-section.tsx:586`-`:615`).
- Защита контента: три независимых переключателя, подписи и порядок дословно как в эскизе.

### D. Вкладка «Оценка результата»

- Рельс вкладки: четыре пункта в порядке «Оценка ответа», «Вердикт», «Шкалы», «Показатели»,
  `aria-label="Подразделы оценки результата"`, активный по умолчанию — первый
  (`editor-tabs.tsx:226-261`).
- Оценка ответа: подпись и подсказка строки умолчания, порядок и состав шести колонок таблицы
  («Вопрос», «Балл», «Цена ответа», «Сложность», `aria-label="Состояние"`, `aria-label="Действия"`),
  тег выдачи `ou-tag--neutral ou-tag--outline` («выдача 6 из 9»), подпись «Балл по умолчанию в секции»,
  пиктограммы типов и их подписи («Один ответ», «Несколько ответов», «Ранжирование»,
  «Распределение баллов»), теги цены ответа «Точное»/«Веса»/«Ступени», пара действий
  (карандаш + `i-rotate-ccw` «Сбросить настройку оценки»), классы `tb-qscoring__row--override`
  и `tb-qscoring__cell--override`. Панель «Развернуть все»/«Свернуть все» визуально стоит
  отдельной строкой справа, как в эскизе.
- Вердикт: четыре радиокнопки в порядке эскиза с именем группы `pass-decision-policy`, таблица
  `tb-table tb-pass-table` с `aria-label="Правила оценки тем"` и колонками «Тема», «Правило оценки темы»,
  ячейка правила `tb-pass-table__rule`.
- Шкалы: панель `ou-formactions--between` с «Предпросмотр расчёта» и «Добавить шкалу» (обе ghost, s),
  карточка `ou-card ou-card--outlined ou-card--sm tb-level-card` с точкой состояния, заголовком
  «КЛЮЧ — метка», действиями «Удалить шкалу» и «Свернуть шкалу»; порядок шести полей формы
  (Ключ, Метка, Агрегация, Источник, Пересчёт итога, Передавать в LMS), обязательность ключа
  и placeholder «напр. ee», текст «Код уровня публикуется как scale.{ключ}.level…», переключатель
  «Задать границы шкалы вручную» с его описанием, поля «Минимум шкалы»/«Максимум шкалы», кнопка
  «Рассчитать по вкладам» (secondary, s), селекты «Благоприятное направление» и «Показывать
  обучающемуся», два переключателя слотов карточки.
- Конструктор уровней: состав и порядок (полоса покрытия, статистика, чередование карточек и порогов,
  кнопка добавления), тексты «Шкала разобрана целиком, N уровня», «порог: 40 и ниже — „…“, выше — „…“»,
  пять элементов ленты тонов в порядке «Авто», «Благоприятный», «Нейтральный», «Внимание», «Критический».
- Вклады вопросов: баннер «N вопросов пока не привязаны ни к одной шкале — проверьте покрытие.»,
  панель `tb-fold-toolbar` с «Развернуть все»/«Свернуть все», свёртки по темам с тегом числа вопросов,
  точки состояния карточек (`--ok` / `--warn`), первая колонка матрицы по типу вопроса, галочка
  «Верный вариант», столбцы по ключам шкал заглавными.
- Модалка оценки вопроса: заголовок «Оценка вопроса в тесте», блок `tb-qscoring__recap` с текстом вопроса
  и фразой «Базовая сложность вопроса: N. Текст и варианты правятся в банке вопросов; здесь — только
  оценка для этого теста.», подписи и подсказки обоих полей, тег «Макс. балл sMax = N», состав сегмента
  «Точное совпадение» / «Веса опций» для одиночного выбора (снимок `impl-qscoring.png` — ступеней там
  нет, как и в `s-qscoring`), пунктирное пояснение точного совпадения, порядок подвала
  «Сбросить настройку» слева и «Отмена», «Предпросмотр балла», главная кнопка справа.
- Ступенчатый режим: операторы в порядке «=», «≥», «≤», «<», «>», кнопки «И условие» (ghost, s)
  и «Добавить строку» (secondary, s), нередактируемая пунктирная строка «Иначе — во всех остальных
  случаях» со значением «0», кнопки «Удалить условие» и «Удалить строку».
- Пустые состояния «нет шкал» и «нет показателей»: `EmptyState layout="page" well` с иконкой,
  заголовки «Пока нет шкал» / «Пока нет показателей», описания дословно, действие primary s
  с иконкой Plus — совпадают с §5.23 и §5.24 полностью.
- Карточка показателя: заголовок «имя — метка», подзаголовок «тип · порядок N[ · управляет статусом]»,
  ручка перетаскивания, «Удалить показатель», «Свернуть показатель», кнопка «Функции» (secondary, s,
  иконка Info), баннер валидации формулы, «Добавить исход» (ghost, s), пустая строка «Исходы не заданы»
  на всю ширину, четыре шаблона конструктора в порядке «Порог», «Категория по уровням шкалы»,
  «Взвешенная сумма», «Сертификация / вердикт».

### E. Вкладка «Обратная связь и итоги»

- Рельс вкладки: четыре пункта в порядке эскиза («Во время теста», «Состав итогов»,
  «Обратная связь», «Отчёт»), `aria-label` = «Подразделы обратной связи и итогов»
  (`editor-tabs.tsx:293-298`, `editor-tabs.tsx:333`), активен по умолчанию первый.
- Полный список надписей итогов: 20 строк в семи группах, состав, порядок групп
  («Первый уровень», «Второй уровень», «Группы рекомендаций», «Числа сводки»,
  «Карточка темы», «Группы разделов», «Итоги раздела») и порядок строк внутри групп
  совпадают со списком 5.18 один в один (`templates/certification/manifest.json`).
- Умолчания надписей совпадают у 17 строк из 20: «Ваш результат», «Рекомендации»,
  «Общий балл», «По шкалам», «По показателям», «Разрез результата»,
  «Пройти обучение», «Мероприятия», «Материалы», «вопросов», «верно», «баллов»,
  «Правильно», «Баллов», «Не пройдено», `{passed} / {total}`, «Итоги раздела».
  Пустое умолчание у «Вердикт: нет порога» — как в эскизе.
- Строка надписи построена как в эскизе: подпись объявления, переключатель
  «Показывать» с `aria-label` «Показывать надпись «…»», поле формулировки,
  поле выключенной надписи заблокировано (`results-labels-pane.tsx:216-253`).
- «Порядок подблоков»: пять позиций в порядке эскиза («Общий балл», «По шкалам»,
  «По показателям», «По темам», «Разрез результата»), у каждой две кнопки-иконки
  «выше»/«ниже» со стрелками, крайние заблокированы
  (`results-labels-pane.tsx:266-347`).
- «Подытоги по подтемам»: три поля-`Select` в порядке эскиза с теми же подписями и
  значениями снимка; подсказка «Где показывать подытоги» совпадает дословно
  (`basic-settings-section.tsx:1247-1341`).
- «Вводный текст»: поле «На экране итогов» превью с кнопкой-карандашом,
  переключатель «В отчёте — тот же текст, что на экране итогов» с описанием
  «Правится в одном месте. Выключите, чтобы задать отчёту своё вводное слово.»,
  выключение открывает второе поле «В отчёте» — ровно правило эскиза (пункт 4
  раздела 6.1); в снимке переключатель выключен и поле показано.
- Условная обратная связь вопроса печатается двумя строками с пометками «Верно» и
  «Неверно» (`question-feedback-registry.tsx:161-168`).
- Документ отчёта: зона `div.zone-block[data-testid="report-document"]`, шапка с
  иконкой файла и словом «Документ», вставки «Добавить блок» между всеми строками,
  ручка перетаскивания, бейдж варианта, название, тег природы «системный», тумблер
  показа, меню действий, мета-теги (в том числе «Доступно вариантов: N»), у строки
  «Разрыв листа» — только удаление (`report-document-list.tsx:92-393`).
- Палитра блоков: заголовок «Добавить блок», описание «Блок встанет в документ на
  место, откуда вы его добавляете.», поле поиска с иконкой и placeholder
  «Поиск блока…», порядок групп «Страницы» → «Служебное» → «Удалённые из документа»,
  подвал «Отмена» (ghost, m) и «Добавить» (primary, m), заблокированная без выбора
  (`report-block-palette.tsx:44-46`, `report-block-palette.tsx:154-200`).
- Отсутствие группы «Удалённые из документа» в живом снимке палитры — НЕ дефект,
  объяснение подтверждено кодом: группа собирается из системных блоков, которых нет
  в документе (`report-block-palette.tsx:87-94`), пустая группа не рисуется вместе с
  заголовком (`report-block-palette.tsx:119-121`), а в документе этого теста стоят
  все десять системных блоков реестра `REPORT_SYSTEM_BLOCKS`
  (`shared/report/report-blocks.ts:49-58`): «Шапка документа», «Вводный блок»,
  «Сводка баллов», «Результаты по темам», «Сводный разрез», «Шкалы», «Показатели»,
  «Рекомендации», «Курсы», «Мероприятия».

### F. Вкладки «Оформление» и «Комментарии»

Вкладка «Оформление»:

- подпись вкладки и её место в риббоне; рельс подписан «Подразделы оформления»,
  активный пункт помечен `is-active` и `aria-current="page"`; порядок показанных пунктов
  («Шаблон» → «Брендирование» → «Цвета» → «Вид диаграмм» → «Облик отчёта») совпадает
  с порядком эскиза;
- карточка шаблона: кнопка-глаз `ou-iconbtn--ghost ou-iconbtn--s` в углу с
  `aria-label="Предпросмотр шаблона"`, миниатюра с `alt` вида «Превью «…»», имя шаблона,
  тег версии `ou-tag--info ou-tag--outline` «v…», описание, пара действий «Заменить
  шаблон» (secondary s) и «Сбросить до умолчаний» (ghost s); разделов `ou-formsection`
  на экране нет — как и оговорено в 5.20;
- «Цвета»: подпись «Тема теста», переключатель из палитр шаблона с «Авто» последним
  пунктом, кнопка «Предпросмотр» (ghost s, иконка глаза 13 px), оба варианта подсказки
  дословно совпадают со спекой 5.21, таблица `ou-tbl--compact design-colors-table`
  с колонкой «Параметр» и по колонке на палитру, `table-layout: fixed`, ячейка
  `design-color-field` = пикер плюс кнопка возврата ТОЛЬКО при переопределении,
  с `title="Вернуть цвет шаблона"` и `aria-label` «Вернуть цвет шаблона: {параметр},
  тема «{тема}»»; подсказка параметра стоит под его именем в той же ячейке.

Вкладка «Комментарии»:

- рельса нет — как в разделе 4 спеки; одна колонка `tb-settings-content`;
- «Добавить комментарий» — primary s с иконкой 14 px; фильтр «Только открытые» включён
  по умолчанию;
- карточка ветки `ou-card--outlined ou-card--sm`; шапка «автор · дата · тег исхода»,
  строка контекста, текст, ветка ответов с левой линией; теги «открыто» (warning),
  «учтено» (success), «отклонено» (нейтральный soft), «изменено после комментария»
  (outline), «объект удалён» (soft) — классы совпадают с эскизом;
- действия ветки: «Ответить» (ghost s), «Закрыть комментарий» / «Открыть заново»
  (secondary s), правое прижатие через распорку; подвал формы — «Отмена» (ghost s) слева
  и главное действие (primary s) справа;
- формат даты «2 сентября, 14:20»; заголовки групп «Тест в целом», «Стартовый экран»,
  «Экран итогов», «Раздел «…»»;
- интервалы панели `review-panel.css` повторяют значения эскиза один в один
  (панель 12, группа 8, действия и ответы 12, отступ ветки ответов 16), поэтому
  претензий по сетке к этой вкладке нет.

## Что осталось непроверенным

### A. Хром ящика и вкладка «Основное»

- Сообщение ведущей сессии называет кнопку «Закрыть» в чистом подвале **secondary**;
  ни код (`test-editor.tsx:791`, `variant="ghost"`), ни снимок `impl-t1-основное.png`
  (плоская надпись без заливки, в отличие от заливки «Отменить» на `impl-dirty.png`)
  этого не подтверждают. Считаю кнопку ghost, то есть совпадающей с эскизом;
  если в живом DOM класс всё же `ou-btn--secondary`, находку надо завести отдельно.
- Тег «Опубликован» (тон `success`) и версия `v16` в шапке — состояний с опубликованным
  тестом в эскизе нет вовсе (там только «Черновик» и «Изменено»), сверить не с чем.
- Ширина ящика: FR-43 (раздел 6.2) требует `min(1120px, 100vw - var(--ou-space-9))`,
  но сам целевой эскиз берёт `ou-drawer--xl` без переопределения, а у ui-kit это
  1040 px / `calc(100vw - var(--ou-space-7))`. Реализация повторяет эскиз; расхождение
  живёт между двумя источниками, а не между эскизом и кодом.
- Заголовок ящика: реализация даёт `h1`, эскиз открывает `h2` и закрывает `</h1>` —
  это признанный дефект разметки эскиза (раздел 9, пункт 10), решение нужно от заказчика.
- Рельс вкладки «Основное»: в `s-basic` рельса нет, в `s-changes` он есть с пунктами
  «Название и назначение» / «Режим теста» / «Выдача в LMS» (раздел 9, пункт 2).
  Реализация следует `s-basic`. Требуется решение, какой вариант считать целевым.
- Вкладка, открываемая по умолчанию, — «Состав и сценарий» (`test-editor.tsx:262`).
  Эскиз правила об умолчании не содержит, хотя реестр состояний и переключатель
  начинаются с «Основного».
- Мобильная раскладка (ширина меньше 960 px): подвал в колонку, тег текущей вкладки вместо
  «Показать изменения», стрелки у полосы вкладок — на снимках не воспроизводилась.
- Состояния `s-errors` и `s-changes` целиком, а также диалоги закрытия при ошибках,
  конфликт версий и оверлей сохранения проверены ТОЛЬКО по коду и CSS: живых снимков
  этих состояний нет (кроме диалога закрытия без ошибок и поповера изменений).
- Ширина и высота поповера в живом DOM измерены глазом по снимку `impl-changes.png`;
  точные значения (в том числе поведение при длинном списке групп) не снимались.

### B. Вкладка «Состав и сценарий»

- Значение Select «Порядок выдачи вопросов в тесте»: в эскизе стоит «Как в тесте», что для правила
  уровня ТЕСТА бессмысленно (это значение уровня темы) и, судя по всему, является дефектом эскиза;
  список опций в эскизе не раскрыт, поэтому набор значений реализации
  («Фиксированный порядок» / «Перемешивание» / «Полное перемешивание») сверить не с чем.
- Нумерация тем: в `s-composition` темы подписаны «1. О компании», а в `s-scenario` та же тема —
  «О компании» без номера; реализация делает наоборот (в составе номера нет, в сценарии есть).
  В находку 9 вынесен только состав; какое из двух написаний эскиза считать эталоном — вопрос решения.
- Состояния, которых эскиз не показывает и потому сверить не с чем: баннеры блока квот (ошибка тега,
  переполнение суммы, нехватка по тегу), поясняющие строки выключенных квот/порогов/вариантов,
  баннеры адаптивного подраздела («Сначала добавьте темы», «Включите хотя бы одну тему», «Добавьте
  уровни сложности»), баннер «Тест в адаптивном режиме» в подразделе «Состав», баннеры полотна
  структуры («Режим изменён», «Страниц требуют сопоставления», «Сначала сохраните черновик»),
  теги `page-row__meta` об ошибках и недоступных шаблонах, тег «Тема недоступна» в шапке темы.
- Раскрытая карточка уровня в живом снимке не показана (аккордеон темы был свёрнут): находки 33-35
  по адаптивным уровням получены по коду, а не по экрану.
- Модалки «Настроить варианты…» и «Добавить тему» в живом виде сняты не полностью: у первой снимка
  нет вовсе (сверка по коду), у второй снимок есть, но список отфильтрован реальными данными dev-базы.
- Тонкая типографика и токены (толщина рамок, тени, цвета точек состояния, шрифтовые пары) не
  сверялись: снимок реализации сделан в тёмной теме, эскиз — в светлой.
- Расхождения, объяснимые данными и режимом снимка (две темы вместо трёх, названия «Демо: …»,
  выключенные пороги и варианты, отсутствие подраздела «Адаптивные уровни» в стандартном тесте,
  «Опубликован»/«v16» вместо «Черновик»/«v8»), дефектами не считались.

### C. Вкладка «Правила прохождения»

- «Показывать уровень сложности при прохождении»: в снимке вместо переключателя стоит
  info-баннер «Показывать нечего», потому что тест стандартный. Расхождение объясняется
  режимом теста; сам баннер эскизом не предусмотрен, но и состояния «стандартный тест»
  для этого подраздела эскиз не показывает.
- Таблица индивидуальных лимитов, поля периода охлаждения, Select плагина, баннер WebTutor,
  Seg «При ошибке проверки допуска», «Интервал, часов» и info-баннер в снимке не видны:
  соответствующие переключатели выключены данными теста. Сверка сделана по коду, но не
  в браузере — ширины, интервалы и облик этих элементов на экране не проверялись.
- Поле «Период охлаждения, календарных дней» (`basic-settings-section.tsx:746`) в эскизе
  отсутствует: эскиз показывает состояние с включённым «Разделять период по результату
  попытки», при котором это поле скрыто. Дефектом не считаю.
- Тексты пояснений, зависящих от состояния («Ограничить повторное прохождение»,
  «Разделять период по результату попытки», «Ограничение между попытками»,
  info-баннер интервала): эскиз показывает только вариант «включено», реализация
  печатает второй вариант при выключенном состоянии. Сверить можно только на включённых
  переключателях.
- Соблюдение шага 4/16/24 внутри разделов по скриншоту измерить точно нельзя; проверено
  только правило фиксированной ширины числового поля (находка № 8) — остальные интервалы
  задаются общим проектным слоем `tb-components.css` и на этом участке отдельных
  переопределений не имеют.
- Реализация снята в тёмной теме, эскиз — в светлой; цветовые различия не разбирались.

### D. Вкладка «Оценка результата»

- Наполненные шкалы и показатели сняты на другом тесте («Определение ведущего стиля
  человекоцентричного лидерства»), поэтому по снимкам сверялась только структура; значения полей,
  число шкал, тексты уровней и вкладов не сопоставлялись.
- Раскрытая карточка шкалы, раскрытая карточка показателя, конструктор формулы, редактор исходов
  и ступенчатый/весовой режимы цены ответа в снимках не раскрыты — сверены по коду и описи.
- Модалки «Предпросмотр балла», «Предпросмотр расчёта» и «Функции» в эскизе не описаны
  (в реестре состояний их нет), поэтому не сверялись.
- Кнопка «Рассчитать по вкладам» в реализации появляется только при включённых ручных границах;
  в эскизе переключатель включён, поэтому по эскизу нельзя судить, должна ли кнопка быть видна
  при выключенном переключателе.
- Пояснение ступеней в эскизе описано только для множественного выбора («T — всего верных»);
  реализация подставляет P/N для сопоставления и ранжирования — эскиз этих типов не показывает.
- Рамка пустого состояния на снимке реализации выглядит сплошной, в эскизе — пунктирной; класс
  (`ou-empty--well`) совпадает, различие может давать тёмная тема, в браузере не проверялось
  (браузер занят ведущей сессией).
- Режим «только чтение»: `ScoringTab` не передаёт `readOnly` ни в один из четырёх разделов
  (`editor-tabs.tsx:263-284`, тип `EditorTabProps` такого поля не имеет), поэтому ветки `readOnly`
  в коде разделов недостижимы. Эскиз состояния «только чтение» для этой вкладки не описывает,
  поэтому в находки не вынесено.
- Ширины `--wf-w-rhs` (120 px, колонка значения условия и балла в таблице весов) и `--wf-w-op-lg`
  (96 px, оператор в конструкторе формулы) сопоставить не удалось: в реализации таблицы весов нет
  (находка 37), а строка условия конструктора формулы фиксированных ширин не задаёт.

### E. Вкладка «Обратная связь и итоги»

- Внутреннее расхождение эскиза: в 5.16 те же поля названы «Заголовок блока по
  темам» и «Заголовок блока по шкалам», а плейсхолдером тем стоит «Разбор
  результатов»; в 5.18 они же — «Подзаголовок тем» и «Подзаголовок шкал», а «Разбор
  результатов» проходит авторским значением. Реализация следует 5.18 и манифесту
  (умолчание тем — «По темам»). Какой из двух голосов эскиза считать эталоном, я не
  решаю.
- Значения полей эскиза «Ваш результат», «Зачёт», «Разбор результатов», тексты
  превью обратной связи, числа в подзаголовках тем — авторские данные эскиза, с
  данными снимка не сверялись.
- Названия пунктов «Страницы» в палитре («Текст автора», «Рекомендации по итогам»,
  «Таблица ответов» против «Страница: заголовок и текст», «Страница: две колонки»,
  «Страница: три колонки») берутся из вариантов ШАБЛОНА
  (`report-block-palette.tsx:65-74`), поэтому расхождением реализации не считаю;
  проверить, обязан ли шаблон «Отчёт: сертификация» объявлять именно варианты
  эскиза, в рамках участка нечем.
- Блоки-страницы в документе отчёта (раскрытая область с полями «Заголовок блока»,
  «Текст», «Подпись», кнопка «Предпросмотр блока», пояснение «У этого варианта нет
  полей…»), мета-тег «Доступно вариантов: 3» — в документе снимка ни одной страницы
  нет; проверено только по коду и описи, вживую не наблюдалось.
- Раздел «По уровням» и модалка `FeedbackEditorModal` вживую не открывались:
  адаптивного теста в снимке нет.
- Карточка «Заголовки и подписи отчёта» показывает 8 строк из 20 — это фильтр
  `reportLabelKeys`, приходящий с сервера (`editor-tabs.tsx:320-326`); состав фильтра
  данными снимка не проверялся.
- Точные отступы (24 px от краёв, 16 px между разделами, 24 px между разделами
  формы из раздела 7 эскиза) по скриншотам не измерялись — в находки попали только
  те два интервала, что заданы в коде явным `gap={2}`.
- Диалоги закрытия и конфликта, состояния dirty/errors/сохранения, поповер
  изменений, мобильные правила (раздел 6.2) — вне участка.

### F. Вкладки «Оформление» и «Комментарии»

1. Пункт рельса «Макет». В живом снимке его нет, но это НЕ пропуск в реализации:
   пункт объявлен (`design-section.tsx:115`), панель написана
   (`design-section.tsx:324-331`, пустое состояние — те же строки), а скрывается он
   правилом PRD-23 «пункт без параметров не показываем»
   (`design-section.tsx:186-198` + отбор `paramsForRail`, `design-section.tsx:143-155`).
   Манифест «Сертификация (РТК)» v1.12.0 (`uploads/templates/certification/manifest.json`)
   объявляет 18 параметров, из них ни одного с `"section": "layout"` — только `branding`
   и `progress`. Версия в манифесте (1.12.0) совпадает с показанной в снимке, то есть
   в базе лежит тот же манифест. Эскиз рисует «Макет» для того же шаблона (там v1.11.0),
   так что расхождение объясняется манифестом и решением PRD-23, дефектом его не считаю.
   Требует решения: показывать ли пункт всегда, как в эскизе, или оставить правило.
2. Состав, подписи и подсказки параметров в «Брендировании», «Цветах» и «Виде диаграмм»
   приходят из манифеста шаблона (11 цветов вместо пяти в эскизе, «Цвет кнопок и активных
   элементов» вместо «Акцент» и т. д.). Построчная сверка невозможна, расхождением
   не считаю.
3. Содержимое пунктов «Макет», «Брендирование», «Вид диаграмм», «Облик отчёта»
   в целевом эскизе не эскизировано ни разу (раздел 8, пункт 4). Карточка «Оформление
   отчёта», её поля, подсказки и раскладка в две колонки сверены быть не могут;
   возможные альтернативные эталоны — `docs/wireframes/approved/prd7-design-tab.html`,
   `prd23-template-themes.html`, `prd27-report-template.html` — в задание не входили.
4. Тег «Встроенный» у карточки шаблона (`design-section.tsx:517-525`) в эскизе
   отсутствует, но он условный (`tpl.isBuiltin`), а «Сертификация (РТК)» загружена
   через админку — в снимке его и не должно быть. Проверить можно только на встроенном
   шаблоне.
5. Состояния, не воспроизведённые в снимке: баннер «Шаблон обновлён» (эскиз `s-design`
   его показывает, в снимке шаблон актуален), баннер «Шаблон недоступен», пустые
   состояния панелей, окно «Выбор шаблона» и окно «Предпросмотр шаблона». Находки 4 и 5
   про баннер выведены из кода и CSS, а не из снимка.
6. Вкладка «Комментарии» показана с нулём комментариев, поэтому карточки веток, ответы,
   форма исхода, состояния «изменено после комментария» и «объект удалён», а также стек
   ящиков при переходе по якорю сверены ТОЛЬКО по коду, без браузера.
7. Пустое состояние панели комментариев («Открытых комментариев нет» / «Комментариев пока
   нет») в эскизе PRD-52 не нарисовано — сверять не с чем.
8. Риббон в `prd52-review-panel.html` старый (Состав / Настройки / Оформление / Структура /
   Оценка / Комментарии) и заменён целевым эскизом; хром ящика и полоса вкладок —
   участок другого проверяющего.
9. `docs/wireframes/approved/prd52-invite-dialog.html` описывает отдельный диалог
   приглашения экспертов; точки входа в него на вкладке «Комментарии» нет ни в эскизе,
   ни в реализации, поэтому диалог не сверялся.
