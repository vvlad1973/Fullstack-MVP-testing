# BRD: Расширение возможностей генерации SCORM-пакетов

**Версия:** 2.17  
**Статус:** Утверждено. **Стенд WebTutor доступен с 2026-08-06** — на нём приняты
SCORM-пакет и retake gate (период охлаждения), а также отыграны reload посередине попытки и
kill вкладки для таймера. Этим снят техдолг BR-14-07 (гейт выката PRD-20) и блокер старта
PRD-21; прежняя оговорка «WebTutor недоступен, проверяем локально» в новых документах больше
не применяется. Далее по требованиям:
BR-09 (Этап 10) и BR-10 (Этап 11) реализованы, 2026-06-04;
BR-11 (Этап 12) — Фазы 0-2 реализованы 2026-06-10; импорт медиа вынесен в отдельный будущий PRD;
BR-12 (Этап 13) «Фиксированные варианты выдачи» реализован полностью, 2026-06-26;
BR-13 (Этап 14) «Встроенный плеер тестирования и отладки тестов» — PRD-18; Фазы 0-4 реализованы 2026-06-27;
BR-14 (Этап 15) «Устойчивость таймера и возобновление timed-тестов» — PRD-20;
Фазы 1-2 реализованы 2026-07-02, LMS-валидация выполнена на живом WebTutor 2026-08-06 —
техдолг снят;
BR-15 (Этап 20) «Тип вопроса „шкала“» реализован 2026-07-30;
BR-16 (Этап 21) «Отчёт о результатах — часть шаблона» реализован 2026-07-31;
BR-17 (Этап 22) «Итоги измерительного теста» — ЗАКРЫТ и ПРИНЯТ 2026-08-01 (дефект приёмки D-4
добит 2026-08-06);
BR-18 (Этап 23) «Авторский порядок выдачи вопросов» — реализован 2026-08-01, приёмка пройдена на
трёх путях воспроизведения (веб-прохождение, экспортированный ZIP, отладочный плеер);
BR-19 (Этап 24) «Интервал между попытками и общая модель допуска» — ЗАКРЫТ 2026-08-06 (реализован
2026-08-01, Фазы 1-6; приёмка веба и пакета пройдена, остаток по разведённым часам закрыт решением
владельца продукта);
BR-20 (Этап 25) «Материалы обратной связи» — реализован в двух проходах: файл в медиатеке
(PRD-32, принято 2026-08-02), затем решение пересмотрено на внешнюю ссылку без загрузки файла
(PRD-42, реализовано 2026-08-05); трек закрыт;
BR-21 (Этап 26) «Оценка целесообразности психодиагностического охвата и управленческой отчётности» —
задача на оценку, заведена 2026-08-01, решения не приняты;
BR-22 (Этап 27) «Единый вход по корпоративной учётной записи» и BR-23 (Этап 28) «Согласие на обработку
персональных данных и срок хранения» — заведены 2026-08-01 с ВНЕОЧЕРЕДНЫМ приоритетом (отсекающие
критерии закупки, см. [§7](#7-приоритизация-этапов)), не начаты;
BR-24 (Этап 29) «Радар компетенций» — заведён 2026-08-01 решением по позиции BR-21-08,
реализован в тот же день; живое прохождение и радар в пакете закрыты 2026-08-08 в ходе PRD-46,
остаётся только финальная растеризация PDF;
BR-25 (Этап 30) «Адаптивное тестирование по модели IRT» — заведён 2026-08-01, начинается ПОСЛЕ
психометрики (зависимость по данным: банк должен быть откалиброван), не начат;
BR-26 (Этап 31) «Управление экспозицией заданий» — заведён 2026-08-01, зависит от накопления
наблюдений (в т.ч. импорта выгрузок из WebTutor); часть требований данных не требует, не начат;
BR-27 (Этап 32) «Импорт и экспорт по IMS QTI» — заведён 2026-08-01 по итогам сверки с встроенным
конструктором WebTutor и с мировым эталоном; данных и психометрики не требует, не начат;
BR-28 (Этап 33) «Листинг кода в тексте задания» — повод зафиксирован 2026-08-01, требования не
собирались, форма решения не согласована;
BR-29 (Этап 34) «Защита текста задания от копирования» — требования согласованы 2026-08-02,
РЕАЛИЗОВАН и влит в `main` 2026-08-02, приёмка ЧАСТИЧНАЯ (перечень непроверенного — в ROADMAP
§0.0.1);
BR-30 (Этап 35) «Компактность состояния прогона в SCORM-пакете» — требования согласованы
2026-08-03, реализация не начата;
BR-31 (Этап 36) «Профиль экспорта SCORM 1.2» — трек заведён 2026-08-03, нереализуем без Этапа 35,
продуктового решения о запуске нет.
Дополнительно без новых BR (см. [«Расширения ранее принятых требований»](#4-бизнес-требования)):
Этапы 37-41 — медиа вопроса внутри блока вопроса (PRD-38, реализован 2026-08-04), слияние карточек
сопоставления (PRD-39, реализован 2026-08-04), период охлаждения по исходу попытки (PRD-40,
реализован 2026-08-05), Moodle cooldown plugin (PRD-41, предложен, не начат) и быстрый переход к
следующему вопросу (PRD-43, реализован 2026-08-05);
Этапы 42-47 — редактор уровней шкалы порогами (PRD-45, принят 2026-08-07), выбор диаграммы профиля
радар/роза (PRD-46, реализован 2026-08-08), диаграмма профиля в PDF-отчёте (PRD-47, принят
2026-08-09), перенос теста между инсталляциями без потерь (PRD-48, закрыт 2026-08-12),
настраиваемые заголовки блоков итогов (PRD-49, реализован 2026-08-12), разрезы результата
(PRD-50, спецификация согласована 2026-08-13, реализация на невлитой ветке). Номера 42-47
присвоены по порядку ЗАВЕДЕНИЯ требования, как предписывает [§7](#7-приоритизация-этапов), и
очерёдности исполнения не выражают.
Тип вопроса «Распределение баллов между шкалами» (PRD-44): требования СОГЛАСОВАНЫ 2026-08-06,
фазы 0-6 реализованы, но номера этапа он по-прежнему не получил — открытый вопрос BRD, см.
конец [§4](#4-бизнес-требования).
Внешний участник (позиция BR-21 / PRD-28) РЕАЛИЗОВАН, принят и влит 2026-08-13; требование BR-23
(согласие на обработку данных) этим не закрывается и остаётся внеочередным.  
**Дата актуализации:** 2026-08-14  
**Технический контракт:** [Платформа SCORM-шаблонов](./spec-template-platform.md)  
**Статусы треков:** [ROADMAP §0](../ROADMAP.md) — нормативный источник; здесь статус даётся справочно

---

## 1. Контекст и постановка задачи

Test Builder генерирует SCORM 2004 4th Edition пакеты с ограниченной визуальной
вариативностью, без полноценных контентных страниц между блоками и с базовой моделью
расчёта результата. Референсный проект Articulate Storyline показывает, что для
корпоративных сертификаций нужны:

- управляемые шаблоны плеера и страниц;
- контентные и системные страницы внутри общего потока прохождения;
- управляемый поток прохождения: переходы между разделами, блокировки, таймеры;
- пользовательские показатели результата и передача этих данных в LMS;
- многомерные измерения компетенций, где один вопрос может влиять на одну или несколько шкал.

**Цель:** расширить SCORM-генератор до уровня, достаточного для корпоративных курсов и
сертификаций, сохранив контролируемую архитектуру Core и единую механику внутренних и
внешних шаблонов.

---

## 2. Ключевые архитектурные решения

### 2.1 Единая шаблонная платформа

Встроенные и внешние шаблоны используют один контракт:

- `manifest.json`;
- `shell.html`;
- макеты страниц;
- частичные шаблоны;
- локальные CSS/JS/ресурсы внутри SCORM ZIP;
- браузерный рендерер шаблонов;
- Runtime API для `template.js`.

Отдельные механики для встроенных и внешних шаблонов не создаются. Различаться может только
источник файлов: адаптер встроенного источника или адаптер загруженного источника.

### 2.2 Исполнение только в браузере

Шаблон исполняется только в браузере обучающегося внутри SCORM-пакета.

Сервер не выполняет HTML/JS шаблона и не рендерит EJS. Сервер:

- валидирует шаблон;
- хранит метаданные;
- копирует выбранный шаблон в SCORM ZIP;
- добавляет Core runtime и данные теста.

### 2.3 Разделение ответственности Core и шаблона

**Core владеет:**

- состоянием теста;
- навигацией;
- ответами;
- оценкой;
- состоянием обратной связи;
- восстановлением сессии;
- стандартными интерактивами вопросов;
- SCORM API;
- telemetry;
- финальными защитами для критичных действий.

**Шаблон владеет:**

- макетом оболочки;
- макетами разных типов страниц;
- визуальным языком;
- локальными ресурсами;
- дополнительным поведением через `template.js`;
- декларативными правилами шаблона для технического/визуального поведения.

### 2.4 Макет шире, чем цвета и шрифты

Шаблон описывает не только параметры оформления, но и макет:

- оболочку курса (`shell.html`);
- макет вопросов;
- макет контентных страниц;
- макет результатов;
- опциональный макет стартовой страницы;
- опциональные системные страницы;
- частичные шаблоны для навигации, прогресса, header/footer.

Стандартные интерактивы вопросов в MVP остаются за Core и вставляются в контролируемый слот
`data-slot="question-interaction"`.

### 2.5 Гибкий движок прохождения

Core должен поддерживать несколько политик прохождения без развилки на отдельные runtime:

- `legacy_flat`: текущее поведение, при котором вопросы выбираются из разделов и перемешиваются в
  общий плоский список;
- `section_linear`: разделы проходятся последовательно, с явными стартовыми страницами, вопросами и
  страницами результата раздела;
- `section_router`: обучающийся возвращается на страницу-маршрутизатор после каждого раздела,
  видит пройденные разделы и результаты по ним, а итоговый результат открывается после выполнения
  политики завершения;
- `section_graph`: графовые переходы между разделами — _вне охвата_ (зависели от снятого движка правил, см. BR-03);
- `adaptive_by_section`: адаптивная выдача вопросов внутри разделов с переходами между разделами.

Раздел (`section`/`topic`) является не только контейнером вопросов, но и сценарным узлом:

```text
section start -> section questions -> section result -> transition -> next section/results
```

Старые тесты без явной `flowPolicy` должны продолжать экспортироваться и проходиться как
`legacy_flat`.

### 2.6 UI только после согласованных wireframes

Любая функциональность, которая добавляет или меняет авторский, административный или learner-facing
UI, не передаётся в frontend-разработку без предварительно подготовленных и явно согласованных
wireframes.

Wireframes должны покрывать:

- основной пользовательский сценарий;
- пустые состояния;
- loading/saving состояния;
- ошибки API и валидации;
- read-only/disabled состояния, если применимо;
- ключевые edge cases, влияющие на поведение пользователя.

Если во время разработки меняется сценарий, состав полей, порядок действий или состояние экрана,
изменение возвращается на повторное согласование wireframes до продолжения UI-реализации.

---

## 3. Анализ референса Storyline

Референс содержит не только визуальную оболочку, но и runtime-логику:

- переменные доступа и результатов (`Test_Blocked`, `Blocks_Done`, `Result_*`);
- условия и переходы;
- выдачу вопросов и расчёт статуса по отдельным разделам;
- страницу-маршрутизатор, с которой обучающийся выбирает раздел, возвращается после прохождения и
  видит статус/результат разделов;
- стартовые страницы разделов и страницы результатов разделов;
- таймеры;
- SCORM commit/reset;
- блокировки;
- предупреждения о нескольких вкладках;
- слайды результатов и сертификации.

Вывод: простого HTML/CSS-шаблона недостаточно. Требуется гибрид:

- декларативный сценарий прохождения (поток, разделы, политики завершения) вместо no-code движка правил;
- возможность расширения кодом через `template.js` и Runtime API;
- явная модель потока прохождения с разделами, промежуточными результатами и политиками переходов;
- будущая фаза расширенных шаблонных интерактивов для переопределения механики вопросов.

---

## 4. Бизнес-требования

### BR-01: Шаблонная платформа SCORM

**Проблема:** внешний вид, навигация и структура страниц жёстко связаны с текущим Core.
Невозможно создавать разные корпоративные плееры без разработки.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-01-01 | Система поддерживает единую механику встроенных и внешних шаблонов согласно [specs/spec-template-platform.md](./spec-template-platform.md) |
| BR-01-02 | Шаблон исполняется только в браузере обучающегося внутри SCORM-пакета |
| BR-01-03 | Шаблон содержит `manifest.json`, `shell.html`, макеты, опциональные частичные шаблоны, ресурсы и скрипты |
| BR-01-04 | Сервер не выполняет код шаблона при экспорте |
| BR-01-05 | Автор выбирает шаблон и параметры через UI без ручного JSON |
| BR-01-06 | Параметры шаблона поддерживают dot-keys, группы, значения по умолчанию, валидацию и CSS-переменные |
| BR-01-07 | Core предоставляет прогресс по вопросам и страницам; шаблон выбирает активный прогресс через `progress.mode` |
| BR-01-08 | Тестовая `navigationPolicy` имеет приоритет над возможностями шаблона |
| BR-01-09 | Тесты без настроек используют `default` и сохраняют обратную совместимость |

### BR-02: Контентные и системные страницы

**Проблема:** текущий поток содержит только вопросы и не позволяет вставлять учебный материал
или служебные экраны.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-02-01 | Автор может добавлять контентные страницы до/после темы |
| BR-02-02 | Поддерживаются авторские типы страниц (`info`, `html`) и системные узлы границ раздела — введение, обзор, итоги раздела (уточнено PRD-19: прежние `intro`/`summary` заменены системными узлами) |
| BR-02-03 | Контентные страницы входят в общий поток страниц, но не влияют на оценку вопросов |
| BR-02-04 | HTML-контент проходит серверную санитизацию и вставляется только через контролируемые слоты |
| BR-02-05 | Медиа контентных страниц упаковываются в SCORM ZIP как локальные ресурсы |
| BR-02-06 | Системные страницы шаблона объявляются в `manifest.systemPages[]` и не считаются учебными страницами |
| BR-02-07 | Автопереход реализуется через Core/Runtime API, а не через произвольный серверный рендер |

### BR-03: Логика теста задаётся сценарием (движок правил снят)

**Снято 2026-07-05.** Универсальный движок правил курса (no-code «событие → условие →
действие», таблица `course_rules`, конвейер `beforeRules`/`afterRules`, изменяемые переменные
времени прохождения) выведен из охвата: продукт — конструктор тестов, а не курсов. Логика
прохождения теста задаётся декларативным **сценарием** (`flowPolicy`: `flowMode`,
`completionPolicy`, `sectionUnlockRules`, `passDecisionPolicy` — BR-06 / PRD-4 / PRD-8 / PRD-19);
результат — шкалами (BR-06-11 / PRD-5) и показателями (BR-04 / PRD-2); адаптивное ветвление —
уровнями (adaptive). Из прежних требований BR-03 сохраняются только два, как часть контракта
Runtime API:

| ID | Требование |
| --- | --- |
| BR-03-07 | `template.js` получает Runtime API для расширенной логики, но не поддерживает прямой доступ к внутренностям Core |
| BR-03-08 | Поддерживаемые SCORM-операции шаблона выполняются через `TestBuilder.scorm` |

### BR-04: Пользовательские показатели результата

**Проблема:** LMS получает только стандартный балл, а корпоративные сценарии требуют
компетентностных флагов, индексов, допусков и категорий.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-04-01 | Автор задаёт пользовательские показатели результата через визуальный конструктор и расширенный DSL |
| BR-04-02 | Пользовательские показатели вычисляются после стандартной оценки |
| BR-04-03 | Значения публикуются в пространстве имён `result.*` после события `result:calculated` |
| BR-04-04 | Значения могут отображаться обучающемуся на макете результатов |
| BR-04-05 | Значения передаются в LMS через `suspend_data` и/или псевдо-`interactions` |
| BR-04-06 | Ошибка формулы не прерывает завершение теста и сохраняется в диагностике |

### BR-05: Внешние шаблоны

**Проблема:** корпоративные заказчики и подрядчики должны создавать новые шаблоны без изменения
кода системы.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-05-01 | Администратор загружает внешний шаблон ZIP |
| BR-05-02 | Система выполняет структурную валидацию и браузерную smoke-проверку |
| BR-05-03 | Внешние URL в шаблоне запрещены; все ресурсы локальны внутри ZIP |
| BR-05-04 | Встроенные и внешние шаблоны экспортируются и валидируются по одному контракту |
| BR-05-05 | В UI доступны статический предпросмотр и живой предпросмотр на демонстрационном курсе |
| BR-05-06 | В SCORM ZIP попадает только выбранный шаблон |
| BR-05-07 | При runtime-ошибке шаблона Core показывает встроенный экран ошибки |

### BR-06: Гибкий поток прохождения и многомерные измерения

**Проблема:** текущая логика теста использует разделы как источник выборки вопросов и как единицу
финального расчёта, но не как явные сценарные блоки прохождения. Референс требует стартовых страниц
разделов, переходов между разделами, промежуточных результатов и сценариев ветвления. Дополнительно
корпоративные оценки требуют, чтобы один вопрос мог измерять одну или несколько компетенций
одновременно, независимо от обычной правильности ответа.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-06-01 | Core поддерживает `flowPolicy`, определяющую режим прохождения: `legacy_flat`, `section_linear`, `section_router`, `adaptive_by_section` |
| BR-06-02 | Отсутствие `flowPolicy` означает `legacy_flat` и сохраняет текущее поведение существующих тестов |
| BR-06-03 | В `legacy_flat` вопросы выбираются по разделам, затем могут перемешиваться в общий плоский список, как в текущем SCORM runtime |
| BR-06-04 | В секционных режимах Core строит поток раздела: стартовые страницы раздела, вопросы раздела, страницы результата раздела, переход к следующему узлу |
| BR-06-05 | Раздел имеет явное состояние: `notStarted`, `inProgress`, `completed`, `passed`, `failed`, `locked` |
| BR-06-06 | Core рассчитывает результат раздела сразу после завершения вопросов раздела и публикует его до показа `content.summary` |
| BR-06-07 | Правила курса могут реагировать на события `section:started`, `section:completed`, `section:resultCalculated` |
| BR-06-08 | Правила курса могут выполнять переходы `nav.goToSection`, `nav.goToPage`, `nav.nextSection`, `nav.retrySection`, `nav.goToResults` с финальными защитами Core |
| BR-06-09 | Поддерживаются независимые политики порядка: порядок разделов, порядок вопросов внутри раздела, глобальное перемешивание для legacy-режима |
| BR-06-10 | Вопрос может быть экзаменационным, измерительным или смешанным: правильность ответа и вклад в шкалы/компетенции хранятся отдельно |
| BR-06-11 | Система поддерживает сущности шкал/компетенций с типом, правилами агрегации, нормализацией и интерпретационными диапазонами |
| BR-06-12 | Один вопрос или один вариант ответа может давать вклад в несколько шкал с разными весами и условиями |
| BR-06-13 | Результаты шкал публикуются в отдельном пространстве имён `scale.*` и могут использоваться пользовательскими показателями `result.*` |
| BR-06-14 | LMS получает стандартный балл отдельно от секционных результатов, шкал и пользовательских показателей |
| BR-06-15 | UI автора должен явно показывать, какие шкалы измеряет вопрос и как вклад зависит от выбранного ответа |
| BR-06-16 | В режиме `section_router` Core показывает страницу-маршрутизатор со списком разделов, их статусами и результатами, а переход к итоговому результату разрешает только после выполнения политики завершения |
| BR-06-17 | После завершения раздела в режиме `section_router` Core возвращает обучающегося на страницу-маршрутизатор, а не автоматически переводит в следующий раздел |
| BR-06-18 | Состояние страницы-маршрутизатора, результаты разделов и доступность итогового результата должны восстанавливаться из `suspend_data` при повторном открытии SCO |

### BR-07: Ограничение повторного прохождения и период охлаждения

**Проблема:** для части корпоративных курсов требуется запретить повторное прохождение ранее
чем через заданное количество календарных дней после последней полноценной попытки. WebTutor
возвращает дату попытки без времени, поэтому ограничение рассчитывается по датам, без часов.
Доступные данные о попытках зависят от внутренних endpoint'ов и могут меняться при обновлениях
LMS, поэтому проверка не должна быть жёстко зашита в глубину SCORM runtime.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-07-01 | Автор или администратор может включить `retakePolicy` для теста и задать период охлаждения в календарных днях |
| BR-07-02 | Период охлаждения задаётся в настройках теста без пересборки кода runtime; UI показывает дату следующего доступного прохождения без ручных расчётов |
| BR-07-03 | Проверка допуска выполняется до старта полноценной попытки и до вызова SCORM `Initialize`, если выбран режим `before_internal_start` |
| BR-07-04 | При активном ограничении Core показывает `block-wall`/системную страницу блокировки и не запускает прохождение |
| BR-07-05 | Заблокированный запуск не должен писать `cmi.*`, менять SCORM-статусы или создавать признаки полноценной попытки со стороны пакета |
| BR-07-06 | Допуск к старту вычисляется через `eligibilityPlugin` с чётким контрактом `context -> allowed true/false` |
| BR-07-07 | Если `eligibilityPlugin` не задан, Core всегда считает допуск разрешённым (`allowed = true`) |
| BR-07-08 | Выбор `eligibilityPlugin` и его конфигурации хранится в настройках конкретного теста; разные тесты могут использовать разные plugins |
| BR-07-09 | Источник даты последней полноценной попытки выбирается внутри плагина, например `webtutor_cooldown`, `suspend_data_cooldown`, `custom` |
| BR-07-10 | Для WebTutor-плагина полноценная попытка определяется не по факту открытия курса, а по настраиваемому фильтру записей: статус, прогресс, дата последнего использования и/или признаки завершения |
| BR-07-11 | Встроенный WebTutor-плагин должен поддерживать администрируемую конфигурацию endpoint'ов, параметров запроса, имени курса, правил фильтрации и правил парсинга даты |
| BR-07-12 | Хрупкая LMS-specific логика размещается в явном адаптере/плагине, доступном для быстрой правки администратором или внедренцем, а не внутри общего Core |
| BR-07-13 | Плагин возвращает нормализованный результат: `allowed`, `reason`, `source`; для заблокированного cooldown, если дату можно вычислить, результат должен содержать `availableDate` в формате `YYYY-MM-DD` |
| BR-07-14 | Core передаёт `availableDate` на `block-wall`/системную страницу блокировки; `nextAllowedDate` допускается как совместимый alias для старых шаблонов и диагностики |
| BR-07-15 | Если плагин недоступен или возвращает ошибку, политика ошибки задаётся настройкой: `failOpen` или `failClosed` |
| BR-07-16 | UI администратора должен показывать активную retake-политику, источник данных, текущую версию адаптера и результат тестовой проверки на выбранном пользователе/курсе |
| BR-07-17 | Все решения retake gate логируются в диагностике runtime без записи персональных данных сверх необходимого минимума |
| BR-07-18 | `suspend_data` может использоваться только как best-effort источник внутри той же SCORM registration; для строгого cooldown между новыми попытками нужен внешний/LMS-specific источник |
| BR-07-19 | Администрируемый реестр eligibility-плагинов и их конфигураций (редактирование endpoint'ов/фильтров без изменения Core — BR-07-11; выбор из >= 2 конфигураций в UI и тестовая проверка плагина — BR-07-16) выделен в отдельный будущий PRD-21 «Администрируемый реестр eligibility-плагинов». В PRD-6 (Phase 1) реестр read-only и сидируется в коде; рантайм cooldown этим не ограничен. **Блокер старта PRD-21 (отсутствие живой LMS для проверки контракта плагина) снят 2026-08-06** |
| BR-07-20 | **Полевая приёмка выполнена 2026-08-06:** SCORM-пакет и retake gate отыграны на живом WebTutor — блокировка по периоду охлаждения срабатывает на реальных учебных записях портала, а не только в локальном эмуляторе. Требование BR-07-03 (гейт до `Initialize`) и контракт плагина `webtutor_cooldown` подтверждены на цели |

### BR-08: Рефакторинг редактора параметров теста

**Проблема:** настройки теста стали центральной точкой сборки сценария: режим теста, темы,
выборка вопросов, проходные правила, адаптивные уровни, ограничения попыток, стартовый контент,
оформление, контентные страницы, экспорт и будущие `flowPolicy`/`retakePolicy`. Сейчас эти зоны
редактируются фрагментарно: базовый wizard находится в монолитной странице тестов, а часть новых
настроек вынесена в отдельные диалоги рядом с карточкой. Это ухудшает предсказуемость UX,
усложняет валидацию и повышает риск регрессий при добавлении новых PRD.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-08-01 | Автор редактирует все параметры теста в едином редакторе с понятной информационной архитектурой, а не через набор разрозненных кнопок на карточке; создание и редактирование теста открываются в одном wide Drawer |
| BR-08-02 | Редактор разделяет настройки на устойчивые доменные разделы: основное, темы/структура, правила прохождения, адаптивность, старт/контент, оформление, публикация/экспорт |
| BR-08-03 | Стандартный и адаптивный режимы используют общий контейнер редактора, но имеют специализированные формы только там, где различается доменная модель |
| BR-08-04 | Переключение режима теста (`flowMode`, `standard/adaptive`) не должно менять содержимое теста (выбранные темы, draw count, авторские страницы, feedback) — меняется только рендер выдачи под новый режим; системные элементы прежнего режима (например, параметры router-page при возврате к router в той же сессии) сохраняются в draft до закрытия редактора и очищаются только после явного сохранения теста в несовместимом режиме |
| BR-08-05 | Валидация выполняется на уровне доменной модели до отправки API: draw count, проходные пороги, адаптивные диапазоны сложности, количество вопросов, ссылки и обязательные поля |
| BR-08-06 | Frontend использует typed DTO/mappers для преобразования между API, формой и payload, без ручной сборки больших `any`-объектов внутри страницы списка |
| BR-08-07 | Backend предоставляет атомарные операции сохранения настроек теста и связанных сущностей; частично сохранённое состояние не должно появляться при ошибке адаптивных уровней или секций |
| BR-08-08 | Список тестов показывает только краткие основные действия; расширенные настройки открываются через редактор, вкладки или единое меню действий |
| BR-08-09 | Все изменения редактора покрываются компонентными, unit и API-тестами на create/edit, standard/adaptive, режимные переключения и ошибки API |
| BR-08-10 | Перед frontend-реализацией должны быть согласованы wireframes для desktop/mobile, пустых состояний, ошибок, unsaved changes и сценариев с большим числом тем |

### BR-09: Градуированная оценка ответа («Цена ответа»)

**Проблема:** для `multiple`/`matching`/`ranking` стандартный балл вычисляется по принципу «всё
или ничего» (точное совпадение с эталоном -> `1`, иначе -> `0`), поэтому частично верный ответ
получает `0`. Корпоративные и образовательные сценарии требуют частичного балла («Цена ответа»:
веса опций или ступенчатая таблица «условие → балл»), а runtime уже резервирует поля `scoreRatio` и статус `partial`
под эту возможность ([scoring-model.md](./scoring-model.md) §11). Дизайн и реализация —
[PRD-10](./prd-10/graded-answer-scoring.md) (Этап 10).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-09-01 | Для типов `multiple`/`matching`/`ranking` автор может включить градуированную «Цену ответа» (частичный балл), а не строго `0`/`1` |
| BR-09-02 | Балл `s ∈ [0, sMax]`, `scoreRatio = s / sMax`; «Цена ответа» настраивается по типу: веса опций (`single`) либо СТУПЕНЧАТАЯ таблица «условие → балл» над счётчиками `(c, x, T)` с полом `max(0, ·)` (`multiple`/`matching`/`ranking`) |
| BR-09-03 | Градуированный балл относится к оси КОРРЕКТНОСТИ и не влияет на вклады в шкалы, которые задаются отдельно явными числами (BR-06-10, [scoring-model.md](./scoring-model.md) §10.11) |
| BR-09-04 | Runtime публикует `scoreRatio` и статус `partial` («Частично правильно») в контракте шаблона, в показ обучающемуся и в LMS стандартными CMI-полями SCORM 2004 |
| BR-09-05 | Exact-match остаётся поведением по умолчанию; тесты и вопросы без включённой градуированной оценки сохраняют текущий расчёт без изменений (обратная совместимость) |

### BR-10: Квоты выдачи вопросов по тегам

**Проблема:** выдача вопросов на тему равномерно случайна (`shuffle(вопросы темы).slice(0, drawCount)`),
поэтому нельзя гарантировать требуемый объём вопросов определённой ПОДТЕМЫ (напр. в РТК внутри
раздела «Финансы» — `finance_base`/`finance_metrics`/`finance_invest`). Подтемы выражаются тегами
вопросов (`questions.tags`); нужна опциональная квота выдачи по тегам. Это ДОСТАВКА, ортогональная
скорингу. Дизайн — [PRD-11](./prd-11/tag-draw-quotas.md) (Этап 11).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-10-01 | Автор может задать на теме опциональную квоту выдачи `[{ тег, количество }]`; подтема = значение `questions.tags` (новой сущности/иерархии тем не вводится) |
| BR-10-02 | Квота не задана → текущая равномерная выдача без изменений (обратная совместимость) |
| BR-10-03 | Квота задана → на каждый тег по режиму: `exact` — РОВНО `count`, `min` — НЕ МЕНЕЕ `count` (случайный отбор внутри тега) в пределах общей выборки `drawCount`; остаток — случайный из вопросов без `exact`-тегов; вопрос с несколькими квота-тегами засчитывается один раз |
| BR-10-04 | Если вопросов с тегом меньше квоты — НЕблокирующее предупреждение (в редакторе и в выдаче); выдаётся сколько есть |
| BR-10-05 | Квоты — свойство доставки; на скоринг, «Цену ответа» (BR-09) и правила прохождения (на теме/тесте, не на теге) не влияют |
| BR-10-06 | Режим квоты (`exact`/`min`) — свойство КАЖДОГО тега (страты), дефолт `exact`; топик-уровневого режима и переключателя гранулярности нет (см. [PRD-11](./prd-11/tag-draw-quotas.md) §3a) |

### BR-11: Импорт и экспорт банка вопросов

**Проблема:** массовый импорт/экспорт вопросов через Excel реализован, но покрывает лишь часть
модели вопроса и теряет данные: для `matching`/`ranking` импорт игнорирует правильные ответы
и навязывает позиционное соответствие (экспорт необратим); теги, условная обратная связь,
«Цена ответа» (BR-09) и медиа не охвачены; `Балл`/`Сложность` со значением `0` подменяются
значениями по умолчанию. Это мешает массовому ведению банка вопросов и обмену с внешними
источниками. Дизайн — [PRD-14](./prd-14/questions-import-export.md), контракт —
[спецификация формата](./questions-import/format.md) (Этап 12).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-11-01 | Экспорт и импорт вопросов взаимно обратимы (round-trip) для всех типов вопросов, включая `matching` (с дистракторами и непозиционными парами) и `ranking` (с явным правильным порядком) |
| BR-11-02 | Импорт/экспорт покрывают поля, доступные автору в редакторе: теги (BR-10/BR-04), условную обратную связь, «Цену ответа» (BR-09); медиа в охват НЕ входит |
| BR-11-03 | Файлы прежнего (10-колоночного) формата импортируются без изменения поведения; новые колонки опциональны (обратная совместимость) |
| BR-11-04 | Контракт файла (колонки, форматы, разделители, правила идентификации и ошибок) зафиксирован в единой спецификации формата, синхронной с кодом |
| BR-11-05 | Невалидная строка не прерывает импорт; ошибки агрегируются построчно с понятной причиной; обновление по `ID` идемпотентно |
| BR-11-06 | Импорт медиа НЕ входит в BR-11 — выделен в отдельный будущий PRD «Медиатека и импорт медиа» (требует формализации процесса: реестр медиа, человекочитаемый дескриптор, дедупликация, очистка сирот, защита раздачи) |
| BR-11-07 | Шкалы (BR-06) и показатели результата (BR-04) импортируются отдельной процедурой со своей структурой; одна книга Excel может содержать вопросы и шкалы/показатели на разных листах (следующий этап) |

### BR-12: Фиксированные варианты выдачи (формы) и ротация на повторе

**Проблема:** существующая выдача на тему — равномерно случайна (BR-10 без квот) или
стратифицирована по тег-квотам (BR-10). В обоих случаях состав формы — случайная комбинация,
не воспроизводимая и не курируемая автором. Корпоративная сертификация (эталон-источник —
РТК «Обновление сертификации для руководителей») требует другой механики: внутри раздела —
несколько ФИКСИРОВАННЫХ, заранее скурированных автором **вариантов** (= наборов вопросов); при
старте теста каждому разделу случайно выпадает один вариант целиком; при повторной попытке
выпадает ДРУГОЙ вариант (ротация), независимо по каждому разделу. Фиксированный состав нужен под
воспроизводимость и пер-вариантную калибровку порогов; ротация — чтобы повтор шёл по другому
набору. Тег-квоты (BR-10) этого не дают: они стохастические и не гарантируют конкретный состав.
Дизайн — [PRD-17](./prd-17/question-set-variants.md) (Этап 13).

**Граница реализуемости (фиксируется по факту проверки WebTutor):** выбор фиксированного варианта
stateless и работает на обоих хостах (веб + SCORM-пакет); ротация «другой вариант на повторе»
требует памяти о прошлых попытках. WebTutor не предоставляет cross-attempt-хранилище
(`adl.data` недоступен — см. NFR-17), поэтому ротация реализуется в СЕРВЕРНОМ (веб) хосте, где
история попыток доступна; в автономном SCORM-пакете ротация деградирует до случайного варианта
каждую попытку (возможен повтор). Это известное ограничение хоста, а не дефект.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-12-01 | Автор может задать на разделе (`test_sections`) опциональный набор из ≥2 вариантов; каждый вариант — именованный набор вопросов (подмножество банка темы), курируемый автором |
| BR-12-02 | «Варианты теста» — отдельный РЕЖИМ выдачи темы. Не задан → текущая выдача без изменений (равномерная или тег-квоты BR-10, из всего банка); обратная совместимость. Задан → контролы выдачи из всей темы (объём выборки, «все вопросы», тег-квоты) не применяются — источником становится выпавший вариант |
| BR-12-03 | Вариант задан → при старте прохождения темы ей случайно выпадает один вариант; его вопросы выдаются ЦЕЛИКОМ (весь набор), в случайном порядке. Темы выбирают варианты независимо; вариант привязан к ТЕМЕ, а не к тесту в целом |
| BR-12-04 | Вопрос может входить в несколько вариантов (варианты не обязаны быть разбиением банка темы); состав вариантов курирует автор |
| BR-12-05 | Ротация на повторе — поведение по умолчанию (без переключателя): при повторной попытке разделу выпадает вариант, не использованный в предыдущих попытках обучающегося; когда все исчерпаны — цикл. Реализуется в серверном (веб) хосте; в SCORM-пакете — деградация до случайного выбора (см. границу реализуемости) |
| BR-12-06 | Состав варианта проверяется на принадлежность банку темы; удаление вопроса делает вариант неполным — автор видит предупреждение, выдаётся что осталось без дублей, публикация защищается контролем выполнимости (как BR-10/референсная защита) |
| BR-12-07 | Варианты — свойство ДОСТАВКИ; на скоринг, «Цену ответа» (BR-09) и расчёт не влияют. Пороги/правила прохождения остаются на разделе/тесте; пер-вариантная калибровка порогов сейчас НЕ вводится (бизнес отказался от выравнивания вариантов), но модель данных не должна её исключать в будущем |
| BR-12-08 | Варианты импортируются/экспортируются через Excel (BR-11) колонкой «Варианты» на листе вопросов — массив вариантов вопроса через разделитель (как теги); обратимость round-trip и построчная диагностика ошибок — по правилам BR-11 |
| BR-12-09 | Автор видит покрытие банка вариантами: сколько вопросов темы задействовано хотя бы в одном варианте и сколько осиротевших (ни в одном — никогда не выдаются); ненулевой остаток — НЕблокирующее предупреждение |

### BR-13: Встроенный плеер тестирования и отладки тестов (препрод-проверка)

**Проблема:** локальный SCORM-плеер ([scripts/scorm-player.mjs](../../scripts/scorm-player.mjs))
и генераторы пакетов (`scorm:sample`/`scorm:template`) — инструменты разработчика: требуют
репозитория, Node и командной строки. Автор и методолог не могут ВНУТРИ сервиса прогнать собранный
тест как обучающийся и в реальном времени увидеть коммуникацию с LMS (RTE), набранные баллы, шкалы,
показатели и фактически выданный набор вопросов, чтобы проверить корректность настройки
«математики» теста до выгрузки в LMS. Веб-предпросмотр существует только статично (шаблон, страница,
расчёт шкал на демо-ответах); полноценного прогона нет, а все обычные попытки пишутся в `attempts`
и аналитику. Дизайн — [PRD-18](./prd-18/debug-test-player.md) (Этап 14).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-13-01 | Внутри сервиса доступен встроенный плеер отладки: по запросу он собирает РЕАЛЬНЫЙ SCORM-пакет из теста тем же пайплайном (`generateScormPackage`), что и production-экспорт, и запускает его во встроенном плеере с SCORM 2004 RTE-шимом — без скачивания ZIP и без CLI |
| BR-13-02 | Плеер содержит инспектор реального времени: коммуникацию с LMS (журнал чтений/записей `cmi.*`), набранные баллы (`score.raw`/`score.scaled`, результаты разделов), шкалы (`scale.*`), пользовательские показатели (`result.*`), фактически выданный набор вопросов (выпавший вариант/квоты, перечень и теги), протокол по каждому вопросу (что выдано, ответ, верность, `scoreRatio`, вклад в шкалы). Плюс отладочная подсветка «Эталон» (тоггл, на баллы не влияет): правильные ответы на реальном рендере вариантов, цена ответа — в протоколе |
| BR-13-03 | Доступ к отладке = доступ на РЕДАКТИРОВАНИЕ теста (владелец + грант `edit` — та же область, что у экспорта SCORM). Отдельный грант или роль не вводится. Уточнение 2026-07-30: право отладки отделено от экспорта — `tests.debug.play` вместо `tests.export.scorm`, поскольку генерация SCORM-пакета закреплена за ролью «Разработчик», а прогон в отладчике остаётся у Автора |
| BR-13-04 | Прогоны отладки полностью ОДНОРАЗОВЫЕ: не пишутся в `attempts`, не учитываются в аналитике и истории обучающегося и не создают записей телеметрии (пакет собирается с ВЫКЛЮЧЕННОЙ телеметрией). Состояние прогона живёт только в плеере (`localStorage`), сбрасывается автором; протокол можно экспортировать (CSV) |
| BR-13-05 | Отлаживается ВСЕГДА живая версия теста (текущее редактируемое состояние, live storage); опубликованный снапшот НЕ отлаживается. Все текущие правки автора отражаются в прогоне после пересборки. Сборка — тем же пайплайном, что production-экспорт; для опубликованного теста отгрузка идёт из снапшота, поэтому отладка живого состояния = «что уйдёт при следующей публикации» |
| BR-13-06 | Встроенный и CLI-плеер используют ОДИН источник RTE-шима и инспектора (общий модуль), чтобы поведение отладки не расходилось между хостами; CLI-плеер сохраняется как dev-инструмент |
| BR-13-07 | Плеер поддерживает повторные прогоны для итеративной проверки: после правки теста автор пересобирает пакет («Пересобрать») и сбрасывает состояние («Сброс») и прогоняет заново. N-прогонная «статистика выдачи» (аудит) из охвата СНЯТА: проблемы выдачи (квота недобирает / битый вариант / неравные размеры) детерминированы и относятся к валидации редактора (BR-10-04, BR-12), симуляция дублирует |
| BR-13-08 | Границы точности явно документированы: плеер воспроизводит RTE и расчёт пакета, но НЕ заменяет приёмку в WebTutor — нет реального Sequencing & Navigation, cross-attempt-хранилища и реальной retake-gate-проверки по данным WebTutor (NFR-17) |
| BR-13-09 | UI плеера передаётся в разработку только после подготовленных и согласованных wireframes (NFR-14): основной сценарий, пустые/loading/ошибки/«недоступно», ключевые edge cases |
| BR-13-10 | Прогон идёт на ПРИМЕНЁННОМ к тесту шаблоне оформления (не на демо/`default`). Плеер открывается в ОТДЕЛЬНОМ окне; инспектор размещён в СВОРАЧИВАЮЩЕМСЯ сайдбаре; основные показатели прогона всегда видны в фиксированной СТАТУСНОЙ панели |

### BR-14: Устойчивость таймера и возобновление timed-тестов

**Проблема:** таймер экзамена был эфемерен и оторван от монотонного времени:
сворачивание вкладки замедляло отсчёт, перезагрузка SCO обнуляла таймер и теряла
прогресс timed-теста, аварийное завершение не засчитывало время. Это давало
обучающемуся выигрыш и теряло данные. Расширяет секционные таймеры и поток
BR-06. Дизайн — [PRD-20](./prd-20/timer-integrity-resume.md) (Этап 15).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-14-01 | Таймер экзамена отсчитывает реальное время: не даёт выигрыша от троттлинга фоновой вкладки и от перевода системных часов в пределах сессии |
| BR-14-02 | Перезагрузка SCO не сбрасывает таймер и не теряет прогресс timed-теста: остаток и позиция восстанавливаются; учёт ведётся по активному времени в контенте (семантика «пауза при закрытии SCO») |
| BR-14-03 | Возобновление единообразно по режимам прохождения (BR-06): линейные — с точного вопроса (в т.ч. внутри темы), router — внутри незавершённой темы; адаптив — исключение (переигрывание/последний результат) |
| BR-14-04 | Учёт времени устойчив к аварийному завершению (kill вкладки): потеря не превышает окна периодического сохранения |
| BR-14-05 | Правка `suspend_data` обнаружима и не даёт «фри тайма» (tamper-evidence, дезтеррент); строгая неподделываемость требует серверного времени/подсчёта — вне scope (уровень B) |
| BR-14-06 | Совместимость и деградация: при отсутствии/недоступности LMS-источника активного времени (`cmi.total_time`) поведение деградирует к прежнему без регресса, в т.ч. для тестов без лимита и адаптивных; ES5/raw-рантайм сохранён |
| BR-14-07 | Границы точности документированы: поведение reload/suspend/`total_time` LMS-специфично и требовало приёмки на целевой LMS (WebTutor) до включения в прод. **Приёмка выполнена 2026-08-06 на живом WebTutor — отыграны reload посередине попытки и kill вкладки, семантика `cmi.total_time` подтверждена; техдолг снят, гейт выката пройден** |

### BR-15: Тип вопроса «шкала»

**Проблема:** психодиагностические опросники (референс — опросник профессионального выгорания
Маслач) состоят из утверждений с одинаковой упорядоченной шкалой ответа. Такой опросник
заводился как `multiple` с шестью вариантами и рисовался шестью вертикальными карточками на
каждом из 22 экранов. Механика ответа при этом не «выбор одного из равноправных вариантов», а
«отметка на шкале»: порядок вариантов содержателен, перемешивать их нельзя, а правильного
ответа может не быть вовсе. Дизайн — [PRD-26](./prd-26/scale-question-type.md) (Этап 20).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-15-01 | Поддерживается тип вопроса «шкала»: утверждение и упорядоченный набор градаций, из которых обучающийся выбирает одну |
| BR-15-02 | Порядок градаций содержателен: перемешивание вариантов ответа к шкальному вопросу не применяется |
| BR-15-03 | Измерительный режим (правильная градация не указана): вопрос не проверяется, не приносит баллов и не увеличивает достижимый максимум; его единственный результат — вклад в шкалы (BR-06-10/12) и показатели (BR-04) |
| BR-15-04 | Проверяемый режим (правильная градация указана): вопрос ведёт себя как одиночный выбор, включая градуированную «Цену ответа» (BR-09) |
| BR-15-05 | Числовое значение градации обучающемуся не показывается — он видит только подписи |
| BR-15-06 | Шкала рисуется из ОДНОГО источника разметки на обоих хостах (BR-01, PRD-12) и участвует в обмене Excel наравне с остальными типами (BR-11) |
| BR-15-07 | Вне охвата: матричная раскладка (модель «один вопрос — один экран» не меняется) и автопереход к следующему вопросу по выбору градации |

### BR-16: Отчёт о результатах как часть шаблона

**Проблема:** отчёт о попытке (PDF) остался единственным ученическим артефактом вне модели
шаблона: его вёрстка собиралась в КОДЕ инлайн-стилями с зашитыми цветами и подложками из
ассетов ядра. Разработчик шаблона не мог ни изменить структуру отчёта, ни объявить его
настраиваемые поля; автор теста не мог выбрать вид отчёта и ничего в нём поправить, поэтому
для сертификационного теста документ уходил обучающемуся с чужим брендингом. Дизайн —
[PRD-27](./prd-27/report-template.md) (Этап 21).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-16-01 | Отчёт объявляется шаблоном как вид варианта: ДВА типа — обычный (оценка баллами) и адаптивный (подтверждённые уровни); это разные типы, а не варианты одного. Шаблон может объявить несколько вариантов каждого типа |
| BR-16-02 | Вариант объявляет редактируемые поля; автор теста заполняет их в настройках теста и выбирает вид отчёта — как для остальных страниц шаблона (BR-01-05, PRD-22) |
| BR-16-03 | Выбор варианта разрешается ОДНОЙ общей функцией для всех хостов: отчёт, выданный в LMS, не должен расходиться с предпросмотром автора |
| BR-16-04 | Макет и стиль выбранного варианта попадают в SCORM-пакет запечёнными на сборке: к моменту растеризации читать файлы рантайма поздно |
| BR-16-05 | Отчёт по попытке, выданной из опубликованного снапшота, собирается вариантом ИЗ ВЫДАЧИ: смена вида автором не переписывает задним числом документы по уже выданным попыткам |
| BR-16-06 | Отсутствие объявленного варианта деградирует на встроенный шаблон ЦЕЛИКОМ — и макет, и стиль; пакеты прежней сборки продолжают работать по прежнему пути (обратная совместимость) |
| BR-16-07 | Браузерная проверка работоспособности шаблона (BR-05-02) рендерит по одному варианту каждого объявленного вида отчёта наравне с экранами |

### BR-17: Итоги измерительного теста

**Проблема:** шкалы (BR-06-11) и показатели (BR-04) рассчитываются и передаются в LMS, но не
показываются обучающемуся: ни одна разметка поставляемых шаблонов к ним не обращается, а тогл
показа у шкал намеренно скрыт. Для измерительной методики экран итогов выдаёт бессмысленное
«верно 22 из 22» вместо значений шкал и их толкования. Дизайн —
[PRD-29](./prd-29/measurement-results.md) (Этап 22).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-17-01 | Шкалы и показатели имеют ОБЩУЮ модель толкования: интервалы для числовых значений и перечень исходов с кодами для строковых. Формула возвращает код, а валидатор сверяет литералы формулы с объявленным перечнем |
| BR-17-02 | У шкалы хранится достижимый домен значений с предзаполнением по вкладам вопросов; благоприятное направление задаётся ОТДЕЛЬНО от направления шкалы |
| BR-17-03 | Видимость измерения обучающемуся трёхпозиционная (не показывать / уровень / уровень и значение), а не булев флаг |
| BR-17-04 | Визуальное представление (цветовая схема уровней, вид шкал и показателей, откат по выполнимости) целиком задаётся параметрами варианта дизайна; в данных цветов нет. Окраска зон дискретная, нюанс значения передаётся маркером |
| BR-17-05 | Экран итогов остаётся ОДНИМ макетом результатов; блоки измерений включаются настройками варианта (авто / показать / скрыть) |
| BR-17-06 | Рекомендации из всех источников собираются в единый блок внизу экрана, структурированный по типу ресурса |
| BR-17-07 | Итоги раздела (BR-06-06) не меняются; вне охвата — таблица решений по уровням, удобства редактора DSL и перенос толкований в книгу Excel |

### BR-18: Авторский порядок выдачи вопросов внутри темы

**Проблема:** BR-06-09 объявляет независимые политики порядка, но порядок вопросов ВНУТРИ темы
на практике всегда случайный и не отключается — перемешивание зашито в выдачу, включая режим
фиксированных вариантов (BR-12), где ломается даже курируемый автором список. Авторского
порядка в банке физически нет: у вопроса нет поля порядка, а чтение вопросов темы идёт без
сортировки. Дизайн — [PRD-30](./prd-30/question-order.md) (Этап 23).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-18-01 | У вопроса есть ОПЦИОНАЛЬНЫЙ индекс в теме; отсутствие значения сохраняет текущее случайное поведение (обратная совместимость) |
| BR-18-02 | На разделе задаётся режим порядка выдачи ПЕРЕЧИСЛЕНИЕМ (случайный / фиксированный), а не булевым флагом — под будущие правила вроде «по возрастанию сложности» |
| BR-18-03 | Сортировка по возрастанию индекса, вопросы без индекса — в конец, равные значения перемешиваются между собой («эшелоны» вместо ложно-строгой очереди) |
| BR-18-04 | Порядок влияет ТОЛЬКО на последовательность предъявления: отбор вопросов и квоты по тегам (BR-10) не затрагиваются; в режиме фиксированных вариантов (BR-12) порядок берётся из списка варианта |
| BR-18-05 | Индекс НЕ входит в контентный хеш вопроса: перестановка не должна ложно помечать устаревшими переопределения оценки, привязанные к содержимому |
| BR-18-06 | В адаптивном режиме сортировка действует внутри уровня; в плоском legacy-режиме (BR-06-03) настройка не действует |

### BR-19: Интервал между попытками и общая модель допуска

**Проблема:** один переключатель BR-07 означает на хостах разное. В SCORM-пакете гейт решает по
записям обучения LMS, то есть период охлаждения стоит на границе НАЗНАЧЕНИЯ, а внутри одного
запуска пакет не ограничен ничем. В веб-хосте тот же переключатель проверяется на старте
ПОПЫТКИ и считается от времени завершения предыдущей попытки, потому что связи попытки с
назначением не существует. Тем же расхождением страдает ограничение числа попыток: в пакете
оно живёт внутри регистрации, в вебе — за всю историю обучающегося. Дизайн —
[PRD-31](./prd-31/attempt-interval.md) (Этап 24).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-19-01 | Назначение теста объявляется ЕДИНИЦЕЙ ДОПУСКА: и период охлаждения, и счётчик попыток считаются в границах назначения одинаково на обоих хостах |
| BR-19-02 | Барьер A «общий период охлаждения» (календарные дни) действует МЕЖДУ назначениями; поведение и ограничения BR-07 для него сохраняются без изменений |
| BR-19-03 | Барьер B «интервал между попытками» (часы) — независимый переключатель, действующий ВНУТРИ назначения; диапазон значений ограничен |
| BR-19-04 | Барьеры действуют в непересекающиеся моменты; решение о допуске принимает ОБЩИЙ движок, а хосты поставляют ему только факты |
| BR-19-05 | В SCORM-пакете барьер B проверяется ПОСЛЕ `Initialize` по данным попыток внутри регистрации; требования BR-07-03 и BR-07-05 (гейт до `Initialize`, отсутствие записи `cmi.*`) относятся к барьеру A и не ослабляются |
| BR-19-06 | Момент «сейчас» берётся из доверенного источника времени портала, а не из системных часов клиента |
| BR-19-07 | Блокировка показывается на ШТАТНЫХ экранах прохождения; новых экранов не вводится. Вне охвата: обмен настройкой через книгу Excel, отдельный экран блокировки, ограничение возобновления прерванной попытки |

### BR-20: Материалы обратной связи (внешняя ссылка)

**Проблема (историческая):** автор прикладывал файл (например, PDF) к обратной связи — темы,
уровня шкалы, исхода показателя, — редактор показывал вложение и позволял сохранить, но загрузка
файла на сервер не была реализована с самого начала: при сохранении файл выбрасывался, в модель
уходил только дескриптор. Вскрыто реализацией BR-17 — первой работой, которая попыталась
показать вложения обучающемуся.

**Первый проход (PRD-32, реализовано и принято 2026-08-02):** вложение стало обычным активом
медиатеки — загрузка PDF (до 5 МБ) с сервера, канонический адрес `url = /api/media/<id>`, индекс
использования и права ученика для пяти мест хранения обратной связи (тест, раздел, тема, шкала,
показатель), показ в блоке «Материалы» на обоих хостах. Заодно закрыт независимый дефект
медиатеки: до этого решения медиа, загруженное ПОСЛЕ появления медиатеки, в SCORM ZIP не
попадало вовсе.

**Пересмотр (PRD-42, реализовано и влито 2026-08-05):** приёмка вскрыла, что относительный адрес
`/api/media/<id>` не резолвится вне живой страницы — ни в отдельном PDF-файле (у него нет
браузерного origin), ни из SCORM-пакета (упаковщик переписывает адрес на путь ВНУТРИ ZIP, не
существующий вне пакета). Вместо резолвинга адреса на лету решение изменило саму модель: загрузка
файла УБРАНА, вложение обратной связи — ЧИСТО ВНЕШНЯЯ ссылка {название, url}, по образцу уже
существующих секций «Курсы» и «Мероприятия» того же редактора. Материал больше не хранится
системой и не попадает в SCORM ZIP как локальный ресурс: автор указывает готовый `http(s)`-адрес,
и этот же адрес используется на обоих хостах и в PDF-отчёте без перекладки. Трек закрыт этим
решением. Дизайн — [PRD-32](./prd-32/feedback-assets-delivery.md),
[PRD-42](./prd-42/feedback-assets-links-only.md) (Этап 25).

**Требования (по факту принятых решений):**

| ID | Требование |
| --- | --- |
| BR-20-01 | Вложение обратной связи — внешняя ссылка {название, url}, а не файл, физически хранимый системой; загрузка файла для этого назначения не предусмотрена |
| BR-20-02 | Требование «вложение — локальный ресурс внутри SCORM ZIP» (NFR-03, BR-05-03) на материалы обратной связи НЕ распространяется — осознанное исключение, симметричное уже существующим внешним ссылкам «Курсы»/«Мероприятия»: материал не является ресурсом ШАБЛОНА |
| BR-20-03 | Право перейти по ссылке не отличается от права видеть саму обратную связь; отдельного контроля доступа к файлу не вводится, поскольку файла как отдельной сущности больше нет |
| BR-20-04 | Ссылка — часть текста обратной связи и переносится в снимок публикации вместе с остальным содержимым теста/темы/шкалы/показателя, без отдельной обработки |
| BR-20-05 | Дескрипторы, сохранённые по первому решению (PRD-32, `url: /api/media/<id>`), не мигрируются и не блокируются валидацией: строка остаётся редактируемой, но ссылка в PDF-отчёте у неё остаётся БИТОЙ, пока автор не заменит адрес вручную на настоящий `http(s)`-URL — принятый технический долг, не дефект |
| BR-20-06 | Обходная дорога прежней редакции («материалы кладутся ссылкой до закрытия трека») стала ПОСТОЯННЫМ решением, а не временной мерой: дальнейшего перехода на файловое хранение не запланировано |

### BR-21: Оценка целесообразности психодиагностического охвата и управленческой отчётности

**Повод:** сверка продуктового позиционирования («клиент загружает любой сложный психологический
опросник, система считает результаты по заданным формулам и показывает руководству наглядные
итоги») с фактическим состоянием кода и требований, выполненная 2026-08-01. Сверка подтвердила, что
расчётное ядро под опросники есть и проверено на живой методике (опросник Маслача: тип вопроса
«шкала» BR-15, вклады с весами и направлениями шкал BR-06, формулы показателей BR-04, загрузка
книгой Excel BR-11), и одновременно вскрыла перечень возможностей, которых нет ни в коде, ни в
требованиях. Часть из них ранее была сознательно снята или отложена, часть не рассматривалась
вообще.

**Существо требования:** по каждой позиции перечня ниже принять и зафиксировать решение —
ввести требования отдельным треком, отклонить с обоснованием либо оставить обходную дорогу.
Настоящий пункт НЕ вводит функциональных требований и НЕ является обязательством реализации: его
результат — решения, а не функциональность. До получения решений позиции перечня остаются вне
охвата, и обещать их вовне нельзя.

**Что уже покрыто другими документами и повторной оценки не требует:** качество самих заданий и
тестов (трудность, дискриминативность, надёжность, дистракторы, импорт обезличенных выгрузок из
LMS) — [BRD психометрической аналитики](./brd-psychometric-analytics.md); IRT, автоматизация
валидности и анализ времени на задание вынесены за охват там же.

| ID | Позиция на оценку | Текущее состояние |
| --- | --- | --- |
| BR-21-01 | Условное ветвление по результату: открытие раздела или выдача блока по значению шкалы, показателя либо по ответу на конкретный вопрос (skip-logic опросника) | Правила открытия раздела умеют только «завершён» и «зачтён» (BR-06); движок правил снят 2026-07-05 (BR-03), поэтому оценка означает пересмотр снятого решения в узкой части «условие от результата», а не возврат no-code движка |
| BR-21-02 | Матричная раскладка шкальных вопросов: несколько утверждений с общей шкалой на одном экране | Прямо вне охвата (BR-15-07): модель «один вопрос — один экран» зафиксирована. Следствие — опросник на 60 утверждений даёт 60 экранов |
| BR-21-03 | Семантический дифференциал: биполярная шкала с подписями полюсов и неподписанными делениями | Требований нет. Технически собирается на типе «шкала», но модель требует непустую подпись у каждой градации и не имеет полей для подписей полюсов, поэтому канонический вид недостижим |
| BR-21-04 | Ипсативный (forced-choice) вопрос: «наиболее похоже / наименее похоже» из набора утверждений | Требований и типа вопроса нет. Обходная дорога — два одиночных выбора на блок с вкладами в разные шкалы; связность блока при этом ничем не гарантируется. Затрагивает применимость к методикам типа DISC |
| BR-21-05 | Нормативные преобразования результата: стены, станайны, z-оценки, процентили относительно нормативной выборки, хранение самих норм | Нормализация шкалы ограничена значениями «нет», «процент» и «пользовательская» (BR-06-11); понятия нормативной выборки в модели нет |
| BR-21-06 | Композитные шкалы (шкала с источником «другие шкалы») и глобальная библиотека шкал для переиспользования между тестами | Отложено при закрытии Этапа 6 в будущий PRD. Частично обходится показателем-формулой над шкалами; оценка должна ответить, достаточно ли этого |
| BR-21-07 | Расширение DSL показателей: функции округления, ограничения диапазона, минимума и максимума, агрегаты по перечню; доступ к ответу на конкретный вопрос | Сегодня доступны четыре арифметические операции, сравнения, условие, обращения к темам, тегам, разделам, шкалам и другим показателям. Пересчёты выражаются вложенными условиями или интервалами шкалы; зависимость от отдельного ответа проводится только через шкалу |
| BR-21-08 | Лепестковая диаграмма (радар) профиля шкал как вид рендера на экране итогов и в отчёте | **РЕШЕНО 2026-08-01: ввести требования** — BR-24 (Этап 29), дизайн [PRD-35](./prd-35/competency-radar.md). Охват первого этапа ограничен завершённой попыткой: экран итогов и отчёт. Групповой профиль и управленческий срез остаются позициями BR-21-09 и BR-21-10 |
| BR-21-09 | Групповой профиль по шкалам: агрегация шкал и показателей по группе, сравнение групп, динамика шкалы во времени | Шкалы и показатели агрегируются только внутри одной попытки; групповая аналитика ограничена процентами, зачётами и разбивкой по темам |
| BR-21-10 | Управленческий срез: экран руководителя со срезами и сводный отчёт по группе или подразделению | Отчёт формируется по ОДНОЙ попытке (BR-16); домашняя страница даёт рабочий стол по правам, отдельного руководительского среза у роли «Менеджер» нет |
| BR-21-11 | Выгрузка результатов во внешние системы отчётности: программный интерфейс или подписка на события вместо ручной выгрузки в Excel | Требований нет; сегодня доступна выгрузка аналитики книгой Excel |

| ID | Требование к самой оценке |
| --- | --- |
| BR-21-12 | Оценка выполняется по каждой позиции отдельно и завершается одним из трёх решений: ввести требования (с указанием трека), отклонить (с обоснованием), оставить обходную дорогу (с её описанием) |
| BR-21-13 | Позиции BR-21-01 и BR-21-02 требуют явного пересмотра ранее принятых решений (BR-03 и BR-15-07 соответственно); решение по ним не может быть принято молчаливым добавлением требований |
| BR-21-14 | Каждая позиция оценивается по спросу (запрос от заказчика или продажи), а не по технической привлекательности; при отсутствии спроса решение по умолчанию — отклонить и зафиксировать обходную дорогу |
| BR-21-15 | Решения фиксируются в этом BRD (правкой перечня выше), а заведённые треки — в [ROADMAP §0](../ROADMAP.md); до внесения решения позиция считается вне охвата |

### BR-22: Единый вход по корпоративной учётной записи (SSO)

**Приоритет: внеочередной.** Основание — [конкурентный анализ](../reports/ANALYSIS_COMPETITORS.md)
2026-08-01: отсутствие единого входа относится к ОТСЕКАЮЩИМ критериям корпоративной закупки. Такой
критерий снимает продукт с рассмотрения независимо от его прочих достоинств, то есть блокирует
ценность всех остальных треков разом. Направление — [«Пройти отсечки закупки»](../reports/ANALYSIS_COMPETITIVE_DIRECTIONS.md).

**Проблема:** войти в систему сегодня можно только паролем либо по одноразовой ссылке-приглашению.
Корпоративный заказчик требует вход по учётной записи своего каталога как условие приобретения, а не
как удобство: пароль в стороннем сервисе означает второй контур учётных данных, который служба
безопасности обязана согласовать отдельно. У прямого конкурента SSO заявлен.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-22-01 | Поддерживается вход через внешнего поставщика идентификации по стандартному протоколу; конкретный протокол и их число определяет PRD |
| BR-22-02 | SSO — ДОПОЛНИТЕЛЬНЫЙ способ входа, а не замена существующих: вход паролем и доступ по ссылке-приглашению сохраняются. Внешний участник (BR-21 / PRD-28) во внешнем каталоге не заводится и продолжает приходить по ссылке |
| BR-22-03 | Сопоставление внешней учётной записи с локальной однозначно и не опирается на отображаемое имя. Адрес почты хранится зашифрованным, а ищется по хешу — механизм сопоставления обязан работать в этой модели, а не в обход неё |
| BR-22-04 | Роли и объектные области доступа остаются за моделью BR-13: поставщик идентификации подтверждает ЛИЧНОСТЬ, а не права. Перенос групп или ролей из внешнего каталога — опция, решение о ней принимает PRD |
| BR-22-05 | Автоматическое создание учётной записи при первом входе по умолчанию ЗАПРЕЩЕНО: иначе каталог заказчика порождает учётные записи без владельца и без назначений. Разрешение — явное решение PRD с указанием, кто становится владельцем |
| BR-22-06 | При входе и при погашении ссылки-приглашения идентификатор сессии пересоздаётся. Требование закрывает известный технический долг: сегодня ни один из двух путей входа этого не делает, и пробел системный — чинить обе точки надо одновременно |
| BR-22-07 | Вне охвата: собственный поставщик идентификации, федерация между заказчиками, SCORM-пакет (внутри пакета вход — дело LMS, а не наше) |

### BR-23: Согласие на обработку персональных данных и срок хранения

**Приоритет: внеочередной.** Основание — тот же конкурентный анализ: формальное соответствие
относится к отсекающим критериям. Дополнительно требование снимает известный технический долг и
разблокирует BR-21 / PRD-28.

**Проблема:** экрана согласия не существует. Страница первого входа отправляет признак согласия
жёстко зашитым значением, а подготовленные строки интерфейса не используются нигде. Срок хранения
персональных данных не определён. У штатного сотрудника основание обработки — трудовые отношения,
и пробел для него не критичен; у внешнего участника единственное основание — согласие, которого
фактически никто не даёт. Хуже того, внешний участник **никогда не увидит экран первого входа**,
потому что в систему не входит: он приходит по ссылке. Его имя и адрес почты при этом остаются
в базе бессрочно.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-23-01 | Согласие фиксируется ЯВНЫМ действием субъекта. Зашитое в код значение признака недопустимо ни при каких условиях |
| BR-23-02 | Момент запроса согласия обязан покрывать участника, который в систему НЕ входит: согласие берётся на границе начала прохождения, а не только на первом входе |
| BR-23-03 | Хранится факт согласия, его дата и ВЕРСИЯ текста, на который человек согласился. Без версии невозможно доказать содержание согласия задним числом |
| BR-23-04 | Срок хранения персональных данных определяется явно, вместе с действием по его истечении: удаление, обезличивание с сохранением результата или бессрочное хранение. Молчаливое «бессрочно» решением не считается |
| BR-23-05 | Отказ от согласия — определённый сценарий с понятным исходом, а не тупик и не молчаливый пропуск |
| BR-23-06 | Согласие встраивается в существующий экран старта прохождения; отдельного шага у обучающегося не появляется |
| BR-23-07 | Право субъекта на отзыв согласия и на удаление своих данных определяется явно: что происходит с уже сданными попытками и с результатами, на которые опирается аналитика |

### BR-24: Радар компетенций

**Основание:** решение по позиции BR-21-08, принятое 2026-08-01 в порядке BR-21-12.

**Проблема:** шкалы (BR-06) показываются обучающемуся по одной. Все виды рендера BR-17 односкальны,
поэтому методика из трёх шкал выглядит как три независимые карточки, и профиль — то есть собственно
вывод измерительной методики — не читается. Особенно это заметно там, где благоприятные направления
шкал разные: список карточек скрывает соотношение между ними. Дизайн —
[PRD-35](./prd-35/competency-radar.md) (Этап 29).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-24-01 | Профиль по нескольким шкалам показывается ОДНОЙ диаграммой — на экране итогов завершённой попытки и в отчёте о результатах (BR-16) |
| BR-24-02 | Луч диаграммы соответствует шкале; расстояние от центра — доля значения от объявленного диапазона шкалы БЕЗ переворота по благоприятному направлению. Оценка уровня несётся цветом и текстовой меткой, а не длиной луча |
| BR-24-03 | Трёхпозиционная видимость BR-17 соблюдается: скрытая шкала на диаграмму не попадает; при показе уровня без значения луч огрубляется до интервала, чтобы графика не раскрывала скрытый балл |
| BR-24-04 | Диаграмма ДОПОЛНЯЕТ карточки шкал, а не заменяет их: толкование и рекомендации остаются текстом |
| BR-24-05 | Показ включается ЯВНЫМ решением автора, независимо для экрана итогов и для отчёта; по умолчанию выключен, существующие тесты вида не меняют |
| BR-24-06 | Диаграмма рисуется только при трёх и более видимых шкалах. Для обучающегося отказ молчаливый, автору редактор объясняет причину |
| BR-24-07 | Диапазон шкалы ХРАНИТСЯ и записывается до попытки; вычисление его в момент показа недопустимо — результат обязан оставаться воспроизводимым после правки состава вопросов (NFR-21) |
| BR-24-08 | Диаграмма одинакова на обоих хостах и в отчёте; расхождение считается дефектом. Скрипты и внешние библиотеки не используются: разметка живёт внутри LMS и растеризуется при печати |
| BR-24-09 | Вне охвата первого этапа: темы как источник лучей, групповой профиль и сравнение попыток (BR-21-09, BR-21-10), сквозной профиль по нескольким тестам |

### BR-25: Адаптивное тестирование по модели IRT

**Место в очереди: ПОСЛЕ психометрики.** Требование не может быть начато раньше, чем банк заданий
откалиброван: последовательность — CTT ([BRD психометрической аналитики](./brd-psychometric-analytics.md))
-> IRT -> адаптивная выдача. Это не приоритет и не отсрочка, а **зависимость по данным**: без
оценённых на реальных ответах параметров задания требование нереализуемо в принципе.

**Проблема двойная.**

_Терминологическая._ Существующий адаптивный режим — лестница по уровням сложности: у уровня есть
диапазон сложности, число заданий и порог; зачёт уровня двигает на уровень вверх, провал — на уровень
вниз, исход темы — достигнутый уровень. Сложность задания при этом **назначена автором вручную**
(`questions.difficulty`, 0–100) и ни на чём не откалибрована. В отрасли термин «адаптивное
тестирование» устойчиво занят другим: выбором следующего задания по модели IRT с оценкой способности
и стандартной ошибкой измерения. Расхождение названия и содержания создаёт репутационный риск на
демонстрации методисту заказчика и в закупке, где «адаптивность» может стоять в спецификации.

_Продуктовая._ Настоящей адаптивной выдачи нет ни у одного из шести рассмотренных конкурентов
(см. [конкурентный анализ](../reports/ANALYSIS_COMPETITORS.md) и
[мировой эталон](../reports/ANALYSIS_WORLD_BEST_PRACTICE.md)). Это единственная позиция, где
психометрика конвертируется не в отчёт для методиста, а в возможность продукта.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-25-01 | Существующий уровневый режим СОХРАНЯЕТСЯ как самостоятельный и не заменяется: он решает обучающую задачу (довести обучающегося до уровня, выдав ресурсы уровня), а не измерительную |
| BR-25-02 | Точное описание существующего режима фиксируется в документации и во внешних материалах: маршрутизация по порогам между полосами сложности. Термин «адаптивное тестирование» без пояснения вовне не используется |
| BR-25-03 | Адаптивная выдача по модели IRT вводится ТОЛЬКО на откалиброванном банке; условие старта измеримо (объём наблюдений на задание, проверка соответствия модели) и задаётся PRD, а не берётся на глаз |
| BR-25-04 | Оценка способности выдаётся со СТАНДАРТНОЙ ОШИБКОЙ измерения; критерий остановки — достижение заданной точности либо предел длины теста |
| BR-25-05 | Правило выбора задания учитывает ограничения по содержанию (спецификация теста — квоты BR-10) и контроль экспозиции задания; без этого адаптация ломает и покрытие тем, и безопасность банка |
| BR-25-06 | Результаты испытуемых, получивших РАЗНЫЕ наборы заданий, сопоставимы между собой — это цель введения режима, а не побочный эффект |
| BR-25-07 | Разделение сред: калибровка выполняется на сервере по накопленным наблюдениям; в SCORM-пакет параметры заданий ЗАПЕКАЮТСЯ, а оценка способности считается в рантайме. Обновление калибровки внутри выданного пакета невозможно — пакет живёт с параметрами на момент сборки |
| BR-25-08 | Деградация обязательна: при недостаточной калибровке или невыполнимости режима тест играется фиксированной формой либо существующим уровневым режимом, а не отказом |
| BR-25-09 | Вне охвата: многоступенчатый тест (MST) как отдельный дизайн, эквивалентирование форм, автоматическая сборка формы под спецификацию по параметрам IRT |

### BR-26: Управление экспозицией заданий

**Место в очереди: после накопления данных.** Как и BR-25, требование зависит не от приоритета,
а от наличия наблюдений: пока о выдаче заданий ничего не известно, управлять нечем. Часть требований
(предупреждение об ожидаемой экспозиции, равномерность распределения вариантов) данных НЕ требует и
может быть выполнена раньше остального.

**Проблема.** Экспозиция задания — доля испытуемых, которым оно было выдано, — не измеряется и не
управляется. Чем выше экспозиция, тем вероятнее утечка содержания задания, после которой оно
измеряет уже не подготовку, а доступ к утечке. Сегодня выдача равномерно случайна в рамках квот,
счётчика выдач нет, и банк вырабатывается незаметно для автора: при банке 25 и выдаче 20 экспозиция
каждого задания около 80% с первого же потока.

**Ограничение, определяющее форму решения.** Классический контроль экспозиции (Sympson-Hetter и
родственные вероятностные методы) опирается на ОБЩИЙ счётчик по всей популяции. В автономном
SCORM-пакете такого счётчика нет и быть не может: пакет не знает ничего за пределами текущего
запуска, а телеметрия опциональна и по замыслу отключаема. Поэтому управление экспозицией переносится
на этап СБОРКИ пакета, а рантайм получает уже готовую статическую поправку. Разбор ограничений и
рычагов — в [мировом эталоне §5.1](../reports/ANALYSIS_WORLD_BEST_PRACTICE.md).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-26-01 | Экспозиция измеряется: доля попыток, в которых задание было выдано, вычисляется по накопленным наблюдениям (веб-прохождения, телеметрия SCORM, импорт выгрузок из LMS) и показывается автору |
| BR-26-02 | Источником наблюдений признаётся ИМПОРТ ВЫГРУЗОК ИЗ WEBTUTOR — единственный путь получить данные там, где телеметрия выключена, а прохождение идёт в LMS заказчика. Формат обмена и ключ сопоставления задания ОБЩИЕ с психометрикой ([BRD психометрики](./brd-psychometric-analytics.md), OQ-01/OQ-02): двух разных импортов не заводится, один поток данных питает и психометрику, и экспозицию |
| BR-26-03 | Автор предупреждается об ОЖИДАЕМОЙ экспозиции при настройке выдачи и при сборке пакета. Величина вычислима заранее из размера страты и объёма выдачи и наблюдений НЕ требует — это требование выполнимо немедленно |
| BR-26-04 | Показатель экспозиции задания ЗАПЕКАЕТСЯ в пакет, и рантайм учитывает его при выдаче: внутри страты вероятность выбора зависит от накопленной экспозиции, «горячие» задания выпадают реже. Это не контроль в реальном времени, а перенос накопленного знания в статическую поправку на момент сборки |
| BR-26-05 | Поправка действует ВНУТРИ существующих ограничений выдачи: квоты по тегам (BR-10) и правила отбора не нарушаются. К фиксированным вариантам (BR-12) поправка не применяется — состав варианта задан автором |
| BR-26-06 | Поправка не смещает трудность выдачи: взвешивание выполняется так, чтобы систематически не вымывать лёгкие или трудные задания (ограничение отношения весов либо взвешивание внутри полос сложности). Требование прямо связано с сопоставимостью форм и не может быть опущено «на первое время» |
| BR-26-07 | Экспозиция — СТАТИСТИКА, а не содержание: она не входит в хеш содержания задания и не делает опубликованные снимки устаревшими (прецедент — авторский порядок выдачи, BR-18). Значение берётся на момент СБОРКИ пакета, а не на момент публикации версии |
| BR-26-08 | Распределение фиксированных вариантов между обучающимися делается РАВНОМЕРНЫМ без глобального счётчика — детерминированным выбором по идентификатору обучающегося из LMS. Это отдельная от экспозиции мера: она не снижает долю показов, но устраняет перекос случайного выбора и вырожденную ротацию в пакете (BR-12, NFR-17) |
| BR-26-09 | Деградация обязательна: без данных об экспозиции выдача работает как сегодня (равномерно случайная в рамках квот); при банке, из которого выдаётся почти всё, поправка не применяется |
| BR-26-10 | Вне охвата: вероятностные методы контроля экспозиции реального времени (Sympson-Hetter и родственные) — в автономном пакете невозможны; вывод задания из оборота решением системы; «окно банка» и переиздание пакета как отдельная модель издания |

### BR-27: Импорт и экспорт по IMS QTI

**Место в очереди: данных не требует.** В отличие от BR-25 и BR-26, требование не зависит ни от
психометрики, ни от накопленных наблюдений: работа целиком лежит в нашей зоне контроля и может
выполняться параллельно любому другому этапу.

**Проблема.** Единственный формат обмена содержанием — книга Excel собственного формата (BR-11).
Из этого следует двойное ограничение. На **входе** банк вопросов заказчика к нам можно только
перепечатать или вручную привести к нашей книге, а именно накопленный банк — главное, что удерживает
заказчика в существующем инструменте. На **выходе** содержание вынимается только в наш формат, то
есть продукт выглядит как замок, ровно в том месте, где автономный SCORM-пакет служит обратным
аргументом. Встроенный конструктор WebTutor обменивается тестами по IMS QTI: это ЕДИНСТВЕННАЯ
позиция сравнения, где он объективно сильнее нас
(см. [сравнение с WebTutor и iSpring §2.5](../reports/ANALYSIS_WEBTUTOR_ISPRING.md)). Отраслевой
стандарт обмена САМИМИ заданиями — QTI 3.0; по этой оси у нас ноль форматов обмена при одном формате
доставки, и сверка с эталоном относит позицию к «высокая применимость, делать»
(см. [мировой эталон §3 и §10](../reports/ANALYSIS_WORLD_BEST_PRACTICE.md)).

**Граница, определяющая форму решения.** Стандарт описывает ЗАДАНИЯ и тесты, но не описывает нашу
выдачу и наш расчёт: квоты по тегам (BR-10), фиксированные варианты (BR-12), сценарий прохождения
(BR-06), вклад в шкалы (BR-06-10/12), «Цена ответа» (BR-09) в его терминах не выражаются. Поэтому
обмен по стандарту не может и не должен подменять внутренний обмен: книга Excel остаётся форматом
переноса БЕЗ потерь, QTI отвечает за перенос содержания между СИСТЕМАМИ.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-27-01 | Система импортирует и экспортирует задания по IMS QTI. Импорт первичен по ценности (забрать накопленный банк заказчика без ручного переноса), экспорт обязателен как отсутствие замка: содержание, внесённое в систему, должно выниматься в отраслевом формате, а не только в нашем |
| BR-27-02 | Единица обмена первой очереди — ЗАДАНИЕ и пакет заданий (банк), импортируемый в тему. Обмен ТЕСТОМ как сценарием (разделы, правила прохождения, выдача) в первую очередь не входит: наша выдача и наш расчёт в терминах стандарта не выражаются, и перенос «половины теста» ввёл бы обе стороны в заблуждение сильнее, чем его отсутствие |
| BR-27-03 | Объявляется и документируется ПРОФИЛЬ соответствия: принимаемые и выпускаемые редакции стандарта, перечень взаимодействий, правил обработки ответа и метаданных. Заявление «поддерживаем QTI» шире объявленного профиля не делается ни в материалах, ни в UI |
| BR-27-04 | Выпуск — в QTI 3.0 как актуальной редакции стандарта; приём — в тех редакциях, которые РЕАЛЬНО выпускают контрагенты (в первую очередь WebTutor), а не только в актуальной. Состав принимаемых редакций определяется по файлам-образцам, полученным от систем-источников и заведённым в репозиторий как эталоны приёмки; документация вендора доказательством не считается. **С 2026-08-06 стенд WebTutor доступен — образцы выгрузки можно получить фактически, и это предпочтительный путь определения состава редакций** |
| BR-27-05 | Отображение наших типов вопросов на взаимодействия стандарта фиксируется таблицей в ОБЕ стороны (одиночный и множественный выбор, сопоставление, ранжирование, шкала) и ведётся в спецификации формата обмена — тем же способом, что контракт книги Excel (BR-11-04) |
| BR-27-06 | Взаимодействия и правила обработки ответа вне профиля не импортируются МОЛЧА: задание отклоняется с понятной построчной причиной, остальные задания импортируются, обновление по идентификатору идемпотентно. Правила диагностики и идемпотентности BR-11-05 распространяются на QTI без изменений |
| BR-27-07 | Свойства, у которых в стандарте нет выражения — вклад в шкалы (BR-06-10/12), «Цена ответа» (BR-09), условная обратная связь, теги-подтемы (BR-10), принадлежность вариантам выдачи (BR-12) — при экспорте либо переносятся штатным механизмом расширений и метаданных стандарта, либо ЯВНО объявляются теряемыми. Автор видит перечень того, что не уйдёт в файл, ДО выгрузки; молчаливая потеря недопустима |
| BR-27-08 | QTI НЕ заменяет книгу Excel: обмен без потерь внутри системы и между нашими инсталляциями остаётся за форматом BR-11. Оба контракта ведутся синхронно с кодом; второго источника истины по модели вопроса не заводится |
| BR-27-09 | Пакет обмена несёт медиа задания. Так как импорт медиа выведен из BR-11 в отдельный будущий PRD «Медиатека» (BR-11-06), первая очередь импортирует задания БЕЗ медиа с явной диагностикой пропущенных ресурсов; полноценный приём медиа выполняется вместе с медиатекой и отдельным решением не дублируется |
| BR-27-10 | Содержание задания из внешнего файла — HTML недоверенного происхождения: оно проходит ту же серверную санитизацию и запрет внешних URL, что и контентные страницы (NFR-05, NFR-03). Импортированное задание не получает прав, которых нет у заведённого вручную |
| BR-27-11 | Импортированные задания попадают в общую модель владения и прав: тема-владелец, автор записи, объектные права (PRD-13/PRD-15). Импорт не является обходным путём мимо контроля доступа и мимо контроля целостности содержания (PRD-15) |
| BR-27-12 | Обмен обратим на нашем подмножестве: задание, выпущенное системой и импортированное обратно, даёт то же содержание (round-trip); расхождение на подмножестве считается дефектом — как в BR-11-01 |
| BR-27-13 | Приёмка выполняется на РЕАЛЬНЫХ файлах сторонних систем (приём) и на выпуске, проверенном схемой или валидатором стандарта (выпуск). «Читаем то, что сами написали» доказательством переносимости не является |
| BR-27-14 | Вне охвата первой очереди: собственные переносимые взаимодействия (PCI), доступностные расширения APIP, обмен РЕЗУЛЬТАТАМИ прохождения (QTI Results Reporting — это другая ось, её закрывают xAPI/cmi5), собственные (не шаблонные) правила обработки ответа и адаптивные задания стандарта |

### BR-28: Листинг кода в тексте задания

**Проблема:** автор не может показать участнику фрагмент исходного кода без потери смысла.
Набранный в поле вопроса листинг приезжает участнику как обычный абзац: пропадают ведущие пробелы
(не значимы для инлайн-разметки), пропорциональный шрифт заменяет моноширинный, а типографический
проход подменяет символы (прямые кавычки на «ёлочки», дефис между пробелами на тире). Вопрос «что
выведет этот фрагмент» — базовая механика ИТ-аттестации, недоступная сегодня. Вскрыто конкурентным
анализом (сравнение со StartExam, §4.1): у конкурента вставка кода — штатное средство оформления
задания наравне с изображениями и формулами LaTeX. Формулы LaTeX — соседний пробел из того же
анализа, но отдельная задача, в охват не входит. Дизайн — [PRD-33](./prd-33/code-listing.md)
(Этап 33).

**Статус требований:** зафиксирован только ПОВОД и препятствия в существующей разметке
(`shared/text/`, единой на веб-хост и SCORM-пакет); требования не собирались, форма решения не
согласована, эскизов нет.

**Вопросы, которые проектирование обязано закрыть первыми (не требования):**

| ID | Вопрос | Почему важен |
| --- | --- | --- |
| BR-28-01 | Синтаксис: обратные кавычки как в markdown или собственная запись | Обратные кавычки сегодня ничего не значат и уже могут встречаться в текстах — риск задним числом изменить смысл существующего содержимого |
| BR-28-02 | Где допускается блок кода: только на контентной странице (блочный рендер) или и в тексте вопроса | Текст вопроса сегодня рендерится в заголовок `<h2>`, где блочные теги невалидны; либо код там остаётся инлайновым, либо заголовок становится блочным контейнером — правка макетов обоих поставляемых шаблонов |
| BR-28-03 | Нужна ли подсветка синтаксиса | Тянет в SCORM-пакет стороннюю библиотеку — сегодня в пакет вендорятся только html2canvas и jsPDF |
| BR-28-04 | Новая грамматика обязана быть обратно совместимой на существующих текстах | `stripMarkdown` участвует в `content_hash` (PRD-15), который пинится в переопределениях оценки (BR-09); изменение результата на старых текстах ложно пометит их устаревшими — прецедент решения: PRD-30 сознательно не включил `order_index` в хеш по той же причине (BR-18-05) |
| BR-28-05 | Что показывать в экране обзора (PRD-19) и в PDF-отчёте (BR-16) | Оба сегодня снимают всю разметку и печатают голый текст с типографикой — для листинга это тот же риск испорченных символов |
| BR-28-06 | Как листинг проходит книгу Excel (BR-11) и как вводится в редакторе | Многострочный текст с ведущими пробелами обязан пережить круговой рейс без потерь; сегодня это не проверялось |

### BR-29: Защита текста задания от копирования

**Проблема:** участник выделяет текст задания и вариантов ответа и уносит его — в поисковую строку
прямо во время попытки, в переписку, в файл, — и тогда утекает не один вопрос, а банк целиком.
Сегодня этому не противостоит ничто: ученический экран ведёт себя как обычная веб-страница. Вскрыто
конкурентным анализом ([сравнение с рынком](../reports/ANALYSIS_COMPETITORS.md) §4.4,
[матрица StartExam](../reports/ANALYSIS_STARTEXAM_MATRIX.md) §9): у конкурентов защита от
копирования есть, у нас — нет, а единственная общая мера против списывания — уникальная выдача
каждому (BR-10/BR-12). Требования согласованы 2026-08-02, включая переформулировку задачи (см.
ниже); код не начат, экранные элементы водяного знака и заглушки скрытия требуют эскиза. Дизайн —
[PRD-34](./prd-34/copy-protection.md) (Этап 34).

**Граница задачи, принятая явно:** «запретить копирование» технически недостижимо ни в браузере,
ни против держателя SCORM ZIP (в пакете разметка и ключи лежат внутри архива открытым текстом).
Запрет снимка экрана недостижим отдельно: ни у веб-платформы, ни у SCORM-пакета в чужой LMS-рамке
нет средства исключить окно из захвата, а PrintScreen браузеру не виден вовсе. Постановка задачи —
не запрет, а ПОВЫШЕНИЕ ЦЕНЫ бытового копирования штатными средствами экрана и АТРИБУЦИЯ утечки
через снимок экрана, когда сам снимок предотвратить нельзя.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-29-01 | Автор включает защиту тремя НЕЗАВИСИМЫМИ настройками теста: базовая защита периметра (умолчание ВКЛ — распространяется и на уже существующие тесты), водяной знак (умолчание ВЫКЛ), скрытие задания при потере окном фокуса (умолчание ВЫКЛ) |
| BR-29-02 | Периметр защиты перечислен поимённо по слотам макета (текст задания, варианты ответа, обратная связь, медиа задания, подсказка) и экрану обзора целиком; результаты, содержательные страницы, стартовый экран и PDF-отчёт защите не подлежат |
| BR-29-03 | Базовая защита — пять мер без перехвата горячих клавиш: запрет выделения, подавление контекстного меню, перехват `copy`/`cut`, подавление перетаскивания мышью, исключение из печати. На попытку копирования участник видит короткое сообщение; запись о попытке НЕ ведётся — трек не вводит наблюдения за участником |
| BR-29-04 | Водяной знак — читаемая обезличенная строка (идентификатор и время, без ФИО и почты), не перекрывающая содержимое; место объявляет макет, разметку и контраст — ядро. Знак обязателен на экране итогов теста независимо от базовой защиты (там же раскрываются правильные ответы при `showCorrectAnswers`) |
| BR-29-05 | Скрытие при потере фокуса закрывает задание заглушкой при уходе на другую вкладку и возвращает автоматически, без действия участника; ход попытки (таймер, ответы) не затрагивается — мера прячет, а не наказывает |
| BR-29-06 | В отладочном прогоне (BR-13) и в предпросмотре шаблона (BR-05) базовая защита и скрытие при потере фокуса НЕ действуют — автор обязан читать и копировать текст своего вопроса; водяной знак при этом показывается с заглушкой вместо идентификатора |
| BR-29-07 | Мера — ОДНА реализация на оба хоста через общий построитель периметра (PRD-12) и применяется инлайн-свойствами, чтобы не зависеть от каскада CSS шаблона; загруженный внешний шаблон (BR-05), ничего не знающий про защиту, получает её в полном объёме |
| BR-29-08 | На сенсорных экранах мера действует по всему периметру единообразно, включая выделение по долгому нажатию — без послаблений для одиночного выбора |
| BR-29-09 | Настройки проходят полный контур: схема теста, редактор, снимок публикации (BR-06/PRD-15), запекание в SCORM-пакет. Книга Excel (BR-11) настройки уровня теста сегодня не переносит вообще — общий пробел, не специфичный для защиты, и в охват BR-29 не входит |
| BR-29-10 | Формулировка «текст скопировать нельзя» приёмочным критерием не является; принята граница — против держателя ZIP мера не работает, и это осознанная плата, а не дефект |

### BR-30: Компактность состояния прогона в SCORM-пакете

**Проблема:** состояние прогона SCORM-пакета целиком лежит в `cmi.suspend_data`, и туда попадает
не только то, что нужно для продолжения прерванной попытки, но и ПОЛНОЕ содержимое выданных
вопросов — дословно, дважды (в каждой завершённой попытке и в снимке текущего прогона). Тест на
25 вопросов даёт 35-75 тысяч символов на одну завершённую попытку при пределе `cmi.suspend_data` в
SCORM 2004, равном 64000, а предела на рост массива попыток НЕТ. Переполнение проявляется не
ошибкой, а МОЛЧА: LMS обрезает строку, разбор падает, и одновременно обнуляются счётчик попыток,
история, таймер (BR-14) и оба барьера допуска (BR-07/BR-19). Требования согласованы 2026-08-03,
реализация не начата. Дизайн — [PRD-36](./prd-36/run-state-compactness.md) (Этап 35).

**Требования:**

| ID | Требование |
| --- | --- |
| BR-30-01 | Состояние прогона не содержит содержимого вопросов (текстов, вариантов, эталонов, обратной связи, адресов медиа); вопрос адресуется ПОЗИЦИЕЙ в статичном `TEST_DATA` пакета, а не идентификатором |
| BR-30-02 | История завершённых попыток не хранится списком: только счётчик, сводка лучшей попытки и сводка последней (для барьера BR-19); размер состояния не зависит от числа выполненных попыток |
| BR-30-03 | Повопросная детализация (выдача, ответы, статусы) хранится ТОЛЬКО у лучшей попытки — единственного потребителя, которому она нужна (сборка `interactions` для LMS) |
| BR-30-04 | Бюджет состояния — 4096 символов (предел `cmi.suspend_data` в SCORM 1.2, пятнадцатикратный запас на 2004); при приближении к бюджету жертвуется НЕ прогон в работе, а сначала повопросная детализация лучшей попытки, затем сама сводка — с обязательной диагностикой, без молчаливой потери |
| BR-30-05 | Поля механизмов допуска и таймера (BR-07, BR-14, BR-19) сохраняются в прежнем виде и прежней семантике; изменяется только форма истории попыток |
| BR-30-06 | Состояние версионируется; пакет, собранный до BR-30, продолжает работать со своим форматом, а пакет после BR-30 приводит старый формат к новому при первом запуске без потери счётчика попыток, лучшей и последней попытки, якоря таймера и обеих дат барьеров |
| BR-30-07 | Отладочный плеер (BR-13) показывает состояние нового формата, включая занятую долю бюджета |

### BR-31: Профиль экспорта SCORM 1.2

**Проблема:** инструмент выгружает единственный профиль — SCORM 2004 4th Edition. Конкурентный
анализ относит это к пробелам: iSpring и WebTutor принимают и отдают SCORM 1.2 наравне с 2004,
AICC, xAPI и cmi5, а SCORM 1.2 остаётся самым распространённым профилем в корпоративных LMS.
Заказчик с LMS, принимающей только 1.2, получает от нас пакет, который его платформа не запустит.
Трек заведён 2026-08-03 по итогам технического разбора: продуктового решения о запуске НЕТ —
нужен заказчик с такой LMS, а профиль 1.2 ДЕГРАДИРОВАННЫЙ по объективным причинам стандарта, не
по недоработке. Дизайн — [PRD-37](./prd-37/scorm12-export-profile.md) (Этап 36).

**Условие входа:** нереализуемо без BR-30 — предел `cmi.suspend_data` в SCORM 1.2 (4096 символов)
на порядок меньше того, что сегодня туда пишется.

**Требования:**

| ID | Требование |
| --- | --- |
| BR-31-01 | Профиль экспорта выбирается автором при выгрузке пакета; профиль 2004 остаётся умолчанием, пакеты 2004 не меняются после появления профиля 1.2 |
| BR-31-02 | Точка ветвления профиля — ОДНА: адаптер среды выполнения (`API`/`LMS*`-методы вместо `API_1484_11`/`Initialize`) и построитель манифеста (схема 1.2, без элементов упорядочивания `imsss` и без скрытия навигации LMS). Сборка данных экспорта, отрисовка, оценивание, шаблоны и телеметрия НЕ ветвятся |
| BR-31-03 | Перекладка модели данных на словарь 1.2 (баллы, статусы, типы взаимодействий, форматы длительности, поля CMI) выполняется по зафиксированной таблице соответствия и покрывается тестами пословарно; значения вне словаря 1.2 не отправляются |
| BR-31-04 | Автор предупреждается о перечне УТРАЧИВАЕМЫХ при выборе профиля 1.2 возможностей ДО выгрузки: раздельные вердикт/завершение схлопываются в один статус, пороги по темам и по вариантам (BR-12-07/PRD-24) не выражаются манифестом, навигация LMS не скрывается, водяной знак (BR-29) без `learner_id` теряет идентификатор, длинные ответы усекаются до 255 символов |
| BR-31-05 | Отладочный плеер (BR-13) и локальные средства приёмки воспроизводят выбранный профиль, включая заглушку среды выполнения 1.2 — иначе профиль остаётся непроверяемым до реальной LMS |
| BR-31-06 | Заявление о поддержке SCORM 1.2 не выходит за перечень утрат из BR-31-04 ни в материалах, ни в UI — тот же принцип объявленного профиля, что и BR-27-03/NFR-24 для QTI |

### Расширения ранее принятых требований (без новых BR)

Часть треков не заводит новых бизнес-требований, а уточняет уже принятые:

| Трек | Что уточняет |
| --- | --- |
| [PRD-19](./prd-19/skip-return-navigation.md) (Этап 16) | BR-06: пропуск вопроса и возврат к нему в пределах попытки, разрешение/запрет смены данного ответа, поэтапное завершение по разделам, системные узлы границ раздела |
| [PRD-22](./prd-22/page-fields-and-sequences.md) (Этап 17) | BR-01-06 и BR-02: состав полей страницы задаёт ИСКЛЮЧИТЕЛЬНО манифест (содержимое + свойства), типы полей закрыты и валидируются при загрузке И активации; последовательности страниц вычисляет ядро |
| [PRD-23](./prd-23/template-themes.md) (Этап 18) | BR-01-06: шаблон объявляет темы оформления, тест хранит вид и цвета по темам; переопределения печатаются последними в каскаде, шаблон без тем работает как прежде |
| [PRD-24](./prd-24/variant-pass-thresholds.md) (Этап 19) | BR-12-07: правило прохождения темы получает источник «по вариантам» — свой порог на каждый вариант, применяемый к выданному составу. Пер-вариантная калибровка, которую BR-12-07 оставлял за скобками, вводится порогами, но не коэффициентами |
| [PRD-38](./prd-38/question-media-placement.md) (Этап 37) | BR-01/BR-02: медиа задания перенесено ВНУТРЬ блока вопроса на обоих хостах (было — отдельная широкая колонка, теснившая варианты ответа); добавлены полноэкранный просмотр изображения и оформленный аудио-плеер, наследующий палитру сцены |
| [PRD-39](./prd-39/matching-connector-merge.md) (Этап 38) | BR-01: раскладка вопроса «сопоставление» получает режим слияния карточек в одну панель вместо пары со стрелкой-коннектором; один общий рендерер (PRD-12), паритет на обоих поставляемых шаблонах |
| [PRD-40](./prd-40/cooldown-by-outcome.md) (Этап 39) | BR-07/BR-19: барьер A (общий период охлаждения) получает раздельные значения для исхода «пройдено» и «не пройдено» — независимый переключатель, по умолчанию выключен; исход читается общим ядром на обоих хостах |
| [PRD-41](./prd-41/moodle-cooldown-plugin.md) (Этап 40, предложен, не начат) | BR-07: контракт eligibility-плагина и рантайм retake gate (PRD-6) без изменений получают второго провайдера — `moodle_cooldown`, для курсов в Moodle (у WebTutor свой провайдер — `webtutor_cooldown`) |
| [PRD-43](./prd-43/quick-advance-toggle.md) (Этап 41) | BR-06/PRD-19: быстрый переход к следующему вопросу выделен из-под переключателя «разрешить возврат к неотвеченным» в независимую настройку `quickAdvance` — все 4 комбинации переключателей работоспособны |
| [PRD-45](./prd-45/scale-levels-editor.md) (Этап 42) | BR-17/BR-06: уровни шкалы задаются рядом «Начало — пороги — Конец» вместо пар границ, поэтому перекрытия и разрывы становятся непредставимыми ПО ПОСТРОЕНИЮ, а не отлавливаются валидацией; код уровня — его идентичность, метка необязательна |
| [PRD-46](./prd-46/scales-chart-choice.md) (Этап 43) | BR-24: у диаграммы профиля появляется второй вид — роза для ипсативных методик, где сумма по шкалам не зависит от ответов и «доля от целого» существует как величина; правило выбора вида, предел оси радара, цвет и пиктограмма шкалы от автора |
| [PRD-47](./prd-47/report-scales-chart.md) (Этап 44) | BR-16/BR-24: PDF-отчёт печатает блок измерений и диаграмму так же, как экран итогов (до трека пакет не получал измерений ВОВСЕ), облик шкал приходит с экрана, у отчёта свой переключатель вида; настройки отчёта переехали на вкладку «Оформление» |
| [PRD-48](./prd-48/test-transfer.md) (Этап 45) | BR-11: обмен расширяется с банка вопросов на ТЕСТ целиком. Книга Excel остаётся форматом содержания и становится полной по листам; полный обратимый перенос между инсталляциями делает отдельный формат `.tbtest`, который сериализует строки таблиц целиком и потому не теряет поле, о котором не знал |
| [PRD-49](./prd-49/results-headings.md) (Этап 46) | BR-16/BR-17/BR-01-06: заголовки блоков итогов перестают быть зашитыми в макет — автор переформулирует и гасит их на всех ученических поверхностях (экран, адаптивные итоги, итоги раздела, PDF); слоты карточки показателя выключаются независимо |
| [PRD-50](./prd-50/result-breakdowns.md) (Этап 47) | BR-17/BR-12-07: результат получает РАЗРЕЗЫ — подытоги по ключам внутри темы и раздела со своими порогами, вердикт темы с учётом ключей, свои подписи вердикта. Повод — разбор реального сертификационного теста, где подытоги считались вручную вне системы |

**Реализуется без BR и без номера этапа — открытый вопрос BRD:**
[PRD-44](./prd-44/scale-allocation-question-type.md) вводит новый тип вопроса «распределение
баллов между шкалами» (constant-sum/ipsative, референс — форс-чойс методики вроде Шварца): один
ответ измеряет НЕСКОЛЬКО шкал с зависимыми весами, что не покрывается ни типом «шкала» (BR-15),
ни моделью вклада BR-06-10/12 (вклад в шкалу там — фиксированное число автора, а не значение,
введённое учащимся). Идея зафиксирована владельцем продукта 2026-08-05, **требования согласованы
2026-08-06**, к 2026-08-07 закрыты фазы 0-6 (ядро и вклад в шкалы, эскиз, DS-компонент, экран
учащегося на обоих хостах, редактор, книга Excel, ведущая и слабая шкала) и приёмки A-02 - A-05;
остаток — фаза 7 и порог покрытия.

Правило BRD «трек не получает номера этапа, пока решения не приняты» при этом сработало наоборот:
решения приняты, реализация идёт, а требование в документе так и не заведено. Нужен ли PRD-44
собственный BR (по образцу BR-15 для типа «шкала») или он остаётся расширением BR-06 — решение
владельца продукта, и до него трек живёт вне нумерации этапов. Пока это единственное место, где
код опережает BRD.

---

## 5. Нефункциональные требования

| ID | Категория | Требование |
| --- | --- | --- |
| NFR-01 | Совместимость | Тесты без `design_settings_json` экспортируются с `default` |
| NFR-02 | Производительность | Генерация пакета с 10 контентными страницами и 5 пользовательскими показателями не превышает 30 с |
| NFR-03 | Размер пакета | Медиа и ресурсы контролируются лимитами загрузки; внешние URL запрещены |
| NFR-04 | LMS | Используются стандартные CMI-поля SCORM 2004 |
| NFR-05 | Безопасность | HTML контентных страниц санитизируется сервером и вставляется только через контролируемые слоты |
| NFR-06 | Шаблоны | Сервер не исполняет код шаблона |
| NFR-07 | Валидация | Шаблон проходит структурную валидацию и браузерную smoke-проверку до активации |
| NFR-08 | Диагностика | Ошибки шаблона и формул пишутся в консоль, telemetry и/или `suspend_data`, где возможно |
| NFR-09 | Поддерживаемость | Совместимость версий контролируется через `templateApiVersion` |
| NFR-10 | Совместимость flow | Тесты без `flowPolicy`, шкал и контентных страниц сохраняют старый плоский порядок и расчёт |
| NFR-11 | Детерминизм | Выборка вопросов, перемешивание, переходы и расчёт шкал воспроизводимы в рамках попытки и восстановления сессии |
| NFR-12 | Поддерживаемость retake gate | LMS-specific eligibility plugins версионируются, имеют явную конфигурацию и могут обновляться без изменения общего Core |
| NFR-13 | Отказоустойчивость retake gate | Ошибка plugin не должна ломать загрузку пакета; поведение определяется политикой `failOpen`/`failClosed` и сопровождается понятным сообщением/диагностикой |
| NFR-14 | UI-процесс | UI-разработка начинается только после подготовки и явного согласования wireframes для основных, пустых, loading/saving, ошибочных и read-only состояний |
| NFR-15 | Поддерживаемость редактора | Авторский редактор теста декомпозирован на доменные модули с typed DTO, чистыми мапперами и тестируемыми правилами валидации |
| NFR-16 | UX целостность | Основные и расширенные настройки теста доступны из единого редакторского сценария; карточка теста не должна становиться контейнером для растущего набора независимых кнопок |
| NFR-17 | Граница хоста (cross-attempt) | WebTutor не предоставляет SCORM-пакету cross-attempt-хранилища (`adl.data` недоступен, `cmi.suspend_data` живёт только within-attempt). Механики, требующие памяти между попытками (ротация вариантов BR-12, anti-repeat), реализуются в серверном хосте; в автономном пакете — деградация до stateless-поведения с явной диагностикой |
| NFR-18 | Точность плеера отладки | Встроенный плеер собирает пакет тем же пайплайном, что production-экспорт (общий `generateScormPackage` и общий сборщик `ExportData`, применённый шаблон оформления теста), но ВСЕГДА из живого состояния теста; расхождение с реальной LMS ограничено отсутствием SN-секвенсирования и cross-attempt-стора и явно документируется (BR-13) |
| NFR-19 | Изоляция отладки | Прогоны плеера не порождают записей в `attempts`, `scorm_packages`, `scorm_attempts`, `scorm_answers` и не влияют на аналитику; сборка отладочного пакета не активирует телеметрию (BR-13) |
| NFR-20 | Один источник выбора варианта | Вид отчёта и вид экрана разрешаются ОДНОЙ общей функцией для всех хостов и для предпросмотра: расхождение выданного документа с тем, что видел автор, недопустимо (BR-16) |
| NFR-21 | Неизменность выданного | Артефакты по попытке, выданной из опубликованного снапшота (экраны, отчёт, применённые пороги), собираются данными И вариантом ИЗ ВЫДАЧИ; позднейшие правки автора не меняют их задним числом (BR-16, PRD-15) |
| NFR-22 | Доверенное время | Решения о допуске (период охлаждения, интервал между попытками) принимаются по доверенному источнику времени, а не по системным часам клиента (BR-19) |
| NFR-23 | Одна модель допуска | Барьеры допуска вычисляет общий движок; хост поставляет ему только факты и не содержит собственной копии правила — иначе один переключатель снова начнёт значить на хостах разное (BR-19) |
| NFR-24 | Границы соответствия обмена | Заявляемое соответствие внешнему стандарту ограничено ОБЪЯВЛЕННЫМ профилем: редакции, взаимодействия и правила обработки ответа перечислены; выпуск проверяется схемой или валидатором стандарта, приём — реальными файлами сторонних систем. Формулировки в материалах и в UI за профиль не выходят (BR-27) |

---

## 6. Модель данных высокого уровня

| Сущность | Назначение |
| --- | --- |
| `tests.design_settings_json` | Выбранный `templateId`, `templateVersion`, `templateApiVersion`, `params` |
| `templates` | Реестр встроенных/загруженных шаблонов, манифест, метаданные источника, статус |
| `content_pages` | Контентные страницы теста в потоке страниц |
| `result_variables` | Пользовательские показатели результата |
| `questions.tags` | Теги вопросов для агрегатов пользовательских показателей |
| `tests.flow_policy_json` | Политика прохождения (сценарий): legacy-плоский режим, секционный поток, адаптивный секционный режим |
| `section_results` / runtime state | Промежуточные результаты и состояние разделов внутри попытки |
| `scales` | Шкалы и компетенции, которые измеряются вопросами |
| `question_measurements` | Вклад вопроса или варианта ответа в одну или несколько шкал |
| `scale_results` | Рассчитанные значения шкал, публикуемые в `scale.*` и передаваемые в LMS при настройке автора |
| `test_question_scoring` («Цена ответа») | Способ начисления балла за ответ по паре (тест, вопрос): «точное совпадение» по умолчанию либо частичный балл (веса опций / ступенчатая таблица) по типу вопроса (BR-09). **Оценка — свойство ТЕСТА, а не вопроса:** колонки `questions.points`/`questions.scoring_json` удалены (PRD-15, блок D); эффективное значение резолвится «переопределение → умолчание раздела → умолчание теста → системное» |
| `test_sections.draw_blueprint_json` | Опциональные квоты выдачи вопросов по тегам (подтемам) на тему (BR-10) |
| `test_sections.form_set_json` | Опциональный набор вариантов выдачи на раздел: список именованных вариантов, каждый — набор `questionIds` (подмножество банка темы). Выпавший вариант выдаётся ЦЕЛИКОМ; в режиме вариантов `draw_count`/`draw_all`/`draw_blueprint_json` не применяются. Отсутствие = выдача из всего банка по BR-10 (BR-12) |
| `attempts.variant_json[].formId` | Стабильный id выпавшего варианта на тему в попытке: пин выданного состава (в случайном порядке предъявления) и источник истории для ротации на следующем повторе (BR-12) |
| `tests.status` | Целевой статус теста: `draft` / `published` / `archived`; заменяет legacy-поле `tests.published` |
| `tests.retake_policy_json` | Политика повторного прохождения конкретного теста: период охлаждения, режим gate, выбранный `eligibilityPlugin`, policy on error, страница блокировки |
| `eligibility_plugins` | Реестр plugins, которые вычисляют допуск к старту попытки по контракту `context -> allowed true/false` |
| `eligibility_plugin_configs` | Администрируемые настройки LMS-specific plugins: endpoint'ы, параметры, фильтры попыток, парсинг дат, версия адаптера |
| `test_editor_draft` / frontend state | Черновик редактора теста: нормализованное представление формы до сохранения, включая выбранные темы, режим, правила, адаптивные уровни и настройки UI |
| Контракт файла импорта/экспорта вопросов | Колонки и форматы Excel для банка вопросов; единый источник истины — [спецификация формата](./questions-import/format.md) (BR-11) |
| `media_assets` (отдельный будущий PRD «Медиатека») | Реестр медиа: исходное имя, mime, тип, размер, checksum, автор, дата; человекочитаемый дескриптор и дедупликация для импорта медиа. Вне BR-11 (BR-11-06) |
| Встроенный плеер отладки (без новых сущностей) | Отладочный SCORM-пакет собирается на лету В ПАМЯТЬ из ЖИВОГО состояния теста (применённый шаблон оформления) и отдаётся встроенному плееру по одноразовому токену сессии; состояние прогона хранится в `localStorage` клиента, не в БД. Gate = edit-scope экспорта; телеметрия отключена (BR-13) |
| `content_pages.settings_json` | Значения полей контентной страницы, объявленных манифестом шаблона в разделе свойств; состав полей задаёт ТОЛЬКО манифест (PRD-22) |
| `tests.design_settings_json` (темы и цвета) | Выбранный вид оформления (`light`/`dark`/`auto`) и цвета по темам; шаблон объявляет доступные темы в манифесте (PRD-23) |
| `tests.report_settings_json` | Выбранный вариант отчёта и значения его полей; вариант запекается в пакет вместе со стилем (BR-16) |
| `scales.config_json`, `result_variables.config_json` | Толкования измерений: интервалы для чисел, перечень исходов с кодами для строк, достижимый домен шкалы, благоприятное направление (BR-17) |
| `scales.learner_visibility`, `result_variables.learner_visibility` | Трёхпозиционная видимость измерения обучающемуся вместо булева флага показа (BR-17) |
| `questions.order_index`, `test_sections.question_order` | Индекс вопроса в теме и режим порядка выдачи раздела (случайный / фиксированный); в контентный хеш вопроса индекс не входит (BR-18) |
| `attempts.assignment_id` | Связь попытки с назначением — техническая основа «назначение = единица допуска»: и период охлаждения, и счётчик попыток считаются в границах назначения (BR-19) |
| Контракт обмена по IMS QTI | Профиль соответствия: принимаемые и выпускаемые редакции стандарта, отображение наших типов вопросов на взаимодействия и обратно, поддерживаемые правила обработки ответа, перенос метаданных и перечень заведомо теряемых свойств. Ведётся синхронно с контрактом книги Excel и НЕ заменяет его: внутренний обмен без потерь остаётся за BR-11 (BR-27) |
| Материалы обратной связи (`title`/`url`) | Внешние ссылки, приложенные к обратной связи теста, раздела, темы, диапазона шкалы и исхода показателя; не хранятся системой и не входят в SCORM ZIP как ресурс — обычный `http(s)`-URL, единый для веба, пакета и PDF-отчёта (BR-20). Легаси-дескрипторы первого решения (`url: /api/media/<id>`) остаются нередактируемым техдолгом до ручной замены автором |
| `tests.copyProtection` / `protectionWatermark` / `protectionHideOnBlur` | Три независимых булевых поля защиты текста задания от копирования (умолчание базовой защиты — включено, у двух прочих — выключено); попадают в снимок публикации без отдельной обработки (BR-29) |
| `cmi.suspend_data` (модель состояния прогона, версия `v2`) | Счётчик попыток, сводки лучшей и последней попытки, прогон в работе, якорь таймера и даты барьеров; содержимого вопросов не несёт, вопрос адресуется позицией в `TEST_DATA`; версионируется, старый формат приводится к новому при первом запуске (BR-30) |

---

## 7. Приоритизация этапов

**Внеочередной приоритет: Этапы 27 и 28.** Номер этапа фиксирует порядок ЗАВЕДЕНИЯ требования, а не
очередь исполнения. Этапы 27 (единый вход) и 28 (согласие на обработку данных и срок хранения)
выполняются ПЕРЕД остальными незакрытыми этапами независимо от своих номеров. Основание —
[конкурентный анализ](../reports/ANALYSIS_COMPETITORS.md) 2026-08-01: оба относятся к отсекающим
критериям корпоративной закупки, то есть их отсутствие снимает продукт с рассмотрения раньше, чем
дело дойдёт до сравнения по существу, и тем самым обесценивает результат всех остальных этапов.
Развёрнутое обоснование и порядок вложений — в
[направлениях развития](../reports/ANALYSIS_COMPETITIVE_DIRECTIONS.md).

| Этап | Состав | Основание |
| --- | --- | --- |
| Этап 1 | Платформа макетов шаблонов + выбор шаблона автором | Базовый контракт, браузерный рендерер, оболочка/макеты страниц, параметры, Runtime API MVP, встроенные шаблоны |
| Этап 2 | Контентные страницы | Использует поток страниц и контракт макетов |
| ~~Этап 3~~ | Снят: логика теста задаётся сценарием (см. Этап 4) | Движок правил выведен из охвата 2026-07-05 (продукт — тесты, не курсы) |
| Этап 4 | Гибкий поток прохождения и результаты разделов | Сохраняет legacy-плоский режим и добавляет секционный flow для Storyline-подобных сценариев |
| Этап 5 | Ограничение повторного прохождения и retake gate | Нужен для курсов с периодом охлаждения; выполняется до внутреннего старта попытки и должен быть администрируемым из-за хрупкости LMS-specific интеграции |
| Этап 6 | Многомерные шкалы и пользовательские показатели результата | Использует результаты Core, результаты разделов, `scale.*`, `result.*`, экспорт в LMS |
| Этап 7 | Администрирование жизненного цикла шаблонов | Загружаемые шаблоны, валидация, smoke-проверка, реестр, живой предпросмотр, активация/деактивация/обновление/экспорт |
| Этап 8 | Расширенные шаблонные интерактивы | Переопределение механики вопросов через `template.js` |
| Этап 9 | Рефакторинг редактора параметров теста | Единый редактор и доменная декомпозиция нужны до дальнейшего наращивания `flowPolicy`, `retakePolicy`, шкал и внешних шаблонов |
| Этап 10 | Градуированная оценка ответа («Цена ответа») | Балл `s ∈ [0, sMax]`/`scoreRatio` для `multiple`/`matching`/`ranking` по способу (веса опций / ступенчатая таблица); ось корректности отдельно от шкал; активирует поля `scoreRatio`/`partial` (PRD-10) |
| Этап 11 | Квоты выдачи вопросов по тегам | Опциональная стратифицированная выдача по тегам-подтемам на тему; гарантирует объём подтемы; доставка, ортогональна скорингу (PRD-11) |
| Этап 12 | Импорт и экспорт банка вопросов | Обратимый Excel-обмен с паритетом по полям вопроса; фазами: исправление round-trip → теги/ОС/«Цена ответа» → UX → мультилист с шкалами/показателями (PRD-14) |
| Этап 13 | Фиксированные варианты выдачи (формы) | Курируемые автором варианты-наборы на раздел, случайный выбор варианта при старте, ротация на повторе в серверном хосте (деградация в SCORM); доставка, ортогональна скорингу (PRD-17) |
| Этап 14 | Встроенный плеер тестирования и отладки тестов | Перенос dev-плеера/инспектора в сервис: сборка реального пакета на лету, прогон с RTE-шимом, инспектор LMS/баллов/шкал/показателей/выдачи, одноразовость прогона, выбор версии (черновик/снапшот), общий с CLI источник шима/инспектора (PRD-18) |
| Этап 15 | Устойчивость таймера и возобновление timed-тестов | Монотонный отсчёт без выигрыша от фоновой вкладки и перевода часов, кросс-reload на модели активного времени, kill-устойчивость, tamper-evidence; деградация к прежнему поведению при недоступности источника (BR-14 / PRD-20) |
| Этап 16 | Навигация прохождения и поэтапное завершение | Пропуск и возврат к вопросу, разрешение смены ответа, промежуточные итоги раздела, обзорный экран, системные узлы границ раздела (PRD-19, уточняет BR-06) |
| Этап 17 | Поля контентной страницы и последовательности | Состав полей страницы задаёт только манифест, типы закрыты и валидируются при загрузке И активации; последовательности вычисляет ядро (PRD-22, уточняет BR-01-06 / BR-02) |
| Этап 18 | Темы шаблона и раздел «Цвета» | Шаблон объявляет темы оформления, тест хранит вид и цвета по темам, переопределения печатаются последними в каскаде; шаблон без тем работает как прежде (PRD-23, уточняет BR-01-06) |
| Этап 19 | Пороги прохождения по вариантам | Свой порог на каждый фиксированный вариант выдачи, применяемый к выданному составу; вся логика заперта в общем резолвере правила темы (PRD-24, уточняет BR-12-07) |
| Этап 20 | Тип вопроса «шкала» | Утверждение и упорядоченные градации; измерительный режим без правильного ответа и проверяемый режим как одиночный выбор; один источник разметки на оба хоста (BR-15 / PRD-26) |
| Этап 21 | Отчёт о результатах как часть шаблона | Вывод вёрстки отчёта из кода в модель шаблона: типы и варианты отчёта, редактируемые поля, один резолвер варианта, запекание макета и стиля в пакет, сборка по снапшоту из выдачи (BR-16 / PRD-27) |
| Этап 22 | Итоги измерительного теста | Показ шкал и показателей обучающемуся: общая модель толкования, домен и благоприятное направление, трёхпозиционная видимость, визуал в параметрах варианта, единый блок рекомендаций (BR-17 / PRD-29) |
| Этап 23 | Авторский порядок выдачи вопросов | Опциональный индекс вопроса в теме и режим порядка на разделе; влияет только на последовательность, не на отбор и квоты (BR-18 / PRD-30) |
| Этап 24 | Интервал между попытками и общая модель допуска | Назначение как единица допуска, барьер «интервал между попытками» внутри назначения поверх общего периода охлаждения, общий движок решения на оба хоста (BR-19 / PRD-31) |
| Этап 25 | Материалы обратной связи (внешняя ссылка) | Первый проход — загрузка PDF в медиатеку (PRD-32, реализован и принят 2026-08-02); пересмотр — файл заменён внешней ссылкой {название, url} по образцу «Курсы»/«Мероприятия», трек закрыт (PRD-42, реализован и влит 2026-08-05) (BR-20) |
| Этап 26 | Оценка целесообразности психодиагностического охвата и управленческой отчётности | Не реализация, а решения: по каждой позиции перечня BR-21 — ввести требования, отклонить или оставить обходную дорогу. Выполняется до любых обещаний вовне; PRD не заводится, пока решения не приняты (BR-21) |
| **Этап 27** | **Единый вход по корпоративной учётной записи (SSO)** — ВНЕОЧЕРЕДНОЙ | Отсекающий критерий корпоративной закупки: вход только паролем и по ссылке-приглашению означает второй контур учётных данных, который служба безопасности заказчика согласует отдельно. Дополнительно закрывает технический долг пересоздания сессии при смене уровня доступа — обе точки входа правятся одновременно (BR-22) |
| **Этап 28** | **Согласие на обработку персональных данных и срок хранения** — ВНЕОЧЕРЕДНОЙ | Отсекающий критерий и снятие технического долга: экрана согласия не существует, признак зашит в код, срок хранения не определён. Разблокирует внешнего участника (PRD-28), для которого согласие — единственное основание обработки и который экран первого входа не увидит никогда (BR-23) |
| Этап 29 | Радар компетенций | Профиль по нескольким шкалам одной диаграммой на итогах завершённой попытки и в отчёте: луч как доля от диапазона шкалы без переворота по направлению, оценка цветом, огрубление луча при скрытом балле, показ только явным решением автора (BR-24 / PRD-35) |
| Этап 30 | Адаптивное тестирование по модели IRT — **ПОСЛЕ психометрики** | Зависимость по данным, а не приоритет: адаптивная выдача невозможна на банке, где сложность назначена автором вручную. Последовательность — CTT ([BRD психометрики](./brd-psychometric-analytics.md)) -> IRT -> адаптивная выдача. Существующий уровневый режим сохраняется как обучающий и не заменяется; попутно закрывается расхождение названия и содержания (BR-25) |
| Этап 31 | Управление экспозицией заданий — **после накопления данных** | Экспозиция не измеряется, банк вырабатывается незаметно для автора. Общий счётчик по популяции в автономном пакете невозможен, поэтому управление переносится на сборку: показатель экспозиции запекается в пакет, рантайм учитывает его при выдаче внутри квот. Данные копятся из веб-прохождений, телеметрии и импорта выгрузок из WebTutor — одним потоком с психометрикой. Предупреждение об ожидаемой экспозиции и равномерное распределение вариантов данных не требуют и выполнимы раньше (BR-26) |
| Этап 32 | Импорт и экспорт по IMS QTI — **данных не требует** | Единственный формат обмена содержанием — книга Excel собственного формата: банк заказчика переносится вручную, а свой вынимается только в наш формат. Закрывает вход (забрать накопленный банк) и выход (отсутствие замка) и снимает единственную позицию сравнения, где встроенный конструктор WebTutor объективно сильнее. Первая очередь — задания и банк, без обмена сценарием теста; профиль соответствия объявляется явно, приём принимается на реальных файлах сторонних систем, выпуск — валидатором стандарта. В отличие от Этапов 30-31 не зависит ни от психометрики, ни от накопленных наблюдений (BR-27) |
| Этап 33 | Листинг кода в тексте задания | Автор не может показать участнику фрагмент исходного кода без потери отступов и с испорченной типографикой; повод вскрыт конкурентным анализом. Требования не собирались, форма решения не согласована (BR-28 / PRD-33) |
| Этап 34 | Защита текста задания от копирования | Периметр защиты, водяной знак атрибуции, скрытие при потере фокуса; постановка задачи — цена копирования и атрибуция утечки, а не запрет, который недостижим технически. Требования согласованы, код не начат (BR-29 / PRD-34) |
| Этап 35 | Компактность состояния прогона в SCORM-пакете | Состояние прогона молча теряется при переполнении `cmi.suspend_data`: содержимое вопросов дублируется в истории попыток без предела роста. Вопрос адресуется позицией, история сжимается до счётчика и двух сводок. Условие входа для Этапа 36 (BR-30 / PRD-36) |
| Этап 36 | Профиль экспорта SCORM 1.2 — **нереализуем без Этапа 35** | Второй, ДЕГРАДИРОВАННЫЙ профиль экспорта для LMS, не принимающих 2004; продуктового решения о запуске нет, нужен заказчик с такой LMS (BR-31 / PRD-37) |
| Этап 37 | Медиа вопроса внутри блока вопроса | Слот медиа перенесён из тесной боковой колонки внутрь блока вопроса на обоих хостах, добавлен полноэкранный просмотр и оформленный аудио-плеер (уточняет BR-01/BR-02, PRD-38) |
| Этап 38 | Слияние карточек сопоставления | Режим слияния вместо стрелки-коннектора в вопросе «сопоставление», один общий рендерер (уточняет BR-01, PRD-39) |
| Этап 39 | Период охлаждения по исходу попытки | Барьер A получает раздельные периоды для исходов «пройдено»/«не пройдено» (уточняет BR-07/BR-19, PRD-40) |
| Этап 40 | Moodle cooldown plugin — предложен, не начат | Второй провайдер eligibility-плагина retake gate для курсов в Moodle, контракт PRD-6 без изменений (уточняет BR-07, PRD-41) |
| Этап 41 | Быстрый переход к следующему вопросу | Отдельный переключатель `quickAdvance`, вынесенный из-под «разрешить возврат к неотвеченным» (уточняет BR-06, PRD-43) |
| Этап 42 | Редактор уровней шкалы — пороги вместо пар границ | Перекрытия и разрывы уровней становятся непредставимыми по построению, а не отлавливаются валидацией; лента покрытия показывает тон по направлению шкалы (уточняет BR-17/BR-06, PRD-45) |
| Этап 43 | Выбор диаграммы профиля: радар или роза | Второй вид диаграммы для ипсативных методик, где сумма по шкалам не зависит от ответов; правило выбора вида, предел оси, облик шкалы от автора — одной точкой на оба хоста (уточняет BR-24, PRD-46) |
| Этап 44 | Диаграмма профиля в PDF-отчёте | Отчёт печатает измерения и диаграмму одинаково в вебе и в пакете — до трека пакет не получал измерений вовсе; настройки отчёта переехали на вкладку «Оформление» (уточняет BR-16/BR-24, PRD-47) |
| Этап 45 | Перенос теста между инсталляциями без потерь | Книга Excel стала полной по листам и осталась форматом СОДЕРЖАНИЯ; полный обратимый перенос выполняет формат `.tbtest`, сериализующий строки таблиц целиком — новая колонка едет потому, что она часть строки (уточняет BR-11, PRD-48) |
| Этап 46 | Настраиваемые заголовки блоков итогов | Заголовки выведены из макета в настройку теста и действуют на всех ученических поверхностях, включая PDF; слоты карточки показателя выключаются независимо, что снимает дублирование подписи об одном и том же (уточняет BR-16/BR-17, PRD-49) |
| Этап 47 | Разрезы результата | Подытоги по ключам внутри темы и раздела со своими порогами, вердикт темы с учётом ключей, свои подписи вердикта; повод — сертификационный тест, где подытоги считались вручную вне системы (уточняет BR-17/BR-12-07, PRD-50) |

---

## 8. Критерии приёмки верхнего уровня

Ниже — критерии этапов, для которых они формулировались на уровне BRD. Для Этапов 11, 13,
15-25, 27-31 и 33-41 приёмочные критерии ведутся в соответствующих PRD и их планах, чтобы не
расходиться с кодом в двух местах; сводный статус — в [ROADMAP §0](../ROADMAP.md). Для Этапов
27, 28, 30 и 31 PRD ещё не заведены — критерии появятся вместе с ними.

### Этап 1

- [ ] Wireframes для UI этапа подготовлены и явно согласованы до frontend-разработки
- [ ] Тест без настроек дизайна экспортируется с `default`
- [ ] Выбранный встроенный шаблон попадает в ZIP как единственный шаблон
- [ ] Core в браузере загружает `manifest.json`, `shell.html`, макет текущей страницы и ресурсы
- [ ] `progress.mode` управляет `progress.active`
- [ ] Оболочка и действия работают через `data-slot`, `data-nav`, `data-action`

### Этап 2

- [ ] Wireframes для UI этапа подготовлены и явно согласованы до frontend-разработки
- [ ] `intro` отображается перед вопросами темы
- [ ] `info` с изображением отображается и использует локальный ресурс
- [ ] `content-body` получает только санитизированный HTML
- [ ] Контентные страницы не влияют на оценку вопросов

### Этап 3 — снят

Движок правил выведен из охвата (2026-07-05, см. BR-03). Логика теста задаётся сценарием —
критерии приёмки см. в Этапе 4.

### Этап 4

- [ ] Wireframes для UI этапа подготовлены и явно согласованы до frontend-разработки
- [ ] Тест без `flowPolicy` проходит в режиме `legacy_flat` и даёт тот же порядок/расчёт, что текущий SCORM runtime
- [ ] В `section_linear` вопросы одного раздела не перемешиваются с вопросами другого раздела
- [ ] После завершения раздела Core рассчитывает `sectionResult` и показывает `content.summary`
- [ ] Правило на `section:resultCalculated` переводит обучающегося в следующий раздел или на заданную страницу
- [ ] Восстановление сессии возвращает обучающегося в тот же раздел, страницу и вопрос

### Этап 5

- [ ] Wireframes для UI этапа подготовлены и явно согласованы до frontend-разработки
- [ ] Администратор включает `retakePolicy` и задаёт период охлаждения в календарных днях
- [ ] Автор выбирает eligibility plugin из активного набора plugins для конкретного теста
- [ ] Если plugin не выбран, Core возвращает `allowed = true` и не блокирует старт
- [ ] При активном cooldown обучающийся видит `block-wall` до внутренней кнопки старта курса
- [ ] `block-wall` показывает дату доступного повторного прохождения из `availableDate`, если plugin её вернул
- [ ] В blocked-сценарии Core не вызывает SCORM `Initialize` и не пишет `cmi.*`
- [ ] WebTutor plugin получает список записей курса и выбирает последнюю полноценную попытку по настраиваемому фильтру
- [ ] Администратор может изменить endpoint'ы, параметры запроса, фильтр попыток
  и правила парсинга даты без правки общего Core
- [ ] Ошибка plugin обрабатывается согласно `failOpen`/`failClosed` и видна в диагностике

### Этап 6

- [ ] Wireframes для UI этапа подготовлены и явно согласованы до frontend-разработки
- [ ] Вопрос может иметь вклад минимум в две шкалы одновременно
- [ ] Вклад шкалы может зависеть от выбранного варианта ответа
- [ ] `scale.*` рассчитывается отдельно от стандартного балла и доступен формулам пользовательских показателей
- [ ] Пользовательский показатель вычисляется и публикуется в `result.*`
- [ ] Макет результатов показывает переменные, разрешённые для обучающегося
- [ ] LMS получает значения через `suspend_data` и/или `interactions`

### Этап 7

- [ ] Wireframes для UI этапа подготовлены и явно согласованы до frontend-разработки
- [ ] Корректный ZIP проходит структурную валидацию и браузерную smoke-проверку
- [ ] ZIP с отсутствующим обязательным слотом отклоняется
- [ ] ZIP с внешним CDN/URL отклоняется
- [ ] Живой предпросмотр на демонстрационном курсе показывает шаблон до активации

### Этап 9

- [ ] Wireframes единого редактора теста подготовлены и явно согласованы до frontend-разработки
- [ ] Создание и редактирование стандартного теста проходят через один редактор и один typed payload
- [ ] Создание и редактирование адаптивного теста проходят через тот же контейнер редактора без потери адаптивных настроек
- [ ] Переключение standard/adaptive показывает последствия и не удаляет сохранённые настройки без подтверждения
- [ ] Валидация блокирует некорректные draw count, pass threshold, диапазоны сложности и пустые обязательные поля до API-запроса
- [ ] Список тестов содержит компактный набор действий; расширенные настройки
  доступны через редактор/меню, а не через разрастающуюся строку кнопок
- [ ] Сохранение теста и связанных секций/адаптивных уровней выполняется атомарно
  или возвращает согласованную ошибку без частичных данных

### Этап 10

- [ ] Wireframes для UI этапа подготовлены и явно согласованы до frontend-разработки
- [ ] Автор включает градуированную оценку для вопроса `multiple`/`matching`/`ranking`
- [ ] Частично верный ответ даёт `scoreRatio` в `(0, 1)`; полностью верный — `1`, полностью неверный — `0`
- [ ] Вопрос без включённой градуированной оценки сохраняет exact-match (`0`/`1`)
- [ ] `scoreRatio` и статус `partial` передаются в шаблон, показ обучающемуся и LMS
- [ ] Вклады в шкалы не зависят от градуированного балла (остаются явными числами)

### Этап 12

- [ ] Экспорт вопроса любого типа при импорте воспроизводит исходный вопрос без потерь (round-trip)
- [ ] `matching` поддерживает дистракторы и непозиционные пары; `ranking` — явный правильный порядок
- [ ] Импорт/экспорт покрывают теги, условную обратную связь и «Цену ответа»
- [ ] Файл прежнего формата импортируется без изменения поведения
- [ ] Невалидная строка не прерывает импорт; причина видна автору построчно
- [ ] Контракт файла зафиксирован в спецификации формата и синхронен с кодом

### Этап 14

- [x] Wireframes плеера отладки подготовлены и явно согласованы до frontend-разработки (СОГЛАСОВАНЫ 2026-06-27)
- [x] Держатель edit-доступа запускает плеер из редактора теста без скачивания ZIP и без CLI
  (пункт меню «Тестовый прогон» → отдельное окно `/author/tests/:id/debug`; 403 без edit → «недоступно»)
- [x] Плеер собирает пакет тем же пайплайном, что production-экспорт (`buildScormExportData` →
  `generateScormPackage`), всегда из живого состояния теста (снапшот не отлаживается); прогон идёт на
  применённом шаблоне оформления
- [x] Плеер открывается в отдельном окне; инспектор — в сворачивающемся сайдбаре; основные
  показатели всегда видны в статусной панели над стейджем
- [x] Инспектор в реальном времени показывает коммуникацию с LMS (`cmi.*`), баллы, `scale.*`,
  `result.*` и фактически выданный набор вопросов (вариант/квоты/перечень) — вкладки на DS Tabs+`tb-table`
- [x] Тоггл «Эталон» подсвечивает правильные ответы на реальном рендере вариантов (на баллы не влияет;
  ✓ для выбора, верный № для ранжирования, буквы пар для сопоставления); цена ответа видна в «Протоколе»
- [x] Прогон не создаёт записей в `attempts` и SCORM-телеметрии и не влияет на аналитику
  (`telemetry: null`, in-memory сессия)
- [x] Протокол прогона экспортируется (CSV); состояние прогона сбрасывается автором
- [x] Документирована граница точности относительно WebTutor (нет SN/cross-attempt/реального
  retake-gate) — PRD-18 §8, BR-13-08, NFR-17/18

---

## 9. Риски и митигации

| Риск | Вероятность | Митигация |
| --- | --- | --- |
| Внешний шаблон ломается в браузере | Средняя | Браузерная smoke-проверка, живой предпросмотр, экран ошибки Core |
| Шаблон конфликтует с бизнес-логикой | Средняя | Ограниченный Runtime API шаблона, финальные защиты Core |
| LMS плохо показывает пользовательские данные | Высокая | Дублирование в `suspend_data` и `interactions` |
| HTML-контент вызывает XSS | Высокая | Серверная санитизация и запрет сырой HTML-интерполяции |
| Большие ресурсы раздувают ZIP | Средняя | Лимиты загрузки, запрет внешних URL, предупреждения по размеру |
| Новый flow ломает старые тесты | Высокая | `legacy_flat` как режим по умолчанию, golden-тесты на старые SCORM-пакеты |
| Секционные переходы дают некорректный маршрут | Средняя | Финальные защиты Core (валидация перехода), детерминированный сценарий, диагностика принятого перехода |
| Многомерные шкалы сложно объяснить автору | Средняя | UI матрицы вкладов, предпросмотр расчёта на демо-ответах, предупреждения о непокрытых шкалах |
| Шкалы смешиваются со стандартным баллом | Средняя | Раздельные пространства `score/section/scale/result`, явные настройки публикации в LMS |
| Градуированный балл смешивают с вкладами в шкалы | Средняя | Раздельные оси: корректность (`scoreRatio`) и вклад (`value_json`); exact-match по умолчанию, рубрика включается явно |
| WebTutor endpoint или формат данных меняется | Высокая | LMS-specific eligibility plugin вынесен в явный адаптер с администрируемой конфигурацией, версией и тестовой проверкой |
| Заблокированный запуск ошибочно считается попыткой | Средняя | Gate выполняется до внутренней кнопки старта и до SCORM `Initialize`; полноценная попытка определяется по фильтру статуса/прогресса, а не только по открытию курса |
| Cooldown невозможно строго обеспечить через `suspend_data` | Высокая | `suspend_data` помечается как best-effort источник; строгий режим требует WebTutor/LMS-specific или внешнего источника |
| Редактор теста становится слишком сложным для поддержки | Высокая | Декомпозиция на доменные вкладки/модули, typed DTO, мапперы, централизованная валидация и компонентные тесты |
| Добавление новых PRD ломает существующее редактирование теста | Высокая | Единый контракт `TestEditorModel`, regression suite на standard/adaptive create/edit и explicit migration strategy для новых настроек |
| Импорт/экспорт вопросов теряет данные или ломает round-trip | Высокая | Единая спецификация формата, синхронная с кодом; golden round-trip тесты на каждый тип вопроса; обратная совместимость старого формата |
| Импорт медиа вводится раньше формализации процесса работы с медиа | Средняя | Импорт медиа вынесен из BR-11 в отдельный будущий PRD «Медиатека» (BR-11-06); в контракт файла PRD-14 медиа не входит |
| Встроенный плеер расходится с CLI-плеером (две копии RTE-шима/инспектора) | Средняя | Единый общий модуль шима/инспектора для CLI и сервиса; CLI-скрипт потребляет тот же источник (BR-13-06) |
| Отладочный прогон попадает в учебную аналитику/телеметрию | Средняя | Сборка с выключенной телеметрией; отсутствие записи в `attempts`; состояние только в `localStorage`; явная пометка «прогон отладки» (BR-13-04, NFR-19) |
| Успешный прогон в плеере воспринимается как гарантия работы в WebTutor | Высокая | Документация и UI-пометка границы точности (нет SN/cross-attempt/реального retake-gate); приёмка в WebTutor остаётся обязательной для соответствующих механик (BR-13-08, NFR-18) |
| Сборка пакета на каждый прогон тяжела для больших тестов | Низкая | Лимит NFR-02 (генерация ≤30 с); кэш сессии по токену; пересборка только при смене версии/правках |
| Семантика `total_time`/reload/kill LMS-специфична (активное время расходится с ожиданием) | ~~Высокая~~ → реализовался благоприятно | Деградация к прежнему поведению при недоступности `total_time` сохранена; приёмка на целевой LMS выполнена 2026-08-06 — reload и kill вкладки ведут себя по модели активного времени, расхождения не выявлено (BR-14-06/07) |
| Клиентский учёт времени подделываем (правка `suspend_data` / изменённый пакет) | Средняя | Tamper-evidence на anchor (дезтеррент); строгая защита — серверное время/подсчёт, уровень B (BR-14-05) |
| Отчёт в LMS расходится с предпросмотром автора | Высокая | Один общий резолвер варианта на все хосты; макет И стиль запекаются в пакет на сборке; проверка работоспособности рендерит каждый объявленный вид отчёта (NFR-20, BR-16-03/04/07) |
| Смена вида отчёта переписывает документы по уже выданным попыткам | Средняя | Отчёт по попытке со снапшотом собирается вариантом ИЗ ВЫДАЧИ (NFR-21, BR-16-05) |
| Толкование измерения расходится с формулой показателя | Средняя | Перечень исходов с кодами — единственный источник литералов; валидатор сверяет литералы формулы с перечнем при сохранении (BR-17-01) |
| Цвета и вид измерений расползаются по данным | Средняя | Визуал целиком в параметрах варианта дизайна, в данных цветов нет; откат вида по выполнимости считает ядро (BR-17-04) |
| Перестановка вопросов ложно помечает переопределения оценки устаревшими | Средняя | Индекс вопроса НЕ входит в контентный хеш (BR-18-05) |
| Один переключатель допуска значит на хостах разное | Высокая | Назначение — единица допуска; решение принимает общий движок, хост поставляет только факты; барьеры действуют в непересекающиеся моменты (NFR-23, BR-19-01/04) |
| Перенос кулдауна и счётчика попыток на назначение меняет поведение существующих тестов | Высокая | Обратное заполнение связи попытки с назначением с явным легаси-бакетом; смена поведения фиксируется в PRD-31 как принятая, а не как побочный эффект |
| Вложение обратной связи обещано автору, но не доезжает до обучающегося | Высокая | Решено сменой модели (PRD-42): материал — простой `http(s)`-URL без хранения и без резолвинга адреса, тот же на всех хостах и в PDF-отчёте; легаси-дескрипторы первого решения с битой ссылкой — принятый техдолг (BR-20) |
| Обмен по QTI молча теряет наши свойства задания (шкалы, «Цена ответа», условная обратная связь, теги) | Высокая | Перечень теряемого показывается автору ДО выгрузки; переносимое уходит расширениями и метаданными стандарта; обмен без потерь остаётся за книгой Excel (BR-27-07/08) |
| Заявленное соответствие стандарту шире фактического | Высокая | Профиль соответствия объявлен и документирован; выпуск проверяется схемой или валидатором, приём — реальными файлами сторонних систем; формулировки в материалах и UI не выходят за профиль (BR-27-03/13, NFR-24) |
| Принимаемая редакция стандарта определена по документации, а файл контрагента не читается | Средняя | Состав принимаемых редакций определяется по файлам-образцам от систем-источников, заведённым в репозиторий как эталоны приёмки; живой WebTutor недоступен и документация вендора доказательством не считается (BR-27-04) |
| Листинг кода меняет `content_hash` задним числом и ложно помечает переопределения оценки устаревшими | Высокая | Новая грамматика обязана быть обратно совместимой на существующих текстах — тот же принцип, что уже применён к индексу порядка (BR-18-05); решение — часть проектирования, а не факт (BR-28-04) |
| Защита от копирования читается как гарантия «текст скопировать нельзя» | Средняя | Формулировка не используется как критерий приёмки; граница по владельцу ZIP зафиксирована явно в требованиях и в материалах (BR-29-10) |
| Защита от копирования расходится между вебом и SCORM-пакетом или ломает отладочный прогон | Средняя | Один общий построитель периметра (PRD-12) применяется инлайн-свойствами на обоих хостах; отладочный прогон и предпросмотр шаблона исключены явным признаком контекста, а не отдельной веткой стилей (BR-29-06/07) |
| Переполнение `cmi.suspend_data` продолжает молча обнулять счётчик попыток и барьеры допуска | Высокая | Содержимое вопросов выведено из состояния, вопрос адресуется позицией, бюджет проверяется фактически перед записью с диагностикой при исчерпании (BR-30) |
| Профиль SCORM 1.2 воспринимается как паритет с 2004, хотя часть возможностей в стандарте отсутствует | Высокая | Перечень утрат — часть публикуемого описания профиля; автор предупреждается до выгрузки, второй профиль не заменяет 2004 (BR-31-04/06) |
