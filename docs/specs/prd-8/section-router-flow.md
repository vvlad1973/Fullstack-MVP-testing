# PRD-8: Сценарий прохождения через страницу-маршрутизатор

**Версия:** 1.0  
**Статус:** Реализован — режим прохождения через страницу-маршрутизатор
(`router_by_topics`) работает в авторском UI, рантайме SCORM и веб-хосте  
**Дата актуализации:** 2026-06-06  
**Связанные документы:** [BRD](../brd-scorm-enhancements.md),
[PRD-1](../prd-1/templates-content-pages.md), [PRD-2](../prd-2/result-variables.md),
[PRD-4](../prd-4/course-flow-sections.md)  
**Этап BRD:** BR-06  
**Зависимости:** `flowPolicy` и результаты разделов из PRD-4, content pages и
системные страницы из PRD-1, редактор параметров теста (вкладки «Структура» и
«Настройки → Сценарий»)  
**Референс:** сертификационный тест для руководителей (`docs/references/RTK_Test_commits_last.story`)

## 1. Обзор

### 1.1 Контекст

В референсном Storyline-проекте есть не только линейное прохождение разделов, но и сценарий,
где обучающийся возвращается на общую страницу выбора после прохождения каждого раздела.
Эта страница показывает доступные разделы, факт прохождения и результаты по разделам. После
завершения всех обязательных разделов становится доступен переход к итоговому результату.

В распакованном референсе видны признаки такого сценария:

- переменная `Blocks_Done` для подсчёта завершённых блоков;
- переменные времени и вопросов по блокам: `Time_Min_1..6`, `Question_Number_1..6`,
  `Answered_1..6`;
- отдельные quiz/result slides по разделам: `Company_v1..v3`, `Finances_v1..v3`,
  `Pravo_v1..v3`, `Tech_v1..v3`, `Comp*_v*`;
- агрегирующий quiz/result `Result`, который собирает результаты отдельных блоков.

Этот сценарий отличается от `section_linear`: следующий раздел не открывается автоматически.
При этом router-паттерн намеренно проще произвольного графа переходов: автору достаточно
понятного маршрутизатора, а не движка правил (выведен из охвата — BRD BR-03).

### 1.2 Цель

Добавить режим `section_router`, в котором:

1. Core показывает router-страницу со списком разделов;
2. обучающийся выбирает доступный раздел;
3. после завершения раздела Core рассчитывает результат раздела;
4. обучающийся возвращается на router-страницу;
5. router-страница показывает статусы и результаты разделов;
6. переход к итоговому результату доступен только после выполнения политики завершения.

### 1.3 Не цель

В рамках PRD-8 не требуется:

- реализовывать произвольный graph-editor переходов;
- добавлять кастомный JavaScript автора;
- менять механику расчёта правильности вопросов;
- реализовывать независимые попытки разделов сверх уже согласованных `retakePolicy`;
- переносить экспорт SCORM в Drawer редактора.

---

## 2. Пользовательский сценарий

```text
SCO loaded
  -> start page / content before test
  -> router page
  -> learner selects section
  -> section intro/content
  -> section questions
  -> section result calculated
  -> optional section summary
  -> router page with updated status/result
  -> repeat until completion policy is satisfied
  -> final result
```

На router-странице обучающийся видит:

- название раздела;
- статус раздела: не начат, в процессе, завершён, заблокирован;
- действие открытия доступного раздела;
- действие перехода к итоговому результату, когда оно разблокировано.

Исхода раздела — «пройден» / «не пройден» — на карточке НЕТ (решение владельца
2026-09-20, закрывает одноимённый открытый вопрос в §8). Маршрутизатор отмечает, ЧТО
участник закрыл раздел, и не говорит, КАК он его закрыл: это меню, оно висит на экране
рядом с ещё не открытыми разделами и объявляет исход задолго до того, как у теста
появится собственный вердикт. Там, где исход раздела показать НУЖНО, о нём говорят экран
итогов раздела (гейт `showSectionResults`, PRD-19 FR-05a) и экран итогов теста, где
формулировку вердикта автор задаёт словарём надписей PRD-49 (`topic.verdict.*`); карточка
маршрутизатора словаря не читает и могла бы только противоречить им.

---

## 3. Модель

### 3.1 `flowPolicy`

Целевой runtime-режим:

```json
{
  "mode": "section_router",
  "router": {
    "title": "Выберите раздел",
    "instructions": "Пройдите все обязательные разделы, затем перейдите к итоговому результату.",
    "completionPolicy": "all_required_completed",
    "sectionOrder": "fixed",
    "showSectionResult": true,
    "allowReturnToCompleted": "summary_only",
    "finalResultButton": "enabled_after_completion",
    "sectionUnlockRules": {
      "section-2": {
        "mode": "after_sections_completed",
        "sectionIds": ["section-1"]
      }
    }
  }
}
```

### 3.2 Значения `completionPolicy`

| Значение | Смысл |
| --- | --- |
| `all_required_completed` | Итоговый результат доступен после завершения всех обязательных разделов |
| `all_required_passed` | Итоговый результат доступен только если все обязательные разделы пройдены |

MVP-рекомендация: начать с `all_required_completed`, потому что она покрывает референсный
паттерн без споров о пересдаче отдельных разделов.

### 3.2.1 Условия разблокировки разделов

Условия разблокировки являются логикой router-flow и настраиваются в разделе **«Настройки →
Сценарий»** вместе с `completionPolicy`. Видимы условно — только при
`flowMode = router_by_topics`. Во вкладке **«Структура»** они **не отображаются**:
Структура отвечает только за «что внутри теста», логика выполнения теста (включая
разблокировку) — собственность вкладки «Настройки → Сценарий».

MVP scope:

| Значение | Смысл |
| --- | --- |
| `always_available` | Раздел доступен сразу |
| `after_sections_completed` | Раздел доступен после завершения выбранных prerequisite-разделов |
| `after_sections_passed` | Раздел доступен после успешного прохождения выбранных prerequisite-разделов |

Для MVP используется только логика `AND`: все выбранные prerequisite-разделы должны выполнить
условие. Произвольные boolean-выражения и условия по процентам вне охвата (требовали бы движка
правил, снятого в BRD BR-03).

### 3.3 Повторный вход в завершённый раздел

| Значение | Смысл |
| --- | --- |
| `disabled` | Завершённый раздел нельзя открыть повторно |
| `summary_only` | Можно открыть только страницу результата/summary раздела |
| `retake_if_allowed` | Повторное прохождение доступно только если это разрешено политиками попыток |

MVP-рекомендация: `summary_only`.

### 3.4 Состояние runtime

```json
{
  "flow": {
    "mode": "section_router",
    "currentPage": "router",
    "router": {
      "completedRequired": 2,
      "totalRequired": 4,
      "finalResultUnlocked": false
    },
    "sections": {
      "section-1": {
        "state": "completed",
        "result": {
          "correct": 7,
          "totalQuestions": 10,
          "percent": 70,
          "passed": true
        }
      }
    }
  }
}
```

Состояние хранится в `suspend_data` и восстанавливается при повторном открытии SCO.

---

## 4. Требования

### 4.1 Авторский UI

| ID | Требование |
| --- | --- |
| FR-01 | В настройках теста доступен сценарий прохождения "Через страницу-маршрутизатор" (`flowMode = router_by_topics`) |
| FR-02 | Вкладка «Структура» для router-режима показывает: зоны «До теста» / «После теста» (как `linear_flat`), системную страницу-маршрутизатор и темы как ветки иерархии под router-row (см. FR-06a) |
| FR-03 | Текст заголовка и инструкции router-страницы — это параметры варианта `kind: router`, заполняются автором в `page-row-expand` (inline-expand) системной router-row |
| FR-04 | Автор выбирает политику завершения (`completionPolicy`) во вкладке «Настройки → Сценарий», условно при `router_by_topics`. В «Структуре» эта настройка не отображается |
| FR-05 | Автор видит, какие разделы обязательные (`test_sections.required`), и может управлять их порядком (DnD веток-тем под router-row) |
| FR-05a | Автор задаёт условия разблокировки раздела (`sectionUnlockRules`) во вкладке «Настройки → Сценарий», условно при `router_by_topics` (не в «Структуре») |
| FR-06a | Вкладка «Структура» визуализирует router-режим как **иерархию**: router-row (корень) + темы как ветки через tree-connectors `├─` `└─` тонкими DS-линиями. Старая «сценарная карта» с `connector-wrap`, `final-result-block`, `compact-router`, `sdp` — устаревшая модель, не применяется |
| FR-06b | Системная страница-маршрутизатор (`page-row` с `kind: router`) — единственная для теста, неудаляемая, без insert-row до/после. Variant привязан тихой логикой §4.3.2 PRD-1 (1 → молча, N → хинт+`…`, 0 → fallback на стандартный шаблон + warning) |
| FR-06c | Темы — полноценные `.topic-block` (как в `linear_by_topics`): страницы темы, пороги, inline-expand. DnD тем под router-row — порядок авторской организации; runtime-порядок задаётся `flowSettings.router.sectionOrder` |

### 4.1.1 Wireframe вкладки «Структура»

Целевая схема для `flowMode = router_by_topics`:

```text
┌─ Зона «До теста» ────────────────────────────┐
│  page-row, kind: info × N                    │
│  + Добавить страницу                         │
└──────────────────────────────────────────────┘

┌─ Внутри теста ───────────────────────────────┐
│  page-row, kind: router (единственная)       │  ← корень иерархии
│    ├── topic-block «Тема 1»                  │  ← ветка
│    ├── topic-block «Тема 2»                  │  ← ветка
│    └── topic-block «Тема 3»                  │
└──────────────────────────────────────────────┘

┌─ Зона «После теста» ─────────────────────────┐
│  page-row, kind: info × M                    │
│  + Добавить страницу                         │
└──────────────────────────────────────────────┘
```

Router-row показывает variant select (всегда видим, как часть строки), хинт
«Доступно N вариантов» при N > 1, warning «Используется маршрутизатор из
стандартного шаблона» при 0 вариантов в шаблоне. Параметры варианта
(заголовок, инструкция, прочие schema-поля) редактируются inline в
`page-row-expand` при раскрытии chevron'а (если у варианта непустая schema).

`completionPolicy` и `sectionUnlockRules` **не отображаются в Структуре** —
они живут во вкладке «Настройки → Сценарий», условно при `router_by_topics`.

Темы как ветки — полноценные `.topic-block` из `linear_by_topics`. Состояния
прогресса (`Не начата` / `В процессе` / `Завершена` / `Недоступна`) — это
runtime-визуализация для обучающегося, **в Drawer'е не отображается**. Автор настраивает
структуру, не имитирует прогресс.

### 4.2 Runtime

| ID | Требование |
| --- | --- |
| FR-07 | Core строит router-страницу как системную страницу flow, а не как вопрос или обычную content page |
| FR-08 | Router-страница получает от Core список разделов, их статусы, результаты и доступные действия |
| FR-09 | При выборе раздела Core переводит обучающегося в начало выбранного раздела |
| FR-10 | После завершения раздела Core рассчитывает `sectionResult` и возвращает обучающегося на router-страницу |
| FR-11 | Router-страница обновляет статусы и результаты без потери уже завершённых разделов |
| FR-12 | Переход к итоговому результату заблокирован, пока не выполнена `completionPolicy` |
| FR-13 | При восстановлении сессии Core открывает последнюю активную страницу: router или текущий раздел |
| FR-14 | Завершение SCO и запись стандартного SCORM score выполняются только на итоговом результате, а не после отдельного раздела |

### 4.3 Шаблоны

| ID | Требование |
| --- | --- |
| FR-15 | Шаблон может объявить layout/system page для router-страницы |
| FR-16 | Если шаблон не содержит router-layout, Core использует встроенный fallback-layout |
| FR-17 | Шаблон получает read-only контекст router-страницы и не рассчитывает статусы разделов самостоятельно |

### 4.4 Диагностика

| ID | Требование |
| --- | --- |
| FR-18 | Core пишет события `router:shown`, `router:sectionSelected`, `router:finalResultUnlocked`, `router:finalResultOpened` |
| FR-19 | Ошибка расчёта результата раздела не должна открывать итоговый результат; router показывает диагностируемую ошибку |

---

## 5. События

```text
router:shown
router:sectionSelected
section:started
section:completed
section:resultCalculated
router:updated
router:finalResultUnlocked
router:finalResultOpened
```

---

## 6. Экспорт SCORM

Экспорт должен добавить в пакет:

- `flowPolicy.mode = section_router`;
- конфигурацию `flowPolicy.router`;
- условия разблокировки разделов `flowPolicy.router.sectionUnlockRules`;
- данные разделов и их порядок;
- router-layout или fallback-layout;
- runtime-код восстановления router-состояния из `suspend_data`.

Старые тесты без `flowPolicy` продолжают экспортироваться как плоский режим.

---

## 7. Критерии приёмки

- Автор может выбрать `router_by_topics` в сценарии прохождения.
- Вкладка «Структура» показывает: зоны «До теста» / «После теста» как в `linear_flat`,
  системную router-row внутри теста, темы как ветки иерархии под router-row.
- Router-row — единственная для теста, неудаляемая, без insert-row до/после.
  Variant привязан тихой логикой (§4.3.2 PRD-1).
- При смене `flowMode` обратно на `linear_flat` / `linear_by_topics` router-row
  удаляется системой молча.
- `completionPolicy` и `sectionUnlockRules` редактируются во вкладке
  «Настройки → Сценарий», условно при `router_by_topics`.
  В «Структуре» эти настройки не показываются.
- SCORM runtime открывает router-страницу до выбора раздела.
- Обучающийся может перейти в доступный раздел с router-страницы.
- Разделы на router-странице блокируются/разблокируются по `sectionUnlockRules`
  в MVP-объёме.
- После завершения раздела обучающийся возвращается на router-страницу.
- Router-страница отмечает завершённые разделы (без исхода — см. §2).
- Итоговый результат заблокирован до выполнения `completionPolicy`.
- После выполнения `completionPolicy` итоговый результат доступен.
- Состояние router-flow восстанавливается после перезагрузки SCO.
- Стандартный SCORM score записывается на итоговом результате.

## 8. Открытые вопросы

| Вопрос | Рекомендация |
| --- | --- |
| Разрешать ли повторное прохождение завершённого раздела? | Для MVP открыть только summary, повторное прохождение вынести в связку с `retakePolicy` |
| Должен ли раздел быть обязательным по умолчанию? | Да, все выбранные темы обязательные по умолчанию; автор может снять обязательность, если политика завершения и итогового решения это допускают |
| ~~Показывать ли процент по разделу на router-странице?~~ ЗАКРЫТ 2026-09-20 | Нет. Карточка маршрутизатора не показывает ни процента, ни исхода — только статус: «Не начата» / «В процессе» / «Завершена» / «Недоступна». Обоснование и границы — §2 |
| Нужно ли поддерживать произвольные условия доступности разделов? | Не в MVP; поддерживаем только `always_available`, `after_sections_completed`, `after_sections_passed` с AND-логикой по выбранным prerequisite-разделам. Более сложные условия вне охвата (движок правил снят, BRD BR-03) |
