# PRD-10: Градуированная оценка ответа (цена ответа)

**Версия:** 1.0  
**Статус:** Реализован — градуированная оценка работает end-to-end
(UI → экспорт → рантайм `Σ s` → порог раздела → сертификация). Серверный градуированный
`check-answer.ts` для веб-попыток и `scoreRatio`/«Частично правильно» в SCORM CMI закрыты
позже, попутно с PRD-15 блоком D; «Частично правильно» в мгновенном фидбеке веб-хоста —
2026-08-06; предпросмотр балла (§7) — 2026-08-14 (см. §10). Открытых пунктов нет  
**Дата актуализации:** 2026-08-14  
**Связанные документы:** [BRD](../brd-scorm-enhancements.md),
[PRD-2](../prd-2/result-variables.md),
[PRD-5](../prd-5/scales-competency-measurements.md),
[Платформа SCORM-шаблонов](../spec-template-platform.md)  
**Модель скоринга:** [scoring-model.md](../scoring-model.md) §11 (нормативная модель цены ответа)  
**Этап BRD:** BR-09 (Этап 10)  
**Эталон-источник:** РТК-сертификация руководителей, ТЗ-2026
(`docs/references/Обновление сертификации для руководителей/`, `key_NEW_15-08-25.xlsx`
плюс pandas-обработчик)  
**Зависимость:** стандартный балл/`checkAnswer` рантайма (PRD-1), `result.*` + DSL (PRD-2),
темы/разделы и правила прохождения (PRD-4)

## Терминология

**«Цена ответа»** — сколько баллов приносит ответ на ОДИН вопрос. Это уровень НИЖЕ правил
прохождения и лишь меняет их вход:

```text
цена ответа (вопрос)  →  правило прохождения темы  →  правило прохождения теста / показатель
```

Сегодня цена ответа зашита как «всё или ничего» (`checkAnswer` = 0/1). PRD-10 делает её
настраиваемой (частичный балл). Правила прохождения тем/теста (`topic_pass_rule_json`,
`overall_pass_rule_json`) НЕ меняются — меняется только их вход. Термин «рубрика» в этом
документе не используется.

## 1. Обзор

### 1.1 Контекст

Для типов `multiple`/`matching`/`ranking` рантайм считает стандартный балл по принципу «всё или
ничего»: точное совпадение с эталоном даёт `1`, любое отклонение — `0`
([resultsPage.js](../../../server/scorm/template/app/render/resultsPage.js) `checkAnswer`).
Частично верный ответ получает `0`. Это блокирует класс сертификационных и образовательных
тестов, где задание оценивается градуированно (0/1/2/3).

Ближайший потребитель — РТК-сертификация руководителей (идёт на платформу сразу после релиза).
Её внешний pandas-обработчик выставляет балл за вопрос по ступенчатому правилу и суммирует баллы
в балл раздела против абсолютного порога. Без градуированной цены ответа этот тест не реализуется
на платформе без доработки.

### 1.2 Что подтверждено эталоном (РТК-2026)

Сверка реального ключа `key_NEW_15-08-25.xlsx` (208 вопросов) и обработанного pandas-результата:

- **одиночный с весами** (ключ с префиксом `%`, напр. `%A2B1C1D0`): балл = вес выбранной опции
  (0/1/2). АДДИТИВНО; уже выразимо сеткой «Вклады вопросов», новый механизм НЕ требуется;
- **множественный**: ступенчатая цена ответа над `(верных, неверных)`. НЕ аддитивна — лишняя
  неверная опция понижает ступень (`ABD` при эталоне `AD` -> `1`, а не `2`);
- **сопоставление**: ступенчатая цена ответа над `(верных пар, всего пар)` (`2/3`, `3/3`);
- **одиночный обычный**: точное совпадение (0/1) — рантайм уже умеет.

Вывод: реальный кодовый пробел — ступенчатая оценка `multiple` и `matching`. Это уточняет
BR-09-02 («доля верных со штрафом»): фактическое правило — ступенчатая таблица, доля —
её частный случай.

### 1.3 Метрики успеха

| Цель | Метрика |
| --- | --- |
| РТК-тест реализуется без внешней постобработки | golden-тест против `key_NEW` + pandas-результата зелёный (Стадия 6) |
| Обратная совместимость | все существующие тесты бит-идентичны (цена ответа по умолчанию — точное совпадение) |
| LMS получает частичный балл | `scoreRatio`/`partial` публикуются стандартными CMI-полями SCORM 2004 |

---

## 2. Пользователи

- **Автор теста** — настраивает цену ответа для вопроса (веса опций или ступенчатая таблица);
  по умолчанию способ — «точное совпадение», настройки не требует.
- **Разработчик шаблона** — получает `scoreRatio`/`partial` в публичном контексте и стандартных
  CMI-полях; цену ответа не настраивает.
- **Обучающийся** — видит частичный балл и статус «Частично правильно» там, где задана частичная
  цена ответа.

---

## 3. Функциональные требования

| ID | Требование | Основание |
| --- | --- | --- |
| FR-01 | Вопрос даёт стандартный балл `s ∈ [0, sMax]` по цене ответа своего типа; `scoreRatio = s / sMax` | §11.1, BR-09-02 |
| FR-02 | Цена ответа по умолчанию — «точное совпадение» (`sMax = 1`); вопрос/тест без неё считается как сейчас (бит-идентично) | §11.8, BR-09-05 |
| FR-03 | `single`: способ «по весам опций» — балл = вес выбранной опции, `sMax = max(вес)` | §11.3 |
| FR-04 | `multiple`: способ «ступенчатый» — балл по ступенчатой таблице над счётчиками `(c, x, T)`; неаддитивна | §11.4 |
| FR-05 | `matching`: способ «ступенчатый» — балл `f(c, P)` по верным парам; единица — `matching_pair` | §11.5, §10.11 |
| FR-06 | `ranking`: та же ступенчатая модель над `(c, N)`; дефолт — полное совпадение порядка | §11.6, §10.11 |
| FR-07 | Ступенчатая таблица — упорядоченный список `{when, score}`, первое совпадение, иначе `0`; пол `s = max(0, ·)` | §11.2, §11.8 |
| FR-08 | Балл цены ответа — ось КОРРЕКТНОСТИ; на вклады в шкалы (`question_measurements`) не влияет | §10.3, BR-09-03 |
| FR-09 | Стандартные баллы агрегируются по теме/разделу как `Σ s`; при «точном совпадении» `Σ s` = «число верных» | §11.7 |
| FR-10 | Порог раздела — правило прохождения (`count`/`percent`) на `Σ s` раздела, без выравнивающих коэффициентов | §11.7 |
| FR-11 | Сертификация — показатель (PRD-2): «1 балл за пройденный раздел; все разделы -> завершение» + `controls_status=success` | §11.7, §9.4 |
| FR-12 | Runtime публикует `scoreRatio` и статус `partial` в контракт шаблона, показ обучающемуся и LMS (CMI 2004) | BR-09-04 |
| FR-13 | Цена ответа проходит валидацию: веса/баллы числовые `>= 0`, предикаты ступеней над `{c,x,T,P,N}`, `sMax > 0` | §11.2 |

---

## 4. Модель данных

Цена ответа — поле вопроса (ось правильности), отдельное от `question_measurements` (вклады в
шкалы). Стоит на уровень ниже правил прохождения тем/теста и лишь меняет их вход (см.
«Терминология»).

Концептуальная форма (точная схема — Стадия 1):

```text
question.scoring (опционально; отсутствие = точное совпадение):
  kind: 'exact' | 'weighted' | 'tiered'               # default 'exact'
  weights?: number[]                                  # weighted (single), по индексам опций
  tiers?: [ { when: Predicate, score: number } ]      # tiered (multiple/matching/ranking)
  sMax?: number                                       # иначе выводится: max(weights) | max(tier.score)

Predicate (конъюнкция условий над счётчиками ответа):
  all: [ { lhs: 'c'|'x', op: '=='|'>='|'<='|'<'|'>', rhs: number | 'T' | 'P' | 'N' } ]
```

Счётчики по типам (§11.2): `c` — верные выбранные единицы, `x` — неверные выбранные, `T` —
эталонные единицы (всего верных опций / пар `P` / элементов `N`).

Хранение (актуализировано): изначально — отдельная nullable-колонка `questions.scoring_json`,
а не примесь в `correct_json` (см. OQ-1). PRD-15 блок D (T-40, миграция 028) перенёс цену
ответа с вопроса на тест — `questions.scoring_json`/`points` дропнуты; актуальный источник —
`test_question_scoring.scoring_json` (per-test override) с разрешением по цепочке override →
дефолт раздела → дефолт теста → системный дефолт через `shared/scoring/effective-scoring`.
Сам движок (`kind: exact|weighted|tiered`) и его семантика не изменились — изменилось только
место хранения конфига.
Durable id единиц НЕ вводится — единицы остаются индексными, как уже принято в PRD-5
(`source_key`); ступенчатая оценка count-based (см. OQ-3 в §11).

---

## 5. Рантайм и конвейер

- Авторитетный движок
  [shared/scoring/engine.ts](../../../shared/scoring/engine.ts) `scoreAnswer(...)` возвращает
  `{score, sMax, ratio}` (`ratio = s/sMax`); рукописный JS-порт
  [engine.js](../../../server/scorm/template/app/scoring/engine.js), вшитый в сборку перед
  `resultsPage.js`. Единственный рантайм-`checkAnswer`
  ([resultsPage.js](../../../server/scorm/template/app/render/resultsPage.js)) делегирует движку и
  возвращает `ratio`; при отсутствии цены ответа — «точное совпадение» (FR-02). Парити-порт сверяется
  golden-тестом (как DSL/шкалы).
- Оценщик ступенчатой таблицы: вычислить счётчики `(c, x, T/P/N)` по типу, пройти `tiers` сверху
  вниз, вернуть `score` первого совпадения, иначе `0`, затем пол `max(0, ·)`.
- Агрегация `Σ s` по теме включается в существующий расчёт результата; правило прохождения
  раздела читает `Σ s` (FR-09, FR-10).
- Конвейер (§4 scoring-model): балл вопроса -> тема/раздел -> правило прохождения -> показатель
  (сертификация, `controls_status`) -> LMS.

---

## 6. Экспорт и LMS

- `test-json` переносит `scoring` в TEST_DATA пакета.
- `scoreRatio` и `partial` публикуются стандартными CMI-полями SCORM 2004 (FR-12, NFR-04).
- Итог раздела и сертификация уходят в LMS через механику показателей PRD-2 (`result.*`,
  псевдо-интеракции, override статуса).

---

## 7. UI (редактор цены ответа)

Содержание:

- переключатель способа цены ответа в карточке вопроса (по умолчанию «Точное совпадение»);
- `single`: ввод весов по опциям (read-only подсветка верного варианта сохраняется);
- `multiple`/`matching`/`ranking`: КОНСТРУКТОР ступенчатой таблицы — автор
  сам задаёт строки «условие (порог над `c`/`x`/`T`) -> значение (балл)», порядок строк =
  приоритет (первое совпадение); пресеты опциональны как заготовка, но базовый режим — ручной
  конструктор;
- предпросмотр балла по гипотетическому ответу;
- состояния: основное, пустое, loading/saving, ошибка валидации.

---

## 8. Совместимость и миграция

- Отсутствие `scoring` = «точное совпадение», `sMax = 1` -> текущий расчёт без изменений
  (FR-02, NFR-10).
- Миграция не меняет существующие вопросы; цена ответа добавляется опционально.
- Durable id единиц `matching`/`ranking` — пререквизит (§10.7 #2); единицы остаются индексными,
  ступенчатая оценка count-based и устойчива к перемешиванию.

---

## 9. Тестирование

- **Golden РТК:** воспроизвести балл за вопрос и балл раздела против
  `key_NEW_15-08-25.xlsx` + `report_processed_pandas_*.xlsx` (выверенная фикстура: одиночный с
  весами, множественный ступенчатый, сопоставление ступенчатое, абсолютные пороги, сертификация
  6 разделов).
- **Совместимость:** существующие golden/`scorm-export`/`mbi-golden` остаются зелёными
  (дефолт «точное совпадение»).
- **Юнит:** оценщик ступенчатой таблицы (граничные `c/x/T`), веса опций, пол `max(0, ·)`,
  агрегация `Σ s`.
- **Парити:** TS-источник и JS-порт `checkAnswer` дают одинаковый балл.

---

## 10. Вне охвата

- Negative marking, зачёт неотмеченных дистракторов, `choose-N` нормализация — §10.6.
- IRT/Rasch, оценка параметров заданий, θ — платформа потребляет готовые данные (§10.6).
- Выравнивающие коэффициенты `a*raw+b` по формам — ТЗ-2026 их убрало; опция на будущее (§3).
- Композитные шкалы и глобальная библиотека шкал — отдельный трек (PRD-5 отложенное).

### Закрыто позже, не в рамках PRD-10 (актуализация 2026-08-14)

- **Предпросмотр балла по гипотетическому ответу (§7)** — реализован 2026-08-14, последний
  пункт [issue #31](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/31).
  Кнопка «Предпросмотр балла» в футере модала «Оценка вопроса в тесте» открывает
  [score-preview-modal.tsx](../../../client/src/features/tests/editor/sections/score-preview-modal.tsx):
  демо-ответ из выпадающего списка, счётчики `c`/`x`, сработавшая ступень, `s` / `sMax` /
  `scoreRatio` и вердикт. Два свойства делают предпросмотр достоверным: балл считает ОБЩИЙ
  движок (`@shared/scoring/engine`), второй реализации правил нет; и считается ЧЕРНОВИК
  конструктора, то есть эффект правки виден до «Применить». Демо-ответы генерирует
  [demo-answers.ts](../../../shared/scoring/demo-answers.ts) из эталона вопроса — автор
  ничего не набирает руками, а случаи, которых эталон дать не может (лишний вариант, когда
  верны все; часть верных при одном верном), просто отсутствуют. Чтобы показать, КАКАЯ
  ступень сработала, `explainAnswer` получил поле `tierIndex` (и то же — JS-двойник в
  пакете, паритет держит `tests/scoring-engine-port.test.ts`).
  Эскиз: `docs/wireframes/approved/prd15-test-scoring.html`, состояние `s-preview`.

### Закрыто позже, не в рамках PRD-10 (актуализация 2026-08-06)

- **«Частично правильно» в МГНОВЕННОМ фидбеке веб-хоста для стандартного режима** —
  реализовано 2026-08-06 (часть
  [issue #31](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/31)).
  `handleStandardConfirm` в
  [take-test.tsx](../../../client/src/pages/learner/take-test.tsx) больше не считает
  верно/неверно своей логикой точного совпадения: `scoreAnswerLocally` делегирует
  `@shared/scoring/engine` и возвращает долю `s / sMax`, а вердикт стал трёхпозиционным
  (`success` / `warning` «Частично правильно» / `error`) — те же тон и формулировка, что
  у пакета (`app/feedback/feedback.js`). Эффективный конфиг оценивания приходит с попыткой
  полем `scoring` рядом с `correctJson` (`questionsForClient` в
  [attempts.ts](../../../server/routes/attempts.ts), маршруты `attempts/start` и `resume`) —
  то же имя поля и то же умолчание, что печёт SCORM-сборка, и тот же гейт «показывать
  правильность ответа»: без ключа ответа конфиг не отдаётся. Адаптивный режим остался
  бинарным намеренно (`scoreRatio` ему не передаётся) — это не пробел, а решение.

### Закрыто ранее, не в рамках PRD-10 (актуализация 2026-08-05)

Ранее в этом статусе числились как нереализованные — на деле уже сделаны, попутно с
PRD-15 блоком D:

- **Градуированный серверный `check-answer.ts` для веб-попыток** — реализован
  ([server/utils/check-answer.ts](../../../server/utils/check-answer.ts)), делегирует
  `@shared/scoring/engine`, принимает эффективный конфиг через `effective-scoring`;
  используется в аналитике (`analytics/attempts.ts`, `analytics/export.ts`,
  `analytics/test-details.ts`, `analytics/combined.ts`) и в адаптивном маршруте попыток.
- **`scoreRatio`/«Частично правильно» в SCORM CMI и у ученика (FR-12)** — реализовано в
  пакете: [feedback.js](../../../server/scorm/template/app/feedback/feedback.js) показывает
  тон `warning` и текст «Частично правильно» по `scoreRatio > 0` через общий
  `TB.feedbackBanner`. На вебе реализовано для итогового балла (см. выше), но не для
  мгновенного фидбеко по вопросу — см. пункт выше.

---

## 11. Решённые вопросы

| # | Вопрос | Решение |
| --- | --- | --- |
| OQ-1 | Хранение цены ответа: расширить `correct_json` или отдельное поле/таблица | Отдельная nullable-колонка `questions.scoring_json`, НЕ примесь в `correct_json`. Отсутствие = `exact`, `sMax = 1`, старые тесты бит-идентичны (FR-02). CHECK на `kind` в SQL; структурная валидация — zod `questionScoringSchema` |
| OQ-2 | Есть ли в DSL PRD-2 доступ к баллу/прохождению темы для показателя-сертификации | DSL даёт `topicById("id").score`/`.passed` и `countPassed()`; `.score` отражает `Σ s`. Отдельная доработка DSL не требуется |
| OQ-3 | Durable id единиц `matching`/`ranking` сейчас индексные | Durable-id не вводится. Единицы остаются индексными (как в PRD-5 `source_key`); ступенчатая оценка считает счётчики `(c, x, T/P/N)`, не per-unit id; перемешивание индекс-безопасно (маппинг только на отображение) |
| OQ-4 | Пресеты ступенчатых таблиц или ручной конструктор | Базовый режим — ручной конструктор (автор задаёт пороги над `c`/`x`/`T` и значения-баллы); пресеты опциональны как заготовка (см. §7) |
