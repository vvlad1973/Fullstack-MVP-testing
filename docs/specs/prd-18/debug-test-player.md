# PRD-18: Встроенный плеер тестирования и отладки тестов

**Версия:** 1.1  
**Статус:** РЕАЛИЗАЦИЯ — Фазы 0–4 ЗАКРЫТЫ (2026-06-27), осталась Фаза 5 (документация границы точности +
приёмка). Решения согласованы (вопрос-ответ 2026-06-26): встроенный плеер с инспектором реального времени;
доступ = edit; прогоны одноразовые; версия — ТОЛЬКО живая (снапшот не отлаживается); запуск из меню
действий теста в ОТДЕЛЬНОМ окне; сворачивающийся сайдбар-инспектор + статусная панель; прогон на
ПРИМЕНЁННОМ шаблоне оформления. Состав статусной панели и инспектора определён (§5), плюс «Эталон» —
подсветка правильных ответов на стейдже (§5.4). Реализация: общий `buildScormExportData` (Фаза 1); общий
`window.TBInspector` compute + тонкий `inspector.js` (Фаза 2); роутер `debug-player.ts`, пакет verbatim,
шим в окне-плеере (Фаза 3); клиент-окно `client/src/features/tests/debug-player/` с 7 DS-вкладками + «Эталон»
(Фаза 4, проверено живым DEV-прогоном). Эскиз согласован
([docs/wireframes/prd18-debug-player.html](../../wireframes/approved/prd18-debug-player.html)); см. §11.  
**Дата актуализации:** 2026-06-27  
**Связанные документы:** [BRD](../brd-scorm-enhancements.md) (BR-13, NFR-18/19, NFR-17),
[PRD-3](../prd-3/external-templates.md) (реестр шаблонов, `resolveTemplateDir`, smoke-предпросмотр),
[PRD-12](../prd-12/web-runtime-parity.md) (единый рендер ученических экранов),
[PRD-15](../prd-15/content-ownership.md) (снапшоты публикации, `exportSourceForTest`),
[PRD-17](../prd-17/question-set-variants.md) (варианты выдачи, паритет хостов golden-тестом),
[scoring-model.md](../scoring-model.md) (расчёт балла/шкал/показателей)  
**Этап BRD:** BR-13 (Этап 14)  
**Источники в коде:** локальный плеер [scripts/scorm-player.mjs](../../../scripts/scorm-player.mjs)
(RTE-шим + 5-вкладочный инспектор), экспорт
[server/routes/tests.ts](../../../server/routes/tests.ts) (`GET /:id/export/scorm`),
сборка пакета [server/scorm/index.ts](../../../server/scorm/index.ts)
(`generateScormPackage`), выбор источника
[server/services/test-snapshot.ts](../../../server/services/test-snapshot.ts)
(`exportSourceForTest`), gate [server/services/test-access.ts](../../../server/services/test-access.ts)

## Терминология

- **Плеер отладки** — встроенный в сервис экран, который собирает РЕАЛЬНЫЙ SCORM-пакет из теста
  и проигрывает его в iframe с SCORM 2004 RTE-шимом, окружённым панелями инспектора. Это
  серверно-аутентифицированный аналог CLI-плеера [scorm-player.mjs](../../../scripts/scorm-player.mjs),
  а не отдельный веб-рантайм.
- **Прогон отладки** — одна сессия проигрывания собранного пакета. Полностью ОДНОРАЗОВЫЙ: не
  пишется в `attempts`/аналитику/телеметрию; состояние — в `localStorage` плеера, сбрасывается
  автором.
- **Инспектор** — набор панелей реального времени над запущенным пакетом: коммуникация с LMS
  (RTE-журнал), баллы, шкалы, показатели, фактически выданный набор вопросов, протокол по вопросам.
  Зеркало 5 вкладок CLI-плеера (Протокол / Шкалы / Показатели / Watch / LMS-журнал).
- **Источник версии** — ВСЕГДА живой ЧЕРНОВИК (текущее редактируемое состояние теста);
  опубликованный снапшот не отлаживается (D-4).

Это инструмент **препрод-проверки** (корректность настройки «математики» теста до выгрузки в LMS),
а не учебный сценарий и не замена приёмке в WebTutor (§1.2).

---

## 1. Обзор

### 1.1 Контекст

Сегодня прогнать собранный тест как обучающийся и в реальном времени увидеть, что пакет сообщает
LMS, какие баллы/шкалы/показатели считаются и какой набор вопросов фактически выдан, можно ТОЛЬКО
из командной строки:

1. `npm run scorm:sample` / `scorm:template` собирает фиктивный пакет в `out/*.zip`;
2. `npm run scorm:player` поднимает локальный плеер на `:5050` с RTE-шимом и инспектором;
3. инспектор показывает Протокол / Шкалы / Показатели / Watch / LMS-журнал.

Это требует репозитория, Node и CLI — недоступно автору и методологу. Внутри сервиса полноценного
прогона нет: предпросмотр статичен (шаблон, страница, расчёт шкал на демо-ответах
[scales-api.ts](../../../client/src/features/tests/editor/scales-api.ts)), а реальный путь
прохождения ([take-test.tsx](../../../client/src/pages/learner/take-test.tsx)) пишет обычную
попытку в `attempts` и аналитику. PRD-18 переносит CLI-плеер с инспектором В СЕРВИС как
аутентифицированную функцию редактора теста.

Ключевой рычаг переиспользования: production-экспорт уже собирает `ExportData` и зовёт
`generateScormPackage` ([tests.ts:918-1065](../../../server/routes/tests.ts#L918-L1065)). Плеер
отладки использует ТОТ ЖЕ сборщик и пайплайн, но ВСЕГДА из живого состояния теста — так автор
отлаживает то, что редактирует прямо сейчас (NFR-18). Для опубликованного теста отгружаемый пакет
собирается из снапшота, поэтому отладку живого состояния следует понимать как «что уйдёт при
следующей публикации», а не как текущий опубликованный снапшот.

### 1.2 Граница реализуемости (NFR-17/NFR-18)

Плеер исполняет РЕАЛЬНЫЙ артефакт пакета, но среда исполнения — шим, а не WebTutor. Что
воспроизводимо и что нет:

| Аспект | Плеер отладки | Реальный WebTutor |
| --- | --- | --- |
| Рендер экранов, выдача, расчёт балла/шкал/показателей, `suspend_data` within-attempt | да (тот же `app.js`/`runtime.js` пакета) | да |
| Коммуникация с LMS (`cmi.*` чтения/записи, completion/success/score) | да (RTE-шим логирует трафик) | да |
| Восстановление сессии within-attempt (resume по `suspend_data`) | да (localStorage) | да |
| Sequencing & Navigation (SN), активное дерево активностей | НЕТ (шим только RTE) | зависит от LMS |
| Cross-attempt-стор, ротация вариантов на повторе (PRD-17) | НЕТ (`adl.data` недоступен; деградация до случайного выбора) | НЕТ (NFR-17) |
| Реальный retake-gate по данным WebTutor (PRD-6) | только МОК ([scorm-player.mjs](../../../scripts/scorm-player.mjs) `/api/mock-webtutor`/ClientBridge) | да |

Вывод: плеер закрывает проверку «математики» теста (выдача, баллы, шкалы, показатели, что уходит в
LMS), но НЕ заменяет приёмку в WebTutor для механик, зависящих от LMS-окружения (SN, cross-attempt,
реальный retake-gate). Это явно документируется и помечается в UI (FR-14, R-3).

### 1.3 Метрики успеха

| Цель | Метрика |
| --- | --- |
| Прогон без CLI | держатель edit-доступа запускает плеер из редактора теста и проигрывает пакет, не скачивая ZIP и не открывая терминал |
| Тот же пайплайн сборки | пакет плеера собран тем же `generateScormPackage` и сборщиком `ExportData`, что `GET /:id/export/scorm`, но всегда из живого состояния теста; для опубликованного теста отгружается снапшот — отладка живой версии = «что уйдёт при следующей публикации» |
| Видимость математики | инспектор показывает в реальном времени `cmi.*`, `score.*`, `scale.*`, `result.*` и фактически выданный набор вопросов (вариант/квоты/перечень/теги) |
| Изоляция | прогон не создаёт записей в `attempts`, `scorm_packages`, `scorm_attempts`, `scorm_answers`; аналитика не меняется |
| Нет дрейфа с CLI | RTE-шим и инспектор — единый источник для CLI- и встроенного плеера |

---

## 2. Пользователи

- **Автор теста (владелец)** — собирает и отлаживает свой тест: проигрывает, читает инспектор,
  экспортирует протокол.
- **Соавтор / методолог / QA с грантом `edit`** — то же в пределах edit-доступа к тесту; правка
  контента не требуется для отладки, но отдельного «debug-доступа» не вводится (D-2).
- **Администратор/суперпользователь** — доступ по общим правилам RBAC (как у экспорта SCORM).
- **Обучающийся** — НЕ пользователь этой функции: учебный путь остаётся в
  [take-test.tsx](../../../client/src/pages/learner/take-test.tsx).

---

## 3. Функциональные требования

| ID | Требование | Основание |
| --- | --- | --- |
| FR-01 | Запуск плеера отладки — ТОЛЬКО пункт в меню действий теста (рядом с экспортом SCORM), без отдельной кнопки; доступ гейтится edit-scope теста (`requireTestScope("edit")`), как экспорт. Согласуется с BR-08-08 (карточка/список не обрастают кнопками; расширенные действия — через единое меню) | BR-13-01, BR-13-03 |
| FR-02 | По запуску сервер собирает SCORM-пакет тем же пайплайном, что `GET /:id/export/scorm`: `ExportData` через общий сборщик + `generateScormPackage`; телеметрия ВСЕГДА выключена (`telemetry: null`) | BR-13-01, BR-13-04, NFR-18 |
| FR-03 | Источник версии — ВСЕГДА живая версия (текущее редактируемое состояние, live storage); опубликованный снапшот НЕ отлаживается. Сборка всегда идёт из живого состояния теста | BR-13-05, D-4 |
| FR-04 | Плеер отражает ТЕКУЩИЕ правки: после изменения теста автор пересобирает пакет («Пересобрать») и видит изменения в новом прогоне; отдельного источника-снапшота и переключателя версий нет | BR-13-05 |
| FR-05 | Собранный пакет хранится В ПАМЯТИ сервера под одноразовым токеном сессии (TTL); файлы пакета отдаются плееру по `GET /api/tests/:id/debug/play/:token/*` (тот же приём, что в CLI-плеере: распаковка в память, статическая раздача same-origin) | BR-13-01 |
| FR-06 | В лаунч-страницу пакета инжектируется SCORM 2004 RTE-шим (`window.API_1484_11`): `Initialize/Terminate/GetValue/SetValue/Commit/GetLastError/...`, полная модель `cmi.*`, persist в `localStorage`, детект resume/ab-initio — единый код с CLI-плеером | BR-13-01, BR-13-06 |
| FR-07 | Инспектор показывает в реальном времени: **LMS-журнал** (поток чтений/записей `cmi.*`, completion/success/score, snapshot `suspend_data`); **Результаты** (`score.raw`/`score.scaled`/`min`/`max`, результаты разделов; в адаптиве — подтверждённые уровни по темам); **Шкалы** (`scale.*` — значение/уровень/%); **Показатели** (`result.*`); **Выдача** (выпавший вариант/квоты, перечень выданных вопросов с типом и тегами); **Протокол** (по каждому вопросу: что выдано, ответ, верность, `scoreRatio`/`partial`, вклад в шкалы) | BR-13-02 |
| FR-08 | Прогон ОДНОРАЗОВЫЙ: ни старт, ни ответы, ни завершение не создают `attempts`/SCORM-телеметрию и не меняют аналитику; всё состояние — в `localStorage` плеера | BR-13-04, NFR-19 |
| FR-09 | Автор может СБРОСИТЬ прогон (очистить `localStorage` сессии) — начать заново с `ab-initio`; и перезапустить сборку (например, после правки теста — пересобрать пакет) | BR-13-04 |
| FR-10 | Протокол прогона экспортируется в CSV (как в CLI-плеере) | BR-13-02 |
| FR-11 | ~~Аудит выдачи (N-прогонная статистика распределения вариантов/квот)~~ — **СНЯТО**: диагностика выдачи (квота недобирает / битый вариант / неравные размеры) детерминирована и относится к ВАЛИДАЦИИ РЕДАКТОРА (PRD-11 BR-10-04, PRD-17), N-прогонная симуляция её дублирует и значимой ценности не несёт (§9) | — |
| FR-12 | Токен сессии и раздача файлов гейтятся тем же edit-scope и принадлежностью токена пользователю; чужой/просроченный токен → `404`/`410` | BR-13-03 |
| FR-13 | Плеер строится на ОБЩЕМ источнике RTE-шима и инспектора с CLI-плеером (`scorm-player.mjs` рефакторится на потребление общего модуля) — паритет поведения отладки | BR-13-06 |
| FR-14 | UI плеера явно помечает границу точности: «прогон отладки, не учитывается; не заменяет приёмку в WebTutor (нет SN/cross-attempt/реального retake-gate)» | BR-13-08, NFR-18 |
| FR-15 | Retake-gate (PRD-6): по умолчанию в отладке gate в режиме `failOpen`/обхода (прогон не должен блокироваться cooldown); опционально — прогон против МОКА WebTutor-гейта (как в CLI-плеере) для проверки экрана блокировки | §9, D-5 |
| FR-16 | Ошибки сборки (невалидный шаблон, неполный вариант PRD-17) показываются автору понятным сообщением, не «500»; диагностика рантайма пакета (ошибки шаблона/формул) видна в инспекторе | NFR-08 |
| FR-17 | Плеер открывается в ОТДЕЛЬНОМ ОКНЕ (новая вкладка/окно), не внутри редактора, чтобы прогон шёл в полноэкранном поле, не стеснённом макетом редактора | BR-13-10 |
| FR-18 | Панель плеера содержит СВОРАЧИВАЮЩИЙСЯ сайдбар с вкладками инспектора (§5): свёрнутый — максимизирует поле пакета, развёрнутый — показывает активную вкладку | BR-13-10 |
| FR-19 | Фиксированная СТАТУСНАЯ панель НАД СТЕЙДЖЕМ в левой колонке (не на всю ширину окна; инспектор поднят к верху и занимает правую колонку — ревью 2026-06-27) с МЕТОДОЛОГИЧЕСКИМ МИНИМУМОМ — Прогресс (где идёт прогон) + Оценка (баллы/% · порог · вердикт), плюс алярм «Ошибка расчёта» при сбое формулы; видна ВСЕГДА, в т.ч. при свёрнутом сайдбаре. Полный состав — §5.1 | BR-13-10 |
| FR-20 | Пакет для прогона собирается с ПРИМЕНЁННЫМ к тесту шаблоном оформления (`design_settings_json.templateId` + `params`, резолв `resolveTemplateDir`), как production-экспорт — отладка идёт на реальном визуальном шаблоне, не на демо/`default` | BR-13-10, NFR-18 |
| FR-21 | Тоггл «Эталон» в риббоне стейджа (DS-свитч `ou-switch`, по умолчанию ВЫКЛ; на подсчёт баллов НЕ влияет) подсвечивает правильные ответы НА РЕАЛЬНОМ рендере вариантов применённого шаблона лёгким маркером: одиночный/множественный — ✓ у верного; ранжирование — правильный №; сопоставление — доска пар с буквами корректных соответствий. Цена ответа (веса опций — одиночный `weighted`; тиры по счётчикам c/x — множественный/сопоставление/ранжирование) показывается во вкладке «Протокол», НЕ на вариантах. Источник «что верно» — контекст рендера (`correct`, `showCorrectAnswers`), лёгкий debug-слой общего шаблона; см. §5.4 | BR-13-02 |

---

## 4. Архитектура

Без новых таблиц БД и без новых сущностей хранения. Всё держится на трёх переиспользуемых блоках
(сборщик `ExportData` → `generateScormPackage` → плеер/инспектор) и одной in-memory сессии.

### 4.1 Серверная часть (новый роутер `server/routes/debug-player.ts`)

| Эндпоинт | Назначение |
| --- | --- |
| `POST /api/tests/:id/debug/session` | gate edit-scope; собирает `ExportData` общим сборщиком ИЗ ЖИВОГО состояния теста, зовёт `generateScormPackage({ ..., telemetry: null })`, кладёт ZIP в in-memory `Map<token, { buffer, files, launch, userId, expiresAt }>`, возвращает `{ token, launch, warnings }` |
| `GET /api/tests/:id/debug/play/:token/*splat` | отдаёт файл из распакованного в память пакета (same-origin); в лаунч-HTML инжектирует RTE-шим; gate edit-scope + владение токеном |
| `DELETE /api/tests/:id/debug/session/:token` | удаляет сессию (явный сброс/закрытие) |

**Рефактор сборщика (обязателен для NFR-18).** Тело `GET /:id/export/scorm`
([tests.ts:918-1065](../../../server/routes/tests.ts#L918-L1065)) выносится в сервис
`buildScormExportData(testId, { source }): Promise<ExportData>` (загрузка
секций/тем/вопросов/шкал/показателей/scoring/адаптива/контент-страниц, резолв `templateDir`). Его
используют ОБА: production-экспорт и `debug/session`. Параметр `source` сохраняется для ЭКСПОРТА
(`draft` → live storage, `published` → активный снапшот, как сейчас решает `exportSourceForTest`),
но `debug/session` ВСЕГДА вызывает сборщик с живым источником (`draft`/live) — отлаживается текущее
редактируемое состояние. Единый сборщик устраняет вторую копию логики и гарантирует, что отладка и
экспорт собирают пакет одним и тем же способом.

**Изоляция (NFR-19).** В `debug/session` телеметрия не включается (никакого `?telemetry=true`),
поэтому `scorm_packages`/`scorm_attempts`/`scorm_answers` не создаются. Записи в `attempts` нет —
плеер не дёргает `/attempts/*`. In-memory сессии живут по TTL и чистятся; рестарт сервера их теряет
(допустимо — прогон одноразовый).

### 4.2 Общий модуль плеера (паритет с CLI, FR-13)

RTE-шим и логика инспектора из [scorm-player.mjs](../../../scripts/scorm-player.mjs) выносятся в
общий источник (напр. `server/scorm/debug-player/` — шим-инжектор + клиентские панели инспектора),
который потребляют:

- CLI-плеер `scripts/scorm-player.mjs` (остаётся dev-инструментом, [keep-preview-generator]-логика —
  не выпиливать);
- встроенный плеер (роутер `debug-player.ts` + клиентская страница).

Так поведение отладки (модель `cmi`, журнал трафика, парсинг `suspend_data`, протокол/шкалы/
показатели/выдача) не расходится между хостами. Паритет фиксируется тестом (по образцу
golden-теста близнеца PRD-17, §11 Фаза 5).

### 4.3 Клиентская часть (новая страница/панель в редакторе)

- Точка входа — ПУНКТ в меню действий теста (рядом с экспортом SCORM), без отдельной кнопки
  ([client/src/features/tests/editor/](../../../client/src/features/tests/editor/)); открывает плеер
  в ОТДЕЛЬНОМ ОКНЕ (новая вкладка/окно), не в редакторе (FR-17).
- Окно плеера (макет): по центру — iframe пакета (same-origin,
  `/api/tests/:id/debug/play/:token/*`), собранного с ПРИМЕНЁННЫМ шаблоном оформления теста
  (FR-20); СВОРАЧИВАЮЩИЙСЯ сайдбар с вкладками инспектора (§5), читающими глобалы запущенного пакета
  и журнал RTE (same-origin доступ к `window` iframe, как в CLI-плеере); фиксированная СТАТУСНАЯ
  панель сверху/снизу с основными показателями (FR-19). Версия — всегда живая (переключателя версий
  нет); действия: «Пересобрать» (свежие правки), «Сброс», «Экспорт протокола (CSV)».
- UI собирается из DS-примитивов (`@skillum/ui-kit`), по согласованным wireframes (Фаза 0).

### 4.4 Поток (happy path)

```text
Редактор -> «Открыть в плеере отладки»
  -> POST /debug/session                         (gate edit-scope; всегда живой источник)
       buildScormExportData(testId, {source:draft/live}) -> generateScormPackage(telemetry:null)
       -> { token, launch }
  -> экран плеера: iframe src=/debug/play/:token/<launch>  (RTE-шим инжектирован)
       инспектор читает cmi-трафик + глобалы пакета в реальном времени
  -> автор проходит тест: видит выдачу/баллы/шкалы/показатели/LMS-журнал
  -> «Сброс» очищает localStorage; «Пересобрать» -> новый token после правок
```

---

## 5. Статусная панель и инспектор (состав показателей)

Состав соотнесён с существующим CLI-плеером
([scripts/scorm-player.mjs](../../../scripts/scorm-player.mjs)): помечено, что
ПЕРЕИСПОЛЬЗУЕТСЯ из готового инспектора, что ПЕРЕНОСИТСЯ, а что НОВОЕ. Базовый ориентир — текущие
5 вкладок инспектора ([:352](../../../scripts/scorm-player.mjs#L352)) и адаптив-бар
([:334](../../../scripts/scorm-player.mjs#L334)); постоянной статусной панели сегодня нет — её
значения уже считаются, но разбросаны по вкладкам (Watch→cmi, LMS-журнал, футер протокола).

### 5.1 Статусная панель (FR-19) — методологический минимум, видна всегда

Аудитория панели — автор/методолог (зона ответственности — корректность математики теста, не
LMS-плумбинг). Правило: на панели только то, что отвечает на вопрос «верна ли математика прямо
сейчас» и нужно видеть НЕПРЕРЫВНО (в т.ч. при свёрнутом инспекторе). Всё остальное — во вкладки
инспектора или в заголовок окна. Действия прогона (Пересобрать/Сброс) — в тулбаре окна; экспорт
протокола в CSV — во вкладке «Протокол» (контекстно, без дубля в тулбаре). Размещение панели: НАД
СТЕЙДЖЕМ в левой колонке (не на всю ширину окна), весь статус-контент левым флангом; инспектор
поднят к верху окна и занимает правую колонку с самого верха (его лента вкладок — на уровне
статусной панели).

Постоянные группы:

| Группа | Элементы (пользовательские формулировки) | Источник | Зачем непрерывно |
| --- | --- | --- | --- |
| Прогресс (РЕЖИМО-ЗАВИСИМ) | **Плоский** (`linear_flat`): «вопрос i из N» + мини-бар + тема (разделов-единиц результата нет → пер-секц. статуса не существует). **Секционный** (`linear_by_topics`/`router`): лента статусов разделов — пройденные (% + ✓/✗) + текущий как АКТИВНЫЙ хвост «идёт». **Адаптив:** лента «уровни по темам» — подтверждённые темы (уровень + ✓) + текущая тема АКТИВНЫМ хвостом (уровень + стрелка ↑/↓ + «идёт»); без дублирования (перенос адаптив-бара CLI) | `state.currentIndex`, `flatQuestions`, `topicsState`/`sectionResults`/`levelsState` | в плоском — двусмысленность промежуточных чисел; в секц./адапт. пер-раздельный/пер-уровневый статус — главный выход логики. МАСШТАБ: при многих темах строка СВЁРНУТА — первые чипы + «+N подтверждено» + текущий активный + «осталось K». Клик по «+N» РАСКРЫВАЕТ статусы всех тем ПРЯМО В СТАТУСНОЙ СТРОКЕ (строка переносится на несколько рядов), повторный клик `свернуть` — обратно. НЕ во вкладку |
| Оценка | «N из M» · «%» · «порог K%» · вердикт «Пройден/Не пройден» | `cmi.score.*` (как %), `passRule`, вердикт = `success_status` пакета | главный методологический итог + калибровка порога; вердикт пакета может расходиться с «балл ≥ порога» (секц. гейт) — это баг, виден тут |

Условная группа (алярм, всплывает ТОЛЬКО при сбое):

| Группа | Элементы | Источник | Зачем |
| --- | --- | --- | --- |
| Ошибка расчёта | красный бейдж «Формула шкалы/показателя „…" не вычислена» — в норме отсутствует | `computeTestScales()`/`computeTestResultVariables()` (поля `errors`) | сломанная формула = молчаливый мусор в LMS; ловить сразу |

Перенесено С панели (зона ответственности / не-метрика):

| Что | Куда | Почему не на панели |
| --- | --- | --- |
| Статус для LMS (`completion`/`success`/`entry`) | вкладка «LMS-журнал» | интеграционный плумбинг, не зона методолога; вердикт прохождения уже в «Оценке» |
| Выдача (вариант/квоты/состав) | вкладка «Выдача» | самодостаточный протокол выдачи; чип на панели лишь дублировал его заголовок |
| Значения/счётчики шкал и показателей | вкладки «Шкалы»/«Показатели» | данные, не сигнал; на панель вынесен только алярм ошибок |
| Версия «живой черновик», дисклеймер «не учитывается / не заменяет WebTutor», id шаблона | заголовок окна (подзаголовок + ⓘ-тултип) | статичные подписи/страховка, а не метрики; контекст «это отладка» задаёт само окно |

### 5.2 Инспектор (FR-18) — вкладки в сворачивающемся сайдбаре

7 вкладок: 5 переиспользуются из CLI-плеера (одна переименована), 2 — новые. Порядок: «Результаты»
ПЕРВОЙ и открыта ПО УМОЛЧАНИЮ (главный методологический итог), «LMS-журнал» — ПОСЛЕДНЕЙ. Бейдж вкладки =
число строк/событий (как сейчас). Если подписи не влезают в ширину сайдбара — лента вкладок
прокручивается стрелками ‹/›: они присутствуют всё время переполнения (на концах disabled — иначе их
ширина «съедает» показанную вкладку), а клик по полускрытой вкладке доскролливает её ЦЕЛИКОМ и
приоткрывает соседнюю наполовину. Скролл-стрелки — пробел DS (кандидат на scrollable-вариант `ou-tabs`).
Сворачивание сайдбара — круглым краевым тогглом (DS `ou-fab--neutral`) на левом крае, центр выровнен по
линии под вкладками. Реализация: `wf-*` в эскизе — только faux-chrome каркаса; в ПРОД-КОДЕ они
недопустимы (каркас — DS-примитивы раскладки + модульный CSS фичи).

| # | Вкладка | Элементы | Источник | Статус |
| --- | --- | --- | --- | --- |
| 1 (по умолч.) | Результаты | агрегат `score.raw/scaled/min/max`; порог и вердикт прохождения; результаты по разделам (`sectionResult`) при секционном потоке; `completion`/`success`. **Адаптив:** вместо балльного агрегата — подтверждённые уровни по темам (тема · уровень · статус подтверждён/идёт) | `cmi.score.*`, секц. результаты пакета, `passRule`, `levelsState` | НОВАЯ |
| 2 | Протокол | строка на вопрос: тема (и уровень для адаптива) · текст/тип · ответ · вердикт (верно/частично N%/неверно/нет ответа) · цена `score`/`sMax` и балл `earned`/`points` · сложность · вклады в шкалы `scaleKey ±delta`; итог-футер; экспорт CSV | `getDrawnRows`, `ScoringEngine.scoreAnswer`, `isActiveMeasure` | ПЕРЕИСПОЛЬЗОВАНИЕ (без селектора попытки CLI-плеера — прогоны одноразовые) |
| 3 | Выдача | РАСКЛАД выдачи (композиция, НЕ поимённый список — он в «Протоколе»): по темам — механизм (выпавший вариант «Вариант N» ИЛИ квоты план-vs-факт по тегам) + распределение выданного набора по тегам-подтемам и по типам вопросов + «выдано N из банка M». Сырой `formId` (внутр. ключ ротации/пин) на холсте НЕ показывается. **Адаптив:** путь по уровням каждой темы (шаг · уровень · ответ · переход) с пер-уровневым статусом и итоговым **подтверждённым уровнем**. Статусы пройденных тем при многих темах — в статусной строке (раскрытие «+N»), не здесь | `variant_json`, `draw_blueprint`, `q.tags`/`q.type`, `topicsState`/`levelsState` | НОВАЯ |
| 4 | Шкалы | таблица `Шкала \| Значение (raw) \| % \| Уровень`; «%» — только для `normalization="percent"` (иначе «—»); уровень — бэнды на raw. Публикация шкал в LMS (`scale_<key>=raw` + `_level`-строкой по `scormTarget`; raw, НЕ %; [resultsPage.js](../../../server/scorm/template/app/render/resultsPage.js) `buildScaleInteractions`, PRD-5 §8.2) НЕ дублируется здесь — проверяется во вкладке «LMS-журнал». Ошибки формул — алярм статусной панели | `computeTestScales()` | ПЕРЕИСПОЛЬЗОВАНИЕ + правка (raw→«Значение», БЕЗ LMS-колонки) |
| 5 | Показатели | таблица `Имя \| Значение`; пользовательские показатели (`result.*`, PRD-2). Публикация в LMS (`var_<имя>`) — во вкладке «LMS-журнал», не здесь; ошибки формул — алярм статусной панели | `computeTestResultVariables()` | ПЕРЕИСПОЛЬЗОВАНИЕ + правка (без LMS-колонки) |
| 6 | Состояние | сырой просмотр `state` / `suspend_data` (распарсенные `attempts`) / полная модель `cmi.*`; фильтр по пути; сырой JSON | `stage.contentWindow.state`, `cmi`, `suspend_data` | ПЕРЕИСПОЛЬЗОВАНИЕ (бывш. «Watch», переименование) |
| 7 | LMS-журнал | гуманизированная хронология (статус прохождения, итог, прогресс %, балл, шкалы, рекомендованные курсы/события); `suspend_data` — разворачиваемый ПОЛНЫЙ сериализованный payload + его размер (важен для лимитов LMS), не усечённый снимок; + сырые вызовы RTE с «скрыть GetValue» | трафик RTE-шима | ПЕРЕИСПОЛЬЗОВАНИЕ 1:1 |

### 5.3 Дельта к существующему плееру

- **Переиспользуется как есть (5):** Протокол, Шкалы, Показатели, LMS-журнал, Watch → «Состояние».
- **Переносится:** адаптив-бар → группа «Прогресс» статусной панели + детально во вкладку «Выдача»;
  «Сброс попытки» → тулбар окна (+ «Пересобрать»); тоггл «Инспектор» → круглый краевой тоггл
  на левом крае сайдбара (DS `ou-fab`); экспорт CSV — во вкладке «Протокол» (не в тулбаре, без дубля);
  WebTutor-мок-бар → опционально в тулбар (FR-15), не в показатели.
- **Новое (3):** постоянная статусная панель (§5.1); вкладка «Выдача»; вкладка «Результаты».
- **Не переносится:** выбор пакета из `out/` (в сервисе пакет собирается на лету).

«Выдача» — прямой ответ на запрошенный «набор вопросов к выдаче»: автор видит, что именно выпало и
почему (вариант/квоты) в ЭТОМ прогоне. Статистику распределения по N прогонам («аудит выдачи») не
вводим — проблемы выдачи детерминированы и ловятся валидацией редактора (§9, FR-11 снято).

### 5.4 Эталон (подсветка правильных ответов на стейдже)

Debug-тоггл «Эталон» в риббоне стейджа (по умолчанию ВЫКЛ — сначала проходишь как ученик). Подсвечивает
правильные ответы МЕЛКИМ маркером в левом жёлобе варианта, СНАРУЖИ варианта (на подсчёт баллов НЕ влияет).
Ключевое: метка кладётся на РЕАЛЬНЫЙ рендер варианта применённого шаблона (`.option` с радио/чекбоксом,
`.rank-item` с грипом, `.matching`-плитки), не на абстрактные плашки и не через тяжёлые шаблонные
`.correct-answer`/`.incorrect-answer` (они для ученического ревью, для отладки не годятся). Глиф по типу:

- одиночный / множественный — `✓` у правильного варианта;
- ранжирование — правильный порядковый `№` у каждого варианта;
- сопоставление — буквенное кодирование: ответам присвоены `A/B/C`, у каждого промпта — буква его
  правильного ответа (без линий-связей, которые громоздки);
- цена варианта (вес опции, PRD-10) — ТОЛЬКО на ОДИНОЧНОМ выборе (`weighted`, режим «Веса опций»:
  `score = вес выбранного`; `scoring-builder.tsx:105-107`, `schema.ts:201`, `engine.ts:15-16`): мелкая
  подпись «вес N» у каждой опции. Множественный/сопоставление/ранжирование оцениваются `tiered`
  (ступенчатые тиры по счётчикам `c`/`x`/total) — пер-вариантной цены у них НЕТ; на стейдже только
  маркер корректности, а сама тир-таблица — в «Протоколе»/настройках, не на варианте.

Данные «что верно» уже есть в контексте рендера (`correct` по варианту, настройка `showCorrectAnswers`,
`TEST_DATA`); рисуется ЛЁГКИМ debug-слоем общего шаблона (`shared/template`), единым для веб- и
SCORM-хостов. Маркеры — на токенах DS (success/accent); точная тема варианта = применённый шаблон в
рантайме.

---

## 6. Безопасность и изоляция

- **Доступ.** Та же ОБЛАСТЬ, что у экспорта — `requireTestScope("edit")`, отдельный grant или
  роль не вводится (D-2), — но собственное право `tests.debug.play`:
  `requirePermission("tests.debug.play")`. Разделение введено 2026-07-30 вместе с ролью
  «Разработчик»: генерация SCORM-пакета ушла к DV, а прогон в отладчике остался у Автора,
  иначе автор не смог бы проверить собственный тест. Право есть у AU, DV, AD и SU.
- **Токен сессии** привязан к пользователю и тесту; раздача файлов проверяет владение и TTL; чужой
  токен → `404`, просроченный → `410`.
- **Изоляция данных.** Нет записи в `attempts` и SCORM-телеметрию (NFR-19); состояние — только в
  `localStorage` плеера, неймспейсится по `(testId, userId, source)` и сбрасывается.
- **Экспозиция контента.** Как у обычного SCORM-пакета: в собранный пакет попадают вопросы и ключи
  (а в режиме вариантов PRD-17 — все варианты, R-7 PRD-17). Это тот же артефакт, что уходит в LMS,
  и доступен только держателю edit-доступа — новой утечки сверх экспорта нет.
- **Санитизация** HTML контент-страниц и слотов — на стороне `generateScormPackage` (NFR-05),
  плеер ничего не ослабляет.

---

## 7. UI и эскизы

Эскиз СОГЛАСОВАН (2026-06-27): [docs/wireframes/prd18-debug-player.html](../../wireframes/approved/prd18-debug-player.html)
(на DS-композиции `ou-*`/`tb-*`; faux-chrome окна `wf-*` как `.wf-drawer` в prd17 — в ПРОД-КОД не
переносятся, там каркас на DS-примитивах раскладки; DS-gate — на уровне канона). Фаза 0 ЗАКРЫТА;
UI-реализация РАЗБЛОКИРОВАНА (NFR-14, BR-13-09).

Эскиз покрывает состояния:

- основной сценарий: запуск из меню действий теста → плеер в ОТДЕЛЬНОМ ОКНЕ; макет окна (iframe
  пакета на применённом шаблоне + сворачивающийся сайдбар (поднят к верху окна) с вкладками
  инспектора + статусная панель НАД СТЕЙДЖЕМ в левой колонке);
- вкладки инспектора «Результаты» (первой, по умолч.) / «Протокол» / «Выдача» / «LMS-журнал» (+ «Шкалы»/
  «Показатели»/«Состояние»);
- свёрнутый сайдбар (статусная панель остаётся видимой);
- loading (сборка пакета идёт), ошибка сборки (неполный вариант / невалидный шаблон);
- ошибка формулы (алярм «Ошибка расчёта» на панели — в норме скрыт);
- **адаптивный режим**: панель «Прогресс · адаптив» (текущий уровень + подтверждённые уровни) +
  вкладка «Выдача» с путём по уровням; «много тем» — сворачивание/раскрытие статусов в строке;
- **Эталон** (§5.4): тоггл-свитч в риббоне подсвечивает правильные ответы на реальном рендере вариантов
  (одиночный/множественный — ✓; ранжирование — №; сопоставление — доска пар с буквами); цена ответа — в «Протоколе»;
- Пояснения + DS Mapping.

Раскладка РЕШЕНА (ревью 2026-06-27): плеер — ПОЛНОЭКРАННЫЙ инструмент (окно на всю ширину поля, не
центрированная колонка с пустыми полями), инспектор ШИРОКИЙ (~34% ширины окна), iframe пакета
занимает всю площадь стейджа. При такой ширине 7 ТЕКСТОВЫХ DS-вкладок (`ou-tabs--underline
ou-tabs--s`) помещаются в один ряд — узкий сайдбар (и вынужденная возня с иконочными вкладками /
переносом) был ОШИБКОЙ раскладки, а не ограничением.

РЕШЕНО на ревью 2026-06-27: статусная панель — НАД СТЕЙДЖЕМ в левой колонке (не сверху/снизу на всю
ширину), инспектор поднят к верху и занимает правую колонку; вкладка «Результаты» — первой и активной;
лента вкладок прокручивается стрелками при нехватке ширины; «Эталон» — на реальном рендере вариантов,
цена ответа — в «Протоколе». Остаётся к проработке на Фазе 4: экран «недоступно» (нет edit-доступа).

Перед эскизами — обязательное открытие DS-справочника
(`ENGINERING_HANDBOOK/design-system/{AI-AGENT,DESIGN_SYSTEM_RT,DESIGN_SYSTEM_RT_API}.md`); в холсте
эскиза — только реальный UI на DS `ou-*`/`tb-*`, пояснения — в таблицах `wf-notes`/`wf-mapping`.
Канва-источник терминов и стилей — `client/src/styles` (не устаревшие копии в docs).

---

## 8. Совместимость

- Никаких изменений учебного пути и схемы БД: плеер не трогает `attempts`, новые таблицы не вводит.
- Production-экспорт остаётся как есть по контракту; меняется только то, что его тело переезжает в
  переиспользуемый `buildScormExportData` (поведение эндпоинта `GET /:id/export/scorm` сохраняется,
  покрывается существующими acceptance-тестами SCORM).
- CLI-плеер `scorm-player.mjs` и генераторы `scorm:sample`/`scorm:template` сохраняются; плеер
  начинает потреблять общий модуль шима/инспектора (рефактор без потери функций).
- Снапшоты PRD-15: плеер их НЕ отлаживает — источник всегда живой (D-4). Отгружаемый пакет
  опубликованного теста по-прежнему собирается из снапшота; чтобы отлаженные правки ушли
  обучающемуся, тест нужно ПЕРЕОПУБЛИКОВАТЬ.

---

## 9. Вне охвата

- **Реальная приёмка в WebTutor** (SN-секвенсирование, cross-attempt-стор, реальный retake-gate по
  данным WebTutor) — не воспроизводится; остаётся за приёмкой в LMS (NFR-18, R-3). Доступен только
  МОК WebTutor-гейта из CLI-плеера для проверки экрана блокировки (FR-15).
- **Отладка опубликованного снапшота / выбор версии-снапшота** — вне охвата: плеер ВСЕГДА отражает
  живое состояние теста (решение 2026-06-26, D-4). Чтобы проверить именно отгружаемую версию
  published-теста — переопубликовать и/или воспользоваться обычным экспортом SCORM.
- **Сохранение/шеринг прогонов отладки** (история debug-сессий, сравнение прогонов) — прогоны
  одноразовые (решение Q3); при будущей потребности — отдельная задача.
- **Запись отладочной попытки в `attempts` с флагом `is_preview`** — отвергнуто (Q3): для
  SCORM-плеера состояние держит сам рантайм (localStorage), запись в БД избыточна.
- **Совместный/многопользовательский прогон** (несколько отладчиков в одной сессии) — вне v1.
- **Аудит выдачи (N-прогонная статистика распределения вариантов/квот)** — СНЯТО (FR-11): проблемы
  выдачи (квота недобирает / битый/пустой вариант / неравные размеры) ДЕТЕРМИНИРОВАНЫ и относятся к
  ВАЛИДАЦИИ РЕДАКТОРА (PRD-11 BR-10-04, PRD-17 §6/FR-18), N-прогонная симуляция её дублирует и
  значимой ценности не несёт. Узкий остаток (стохастика пересечения тегов / `min`-добор; паритет
  «собранный пакет == конфиг») — при будущей потребности отдельной QA-задачей, не в плеере.
- **Автоматические проверки «математики»** (ассерты на ожидаемые баллы/шкалы как тест-кейсы) — вне
  v1; возможное будущее.
- **Отладка медиа/тяжёлых ресурсов на пропускную способность** — вне охвата.

---

## 10. Решения и открытые вопросы

| # | Вопрос | Решение |
| --- | --- | --- |
| D-1 | Что запускать: веб-предпросмотр, встроенный SCORM-плеер или оба | Встроенный SCORM-плеер с инспектором реального времени (Q1 2026-06-26): нужна коммуникация с LMS/RTE, что даёт только SCORM-рантайм, а не веб-предпросмотр |
| D-2 | Модель доступа к отладке | Переиспользовать edit-доступ (владелец + грант `edit`), как у экспорта SCORM (Q2). Отдельный грант/роль/capability не вводится |
| D-3 | Персистентность прогонов | Полностью одноразовые (Q3): без записи в `attempts`/телеметрию; состояние в `localStorage`; протокол экспортируется CSV |
| D-4 | Версия теста для отладки | ТОЛЬКО живая версия (live storage, текущее редактируемое состояние) — переключателя на снапшот НЕТ (уточнение 2026-06-26: «все текущие изменения должны применяться в сессию»). Снапшот не отлаживается; published уходит обучающемуся из снапшота — для его проверки переопубликовать. Сборка — общий с production-экспортом сборщик |
| D-5 | Retake-gate в отладке | По умолчанию обход/`failOpen` (прогон не блокируется cooldown); опционально — прогон против МОКА WebTutor-гейта для проверки экрана блокировки (FR-15) |
| D-6 | Единый или отдельный код шима/инспектора с CLI-плеером | Единый общий модуль (FR-13), CLI-плеер рефакторится на его потребление; паритет — тестом-близнецом (как PRD-17) |
| D-7 | Размещение и макет плеера | Плеер открывается в ОТДЕЛЬНОМ ОКНЕ (уточнение 2026-06-26); макет: iframe пакета + СВОРАЧИВАЮЩИЙСЯ сайдбар с вкладками инспектора + фиксированная СТАТУСНАЯ панель НАД СТЕЙДЖЕМ в левой колонке (инспектор поднят к верху; ревью 2026-06-27) с основными показателями (FR-17/18/19) |
| D-8 | Шаблон оформления в прогоне | Пакет собирается с ПРИМЕНЁННЫМ к тесту шаблоном (`design_settings_json`), как production-экспорт — отладка на реальном визуальном шаблоне, не на демо/`default` (FR-20) |
| D-9 | Эталон (подсветка правильных ответов) — форма и место (ревью 2026-06-27) | Тоггл-СВИТЧ «Эталон» в риббоне стейджа (по умолч. ВЫКЛ, на баллы не влияет); лёгкий debug-маркер НА РЕАЛЬНОМ рендере вариантов (✓ — одиночный/множественный; № — ранжирование; буквы пар на доске — сопоставление), НЕ на абстрактных плашках и НЕ через тяжёлые `.option.correct-answer`. Цена ответа — в «Протоколе», не на вариантах. ФАКТ ПО КОДУ: пер-вариантная «Веса опций» = `weighted` = ТОЛЬКО одиночный; множественный/сопоставление/ранжирование = `tiered` (тиры по c/x, не пер-вариантная) — `scoring-builder.tsx:105-107`, `schema.ts:201`, `engine.ts:15-16` (FR-21, §5.4) |
| OQ-1 | TTL и лимит числа одновременных in-memory debug-сессий | Задать на фазе реализации (напр. TTL ~30 мин, вытеснение LRU); решить, переживает ли сессия HMR/перезапуск dev-сервера (по умолчанию — нет, прогон одноразовый) |
| OQ-2 | ~~Объём панели «Аудит выдачи»~~ — снято | «Аудит выдачи» убран из охвата (§9): диагностика выдачи — статическая валидация редактора, N-прогонная симуляция дублирует |
| OQ-3 | Куда вынести общий модуль плеера физически (`server/scorm/debug-player/` vs `shared/`) | Уточнить при рефакторинге `scorm-player.mjs`; критерий — общий потребитель CLI + сервер без дублирования |
| OQ-4 | ~~Прогон опубликованного снапшота / выбор версии снапшота~~ — снято | Отлаживается ТОЛЬКО живая версия (D-4); отладка снапшотов вне охвата (§9) |

---

## 11. Фазы реализации

| Фаза | Состав | Критерий завершения |
| --- | --- | --- |
| 0 ✓ ЗАКРЫТА | Этот PRD + эскиз плеера (`docs/wireframes/prd18-debug-player.html`, состояния по §7 + «Эталон» §5.4; на DS-компонентах ou-*/tb-*) — **СОГЛАСОВАН 2026-06-27** | PRD + эскиз согласованы (NFR-14) — выполнено |
| 1 ✓ ЗАКРЫТА | Рефактор сборки: вынесен `buildScormExportData(testId, {source})` из `GET /:id/export/scorm` (параметр источника сохранён); экспорт без изменения поведения; `debug/session` вызывает с живым источником (`server/scorm/build-export-data.ts` + `ScormBuildError`) | tsc/тесты зелёные; экспорт SCORM без регрессий — выполнено |
| 2 ✓ ЗАКРЫТА | Общий модуль плеера: RTE-шим + расчётный слой инспектора вынесены в `server/scorm/debug-player/assets/{shim,inspector-compute}.js` (compute на `window.TBInspector`); CLI-плеер инлайнит их, `inspector.js` стал тонким рендером; тест-близнец паритета | CLI-плеер работает на общем модуле; паритет под тестом — выполнено |
| 3 ✓ ЗАКРЫТА | Серверный плеер: роутер `debug-player.ts` (`session`/`play`/`session DELETE` + `shim.js`/`inspector-compute.js`), in-memory сессии по токену (TTL 30 мин, LRU 50, владение по userId), gate edit-scope, телеметрия выключена; пакет отдаётся VERBATIM, шим хостится В ОКНЕ-ПЛЕЕРЕ (SCO находит `API_1484_11` по `window.parent`, R-7) | прогон собирается и проигрывается через API; изоляция (нет записей в `attempts`/телеметрию) под тестом — выполнено |
| 4 ✓ ЗАКРЫТА | Клиент: ОКНО плеера (отдельное окно, full-screen: тулбар «Эталон»/«Сбросить» + статусная панель над стейджем + iframe + сворачивающийся сайдбар-инспектор, 7 вкладок Результаты/Протокол/Выдача/Шкалы/Показатели/Состояние/LMS на DS Tabs+`tb-table`) + пункт меню «Тестовый прогон»; экспорт CSV; «Эталон» §5.4 (✓/№/буквы пар на реальном рендере вопроса) | автор запускает и отлаживает тест из UI; панели показывают результат/баллы/шкалы/показатели/выдачу/LMS; проверено живым DEV-прогоном — выполнено |
| 5 | Документация границы точности (FR-14/NFR-18) + приёмка | приёмка по §12 BRD (Этап 14) пройдена |

---

## 12. Риски

| # | Риск | Суть | Митигация |
| --- | --- | --- | --- |
| R-1 | Дрейф встроенного и CLI-плеера | Две копии RTE-шима/инспектора разойдутся | Единый общий модуль (FR-13); тест-близнец паритета (как PRD-17 §11) |
| R-2 | Отладочный прогон в учебной аналитике/телеметрии | Случайная запись `attempts`/`scorm_*` загрязнит статистику | Сборка с `telemetry: null`; плеер не дёргает `/attempts/*`; состояние только в `localStorage`; тест-инвариант изоляции (NFR-19) |
| R-3 | «Прошло в плеере = пройдёт в WebTutor» | Автор примет успешный прогон за гарантию | Явная пометка границы точности (FR-14); документация (нет SN/cross-attempt/реального retake-gate); приёмка в WebTutor обязательна для таких механик |
| R-4 | Дубль сборки `ExportData` | Вторая копия логики сборки разойдётся с экспортом → отлаживается не то, что отгрузится | Единый `buildScormExportData` для экспорта и отладки (Фаза 1, NFR-18) |
| R-5 | Стоимость сборки на каждый прогон | Большой тест собирается ощутимо | Лимит NFR-02 (≤30 с); кэш сессии по токену; пересборка только по «Пересобрать»/смене версии |
| R-6 | Экспозиция ключей в режиме вариантов | Пакет несёт все варианты/ключи (R-7 PRD-17) | Тот же артефакт, что экспорт; доступен только edit-держателю; новой утечки сверх экспорта нет |
| R-7 | Same-origin доступ инспектора к iframe | Инспектор читает `window` пакета — ломается при смене origin | Раздача пакета тем же origin, что плеер (как в CLI-плеере); same-origin зафиксировать в реализации |
| R-8 | In-memory сессии и масштаб | Множество одновременных debug-сессий раздувают память | TTL + вытеснение (OQ-1); сессия одноразовая, переживать рестарт не обязана |
