# PRD-53. Профиль по группе шкал

Дата дизайна: 2026-09-04. Статус: СОГЛАСОВАНА, реализована в ветке `prd-53-scale-group-profile`;
приёмка в браузере пройдена частично (см. [§14](#14-состояние-на-2026-09-07)).
Смежные спеки: [PRD-44 «Вопрос-распределение баллов»](../prd-44/scale-allocation-question-type.md),
[PRD-5 «Шкалы и измерения компетенций»](../prd-5/scales-competency-measurements.md),
[PRD-2 «Показатели результата»](../prd-2/result-variables.md),
[PRD-29 «Итоги измерительного теста»](../prd-29/measurement-results.md),
[PRD-48 «Перенос теста без потерь»](../prd-48/test-transfer.md).

## 1. Задача

Типологический опросник толкует результат не по одной шкале, а по НАБОРУ шкал, оказавшихся наверху.
Опросник ЧИЛ («Определение ведущего стиля человекоцентричного лидерства») после пилота 2026-08 требует
именно этого: берём максимум из четырёх стилей, относим в «верхнюю зону» все, отставшие от него не
более чем на Δ = 5 баллов, и по составу зоны выдаём один из пятнадцати профилей — сфокусированный,
двухвекторный, широкий или сбалансированный.

Продукт такого понятия не знает. Он умеет толковать одну шкалу диапазонами, один показатель-строку
исходами и одно место в рейтинге через `topScale`. Множества шкал в модели нет.

Задача — дать автору первоклассную механику «профиль по группе шкал»: объявить группу, порог верхней
зоны и тексты для каждого сочетания, не прибегая ни к скриптам, ни к кодированию множества числом.

## 2. Как это решено сегодня и почему так нельзя оставлять

2026-09-04 методика V5 заведена на трёх установках КОНФИГУРАЦИЕЙ, без правок кода
(`.playwright-mcp/chil-v5-*`). Приём такой:

- множество из четырёх шкал кодируется числом 1..15 битовой маской (Ц=1, В=2, К=4, П=8) — ровно так,
  как считает сама книга-источник в ячейке `AC3`;
- показатель объявлен ЧИСЛОВЫМ, а толкование положено в `config_json.bands` пятнадцатью диапазонами
  вида `9..9`;
- работает это потому, что `buildMeasureView` ветвится по типу ЗНАЧЕНИЯ, а не по объявленному типу
  показателя ([shared/template/measure-view.ts](../../../shared/template/measure-view.ts)).

Обе опоры — детали реализации, а не обещания продукта. Что из этого следует для автора:

| Что автор видит или делает | Чем это оборачивается |
| --- | --- |
| Показатель типа «число» печатает абзац текста | Тип показателя перестал что-либо значить |
| Пятнадцать диапазонов `1..1` … `15..15` | Связь «9 = Целеустремленный + Процессный» не записана нигде |
| Порог Δ зашит в формулу четырьмя отдельными числами | Смена порога — правка формулы в четырёх местах |
| Тексты «других стилей» лежат пятнадцатью копиями | Описаний стилей всего четыре; порядок в них канонический, а методика требует по убыванию балла |
| Пятая шкала | 31 сочетание, собираемое скриптом заново |
| Перенос на другую установку | Книга Excel матрицу не везёт — стенд приехал без текстов (PRD-48) |

Временное решение принято владельцем осознанно и остаётся в силе до реализации этой спеки.

## 3. Решения владельца (2026-09-04)

Приняты в диалоге, пересмотру в ходе реализации не подлежат.

1. Движок живёт в ПОКАЗАТЕЛЯХ, а не отдельной сущностью «Типология»: новая сущность потянула бы за
   собой миграцию, снимки, книгу импорта, SCORM и аналитику, а показатель уже везде проведён.
2. Генератор матрицы обязателен, а не удобство: при пяти шкалах сочетаний 31, при шести — 63.
3. Порядок блоков итогов и правки вёрстки отчёта в этот трек НЕ входят (см. [§10](#10-вне-охвата)).

## 4. Модель

### 4.1. Верхняя зона и код набора

Группа — перечень ключей шкал. Шкала входит в верхнюю зону, если её НОРМАЛИЗОВАННОЕ значение отстало
от максимума по группе не более чем на порог:

```text
inTop(k)  ⟺  normalized(k) ≥ max(группа) − Δ
```

Нормализованное, а не сырое, — по той же причине, по которой на нём ранжирует `topScale` (PRD-44
FR-20): сырые значения шкал с разными доменами несопоставимы, а `direction: inverse` уже применён к
нормализованному. Граница включается: отставание ровно на Δ ещё считается верхней зоной.

Шкала без значения (`hasValue: false`) в группе не участвует — ни в максимуме, ни в наборе. Это то же
правило, по которому её исключает `rankScales`: неотвеченная шкала не имеет места в сравнении, а её
ноль занял бы низ рейтинга впереди действительно измеренных.

**Код набора** — ключи вошедших шкал, отсортированные в АВТОРСКОМ порядке шкал теста (`scales.sort_order`)
и соединённые знаком `+`:

```text
cel            один стиль
cel+pro        два
cel+vdo+kom    три
cel+vdo+kom+pro все четыре
```

Разделитель `+` однозначен: ключ шкалы подчиняется `^[a-z][a-z0-9_]{0,63}$` и знака `+` содержать не
может. Порядок канонический, чтобы одна и та же зона на всех хостах давала одну и ту же строку;
сравнение при этом всё равно ведётся по МНОЖЕСТВУ (см. [§4.3](#43-толкование-по-коду-набора)), потому
что автор вправе набрать код руками в другом порядке.

### 4.2. Источник DSL

Новый источник формулы:

```text
topGroup(["cel","vdo","kom","pro"], 5).code
topGroup(["cel","vdo","kom","pro"], "10%").count
```

Второй аргумент — порог:

- ЧИСЛО — абсолютный, в тех же единицах, что и значения шкал;
- СТРОКА вида `"N%"` — доля от максимума по группе. Нужна, когда шкалы группы имеют разные домены или
  домена не имеют вовсе; от домена не зависит и потому работает всегда.

Свойства:

| Свойство | Тип | Что несёт |
| --- | --- | --- |
| `code` | строка | Канонический код набора; `""` при пустой группе |
| `count` | число | Сколько шкал в верхней зоне; `0` при пустой группе |
| `max` | число | Максимум по группе; `0` при пустой группе |

**Свойства «названия шкал набора» НЕТ, и это не упущение.** Контекст вычислителя
(`EvalContext.scales`) несёт `ScaleResult`, у которого `label` — подпись УРОВНЯ («Высокий»), а не
название шкалы; названий шкал в контексте нет вовсе. Завести их значило бы расширить контракт
контекста и синхронно поправить обоих хостов (`buildEvalBase` и `buildResultVarContext`) ради строки,
которая всё равно нужна не в формуле, а в МЕТКЕ исхода. Метку составляет генератор матрицы
([§5.2](#52-генератор-матрицы)) — он работает в редакторе, где названия шкал под рукой, — и автор её
правит. Формуле название шкалы не нужно.

Пустая группа (ни одной шкалы со значением) не бросает исключение и не выдумывает набор — по общему
правилу вычислителя: ошибка формулы не должна ломать завершение попытки.

### 4.3. Толкование по коду набора

Показатель с `topGroup(...).code` объявляется СТРОКОВЫМ и толкуется существующим механизмом исходов
(`config_json.outcomes`), где `code` исхода — код набора.

Сравнение — по множеству, а не по строке: обе стороны разбираются по `+`, приводятся к каноническому
порядку и сравниваются. Иначе исход `pro+cel`, набранный автором вручную, молча не срабатывал бы.

Правило безопасно для показателей, к профилям отношения не имеющих: код без `+` нормализуется сам в
себя, и сравнение вырождается в прежнее точное равенство. Код с `+`, не являющийся набором ключей,
нормализуется одинаково с обеих сторон и потому тоже сравнивается верно.

**Запасной исход по размеру набора.** Число сочетаний растёт как 2ⁿ−1: четыре шкалы — 15, пять — 31,
шесть — 63. Заполнять 63 текста никто не станет, поэтому исход ищется в два шага:

1. точный код набора (`cel+pro`);
2. если такого нет — зарезервированный код `count:<N>`, где N — размер набора.

Автор шести шкал пишет четыре текста (`count:1` … `count:4`) и дополняет отдельные наборы по мере
надобности. Для ЧИЛ ничего не меняется: там заполнены все пятнадцать точных кодов.

### 4.4. Шкалы вне профиля

У показателя-профиля появляется свойство `config_json.restScales`:

```jsonc
{
  "restScales": {
    "show": true,
    "label": "Ознакомьтесь с другими стилями",
    "keys": ["cel", "vdo", "kom", "pro"]
  }
}
```

`keys` дублируют группу из формулы намеренно. Контекст отрисовки формулы не несёт — он получает уже
ПОСЧИТАННЫЕ значения, — и разбирать её на этапе показа значило бы тащить парсер в оба хоста ради
списка, который редактор и так знает. Список пишет редактор из той же формы, что порождает формулу.

Включённое, оно печатает ВТОРУЮ карточку сразу за карточкой профиля: перечень шкал группы, НЕ вошедших
в верхнюю зону, каждая — название и описание из самой шкалы (`scales.description`), в порядке убывания
значения.

Это снимает пятнадцать рукописных копий и заодно чинит порядок: сегодня текст привязан к коду и потому
перечисляет стили канонически, а методика требует по убыванию балла.

Пустой перечень (сбалансированный профиль — вошли все) карточку не печатает вовсе.

**Поле «Описание» появляется в редакторе шкалы.** Колонка `scales.description` существовала в базе и
переносилась книгой Excel (лист «Шкалы», колонка «Описание»), но в карточке шкалы поля НЕ БЫЛО: автор
не мог его заполнить нигде, кроме книги. То есть блок «вне профиля» в интерфейсе печатал бы одни
названия, а FR-13 работал бы только у тех, кто настраивал тест через Excel. Обнаружено 2026-09-06 при
реализации [§5.3.2](#532-форма-шаблона): предупреждение о пустых описаниях отправляло автора на
вкладку, где заполнять нечего.

Поле — многострочное, во всю ширину карточки, между «Ключ / Метка» и блоком расчёта: оно отвечает на
вопрос, ЧТО шкала измеряет, а не как считается. Пустое сохраняется как `null`, а не пустая строка —
колонка nullable, и книга читает её так же; иначе «стёр текст» и «никогда не писал» хранились бы
по-разному, а выглядели одинаково.

### 4.5. Чего в схеме НЕ меняется

**Миграции нет.** Профиль целиком выражается уже существующими колонками: правило — в
`result_variables.formula`, тексты — в `result_variables.config_json.outcomes`, настройки карточек —
там же (`showName`, `showLevel`, новое `restScales`). Ни новой таблицы, ни новой колонки.

## 5. Редактор автора

### 5.1. Пятый шаблон конструктора

Визуальный конструктор показателей ([result-variables-builder.ts](../../../client/src/features/tests/editor/result-variables-builder.ts))
знает четыре шаблона — «Порог», «Категория по уровням шкалы», «Взвешенная сумма», «Сертификация /
вердикт». Добавляется пятый: **«Профиль по группе шкал»**, тип результата — строка.

Форма шаблона:

| Поле | Контрол | По умолчанию |
| --- | --- | --- |
| Шкалы группы | Мультивыбор из шкал теста, не менее двух | пусто |
| Порог верхней зоны | Число + переключатель единиц «баллы / % от максимума» | 0 баллов |
| Показывать шкалы вне профиля | Тумблер | выключен |
| Заголовок блока «вне профиля» | Строка, активна при включённом тумблере | пусто |

Из формы генерируется каноничный DSL — так же, как остальные шаблоны генерируют `topicById(...)` и
`scaleById(...)`.

### 5.2. Генератор матрицы

Кнопка **«Собрать наборы»** рядом с редактором исходов. Строит все 2ⁿ−1 сочетаний выбранной группы и
заводит на каждое исход:

- `code` — канонический код набора;
- `label` — автоназвание по размеру и составу: «Двухвекторный: Целеустремленный и Процессный». Слова
  размера (сфокусированный / двухвекторный / широкий / сбалансированный) — предложенные, автор правит;
- `text` и `feedback` — пустые.

Правила поведения:

- существующие исходы не затираются: генератор ДОБАВЛЯЕТ недостающие коды и не трогает заполненные;
- при n ≥ 5 перед построением показывается предупреждение с числом строк и предложением обойтись
  запасными исходами `count:<N>`;
- отдельная кнопка **«Заготовки по размеру набора»** заводит только четыре строки `count:1` … `count:4`.

### 5.3. Ошибки и предупреждения

У профиля скверный отказ: он не падает. Если для набора участника нет текста, ничего не сломается и
никто не увидит ошибки — участник просто получит карточку без толкования, а автор узнает об этом от
участника. Поэтому диагностика здесь подробнее, чем у остальных шаблонов, и описана целиком.

**Строгость.** Ошибка означает: настройка не может работать, и сохранение отклоняется
(`422`, `{ error: "Невалидная формула", field: "formula", validation }` — см.
[server/routes/result-variables.ts](../../../server/routes/result-variables.ts)). Предупреждение
означает: настройка законная, но почти наверняка не та, что автор имел в виду; сохранение проходит,
публикация не блокируется. Подсказка — арифметика, о которой автор не думал; ни то, ни другое не
мешает.

**Три источника, потому что они знают разное.** Валидатор формулы видит только строку источника.
Форма шаблона видит вдобавок исходы показателя, названия и описания шкал. Рантайм видит ответы
участника. Проверку из одного источника нельзя перенести в другой: коды исходов приходят из ДАННЫХ
показателя, а не из формулы, поэтому покрытие наборов валидатор проверить не может в принципе.

#### 5.3.1. Валидатор формулы

[shared/formula/validate.ts](../../../shared/formula/validate.ts), двойник —
[server/scorm/template/app/dsl/formula.js](../../../server/scorm/template/app/dsl/formula.js).
Срабатывает на каждое изменение источника (в редакторе — с задержкой 400 мс через
`POST /api/tests/:id/result-variables/validate-formula`) и повторно на сохранении.

| Код | Строгость | Условие | Сообщение |
| --- | --- | --- | --- |
| `scale-group-small` | Ошибка | В группе меньше двух шкал | «В группе профиля нужны хотя бы две шкалы» |
| `scale-group-threshold` | Ошибка | Порог не разобрался: доля вне `0…100%` или негодная строка. ОТРИЦАТЕЛЬНОЕ число сюда не доходит — `topGroup([...], -1)` не принимает парсер, и находка выходит как `syntax` | «Порог верхней зоны — неотрицательное число или строка вида «10%»» |
| `unknown-scale` | Ошибка | Ключа группы нет среди шкал теста | «Неизвестная шкала «…»» |
| `syntax` | Ошибка | Источник не разобрался парсером | Текст разборщика с позицией |
| `type-mismatch` | Ошибка | `topGroup(...).code` при объявленном числовом типе показателя (и наоборот для `count`/`max`) | «Формула возвращает …, а тип показателя — …» |
| `var-order` | Ошибка | Профиль ссылается через `var()` на показатель, считающийся позже | «…» (общее правило PRD-2: граф показателей ациклический) |
| `scale-group-normalization` | Предупреждение | Порог АБСОЛЮТНЫЙ, а шкалы группы нормализованы по-разному | «Шкалы группы нормализованы по-разному: порог в баллах сравнивает несопоставимые величины. Задайте порог долей от максимума» |
| `scale-group-code` | Предупреждение | Используется свойство `.code` | «Коды исходов должны быть наборами ключей шкал через «+»» |
| `scale-unresolved` | Предупреждение | Шкалы теста недоступны проверке (`refs.scaleKeys` пуст) | «…» (общее для всех источников по шкалам) |

Три уточнения к таблице.

`scale-group-threshold` срабатывает только на то, что ПАРСЕР пропустил: долю вне диапазона и
негодную строку. Отрицательное число до него не доходит — `topGroup([...], -1)` парсер отвергает,
и по формуле выходит `syntax`. Поэтому форма шаблона проверяет НАБРАННОЕ значение отдельно, тем же
`parseGroupThreshold` и тем же текстом (`PROFILE_THRESHOLD_MESSAGE`): иначе автор, набравший «-1»,
увидел бы жалобу на синтаксис формулы, которую он не писал.

`scale-group-normalization` — предупреждение, а НЕ ошибка, и это осознанно: смешанная нормализация
законна, порог просто перестаёт значить то, что автор думает. Условие сужено до абсолютного порога:
долевой порог сравнивает отношения и от разной нормализации свободен, поэтому при `"N%"` сообщение
не выдаётся.

`scale-group-code` выдаётся ВСЕГДА, когда взято свойство `.code`, — это не находка, а памятка для
того, кто пишет источник руками в режиме DSL. В форме шаблона коды заводит генератор, памятка там
избыточна, поэтому в конструкторе она подавляется (FR-27).

#### 5.3.2. Форма шаблона

Считается в редакторе, на данных показателя и шкал. В валидатор не переносится: там этих данных нет.

| Условие | Строгость | Сообщение |
| --- | --- | --- |
| В группе n ≥ 5 шкал | Подсказка | «Пять шкал дают 31 набор — столько текстов пишут редко. Можно обойтись заготовками по размеру набора: пять текстов вместо тридцати одного» (числа подставляются) |
| Есть код набора, не покрытый ни точным исходом, ни `count:<N>` | Предупреждение | «Не для всех наборов есть текст». В описании — первые пять непокрытых кодов и «и ещё N» |
| `restScales.show` включён, а у шкалы группы пустое `description` | Предупреждение | «Блок «вне профиля» напечатает голые названия». В описании — имена этих шкал и указание, что текст правится на вкладке «Шкалы» |
| В группе n > 12 шкал | Ошибка | Генератор наборов отказывается строить матрицу: `subsetCodes` выше `MAX_GROUP_FOR_SUBSETS` возвращает пустой список. Доступны только заготовки по размеру набора |

Покрытие считается тем же сопоставлением, что и в рантайме (`findOutcome` из
[shared/scales/interpretation.ts](../../../shared/scales/interpretation.ts)), а не сравнением строк:
иначе исход `pro+cel` считался бы непокрытым, хотя срабатывает. Набор считается покрытым, если для
него есть точный код ИЛИ запасной `count:<N>` его размера.

#### 5.3.3. Рантайм

| Условие | Что видит участник |
| --- | --- |
| Ни точного исхода, ни `count:<N>` | Карточка профиля с названием и без толкования — тот самый молчаливый отказ, ради которого написан §5.3.2 |
| Ни одна шкала группы не имеет значения | Профиль пуст (`code:""`, `count:0`), карточка не печатается ([FR-21](#8-функциональные-требования)) |
| `restScales.show`, но все шкалы группы вошли в профиль | Вторая карточка не печатается ([FR-14](#8-функциональные-требования)) |

Рантайм ничего не сообщает автору: сообщать некому и поздно. Всё, что можно поймать заранее,
ловится формой.

#### 5.3.4. Как это показывается

**Место зависит от того, есть ли у сообщения поле.** Ошибку, которую автор исправляет в конкретном
поле формы, печатать баннером вредно: сообщение оказывается далеко от места правки, а поле выглядит
исправным. Поэтому:

- сообщение, приписанное полю, печатается У ПОЛЯ — `ou-formfield__msg--error`, а само поле получает
  `ou-field--error`. Так выводятся `scale-group-small` (у поля «Шкалы группы») и
  `scale-group-threshold` (у поля «Порог верхней зоны»);
- сообщение без своего поля печатается баннером над блоком «Исходы». Так выводятся `unknown-scale`
  (шкалу удалили из теста — поля у неё уже нет) и все находки формы из [§5.3.2](#532-форма-шаблона).

Шкала группы, удалённая из теста, всё равно ПОКАЗЫВАЕТСЯ в списке — отключённой строкой с ключом и
пометкой «шкала удалена из теста». Иначе автор видит ошибку про ключ `eng`, но не видит самого `eng`
и не может его снять.

Баннеры идут в одном порядке: ошибки, предупреждения, подсказки. Внутри группы — в порядке появления.

- Ошибка — `ou-banner--error`, `role="alert"`, с заголовком.
- Предупреждение — `ou-banner--warning`, `role="status"`, с заголовком.
- Подсказка — `ou-banner--warning`, `role="status"`, без заголовка (одна строка описания).

Точка статуса в шапке карточки показателя следует худшему ярусу: ошибка — `tb-status-dot--err`.

Показываются ВСЕ сообщения, а не первое. Сегодняшний `toBanner`
([result-variables-section.tsx](../../../client/src/features/tests/editor/sections/result-variables-section.tsx))
печатает один баннер: первую ошибку, иначе первое предупреждение, и предупреждение — тоном `info`.
Для профиля этого мало: непокрытые наборы, пустые описания и разная нормализация — независимые
находки, и автор должен видеть их разом (FR-26).

### 5.4. Где автор пишет тексты профиля

У исхода ДВА текста, и они живут в разных местах редактора. Это не деление ради деления: они отвечают
на разные вопросы и пишутся в разное время.

| Текст | Поле исхода | Где правится | Что отвечает |
| --- | --- | --- | --- |
| Толкование | `text` | Карточка исхода в показателе ([§5.2](#52-генератор-матрицы)) | Что означает этот профиль |
| Рекомендации | `feedback` | Вкладка «Обратная связь и итоги» → рейл «Обратная связь», карточка «По профилям» | Что участнику с этим делать |

Толкование — часть определения исхода: без него исход не имеет смысла, и правится он там же, где код
набора и метка. Рекомендации — обратная связь, и место у неё общее со всей остальной обратной связью
теста: тексты теста, тем, подтем и уровней уже собраны на одном экране, и автор, севший писать тексты,
не должен ходить за ними по вкладкам. Отсюда и `feedback` вместо `text`: это тот же блок с текстом,
ссылками и вложениями, что у остальных источников обратной связи (PRD-32/PRD-42).

**Карточка «По профилям».** Повторяет `BreakdownFeedbackCard` (PRD-50) и стоит в рейле между «По
темам» и «По подтемам»:

- секция на КАЖДЫЙ показатель-профиль, заголовок секции — метка показателя;
- строка на каждый исход этого показателя, подпись строки — метка исхода;
- в строке — предпросмотр обратной связи с карандашом; пустое поле показывает тот же карандаш, что
  заполненное, иначе незаполненная строка читается как неактивная;
- запасные исходы `count:<N>` стоят в списке наравне с точными: у них тоже своя рекомендация.

Порядок строк — порядок исходов в показателе, то есть порядок, в котором их завёл генератор: сначала
наборы из одной шкалы, затем из двух и так далее.

**Сопоставление — то же.** Рекомендация ищется тем же `findOutcome`, что и толкование
([§4.3](#43-толкование-по-коду-набора)): сначала точный код набора, затем запасной `count:<N>`.
Разойтись они не могут — иначе участник получил бы толкование одного профиля и рекомендации другого.

**Пустое поле ничего не печатает.** Исход без `feedback` даёт карточку итогов без блока рекомендаций,
а не пустой блок с заголовком.

## 6. Книга Excel

**Нового листа не нужно.** Лист «Исходы показателей» (PRD-48) уже переносит исходы с ПРОИЗВОЛЬНЫМ
кодом — колонка «Код» это свободный текст, — поэтому `cel+pro` и `count:2` он повезёт без единой
правки. Группу и порог везёт формула на листе «Показатели».

Не хватает ровно одного: **колонки «Рекомендации»**. Сегодня обратная связь исхода из книги исключена
осознанно («книга — формат для текста, остальное несёт пакет `.tbtest`»), но у профиля рекомендации
это второй крупный текст методики, и без них лист переносит половину.

| Колонка | Было | Станет |
| --- | --- | --- |
| Показатель, Код, Метка, Текст, Тональность | есть | без изменений |
| Рекомендации | нет | текст обратной связи исхода (`feedback.text`) |

Колонка подчиняется общему правилу слияния этого листа: колонка, которая в книге ЕСТЬ, задаёт своё
поле целиком (опустошённая ячейка стирает текст), колонка, которой НЕТ, оставляет сохранённое
значение. Вложения, ссылки и формат обратной связи книга по-прежнему не везёт — они переносятся
вместе с прочим содержимым и при слиянии сохраняются.

## 7. Совместимость

### 7.1. Прошлые попытки

Экран итогов берёт из попытки ЗНАЧЕНИЯ, а толкование — из ТЕКУЩЕЙ конфигурации показателя. Значит при
переводе теста на новую механику значения прошлых попыток остаются прежними:

- попытки до 2026-09-04 несут `lead_style` СТРОКОЙ (`"kom"`) — их читают старые исходы;
- попытки после — ЧИСЛОМ (код маски `9`) — их читают диапазоны временного решения;
- попытки после перевода на PRD-53 — СТРОКОЙ-кодом набора (`cel+pro`) — их читают новые исходы.

Все три толкования уживаются в одной записи: диапазоны отвечают на число, исходы — на строку, а
исходы старой и новой формы различаются кодом. Требование: **скрипт перевода не затирает ни старые
исходы, ни диапазоны**.

### 7.2. Показатель без значения печатает пустую карточку

Показатель, заведённый ПОСЛЕ попытки, значения в ней не имеет, и карточка рендерится пустой — примерно
49 px пустого места. Обнаружено 2026-09-04 при добавлении показателя «Резюме профиля» к тесту с 25
завершёнными попытками.

Это общий дефект, а не свойство профиля, но перевод ЧИЛ на PRD-53 гарантированно его вызовет, поэтому
он чинится здесь: измерение без значения карточку не печатает.

### 7.3. Двойник рантайма

Разбор и вычисление продублированы в
[server/scorm/template/app/dsl/formula.js](../../../server/scorm/template/app/dsl/formula.js) —
plain-JS двойник для SCORM-пакета. Новый источник обязан появиться там же, а паритет — держаться
золотым корпусом (PRD-2 §12). Расхождение двойников означает, что пакет и веб посчитают профиль
по-разному, и заметят это только на стенде LMS.

### 7.4. Переименование шкалы

Ключ шкалы фигурирует в формуле списком. [rename.ts](../../../shared/formula/rename.ts) сегодня
переписывает только `topicByName`. Ключ шкалы не переименовывается ни сейчас, ни этой спекой — правило
остаётся прежним: ключ это идентификатор, менять его нельзя.

## 8. Функциональные требования

| Номер | Требование |
| --- | --- |
| FR-01 | Источник `topGroup(keys, порог)` разбирается парсером; ключи — непустой список строк |
| FR-02 | Порог принимает число (абсолютный) и строку `"N%"` (доля от максимума по группе) |
| FR-03 | Свойства: `code` (строка), `count` и `max` (числа). Названий шкал источник не отдаёт |
| FR-04 | Шкала входит в верхнюю зону при `normalized ≥ max − Δ`; граница включается |
| FR-05 | Шкала без значения не участвует ни в максимуме, ни в наборе |
| FR-06 | Код набора канонический: ключи в авторском порядке шкал теста, через `+` |
| FR-07 | Пустая группа даёт `code:""`, `count:0`, `max:0` и не бросает исключение |
| FR-08 | Исход сопоставляется по МНОЖЕСТВУ: порядок ключей в коде исхода значения не имеет |
| FR-09 | При отсутствии точного кода ищется запасной исход `count:<N>` |
| FR-10 | Валидатор проверяет: ключи существуют, шкал в группе ≥ 2, порог ≥ 0 |
| FR-11 | Валидатор предупреждает о разной нормализации шкал группы — только при абсолютном пороге |
| FR-12 | `config_json.restScales` печатает вторую карточку со шкалами вне профиля |
| FR-13 | Шкалы вне профиля идут по убыванию значения, текст берётся из `scales.description` |
| FR-14 | Пустой перечень «вне профиля» карточку не печатает |
| FR-15 | Конструктор показателей получает шаблон «Профиль по группе шкал» и генерирует каноничный DSL |
| FR-16 | Кнопка «Собрать наборы» заводит недостающие исходы и не трогает заполненные |
| FR-17 | При n ≥ 5 генератор предупреждает о числе строк и предлагает `count:<N>` |
| FR-18 | Кнопка «Заготовки по размеру набора» заводит четыре исхода `count:1` … `count:4` |
| FR-19 | Лист «Исходы показателей» получает колонку «Рекомендации» (`feedback.text`); нового листа нет |
| FR-20 | Двойник рантайма поддерживает `topGroup`; паритет закреплён золотым корпусом |
| FR-21 | Измерение без значения не печатает карточку (см. [§7.2](#72-показатель-без-значения-печатает-пустую-карточку)) |
| FR-22 | Скрипт перевода ЧИЛ не затирает старые исходы и диапазоны прошлых попыток |
| FR-23 | Схема БД не меняется: ни новой таблицы, ни новой колонки, ни миграции |
| FR-24 | Форма предупреждает о наборах без текста и перечисляет непокрытые коды; покрытие считается тем же `findOutcome`, что и в рантайме |
| FR-25 | Форма предупреждает о пустом `description` шкалы группы при включённом блоке «вне профиля» и называет эти шкалы |
| FR-26 | Показываются ВСЕ сообщения профиля разом, в порядке «ошибки, предупреждения, подсказки», а не первое |
| FR-27 | Памятка `scale-group-code` подавляется в конструкторе: коды заводит генератор |
| FR-28 | Группа больше `MAX_GROUP_FOR_SUBSETS` (12 шкал) матрицу не строит; доступны только заготовки по размеру набора |
| FR-29 | Рекомендации исхода правятся в рейле «Обратная связь», карточка «По профилям»; толкование остаётся в карточке исхода |
| FR-30 | В карточке «По профилям» строка на каждый исход показателя, включая запасные `count:<N>`, в порядке исходов |
| FR-31 | Рекомендация ищется тем же `findOutcome`, что и толкование; исход без `feedback` блока рекомендаций не печатает |
| FR-32 | Ошибка со своим полем печатается у поля, остальные — баннером; точка статуса карточки следует худшему ярусу |
| FR-33 | Шкала группы, удалённая из теста, показывается отключённой строкой с ключом и пометкой, а не исчезает из списка |
| FR-34 | Карточка шкалы получает поле «Описание»; пустое сохраняется как `null`, круг «редактор → API → редактор» замкнут |

## 9. Приведение опросника ЧИЛ

Отдельным шагом трека, после реализации движка. Что делает скрипт перевода:

1. `lead_style`: тип `number` → `string`, формула — `topGroup(["cel","vdo","kom","pro"], 5).code`,
   пятнадцать диапазонов переносятся в исходы с кодами наборов. Старые исходы (`cel`/`vdo`/`kom`/`pro`
   от версии до пилота) и диапазоны маски ОСТАЮТСЯ — их читают прошлые попытки.
2. `other_styles` упраздняется: его работу берёт `restScales` показателя-профиля. Четыре описания
   стилей переезжают в `scales.description`, откуда блок их и собирает. Сам показатель гасится, а не
   удаляется, — его значения лежат в прошлых попытках.
3. `profile_summary` остаётся отдельным показателем: фраза «У вас наиболее выражены два стиля: …» —
   требование прототипа отчёта. Формула у него та же `topGroup(...).code`, а пятнадцать фраз лежат
   его собственными исходами. Своего расчёта он не ведёт: `lead_style` читает его через `var()`, как
   и сегодня, — правило верхней зоны остаётся в базе в единственном экземпляре.
4. Три установки приводятся одним и тем же скриптом: dev, `test-dev.edtech-rtk.ru`
   (`test_builder_test`) и `test.edtech-rtk.ru` (`scormDb`).

## 10. Вне охвата

- **Порядок и состав блоков итогов.** По прототипу отчёта «Ознакомьтесь с другими стилями» идёт ПОСЛЕ
  рекомендаций; сегодня оба показателя печатаются одним блоком, а место блока рекомендаций задано
  макетом шаблона — [results-order.ts](../../../shared/template/results-order.ts) знает только
  `summary/scales/indicators/topics/breakdown`. Отдельный трек.
- **Правки вёрстки отчёта из «Корректировки по скиллуму».** Подписи баллов на секторах розы, альбомная
  ориентация и кегль, «едет вёрстка», разнобой шрифтов плашек. Отдельный трек.
- Профиль по набору ПОКАЗАТЕЛЕЙ (не шкал).
- Пороги, зависящие от испытуемого (нормы по выборке, стены, T-баллы).
- Адаптивные тесты: группа шкал в адаптивном режиме не рассматривается.

## 11. Риски и ограничения

1. **Рост матрицы.** 2ⁿ−1 при семи шкалах — 127 сочетаний. Запасные исходы по размеру набора делают
   такой опросник выразимым, но осмысленность стольких профилей — вопрос методики, а не продукта.
   Продукт предупреждает и не запрещает.
2. **Порог в баллах у шкал с разными доменами.** Формально допустим, содержательно почти всегда
   ошибка. Отсюда предупреждение валидатора и наличие процентной формы порога.
3. **Расхождение двойников DSL.** Веб считает `shared/formula`, пакет — `formula.js`. Ошибка проявится
   только в LMS. Смягчение — золотой корпус, тот же приём, что для существующих источников.
4. **`scales.description` начинает читать ученик.** Сегодня это авторское поле, и в существующих
   тестах там может быть служебный текст. Блок «вне профиля» выключен по умолчанию, а валидатор
   предупреждает о пустых описаниях; но автор, включивший тумблер, должен описания перечитать.
5. **Прошлые попытки живут на трёх толкованиях сразу.** Запись показателя после перевода несёт и
   диапазоны, и два поколения исходов. Это осознанная цена за то, что человек, открывший свой прошлый
   результат, видит его прежним, а не сырым ключом шкалы.

## 12. Этапы

| Этап | Содержание |
| --- | --- |
| Э0 | Эскизы: форма шаблона «Профиль по группе шкал», генератор матрицы, карточка «вне профиля» |
| Э1 | Движок: `topGroup` в парсере, вычислителе, валидаторе; сопоставление исхода по множеству; запасной `count:<N>` |
| Э2 | Двойник рантайма `formula.js` + золотой корпус паритета |
| Э3 | Карточка «вне профиля» (`restScales`), поле «Описание» в карточке шкалы и правка FR-21 (измерение без значения) |
| Э4 | Редактор: пятый шаблон конструктора, генератор матрицы, диагностика формы ([§5.3.2](#532-форма-шаблона), [§5.3.4](#534-как-это-показывается)), карточка «По профилям» в рейле обратной связи ([§5.4](#54-где-автор-пишет-тексты-профиля)) |
| Э5 | Книга Excel: колонка «Рекомендации» на листе исходов, выгрузка и загрузка |
| Э6 | Приведение ЧИЛ на трёх установках, доки, приёмка |

## 13. Критерии приёмки

1. Автор заводит показатель шаблоном «Профиль по группе шкал» на четырёх шкалах с порогом 5 баллов,
   жмёт «Собрать наборы» и получает пятнадцать исходов с проставленными кодами и названиями.
2. Повторное нажатие «Собрать наборы» после заполнения текстов ничего не затирает.
3. Контрольное заполнение книги ЧИЛ (42/7/7/42) даёт код `cel+pro`, профиль «Двухвекторный:
   Целеустремленный и Процессный» и карточку «вне профиля» с Вдохновляющим и Командным в порядке
   убывания балла.
4. Заполнение, где все четыре шкалы в пределах 5 баллов, даёт код из четырёх ключей и НЕ печатает
   карточку «вне профиля».
5. Заполнение, где отставание ровно 5, включает шкалу в набор; отставание 6 — не включает.
6. Исход, набранный автором как `pro+cel`, срабатывает на наборе `cel+pro`.
7. Тест с шестью шкалами и только четырьмя исходами `count:1` … `count:4` выдаёт текст на любое из 63
   сочетаний.
8. Показатель, заведённый после завершения попытки, в её итогах карточку не печатает.
9. Выгрузка книгой и загрузка в чистую установку переносят матрицу профилей целиком; экран итогов там
   совпадает с исходным.
10. Одно и то же заполнение даёт один и тот же код в вебе и в SCORM-пакете (сверка на стенде WebTutor).
11. Прошлая попытка, завершённая до перевода, по-прежнему печатает свой прежний текст, а не сырой код.
12. Группа из одной шкалы, порог `-1` и ключ несуществующей шкалы дают ошибку и НЕ сохраняются:
    первые две названы у своих полей, третья — баннером, удалённая шкала видна отключённой строкой.
13. Группа из пяти шкал, где заполнены только `count:1` и `count:2`, даёт предупреждение с перечнем
    непокрытых кодов и числом «и ещё N»; после «Заготовок по размеру набора» предупреждение уходит.
14. Группа из шкал с разной нормализацией и абсолютным порогом даёт предупреждение, но СОХРАНЯЕТСЯ;
    тот же набор с порогом `"10%"` предупреждения не даёт.
15. Включённый блок «вне профиля» при пустом описании шкалы даёт предупреждение с её именем.
16. Автор заполняет описание шкалы в её карточке, сохраняет и видит текст в блоке «вне профиля» у
    участника; после очистки поля блок печатает одно название, а форма профиля предупреждает об этом.
17. Три независимые находки (непокрытые наборы, пустое описание, разная нормализация) показываются
    одновременно, а не по одной.
18. Рекомендации пятнадцати профилей ЧИЛ правятся в рейле «Обратная связь», карточка «По профилям»;
    строки идут в порядке исходов, у незаполненных виден карандаш.
19. Исход с толкованием, но без рекомендаций печатает у участника толкование и НЕ печатает пустой
    блок рекомендаций.
20. `npm run check` и `npm test` зелёные; схема БД не изменилась.

## 14. Состояние на 2026-09-07

Ветка `prd-53-scale-group-profile`. План реализации —
[docs/plans/2026-09-04-prd53-scale-group-profile.md](../../plans/2026-09-04-prd53-scale-group-profile.md).

| Этап | Состояние |
| --- | --- |
| Э1 движок (`topGroup`, парсер, вычислитель, валидатор) | ГОТОВО, 33 теста |
| Э2 двойник рантайма и золотой корпус | ГОТОВО, 62 случая паритета |
| Э3 карточка «вне профиля», поле «Описание» шкалы, FR-21 | ГОТОВО, оба хоста |
| Э4 редактор | ГОТОВО: пятый шаблон, генератор матрицы, диагностика формы, блок «вне профиля»; 20 приёмочных тестов |
| Э5 книга Excel | ГОТОВО, 8 тестов |
| Э6 перевод ЧИЛ | Скрипт проверен сухим прогоном И применён набело — на КОПИИ ЧИЛ (`a0dca3f5-c58b-4222-9894-eff70fe83690`, dev); на трёх установках НЕ ПРИМЕНЁН |
| Э7 доки и приёмка | ЧАСТИЧНО: спека и руководство автора обновлены; приёмка в браузере пройдена НЕ ЦЕЛИКОМ (см. ниже) |

Два ограничения, определяющие порядок дальнейших работ.

**Приёмка в браузере пройдена частично (2026-09-07).** Проход делался на сервере ветки против
согласованных эскизов и живого опросника ЧИЛ. Чтобы не трогать рабочий тест dev, его КОПИЯ была
заведена штатным переносом (`POST /api/tests/transfer`) из выгрузки `.tbtest` и переведена
скриптом только она; исходный `26553608-…` остался нетронут.

ПРОЙДЕНО:

- редактор: пятый шаблон в конструкторе; форма (шкалы группы тумблерами с ключами, порог с
  переключателем «баллы / % от максимума», пояснение с подстановкой порога);
- генератор: «Собрать наборы» на четырёх шкалах даёт ровно 15 исходов с кодами `cel`, `cel+vdo`,
  … `cel+vdo+kom+pro` и метками «Сфокусированный: …», «Двухвекторный: … и …», «Широкий: …»,
  «Сбалансированный: …» — совпало с эскизом дословно (критерий приёмки 1);
- перечень исходов: сворачиваемые карточки, своя пара «Развернуть все / Свернуть все», в карточке
  «Название для обучающегося», «Код набора» с подсказкой, тон, толкование;
- поле «Описание» в карточке шкалы с подсказкой про блок «вне профиля» (FR-34);
- скрипт перевода: сухой прогон и применение набело на копии;
- ЭКРАН ИТОГОВ веб-хоста на реальной попытке. Шкалы 35 / 35 / 21 / 7 при пороге 5 дали верхнюю
  зону из двух шкал, `profile_summary` вернул КОД НАБОРА `"cel+pro"`, исход подобрался по нему;
  напечатаны текст профиля, «Описание ваших результатов» и блок «вне профиля» — Командный (21),
  затем Вдохновляющий (7), по убыванию балла, текстами ИЗ ОПИСАНИЙ ШКАЛ (критерии 6, 16).

НЕ ПРОЙДЕНО: состояния эскиза «предупреждения» и «обратная связь»; отчёт PDF; SCORM-пакет и
сверка на стенде WebTutor (критерий 10). До них трек закрытым считать нельзя.

НАХОДКИ приёмки — четыре, все по внешнему виду формы, ни одна не трогает механику:

| № | Эскиз | Реализация |
| --- | --- | --- |
| F1 | заголовок блока «Исходы» | «Толкование результата» — общий заголовок блока толкования у ВСЕХ показателей |
| F2 | три кнопки под пустым блоком | «Собрать наборы» и «Заготовки…» над блоком, «Добавить исход» под ним |
| F3 | баннера нет | «Формула возвращает код исхода…» — общий пояснитель строковых показателей, существовавший до PRD-53 |
| F4 | подзаголовок карточки «строка · конструктор» | «строка · порядок 7» |

F1–F3 — места, где эскиз назвал по-своему блок, общий для всех показателей: переименовать его
ради одного шаблона значило бы дать одному блоку два имени. Решения владельца ждут.

РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-09-07: ошибка, у которой есть своё поле, печатается И у поля, И баннером —
это НОРМА, а не дубль. Поведение соответствует замыслу FR-32 в паре с общим контуром индикации:
карточка бывает ниже сгиба, и один баннер вверху формы до неё автора не доводит.

**Два эскиза согласованы 2026-09-06** и лежат в `docs/wireframes/approved/`:
`prd53-profile-indicator.html` (пять состояний карточки показателя) и
`prd53-scale-description.html` (поле «Описание» в карточке шкалы).

**Карточка «По профилям» из [§5.4](#54-где-автор-пишет-тексты-профиля) НЕ реализована.**
Рекомендации исхода сегодня правятся там же, где у остальных показателей, — кнопкой
«Рекомендации» в строке исхода. Переезд в рейл «Обратная связь» описан, нарисован и ждёт своей
задачи.

**Рейл внутри рейла остаётся.** Вкладка «Оценка результата» → «Шкалы» открывает второй рейл
внутри первого: два по 240px съедают больше половины ширины ящика, и сильнее всего это давит на
матрицу вкладов, которая шире всего. Владелец выбрал вложенные пункты в ОДНОМ рейле; примитива
для этого в ДС нет, задача передана в трек ui-kit.

**Перевод ЧИЛ применяется ТОЛЬКО после выката.** Формулу `topGroup` понимает лишь код этой ветки:
dev-база общая для всех рабочих копий, а на удалённых установках развёрнута сборка без источника.
Применение раньше оставит тест без результата.

Пока перевод не применён, на трёх установках действует временное конфигурационное решение
(см. [§2](#2-как-это-решено-сегодня-и-почему-так-нельзя-оставлять)), принятое в браузере:

| Установка | База | Тест |
| --- | --- | --- |
| dev | `test_builder` (localhost:55432) | `26553608-e1c6-428d-b09d-7b9939a526d8` |
| `test-dev.edtech-rtk.ru` | `test_builder_test` | `640be915-1de2-4700-8fe1-5b3a890a32bb` |
| `test.edtech-rtk.ru` | `scormDb` | `e60f95ed-b67d-47aa-b80c-793941eaaf9a` |
