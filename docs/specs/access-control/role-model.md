# Ролевая модель и матрица полномочий

**Версия:** 1.5
**Статус:** Согласован (правки трека PRD-15 — 2026-06-11; TD-01 гранты тем
только пользователям — 2026-06-12; роль «Разработчик» и разделение отладки
и генерации SCORM — 2026-07-30; право `media.manage` для медиатеки — 2026-08-02;
право `tests.review.invite` для приглашения рецензентов — 2026-09-10)
**Дата актуализации:** 2026-09-10
**Связанные документы:** [BRD](../brd-access-control.md),
[PRD-13](../prd-13/role-based-access-control.md),
[BRD владения контентом](../brd-content-ownership.md),
[PRD-15](../prd-15/content-ownership.md),
[Аудит требований архитектуры](../../reports/AUDIT_ARCHITECTURE_REQUIREMENTS.md),
[План требований архитектуры](../../plans/PLAN_ARCHITECTURE_REQUIREMENTS.md)

---

## 1. Назначение документа

Документ — единый источник истины по ролям и правам в системе. Он фиксирует:

- состав ролей и их определения;
- каталог именованных прав (capabilities);
- карту «роль -> права»;
- матрицу полномочий по функциональным доменам;
- модель разграничения доступа к тестам (владелец и гранты);
- модель разграничения доступа к темам: владелец, видимость и гранты (трек PRD-15);
- правила назначения ролей с потолками;
- механизм суперадминистратора из конфигурации;
- сопоставление прав с эндпоинтами API.

Серверный и клиентский код реализуют разграничение строго против этого документа.
Изменение прав начинается с правки этого файла.

---

## 2. Роли

В системе шесть ролей. Они не строго линейные: «Автор» (производство контента) и
«Менеджер по обучению» (доставка обучения) — это два ортогональных набора прав, которые
объединяются на уровне «Администратора».

| Код | Роль | Определение |
| --- | --- | --- |
| SU | Суперадминистратор | Полные исключительные права на все функции и системную конфигурацию. Задаётся в конфигурации, через UI не назначается |
| AD | Администратор | Контент (как Автор), администрирование (как Менеджер, полное), управление шаблонами, доступом к тестам, логами и SCORM-пакетами |
| DV | Разработчик | Всё, что может Автор, плюс генерация SCORM-пакета и жизненный цикл шаблонов оформления. Область по объектам — та же, что у Автора |
| AU | Автор | Темы, вопросы, тесты: создание, изменение, публикация, отладка. Правит только свои тесты и те, к которым выдан доступ. SCORM-пакет НЕ выгружает |
| MG | Менеджер по обучению | Создание групп и обычных пользователей, назначение тестов, доступ к результатам. Назначает только тесты, к которым выдан доступ |
| US | Обычный пользователь | Прохождение назначенных тестов, просмотр собственных результатов и истории |

«Разработчик» и «Автор» составляют класс авторских ролей (`AUTHORING_ROLES` в
`shared/access/roles.ts`): всякое правило области из раздела 6, названное для AU,
действует и для DV. Разделяет их одно — упаковка теста для LMS и владение
шаблонами оформления.

Базовые принципы:

- пользователь может иметь несколько ролей одновременно (например, Автор и Менеджер по
  обучению); его права — объединение прав всех назначенных ролей плюс права
  суперадминистратора, если он задан в конфигурации;
- права набора ролей гейтят само действие (что в принципе разрешено пользователю);
- владелец и гранты гейтят конкретный объект (к какому тесту применимо действие);
- статус учётной записи (`pending` / `active` / `inactive`) действует поверх ролей и
  блокирует вход независимо от прав.

Роли хранятся в связи «многие-ко-многим» (`user_roles`). Значение «Суперадминистратор» в
составе хранимых ролей не используется и вычисляется из конфигурации (раздел 8).

---

## 3. Каталог прав (capabilities)

Право — именованная единица разрешения. Имена в пространстве `домен.действие`. Колонка
«Область» отмечает права, для которых результат зависит от владельца теста или гранта
(см. раздел 6).

| Право | Назначение | Область |
| --- | --- | --- |
| `auth.self` | Вход, чтение своего профиля, смена своего пароля, первый вход | нет |
| `attempts.take` | Прохождение назначенного теста (старт, ответ, сохранение, завершение) | по назначению |
| `attempts.self.read` | Просмотр собственных результатов и истории | свои попытки |
| `topics.read` | Список и чтение тем видимой области (для выбора в тесты) | видимая область (раздел 6.5) |
| `topics.manage` | CRUD тем, курсов, мероприятий, дублирование, массовые операции | владение или грант `manage` (раздел 6.6) |
| `topics.access.grant` | Выдача и отзыв грантов доступа к теме | владелец темы; AD/SU — любые |
| `topics.owner.change` | Смена владельца темы | нет (только AD/SU) |
| `questions.read` | Список и чтение вопросов видимых тем | видимая область (раздел 6.5) |
| `questions.manage` | CRUD вопросов, дублирование, массовые операции | по теме вопроса: владение или грант `manage` |
| `questions.importExport` | Импорт и экспорт вопросов | видимая область тем |
| `folders.manage` | CRUD папок вопросов и папок тестов | нет (папки без владения) |
| `media.manage` | Управление реестром медиатеки: пересборка индекса использования, удаление актива | нет (чтение файла отдельным правом не описано — его решает правило доступа к активу: владение, видимость, связь с доступным контентом) |
| `tests.read` | Список и чтение тестов | по доступу |
| `tests.create` | Создание теста (создатель становится владельцем) | нет |
| `tests.edit` | Правка теста: секции, оформление, content-pages, показатели, шкалы | по доступу (edit) |
| `tests.publish` | Смена статуса теста (черновик / опубликован / архив) | по доступу (edit) |
| `tests.delete` | Удаление теста | только владелец |
| `tests.export.scorm` | Экспорт теста в SCORM-пакет | по доступу (edit) |
| `tests.debug.play` | Тестовый прогон теста во встроенном плеере-отладчике (PRD-18) | по доступу (edit) |
| `tests.access.grant` | Выдача и отзыв грантов доступа к тесту | владелец теста; AD/SU — любые |
| `tests.review.invite` | Приглашение рецензентов теста: грант `review`, персональная ссылка, заведение внешней учётной записи рецензента (PRD-52) | владелец теста; AD/SU — любые |
| `tests.owner.change` | Смена владельца теста | нет (только AD/SU) |
| `templates.read` | Чтение каталога активных шаблонов для выбора в тесте | нет |
| `adminTemplates.manage` | Жизненный цикл шаблонов: загрузка, активация, удаление, экспорт | нет |
| `assignments.manage` | Назначение тестов, отзыв и переотправка токенов | по доступу (assign) |
| `groups.manage` | CRUD групп и управление составом | нет |
| `users.read` | Просмотр списка и карточек пользователей | нет |
| `users.create` | Создание пользователей | с потолком роли |
| `users.manage` | Правка, блокировка, сброс пароля, импорт, сброс попыток | нет |
| `users.role.assign` | Назначение и смена роли пользователя | с потолком роли |
| `analytics.read` | Аналитика по тестам, попыткам, результатам | по доступу |
| `analytics.export` | Экспорт аналитики | по доступу |
| `scormPackages.manage` | Управление SCORM-пакетами, регенерация ключей телеметрии | нет |
| `logs.read` | Просмотр журнала приложения | нет |
| `system.config` | Системная конфигурация и параметры безопасности | нет (только SU) |
| `system.roles.assignAdmin` | Назначение роли «Администратор» | нет (только SU) |

---

## 4. Карта «роль -> права»

Право пользователя есть, если им обладает хотя бы одна из его ролей (объединение по
строкам таблицы). Легенда: «+» право есть; «о» право есть с ограничением области или потолка
(см. примечания); пусто — права нет.

| Право | SU | AD | DV | AU | MG | US |
| --- | :--: | :--: | :--: | :--: | :--: | :--: |
| `auth.self` | + | + | + | + | + | + |
| `attempts.take` | + | + | + | + | + | + |
| `attempts.self.read` | + | + | + | + | + | + |
| `topics.read` | + | + | о | о | | |
| `topics.manage` | + | + | о | о | | |
| `topics.access.grant` | + | + | о | о | | |
| `topics.owner.change` | + | + | | | | |
| `questions.read` | + | + | о | о | | |
| `questions.manage` | + | + | о | о | | |
| `questions.importExport` | + | + | о | о | | |
| `folders.manage` | + | + | + | + | | |
| `media.manage` | + | + | + | + | | |
| `tests.read` | + | + | о | о | о | |
| `tests.create` | + | + | + | + | | |
| `tests.edit` | + | + | о | о | | |
| `tests.publish` | + | + | о | о | | |
| `tests.delete` | + | + | о | о | | |
| `tests.export.scorm` | + | + | о | | | |
| `tests.debug.play` | + | + | о | о | | |
| `tests.access.grant` | + | + | о | о | | |
| `tests.review.invite` | + | + | о | о | | |
| `tests.owner.change` | + | + | | | | |
| `templates.read` | + | + | + | + | | |
| `adminTemplates.manage` | + | + | + | | | |
| `assignments.manage` | + | + | | | о | |
| `groups.manage` | + | + | | | + | |
| `users.read` | + | + | | | + | |
| `users.create` | + | + | | | о | |
| `users.manage` | + | + | | | | |
| `users.role.assign` | + | о | | | | |
| `analytics.read` | + | + | о | о | о | |
| `analytics.export` | + | + | о | о | о | |
| `scormPackages.manage` | + | + | | | | |
| `logs.read` | + | + | | | | |
| `system.config` | + | | | | | |
| `system.roles.assignAdmin` | + | | | | | |

Примечания к ограничениям («о»). Везде, где ниже назван AU, то же ограничение
действует и для DV — это один класс авторских ролей.

- `topics.read` / `questions.read` / `questions.importExport` для AU ограничены видимой
  областью тем (раздел 6.5): свои, с грантом, общие.
- `topics.manage` / `questions.manage` для AU ограничены владением темой или грантом
  `manage` (раздел 6.6); удаление темы — только владелец и AD/SU.
- `topics.access.grant` для AU — только на свои темы (владелец); AD/SU — на любые.
- `tests.access.grant` для AU — только на свои тесты (владелец); AD/SU — на любые
  (унификация PRD-15, BRC-27).
- `tests.review.invite` — отдельное право с той же областью, что `tests.access.grant`
  (владелец; AD/SU — любые). Отделено от него сознательно: выдать человеку доступ к тесту
  руками и позвать его рецензировать — разные действия. У MG права нет: рецензирование не
  входит в дорожку назначений. Заведение внешней учётной записи рецензента входит в это
  право и НЕ требует `users.create` — без учётной записи комментарий нечем подписать
  (PRD-52 раздел 14).
- `tests.read` / `tests.edit` / `tests.publish` / `tests.delete` / `tests.debug.play` для
  AU ограничены областью доступа (раздел 6): владение и гранты `edit`; удаление — только
  владение.
- `tests.export.scorm` есть только у DV (и у AD/SU) и ограничен той же областью доступа
  (edit). Автор упаковкой для LMS не занимается, но прогоняет свой тест в плеере-отладчике:
  `tests.debug.play` — отдельное право с той же областью.
- `adminTemplates.manage` у DV не ограничен областью: деактивация или удаление шаблона
  влияет на все тесты, к нему привязанные.
- `tests.read` для MG ограничен тестами с грантом `assign`.
- `assignments.manage` для MG ограничен тестами с грантом `assign`.
- `analytics.read` / `analytics.export` для AU — по доступным на правку тестам, для MG — по
  назначаемым тестам.
- `users.create` для MG — только роль «Обычный пользователь».
- `users.role.assign` для AD — вплоть до Разработчика, Автора и Менеджера; роль
  «Администратор» назначает только SU (право `system.roles.assignAdmin`).

---

## 5. Матрица полномочий по функциональным доменам

Легенда: «+» полный доступ; «о» ограниченный или с оговоркой; пусто — нет.

| Домен функций | SU | AD | DV | AU | MG | US |
| --- | :--: | :--: | :--: | :--: | :--: | :--: |
| Вход, свой профиль, смена своего пароля | + | + | + | + | + | + |
| Прохождение назначенных тестов, свои результаты и история | + | + | + | + | + | + |
| Темы (CRUD, курсы, мероприятия) | + | + | о видимые; правка по владению и грантам | о видимые; правка по владению и грантам | | |
| Вопросы (CRUD, импорт, экспорт, массовые операции) | + | + | о в пределах видимых тем | о в пределах видимых тем | | |
| Выдача и отзыв доступа к теме, смена владельца темы | + | + | о гранты своих тем | о гранты своих тем | | |
| Папки вопросов и папки тестов | + | + | + | + | | |
| Медиатека — управление реестром (пересборка индекса использования, удаление актива); чтение файла правом не гейтится — см. правило доступа к активу | + | + | + | + | | |
| Тесты — правка, оформление, content-pages, показатели, шкалы | + | + | о свои и по доступу | о свои и по доступу | | |
| Тесты — удаление и архивирование | + | + | о только свои | о только свои | | |
| Публикация теста | + | + | о область правки | о область правки | | |
| Тестовый прогон в плеере-отладчике | + | + | о область правки | о область правки | | |
| Экспорт SCORM | + | + | о область правки | | | |
| Каталог шаблонов (выбор шаблона в тесте) | + | + | + | + | | |
| Назначение тестов (пользователю, группе, magic-link, отзыв) | + | + | | | о только по доступу | |
| Группы (CRUD, состав) | + | + | | | + | |
| Пользователи — создание | + | + | | | о только обычных | |
| Пользователи — правка, блокировка, сброс пароля, импорт | + | + | | | | |
| Назначение роли пользователю | + все | о до Разработчика, Автора и Менеджера | | | | |
| Выдача и отзыв доступа к тесту, смена владельца | + | + | о гранты своих тестов | о гранты своих тестов | | |
| Аналитика и результаты | + | + | о по доступным тестам | о по доступным тестам | о по назначаемым тестам | |
| Админ-реестр шаблонов (загрузка, активация, удаление) | + | + | + | | | |
| SCORM-пакеты (управление, регенерация ключей, телеметрия) | + | + | | | | |
| Журнал приложения (логи) | + | + | | | | |
| Системная конфигурация (роли, параметры безопасности) | + | | | | | |

---

## 6. Разграничение доступа к тестам

### 6.1 Владелец теста

Каждый тест имеет владельца (`tests.ownerId`). Владелец устанавливается при создании
(создатель теста) и меняется только администратором или суперадминистратором.

- Владелец из класса авторских ролей (Автор или Разработчик) может править, публиковать,
  отлаживать и удалять свой тест; выгрузить SCORM-пакет — только Разработчик.
- Удаление и архивирование доступны только владельцу (и AD/SU), но не держателям гранта
  `edit`.

### 6.2 Гранты доступа

Доступ к чужому тесту выдаётся явным грантом в таблице `test_access_grants`.

| Уровень гранта | Кому адресован | Что разрешает |
| --- | --- | --- |
| `edit` | Автор, Разработчик | Правка, оформление, показатели, шкалы, публикация, отладка теста; экспорт SCORM — если роль держателя его допускает |
| `assign` | Менеджер | Назначение теста пользователям и группам, просмотр его результатов |

На пару «тест + пользователь» допускается один грант. Уровень `edit` для целей проверки
области покрывает и `assign`, но фактическое действие всё равно ограничено правами роли
держателя (Автор не назначает и не выгружает SCORM, Менеджер не правит).

### 6.3 Кто выдаёт доступ

Гранты доступа к тесту выдаёт и отзывает владелец теста (на свои тесты) и
Администратор/Суперадминистратор (на любые) — право `tests.access.grant` с областью
владельца (унификация PRD-15, BRC-27). Смену владельца выполняют только Администратор и
Суперадминистратор (`tests.owner.change`).

### 6.4 Алгоритм разрешения доступа

Роль определяет допустимость действия, владелец и грант — допустимость для конкретного
теста.

Резолюция работает с набором ролей пользователя: проверяется наличие нужной роли
(`hasRole`), а не равенство единственной роли.

```text
roles(u)          := effective roles of u from user_roles
hasRole(u, R)     := R in roles(u)
hasAuthoring(u)   := hasRole(u, AU) OR hasRole(u, DV)
hasSuper(u)       := email(u) in SUPERADMIN_EMAILS
isAdminOrSuper(u) := hasSuper(u) OR hasRole(u, AD)

canReadTest(u, t):
  isAdminOrSuper(u)                                            -> true
  hasAuthoring(u) AND (t.ownerId == u.id OR grant(t, u, edit)) -> true
  hasRole(u, MG) AND grant(t, u, assign|edit)                  -> true
  assignedToUser(t, u)                                         -> true
  else                                                         -> false

canEditTest(u, t):
  isAdminOrSuper(u)                                            -> true
  hasAuthoring(u) AND (t.ownerId == u.id OR grant(t, u, edit)) -> true
  else                                                         -> false

canDeleteTest(u, t):
  isAdminOrSuper(u)                                            -> true
  hasAuthoring(u) AND t.ownerId == u.id                        -> true
  else                                                         -> false

canPublishTest(u, t)  := canEditTest(u, t)
canExportScorm(u, t)  := canEditTest(u, t)   // право tests.export.scorm — DV/AD/SU
canDebugTest(u, t)    := canEditTest(u, t)   // право tests.debug.play — AU/DV/AD/SU

canAssignTest(u, t):
  isAdminOrSuper(u)                                            -> true
  hasRole(u, MG) AND grant(t, u, assign|edit)                  -> true
  else                                                         -> false

canReadTestAnalytics(u, t):
  isAdminOrSuper(u)                                            -> true
  hasAuthoring(u) AND canEditTest(u, t)                        -> true
  hasRole(u, MG) AND canAssignTest(u, t)                       -> true
  else                                                         -> false

canGrantAccess(u, t)  := isAdminOrSuper(u) OR (hasAuthoring(u) AND t.ownerId == u.id)
canChangeOwner(u, t)  := isAdminOrSuper(u)
```

Область по объектам одинакова для Автора и Разработчика: пара «право + область» проверяется
раздельно, поэтому Разработчик не получает доступа к чужим тестам — он лишь может упаковать
в SCORM те, что и так правит.

Списки тестов фильтруются по объединению областей всех ролей пользователя: владение и
гранты `edit` (по авторским ролям), гранты `assign` (по роли Менеджера), все тесты (по роли
Администратора или суперадминистратору), назначенные тесты (для прохождения). Пользователь
с ролями Автор и Менеджер видит объединение этих областей.

### 6.5 Владение и видимость тем (PRD-15)

Каждая тема имеет владельца (`topics.owner_id`) и видимость (`topics.visibility`):

- приватная (`private`) — тему видят владелец, получатели грантов и AD/SU;
- общая (`shared`) — тему видят все авторы на уровне использования.

Новая тема создаётся приватной, владелец — создатель. Legacy-темы: владелец пуст,
видимость общая (разрушающие операции над ними — только AD/SU). Смену владельца выполняет
администратор (`topics.owner.change`). Вопросы наследуют область видимости своей темы;
отдельных прав на вопрос нет.

Видимая область автора = свои темы + темы с личным грантом + общие.

### 6.6 Гранты доступа к темам

Грант хранится в `topic_access_grants`; получатель — только пользователь
(решение TD-01 от 2026-06-12: группы предназначены исключительно для назначения
тестов, доступ к темам группам не выдаётся; миграция 025).

| Уровень гранта | Что разрешает |
| --- | --- |
| `use` | Видеть тему и её вопросы, использовать тему в своих тестах |
| `manage` | Дополнительно: CRUD вопросов темы, правка самой темы (без удаления) |

Удаление темы — только владелец и AD/SU. Гранты на свою тему выдаёт и отзывает владелец
(`topics.access.grant` с областью владельца); AD/SU — на любые. Выдача и отзыв
журналируются.

### 6.7 Отзыв грантов темы

Отзыв не блокируется наличием зависимых тестов. Два режима:

- мягкий (по умолчанию): грант переходит в состояние «отозван, используется» — тема
  исчезает из банка получателя и недоступна новым тестам; для существующих тестов
  получателя действует производное право чтения «тема в контексте моего теста» (состав
  секций, вопросы выдачи, статистика), без доступа к банку;
- жёсткий (AD/SU): через разрешение зависимостей (замена или удаление секций, снятие с
  публикации, смена владельца, материализация снапшота); принудительно — только
  административным переопределением с записью в журнал.

### 6.8 Инвариант доставки

Путь доставки и оценивания (выдача вопросов в попытке, проверка ответов, собранный
SCORM-пакет) права на контент не проверяет: обучаемый проходит тест по факту назначения.
Потеря автором доступа к теме не влияет на работу опубликованных тестов.

```text
visibleTopic(u, topic):
  isAdminOrSuper(u)                                  -> true
  topic.visibility == shared AND hasRole(u, AU)      -> true
  topic.ownerId == u.id                              -> true
  grant(topic, u, use|manage) is active              -> true
  else                                               -> false

canManageTopicContent(u, topic):
  isAdminOrSuper(u)                                  -> true
  topic.ownerId == u.id                              -> true
  grant(topic, u, manage) is active                  -> true
  else                                               -> false

canDeleteTopic(u, topic)      := isAdminOrSuper(u) OR topic.ownerId == u.id
canGrantTopicAccess(u, topic) := isAdminOrSuper(u) OR topic.ownerId == u.id
canChangeTopicOwner(u, topic) := isAdminOrSuper(u)
```

---

## 7. Состав и назначение ролей

Пользователь обладает набором ролей. Роли назначаются и отзываются по одной; права
пользователя — объединение прав всех его ролей. Управление каждой ролью (выдача или отзыв)
ограничено потолком назначающего.

| Назначающий | Может назначать и отзывать роли | Не может |
| --- | --- | --- |
| Суперадминистратор | Администратор, Автор, Менеджер, Обычный пользователь | Суперадминистратор (только из конфигурации) |
| Администратор | Автор, Менеджер, Обычный пользователь | Администратор, Суперадминистратор |
| Менеджер по обучению | Обычный пользователь (только при создании) | Все остальные |
| Автор, Обычный пользователь | — | Все |

Дополнительные правила:

- роль «Суперадминистратор» через UI не назначается никогда;
- Администратор не может изменять состав ролей или понижать учётную запись
  Суперадминистратора;
- Менеджер задаёт роль только в момент создания пользователя и только «Обычный
  пользователь»; изменение состава ролей существующих пользователей Менеджер не выполняет;
- управление конкретной ролью требует полномочий над ней по потолку: например, отозвать
  роль «Администратор» может только Суперадминистратор;
- набор ролей может быть любой комбинацией (например, Автор и Менеджер по обучению);
  «Обычный пользователь» как отдельная роль избыточен, если у пользователя уже есть любая
  иная роль (прохождение назначенных тестов доступно всем — см. раздел 5 и
  [PRD-13](../prd-13/role-based-access-control.md), FR-23).

Контроль выдачи реализуется через множество назначаемых ролей субъекта и сверку изменений
набора: при правке ролей пользователя сервер вычисляет добавленные и снятые роли и сверяет
каждую из них с этим множеством. UI показывает только назначаемые субъекту роли, но сервер
остаётся источником истины и повторяет проверку.

```text
assignableRoles(actor):
  hasSuper(actor)     -> { AD, DV, AU, MG, US }
  hasRole(actor, AD)  -> { DV, AU, MG, US }             // без AD
  hasRole(actor, MG)  -> { US } (только при создании пользователя)
  else                -> { }

applyRoleChange(actor, target, requestedRoles):
  if isSuper(target) AND NOT hasSuper(actor)  -> reject 403   // суперадмина правит только SU
  added   := requestedRoles \ currentRoles(target)
  removed := currentRoles(target) \ requestedRoles
  for r in (added UNION removed):
    if r not in assignableRoles(actor)        -> reject 403
  commit
```

Следствие: роль «Администратор» входит в `assignableRoles` только у суперадминистратора,
поэтому выдать или снять её может только он. Значение «Суперадминистратор» в запросе
невозможно — его нет в перечислении ролей `user_roles`.

---

## 8. Суперадминистратор из конфигурации

- Список суперадминистраторов задаётся переменной окружения `SUPERADMIN_EMAILS` (массив
  адресов через запятую).
- Права суперадминистратора вычисляются в рантайме на каждый запрос: пользователь, чей
  email входит в `SUPERADMIN_EMAILS`, получает права SU независимо от своего набора ролей в
  БД. Статус не кэшируется в сессии, поэтому правка конфигурации действует немедленно.
- При старте приложения для каждого адреса из списка выполняется автопровижининг: учётная
  запись создаётся, если отсутствует, без назначенных ролей (`user_roles` пуст); права SU
  действуют только по совпадению email. Существующим учётным записям автопровижининг роли
  не добавляет.
- Суперадминистратор не хранится как назначаемая роль в `user_roles`, поэтому права SU
  нельзя выдать или отозвать через UI — только правкой конфигурации.
- Исключение учётной записи из `SUPERADMIN_EMAILS` немедленно прекращает права SU; у
  учётной записи остаётся только то, что было явно назначено в `user_roles` (у
  автопровиженной — ничего, доступ сводится к собственному профилю). Это исключает «тихого»
  остаточного администратора после удаления из конфигурации.
- Назначение суперадмину хранимой роли — отдельное журналируемое действие по обычным
  правилам (раздел 7); оно не отменяет принципа: удаление из конфигурации снимает именно
  привилегии суперадмина.

---

## 9. Сопоставление прав с эндпоинтами API

Таблица сопоставляет существующие эндпоинты (19 доменов) с требуемым правом и областью.
Колонка «Область» означает дополнительную проверку владельца или гранта (раздел 6).

| Метод и путь | Право | Область |
| --- | --- | --- |
| `POST /api/auth/login` | публично | — |
| `POST /api/auth/logout` | публично | — |
| `GET /api/auth/me` | сессия | — |
| `POST /api/auth/change-password` | `auth.self` | — |
| `POST /api/auth/complete-first-login` | `auth.self` | — |
| `POST /api/auth/forgot-password`, `reset-password`, `verify-reset-token` | публично | — |
| `GET /access/:token` | токен | — |
| `GET /api/learner/tests`, `/api/learner/attempts`, `/api/learner/assigned-tests` | `attempts.self.read` | свои |
| `POST /api/tests/:id/attempts/start`, `start-adaptive` | `attempts.take` | по назначению |
| `POST /api/attempts/:id/answer-adaptive`, `save-progress`, `finish` | `attempts.take` | своя попытка |
| `GET /api/tests/:id/resume`, `GET /api/attempts/:id/result` | `attempts.take` | своя попытка |
| `GET /api/topics`, `GET /api/questions` | `topics.read` / `questions.read` | видимая область (6.5) |
| `GET /api/folders`, `GET /api/test-folders` | `folders.manage` | — |
| `POST /api/topics` | `topics.manage` | создатель становится владельцем |
| `PUT/DELETE /api/topics/*`, `bulk-*`, `duplicate` | `topics.manage` | владение или грант `manage`; удаление — владелец; плюс выполнимость выдачи (PRD-15) |
| `GET/POST/DELETE /api/topics/:id/access` | `topics.access.grant` | владелец темы; AD/SU — любые |
| `PATCH /api/topics/:id/owner` | `topics.owner.change` | — |
| `GET /api/topics/duplicates` | `topics.owner.change` | — (административный отчёт) |
| `POST/PUT/DELETE /api/questions/*`, `bulk-*`, `duplicate` | `questions.manage` | по теме вопроса (6.6); плюс выполнимость выдачи |
| `GET /api/questions/export`, `POST /api/questions/import` | `questions.importExport` | видимая область тем |
| `POST/PUT/DELETE /api/folders/*`, `/api/test-folders/*` | `folders.manage` | — |
| `GET /api/tests`, `GET /api/tests/:id` | `tests.read` | по доступу |
| `POST /api/tests` | `tests.create` | — |
| `PUT /api/tests/:id`, design, content-pages, result-variables, scales, measurements | `tests.edit` | edit |
| `PATCH /api/tests/:id/status`, `POST /api/tests/:id/restore` | `tests.publish` | edit |
| `DELETE /api/tests/:id` | `tests.delete` | владелец |
| `GET /api/tests/:id/export/scorm` | `tests.export.scorm` | edit |
| `POST/GET/DELETE /api/tests/:id/debug/*` | `tests.debug.play` | edit (PRD-18) |
| `GET /api/tests/:id/access`, `POST /api/tests/:id/access`, `DELETE /api/tests/:id/access/:userId` | `tests.access.grant` | владелец теста; AD/SU — любые |
| `POST /api/tests/:id/review/preview`, `POST /api/tests/:id/review/invite` | `tests.review.invite` | владелец теста; AD/SU — любые (PRD-52) |
| `PATCH /api/tests/:id/owner` | `tests.owner.change` | — |
| `GET /api/tests/:id/design`, `GET /api/tests/:id/screen-template/:screen` | сессия | область чтения теста, включая назначение (6.4) |
| `POST /api/tests/:id/republish-force` | `tests.publish` | edit (экстренная переопубликация, PRD-15) |
| `GET/PUT /api/tests/:id/question-scoring` | `tests.read` / `tests.edit` | по доступу / edit (PRD-15) |
| `GET /api/templates`, `GET /api/templates/:id` | `templates.read` | — |
| `GET/POST/PUT/DELETE /api/admin/templates/*` | `adminTemplates.manage` | — |
| `GET/POST/DELETE /api/tests/:id/assignments`, `bulk` | `assignments.manage` | assign |
| `DELETE /api/assignments/:id`, `resend*`, `revoke*` | `assignments.manage` | assign |
| `GET/POST/PUT/DELETE /api/groups/*` | `groups.manage` | — |
| `GET /api/users`, `GET /api/users/:id`, `attempts-summary`, `groups` | `users.read` | — |
| `POST /api/users`, `POST /api/users/bulk-*` | `users.create` | потолок роли |
| `PUT /api/users/:id` (смена роли) | `users.role.assign` | потолок роли |
| `PUT /api/users/:id`, `reset-password`, `activate`, `deactivate`, `reset-attempts` | `users.manage` | — |
| `GET /api/analytics/*`, `combined*` | `analytics.read` | по доступу |
| `GET /api/analytics/**/export/excel`, `/api/export/*` | `analytics.export` | по доступу |
| `GET/POST /api/scorm-packages/*` | `scormPackages.manage` | — |
| `POST /api/scorm-telemetry/start`, `answer`, `finish` | секретный ключ | — |
| `GET /api/logs`, `GET /api/logs/dates` | `logs.read` | — |

---

## 10. Глоссарий

- Набор ролей — множество ролей пользователя из `user_roles`; права пользователя —
  объединение прав всех ролей набора плюс права суперадминистратора, если он задан в
  конфигурации.
- Эффективный набор прав — итоговое множество прав пользователя, вычисленное как
  объединение по набору ролей.
- Область (scope) — дополнительная проверка применимости действия к конкретному объекту
  (тесту или теме) по владельцу, гранту или видимости.
- Грант — запись в `test_access_grants` (доступ `edit` / `assign` к тесту) или в
  `topic_access_grants` (доступ `use` / `manage` к теме; получатель — только
  пользователь, TD-01).
- Видимая область тем — множество тем, которые автор видит и может использовать: свои,
  с активным грантом, общие (раздел 6.5).
- Мягкий отзыв — состояние гранта темы «отозван, используется»: тема скрыта из банка
  получателя, но его существующие тесты сохраняют производное чтение (раздел 6.7).
- Потолок назначения — максимальная роль, которую данный субъект вправе назначить другому.
