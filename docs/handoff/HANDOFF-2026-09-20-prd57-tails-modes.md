# Передача работы: PRD-57 — хвосты трека, долги и три режима ввода

Дата: 2026-09-20. Ветка: `feat/prd57-e10-track-tails`, влита в `main` (голова `aa995458`).
Всё, о чём тут написано, уже в `main`; PR не открывался — вливали быстрой перемоткой, как и
предыдущие этапы трека.

Статус трека: **закрыт весь, кроме одного пункта** — замеров на живом стенде WebTutor
([issue #51](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/51)). Инструмент для
них собран и лежит в репозитории; сам прогон требует доступа к порталу.

## 1. Что сделано

### Э9 «Развёрнутый ответ» — закрыт

Этап был доделан и принят (7 из 7) в начале сессии: план отмечен выполненным, ветка влита.

### Э10 «Хвосты трека» ([#50](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/50), закрыт)

Пять хвостов §7, каждый — доведение уже сделанных типов до того, что у остальных работает с
прошлых треков:

- **периметр защиты пропускает поля ввода** (FR-29, AC-06): инлайновое `user-select: text`
  полям внутри защищённого региона плюс пропуск таких событий слушателями. Текст задания и
  варианты выбора защищены по-прежнему;
- **книга Excel возит текстовые типы** (FR-31, AC-08): правила живут в колонке эталона,
  грамматика — строка на правило, `регулярное:` для выражения, оператор с допуском у числа,
  имя пропуска в квадратных скобках; вид ответа и связка пропуска объявляются у самого
  пропуска (`[year: число, все]`), потому что колонка одна на задание;
- **импорт выгрузки LMS принимает текстовые взаимодействия** (FR-34): текст приезжает как
  набран, значения пропусков раскладываются по именам из эталона;
- **аналитика описывает свободный текст** (FR-32): тип, содержимое, эталон и ответ печатаются
  словами, сводка объёма и длины, ответы списком и выгрузкой; заодно закрыт долг Э3 —
  разброс написаний короткого ответа теперь виден и в обычном тесте;
- **время на задании** (FR-33): закреплено тестами на обоих путях.

Приёмка на живом приложении: 15 из 15.

### Замеры на стенде — инструмент готов ([#51](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/51), ОТКРЫТ)

`npm run scorm:probe` собирает `out/prd57-stand-probe.zip` — отдельный SCO из трёх файлов.
Он снимает пять фактов: предел длины `learner_response` у `fill-in` и `long-fill-in`, тип
`numeric` с эталоном-диапазоном, число в `cmi.interactions.n.result`, доступность рабочего
потока внутри SCO и показ формулы тремя способами.

Инструкция — `docs/guides/stand-probe-prd57.md`. Отчёт снимается кнопкой «Скопировать
отчёт» прямо на экране модуля.

### Долги трека (Э12)

- **панель вставки** над полем текста задания: «Листинг» (язык выбирается меню), «Формула»,
  «Пропуск». Разметка встаёт в позицию курсора, выделенное оборачивается;
- **предпросмотр задания** (FR-24g): окно по кнопке подвала ящика, рисует задание ТЕМ ЖЕ
  компонентом, что и прогон (`TemplateQuestionScreen`). Разметку считает новый маршрут
  `POST /api/questions/preview`.

Приёмка: 9 из 9.

### §4.3 «Три режима ввода» (Э13) — закрыта

Выяснилось по ходу разбора долгов: §4.3 и AC-01b не делал НИ ОДИН этап трека. Сделано
отдельным этапом:

- формат живёт колонкой `questions.prompt_format` (`markdown` | `richText` | `html`,
  миграция `0037`); написание повторяет `feedbackContentSchema.format` и
  `tests.description_format`;
- выдача (`promptHtmlOf`) ветвится по формату в одном месте: санитайз, типографика по
  текстовым узлам, приведение листинга к внутренней форме, подсветка, формулы;
- проекции текста словами, колонка «Формат текста» в книге;
- отчёт о потерях при переключении (`shared/text/mode-switch.ts`) — находки с числами;
- визуальный редактор (`rich-prompt-editor.tsx`) с атомарными узлами
  (`shared/text/rich-atoms.ts`); вид механик — подсвеченный листинг и картинка формулы —
  приходит с сервера тем же маршрутом предпросмотра.

Приёмка: 16 из 16 плюс отдельные 6 из 6 на вид механик в визуальном поле.

## 2. Что НЕ сделано

1. **Замеры на стенде (#51)** — единственный незакрытый пункт трека. Нужен доступ к порталу
   `university.rt.ru`: загрузить зонд как учебный модуль, открыть, прислать JSON. Шестой
   замер (формула в PDF-отчёте) снимается настоящим пакетом, а не зондом — см. раздел 3
   инструкции.
2. **Диагностика вырезанного санитайзером** (эскиз `prd57-question-text.html`, состояние
   `s-diag`). Сервер уже возвращает список вырезанного
   (`normalizePromptByFormat` → `removed`), но маршрут его не использует, и автору ничего не
   показывается: небезопасное снимается молча, и человек видит, что «текст изменился сам».
   Объём небольшой: пробросить `removed` в ответ и показать баннером в ящике.
3. **Подпись в согласованном эскизе устарела.** Модалка перехода обещает, что «в режиме HTML
   типографика больше не работает», а FR-09d (решение владельца 2026-09-18, то есть позже)
   требует обратного. Реализовано требование; эскиз в этой подписи стоит поправить.

## 3. Как проверить

```bash
# наборы трека
npm test -- tests/question-prompt-format.test.ts tests/prompt-html-format.test.ts \
  tests/mode-switch.test.ts tests/rich-atoms.test.ts tests/plain-text-format.test.ts \
  tests/workbook-import-text-types.test.ts client/src/features/questions

# зонд для стенда
npm run scorm:probe   # → out/prd57-stand-probe.zip
```

Живые прогоны делались скриптами в рабочем каталоге сессии (`e10-accept.mjs`,
`e12-accept.mjs`, `e13-accept.mjs`, `e13b-accept.mjs`): они поднимают CDP, ходят по API и
по экрану, а в конце удаляют за собой данные. В репозиторий не клались намеренно — это
одноразовые проверки этапа, а не гейты.

## 4. Грабли, которые стоили времени

- **Отладочный плеер PRD-18 отключает защиту PRD-34 намеренно** (FR-25). Приёмка периметра
  там проверяет его ОТСУТСТВИЕ: первый прогон дал «текст задания не защищён» именно поэтому.
  Периметр принимать на веб-хосте участника или на стенде.
- **`tests/scorm-latency.test.ts` выдёргивает функции из `resultsPage.js` по списку имён.**
  С Э3 туда добавился `correctPatternFor`, список не обновили — три теста падали с
  `ReferenceError` с тех пор, потому что набор никто не гонял. Починено.
- **heredoc превращает `\b` в регулярках в управляющий символ** (0x08), и выражение молча
  перестаёт совпадать. Полчаса на поиск. Регулярки писать через Edit, а после записи файла
  скриптом проверять, что управляющих символов в нём нет.
- **Сериализация HTML не экранирует угловые скобки в значении атрибута.** Исходник листинга,
  положенный в `data-source` как есть, обрывал разбор узла на первой `>`. Кодируется
  `encodeURIComponent`.
- **Границы атома нельзя искать выражением:** внутри атома кода лежит подсветка, то есть
  свои `<span>`. Ищутся счётчиком вложенности.
- **Сравнивать `innerHTML` со строкой бесполезно:** браузер нормализует разметку, и сравнение
  всегда отвечает «текст изменился». Решение «подставлять вид» принимается по флагу «автор
  печатал».
- **Задание в банке создаётся плавающей кнопкой дерева** (`ct-fab-toggle` →
  `ct-fab-question`), а не кнопкой с подписью: приёмка, ищущая «Создать вопрос», не найдёт
  ничего.
- **`ModalDialog` не пробрасывает `data-testid`** на корень — окно опознаётся по заголовку.
- **`GET /api/questions/:id` не существует** — вопрос перечитывается списком
  `/api/questions?topicId=`.
- **Гард полноты справки шаблона книги** требует описания КАЖДОЙ колонки каждого листа: новая
  колонка сразу валит набор, пока её не опишешь. Так и задумано.

## 5. Решения, принятые в этой работе

- **Формат текста задания — три значения, а не два.** `richText` и `html` хранят одно и то
  же; помнить их по отдельности нужно, чтобы автор возвращался в тот редактор, которым
  набирал.
- **Хеш содержимого формат не включает.** Иначе один проход сдвинул бы его у каждого
  существующего задания и ложно зажёг индикатор устаревших переопределений оценки.
- **Правила проверки в книге живут в колонке эталона.** Они И ЕСТЬ эталон и лежат в том же
  `correct_json`, который уже возят снимок публикации, выпечка пакета и перенос теста.
- **Вид механик в визуальном поле берётся у сервера.** Подсветка и MathJax живут только там;
  запросов это стоит столько, сколько раз текст заменили не набором.
- **Перевод разметки в HTML печатает листинг стандартной записью** `<pre><code
  class="language-sql">`, а не внутренней формой выдачи: её узнаёт автор, понимает внешний
  редактор, и по ней выдача восстанавливает подсветку.

## 6. Следующий шаг

По треку — только замеры (#51): прогнать зонд на стенде и внести результаты в PRD-57 как
факты с датой. Что не подтвердится, чинится там же; конструкция под это приспособлена —
пределы лежат в настройках, у рабочего потока есть путь отката, у взаимодействия числового
ответа — `fill-in` по умолчанию.

Рядом, вне трека: доделать диагностику вырезанного (раздел 2, пункт 2) и продолжить
[PRD-58](../specs/prd-58/open-response-grading.md) — закладка §5.6 под него готова и
проверена.
