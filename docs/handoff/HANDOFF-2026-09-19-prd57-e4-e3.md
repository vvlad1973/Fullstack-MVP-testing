# Передача работы: PRD-57, этапы Э4 и Э3

Дата: 2026-09-19. Ветки: `feat/prd57-e4-answer-rules` и `feat/prd57-e3-short-answer`
(вторая ветвится от первой). Базовый коммит цепочки:
`697a7f4f docs(prd-57): в плане Э4 подписана связь перечисления типов с импортом`
в ветке `feat/prd57-e0-wireframes`.
Статус: оба этапа готовы к приёмке владельцем, приняты в браузере автором работы.
PR не открывались ни по одной из трёх веток.

## 1. Задача

Два этапа трека [PRD-57](../specs/prd-57/open-response-and-formatting.md):

- **Э4**, задача [#44](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/44) — правила
  сравнения: нормализация, список правил, связка, вид ответа. Устройство:
  [docs/specs/2026-09-19-prd57-e4-answer-rules-design.md](../specs/2026-09-19-prd57-e4-answer-rules-design.md),
  план: [docs/plans/2026-09-19-prd57-e4-answer-rules.md](../plans/2026-09-19-prd57-e4-answer-rules.md).
- **Э3**, задача [#43](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/43) — короткий
  ответ с автопроверкой. Устройство:
  [docs/specs/2026-09-19-prd57-e3-short-answer-design.md](../specs/2026-09-19-prd57-e3-short-answer-design.md),
  план: [docs/plans/2026-09-19-prd57-e3-short-answer.md](../plans/2026-09-19-prd57-e3-short-answer.md).

**Требование менялось по ходу.** Решением владельца 2026-09-19 в Э3 добавлен пятый предмет,
которого в исходной постановке не было: по-ответный исход попытки вместо пересчёта верности в
аналитике. Повод описан в комментарии к #43; кратко — аналитика пересчитывала верность по
ЖИВЫМ вопросам, и правка эталона после прохождения заставляла отчёт противоречить вердикту,
который засчитала сама попытка.

## 2. Что сделано

### Э4 — движок сравнения (ветка `feat/prd57-e4-answer-rules`, 13 коммитов)

- `shared/answer-check/` — новый чистый модуль: `normalize.ts` (форма сравнения: регистр,
  пробелы, `ё`, кавычки, дефисы), `wildcard.ts` (обычное сравнение `*` и `?`
  двухуказательным проходом, БЕЗ `RegExp`), `number.ts` (разбор числа и допуск),
  `rules.ts` (набор, связка, исход по каждому правилу), `index.ts`.
- `shared/schema.ts` — `answerRuleSetSchema`: набор правил хранится в `questions.correct_json`.
- `shared/questions/question-type.ts` — тип `short` в перечне, признак `isTextEntry`, третий
  вход в `isMeasurementOnly` (текстовый ответ БЕЗ правил неоцениваем).
- `shared/scoring/engine.ts` — `Answer` принимает строку; ветки в `exactCorrect` и
  `countTallies`. Тот же тип расширен у движка шкал (`shared/scales/engine.ts`): они
  присваиваются друг другу в адаптивном пути.
- `shared/template/question-interaction.ts` — `renderShortAnswer`;
  `shared/template/short-answer-dom.ts` — привязка ввода делегированием.
- `server/scorm/template/app/scoring/engine.js` — ES5-двойник ДЕЛЕГИРУЕТ сравнение в
  `TBTemplate.checkRuleSet`, а не повторяет его;
  `server/scorm/template/app/render/questions/short.js` — обёртка над общим рендером;
  `app/actions/answers.js` — привязка ввода в пакете.
- `client/src/features/questions/answer-rules/` — блок «Проверка ответа» и модель черновика,
  держащая наборы ОБОИХ видов ответа; подключён в `question-editor-drawer.tsx`.

### Э3 — короткий ответ (ветка `feat/prd57-e3-short-answer`, 11 коммитов, 34 файла)

- Системный потолок длины `limits.shortAnswerMaxLength` = 250 во всех файлах `config/` и в
  `server/config.ts`.
- `shared/schema.ts` — `shortAnswerDataSchema` (`maxLength` в `data_json`);
  `shared/questions/short-answer.ts` — одно правило «авторский предел либо потолок», которым
  пользуются оба хоста.
- Предел подставляет СЕРВЕР: `server/routes/attempts.ts` (`questionsForClient`) и
  `server/scorm/builders/test-json.ts` (два места) — в пакете конфигурации в рантайме нет.
- `server/scorm/template/app/render/resultsPage.js` — взаимодействие `fill-in`, ответ строкой
  как есть, `correctPatternFor` (эталон только для буквальных правил).
- `server/services/analytics/answer-spread.ts` — частотная свёртка написанных ответов;
  подключена в `server/routes/analytics/test-details.ts`.
- `shared/scoring/aggregate.ts` — `AggregateQuestion.id`, `QuestionOutcome`,
  `AggregateResult.questionOutcomes`; заполняется в том же проходе, где считаются баллы.
- `server/services/analytics/answer-outcome.ts` — общий читатель «сначала сохранённое, потом
  расчёт». На него переведены шесть мест: `routes/analytics/export.ts` (четыре),
  `services/analytics/test-answer-facts.ts`, `routes/analytics/attempts.ts`.
- `client/src/pages/author/analytics.tsx` — ветки `short` в разборе ответа и эталона.

## 3. Что НЕ сделано

- **PR не открыты ни по одной ветке.** В `origin` лежат три: `feat/prd57-e0-wireframes`
  (документы Э0 плюс спеки и планы Э4 и Э3), `feat/prd57-e4-answer-rules`,
  `feat/prd57-e3-short-answer`. Цепочка: каждая следующая ветвится от предыдущей.
- **Полный `npm test` не запускался** — по правилу владельца он требует явного разрешения.
  Прогнано затронутое (см. раздел 5).
- **Замер предела `fill-in` на живом WebTutor не делался** — задача
  [#51](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/51). До него потолок равен
  рекомендации стандарта. Спека (FR-20) требует замер ДО реализации; владелец 2026-09-19
  решил идти вперёд с числом из стандарта, положив его в настройки.
- **Проверка потолка на клиенте отсутствует.** Канала серверных настроек к браузеру в
  продукте нет ни под каким именем, и заводить его ради одного числа сочтено дороже пользы.
  Сейчас автор узнаёт о потолке при сохранении. Серверная проверка `maxLength <= потолок`
  **не написана**: в плане она отмечена как отдельный тест маршрута вопросов, но до неё
  не дошло. Это единственный незакрытый пункт Э3.
- **Правка дизайн-системы из Э0 не поднята в исходник** (см. передачу
  [HANDOFF-2026-09-19-prd57-e0-wireframes.md](HANDOFF-2026-09-19-prd57-e0-wireframes.md),
  раздел 3). Состояние не изменилось.

## 4. Как проверить

Из рабочего места `.claude/worktrees/prd57-e3`:

```bash
npm run check
npm test -- tests/answer-check-normalize.test.ts tests/answer-check-wildcard.test.ts \
  tests/answer-check-number.test.ts tests/answer-check-rules.test.ts \
  tests/answer-check-schema.test.ts tests/answer-check-question-type.test.ts \
  tests/answer-check-scoring.test.ts tests/answer-check-render.test.ts \
  tests/short-answer-limit.test.ts tests/scorm-interactions.test.ts \
  tests/answer-outcomes.test.ts tests/analytics-stored-outcomes.test.ts \
  tests/config.limits.test.ts tests/scoring-engine-port.test.ts tests/scale-host-parity.test.ts
```

Приёмка на живом приложении. Сценарии лежат в каталоге черновиков сессии и в репозиторий не
клались; повторить их можно вручную:

1. Поднять приложение своим портом: `PORT=8136 npm run dev` из рабочего места.
2. Войти учётной записью приёмки (реквизиты — в памяти проекта,
   `reference_dev_acceptance_account`).
3. Завести короткий ответ с тремя правилами через связку «любое», убедиться, что свёрнутые
   строки называют, что правило проверяет, а переключение вида ответа на «Число» и обратно
   возвращает набранное.
4. Пройти тест ответом «федеральная служба по экологическому, технологическому и атомному
   надзору» при правиле `Федеральная служба по * надзору` — должен начислиться балл.
5. ИЗМЕНИТЬ эталон вопроса после прохождения и открыть разбор попытки: он обязан показать
   исход, который засчитала попытка, а не пересчитанный по новому эталону.

Пакет: `npm run scorm:template`, затем `npm run scorm:player` и прохождение того же вопроса.
Отладочный плеер открывается по `/author/tests/<id>/debug`.

## 5. Состояние проверок

Прогон 2026-09-19 из рабочего места Э3:

- `npm run check` — чисто.
- Новые наборы: **15 файлов, 201 тест, все зелёные.**
- Затронутые соседи (аналитика, ящик вопроса, экраны участника и автора, выгрузка, паритет
  движков): **54 файла, 628 тестов, все зелёные.**
- Полный `npm test` не запускался. Падений, существовавших до работы, не выявлено — но и не
  искалось: полный прогон не делался.

## 6. Принятые решения

Все согласованы с владельцем в переписке 2026-09-19.

| Решение | Почему |
| --- | --- |
| Э4 заводит тип-заготовку «Короткий ответ», Э3 её достраивает | Иначе круг: блок правил некуда прицепить, а тип нечем проверить |
| Правила хранятся в `questions.correct_json` | Правила и ЕСТЬ эталон; миграции нет, снимки и выпечка везут колонку |
| Регулярные выражения отложены в Э7 вместе с бюджетом | Откат разворачивается на 24–30 символах; выпускать проверку без бюджета нельзя |
| Обычное сравнение считается БЕЗ `RegExp` | Двухуказательный проход не имеет катастрофического случая — это и делает выпуск до Э7 честным |
| По-ответный вердикт перенесён из Э4 в Э3 | Э4 трогает движок, а это правка формы результата и путей аналитики |
| Исходы хранятся в `result_json`, а не отдельной таблицей | Колонка `jsonb`, миграции нет, читатели и так идут по попыткам |
| Потолок длины 250 из стандарта, число в настройках | Замер на стенде должен менять настройку, а не код и не заведённые задания |
| Цена ответа при импорте — отдельной задачей #54 | Считать один раз при импорте по снимку, а не возить `meta_`-блоком |
| Э3 делается одним куском, без дробления на Э3а и Э3б | Решение владельца |

## 7. Грабли

- **Приёмка в браузере нашла то, чего не нашли тесты, — трижды.** Юнит-тесты проверяли
  модули, а не путь до хранения и не вызов из хоста. Конкретно: у пакета СВОЙ распределитель
  рендереров (`app/render/questions/index.js`), и `short` попадал в «Неизвестный тип
  вопроса»; привязка ввода брала поле один раз при подключении, а пакет подписывается на
  старте, когда вопроса ещё нет, — весь ввод терялся молча; `AttemptResult` перечисляет поля
  поимённо, и `questionOutcomes` пропадало между агрегатом и базой. Не пропускать живую
  приёмку.
- **`AttemptResult` — второй контракт результата.** `AggregateResult` считает, а в базу
  пишется отдельная форма из `shared/schema.ts` (`attemptResultSchema`). Новое поле надо
  объявлять В ОБОИХ, иначе оно срезается без ошибки. В том же файле про это есть
  предупреждение применительно к zod и настройке «Тест пройден, если».
- **Тип `Answer` объявлен дважды** — в `shared/scoring/engine.ts` и `shared/scales/engine.ts`,
  и они присваиваются друг другу в адаптивном пути. Расширять только вместе.
- **Дев-сервер не перезапускается сам** (`tsx` без `--watch`). Серверные правки не видны, пока
  процесс не убит и не поднят заново. Порт освобождать через `Get-NetTCPConnection` в
  PowerShell: `pkill` в Git Bash этот процесс не берёт.
- **Профиль браузера Playwright MCP занят другой сессией.** Приёмка велась своим экземпляром
  через готовый помощник `scripts/check/editor-conformance/cdp.mjs` (в нём есть `launch`,
  `sessionCookie` и `splitCookie`).
- **Имена полей в API не угадываются.** Завершение попытки отдаёт `totalCorrect`,
  `totalEarnedPoints`, а не `correct`/`earnedPoints`; старт попытки — это
  `POST /api/tests/:id/attempts/start`; статистика теста — `GET /api/analytics/tests/:id`, без
  `/details`. Неверный путь возвращает HTML страницы, а не ошибку, и выглядит как пустой
  ответ.
- **Удаление теста требует подтверждения названием**: `DELETE /api/tests/:id` с телом
  `{ "confirmTitle": "<название>" }`, иначе 400 `title_mismatch`.
- **Дев-база общая для всех рабочих мест.** Данные приёмки за собой удалены; при повторной
  приёмке прибирать так же.

## 8. Следующий шаг

1. Дописать серверную проверку потолка длины: `maxLength` вопроса типа `short` не больше
   `config.limits.shortAnswerMaxLength`, ошибка полем в `POST` и `PUT /api/questions`
   (`server/routes/questions.ts`). Это единственный незакрытый пункт Э3, план его называет в
   задаче 3.
2. Решить судьбу трёх веток: PR по каждой отдельно либо слияние цепочкой.
3. Дальше по приоритетам: [#51](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/51)
   замеры на стенде (держат детали Э2, Э5, Э7 и Э9),
   [#49](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/49) Э9 развёрнутый ответ,
   [#55](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/55) навигатор состава теста
   (начинается с эскизов; развилка А/Б записана в комментарии к задаче).
