# Передача работы: PRD-51 «Отчёт как документ из блоков»

Дата: 2026-08-16. Ветка: `main`. Базовый коммит трека: `302f24d1` (`chore(release): раздел
2.16.0 в журнале изменений`). Последний коммит: `e272cb2f`.
Статус: **трек реализован, принят и запушен**; открыты четыре хвоста из §3.

## 1. Задача

Отчёт о результатах печатался одной цельной раскладкой `report.html`, и автор теста не мог
ни переставить разделы, ни выключить лишний, ни вставить свой методический текст. Требование
владельца: сделать редактор отчёта похожим на структуру теста — блоки, кнопки добавления,
перемещение.

Документы трека:

- спецификация: [docs/specs/prd-51/report-document-blocks.md](../specs/prd-51/report-document-blocks.md);
- планы: [Э1-Э2](../plans/2026-08-15-report-document-blocks.md),
  [Э3](../plans/2026-08-16-report-document-editor.md),
  [Э4](../plans/2026-08-16-certification-report.md),
  [Э5](../plans/2026-08-16-template-contract-2-0.md);
- приёмки: [Э1-Э2](../reports/prd51-e1-e2-acceptance.md), [Э3](../reports/prd51-e3-acceptance.md),
  [Э4](../reports/prd51-e4-acceptance.md), [Э5](../reports/prd51-e5-acceptance.md),
  [хвосты](../reports/prd51-tails-acceptance.md).

Требование менялось по ходу: начиналось как «сделать шаблон отчёта для „Сертификации“ по
референсу», но третий лист референса контрактом не выражался, и владелец решил править
платформу, а не обходить ограничение.

## 2. Что сделано

56 коммитов, 143 файла (+16 977 / −379). Всё в `main`, запушено в `origin`.

**Ядро.** `shared/report/report-blocks.ts` — закрытый реестр десяти системных блоков плюс
`page` и `page-break`. `shared/report/report-document.ts` — разрешение документа, одна
функция на все три выдачи. `shared/report/render-report.ts` — сборка DOM: блоки становятся
ПРЯМЫМИ детьми корня, иначе ломается постраничная раскладка.
`server/storage/report-blocks-repository.ts` — слой данных, замена документа одной
транзакцией.

**Хранение.** Таблица `report_blocks` (миграция `drizzle/0024_prd51_report_blocks.sql`), по
ветви на режим теста. Документ сохраняется в ОДНОЙ транзакции с остальным ящиком настроек
(`server/services/test-settings.ts`).

**Шаблоны.** Эталон разобран на 21 раскладку
(`server/scorm/templates/default/layouts/report/`, 12 обычных и 9 адаптивных).
«Сертификация» получила свой светлый документ: `templates/certification/layouts/report/`
плюс переписанный `templates/certification/styles/report.css`.

**Редактор.** `client/src/features/tests/editor/sections/report-document-list.tsx`,
`report-block-palette.tsx`, `report-block-fields.tsx`,
`client/src/features/tests/editor/use-report-document.ts`. Контрол полей PRD-22 вынесен в
общий модуль `sections/placeholder-control.tsx`.

**Контракт.** Спецификация формата поднята до 2.0.0 (новые §8.4.7-8.4.9), руководство
разработчика до 2.0.0 (раздел 7.3b с рецептом), оба PDF в `docs/dist/` пересобраны.

## 3. Что НЕ сделано

1. **ROADMAP не знает о PRD-51.** В `docs/ROADMAP.md` трек не упомянут вовсе (строка 5:
   «PRD-1...PRD-50»), а спецификация §14 обещает, что расхождение механик экрана итогов и
   документа «живёт в техдолге `docs/ROADMAP.md`». Такой записи там нет — ссылка ведёт в
   пустоту. Нужна запись о треке и строка долга в §0.3.
2. **Чекбоксы планов не отмечены.** Четыре плана выполнены, но 178 пунктов стоят как
   `- [ ]`: планы исполнялись прямо в сессии, а не через `executing-plans`. Читатель решит,
   что работа не начата.
3. **`npm run test:cov` не запускался.** Он чистит `./coverage` целиком, а машину делят
   параллельные сессии. Порог 80 % на этом коде не проверен.
4. **Ветка `prd-51-report-document-blocks` не удалена** — осталась локально, в `origin` не
   заливалась.

Вне границ трека: [issue #39](https://github.com/vvlad1973/Fullstack-MVP-testing/issues/39) —
перенос теста между инсталляциями документ отчёта не забирает. Долг зафиксирован в гарде
`server/__tests__/test-transfer-completeness.test.ts` (список `NOT_CARRIED`).

## 4. Как проверить

```bash
npm test                 # 585 файлов, 9089 тестов, 0 падений
npm run test:it          # 18 файлов, 89 тестов, 0 падений
npx tsc --noEmit         # чисто, кроме двух предсуществующих ошибок в analytics
```

Ручная проверка документа на стенде:

```bash
PORT=8097 npm run dev
```

Учётная запись приёмки берётся из памяти проекта (`reference_dev_acceptance_account`); в
этот документ реквизиты не выносятся. Тест приёмки —
`608932a1-b0d0-4953-8484-ba45890ddd1f` («PRD-51 приёмка Э3 · документ отчёта»), шаблон
«Сертификация», блоки разделов настроены. Вкладка «Настройки» → карточка «Отчёт о
результатах» → список блоков.

Проверка на бумаге:

```bash
curl -b <cookies> "http://localhost:8097/api/tests/<id>/export/scorm" -o out/check.zip
npm run scorm:player     # http://localhost:5050, загрузить пакет, пройти, «Скачать отчёт»
```

## 5. Состояние проверок

Полный прогон на коммите `e272cb2f`: **585 файлов, 9089 тестов, 0 падений**, код выхода 0.
Интеграционные на pglite: 18 файлов, 89 тестов, 0 падений.

Падений, предшествующих работе, в суите нет. Две ошибки типов — в
`client/src/pages/author/analytics.tsx` и `client/src/pages/author/test-analytics.tsx` —
существовали ДО трека и к нему не относятся; не ищите их причину здесь.

Первый полный прогон дал 57 падений и потребовал двух заходов; причины в §7.

## 6. Принятые решения

**Согласовано с владельцем — молча не переигрывать:**

- весь документ состоит из блоков, включая шапку и вводный блок, а не «зона авторских
  страниц» внутри готовой страницы;
- своя таблица `report_blocks`, а не зона в `content_pages`;
- разрыв листа доступен автору как блок;
- минимальный документ по умолчанию — титул и вердикт. Это УМОЛЧАНИЕ, а не запрет: автор
  вправе выключить и их;
- полосы разрезов показывают реальную долю и значение в процентах, а не только цвет уровня;
- паритет «Сертификации» со стандартным шаблоном по отчёту не требуется;
- логотип и иллюстрация — поля шаблона с нейтральными умолчаниями: чужие товарные знаки в
  репозиторий не кладутся.

**Приняты при исполнении:**

- имена классов отчёта у «Сертификации» взяты ЭТАЛОННЫЕ, а не из эскиза: это общий словарь
  двух шаблонов, по которому написан гард селекторов;
- из-под паритета шаблонов выведено ВСЁ семейство отчёта (`report`, `report.adaptive`,
  `report.block`), а не только блоки: «Сертификации» нужно своё поле оболочки;
- титул и обращение к слушателю — РАЗНЫЕ блоки и разные плиты, срастаются соседским
  правилом CSS. Один контейнер на двоих невозможен: движок разбирает раскладку каждого
  блока отдельным разбором, и незакрытый `div` браузер закроет сам;
- `report_blocks` записана в `NOT_CARRIED` гарда переноса, а не в `CARRIED`: по смыслу её
  место среди переносимого, но пока перенос её не забирает, и запись обязана говорить правду.

## 7. Грабли

**Манифест шаблона живёт в БАЗЕ.** Правка `templates/certification/manifest.json` не видна
приложению, пока шаблон не перезалит через `PUT /api/admin/templates/<id>/update`: реестр
читает свою копию в `uploads/templates/<id>`, а не файл репозитория.

**Экспорт опубликованного теста берёт СНАПШОТ.** После смены шаблона или настроек пакет
остаётся прежним байт в байт, пока не опубликованы изменения
(`POST /api/tests/:id/republish-force`). На этом дважды потерян прогон.

**Результат попытки заморожен вместе с попыткой.** Блоки разделов, заведённые ПОСЛЕ сдачи
попытки, в её отчёте не появятся: `groupKey` тем в сохранённом результате остался `null`.
Нужна новая попытка, а не правка кода.

**Полный прогон на загруженной машине не стартует.** Первый заход дал 585 файлов с
`Vitest failed to find the runner` — ни один тест не запустился; это голодание воркеров, а
не регрессия. Помогает `TEST_MAX_WORKERS=2 npm test`.

**`git add -A` на общей рабочей копии.** В дереве лежит ЧУЖАЯ незакоммиченная работа по
деплою: `config/mirror.config.jsonc`, `docker/templates/nginx-site-*.conf`,
`scripts/deploy/deploy-mirror.bat`, правки `scripts/deploy/deploy.sh`, `deploy.bat`,
`run-deploy.sh` и `.gitattributes`. К PRD-51 отношения не имеет — не коммитить.

**Контейнер `test-builder-db` был остановлен** и поднят командой `docker start
test-builder-db` 2026-08-16: без него падает `tests/migration-prd7.test.ts`. Если его
остановили намеренно, состояние надо вернуть.

**Мок хранилища.** Новый метод `IStorage`, который читает маршрут тестов или попыток, ломает
разом восемь файлов с моками (`tests/routes.tests.test.ts`,
`tests/routes.attempts-tests.test.ts` и далее): маршрут отвечает пятисоткой. Точечные
прогоны этого не показывают — проверять полным.

## 8. Следующий шаг

Закрывать хвосты §3 в таком порядке:

1. `docs/ROADMAP.md` — завести запись о PRD-51 по образцу записи PRD-50 и строку долга
   «экран итогов остаётся на `resultsBlockOrder`» в §0.3. Спецификация §14 уже ссылается на
   эту запись, и сейчас ссылка ведёт в пустоту.
2. Отметить выполненные пункты в четырёх планах либо проставить в их шапке «выполнен, см.
   отчёт приёмки» — сегодня план читается как неначатый.
3. Согласовать момент и прогнать `npm run test:cov` в одиночку.
4. Решить судьбу локальной ветки `prd-51-report-document-blocks`.
