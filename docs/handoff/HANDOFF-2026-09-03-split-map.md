# Карта разделения работ: четыре параллельные сессии

Дата: 2026-09-03. Трек: [перестройка настроек редактора
теста](../plans/2026-09-02-editor-settings-restructure.md).

Документ для сессии-интегратора и для всех четырёх параллельных сессий: кто что берёт, где
границы по файлам и в каком порядке вливать.

## 1. Что уже закрыто

Э0 (эскизы, приняты 2026-09-03), Э1 (подытоги без вердикта), Э3 (перестройка ящика, восемь
шагов), Э2.1-Э2.5 (хранение и выдача обратной связи подтем, замена текста темы, реестр
вопросов, тексты уровней), Э2.6 частично: пакет SCORM, снимок публикации, перенос `.tbtest` и
книга Excel уже возят тексты подтем.

Остаток Э2.6 (строка листа-примера книги, тест кругового пути) держит интегратор — правка
идёт в файлах, которые он ещё не отпустил.

## 2. Разделение

| Сессия | Этап | Основные файлы | Оценка |
| --- | --- | --- | --- |
| A | Э4 «Свободная навигация» | `shared/schema.ts` (колонка), новая миграция, `shared/template/question-progress-context.ts`, `server/scorm/template/app/render/mainRender.js`, `NavigationPane` | 1,5-2 сессии |
| B | Э5 «Именование и книга» | `server/utils/workbook-settings.ts` (алиасы), подписи в `sections/*.tsx`, `workbook-template.ts` (справка и пример) | 1 сессия |
| C | Э6 «Шаблоны и контракт» | `server/scorm/templates/default/layouts/**`, `templates/certification/**`, спека формата + руководство разработчика + PDF | 1-1,5 сессии |
| D | Э7.1-Э7.3 «Документация» | `docs/guides/test-authoring-guide.md`, `docs/guides/import-workbook-guide.md`, `docs/ROADMAP.md` | 1 сессия |

Приёмка (Э7.4-Э7.6) — за интегратором, после того как влиты A, B и C. Разделять её нельзя:
проверять надо ящик целиком, а не по вкладке на сессию.

Передачи: [Э4](HANDOFF-2026-09-03-e4-free-navigation.md),
[Э5](HANDOFF-2026-09-03-e5-naming-and-workbook.md),
[Э6](HANDOFF-2026-09-03-e6-templates-and-contract.md),
[Э7.1-Э7.3](HANDOFF-2026-09-03-e7-docs.md).

## 3. Где сессии пересекаются

Пересечений всего три, и все они управляемые:

1. **`server/utils/workbook-settings.ts` — A и B.** B заводит МЕХАНИЗМ алиасов (правка
   структуры `SettingParam`), A ДОБАВЛЯЕТ параметр. Порядок: сначала B вливает алиасы, потом
   A добавляет строку. Иначе A придётся переписывать свою строку под изменившуюся структуру.
2. **`shared/template/*` — A и C.** A правит расчёт доступности вопросов, C — раскладки и
   контракт. Файлы разные; общий только каталог. Конфликтов не будет, но C не должен
   «попутно» чистить контекст прогресса.
3. **Руководства — B, C и D.** D пишет руководства автора и импорта, C — руководство
   РАЗРАБОТЧИКА шаблонов и спеку формата. B не правит руководств вовсе: перечень пар «старое
   имя → новое» B отдаёт D сообщением.

Ничего из этого не требует блокировок: достаточно порядка «B перед A в одном файле» и запрета
на попутные правки в чужих файлах.

## 4. Предусловие старта

Все четыре сессии стартуют от коммита, в котором лежат Э1-Э3 и Э2.1-Э2.6. Пока он не влит в
`main`, параллельный старт бессмысленен: сессии будут писать под старое дерево вкладок.

Каждая сессия работает **в своём git worktree**. Общими остаются dev-БД, SMTP-релей и сама
машина — отсюда два правила:

- **полный `npm test` — только по явному разрешению владельца**, он занимает машину на восемь
  минут; в работе точечные прогоны `npm test -- <путь>`;
- **`npm run test:cov` запускается в одиночку**: покрытие чистит `./coverage`, и два прогона
  съедают результаты друг друга.

## 5. Порядок вливания

`B → A` (в общем файле), `C` и `D` независимо в любой момент. `D` дописывает разделы про Э4 и
Э5 после того, как A и B сообщат о готовности. Последним идёт интегратор: остаток Э2.6,
приёмка в браузере, приёмка на бумаге, полный прогон и `test:it`.
