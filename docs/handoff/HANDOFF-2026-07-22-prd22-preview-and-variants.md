# Передача работы: PRD-22 (поля страницы, последовательности) и правки предпросмотра шаблона

Дата: 2026-07-22. Ветка: `feat/prd12-fr6-content-pages`.
Базовый коммит: `f05a650 chore(templates): перегенерировать preview.html под рефакторинг рантайма FR-6`.
Статус: в работе. Ничего из описанного НЕ закоммичено.

## 1. Задача

Две связанные линии.

Первая — PRD-22 «Поля контентной страницы и последовательности страниц»
([docs/specs/prd-22/page-fields-and-sequences.md](../specs/prd-22/page-fields-and-sequences.md),
37 требований). Повод: вариант «Галерея» шаблона `certification` был недоступен в «Структуре».
Разбор показал три системных пробела, а не единичную ошибку: авторская страница жёстко
приравнена к одному виду варианта; свойства страницы (точки навигации, подпись кнопки, фон)
не объявлены ни в одном манифесте; количество точек вводилось автором вручную. Решение:
галерея — обычная информационная страница со своим макетом; состав полей задаёт ТОЛЬКО
манифест (`placeholders[]` — содержимое, новый `settings[]` — свойства); последовательности
вычисляются ядром из структуры теста. Все этапы PRD-22 (Э0-Э7) реализованы.

Вторая — план [docs/plans/PLAN_PRD22_PREVIEW_AND_VARIANTS.md](../plans/PLAN_PRD22_PREVIEW_AND_VARIANTS.md)
из 10 этапов, согласованный 2026-07-22 по итогам приёмки: четыре дефекта предпросмотра
шаблона плюс два требования заказчика (сетка из 6 раскладок контентной страницы в двух
семействах; понятные названия цветов). Выполнен только Э1.

Требования, изменённые заказчиком по ходу и зафиксированные в плане:

- фоллбек варианта не должен быть тихим: решение о замене принимает автор через
  сопоставление, вариант № 1 — лишь то, что показывается ДО сопоставления, и предвыбор в
  диалоге;
- тесты с проблемными страницами подсвечиваются в списке тестов жёлтым знаком.

## 2. Что сделано

### 2.1 PRD-22, этапы Э0-Э7

- Реестр типов полей и режимов ввода: [shared/template/field-types.ts](../../shared/template/field-types.ts).
- Ядро последовательностей (участок структуры, отрезок не короче двух страниц, блок
  `page.dots/dotIndex/dotsTotal/canGoBack`): [shared/template/page-sequences.ts](../../shared/template/page-sequences.ts).
- Подстановка базового пути к ресурсам шаблона: [shared/template/asset-base.ts](../../shared/template/asset-base.ts);
  применяется в рантайме пакета ([server/scorm/template/app/templateLoader.js](../../server/scorm/template/app/templateLoader.js)),
  на веб-хосте ([server/routes/tests.ts](../../server/routes/tests.ts)), в наборе шаблона
  ([server/services/template-package.ts](../../server/services/template-package.ts)) и в
  предпросмотре реестра ([server/routes/admin-templates.ts](../../server/routes/admin-templates.ts)).
- Маршрут раздачи файлов шаблона `GET /api/templates/:id/assets/*`:
  [server/routes/templates.ts:173](../../server/routes/templates.ts).
- Санитайзер HTML вынесен в общий модуль `shared/security/html-sanitize.ts`;
  `server/utils/html-sanitizer.ts` оставлен тонким реэкспортом.
- Хранение свойств страницы: колонка `content_pages.settings_json`, миграция
  `drizzle/0001_content_pages_settings_json.sql`, нормализация и санитизация в
  [server/routes/content-pages.ts](../../server/routes/content-pages.ts), доставка в пакет —
  [server/scorm/builders/test-json.ts](../../server/scorm/builders/test-json.ts).
- Валидация типов полей при загрузке и активации шаблона:
  [server/services/template-validation.ts](../../server/services/template-validation.ts),
  [server/routes/admin-templates.ts](../../server/routes/admin-templates.ts).
- Редактор: контролы по перечню типов, поле идентификатора последовательности, предпросмотр
  изображения, режимы ввода — [client/src/features/tests/editor/use-content-pages.ts](../../client/src/features/tests/editor/use-content-pages.ts),
  [client/src/features/tests/editor/sections/start-pages-section.tsx](../../client/src/features/tests/editor/sections/start-pages-section.tsx).
- Новый компонент дизайн-системы `RichTextEditor`: [vendor/ui-kit/src/components/RichTextEditor.tsx](../../vendor/ui-kit/src/components/RichTextEditor.tsx),
  стили добавлены в ОБЕ копии `skillum-ds.css`.
- Шаблоны: вариант галереи в `server/scorm/templates/default` и `templates/certification`,
  макеты на `data-placeholder` и `page.*`.

### 2.2 Дефект навигации галереи (найден и закрыт 2026-07-22)

Кнопка «Далее» на странице галереи в SCORM-рантайме не работала: обработчик искался
селектором `.navigation [data-nav="next"]`, а макет `certification` кладёт кнопки в
`.gallery__nav`. Контракт — атрибут `data-nav`, а не класс обёртки.

- [server/scorm/template/app/render/contentPage.js](../../server/scorm/template/app/render/contentPage.js) —
  добавлена `findScreenNavButton(app, dir)`: поиск по `data-nav` с пропуском кнопок внутри
  `[data-slot="page-content"]` (авторский контент не должен становиться навигацией экрана).
- [server/scorm/template/app/contentFlow.js](../../server/scorm/template/app/contentFlow.js) —
  поститоговые страницы перевешивают кнопку макета, если `.navigation` нет.
- [server/scorm/template/app/routerFlow.js](../../server/scorm/template/app/routerFlow.js) —
  хаб снимает лишнюю кнопку по контракту `data-nav`.
- Тест: [tests/runtime.content-page-nav.test.ts](../../tests/runtime.content-page-nav.test.ts), 8 проверок.

### 2.3 Этап Э1 плана: ложные предупреждения проверки шаблона

- [shared/template/preview-context.ts](../../shared/template/preview-context.ts) — функция
  `expectedContentSlots(layoutKey)`: слот `page-content` ожидается только когда экран
  рендерится общей обёрткой (`layoutKey === "content"`). Применена в трёх местах.
- [shared/template/smoke-runner.ts](../../shared/template/smoke-runner.ts) — формулировка
  отсутствующего слота больше не утверждает подмену стандартным шаблоном (это правда только
  при отсутствии макета); добавлена `checkPlaceholdersReachTheScreen`: предупреждение, если
  объявленные поля варианта не имеют области в макете — отдельно для полной потери и для
  частичной (с перечислением ключей).
- Тесты: [tests/smoke-runner.test.ts](../../tests/smoke-runner.test.ts), 26 проверок.

### 2.4 Прочее

- План работ: [docs/plans/PLAN_PRD22_PREVIEW_AND_VARIANTS.md](../plans/PLAN_PRD22_PREVIEW_AND_VARIANTS.md).
- Скилл сборки этого документа: `.claude/skills/handoff/SKILL.md`.
- Документы PRD-22: обновлены `spec-template-platform.md`, `guides/template-development.md`,
  `prd-1/templates-content-pages.md`, `prd-3/external-templates.md`, `ROADMAP.md`.

### 2.5 Чужие правки в дереве

`config/*.jsonc` (реальные параметры SMTP) и `.vscode/settings.json` изменены НЕ этой
работой. Их не трогать и не коммитить вместе с остальным. Архивы
`templates/certification3-1.3.0.zip` и `templates/certification4.zip` присланы заказчиком для
разбора.

## 3. Что НЕ сделано

- Этапы Э2-Э10 плана: показ текста предупреждений в модалке; рельс предпросмотра от
  вариантов; сетка из 12 вариантов контентной страницы; нетихий фоллбек; подсветка проблемных
  тестов в списке; перевод `ColorPicker` на общий `Popover`; порядок в названиях цветов;
  документы; сквозная приёмка. Не начаты.
- PRD-22: приёмка в живом редакторе после применения миграции к базе стенда и обновление
  ЗАГРУЖЕННОЙ копии шаблона `certification` через раздел «Шаблоны» (раздел 6 PRD).
- Строка этапа Э6 в таблице PRD-22 помечена «Впереди», хотя документы правились. Расхождение
  внутри документа: шапка называет Э6 закрытым. Не выверено.
- Аудит остатка PRD-22 (многоагентный, 2026-07-22) дал 91 подтверждённую находку; сводный
  список НЕ собран — синтез прервался по лимиту сессии. Данные лежат в журнале запуска
  `wf_1a98f588-629` в каталоге сессии.
- Дефект шаблона `certification4`: верстка старта не сжимается (адаптив написан через
  `@media` по ширине ОКНА, а контейнер предпросмотра узкий), из-за чего изображение
  схлопывается до 20 px. Правится в шаблоне заказчика, не в платформе.

## 4. Как проверить

Стенд поднимается локально:

```bash
npm run dev          # http://localhost:8081
npm run scorm:player # http://localhost:5050, играет пакеты из out/
```

Учётные записи стенда — в базе разработки (параметры подключения в `.env`); пароль
администратора при необходимости переустанавливается инструментом `npm run create-admin`.

Проверка правок Э1 вручную: «Шаблоны» -> у карточки «Стандартный» меню -> «Предпросмотр» ->
«Перепроверить». Ожидается «Проверка пройдена, 16 из 16 экранов», предупреждений нет. Для
«Сертификация (РТК)» — 15 из 15.

Проверка навигации галереи: «Тесты» -> «Сертификационный тест для руководителей» -> меню ->
«Тестовый прогон» -> «Начать тестирование» -> раздел «О компании» -> «Начать». На галерее
кнопка «Далее» листает три слайда, индикатор двигается, после третьего идут вопросы.

Сборка пакета для локального плеера:

```bash
node --import tsx scripts/generate-template-scorm.ts uploads/templates/<id>
```

## 5. Состояние проверок

Прогон 2026-07-22 после Э1: `4791 passed, 9 failed` (263 файла).

Все 9 падений ПРЕДШЕСТВУЮТ этой работе и к ней не относятся: `tests/routes.analytics-*` и
`tests/routes.scorm-telemetry-analytics.test.ts` падают с `source.getScales is not a function`
— роут аналитики получил `loadScoringConfig` в коммите `a6ab296` (2026-07-15), а моки в
тестах старше. Не чинилось: вне объёма задачи.

Интеграционные тесты `npm run test:it` — 26 из 26 зелёных. `npx tsc --noEmit` — чисто.
`markdownlint-cli2` по новым документам — 0 замечаний.

Не проверено: полный прогон после последнего усиления `checkPlaceholdersReachTheScreen`
(частичная потеря полей) — прогонялся только `tests/smoke-runner.test.ts`, 26 из 26.

## 6. Принятые решения

- Отдельного вида страницы «галерея» нет: это вариант информационной страницы со своим
  макетом. Первоначальная попытка завести отдельный вид была отвергнута заказчиком и
  полностью откачена (этап Э0 PRD-22).
- Вкладка «Структура» НЕ меняется — жёсткое требование заказчика. Всё новое умещается в уже
  реализованную форму страницы (FR-20).
- Свойства страницы объявляются в манифесте отдельным блоком `settings[]`, а не в
  `placeholders[]`: разведены по природе — содержимое против свойств.
- Последовательность — отрезок не короче двух подряд идущих страниц одного участка с
  одинаковым непустым `sequenceId`. Группировка смотрит на идентификатор, а НЕ на вариант:
  поэтому демо-набор из страниц разных вариантов образует одну последовательность.
- Ожидание слота `page-content` снято для макетов варианта: поля попадают на экран через
  `data-placeholder`. Взамен введена проверка, что объявленные поля имеют область в макете.
- Фоллбек не тихий: `templateKey` в базе не переписывается, вариант меняется только через
  модалку «Сменить вариант» (она уже показывает теряемые поля). Это согласовано 2026-07-22 и
  отменяет более раннее предложение молча деградировать к варианту № 1.
- Правило «макет vs fallback» (отчёт `docs/reports/AUDIT_TEMPLATE_LAYOUT_FALLBACK.md`)
  согласовано ещё 2026-07-17, этапы 1-5 по нему выполнены. Не считать его открытым вопросом.

## 7. Грабли

- Дев-сервер не перезапускается сам: `tsx` работает без `--watch` намеренно (vite 8 виснет
  под `tsx watch`). После правки серверного TypeScript нужен ручной перезапуск `npm run dev`,
  иначе проверяется старый код.
- Рантайм-JS пакета (`server/scorm/template/app/**`) и `shared/template` подхватываются
  пересборкой пакета, а не перезапуском сервера. Отладочный плеер собирает пакет на лету —
  достаточно перезагрузить страницу отладки.
- Локальный SCORM-плеер залипает на состоянии попытки: «Сброс попытки», очистка localStorage
  и новый `object_id` не помогают. Надёжно помогает только загрузка пакета под НОВЫМ именем
  файла.
- `skillum-ds.css` существует в двух копиях: `vendor/ui-kit/css/` — источник,
  `client/src/styles/vendor/` — то, что реально грузится. Править обе.
- Предпросмотр шаблона в реестре собирается ОТДЕЛЬНЫМ маршрутом `smoke-bundle`
  ([server/routes/admin-templates.ts](../../server/routes/admin-templates.ts)), а не
  `readTemplateBundle`. Правку, касающуюся макетов, нужно вносить в оба места — на этом уже
  один раз потерялась подстановка базового пути.
- Смена идентичности экранов делает СОХРАНЁННЫЕ отчёты проверки устаревшими: точки в рельсе
  серые до нажатия «Перепроверить». Гейт активации при этом работает.
- База стенда — не seed-овая: `admin@test.com / admin123` не подходит, в ней лежат настоящие
  пользователи.
- `npx` тихо правит `package.json` (бампает `tsx`). Вызывать бинарники из `node_modules/.bin/`.

## 8. Следующий шаг

Этап Э2 плана: показать текст предупреждений в модалке проверки шаблона. Точка входа —
[client/src/features/templates/preview-check-modal.tsx:276](../../client/src/features/templates/preview-check-modal.tsx),
где блок сообщений сейчас рендерится только при статусе `fail`; нужен такой же блок для
`warn` с тоном warning и стилем `tpl-check-errors--warn` в слое компонентов дизайн-системы.
