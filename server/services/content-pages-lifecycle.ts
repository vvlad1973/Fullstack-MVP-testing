/**
 * @module server/services/content-pages-lifecycle
 * @description Совместимость адреса. Планировщик системных `content_pages` переехал в
 * {@link module:shared/content-pages/lifecycle}: он всегда был чистой функцией, а
 * теперь нужен ОБОИМ хостам — сервер раскладывает по нему строки в транзакции, а
 * редактор предсказывает по нему структуру теста, которого ещё нет (вкладка
 * «Структура» до первого сохранения). Две реализации разошлись бы на первой же правке,
 * и автор увидел бы перед сохранением одну структуру, а получил другую.
 *
 * Новый код импортирует из `@shared/content-pages/lifecycle` напрямую.
 */
export * from "@shared/content-pages/lifecycle";
