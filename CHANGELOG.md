# Changelog

## [2.16.0](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.15.1...v2.16.0)

### Features

- **feat**(tests): новый тест по умолчанию отдаёт в LMS текущую попытку (2026-08-15) [`828a6040523f388b37d690d9b14694db564291be`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/828a6040523f388b37d690d9b14694db564291be)
  Умолчание настройки «результат для LMS» переведено на «последнюю» (миграция 0023
  меняет DEFAULT колонки). Так ведёт себя платформа: содержимое отчитывается за
  текущую попытку, а какую засчитать — решает LMS своей настройкой. На WebTutor это
  единственно возможное поведение: состояние прогона не переживает границу попытки,
  поэтому сохранённой «лучшей» к моменту отправки там просто не существует.
  
  Тесты, заведённые ДО колонки, остались на «лучшей»: значение проставлено миграцией
  0022, и менять его задним числом нельзя — это молча поменяло бы оценку в LMS у
  курсов, которые уже идут. Пакет без поля рантайм по-прежнему читает как «лучшую»,
  поэтому уже выданные пакеты не меняются вовсе.

- **feat**(db): npm run dev сам проверяет и догоняет схему dev-БД (2026-08-15) [`e9284f5edbc9fc62d13aa6e9a61f06bcc4c05cad`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e9284f5edbc9fc62d13aa6e9a61f06bcc4c05cad)
  `predev` сверяет журнал drizzle с журналом применения в базе ПО ХЕШУ файла (время
  для этого не годится: `migrate` сравнивает именно его, и перегенерированная миграция
  уже ломала выкат) и решает, что делать с отставанием.
  
  Аддитивные миграции применяются сразу: `ADD COLUMN` со значением по умолчанию
  совместим со старым кодом, и соседняя сессия его не заметит. Разрушительные —
  DROP COLUMN, RENAME, смена типа, DROP TABLE/CONSTRAINT, TRUNCATE — не применяются
  никогда: dev-БД одна на все рабочие копии, и снятая колонка ломает чужую сессию
  мгновенно. В этом случае проверка печатает список с причинами, называет команду
  `npm run db:migrate` и не пускает сервер, пока человек не решит.
  
  Проверка молчит, когда применять нечего, не работает при NODE_ENV=production
  (там схему ведёт деплой) и отключается через SKIP_MIGRATION_CHECK=1. Недоступная
  база её не роняет: об этом сообщит сам сервер.

- **feat**(tests): автор выбирает, какой результат уходит в LMS (2026-08-15) [`d818ee8b64636eadd7c1f16f791439506fc4228a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d818ee8b64636eadd7c1f16f791439506fc4228a)
  Пакет всегда подменял результат на лучшую попытку. Стандарт этого не решает: SCORM
  ничего не требует от LMS о хранении истории, а платформы (Moodle, Blackboard,
  Teachbase) держат выбор «лучшая / последняя» у себя и ждут от содержимого данные
  ТЕКУЩЕЙ попытки. Подменяя, пакет перебивает настройку LMS, а в отчёте попытки
  оказываются ответы ЧУЖОЙ попытки. Но LMS, хранящая лишь снимок, при «последней»
  безвозвратно перекроет удачную попытку неудачной — поэтому выбор отдан автору,
  а не зашит.
  
  Настройка `lmsAttemptResult` (колонка + миграция 0022, «Ограничения» редактора)
  идёт через снимок и перенос теста автоматически. Умолчание `best` — поведение уже
  выданных пакетов, так что ни один существующий тест не меняется; в TEST_DATA
  выпекается только значение `last`, поэтому пакеты без настройки байт-в-байт прежние.
  
  При `last` повопросная детализация в suspend_data не хранится вовсе: её единственный
  потребитель — сборка interactions по лучшей попытке. В вебе настройка не применяется.
  
  Проверено в браузере на собранном пакете: при «лучшей» в LMS уходят сохранённые 95 %,
  при «последней» — текущие 25 % с вердиктом «не пройден» и полным набором interactions.

- **feat**(scorm): отладочный плеер показывает состояние формата 2 и долю бюджета (2026-08-15) [`325afbf54e4d695d617390740ca133c8313d22f6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/325afbf54e4d695d617390740ca133c8313d22f6)
  Панель прогона получила показатель «Состояние прогона»: сколько символов занято из
  4096 и какая это доля. Переполнение проявляется молча — LMS обрезает строку, и разом
  теряются счётчик попыток, таймер и оба барьера, — поэтому запас видно ДО того, как он
  кончится. Состояние пакета, собранного до PRD-36, помечается как формат 1.
  
  Инспектор читает попытки нового формата (лучшая и последняя вместо списка), строит
  протокол прошлой попытки из позиций через кодек пакета и описывает запись состояния
  по смене сводки, а не по росту длины массива. Записи легаси-пакетов по-прежнему
  показываются как раньше — обе формы обслуживает одна вкладка.

- **feat**(scorm): экраны и отчёт строятся по сводке попытки (2026-08-15) [`9b56c3de982ca871a1fe3337a061e06ad9e0879a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9b56c3de982ca871a1fe3337a061e06ad9e0879a)
  Хелперы состояния отдают сводку, развёрнутую обратно в форму результата: названия
  тем, материалы, порог и блок раздела возвращаются из TEST_DATA по позиции, ключ
  разреза — из списка ключей. Экраны, «Мой результат», PDF и барьер интервала читают
  её теми же именами, что и раньше, поэтому их логика не менялась.
  
  getAllAttempts удалён, а не оставлен заглушкой: пустой список молча означал бы
  «завершённых попыток не было» и открыл бы лимит заново. «Есть завершённые» теперь
  счётчик, число попыток в отчёте — тоже.
  
  Отчёт в LMS по лучшей попытке собирается из её собственных рядов — выдачи, ответов
  и СТАТУСОВ. Этим закрыт третий дефект §8: статусы с попыткой не сохранялись, и
  interactions прошлой попытки фильтровались статусами текущей, то есть у теста с
  «возвратом к неотвеченным» в LMS уезжал искажённый протокол.
  
  Гейт повторного входа опознаёт оба формата: пакеты, собранные до PRD-36, продолжают
  писать свой массив, и их учащийся не должен упереться в кулдаун при возврате.

- **feat**(scorm): состояние прежнего формата приводится к формату 2 (2026-08-15) [`a63dafdf82871803c72394d34d727b577b2bf7a9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a63dafdf82871803c72394d34d727b577b2bf7a9)
  Пакет после правки встречает три вида состояния. Формат 2 своего пакета проходит
  насквозь. Формат 1 сворачивается: массив попыток превращается в счётчик, лучшую
  и последнюю, содержимое вопросов и названия тем отбрасываются, тема адресуется
  номером раздела. Формат 2 ЧУЖОГО пакета опознаётся по отпечатку состава — его
  позиции адресуют другие вопросы, поэтому детализация и прогон в работе снимаются,
  а счётчик попыток, якорь таймера и обе даты барьеров сохраняются.
  
  Приведение подключено в readSuspendObj, то есть в одной точке: ни один потребитель
  состояния не разбирает версию формата сам.

- **feat**(scorm): бюджет suspend_data с объявленным порядком жертв (2026-08-15) [`a15ee1e068e565746f172056ded484f09924cd9d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a15ee1e068e565746f172056ded484f09924cd9d)
  Перед записью длина состояния сверяется с бюджетом 4096, и превышение снимается
  по объявленному порядку: детализация лучшей, затем карта перемешивания прогона,
  затем вырождение сводок. Счётчик попыток, момент завершения, якорь таймера PRD-20
  и дата кулдауна PRD-6 не жертвуются никогда — на них стоят лимит попыток и оба
  барьера допуска. Режется копия: исходное состояние в памяти рантайма не портится.
  
  Чтение различает три исхода вместо двух: пусто, разобрано, повреждено. Раньше
  обрезанная LMS строка возвращалась как «состояния нет» и молча открывала заново
  и лимит попыток, и таймер, и барьеры; теперь это попадает в диагностику.

- **feat**(scorm): попытка хранится сводкой, история списком не ведётся (2026-08-15) [`48f4d761c64966e621caae8b5ef851d9930d2816`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/48f4d761c64966e621caae8b5ef851d9930d2816)
  saveAttemptResult собирает сводку в ЕДИНСТВЕННОЙ точке (TBRunState.buildSummary)
  независимо от того, каким из пяти путей завершилась попытка, и кладёт в состояние
  счётчик, лучшую и последнюю вместо растущего массива attempts[].
  
  Сводка адресует тему номером раздела, ключ разреза — номером в списке ключей
  пакета, и несёт выданный вариант PRD-17 с разрешённым порогом PRD-24: без них
  метка «Требуется…» у сохранённой попытки печаталась бы пустой. Названия тем,
  рекомендованные материалы и тексты ключей в состояние больше не попадают — они
  и так запечены в TEST_DATA.
  
  Повопросные ряды хранит только лучшая (FR-06): последняя — сводка без детализации.
  Отбор области разреза переехал из suspendAttempts.js в buildSummary, туда же, где
  ключ превращается в номер; тест breakdown-block-hosts проверяет тот же смысл через
  новую точку.

- **feat**(scorm): выдача и варианты адресуются позициями в TEST_DATA (2026-08-15) [`a4ccad1892788b232f456485e237c3d27240c186`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a4ccad1892788b232f456485e237c3d27240c186)
  generateVariant пинит адрес каждого выданного вопроса (номер раздела + номер
  в банке раздела) и карту выданных вариантов PRD-17 по темам. Адрес снимается
  рядом с розыгрышем — только там ещё известно, из какого банка пришёл вопрос.
  
  TEST_DATA получает канонический список ключей разреза: в состоянии прогона ключ
  адресуется НОМЕРОМ в нём. Список собирается из тегов вопросов (из них ключи и
  возникают) и из ключей порогов раздела — порог по ключу, которого нет ни у одного
  вопроса, иначе остался бы без адреса. Теста без тегов и порогов правка не касается.
  
  Проверка позиций исполняет настоящий generateVariant, а не сверяет исходник
  регуляркой: адрес и содержимое обязаны сойтись, иначе ученик увидит чужой вопрос.

- **feat**(scorm): кодек рядов состояния прогона (PRD-36 §4.6) (2026-08-15) [`62217313cf1bd6a8f72b6363e9e6e8bb5fda05f4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/62217313cf1bd6a8f72b6363e9e6e8bb5fda05f4)
  Модуль runState.js (глобал TBRunState) — чистые функции без обращения к SCORM,
  поэтому проверяются из исходников без сборки пакета. Ряд хранится одной строкой,
  элемент адресуется позицией: выдача парами «раздел.вопрос», ответы по типу вопроса
  (единичный выбор, множественный, ранжирование, сопоставление, распределение баллов),
  статусы одним символом, перемешивание вариантов картой-строкой.
  
  Отпечаток состава пакета нужен для FR-02: позиция валидна только внутри своего
  пакета, и состояние, пришедшее от другого, обязано опознаваться, а не молча
  адресовать чужие вопросы.
  
  Порча ряда (FR-23) считается его отсутствием: NaN-адрес отсекается, недостающие
  ячейки читаются как пустые ответы.

- **feat**(debug-player): отладочный прогон объясняет невыполнимую выдачу (2026-08-15) [`1cabbdeec02872ee25019d00616c496b48a537d1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1cabbdeec02872ee25019d00616c496b48a537d1)
  Отладочный проигрыватель выполнимость выдачи не проверял вовсе: непроходимая
  адаптивная лестница выглядела как «Вопрос 1 из 0» и замерший экран. Именно на
  этом встала приёмка Э5 PRD-50 — прогон шёл мимо гейта публикации, потому что
  публикации не было.
  
  Сессия отдаёт находки `assessTestPublish` вместе с токеном, проигрыватель
  печатает их лентой над сценой. Не отказ, и это осознанно: отладочный прогон не
  публикация, а в многотемном тесте сломан может быть один уровень — отлаживать
  остальное автор вправе. Сбой самой проверки роняет замечание, а не сессию.
  
  Проверено в браузере на dev: тест `3542edef` с пустым уровнем печатает ленту с
  разбором по уровню и действием «Открыть тест в редакторе», сцена под ней живая.

- **feat**(tests): редактор предупреждает о невыполнимой выдаче при сохранении (2026-08-15) [`074f7de11f2ffb61b1b0fbdbe4351208072e7b1f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/074f7de11f2ffb61b1b0fbdbe4351208072e7b1f)
  PRD-15 FR-05 требует звать сервис выполнимости на ВСЕХ путях изменения, а для
  черновика назначает политику «предупреждение без блокировки». Авторская правка
  самой адаптивной лестницы была единственным путём мимо проверки: уровень с
  диапазоном сложности, под который в теме нет ни одного вопроса, сохранялся
  молча. Автор узнавал о нём либо от вставшего прогона («Вопрос 1 из 0»), либо
  от отказа публикации — то есть сильно позже, чем ошибся.
  
  Заведён `GET /api/tests/:id/feasibility`: тот же `assessTestPublish`, что уже
  сторожит публикацию (FR-06), но только на чтение. Одна проверка на оба пути —
  язык находок не разъезжается.
  
  Редактор читает его после успешного сохранения и отдаёт находки списку тестов,
  который показывает их тем же advisory-диалогом, что и замечания публикации
  (PRD-50 FR-45 - FR-47). Внутри ящика показывать нельзя: сохранение его
  закрывает — по этой же причине чтение сделано ЧАСТЬЮ сохранения (await), а не
  фоновым запросом: иначе находки приезжают, когда показывать их уже некому.
  
  Диалогу добавлен `advisoryFooter`: зашитая строка режима говорит «Тест
  опубликован», что для сохранённого черновика прямая неправда. Формулировка
  находки тоже своя (`describeFeasibilityState`): существующая написана для
  удаления и говорит «останется», а здесь ничего не удаляют.
  
  Проверено в браузере на dev: сохранение теста с пустым уровнем печатает диалог
  «Тест сохранён, но выдать вопросы по нему нельзя» с разбором по уровню.

- **feat**(prd-50): поле места подытогов говорит про адаптивный режим (2026-08-15) [`a0a00bd54416986c9ec6776db044a151e608b58f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a0a00bd54416986c9ec6776db044a151e608b58f)
  Значение «В карточках тем» в адаптивном тесте не печатает ничего. Подменять
  выбор автора значением «блоком» молча хуже, чем честно назвать ограничение
  режима, — поэтому подсказка, а не подстановка.

- **feat**(prd-50): адаптивные экран и отчёт обоих шаблонов печатают сводный блок (2026-08-15) [`ea7f7cecfeb7f83ff76cf1c7d30c1f087e432b4c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ea7f7cecfeb7f83ff76cf1c7d30c1f087e432b4c)
  Состав подблоков этого экрана объявляет манифест — разрез встал последним, после
  тем, шкал и показателей. Сводки баллов у адаптивного экрана нет и быть не может,
  состав это по-прежнему и говорит.

- **feat**(prd-50): оба хоста доводят записи области теста до адаптивных итогов (2026-08-15) [`78359668430de0795fd40ba2f8cd66f05413217a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/78359668430de0795fd40ba2f8cd66f05413217a)
  Веб берёт их из сохранённой попытки, пакет — из результата, восстановленного в
  стандартную форму: сама лестница несёт только потемные счётчики уровней.
  Адаптивная выгрузка PDF пересобирает результат вручную, поэтому записи ей
  доносятся отдельной строкой — иначе документ печатал бы пустой блок при живом
  блоке на экране (§5.2).

- **feat**(prd-50): адаптивный контекст итогов несёт сводный блок разреза (2026-08-15) [`055042251b83f0a8dbf6f25cd40d89e0751aefd0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/055042251b83f0a8dbf6f25cd40d89e0751aefd0)
  Заполнение блока вынесено в общий fillBreakdownBlock: оба режима отвечают им на
  один и тот же вопрос, и вторая копия — это способ разойтись в округлении,
  формулировке или гейте. Секционные записи адаптивный вход не берёт: карточка
  темы говорит уровнем, и полоса процентов рядом с ним предлагала бы смысл,
  которого у лестницы нет.

- **feat**(prd-50): сводный разрез по тесту печатается отдельным блоком итогов (2026-08-14) [`f4e95235793071b0c4017161ac155dac657f1832`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f4e95235793071b0c4017161ac155dac657f1832)
  Ключ `breakdown` заведён в порядке блоков итогов и объявлен в манифестах обоих
  поставляемых шаблонов; заголовок берётся из надписи `results.breakdown`. Шаблон,
  не знающий ключа, блок не печатает — состав подблоков чистится о его собственное
  объявление.
  
  Записи области ТЕСТА берутся из сохранённого результата попытки и печатаются как
  есть: ключ, выданный в двух разделах, ядро свело отдельным проходом, и складывать
  секционные строки нельзя. Пакет уносит эти записи в suspend_data, иначе «Мой
  результат» и отчёт по сохранённой попытке печатали бы блок пустым.
  
  Показом управляет новое положение настройки разреза: «в карточках тем» (пусто —
  именно оно), «сводным блоком по тесту», «и там, и там». Видимость по-прежнему
  общая: `hidden` гасит обе проекции.

- **feat**(prd-50): экран итогов и отчёт печатают блоки разделов (2026-08-14) [`1efa1c41edd7248c10992b07f9b409feb2fa8944`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1efa1c41edd7248c10992b07f9b409feb2fa8944)
  FR-24/FR-25/FR-27: заголовок блока, счётчик «пройдено N из M» и карточки его тем,
  следом карточки тем без блока. Правлены оба штатных шаблона синхронно —
  `results.html` и `report.html`, стили в `theme.css` и `report.css` в токенах
  каждого шаблона; предпросмотры пересобраны генератором.
  
  Тема печатается РОВНО ОДИН раз: плоский `result.topicResults` остаётся полным
  (его печатает сторонний шаблон из реестра PRD-3), поэтому раскладка, печатающая
  блоки, печатает `ungroupedTopics`, а не весь плоский список. Три цикла и одна и
  та же карточка в каждом — частичных шаблонов у макета нет, а фильтровать список
  DSL не умеет.
  
  Тест без блоков печатает прежний плоский список и ни одного узла блока: полей
  `topicGroups`/`ungroupedTopics` у такого контекста нет вовсе.

- **feat**(prd-50): блоки разделов доезжают до сборщика контекста на обоих хостах (2026-08-14) [`4023fd72c03e5ca649a9f05e5f01d722546748b6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4023fd72c03e5ca649a9f05e5f01d722546748b6)
  FR-11/FR-27: ядро блоки считать умело с задачи 2, но их никто не передавал.
  Веб-адаптер перечисляет поля темы поимённо — `groupKey` терялся; список блоков
  теста (`tests.section_groups_json`) не ехал вовсе; пакет SCORM не нёс ни того,
  ни другого. Экран печатал плоский список при любых настройках автора.
  
  Проведено ровно тем же путём, что настройка разрезов: `sectionGroupsJson` в
  `MeasuresSource` из ВЫДАННОЙ версии теста — и в контекст экрана, и во вход
  отчёта. Ключ блока попадает в сохранённый результат попытки, поэтому попытка
  помнит принадлежность, с которой её оценивали.
  
  Пакет: блоки выпекаются в `TEST_DATA` только когда автор их завёл, раздел несёт
  `groupKey` тем же правилом — пакет теста без блоков остаётся байт-в-байт
  прежним. Рантайм читает ключ с самой темы, а у попытки, сохранённой до
  пересборки пакета, — из раздела: иначе «Мой результат» печатал бы плоский
  список там, где финиш того же теста печатает блоки.

- **feat**(prd-50): подписи вердикта темы и счётчика групп уходят в словарь PRD-49 (2026-08-14) [`62143bf0091f28c4746415986b5fcb2cf4a4e7fc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/62143bf0091f28c4746415986b5fcb2cf4a4e7fc)
  FR-34 (Э3, задача 4): «Пройдено»/«Не пройдено» темы и формат счётчика блока
  («N / M») больше не зашиты в shared/template/result-context.ts — они читаются
  из существующего словаря надписей PRD-49 через новые ключи
  topic.verdict.passed/failed/unknown и group.counter (формат "{passed} / {total}"),
  с откатом на прежние слова, если словарь не передан. Строка разреза внутри
  карточки темы печатает то же слово, что и сама карточка — один резолвер на оба
  места. Заодно объявлен results.breakdown — заголовок будущего блока разреза
  по области теста (Э4), чтобы оба манифеста не пришлось править дважды.
  
  Ключи объявлены в манифестах обоих поставляемых шаблонов («Стандартный»,
  «Сертификация»); предпросмотры пересобраны генератором.

- **feat**(prd-50): блоки итогов заводятся в редакторе, у раздела свой выбор блока (2026-08-14) [`62f3c20eb27e832d64b40f842922326bdac71a10`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/62f3c20eb27e832d64b40f842922326bdac71a10)
  FR-11/FR-12/FR-43: «Блоки итогов» — список рядом со структурой теста
  (topics-structure-section.tsx), автор заводит подпись, ключ генерируется из
  порядкового номера (group-1, group-2, ...) и никогда не выдумывается автором.
  У каждого раздела появляется селектор «Блок» с заведёнными блоками и пунктом
  «Без блока» (умолчание); селектор скрыт, пока блоков нет, — тест без блоков
  выглядит и ведёт себя как раньше. Удаление блока сразу снимает ссылку у всех
  разделов, которые на него указывали, чтобы в интерфейсе не было висящих ссылок
  (ядро такую ссылку и так трактует как «без блока», FR-12).
  
  Модель редактора и мапперы: TestEditorModel.sectionGroups и
  EditorSection.groupKey — оба опциональны, чтобы не трогать тестовые фикстуры
  соседних вкладок; apiToEditorModel/editorModelToPayload и
  mapEditorSectionsToPayload проводят их до PUT/POST тем же путём, каким уже
  ходят breakdownRules и formSet.
  
  Новые unit- и component-тесты покрывают round-trip API&lt;-&gt;модель и заведение/
  правку/удаление блока и выбор у раздела через updateModel.

- **feat**(prd-50): контекст выдачи несёт блоки разделов со счётчиком (2026-08-14) [`b64d4323ad41455ea382607e9497924273f1826f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b64d4323ad41455ea382607e9497924273f1826f)
  FR-25/FR-27/FR-29: `result.topicGroups` — блоки с темами, счётчиком и готовой
  надписью «1 / 2». Имя именно такое: `result.blocks` занято списком блоков итогов
  PRD-49. Рядом едет `result.ungroupedTopics` — разделы без блока, которые макет
  печатает после всех блоков: DSL не умеет фильтровать список, и без второго поля
  макет не отличил бы уже напечатанную карточку от ещё не напечатанной.
  
  Плоский `topicResults` остаётся полным, поэтому шаблон из реестра PRD-3, не
  знающий блоков, печатает ровно то же, что печатал. Тест без блоков не получает
  ни одного нового поля.
  
  Счётчик считает то же ядро, что и агрегат (`shared/scoring/section-groups`), по
  вердикту второго прохода; надпись собирается форматом `{passed} / {total}` —
  авторская формулировка придёт со словарём PRD-49 следующей задачей.

- **feat**(prd-50): ядро считает «пройдено N из M» по блоку разделов (2026-08-14) [`4ab54fb732c9781caf2eb6fe9e65af5a56c8de32`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4ab54fb732c9781caf2eb6fe9e65af5a56c8de32)
  FR-24 - FR-27: счётчик складывает вердикты разделов ПОСЛЕ второго прохода —
  того самого, где Э2 стал учитывать пороги ключей разреза. Знаменатель — только
  разделы с вынесенным вердиктом (FR-26); ссылка на несуществующий блок значит
  отсутствие блока, пустой блок не печатается (FR-12), а разделы без блока
  возвращаются отдельным списком для печати после всех блоков (FR-25).
  
  Правило одно на всех: `shared/scoring/section-groups` зовут и агрегат, и
  сборщик контекста, поэтому экран и сохранённый результат не могут разойтись в
  одном числе. Тест без блоков не получает ни поля, ни ключа у темы.

- **feat**(prd-50): блоки разделов хранятся у теста, а раздел ссылается на блок (2026-08-14) [`a73ae84c6ec56a6e4bea7da9246d5f2c7a4a4750`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a73ae84c6ec56a6e4bea7da9246d5f2c7a4a4750)
  FR-11/FR-12: `tests.section_groups_json` — упорядоченный список `{ key, label,
  order }`, `test_sections.group_key` — необязательная ссылка. Ссылка на
  несуществующий ключ и пустой блок допустимы: разрешает их ядро, а не база,
  поэтому внешнего ключа нет. Поля объявлены и в теле `PUT /api/tests/:id`, и в
  белом списке сохранения — иначе zod срезал бы их с бодрым 200 OK.
  
  Миграция сгенерирована и НЕ применена: базу приводит в порядок выкат.

- **feat**(prd-10): автор видит балл до «Применить», а не настраивает цену вслепую (2026-08-14) [`2d8c7499a285169f6f995104d0c9d319be5f449d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2d8c7499a285169f6f995104d0c9d319be5f449d)
  Конструктор цены ответа показывал только правила, но не их следствие: чтобы
  узнать, сколько даст ответ «две верных из трёх», автор сохранял настройку и
  проходил тест. Кнопка «Предпросмотр балла» в футере модала оценки открывает
  демо-ответ, счётчики c/x, сработавшую ступень и s/sMax/scoreRatio с вердиктом.
  
  Балл считает ОБЩИЙ движок по ЧЕРНОВИКУ конструктора — второй реализации правил
  не появилось, а эффект правки виден до «Применить».
  
  Демо-ответы генерируются из эталона, автор их не набирает: случай, которого
  эталон дать не может (лишний вариант, когда верны все; часть верных при одном
  верном; перепутанная пара, когда пара одна), просто отсутствует в списке, а не
  показывается сломанным.
  
  explainAnswer получил tierIndex: без него «сработавшую строку» пришлось бы
  вычислять второй копией предиката ступени. То же поле — в JS-двойнике пакета,
  иначе золотой тест паритета красный.

- **feat**(prd-50): предупреждения публикации о ключах, порогах и вариантах (2026-08-14) [`a09ffb06210a73b822d1f1670b95e0ff7bd92106`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a09ffb06210a73b822d1f1670b95e0ff7bd92106)

- **feat**(prd-50): таблица «раздел — ключ» с порогами и попаданием в варианты (2026-08-14) [`483dbbf761a97aac7cc870daf9b565381870a297`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/483dbbf761a97aac7cc870daf9b565381870a297)

- **feat**(prd-50): строка разреза несёт вердикт ключа на итогах и в отчёте (2026-08-14) [`b5d4329a2ae389776968cff5d4ebd7d51efc1b82`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b5d4329a2ae389776968cff5d4ebd7d51efc1b82)

- **feat**(prd-50): пороги ключей доезжают до веб-хоста и пакета SCORM (2026-08-14) [`a1733c9effd80188dc08e3fc29449a15d6c7eace`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a1733c9effd80188dc08e3fc29449a15d6c7eace)

- **feat**(prd-50): вердикт темы учитывает пороги ключей разреза (2026-08-14) [`2b9ba03b4fa4588e8475efeddf82c5a7d7c2a59b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2b9ba03b4fa4588e8475efeddf82c5a7d7c2a59b)

- **feat**(prd-50): пороги ключей разреза хранятся у раздела (2026-08-14) [`ae6280755a649d9887f9a4a798f0b6cb190fd3d5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ae6280755a649d9887f9a4a798f0b6cb190fd3d5)

- **feat**(prd-50): сохранённая попытка в пакете печатает полосы разреза (2026-08-14) [`999b5c2614a147c69869424fcc0c66b446e9d8e6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/999b5c2614a147c69869424fcc0c66b446e9d8e6)

- **feat**(prd-50): рантайм пакета считает разрезы адаптивного прогона (2026-08-14) [`8470b2d7af456fdae0424f3528515e542aff6631`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8470b2d7af456fdae0424f3528515e542aff6631)

- **feat**(prd-50): разрезы считаются и в адаптивном режиме на веб-хосте (2026-08-14) [`290b676f1ac8f266276b5363729bded3b3227281`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/290b676f1ac8f266276b5363729bded3b3227281)

- **feat**(prd-50): записи области теста сохраняются вместе с попыткой (2026-08-14) [`029cb2350c89892629f9986622cd65dace5e6adb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/029cb2350c89892629f9986622cd65dace5e6adb)

- **feat**(prd-50): строка разреза несёт полный состав по §8.1 (2026-08-14) [`2126a9d2c0d01706ba892fdff43a38f892a118ec`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2126a9d2c0d01706ba892fdff43a38f892a118ec)

- **feat**(docs): якоря заголовков в PDF, чтобы оглавления руководств кликались (2026-08-14) [`c3780a76bc8c1542051b41935e8c86dad650d959`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c3780a76bc8c1542051b41935e8c86dad650d959)
  markdown-it отдаёт голые заголовки без id, поэтому Chrome при печати не
  находил цели для ссылок вида `#3-тема--папка-для-вопросов` и записанные
  руками оглавления печатались мёртвым текстом. Слаг совместим с github:
  кириллица выживает, точки, кавычки-ёлочки и длинные тире отбрасываются,
  пробелы становятся дефисами, повторы получают числовой хвост.
  
  Сборка теперь предупреждает о ссылках, для которых цели не нашлось, —
  опечатка в оглавлении перестаёт быть невидимой.

- **feat**(templates): «Сертификация» догнала эталон по полосам разреза, шкальному вопросу и настройкам итогов (2026-08-14) [`fcd13d2f6f7041d732bef60ed05688a3dcf4b0b2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fcd13d2f6f7041d732bef60ed05688a3dcf4b0b2)

- **feat**(prd-50): полосы разреза в карточке темы на итогах и в отчёте (2026-08-14) [`9bcb090bb74156ff13a901ac60e73517f7dccede`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9bcb090bb74156ff13a901ac60e73517f7dccede)
  Настройка показа (tests.breakdown_display_json) доезжает до пакета SCORM:
  test-json.ts печёт её в TEST_DATA.breakdownDisplay только когда автор её
  включил (visibility !== hidden), а viewResults.js подаёт её в оба вызова
  buildResultContext вместе с breakdown-записями темы — свежими на экране
  финиша (calculateResults ещё держит их в памяти) и пустыми на «Моём
  результате» сохранённой попытки (saveAttemptResult их не хранит: бюджет
  suspend_data 64K).
  
  Вёрстка полос добавлена в results.html и report.html (default), стили —
  в theme.css (экран) и report.css (отчёт, живёт отдельно от theme.css:
  страница отчёта не смеет зависеть от сцены). Предпросмотры обоих
  шаблонов пересобраны генератором.

- **feat**(prd-50): переключатель показа подытогов и проводка настройки до экрана (2026-08-14) [`ecad249bb2a551ddd80007dd67cbd5b277ebdffc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ecad249bb2a551ddd80007dd67cbd5b277ebdffc)
  Замыкает разрыв PRD-50 Э1 Task 7: настройка breakdown_display_json была
  заведена в БД, но негде было её задать и она не доезжала до контекста итогов.
  
  \- редактор теста: блок «Подытоги по ключам» в панели «Правила прохождения»
    (селектор видимости hidden/bar/bar_and_value + селектор базы units/points,
    виден только когда видимость не hidden); проведена через модель редактора,
    API DTO и белый список test-settings.ts
  \- server/routes/attempts.ts: resultsMaterialForAttempt заполняет
    MeasuresSource.breakdownDisplayJson из выданной версии теста — раньше поле
    объявлялось и читалось, но никто его не заполнял, поэтому даже включённая
    настройка никогда не доходила до экрана

- **feat**(prd-50): строки разреза в контексте рендера и настройка показа (2026-08-14) [`7e21c2eda3e41b73583631d3141a6bfd213f2fc4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7e21c2eda3e41b73583631d3141a6bfd213f2fc4)

- **feat**(prd-50): tag() и sectionById() читают настоящие данные (2026-08-14) [`aca915c182ad21fee1464d5d2364be0929cb2889`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/aca915c182ad21fee1464d5d2364be0929cb2889)

- **feat**(prd-50): пакет SCORM подаёт ключи разреза и выпекает теги адаптивных разделов (2026-08-13) [`c60b609a6f09ba0858177506469c02fa800146bd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c60b609a6f09ba0858177506469c02fa800146bd)

- **feat**(prd-50): веб-хост подаёт ключи разреза и сохраняет записи с попыткой (2026-08-13) [`73d9472d1bbe2c3dd214ed5bd96814c747db0b1c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/73d9472d1bbe2c3dd214ed5bd96814c747db0b1c)

- **feat**(prd-50): агрегат считает разрезы по разделам и по тесту (2026-08-13) [`e565ef7414484f57035829493f8e95e46d413dfd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e565ef7414484f57035829493f8e95e46d413dfd)

- **feat**(prd-50): движок разрезов результата (2026-08-13) [`d69a23a719633458c77b81e4af59f3e9077d3dcc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d69a23a719633458c77b81e4af59f3e9077d3dcc)

- **feat**(workbook): предпросмотр считает строки всех листов книги (2026-08-11) [`7d3a383c1c3cc576b5d8808e8091981adb5259fd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7d3a383c1c3cc576b5d8808e8091981adb5259fd)

- **feat**(workbook): шаблон книги несёт лист оформления (2026-08-11) [`3b3f54d44cd3b5e4100eebe7dd6bed38569ed7f5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3b3f54d44cd3b5e4100eebe7dd6bed38569ed7f5)

- **feat**(workbook): загрузка оформления и настроек отчёта (2026-08-11) [`8c1688d41ae91e7774319f2a0987813825c3cda2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8c1688d41ae91e7774319f2a0987813825c3cda2)

- **feat**(workbook): выгрузка оформления и настроек отчёта (2026-08-11) [`1e8a08800c182c48141b1e844991b43eb0c4cbcb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1e8a08800c182c48141b1e844991b43eb0c4cbcb)

- **feat**(design): нормализация параметров оформления по манифесту (2026-08-11) [`667126e0e98fb52871f98fb3edd77bee754e2e7b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/667126e0e98fb52871f98fb3edd77bee754e2e7b)
  Значения оформления и полей отчёта приводятся к типам, которые объявляет
  манифест ПРИЁМНИКА: булево, число, select по options, multiselect
  массивом, цвет, медиа-конверт. Раньше этого не делал никто — маршрут
  оформления проверяет только набор ключей, а из ячейки Excel всё приходит
  строкой, и «false» стал бы истиной.
  
  Незаявленный ключ не пишется, а называется: молча потерянная половина
  настроек выглядит как «оформление перенесено». Переопределение по
  палитре принимается только у шаблона с палитрами и только для цвета —
  те же проверки, что у PUT /api/tests/:id/design.
  
  Вид отчёта, которого приёмник не объявляет, — ошибка ветви: продукт
  молча подменяет его видом по умолчанию, и обнаружить подмену можно
  только глазами в готовом PDF. Ветвь без вида законна и вида не получает.

- **feat**(workbook): контракт листа «Оформление» (2026-08-11) [`a7d32def18e5e32431b3ddd91f4cd43336788755`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a7d32def18e5e32431b3ddd91f4cd43336788755)
  Лист несёт шаблон, палитру теста, параметры оформления (плоские и по
  палитрам) и настройки отчёта обеими ветвями режимов. Модуль разбирает
  ячейки и не знает манифеста: типизация и фильтрация ключей — забота
  импорта.
  
  Версии шаблона на лист не попадают: чужая версия подняла бы у автора
  баннер «Шаблон обновлён», одна кнопка в котором выбрасывает параметры,
  незнакомые новому манифесту.
  
  Общие для листов помощники (byLabelOrValue, encodeFieldValue,
  decodeFieldValue, MODE_LABELS) переехали в workbook-settings, откуда их
  теперь берут и «Страницы».

- **feat**(workbook): шаблон книги несёт лист адаптивных уровней (2026-08-11) [`2335111bea315b829e21f7916ee6f04d9355c158`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2335111bea315b829e21f7916ee6f04d9355c158)

- **feat**(workbook): выгрузка и загрузка адаптивных уровней (2026-08-11) [`0b30d66005fb616a4e966adb7a81c8577e7a1827`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0b30d66005fb616a4e966adb7a81c8577e7a1827)

- **feat**(workbook): материалы адаптивных уровней и обратная связь темы (2026-08-11) [`bd1204870066104bda0077c5692063080a15be0e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bd1204870066104bda0077c5692063080a15be0e)
  «Рекомендации» получают третьего владельца — адаптивный уровень: значение
  «Уровень» в колонке «Кому» и новая колонка «Номер уровня». Материал уровня
  это его links[], где есть только заголовок и ссылка, поэтому типы
  «Материал» и «Мероприятие» уровню недоступны: такая строка отвергается с
  объяснением, а не пропускается молча — иначе перенос выглядел бы удавшимся.
  
  Строка уровня подчинена листу «Адаптивные уровни» так же, как обычная
  строка подчинена «Обратной связи»: набор известных адресов передаётся
  третьим аргументом parseFeedbackSheets, а книга без листа уровней сообщает
  о таких строках как о сиротах. Контракт Э2 не сломан — аргумент
  необязательный, старый вызов импорта читается как «уровней в книге нет».
  
  На листе «Обратная связь» владелец «Уровень» отвергается и говорит, где
  живёт текст уровня: у этого листа есть колонка «Формат», а формат уровня
  модели хранить негде.
  
  «Структура» получает колонку «Обратная связь при непройденном уровне»
  (adaptive_topic_settings.failure_feedback). Место выбрано по модели:
  значение одно на тему, а «Структура» — ровно одна строка на тему. Пустая
  ячейка — «оставить как есть».
  
  Справка шаблона и выпадающий список «Кому» на «Рекомендациях» подтянуты
  следом: гард полноты справки требует строки на каждую колонку, а список
  без «Уровня» запрещал бы ввод значения, которое лист теперь принимает.

- **feat**(workbook): контракт листа «Адаптивные уровни» (2026-08-11) [`0bf352d4ebe4c96e2edf6114721268cdb9c55619`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0bf352d4ebe4c96e2edf6114721268cdb9c55619)
  Лист переносит уровни адаптивной темы целиком: название, окно сложности,
  число вопросов, порог с типом и текст обратной связи. Уровень адресуется
  парой «Раздел» + «Номер», потому что id уровня заводится заново на каждом
  стенде, а другого естественного ключа у него нет.
  
  «Номер» в книге считается с 1, а level_index в модели — с 0. Пересчёт
  живёт в одном модуле: разбор вычитает единицу, выгрузка прибавляет. Обе
  стороны закреплены тестом по отдельности — круг такую ошибку не ловит,
  две противоположные погрешности гасят друг друга.
  
  Колонки «Формат» на листе нет: adaptive_levels.feedback — простой текст,
  формат уровня некуда сохранить. Словарь «Тип порога» взят из
  workbook-settings, третьей копии не заводится.

- **feat**(workbook): шаблон книги несёт листы страниц (2026-08-11) [`8084a2d3b187171530bfd3b4fabbf64e84e3bf01`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8084a2d3b187171530bfd3b4fabbf64e84e3bf01)

- **feat**(workbook): загрузка страниц теста и их полей (2026-08-11) [`8e61651c02b74e28adac76232c4c1de5b91fef65`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8e61651c02b74e28adac76232c4c1de5b91fef65)

- **feat**(workbook): выгрузка страниц теста и их полей (2026-08-11) [`6a07048ee70ebd076154563927c4d3e3f67e9b78`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6a07048ee70ebd076154563927c4d3e3f67e9b78)

- **feat**(workbook): контракт листов «Страницы» и «Поля страниц» (2026-08-11) [`b348a1723f80a825424b56c70e61af210b5c6d88`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b348a1723f80a825424b56c70e61af210b5c6d88)

- **feat**(workbook): шаблон книги несёт листы обратной связи и рекомендаций (2026-08-10) [`c70430f6eadf5bb1dc37911564431accee5c0f74`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c70430f6eadf5bb1dc37911564431accee5c0f74)

- **feat**(workbook): загрузка обратной связи и рекомендаций (2026-08-10) [`cca7aaf26552776b082df5fd220319a6beab596c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cca7aaf26552776b082df5fd220319a6beab596c)

- **feat**(workbook): выгрузка обратной связи и рекомендаций (2026-08-10) [`e2621f17d9b73dd99ee0541bb5bacf2eb30e7fd5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e2621f17d9b73dd99ee0541bb5bacf2eb30e7fd5)

- **feat**(workbook): контракт листов «Обратная связь» и «Рекомендации» (2026-08-10) [`929a424d07cd215c8c31d5402692c43f131923a8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/929a424d07cd215c8c31d5402692c43f131923a8)
  Лист «Рекомендации» подчинён «Обратной связи»: курсы, материалы и
  мероприятия лежат внутри feedback_json владельца, поэтому владелец,
  не названный на первом листе, делает строку второго сиротой.
  
  Владелец адресуется двумя колонками — «Уровень» и «Раздел»: тема с
  именем «Тест» в банке существует, и общая колонка молча увела бы её
  обратную связь на уровень теста.
  
  Словарь форматов и нормализация ячейки экспортированы из
  workbook-settings: метки формата в книге одни на все листы.

- **feat**(workbook): шаблон книги перечисляет все параметры листа «Настройки» (2026-08-10) [`059aa5dc44bf69c2b4700f50baa4924b1f02fb39`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/059aa5dc44bf69c2b4700f50baa4924b1f02fb39)

- **feat**(workbook): правила разблокировки разделов переносятся именами тем (2026-08-10) [`ca8e4db07f93c7b444ec41be0704f25c1c50a406`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ca8e4db07f93c7b444ec41be0704f25c1c50a406)

- **feat**(workbook): код темы едет книгой, чтобы формулы показателей не теряли адресата (2026-08-10) [`6d2481a288fb541e9caee7738812be9ec1ff3273`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6d2481a288fb541e9caee7738812be9ec1ff3273)

- **feat**(workbook): «Структура» переносит выдачу всей темы, лимит времени и балл по умолчанию (2026-08-10) [`558774c7d30a3fb704a8156fc7f6363b54c9cda5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/558774c7d30a3fb704a8156fc7f6363b54c9cda5)

- **feat**(workbook): папка теста переносится путём, недостающие создаются (2026-08-10) [`12b179b0d90977e450bd66d9fcfafbd07cac6def`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/12b179b0d90977e450bd66d9fcfafbd07cac6def)

- **feat**(workbook): лист «Настройки» переносит все параметры теста (2026-08-10) [`8c0cae1c3b03c65489f17907d27e4340f313fb09`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8c0cae1c3b03c65489f17907d27e4340f313fb09)

- **feat**(workbook): реестр параметров листа «Настройки» (2026-08-10) [`d7834e4ad9b6c35344d28eaf943e9e85d512addf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d7834e4ad9b6c35344d28eaf943e9e85d512addf)

### Fixes

- **fix**(scorm): отпечаток пакета учитывает состав банка вопросов (2026-08-15) [`9164a760b0103dcfec9d6c1ef8fcf620f8724311`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9164a760b0103dcfec9d6c1ef8fcf620f8724311)
  Отпечаток сверял число разделов, вопросов и ключей — то есть КОЛИЧЕСТВА. Автор,
  заменивший вопрос в банке или переставивший их, количеств не меняет: отпечаток
  совпадал, состояние признавалось своим, а сохранённые позиции указывали уже на
  другие вопросы. В отчёте LMS по лучшей попытке оказались бы вопросы, которых
  учащийся не видел, а восстановленный прогон показал бы ему не то, что он отвечал.
  
  Теперь в отпечаток входит слепок идентификаторов вопросов в запечённом порядке
  (djb2, тот же приём, что у подписи якоря таймера). Пересборка пакета из того же
  теста отпечаток не меняет — иначе каждый ре-экспорт терял бы прогон ученика.
  
  Заодно тестами закрыты все пять механизмов, которые делят состояние: учёт попыток,
  якорь PRD-20, интервал PRD-31, кулдаун PRD-6 и остаток времени по разделам PRD-4 —
  каждый исполняется на исходниках поверх настоящей строки состояния, в новом формате
  и в состоянии пакета, собранного до правки. Добавлен страж: если рантайм начнёт
  писать в состояние новое поле, а в перечень его не внесут, тест упадёт — именно так
  и потерялись секционные бюджеты.

- **fix**(scorm): приведение формата сохраняет остаток времени по разделам (2026-08-15) [`ef5672e38c13210cc9acc1ad33a751fe72097672`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ef5672e38c13210cc9acc1ad33a751fe72097672)
  Секционные бюджеты PRD-4 живут в том же состоянии, но пишет их таймер, а не учёт
  попыток, — и приведение формата 1 к формату 2 собирало новый объект без них.
  Учащийся, у которого шёл секционный тест с ограничением времени, после переиздания
  пакета получил бы лимит раздела заново: молча и ровно там, где считается время.
  
  Найдено разбором рисков выката на WebTutor, а не тестом: поле не упоминается ни
  в одном требовании PRD-36, потому что принадлежит другому механизму.

- **fix**(scorm): бюджет состояния берётся по профилю выгрузки (2026-08-15) [`3e11ca517e479ef0e8b8f6d1a7b9b52348332c32`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3e11ca517e479ef0e8b8f6d1a7b9b52348332c32)
  4096 — предел SCORM 1.2 и ПРОЕКТНАЯ цель: под неё подогнана форма состояния и по
  ней принимается §11.3. Резать по ней в профиле 2004, где LMS принимает 64000, —
  значит выбрасывать данные, которые LMS готова хранить: тест на двадцать тем терял
  всю сводку (итоги по темам, шкалы, показатели, разрезы), оставляя ученику голый
  процент с вердиктом, хотя состояние весит 5484 символа при пределе в 64000.
  
  При записи теперь сверяется эксплуатационный бюджет: `TEST_DATA.stateBudget`, если
  пакет собран профилем со своим пределом, иначе 64000. Порядок жертв §6.2 не менялся
  — он остаётся страховкой в 2004 и станет рабочим механизмом в профиле 1.2 (PRD-37).
  
  Спека поднята до 1.2: §2.4 различает два бюджета, §6 и FR-14/FR-15 переписаны.

- **fix**(scorm): перемешивание сопоставления переживает перерыв (2026-08-15) [`f7b684fc9651e8bbb44d4d4e8bbe13a51fad53af`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f7b684fc9651e8bbb44d4d4e8bbe13a51fad53af)
  Найдено браузерной приёмкой: у вопроса на сопоставление перемешивание — не
  перестановка, а ПАРА перестановок ({ left, right }: две колонки тасуются
  независимо), и кодек, знавший только массивы, писал вместо неё пустую ячейку.
  Ученик после продолжения видел карточки в авторском порядке — ровно тот дефект
  §8, который эта работа закрывает, только для одного типа вопроса.
  
  Колонки разделяются `~`; ячейка без разделителя читается как обычная
  перестановка, поэтому пакеты тестов без сопоставления не меняются ни на символ.

- **fix**(scorm): продолжение прогона сохраняет порядок вариантов и выданный вариант (2026-08-15) [`1b28b82d714481ab70af164256865d167f143143`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1b28b82d714481ab70af164256865d167f143143)
  Снимок прерванного прогона хранит ряды вместо копий выданных вопросов: выдачу
  позициями, ответы, статусы, карту перемешивания и карту выданных вариантов PRD-17.
  Восстановление разворачивает вопросы из TEST_DATA по адресам.
  
  Этим закрыты два дефекта §8 спеки. Перемешивание вариантов ответа не переживало
  перерыва: экран после продолжения показывал варианты в авторском порядке вместо
  того, в котором ученик их видел. Выданный вариант не сохранялся вовсе, поэтому
  deliveredFormId после восстановления возвращал null и правило by_variant PRD-24
  молча съезжало на общий порог темы — вердикт после перерыва мог отличаться от
  вердикта без перерыва.
  
  Восстановление внутри незавершённой темы маршрутизатора переведено на ту же
  гидратацию, что и линейное: второй разбор снимка рядом с первым разошёлся бы.
  Собранный generateVariant вариант при этом дополняется, а не затирается — в нём
  лежат выданные questionIds, восстановить которые больше неоткуда.

- **fix**(server): запрос без тела получает 400, а не 500 (2026-08-15) [`b4b6f0e40f4d73ed79e1d25771f5bc43fac917b2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b4b6f0e40f4d73ed79e1d25771f5bc43fac917b2)
  Express 5 оставляет `req.body` неопределённым, если ни один парсер не сработал —
  например, когда клиент не прислал `Content-Type: application/json`. Обработчики
  же почти везде начинаются с деструктуризации: в маршрутах 39 таких мест против
  13 защищённых. Она падает раньше любой собственной проверки, поэтому запрос без
  заголовка получал 500, а в журнал уходило «Cannot destructure property ... of
  req.body as it is undefined» — запись, выглядящая как поломка сервера, хотя
  виноват вызывающий.
  
  Чинится причина, а не 39 следствий: `middleware/ensure-body` подставляет пустой
  объект сразу за парсерами. Пустой, а не заглушку с полями, — дальше маршрут сам
  решит, чего в теле не хватает, и ответит своим кодом. Ложные, но ОПРЕДЕЛЁННЫЕ
  значения (`null`, пустая строка) не трогаются: это разобранное тело, а не его
  отсутствие, и подменять их значило бы врать обработчику о том, что прислал
  клиент.
  
  Найдено на `DELETE /api/tests/:id` при уборке стенда. Проверено там же на живом
  сервере: тот же запрос теперь отвечает `400 title_mismatch`.

- **fix**(tests-editor): явное «не проверять» у ключа переживает сохранение (2026-08-15) [`d563ac15dc5a4935b529293c1ec78a96aa12e021`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d563ac15dc5a4935b529293c1ec78a96aa12e021)
  Долг, зафиксированный приёмкой Э2 PRD-50. `normalizeBreakdownRules` выбрасывал
  всю структуру порогов, если в ней не было ни одного `percent` и не было
  умолчания, — то есть решение автора «этот ключ не гейтит» не доезжало до базы.
  
  Сегодня это незаметно, и потому долг прожил так долго: строка ключа без правила
  рисуется как «Не проверять», а движок считает отсутствие структуры
  информационным для всех ключей. Автор видит после перезагрузки ровно то, что
  выбрал.
  
  Ловушка отложенная. Умолчание (`default`) движок уже понимает, но редактора у
  него пока нет; в тот день, когда появится, «отсутствует» станет значить
  «наследует умолчание», и потерянный `none` молча поменяет смысл гейта — без
  единого признака в интерфейсе.
  
  Теперь структура сохраняется, если у ключа есть ЛЮБОЕ решение. Пустая
  по-прежнему схлопывается в `null`: это не выбор автора, а мусор.

- **fix**(learner): адаптивный экран вопроса показывает обратный отсчёт (2026-08-15) [`e00b428184ef963cb0bf57b7b777b6cf8f410e49`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e00b428184ef963cb0bf57b7b777b6cf8f410e49)
  Экран вопроса адаптивного теста не передавал проп `timers` вовсе, тогда как
  стандартный передаёт его тем же компонентом. Учащийся на адаптивном тесте с
  ограничением по времени не видел ни одного отсчёта: ни в шапке, ни где-либо
  ещё, — и узнавал о лимите только когда прогон заканчивался сам.
  
  Передаются оба значения, как на стандартном экране и в пакете: общий лимит
  теста и бюджет темы (секционные часы адаптивного режима, PRD-4 v1.1 §3.2).
  Рисует их общий художник `paintSceneTimers` в штатные DS-чипы шапки.
  
  Проверено в браузере на dev: адаптивный тест с лимитом 3 минуты печатает
  чип «ТЕСТ 2:55» и отсчитывает (2:28, 2:26); чип раздела остаётся скрытым —
  у этого теста лимита темы нет, что и есть верное поведение.

- **fix**(attempts): истечение времени завершает адаптивную попытку на сервере (2026-08-15) [`589d1643f4b1ab4d38894c1c8cec7f5d8b3e7385`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/589d1643f4b1ab4d38894c1c8cec7f5d8b3e7385)
  Обработчик общего таймера был разведён по режимам несимметрично. В standard
  он слал `POST /api/attempts/:id/finish`, в adaptive не обращался к серверу
  вовсе: правил локальное состояние и уводил на страницу результата. Попытка
  оставалась открытой — `finished_at` и `result_json` пустые, — и учащийся
  получал «Результаты не найдены. Результаты этой попытки ещё не сформированы
  или были потеряны». Прогон терялся целиком вместе с уже данными ответами.
  
  Заведён `POST /api/attempts/:attemptId/finish-adaptive`: считает итог тем же
  `buildAdaptiveResult` из уже сохранённых ответов (после сигнала новым взяться
  неоткуда), проставляет `finished_at` и `result_json`. В отличие от
  `expire-topic-adaptive` ничего не двигает — прогон окончен, недостигнутые темы
  и уровни так и остаются несыгранными. Идемпотентен: повтор на завершённой
  попытке возвращает сохранённый итог. Маршрут добавлен в правила magic-link,
  иначе внешний участник (PRD-28) не смог бы завершить по времени.
  
  При сбое запроса попытка НЕ помечается завершённой: она остаётся
  возобновляемой, что лучше страницы результата, которой нечего показать.
  
  Воспроизведено и проверено в браузере на dev: адаптивный тест с лимитом
  3 минуты, два ответа, ожидание нуля. До правки — «Результаты не найдены» и
  NULL в обеих колонках; после — экран итогов с разрезом по темам и
  `finished_at` 10:00:25, `result_json` с одной темой.

- **fix**(tests-api): снятие настройки через API больше не теряется (2026-08-15) [`7260cb2fb566537b7d09ac631799c49980bafbe2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7260cb2fb566537b7d09ac631799c49980bafbe2)
  PUT /api/tests/:id схлопывал явный null в undefined (`?? undefined`), а
  undefined Drizzle выбрасывает из UPDATE. Снять настройку через API было нельзя
  вовсе: сервер отвечал 200, колонка держала прежнее значение.
  
  Редактор шлёт null именно как «снято»: так автор выключает интервал между
  попытками, удаляет последний блок разделов, стирает вводные тексты и настройки
  отчёта, очищает адрес вебхука. Автор снимал галку, видел «Сохранено» и получал
  её обратно после перезагрузки — кулдаун при этом продолжал действовать.
  
  Схема различие уже хранила, теряла его только эта строка. Проверено кругом
  «включил -&gt; снял -&gt; перечитал» на живом стенде до и после правки.

- **fix**(prd-50): плашка вердикта темы в отчёте берёт авторское слово из словаря (2026-08-14) [`3a556ae3f7dc8c7053b1602061541d3ff01ed692`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3a556ae3f7dc8c7053b1602061541d3ff01ed692)
  Строка разреза внутри карточки отчёта печатала авторское «Не зачтено», а
  плашка над ней — жёсткое «Не пройден»: одна карточка говорила двумя
  словарями. Различие формулировок документа и экрана задумывалось для двух
  зашитых строк и на авторские не распространяется.

- **fix**(deploy): выкат сам чинит реестр миграций вместо ручного SELECT на стенде (2026-08-14) [`b436e9cda958b30be0a63cd5a4a48a7c1e84f51f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b436e9cda958b30be0a63cd5a4a48a7c1e84f51f)
  `drizzle-kit migrate` выбирает, что применять, по ВРЕМЕНИ: берёт MAX(created_at)
  в реестре и накатывает всё, у чего `when` в журнале больше. Хеши он не смотрит.
  Поэтому миграция, пересобранная УЖЕ после применения, получает новое `when`,
  выглядит непринятой, накатывается второй раз и умирает на «already exists» —
  обрывая релиз до тех миграций, ради которых он затевался. Ровно так вело себя
  `0018_prd28_external_participant` после пересборки в PRD-28.
  
  Расхождение живёт в БАЗЕ каждого инстанса, значит в репозитории его не исправить,
  а просить человека сходить с запросом на прод — не выкат. Теперь перед migrate
  деплой запускает `dist/reconcile-migration-ledger.cjs`: он равняет `created_at`
  строк, чьи хеши уже в реестре, по журналу — и НИКОГДА не вставляет строк, потому
  что пометить непринятую миграцию принятой значит молча пропустить её DDL.
  
  Шаг идемпотентен (повторный прогон правит ноль строк) и нефатален: если он не
  отработает, migrate следом падает со своей внятной ошибкой.
  
  Проверено на dev: расхождение возвращено вручную, скрипт назвал миграцию и
  выправил её, migrate после этого прошёл с кодом 0.

- **fix**(prd-50): сбой подсчёта предупреждений не превращает успешную публикацию в 500 (2026-08-14) [`adb59bf11258b1d896527bdb621d22ef4648dba2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/adb59bf11258b1d896527bdb621d22ef4648dba2)

- **fix**(prd-50): вердикт ключа объявлен в схеме, иначе zod срезал бы его молча (2026-08-14) [`adf425bb816cfaca16c85b584a0ab354542b0e24`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/adf425bb816cfaca16c85b584a0ab354542b0e24)

- **fix**(prd-50): аналитика SCORM пересчитывает показатели с разрезами (2026-08-14) [`00befd80049189bdea350263d588a9a4f74d5769`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/00befd80049189bdea350263d588a9a4f74d5769)

- **fix**(tests): фикстура pglite догнала схему, четыре теста ствола приведены к факту (2026-08-14) [`d5812fdd83ac5f91360bc1ccdf40531541e57e42`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d5812fdd83ac5f91360bc1ccdf40531541e57e42)
  Фикстура `tests/it/schema.sql` регенерирована штатной командой из шапки
  харнесса (`drizzle-kit export --sql`). Разница ровно в двух колонках:
  `pass_decision_policy`, отставшая со ствола, и `breakdown_display_json`
  из PRD-50. Отсюда 85 падений в наборе DAL — INSERT в `tests` не мог
  собраться вовсе; теперь 293 из 293 зелёные.
  
  Заодно приведены к факту четыре теста, отставшие от сознательных правок кода:
  
  \- нечитаемый файл на импорте вопросов и на предпросмотре пользователей
    больше не роняет обработчик в 500, а честно отвечает 400 «Failed to read
    file» — это ошибка клиента, и тесты ждали старую ветку падения;
  \- `passDecisionPolicy` теперь удерживается вместе с прочими колонками
    «Оценивания», когда автор отказался от этой части переноса;
  \- мок `middleware/upload` не отдавал `workbookUploadSingle`, из-за чего
    файл целиком не собирался — мок обязан нести все экспорты, которые
    импортирует роутер.

- **fix**(tests): селектор блока рекомендаций отвязан от тега (2026-08-14) [`47d9c04e309e2cb1260a4efdb19cc602aa9cb6fa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/47d9c04e309e2cb1260a4efdb19cc602aa9cb6fa)
  `f3833fcf` заменил `&lt;p class="tb-recs-group__text"&gt;` на `&lt;div&gt;`: авторский
  абзац внутри `&lt;p&gt;` браузер закрывал досрочно, и разметка автора уезжала
  из-под правил блока. Тесты продолжали искать `p.tb-recs-group__text` и с
  того дня были красными на стволе — 11 падений, которые к своей задаче не
  относился никто.
  
  Селектор теперь по классу, без тега: обёртку выбирает раскладка, а тест
  проверяет, что тексты доехали, а не каким элементом они обёрнуты.

- **fix**(template): тумблеры слотов карточки измерения на адаптивных итогах эталона (2026-08-14) [`f851ceb4e766e9e97b0d81ae92677ab982b8096a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f851ceb4e766e9e97b0d81ae92677ab982b8096a)

- **fix**(prd-50): строгая проверка только для составного ключа, не для sectionById (2026-08-14) [`eb5ab86111ec57f2aec44abb306ab482e3ae779f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eb5ab86111ec57f2aec44abb306ab482e3ae779f)
  ValidationRefs получил отдельное поле scopeKeys для левой части составного
  tag("&lt;раздел&gt;::&lt;ключ&gt;"); проверка обычного sectionById(...) осталась завязана
  только на sectionKeys, как и до PRD-50. server/storage/scales-variables-repository.ts
  больше не передаёт sectionKeys (это включало строгую проверку sectionById задним
  числом и ломало сохранение старых формул), вместо этого передаёт scopeKeys.

- **fix**(prd-50): полосы разреза доезжают до скачиваемого PDF-отчёта (2026-08-14) [`84d05d1b4cec3de186d2b5bd412a4bc549d1f7f3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/84d05d1b4cec3de186d2b5bd412a4bc549d1f7f3)
  buildReportContext строил вход экрана без breakdownDisplay, поэтому скачанный
  с сайта PDF никогда не печатал полосы разреза по темам, хотя разметка в
  report.html их уже ждала. Поле заведено во ВХОД (ReportInput.breakdownDisplay)
  тем же приёмом, что intro/feedback/hasPassThreshold, и передаётся дальше в
  buildResultContext.
  
  Проведено до обоих настоящих строителей отчёта:
  \- веб-маршрут (server/services/result-context.ts: reportFeedbackMeta берёт
    настройку из того же MeasuresSource, что и построитель экрана);
  \- пакет SCORM (pdfExport.js: pdfReportMeta берёт TEST_DATA.breakdownDisplay,
    как это уже делает экран итогов в viewResults.js; заодно у pdfStandardInput
    не было и самих строк разреза по теме — добавлены тем же vrTopicBreakdown,
    которым их читает экран, иначе одного переключателя было бы недостаточно).
  
  Предпросмотр отчёта в редакторе и smoke-раннер шаблонов используют демо-вход
  (buildReportPreviewInput) без реального теста позади — у них нет доступа к
  настройке точно так же, как нет доступа к intro/hasPassThreshold; трогать не
  стал, чтобы не выдумывать источник.

- **fix**(prd-50): живой гейт секции в валидаторе и тест паритета рантайма (2026-08-14) [`c3988ab16f20467c427317b15481f6bec84b171c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c3988ab16f20467c427317b15481f6bec84b171c)

- **fix**(tests): сторож полей отчёта не требует удалённую галочку радара (2026-08-11) [`770a49da03d2b40450a6e76872eccf6561f8cb4d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/770a49da03d2b40450a6e76872eccf6561f8cb4d)

- **fix**(tests): выгрузка схемы для pglite догнала shared/schema.ts (2026-08-11) [`797fcbb44114ec26041875edad40120c387584a9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/797fcbb44114ec26041875edad40120c387584a9)

- **fix**(workbook): предпросмотр считает то же, что сделает импорт (2026-08-11) [`aba2ad3f52da25be5e0443b7046bf656decc1723`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/aba2ad3f52da25be5e0443b7046bf656decc1723)
  Приёмка PRD-48 нашла: у одной и той же книги предпросмотр показывал
  «pages: 0», а боевой импорт обновлял три страницы. Счётчик был ни при
  чём — системные страницы материализует СОХРАНЕНИЕ того же прогона, а под
  dryRun его нет, и проход страниц заставал приёмник в состоянии, где книге
  не к чему приложиться: каждая системная строка получала ошибку «такой
  страницы в тесте нет».
  
  Предпросмотр теперь считает по состоянию, которое приёмник ПОЛУЧИТ.
  Найдено и закрыто три расхождения:
  
  \- системные страницы под dryRun проецируются тем же чистым планировщиком
    planSystemPages, который исполняет служба настроек, по той же тройке
    (сценарий + темы + шаблон); патч теста строится на обоих путях, не
    пишется только он сам;
  \- поля страниц типизируются манифестом шаблона, который назвало
    «Оформление» ЭТОЙ книги: на пути записи оформление применено до прохода
    страниц, под dryRun строка теста ещё старая;
  \- отказ службы настроек (flowPolicy) назывался только на пути записи —
    предпросмотр молчал о книге, которую импорт отвергнет целиком.
  
  extractFlowMode/extractTemplateId/DEFAULT_TEMPLATE_ID переехали в чистый
  content-pages-lifecycle: у предсказания и у исполнения должен быть один
  источник умолчаний.
  
  Тест — одна книга со всеми заполненными листами, два прогона, отчёты
  сравниваются целиком: проверка «счётчик ненулевой» этот дефект пропустила.

- **fix**(design): пустое текстовое значение параметра переживает круг (2026-08-11) [`4a2c54529add30ab93f20470a5089b2122b4a1b9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4a2c54529add30ab93f20470a5089b2122b4a1b9)

- **fix**(workbook): отвергнутая строка оформления не стирает параметры приёмника (2026-08-11) [`b485a9b5b1d40c09f051a28f19de296699bf1058`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b485a9b5b1d40c09f051a28f19de296699bf1058)

- **fix**(workbook): настройки отчёта переносятся и у теста без сохранённого оформления (2026-08-11) [`f4d09013a6dcd5e1113181591aead977ba997c8b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f4d09013a6dcd5e1113181591aead977ba997c8b)

- **fix**(workbook): книга не заводит значение-стиратель, как и решено в спеке (2026-08-11) [`d9d5ebc239a74c208e61690a3a8fc00be2e60df7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d9d5ebc239a74c208e61690a3a8fc00be2e60df7)
  Прочерк как способ очистить «Обратную связь при непройденном уровне» отменён:
  PRD-48 §4.4 решает обратное — очистить текстовый параметр книгой нельзя, и
  отдельного значения-«стирателя» не заводим, чтобы не отличать «пусто» от
  «пусто нарочно» в каждой ячейке каждого листа.
  
  Довод «колонка умеет только добавлять» верен и для «Описания», и для вводных
  блоков, и для обратной связи теста — там он сознательно принят. Книга, где
  одна ячейка из сотни понимает волшебное значение, хуже книги, которая
  последовательно не умеет стирать.
  
  Разбор колонки знает два состояния: пусто — оставить как есть, текст —
  заменить. Прочерк едет текстом.

- **fix**(workbook): пустое имя уровня не отвергается, а получает умолчание (2026-08-11) [`80356d2f8df318f257b4594d7f7082ade421f516`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/80356d2f8df318f257b4594d7f7082ade421f516)
  Выгрузка писала пустое «Название», а разбор его отвергал: редактор имя
  уровня не проверяет, поэтому уровень без имени сохраняется штатно и приезжает
  в книгу пустой ячейкой. Книга оказывалась строже редактора (PRD-48 §9), а
  цена высокая — если это единственный уровень темы, срабатывает гейт
  «адаптивный раздел без уровней» и отменяется ВЕСЬ save: ни настроек, ни
  структуры, ни обратной связи, ни страниц.
  
  Теперь пустая ячейка берёт то же умолчание, что и кнопка редактора, —
  «Уровень N» по номеру строки.

- **fix**(workbook): книга не удаляет уровни темы, которых не назвала (2026-08-11) [`52f82f2d4a9950b2ef0bce7465754083c17e20c2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/52f82f2d4a9950b2ef0bce7465754083c17e20c2)
  Запись темы создавалась и с пустым списком уровней — ради одной лишь
  «Обратной связи при непройденном уровне». Для службы это не «у темы только
  текст», а «уровней нет»: adaptiveSettings переписывает настройки целиком,
  и лестница темы удалялась.
  
  Спасал только гейт «адаптивный раздел без уровней», но validateFlowPolicy
  читает режим из ПРИСЛАННОГО payload: книга без листа «Настройки» гейт не
  включает, а книга, у которой все строки уровней темы отвалились ошибкой,
  проходит его тем же путём. Теперь запись делается, только если книга назвала
  уровни, а текст без уровней даёт предупреждение — как и без листа вовсе.

- **fix**(workbook): пустая ячейка обратной связи темы не стирает текст (2026-08-11) [`78cf06a8848d49df4e469f4065c51549e44e5914`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/78cf06a8848d49df4e469f4065c51549e44e5914)
  Колонка «Обратная связь при непройденном уровне» листа «Структура» обещала
  «пусто — текст остаётся прежним» в трёх местах (подсказки книги, format.md,
  JSDoc разбора), а на деле уезжала в payload как null. adaptiveSettings
  переписывает adaptive_topic_settings целиком, поэтому текст темы исчезал —
  в том числе от книги прежнего формата, у которой такой колонки нет вовсе.
  
  Обещание выполнено кодом, симметрично keepUnnamedSectionFeedback: перед
  сохранением тема с неназванным текстом получает свой текущий. Стереть текст
  книгой теперь можно прочерком в ячейке — иначе колонка умела бы только
  добавлять, и перенос теста тащил бы за собой чужую формулировку.

- **fix**(workbook): отвергнутая строка страницы больше не называет зону (2026-08-11) [`b70c857f625754281268c398220e6bbece69ec26`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b70c857f625754281268c398220e6bbece69ec26)

- **fix**(workbook): страница не привязывается к теме, которой нет в тесте (2026-08-11) [`d7398c88760d363033aa6e877ff609140944b4b9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d7398c88760d363033aa6e877ff609140944b4b9)

- **fix**(workbook): импорт предупреждает о пустом содержимом страниц без листа полей (2026-08-11) [`2fe83f3e184585bf6b3050e3e81fc6dc3fe0ca66`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2fe83f3e184585bf6b3050e3e81fc6dc3fe0ca66)

- **fix**(workbook): импорт предупреждает об удалении авторских страниц зоны (2026-08-11) [`d63e99da57f942f8c5e75f52247539f91a336005`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d63e99da57f942f8c5e75f52247539f91a336005)

- **fix**(workbook): импорт предупреждает об очистке рекомендаций и не обещает лишнего (2026-08-11) [`fddd8bedc03b02e041971cac10756cf70344a1f2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fddd8bedc03b02e041971cac10756cf70344a1f2)

- **fix**(workbook): книга без листа «Обратная связь» не стирает обратную связь разделов (2026-08-10) [`4e9e818acb3f769f32125eb371078baed2366c52`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e9e818acb3f769f32125eb371078baed2366c52)

- **fix**(workbook): при создании теста импортом название берётся из формы, а не из книги (2026-08-10) [`71029be023555ccb691b47bf96408909752957a6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/71029be023555ccb691b47bf96408909752957a6)

- **fix**(workbook): пустой лист «Структура» больше не глотает настройки теста (2026-08-10) [`a44c6e4a31f299d2b704abb3f70b5b93da231be1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a44c6e4a31f299d2b704abb3f70b5b93da231be1)

- **fix**(workbook): отказ применения настроек виден построчно, а не ответом 500 (2026-08-10) [`47adfd09045c9dd1e9a085291887e762975e5edf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/47adfd09045c9dd1e9a085291887e762975e5edf)

- **fix**(workbook): импорт больше не навязывает страницу-маршрутизатор (2026-08-10) [`d84d6e0c011b212f1a43c59f4b0e490abbb880c7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d84d6e0c011b212f1a43c59f4b0e490abbb880c7)

- **fix**(workbook): реестр настроек не строже редактора и не теряет незнакомые значения (2026-08-10) [`033b719114b94025ef4c1d77b3b6ec14dfbbd5c0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/033b719114b94025ef4c1d77b3b6ec14dfbbd5c0)

### Documentation

- **docs**: статус, контракт шаблонов и руководства сверены с кодом (2026-08-15) [`5f32a5163f81e6780194194ad888c8ead6228f8d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5f32a5163f81e6780194194ad888c8ead6228f8d)
  Проход актуализации по коду на HEAD. Четыре расхождения статуса, все по PRD-36:
  ветка влита в main (merge 78ba8d77), а документ и шапка спеки говорили «в ветке»;
  версия спеки 1.3, а не 1.1; трек стоял в «не начатых вовсе» при влитом коде;
  в сводке трекера числились открытыми #36 и #37, закрытые в тот же день.
  
  Отдельный долг, которого реестр не видел: контракт формата шаблонов отстал от
  кода на этапы Э2-Э5 PRD-50. Поставляемые шаблоны печатали блоки разделов и
  сводный разрез, а спека знала только подытоги в карточке темы — сторонний шаблон
  из реестра PRD-3 узнать о новых полях не мог ниоткуда. Спека и руководство
  разработчика подняты до 1.7.0: вердикт ключа в строке разреза (§10.6),
  topicGroups[] / ungroupedTopics[] (§10.7), result.breakdown[] пятым подблоком
  итогов (§8.3.3, §10.5, §10.8).
  
  Руководство автора получило разделы о блоках итогов (7.1a), порогах по подтемам
  (7.3a), трёх полях показа разреза, настройке «Результат для LMS при нескольких
  попытках» и таблицу предупреждений публикации.
  
  Справочники приведены к схеме: 31 таблица вместо 29, шестой тип вопроса
  allocation, колонки миграций 0014-0023, движок shared/breakdown/, сервис
  breakdown-warnings.ts, состояние прогона в cmi.suspend_data. В README и CLAUDE.md
  было написано прямо обратное про dev-БД: «npm run dev миграции НЕ применяет», —
  хотя predev применяет аддитивные сам.
  
  Проверки: npm run lint:md — 167 файлов, 0 замечаний; npm run docs:pdf пересобрал
  все четыре PDF.

- **docs**(roadmap): границы состояния прогона и поправка к обоснованию умолчания (2026-08-15) [`77bce66547d6b9410877e5ef8818a33b7b9848be`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/77bce66547d6b9410877e5ef8818a33b7b9848be)
  В коммите 828a6040 записано, будто на WebTutor «лучшей попытки» не существует,
  потому что состояние не переживает границу попытки. Это неверно: граница обнуления
  проходит по НАЗНАЧЕНИЮ. Внутри назначения suspend_data живёт — на нём стоит барьер B
  PRD-31 (интервал в часах), приёмка которого на живом WebTutor пройдена 2026-08-06.
  Решение об умолчании «последняя» принято владельцем продукта и не пересматривается;
  исправлено только объяснение.
  
  Заодно заведена справочная строка о границах: какая механика где действует и почему
  их две — интервал между попытками по состоянию (часы) и кулдаун между назначениями
  по записям обучения портала (сутки, матч по имени курса). Повод: замер 2026-06-04
  читается шире, чем следует, и уже привёл к выводу, будто барьер B и лимит попыток
  на WebTutor не работают.

- **docs**(prd-36): приёмка в браузере пройдена, дефект сопоставления отмечен в §8 (2026-08-15) [`62219996bfc4707a07de019f2f2b237e5ac8bcf3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/62219996bfc4707a07de019f2f2b237e5ac8bcf3)
  Пакет проигран headless-хромом в локальном SCORM-плеере: 23 проверки, включая
  завершение попытки, отправку interactions и балла в LMS, продолжение прерванного
  прогона и приведение состояния пакета, собранного до правки.
  
  В §8 уточнено, что перемешивание вопроса на сопоставление — пара перестановок
  (две колонки тасуются независимо), и кодировка обязана нести обе: этим дефектом
  приёмка и окупилась.

- **docs**(prd-36): статус трека — реализован, приёмка на стенде впереди (2026-08-15) [`0965ec6b263fe68f3eabaf5d5c1cc4acb20ae0b7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0965ec6b263fe68f3eabaf5d5c1cc4acb20ae0b7)
  Задачи 1-10 плана выполнены. Замер после правки: контрольный состав §11.3 занимает
  3651 символ из 4096 вместе с прогоном в работе, размер состояния перестал зависеть
  от числа попыток. Приёмка в браузере и на WebTutor (§11 пункты 4-9, 13) не проведена.

- **docs**(prd-36): спека 1.1 и план работ по компактности состояния прогона (2026-08-15) [`54bbed6371f577a278929fd4ec1246c4c69587c8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/54bbed6371f577a278929fd4ec1246c4c69587c8)
  Анализ issue #38 по коду на HEAD: спека 1.0 не учитывала записи разрезов PRD-50
  и идентификатор варианта PRD-17/PRD-24, а дубль TEST_DATA в состоянии оказался
  шире содержимого вопросов — туда же попадают названия тем, материалы, правила
  порогов и тексты ключей.
  
  Спека 1.1: FR-01/FR-02 расширены, введён §4.6 (плотная кодировка рядов; без неё
  контрольный тест не влезает в 4096 в полтора раза), перемешивание хранится
  картой-строкой, §6 получил поимённый расчёт бюджета, §8 — третий дефект
  (порог by_variant деградирует после восстановления прогона), в риски внесены
  непокрытые модули состояния и пять точек записи попытки.
  
  План: 11 задач от характеризационных тестов до приёмки на WebTutor.

- **docs**(handoff): вписан итог полного прогона на HEAD (2026-08-15) [`017482ccfcc665e193b9b405740a81d4b1298eb3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/017482ccfcc665e193b9b405740a81d4b1298eb3)
  562 файла, 8861 тест, все зелёные, 751 секунда. Прогон снят после всех правок
  серии, включая `ensure-body`, поэтому это последнее слово о состоянии кода, а не
  о его промежуточном срезе. Из «следующих шагов» остался один — решение по PRD-36.

- **docs**(handoff): передача обновлена по факту закрытия хвостов (2026-08-15) [`c6ee40a28a92ee2ddb7723a6d33ac62021eff994`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c6ee40a28a92ee2ddb7723a6d33ac62021eff994)
  Со времени первой версии закрылись три из четырёх «следующих шагов»: обе issue
  закрыты, стенд прибран, найденная попутно ошибка 500 на запросе без тела
  исправлена и оказалась системной (39 незащищённых деструктуризаций против 13
  защищённых).
  
  Добавлены два решения, которые преемник иначе переиграет молча: почему пустое
  тело чинится одним middleware и почему ложные, но определённые значения не
  подменяются; и почему учётку разбора удаляли только после передачи владения.
  Туда же две грабли: внешних ключей на `users` в схеме нет, поэтому удаление
  учётки молча оставляет висячие ссылки, а учётная запись приёмки не имеет роли
  `learner` и ученический прогон под ней невозможен.
  
  Итог полного прогона на HEAD в документ НЕ вписан: на момент правки он ещё идёт,
  и выдавать ожидание за факт нельзя.

- **docs**(handoff): передача работы по адаптивному прогону и выполнимости выдачи (2026-08-15) [`b0900e84090f86a503d224682ab5aa1493d0ad00`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b0900e84090f86a503d224682ab5aa1493d0ad00)
  Сессия закрыла помеху приёмки Э5 PRD-50 и три пути, где невыполнимая выдача
  молчала. Документ фиксирует то, чего нет ни в коде, ни в истории: почему
  постановка задачи в реестре была неточной, почему отладочный прогон не
  блокируется, почему явное «не проверять» чинится без наблюдаемого дефекта и
  почему разрыв страницы отчёта не закрепляется регрессом.
  
  Отдельно записано, что полная сюита прогонялась на `9c6bf241`, а не на HEAD:
  после неё легли ещё три коммита.

- **docs**(roadmap): закрыты оба пункта долга, заведённые сегодня (2026-08-15) [`8c07b3c0e9fad16fa65eeef8fb10ada59e42b67e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8c07b3c0e9fad16fa65eeef8fb10ada59e42b67e)
  Обе строки заводились утром как «не отыгран» и «не доигрывается» — к вечеру
  обе перестали быть правдой, и оставлять их в открытых значило бы врать в
  документе, который сам объявляет себя нормативным источником статуса.
  
  Разрыв страницы отчёта: ветка отыграна в браузере на отчёте из 18 разделов,
  разрез прошёл по границе ряда карточек, дефекта нет. Регрессом не
  закрепляется сознательно — в jsdom такой тест проверял бы фикстуру, а не
  пагинацию; пороговая часть давно закреплена на чистой функции.
  
  Адаптивные тесты стенда: оба наблюдения разведены, дефект прогона исправлен,
  предупреждения о невыполнимой выдаче доведены до всех трёх путей, на стенде
  заведена проходимая фикстура со скриптом засева.
  
  Строки §0.3 переехали в закрытые с полным разбором, §0.0.4 сократился до семи
  пунктов, §0.0.1 остался без хвостов, шапки документа и спеки PRD-50 приведены
  к факту.

- **docs**: долг Э2 PRD-50 снят из реестра (2026-08-15) [`0b4d5141a4ab50b6920385bcfccd03d0fd90bdc5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0b4d5141a4ab50b6920385bcfccd03d0fd90bdc5)
  Строка §0.0.1, строка главной таблицы §0 и шапка спеки помечали долг как
  открытый и не перепроверявшийся. Он снят коммитом d563ac15 — там же разобрано,
  почему наблюдаемого следа он не давал и почему всё равно требовал правки.

- **docs**: статусы приведены к факту после слияния PRD-50 (2026-08-15) [`0b4f98cc9ee1c2ddfa560b1afe551952b86e79cb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0b4f98cc9ee1c2ddfa560b1afe551952b86e79cb)
  ROADMAP объявляет себя нормативным источником статуса, а держал PRD-50
  незакрытым: «ветка НЕ влита, впереди Э3 и Э4 и приёмка» — при влитой ветке,
  закрытых Э1 — Э5 и пройденных приёмках по всем этапам. Строка §0.0.1 и строка
  главной таблицы §0 переписаны по факту, с тремя оставшимися хвостами и ссылками
  на протоколы и issue.
  
  Слияние снимает пункт 4 §0.0.4 (паритет «Сертификации»): условие снятия было
  записано в самом пункте. Строка §0.3 убрана из таблицы в закрытые, но с
  оговоркой — структурный выход `manifest.extends` не сделан и вернётся при
  следующем расхождении.
  
  Документационный долг §0.0.5 закрыт целиком: шапки PRD-44 («реализация не
  начата» при закрытых фазах 0-6), PRD-48 («частично выполнена» при собственном
  §9 «РЕАЛИЗОВАНО ПОЛНОСТЬЮ») и PRD-50 приведены к факту.
  
  Долг Э2 (явное «не проверять» у ключа не переживает сохранение) сохранён
  открытым: он упоминается только в ROADMAP, ни в спеке, ни в протоколах следа
  нет, и с момента записи его никто не перепроверял.

- **docs**(roadmap): долг по отчёту и suspend_data заведён в трекере (2026-08-15) [`683934ad327a93ce786fd270bd5679e858ef958e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/683934ad327a93ce786fd270bd5679e858ef958e)
  Из трёх наблюдений разбора в трекере была видна только адаптивная помеха
  (#36). Разрыв страницы отчёта внутри длинного блока (#37) и переполнение
  `cmi.suspend_data` (#38) держались строками реестра: первый — заведённой
  сегодня, второй — с 2026-08-03, но как след происхождения трека PRD-36.
  
  Обе заведены, §0.0.6 и соответствующие строки §0.3 ссылаются друг на друга.
  Для #38 нормативным источником остаётся спецификация PRD-36: issue нужна,
  чтобы риск был виден в трекере, а не подменяет трек.

- **docs**(roadmap): два наблюдения приёмки PRD-50 переведены в технический долг (2026-08-15) [`efe0c28ee4a3b3983ce913ffa01e75a34b70f375`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/efe0c28ee4a3b3983ce913ffa01e75a34b70f375)
  Разрыв страницы отчёта внутри длинного блока и непроходимость адаптивных
  тестов стенда до сих пор держались только строками в протоколах приёмок —
  то есть в документах, которые после закрытия этапа никто не перечитывает.
  
  В §0.3 заведены две строки с полными формулировками, в §0.0.4 — пункты 9 и 10.
  По адаптивному наблюдению дополнительно заведена issue #36: протокол Э5
  называл обе его причины данными стенда, но истёкший таймер темы, не
  переключающий экран, на данные не похож и требует разбора.

- **docs**(prd-50): протокол браузерной приёмки Э5 (2026-08-15) [`757969ccea17aa380fcdb8a97ea384180560a67b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/757969ccea17aa380fcdb8a97ea384180560a67b)
  Сквозной прогон адаптивного теста на стенде невозможен — оба адаптивных теста
  dev-БД непроходимы по своим данным. Печать проверена монтированием отгружаемых
  раскладок с контекстом общего ядра; помеха описана отдельно, чтобы не выглядеть
  пропущенной проверкой.

- **docs**(prd-50): этап Э5 — разрез результата в адаптивном режиме (2026-08-15) [`030557cd3a6b0c997758ac942552a254faea4f97`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/030557cd3a6b0c997758ac942552a254faea4f97)
  Записи адаптивной попытки считались и хранились с Э1-доделки, но ни один экран
  не читал их обратно: автор включал подытоги, видел их в обычном тесте и молча
  терял в адаптивном. FR-48 закрывает это расхождение и ничего не добавляет к
  модели. Разрез раздела в адаптивном режиме не печатается сознательно — карточка
  темы там говорит подтверждённым уровнем.

- **docs**(prd-50): протокол браузерной приёмки Э3 и Э4 (2026-08-14) [`99349b36b68807f4ff269fb5f57aeadb642cdcc1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/99349b36b68807f4ff269fb5f57aeadb642cdcc1)

- **docs**(roadmap): ROADMAP и BRD догнали факт после пятидневного разрыва (2026-08-14) [`75c7a72c1099d61a8c87a8f0017b325198b48365`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/75c7a72c1099d61a8c87a8f0017b325198b48365)
  Документы стояли на 2026-08-09 и разошлись с кодом содержательно, а не
  косметически: PRD-28 значился «НЕ НАЧАТ» при влитом и принятом в браузере
  треке, PRD-44 — «идея, требования не собирались» при согласованной спеке
  и семи закрытых фазах плана, а PRD-48, PRD-49 и PRD-50 не значились вовсе.
  Туда же добавлены две работы, PRD не заводившие: настройка «Тест пройден,
  если» и регистрация прогона измерительного теста в аналитике.
  
  Сводка открытых задач трекера пересобрана по факту: открыты #35, #32, #28,
  #27, #26; закрыты #12, #31, #33, #34. Хвост PRD-10 снят вместе с #31.
  
  В §0.3 добавлена строка долга, подтверждённая замером: копия
  tb-components.css для эскизов отстала от приложения на 250 корневых
  селекторов, причём 33 эскиза берут копию, а 13 — источник. Обратный
  случай записан рядом: паритет «Сертификации» с эталоном закрыт на этой же
  ветке (гард расширен на настройки, порядок блоков итогов и стили), но
  пункт держится открытым до слияния.
  
  В BRD подняты статусы BR-17, BR-19, BR-24 и BR-29, а PRD-45 - PRD-50
  заведены Этапами 42-47 по порядку ЗАВЕДЕНИЯ требования, как предписывает
  §7. Номера этапа не получил только PRD-44: требования согласованы и код
  идёт, а решения, нужен ли ему свой BR, нет — это записано открытым
  вопросом, а не додумано.

- **docs**(prd-50): протокол приёмки Э1, доделки и Э2 в браузере (2026-08-14) [`4f58659b0222e7df93c3faab1bd0a8ac27f4ad64`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4f58659b0222e7df93c3faab1bd0a8ac27f4ad64)

- **docs**(db): пересобранная миграция ломает migrate по времени — признак и лечение (2026-08-14) [`c696910598606da3733535c822005a832249a2d4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c696910598606da3733535c822005a832249a2d4)

- **docs**(prd-50): долг Э2 — явное «не проверять» у ключа не переживает сохранение (2026-08-14) [`305018ed91b701873b62ab43e5f6d269bf0f5e2d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/305018ed91b701873b62ab43e5f6d269bf0f5e2d)

- **docs**(wireframes): состояние «Предпросмотр балла» переехало на актуальную поверхность (2026-08-14) [`034b8a0576a50535ae9feb2f9e0ef7ad27d8b7f4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/034b8a0576a50535ae9feb2f9e0ef7ad27d8b7f4)
  Кнопка «Предпросмотр балла» в модале «Оценка вопроса в тесте» была нарисована,
  но никуда не вела: само состояние жило в prd10-question-scoring.html, то есть на
  поверхности, которой больше нет — конструктор цены ответа с PRD-15 блока D
  находится в модале теста, а не в карточке вопроса.
  
  Демо-ответы собираются из эталона вопроса, а не набираются автором: эскиз
  показывал готовый список комбинаций, но не говорил, откуда он берётся.

- **docs**(templates): формат шаблонов 1.6.0 — разрез, разрыв страницы, перечень надписей отчёта (2026-08-14) [`a2dadc2f4f6e8ef925820b9265afcc7cef84ea2e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a2dadc2f4f6e8ef925820b9265afcc7cef84ea2e)
  Спецификация отстала от кода на разрез результата и на принудительный разрыв листа,
  а руководство разработчика — ещё и на три предыдущие редакции формата: надписи итогов,
  порядок подблоков и вводный блок в нём не описаны вовсе, хотя в контракте они с 1.4.0
  и 1.5.0. Внешний шаблон писался по руководству, поэтому его отставание дороже: автор
  такого шаблона не знал ни про labels[], ни про result.blocks[], и печатал жёсткие
  строки, которых автору теста нечем переформулировать.
  
  Спецификация (1.5.0 -&gt; 1.6.0):
  
  \- §10.6 — строка разреза целиком: ключ, счётчики, баллы, три доли, ширина полосы и
    готовая подпись. Числа объявлены обязательными с самого начала, потому что сторонний
    шаблон не покажет «верно 4 из 7» из ширины полосы, а добавление поля позже — правка
    контракта на оба хоста и на все собранные пакеты. Полей вердикта ключа в строке пока
    нет, и это сказано прямо;
  \- §8.4.6 — метка data-page-break: почему атрибут, а не класс, любая глубина, правила
    игнорирования и полноразмерный лист. До этого контракт жил только в дизайн-документе;
  \- §8.3.2 — перечень надписей ОТЧЁТА считается сканированием макетов вариантов, а не
    объявляется вторым списком; следствие для разработчика шаблона названо явно;
  \- §8.3, §10.2, §16 — перекрёстные ссылки и место настройки показа.
  
  Руководство (1.3.0 -&gt; 1.6.0):
  
  \- §3.4 и §3.5 — объявление надписей и порядка подблоков с проверками активации;
  \- §7.3 — labels.*, result.blocks[], result.introHtml и разрез с разметкой и правилами,
    включая запрет подавать его как разложение раздела;
  \- §7.3a — слой надписей отчёта и правило «не печатаете labels.&lt;ключ&gt; — автору не
    предложат»; разрез в отчёте;
  \- §15 и §16 — три пункта чек-листа и четыре строки типичных ошибок.
  
  Проверка: npm run lint:md чисто; состав строки разреза и надписи контролов сверены с
  кодом на HEAD ветки, а не с планом.

- **docs**(prd-50): пометить регрессный прогон аналитики в проверке Э2 (2026-08-14) [`a62b3866250c8c6d8776dff57359077dac4cfa3a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a62b3866250c8c6d8776dff57359077dac4cfa3a)

- **docs**(prd-50): аналитика SCORM закреплена за Э1-доделкой, дубликат из Э2 снят (2026-08-14) [`45f9768d1b29edce57cae08793740ec299c93a9e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/45f9768d1b29edce57cae08793740ec299c93a9e)

- **docs**(prd-50): план доделки Э1 — адаптивный режим, контракт строки, сохранение записей (2026-08-14) [`691ae1b8576108315412868de89dabca750bea0b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/691ae1b8576108315412868de89dabca750bea0b)

- **docs**(prd-50): план Э2 — пороги ключей, гейт темы, предупреждения публикации (2026-08-14) [`0e2f75c82fb260d102ed616567f640846b63912f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0e2f75c82fb260d102ed616567f640846b63912f)

- **docs**(prd-50): план Э3 и Э4 — группы разделов, надписи вердикта, блок области теста (2026-08-14) [`7e1cf2f87046e37807622ce5beb2346d4198e5a7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7e1cf2f87046e37807622ce5beb2346d4198e5a7)

- **docs**(prd-50): спека приведена к факту — контракт строки, карта требований по этапам (2026-08-14) [`f931b57dccf4b173ed4e13d11fc91a88267d9205`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f931b57dccf4b173ed4e13d11fc91a88267d9205)

- **docs**(references): книга сертификационного теста руководителей (2026-08-14) [`7e4ff50c275d38539c76310577f0edfa8c35ce47`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7e4ff50c275d38539c76310577f0edfa8c35ce47)
  Исходник, на котором построен разбор PRD-50: спека ссылается на него в
  §1.1 и §14, а без файла обе ссылки висят в пустоту. Лист «Вклады
  вопросов» здесь и есть главный аргумент за нативный подытог по ключу —
  464 строки, целиком выводимые из правильных ответов.

- **docs**(prd-50): решения владельца и долг по итогам ревью этапа (2026-08-14) [`e70bd2741ac8c1aa3e91336b1e56ad4c4e157bf4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e70bd2741ac8c1aa3e91336b1e56ad4c4e157bf4)

- **docs**(prd-50): шапки новых тестов на английском, как требует конвенция (2026-08-14) [`3b5f5c2d1262a7e9766b286a456e2b1c7384fbdd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3b5f5c2d1262a7e9766b286a456e2b1c7384fbdd)

- **docs**(prd-50): план этапа Э1 и три правки спеки (2026-08-13) [`9c084eeeb352c7fa669c6829226f35b43b504b2e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9c084eeeb352c7fa669c6829226f35b43b504b2e)
  План Э1: движок разрезов, подача ключей обоими хостами, живые tag()
  и sectionById(), контекст рендера, переключатель показа, полосы
  в раскладках.
  
  Правки спеки по итогам чтения кода: теги стандартных разделов уже
  выпекаются в TEST_DATA (не выпекаются только у адаптивных); область
  раздела в формулах адресуется составным ключом вместо второго
  аргумента, поэтому грамматика DSL и её JS-двойник не трогаются;
  видимость разреза перенесена в Э1, иначе первый этап начал бы
  печатать полосы без спроса автора.

- **docs**(prd-50): спецификация согласована (2026-08-13) [`ad6f576dff03c2e01c2d90c2afdec12be924195a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ad6f576dff03c2e01c2d90c2afdec12be924195a)

- **docs**(prd-50): спецификация разрезов результата (2026-08-13) [`b2304e9e80447159d3a0d7e622e2642d6b345c1b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b2304e9e80447159d3a0d7e622e2642d6b345c1b)
  Подытоги по ключам разреза в области раздела и теста, пороги ключей как
  гейт темы, группы разделов со счётчиком, настраиваемые подписи вердикта.
  
  Основание — разбор отчётов и книги сертификационного теста руководителей:
  лист «Вклады вопросов» на 464 строки проверен целиком и выводится из
  правильных ответов без исключений, то есть подытог по тегу сервис может
  считать сам.

- **docs**(changelog): раздел 2.14.0 (2026-08-11) [`8794e32dceebb70995abdb5ae6c65b14e1666d7e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8794e32dceebb70995abdb5ae6c65b14e1666d7e)

- **docs**(prd-48): полный прогон зелёный, разбор трёх дотрековых причин (2026-08-11) [`51b7ae80d8d873ae1bc27581e29a636576aca79e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/51b7ae80d8d873ae1bc27581e29a636576aca79e)

- **docs**(prd-48): приёмка этапов 5 и 6, дефект счётчика предпросмотра (2026-08-11) [`cf5769686f5124310cbdca26209c42c50dd71825`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cf5769686f5124310cbdca26209c42c50dd71825)

- **docs**(workbook): шапка импортёра знает про лист «Оформление» (2026-08-11) [`5cff34fef36272ded43bb480df1e7458c9dda799`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5cff34fef36272ded43bb480df1e7458c9dda799)

- **docs**(workbook): справка книги сверена по всем листам (2026-08-11) [`43c6ea2f860f07b35fe75294478449cad04e9ce9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/43c6ea2f860f07b35fe75294478449cad04e9ce9)

- **docs**(workbook): руководство описывает все листы книги (2026-08-11) [`a329303ce314647bf9eb2c9749ff6753f3fe0523`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a329303ce314647bf9eb2c9749ff6753f3fe0523)

- **docs**(prd-48): контракт листа оформления (2026-08-11) [`b33a200850dde15f7c1299e10ee7870106be8530`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b33a200850dde15f7c1299e10ee7870106be8530)

- **docs**(prd-48): план этапа 5 — оформление и отчёт (2026-08-11) [`4bf13c61b44fe8b81916ae6a0179e18c3aa56a90`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4bf13c61b44fe8b81916ae6a0179e18c3aa56a90)

- **docs**(prd-48): приёмка этапов 3 и 4 пройдена на dev-стенде (2026-08-11) [`e7f79ac2171405fc19ab4b22d6218b0474c3571e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e7f79ac2171405fc19ab4b22d6218b0474c3571e)

- **docs**(workbook): оптовая семантика листа адаптивных уровней названа явно (2026-08-11) [`36bf56ac4b72eb95878e5b14c899210295ad0af4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/36bf56ac4b72eb95878e5b14c899210295ad0af4)
  Правило «лист со строками описывает уровни теста целиком» выводилось из
  соседних пунктов, но дословно нигде не стояло: тема, стоящая на «Структуре»
  без строк уровней на НЕПУСТОМ листе, свои уровни теряет. Поведение не
  меняется — фиксируется, чтобы не всплыло сюрпризом.

- **docs**(prd-48): контракт листа адаптивных уровней (2026-08-11) [`deecca684d3a3fd0dea2237346588239f022ea77`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/deecca684d3a3fd0dea2237346588239f022ea77)

- **docs**(prd-48): план этапа 4 — адаптивные уровни (2026-08-11) [`e7fbd92582dc7b78704f6bdbab9d921cff5c76a1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e7fbd92582dc7b78704f6bdbab9d921cff5c76a1)

- **docs**(prd-48): итог ревью этапа 3 (2026-08-11) [`6cc0e9e9f56253bf5192bcf7a66a9dc1b873a113`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6cc0e9e9f56253bf5192bcf7a66a9dc1b873a113)

- **docs**(workbook): «Задержка, мс» описана без противоречия (2026-08-11) [`833400248c5696b36aadf280400202c140514f97`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/833400248c5696b36aadf280400202c140514f97)

- **docs**(workbook): шапка импортёра знает про все листы книги (2026-08-11) [`43cf1aa207678c8ed41e2930683450b5e3d9a350`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/43cf1aa207678c8ed41e2930683450b5e3d9a350)

- **docs**(prd-48): контракт листов страниц и ограничение круга по настройкам (2026-08-11) [`5284a391382d0b7284e59eefb8147d773eb1041c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5284a391382d0b7284e59eefb8147d773eb1041c)

- **docs**(prd-48): разметка плана этапа 3 проходит линтер (2026-08-11) [`9c6b4c539292b5a73218ac3c6894b9a495816ae7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9c6b4c539292b5a73218ac3c6894b9a495816ae7)

- **docs**(prd-48): план этапа 3 — страницы теста (2026-08-11) [`bcaeab7910e5b1bc9fbfacd7d880f6c24cce1c8a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bcaeab7910e5b1bc9fbfacd7d880f6c24cce1c8a)

- **docs**(prd-48): приёмка этапа 2 пройдена на dev-стенде (2026-08-11) [`9938118059d75b00b9be14dc8dcdf2b248591625`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9938118059d75b00b9be14dc8dcdf2b248591625)

- **docs**(workbook): контракт листов обратной связи в спецификации формата (2026-08-10) [`87696004af914227d594e6ce671342aa0ca0cba3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/87696004af914227d594e6ce671342aa0ca0cba3)

- **docs**(prd-48): план этапа 2 — обратная связь и рекомендации (2026-08-10) [`a5626c20c0abfae8175c01463480c0bca01a713b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a5626c20c0abfae8175c01463480c0bca01a713b)

- **docs**(prd-48): приёмка этапа 1 пройдена на dev-стенде (2026-08-10) [`ef1cdf8757cebbb3fce49fc9096bbfe799b19cfc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ef1cdf8757cebbb3fce49fc9096bbfe799b19cfc)

- **docs**(prd-48): статус этапа 1, точность руководства и комментария о ветви маршрутизатора (2026-08-10) [`8594a3902a74a29b93cab99562006879ffb1b0f6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8594a3902a74a29b93cab99562006879ffb1b0f6)

- **docs**(workbook): руководство больше не обещает переключение в маршрутизатор (2026-08-10) [`4b4392c55bcd55ac7491b729e7a4ab2e1310db46`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4b4392c55bcd55ac7491b729e7a4ab2e1310db46)

- **docs**(prd-48): сценарий задаёт лист «Настройки», листы «Настройки» и «Оценка» в контракте формата (2026-08-10) [`70c0e9f088ea6c6da6c7d7110ddbb849178acc10`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/70c0e9f088ea6c6da6c7d7110ddbb849178acc10)

- **docs**(prd-48): правило английских комментариев в плане этапа 1 (2026-08-10) [`2b5aa6842f6b04b0816cab1201c772ed48c268aa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2b5aa6842f6b04b0816cab1201c772ed48c268aa)

- **docs**(prd-48): спека полного переноса теста книгой и план этапа 1 (2026-08-10) [`bc545b64250413808f1666591ab9cf0185410b2a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bc545b64250413808f1666591ab9cf0185410b2a)

## [2.15.1](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.15.0...v2.15.1)

### Fixes

- **fix**(report): убраны пустые строки между абзацами авторских текстов (2026-08-13) [`f3833fcf2752871b8a3c2f37b1dff67986b4f826`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f3833fcf2752871b8a3c2f37b1dff67986b4f826)
  Текст рекомендации и вводный блок приходят РАЗМЕТКОЙ: простой текст ядро отдаёт
  уже с `&lt;br&gt;` (`richTextToHtml`), форматированный переносит абзацы своими тегами.
  `white-space: pre-wrap` на контейнере при этом печатал ещё и каждый перевод строки
  МЕЖДУ тегами — поверх отступа абзаца. В отчёте между абзацами методички зияла дыра
  в две строки: замер на настоящем тексте автора даёт 45px против 8px после правки.
  
  Сброс `white-space: normal` на детях не помогал бы: пустую строку рисует текстовый
  узел `\n`, который лежит в САМОМ контейнере, а не в абзаце. Поэтому `pre-wrap` снят
  с контейнера, а отступы оставлены тегам.
  
  На экране итогов текст тот же самый, поэтому правило зеркальное — и там же
  исправлена обёртка: авторский `&lt;p&gt;` внутри `&lt;p class="tb-recs-group__text"&gt;`
  браузер закрывал досрочно, и разметка автора уезжала из-под правил блока.

- **fix**(prd-49): зонтик итогов печатается в отчёте, а панель предлагает только реальные надписи (2026-08-13) [`4ea700b64f66afb153fd0b00648770aff7d105c5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4ea700b64f66afb153fd0b00648770aff7d105c5)
  Тумблер «Заголовок итогов» стоял включённым и не давал в документе ничего: макеты
  отчёта его не печатали. Спека этого не разрешала — §3 требует, чтобы та же иерархия
  и те же ключи действовали на адаптивных итогах, на итогах раздела и в отчёте, а
  надпись «Ваш результат» стояла зонтиком над сводкой, темами, шкалами и
  показателями. Теперь зонтик печатают все четыре макета отчёта — после вводного
  блока, над разделом результата, — и один тумблер гасит его сразу в обоих местах.
  
  У показателей убрано отдельное отчётное умолчание (`defaults.report`): оно
  существовало ровно потому, что зонтика в документе не было, и роль зонтика отдали
  блоку показателей. С зонтиком на месте документ говорил бы «Ваш результат» дважды
  подряд, поэтому блок вернулся к общей формулировке «По показателям» — как в таблице
  §5 спеки и как на экране.
  
  Панель «Заголовки и подписи отчёта» перестала предлагать надписи, которых в
  документе нет: числа сводки, строки карточки темы, подпись итогов раздела и сводку
  баллов он печатает своей вёрсткой. Перечень СЧИТАЕТСЯ по макетам вариантов отчёта
  (`readReportLabelKeys`, поле `reportLabelKeys` в ответе `GET /api/templates/:id`), а
  не объявляется в манифесте вторым списком: знает об этом только макет, и второй
  список разошёлся бы с ним молча. Для отчёта такой расчёт точен, а не приблизителен —
  порядок подблоков документу не передаётся, поэтому прямой путь `labels.*` там
  единственный способ попасть заголовку в PDF. Шаблон без вариантов отчёта берёт
  перечень «Стандартного» — тем же правилом, каким берёт его макеты (FR-10). Поля нет
  в ответе — панель показывает все объявления, как раньше.
  
  Считается на запрос, а не при регистрации: иначе перечень отставал бы от файлов
  шаблона до перезапуска, как сам манифест.
  
  Проверка: новый набор на перечень (оба шаблона, порядок манифеста, шаблон без
  вариантов), проверка печати зонтика и его гашения в обоих видах отчёта, три теста
  панели — все написаны до правки и на прежнем коде краснели. 198 тестов по затронутым
  наборам. Живьём: маршрут отдаёт семь ключей, ответ результата несёт «Ваш результат»,
  макет его печатает.

- **fix**(prd-49): выключенный слот карточки гаснет и в отчёте (2026-08-13) [`2b593e3f8f1d0233dac5a637aab111fa0e77f37c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2b593e3f8f1d0233dac5a637aab111fa0e77f37c)
  Тумблеры «Показывать название» и «Показывать уровень» гасили слот только на экране
  итогов: в PDF название показателя и метка исхода печатались по-прежнему, и в
  документе возвращалась ровно та пара подписей об одном и том же, ради которой
  PRD-49 и затевался. Ученик уносит отчёт с собой — это такая же ученическая
  поверхность, как экран.
  
  Признаки `hideName`/`hideLevel` в контексте документа были всегда: отчёт собирает
  тот же `buildResultContext` с тем же входом измерений (`shared/report/report-measures`).
  Их не читали ЧЕТЫРЕ макета — `report.html` и `report.adaptive.html` обоих поставляемых
  шаблонов, по блоку шкал и блоку показателей в каждом. Теперь слоты печатаются через
  ту же проверку `unless`, что на экране; проверка именно инвертированная, потому что
  рукописный контекст (превью шаблона, фикстуры) этих полей не ставит вовсе.
  
  Данные не чистятся и здесь: название и метка уровня остаются в базе, их читают
  аналитика и выгрузка. Прежнее обещание «их берёт отчёт» убрано из подсказки
  редактора, из PRD-49 §2 (решение 4) и §6 и из шапки `measure-view`.
  
  Проверка: тест написан до правки и падал на обоих шаблонах, после правки зелёный;
  он же держит обратное — соседние слоты той же пары карточек остаются на месте.
  Приёмки в браузере на скачанном PDF не было.

- **fix**(prd-49): надписи итогов перестали делить панель на две колонки заголовков (2026-08-13) [`11016f417d6736921c861035aabd4f5a17886f51`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/11016f417d6736921c861035aabd4f5a17886f51)
  Карточка «Заголовки и подписи отчёта» была `FormSection`, обёрнутой вокруг панели,
  которая сама разложена секциями. У секции раскладка в две колонки — 280px под
  заголовок и остаток под содержимое, — поэтому вложение давало 280 + 280 подряд:
  полю формулировки оставалась полоска шириной в три буквы, а вкладке «Оформление» —
  горизонтальная прокрутка. Теперь это карточка, как соседнее «Оформление отчёта».
  
  Группы надписей и порядок подблоков печатают заголовок НАД строками: подраздел
  живёт в панели-ящике, где колонка в 280px забирает у полей половину ширины, а все
  прочие подразделы оформления рисуют поля во всю ширину. Модификатор `--stacked`
  в стилях дизайн-системы был, а включить его было нечем — у `FormSection` появился
  проп `stacked`.
  
  Проверено в браузере на обеих панелях («Итоги» и «Отчёт о результатах»):
  горизонтальной прокрутки нет, поля во всю ширину карточки.

- **fix**(design): настройки без templateId больше не оставляют вкладку без шаблона (2026-08-13) [`c18c795fcc7c84cdee65b04960daaed45dd8baa1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c18c795fcc7c84cdee65b04960daaed45dd8baa1)
  У части тестов `design_settings_json` несёт одни `params` без `templateId` —
  так переезжает перенесённый тест и так ложится внешняя запись в базу. Все пути
  выдачи подставляют «default» сами, поэтому ученику показывали стандартный
  шаблон, а редактор отвечал буквально: манифест не запрашивался, `design.template`
  оставался пустым, и вкладка «Оформление» показывала пустые «Брендирование»,
  «Цвета», «Макет», а «Итоги» — баннер «Шаблон не поддерживает настройку
  заголовков», хотя манифест объявляет пятнадцать надписей. Баннера «Шаблон
  недоступен» при этом не было: он требует непустого сохранённого `templateId`.
  
  Теперь `GET /api/tests/:id/design` дописывает `templateId` умолчанием, когда его
  нет, — читающая сторона ведёт себя так же, как выдача. Первое сохранение
  оформления кладёт ключ в базу, и строка лечится сама.

## [2.15.0](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.14.0...v2.15.0)

### Features

- **feat**(report): разрыв страницы по требованию макета, и три правки самой раскладки (2026-08-13) [`d2a67f861b32f79479920a48a6f76bb9d8be3de8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d2a67f861b32f79479920a48a6f76bb9d8be3de8)
  Раскладка делит документ сама — по высоте листа, по границам карточек и по
  нижним границам строк. Решение машинное: она знает, что помещается на лист, но
  не знает, что документ читается разделами. Верстальщик шаблона не мог сказать
  «показатели начинаются с новой страницы», а именно так собирают методические
  отчёты. Теперь может: пустой узел `&lt;div data-page-break&gt;&lt;/div&gt;` в макете, на
  ЛЮБОЙ глубине — между карточками, внутри карточки, в теле `{{#each}}`.
  
  Атрибут, а не класс: имена классов принадлежат шаблону, и внешний шаблон,
  переименовавший метку у себя, потерял бы правило молча. Стилей не требует и
  следа на бумаге не оставляет — узел пуст, а разрез идёт по его ВЕРХНЕЙ границе.
  Приказ старше всех прочих правил, включая «карточку не рвём»; пустых листов не
  даёт (метка в начале, подряд идущая и хвостовая игнорируются); лист остаётся
  полноразмерным A4 с подложкой во всю высоту. Правило целиком в
  `sliceBySafeLines`: линии делят содержимое на разделы, раздел режется по
  строкам как раньше, — поэтому метка между карточками и метка внутри карточки
  работают одним механизмом. Автор теста не участвует: ни полей, ни интерфейса.
  Спека — `docs/specs/2026-08-13-report-page-break-design.md`.
  
  Проверка непрерывности, написанная под приёмку (сверка визуальных строк
  документа со строками листов), вскрыла три давних дефекта той же раскладки.
  
  ПОЛЕ СТРАНИЦЫ СЧИТАЛОСЬ СОДЕРЖИМЫМ. Меряется всё от верхнего края корня, то
  есть вместе с полем, а показывает кусок окно, стоящее уже за полем. Окно
  показывало на высоту поля БОЛЬШЕ отмеренного: лишняя полоса приходила из
  следующего куска, строка на её краю резалась пополам и выходила целиком на
  следующем листе. Заодно лист «ровно в полезную высоту» считался переростком и
  делился надвое — второму доставалось восемь пикселей.
  
  ОДИН БЛОК ВЫШЕ СТРАНИЦЫ обрезался молча. Быстрый путь «документ в один лист»
  отдавал его как есть, и конец текста не уезжал на второй лист, а пропадал.
  
  ПЕРЕРОСТОК УНОСИЛО НА СВОЙ ЛИСТ. Модуль описывал обратное правило — «её незачем
  переносить, ей всё равно не хватит места», — а код переносил: первая страница
  шестистраничного отчёта несла одну шапку и семь сотен пикселей пустой бумаги.
  Переносится только та карточка, которой новый лист поможет; те же документы
  стали короче на лист.

- **feat**(prd-28): внешний участник в списке пользователей и перевод в штатные (2026-08-13) [`e5239e0ae5abe3cdd980fb61e5f0646526bb75c9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e5239e0ae5abe3cdd980fb61e5f0646526bb75c9)

- **feat**(prd-28): отчёт прогона и выгрузка ссылок книгой (2026-08-13) [`e8b490d4548f0a5809a8cdc2401f5007987d6448`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e8b490d4548f0a5809a8cdc2401f5007987d6448)

- **feat**(prd-28): вкладка приглашения участников списком (2026-08-13) [`2a4ef9246d2a7f9cede53887fd9d255953cbd20c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2a4ef9246d2a7f9cede53887fd9d255953cbd20c)

- **feat**(prd-28): аудит прогона рассылки и выгрузки ссылок (2026-08-13) [`86a5e96e1b20ab7a9662ec0e482f40396691ecc8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/86a5e96e1b20ab7a9662ec0e482f40396691ecc8)

- **feat**(prd-28): эндпоинты предпросмотра, прогона и шаблона книги участников (2026-08-13) [`85006dd59aa50be1a1e614b0eab0b79281e8c503`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85006dd59aa50be1a1e614b0eab0b79281e8c503)

- **feat**(prd-28): прогон рассылки участников с отчётом по каждому адресу (2026-08-13) [`18635727fa871b4b2957e741e364a55625313b11`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/18635727fa871b4b2957e741e364a55625313b11)

- **feat**(prd-28): классификация строк предпросмотра по состоянию системы (2026-08-13) [`cbf0167ceee99cbb9ab813b15eceaadde3f94eb6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cbf0167ceee99cbb9ab813b15eceaadde3f94eb6)

- **feat**(prd-28): разбор книги участников со схлопыванием повторов (2026-08-13) [`f04ebd5469bf2d75c4bcd19f9eb1c867c19e6fc6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f04ebd5469bf2d75c4bcd19f9eb1c867c19e6fc6)

- **feat**(prd-28): потолок строк и почасовой предел писем вынесены в настройки (2026-08-13) [`55069346e86f026dd1656666c7c0a7402f353bd3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/55069346e86f026dd1656666c7c0a7402f353bd3)

- **feat**(analytics): окно детализации показывает ответ, шкалы и показатели (2026-08-13) [`0f42ac04ff1705f620f7d86fabfe3890893d1cbf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0f42ac04ff1705f620f7d86fabfe3890893d1cbf)
  Окно печатало только баллы и галочку, поэтому у измерительной методики в
  нём не оставалось ничего: четырнадцать строк «0/1 ✗» под зелёным
  «Пройден». Ответ ученика не выводился вообще — ни здесь, ни у контрольного
  теста, хотя сервер всё это время слал его в готовом к печати виде.
  
  Окно берёт то, что уже приезжало: шкалы и показатели прогона, сам ответ,
  эталон при ошибке и вклад ответа в шкалы. Балл и галочка снимаются у
  вопроса, который не проверяется, а частично верный ответ получает тег
  вместо крестика — «1.5/3» рядом с крестиком были двумя несогласными
  утверждениями в одной строке.
  
  Признаки прогона необязательны, и отсутствие значит «показать»: ответ
  сервера, который их не шлёт, рисуется ровно как раньше, а не обнуляет
  каждую строку.

- **feat**(analytics): прохождение измерительного теста регистрируется целиком (2026-08-13) [`d1e6d9aa35ca1b473f3e4a98567052019ad2c166`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d1e6d9aa35ca1b473f3e4a98567052019ad2c166)
  Аналитика была устроена только вокруг оценки, поэтому прогон методики
  попадал в неё как «0.0 % · Сдан»: ни шкал, ни показателей до автора не
  доезжало, а порог 70 % — умолчание любого нового теста — превращал прогон,
  который ничего не оценивает, в зелёный вердикт. Excel повторял то же и
  сверх того печатал «Неверно» и 0 баллов каждому измерительному ответу,
  сырой JSON вместо утверждений и `{}` вместо эталона, которого у типа нет.
  
  Теперь измерения попытки едут вместе с ней — СОХРАНЁННЫЕ, а не пересчёт по
  сегодняшней конфигурации: аналитика отвечает «что получил этот человек», а
  пересчёт молча переписал бы историю после правки шкалы. Метрики, которым
  нечего усреднять, отвечают `null` («неприменимо»), а не нулём; в книге
  появились колонки шкал и показателей, «Вклад в шкалы» по каждому ответу и
  прочерк там, где вопрос не бывает верным или неверным.
  
  Признаков два, не один: балл — это данные, вердикт — суждение, и тест с
  авторским «порога нет» всё равно набрал реальные баллы. Адаптивный прогон
  освобождён от обоих: его судят подтверждённые уровни, а `possiblePoints`
  он не несёт вовсе — так выглядят 49 из 72 завершённых попыток базы.

- **feat**(prd-28): письмо без разовой ссылки ведёт на адрес теста (2026-08-13) [`cb6093f55d307810eec8b67a1b29b09aeaa02fc5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cb6093f55d307810eec8b67a1b29b09aeaa02fc5)

- **feat**(prd-28): выдача ссылки возвращает её и исход отправки вызывающему (2026-08-13) [`6a4278b7261811c37d2a8c6da80afc62a1a8da0a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6a4278b7261811c37d2a8c6da80afc62a1a8da0a)

- **feat**(prd-28): заведение внешнего участника поштучно и перевод в штатные (2026-08-13) [`9d90457407426da314afeabd292a00a100e898a4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9d90457407426da314afeabd292a00a100e898a4)

- **feat**(prd-28): внешней учётке отказано в восстановлении пароля и приглашении (2026-08-13) [`78c753eaa56a4a513b0612ae2aaf52f1fef14c84`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/78c753eaa56a4a513b0612ae2aaf52f1fef14c84)

- **feat**(prd-28): внешняя учётка заводится без пароля и не пускает по паролю (2026-08-13) [`dbf4817dcfd33e8f215a441d97570d73b28f0ff4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dbf4817dcfd33e8f215a441d97570d73b28f0ff4)

- **feat**(prd-28): признак внешнего участника и учётка без пароля в схеме (2026-08-13) [`4cb7a0db543f9e829db2fe1641e9dbdb0150f9d6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4cb7a0db543f9e829db2fe1641e9dbdb0150f9d6)

- **feat**(editor): переключатель «Тест пройден, если» пишет настройку теста (2026-08-12) [`69602b58d868dfbd3656c7bf29ff4a2cc9d716ae`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/69602b58d868dfbd3656c7bf29ff4a2cc9d716ae)
  Группа показывала выведенное из правил тем значение и ничего не сохраняла,
  поэтому выбор автора жил до перезагрузки страницы. Теперь она правит ту
  самую колонку, по которой считается вердикт.

- **feat**(scorm): пакет судит попытку той же политикой, что и веб (2026-08-12) [`ffbce79f9ccda0101ffebb7c69ae1adf60c637a8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ffbce79f9ccda0101ffebb7c69ae1adf60c637a8)
  Рантайм считал вердикт своим правилом, поэтому один и тот же тест мог
  разойтись в оценке между LMS и веб-хостом. Признак обязательности темы и
  политика едут в пакет, эталонные предпросмотры пересобраны.

- **feat**(api): политика вердикта сохраняется, применяется и переносится (2026-08-12) [`45d4238a35d96b9e0306e0feba488c4a6147d39b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/45d4238a35d96b9e0306e0feba488c4a6147d39b)
  Маршрут теста принимает настройку, подсчёт попытки её применяет, а перенос
  между инсталляциями показывает её в разнице — иначе настройка молча
  терялась бы при переносе, как это уже было с обликом итогов.

- **feat**(scoring): вердикт теста считается по выбранной политике (2026-08-12) [`e16448bf34b9c018ba3571d508f28a50ba1435c9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e16448bf34b9c018ba3571d508f28a50ba1435c9)
  Общий порог и гейты тем сводились в вердикт одним зашитым правилом, из-за
  чего «зачёт по обязательным темам» и «зачёт по всем темам» выразить было
  нечем. Политика приходит одним значением и решает, что именно проверяется.
  
  Отсутствие политики сохраняет прежнее правило: пакет и тест, собранные до
  этой правки, судятся ровно как раньше.

- **feat**(db): колонка pass_decision_policy и засев по существующим тестам (2026-08-12) [`26c8b4bf84d75a6262b987b029e87c818a4e7997`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/26c8b4bf84d75a6262b987b029e87c818a4e7997)
  Настройка «Тест пройден, если» не хранилась нигде: редактор показывал её,
  выводя из правил тем, поэтому автор не мог выбрать сочетание, которого
  темы не подразумевают.
  
  Засев выводит значение из текущих правил (есть собственное правило темы —
  overall_and_required_topics, иначе overall_only), чтобы вердикт ни одного
  существующего теста не изменился под автором.

- **feat**(prd-49): тумблеры слотов шкалы доезжают и до пакета (2026-08-12) [`3169912939ae1534dbc991a67c0975e3027ba308`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3169912939ae1534dbc991a67c0975e3027ba308)
  Выпечка переносила у шкалы только разобранную интерпретацию, поэтому в LMS
  тумблер шкалы молчал, хотя в вебе работал. В пакет кладутся разрешённые
  булевы значения, а не сырой config_json: авторским настройкам в выдаче
  делать нечего.

- **feat**(prd-49): надписи отчёта доведены до хостов и до «Сертификации» (2026-08-12) [`fa21fd6836b5988acf7e5e336f6737e3c9503f26`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fa21fd6836b5988acf7e5e336f6737e3c9503f26)
  Слой отчёта разрешался, но никто его не заполнял: PDF выходил вообще без
  заголовков разделов. Теперь слои собирает сборщик пакета и маршрут
  результата, а рантайм и клиент только передают готовую карту.
  
  Макеты отчёта «Сертификации» переведены на надписи — паритет шаблонов.
  
  Тесты приведены к новым умолчаниям: документ печатает «Результаты по
  темам» и «Ваш результат» по объявленным умолчаниям отчёта, а адаптивный
  экран — «По темам» и «По показателям».

- **feat**(prd-49): тумблеры слотов карточки доезжают до экрана ученика (2026-08-12) [`8709d8b3404d5c625c65d703a8816f424e9a6008`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8709d8b3404d5c625c65d703a8816f424e9a6008)
  Редактор писал showName/showLevel в config_json, ядро их понимало, но
  между ними не было никого: настройка автора ни на что не влияла.
  
  Читаются как «!== false»: отсутствие ключа означает показывать, любое
  другое прочтение погасило бы слоты у всех уже существующих тестов.

- **feat**(prd-49): надписи разрешаются по экранам на обоих хостах (2026-08-12) [`314d99d86d1c537fc51580f84e76ffe73b9ddfe5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/314d99d86d1c537fc51580f84e76ffe73b9ddfe5)
  В пакет уезжает карта на КАЖДЫЙ экран, а не одна на всех: умолчание экрана
  объявлено в манифесте, и одна карта печатала бы на адаптивных итогах
  формулировку экрана итогов. Рантайм различает старую плоскую карту и новую
  по типу значения, поэтому уже собранные пакеты читаются как прежде.
  
  Итоги раздела в вебе получили надписи тем же путём, что экран итогов, —
  от выданной версии теста.
  
  Отчёт: reportSettingsSchema принимает свой слой переопределений, иначе он
  молча терялся при сохранении. Двум надписям объявлено умолчание отчёта,
  чтобы прежние слова документа («Ваш результат», «Результаты по темам»)
  не поехали вслед за новой иерархией экрана.

- **feat**(prd-49): отчёт печатает надписи и получил свой слой переопределений (2026-08-12) [`798248cd7a9f4b89490a58f3cf5bc7235f1d63f7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/798248cd7a9f4b89490a58f3cf5bc7235f1d63f7)
  Слой отчёта разрешается там же, где выбирается вариант отчёта: только эта
  сторона видит и манифест, и настройки теста, а в LMS манифеста нет.
  
  Порядок разделов отчёта осознанно оставлен фиксированным: состав документа
  шире экранного (вводный блок, вердикт, карточки курсов и мероприятий по
  невзятым темам), показатели печатаются карточкой на каждый ради разреза
  страниц A4, а темы стоят вторыми — перевод на подблоки экрана молча
  переставил бы разделы во всех выдаваемых PDF.

- **feat**(prd-49): панель «Итоги» и тумблеры слотов в редакторе (2026-08-12) [`130e305a59a7769aa1a4495122b3ed9bbb4aa114`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/130e305a59a7769aa1a4495122b3ed9bbb4aa114)
  Вкладка «Оформление» получила панель с заголовками, подписями и порядком
  подблоков. Панель показывается только у шаблона, объявившего надписи:
  шаблон без объявлений печатает свои строки, и настраивать в нём нечего.
  
  Три состояния хранятся явно: ключа нет — текст шаблона, {on:true,text} —
  своя формулировка, {on:false} — надписи нет. Очистка поля удаляет ключ,
  и тест снова следует за шаблоном.
  
  Тумблеры слотов карточки — общий компонент для шкалы и показателя, с
  подсказкой, что гасится показ, а тексты остаются в данных.

- **feat**(prd-49): адаптивные итоги и итоги раздела пакета получают надписи (2026-08-12) [`7eefc39b83e2dd669d7099b8220de2436a9391d1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7eefc39b83e2dd669d7099b8220de2436a9391d1)
  Рантайм отдавал надписи только экрану итогов, поэтому в LMS два соседних
  экрана оставались с пустыми заголовками, пока веб их уже печатал.
  
  Адаптивный экран берёт СВОЙ список подблоков: список экрана итогов принёс
  бы ему сводку баллов, которой там никогда не было. Итогам раздела порядок
  не нужен — подблоков у них нет, им нужны только надписи.

- **feat**(prd-49): слоты карточки выключаются и в «Сертификации» (2026-08-12) [`8e6475ffdc3fc765dc4a6be6628f5397d40a1a52`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8e6475ffdc3fc765dc4a6be6628f5397d40a1a52)
  Настройка должна вести себя одинаково в обоих шаблонах: до этой правки
  выключенное название карточки исчезало на стандартном шаблоне и печаталось
  на «Сертификации».

- **feat**(prd-49): шаблон «Сертификация» печатает надписи итогов (2026-08-12) [`f7f5b1983f2cb0d144e7c2a46e0797c75979a2e4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f7f5b1983f2cb0d144e7c2a46e0797c75979a2e4)
  Манифест объявил те же 15 надписей и СВОЙ порядок подблоков: у этого
  шаблона показатели идут перед шкалами, а на адаптивном экране сводки нет.
  Объявление снято с фактических макетов, а не скопировано у стандартного —
  иначе вид уже собранных тестов переставился бы молча.
  
  Макеты итогов, адаптивных итогов и итогов раздела переведены на зонтик и
  цикл по подблокам. Отчёт не тронут: у стандартного шаблона он ещё на
  жёстких строках, паритет держится в обе стороны.

- **feat**(prd-49): слоты карточки измерения выключаются независимо (2026-08-12) [`825578b647af250d17673870fbfe988ae5889404`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/825578b647af250d17673870fbfe988ae5889404)
  Название, уровень и пояснение карточки перестали быть одним куском: банер
  печатается, если есть метка ИЛИ пояснение, поэтому «текст без заголовка»
  стал выразим, а очистка метки больше не уносит текст молча.
  
  Признаки инвертированы (hideName/hideLevel/hideBanner), как hideScoreSummary:
  отсутствие ключа обязано означать «показывать», иначе контекст, собранный
  не через buildMeasureView, теряет слот молча — на этом и поймал тест макета.
  
  Метка уровня остаётся в данных: у числовой зоны пустая метка печатает КОД
  уровня, поэтому гасится показ, а не содержимое.

- **feat**(prd-49): надписи итогов доезжают до веб-хоста и пакета SCORM (2026-08-12) [`49567098c96c01e8c4d1e03fc7ccc35a764573d2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/49567098c96c01e8c4d1e03fc7ccc35a764573d2)
  Веб разрешает надписи выданной версии теста: объявления берутся из манифеста
  активного шаблона, значения — из design_settings_json, порядок подблоков —
  по экранам. Заголовки положены и контрольному тесту без измерений, поэтому
  разрешение стоит вне гейта измерений.
  
  В пакет едет УЖЕ РАЗРЕШЁННАЯ плоская карта: манифеста в LMS нет, и второго
  источника умолчаний у рантайма быть не может. Дерево строит только ядро —
  рантайм передаёт карту как есть.
  
  Порядок разрешается по экранам обязательно: у адаптивных итогов свой состав
  без сводки баллов.

- **feat**(prd-49): адаптивные итоги и итоги раздела печатают надписи из контекста (2026-08-12) [`a90f347aed71579da995abd4f0397f2c6b3ee1a6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a90f347aed71579da995abd4f0397f2c6b3ee1a6)
  Адаптивный экран переведён на зонтик и цикл по result.blocks, как обычные
  итоги. Ветки isSummary у него нет: сводки баллов на этом экране никогда не
  было, и манифест объявляет его состав без неё.
  
  Итоги раздела получили надпись section.eyebrow и подписи чисел из labels.
  
  ГОЧА: токенайзер DSL разбирает {{ }} даже внутри HTML-комментариев, поэтому
  пример синтаксиса в комментарии макета ломал разбор.

- **feat**(prd-49): макет итогов печатает зонтик и подблоки из контекста (2026-08-12) [`68ecf44d4a58444bf1e4b3312866396898069432`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/68ecf44d4a58444bf1e4b3312866396898069432)
  Стандартный экран итогов больше не решает сам, какие блоки показать и как их
  назвать: он печатает надпись первого уровня из labels.results.heading и идёт
  циклом по result.blocks — состав и порядок отобрало ядро. Внутренности
  подблоков перенесены дословно, изменены только внешние пути (@root.) и
  подписи чисел, строк карточки темы и групп рекомендаций.
  
  Разделители: вместо трёх условных &lt;hr&gt; с разными гейтами один
  {{#unless @first}} внутри цикла. Рекомендации остались за циклом — это
  раздел первого уровня, а не подблок.
  
  Фикстуры соседних тестов приведены к новому контракту: контекст итогов
  несёт blocks и labels. В SCORM-тесте вложений группа «Материалы» опознаётся
  по её месту в блоке — надписи в пакет ещё не проведены (задача 10).

- **feat**(prd-49): надписи на адаптивных итогах и итогах раздела (2026-08-12) [`192436b11e7a480ef4a0b0a5bec9bf519a510309`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/192436b11e7a480ef4a0b0a5bec9bf519a510309)
  Сборка подблоков и дерева надписей вынесена в общую функцию
  attachBlocksAndLabels — оба билдера (стандартный и адаптивный экраны)
  зовут её, а не держат по копии правил видимости. Адаптивный экран
  всегда передаёт hasSummary=false, поэтому сводка баллов не может
  появиться, даже если сохранённый автором порядок подблоков её просит.
  buildSectionResultContext получил опцию labels — подблоков у итогов
  раздела нет, поэтому билдер собирает только дерево надписей.

- **feat**(prd-49): контекст итогов несёт подблоки и надписи (2026-08-12) [`c58095f351e266c5406015f15c0651fb1e64500f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c58095f351e266c5406015f15c0651fb1e64500f)
  Контекст экрана итогов получает `result.blocks` — готовый упорядоченный массив
  видимых подблоков зонтика («Общий балл», «По шкалам», «По показателям», «По темам»),
  каждый со своей надписью. Порядок выражен ДАННЫМИ, а не разметкой: печатный отчёт
  режется по реальному порядку DOM, поэтому перестановка средствами CSS увела бы разрез.
  
  Разрешённые надписи едут деревом в `labels` — плоские ключи с точками недоступны
  DSL-пути. Без `opts.labels` контекст прежний: ключа `labels` нет вовсе.
  
  Видимость сводки читается по инвертированному `hideScoreSummary` — тому же признаку,
  по которому её печатает макет: контрольный тест без измерений до переключателя не
  доходит и сводку показывает всегда.

- **feat**(prd-49): дизайн-настройки принимают надписи и порядок подблоков итогов (2026-08-12) [`09933db0a9ba69b490646dd87240124cdce8cecd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/09933db0a9ba69b490646dd87240124cdce8cecd)
  designSettingsSchema теряла labels и resultsBlockOrder — незаявленные ключи
  zod-схема молча отбрасывает при parse. Добавлены оба поля с типами из
  shared/template/labels и shared/template/results-order.
  
  Второе место потери — маршрут PUT /api/tests/:id/design: он пересобирает
  designSettings по явному списку полей, labels/resultsBlockOrder в список не
  входили и терялись бы при сохранении. Добавлены разбор, валидация через
  designSettingsSchema.shape и запись в сохраняемый объект — по тому же
  принципу «пишем, только если несёт смысл», что уже применён к theme/paramsByTheme.

- **feat**(prd-49): шаблон объявляет надписи блоков итогов в манифесте (2026-08-12) [`5ca7f6f55033e992b0ea320c7300feb41bed8ddf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5ca7f6f55033e992b0ea320c7300feb41bed8ddf)
  manifest.json стандартного шаблона получает раздел `labels[]` (15 ключей
  с текстом по умолчанию) и `resultsBlockOrder`; template-validation.ts
  проверяет корректность объявления (обязательный default, отсутствие
  дублей ключей) как блокирующую ошибку активации шаблона.

- **feat**(prd-49): порядок подблоков итогов (2026-08-12) [`f3c3897594478f18b86fd6088ee184fdc0b01870`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f3c3897594478f18b86fd6088ee184fdc0b01870)

- **feat**(prd-49): резолвер надписей экрана итогов (2026-08-12) [`57e7983eb0bf022c3f222fe0cc1597b46a711792`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/57e7983eb0bf022c3f222fe0cc1597b46a711792)

- **feat**(workbook): лист «Исходы показателей» (2026-08-12) [`3e2698f7be63e71c5cc525973290c6ebf1c9fd26`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3e2698f7be63e71c5cc525973290c6ebf1c9fd26)
  Тексты, которые ученик читает на итогах, живут в config_json.outcomes и не имели
  колонки нигде. Тест, перенесённый книгой, приезжал печатая сырой ключ шкалы
  вместо названия стиля — молча, без единой ошибки импорта. Теперь книга их несёт:
  одна строка = один исход, потому что исход несёт абзац текста, а грамматика,
  упакованная в одну ячейку, руками не читается и не правится.
  
  Правило то же, что у «Шкал»: колонка есть — книга задаёт поле целиком, колонки
  нет — не трогает. Пустая ячейка «Текст» ОЧИЩАЕТ текст: это единственный способ
  его убрать. Различить «колонки нет» и «ячейка пуста» по объекту строки нельзя —
  sheetToObjects выбрасывает пустые ячейки, поэтому наличие колонки читается из
  заголовков, как это уже делает импорт вопросов.
  
  Обратная связь исхода (вложения, ссылки) в книгу не идёт и переносится из
  сохранённого исхода с тем же кодом: книга не должна становиться причиной
  исчезновения вложения. Лист меняет исходы только у названных в нём показателей.

- **feat**(transfer): форма выборочного импорта (2026-08-12) [`ff8f0337687a42a9a60cac5c078096dcd290288e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ff8f0337687a42a9a60cac5c078096dcd290288e)
  Три шага по эскизу wf-transfer-import: выбор файла, выбор частей и режимов,
  отчёт. На втором шаге видно всё, ради чего шаги и заведены: состав пакета по
  пяти частям, состояние каждой темы, плитки «будет создано / обновлено /
  удалено» и — до всякой записи — перечень удаляемого поимённо.
  
  План пересчитывается на КАЖДОЕ изменение опции, а пока пересчёт идёт, кнопка
  «Импортировать» недоступна: применить план, которого автор не видел, — ровно та
  беда, ради которой шаги и разделены. Ответы гонок отсекаются номером запуска —
  приземляется только последний.
  
  Тема без прав управления открывается сразу в единственно доступном варианте
  «создать новую тему»: сервер откажет в любом другом, и предлагать заведомо
  запрещённое нечестно.
  
  В дизайн-систему добавлены две недостающие мелочи вместо подделки классами:
  WizardSteps умеет рисовать только полосу шагов (navOnly) для мастеров, чьи
  панели живут снаружи, а KPI-карточка принимает variant, как обычная карточка.
  
  Приёмка в браузере на dev: снимки трёх шагов сверены с эскизом, план дал
  0/86/0, применение прошло, в меню теста появилась выгрузка пакета.

- **feat**(transfer): роуты трёхшагового импорта (2026-08-12) [`dcc44eadb23184f65fde7af546788d5b3d8fb127`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dcc44eadb23184f65fde7af546788d5b3d8fb127)
  inspect (разбор без записи, пакет уходит в хранилище по одноразовому токену),
  plan (что сделали бы выбранные опции) и apply (исполнение). Умолчания опций —
  безопасные: upsert везде, где режим вообще предлагается.
  
  Ответы: 422 на нечитаемый пакет и чужую версию формата, 403 на политику темы без
  прав (с указанием темы), 404 на неизвестный токен и 410 на истёкший — разница
  важна, потому что во втором случае автору надо просто загрузить файл заново.
  После применения сессия удаляется: свою работу она сделала, а держать байты в
  памяти дольше незачем.
  
  Проверено живым HTTP на dev (порт 8097, тест-копия от прошлой приёмки): разбор
  14 вопросов и 4 шкал, план на самого себя дал 86 обновлений и НОЛЬ удалений,
  применение прошло, повторная выгрузка совпала с исходной байт в байт.

- **feat**(transfer): применение плана одной транзакцией (2026-08-12) [`a7175f089061944769c31e4030dfca0a2e9131e9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a7175f089061944769c31e4030dfca0a2e9131e9)
  applyTransfer исполняет выборочный импорт в порядке, который и есть суть:
  сначала права, потом медиа, потом решения, потом перенумерация ссылок, и всё
  записывается одной транзакцией.
  
  План ПЕРЕСЧИТЫВАЕТСЯ на сервере из пакета и заново прочитанного приёмника, а не
  берётся у клиента: увиденное автором — основание его согласия, а не указание
  серверу, и между показом плана и нажатием кнопки приёмник мог измениться.
  
  Права на тему проверяются ДО первой записи: политика, отличная от «создать новую
  тему», трогает существующую тему, и отказ обязан прийти раньше любого изменения.
  Спрятать вариант в форме проверкой не считается.
  
  Обновление пишет содержимое пакета в строку приёмника, но не присваивает её:
  владелец, папка и аудит авторства остаются местными. Имя созданной темы берётся
  из плана — того самого, который показали автору.
  
  Снимок приёмника собирает отдельный модуль: решения остаются чистыми, а всё, что
  знает про базу, живёт в одном месте. История ответов читается ключами
  answers_json — таблицы ответов у веб-попыток нет.

- **feat**(transfer): ядро решений по частям и режимам (2026-08-12) [`5d6de83b12e6d9513153923d12b7d9426ba35941`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5d6de83b12e6d9513153923d12b7d9426ba35941)
  diffTransfer — чистая функция (пакет, снимок приёмника, опции) -&gt; список операций
  create/update/delete. Опасная половина импорта — удаление, а правило, которое
  нельзя прогнать без базы, никто и не прогоняет: здесь каждый случай формы описан
  строкой таблицы, включая оба режима и все три политики темы.
  
  Два сквозных правила. Удаление возникает только там, где режим действительно
  способен стереть: replace части или полная замена вопросов темы; итоги,
  оформление и медиа не удаляют никогда, потому и предлагаются без режима.
  Сопоставление идёт по идентификатору, а запасным ходом — по устойчивой ручке
  автора: шкала по key, показатель по name, страница по kind. Текст вопроса ручкой
  не считается никогда.
  
  Строка теста — единственное место, где перечень полей неизбежен: в неё пишут три
  части сразу. Названы ровно две выемки (правило прохождения с ценой по умолчанию;
  вводный блок, оформление и настройки отчёта), всё остальное едет по умолчанию,
  поэтому забыть новую колонку нельзя.
  
  Правило свободного имени темы переехало в shared/topics/naming: план ПОКАЗЫВАЕТ
  имя, а писатель его СОЗДАЁТ, и второе, разъезжающееся правило сделало бы план
  лживым.

- **feat**(transfer): импорт пакета с сохранением исходных идентификаторов (2026-08-12) [`9af1dfa48f4eb530d9b0f09166dd887cbb26ac90`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9af1dfa48f4eb530d9b0f09166dd887cbb26ac90)
  Три прохода: медиа (адреса нужны до перенумерации), перенумерация, запись одной
  транзакцией — полуимпортированный тест хуже отсутствующего, потому что ничто в
  нём не сообщает, что он половинчатый.
  
  Идентификаторы сохраняются: id сам себе свидетельство происхождения, поэтому
  повторный импорт узнает строки, которые создал в прошлый раз, без колонки
  провенанса и без догадок по тексту. Сопоставление по содержанию измерено и
  отвергнуто: content_hash есть у 990 вопросов из 1334 и даёт лишь 812 различных
  значений — оно спарило бы разные вопросы и разорвало бы пары одинаковых. Новый
  идентификатор выдаётся только при настоящем столкновении и попадает в отчёт.
  
  Приёмка — переносом в ЧИСТУЮ базу на pglite: круг «выгрузил-загрузил» на одной
  базе проходит, даже если пакет не несёт ничего, и именно он пропустил исходный
  дефект.
  
  Права на выгрузку — tests.read, как у книги: пакет не раскрывает большего, а
  скоуп SCORM отрезал бы автора от переноса собственного теста.

- **feat**(transfer): разбор пакета без записи (2026-08-12) [`359e6d28e594bef404b65a86def9edb9022cb0c1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/359e6d28e594bef404b65a86def9edb9022cb0c1)
  Шаг inspect: пакет читается, не записывая ничего, и автору показывается опись по
  пяти частям, состояние каждой темы и медиа, которое источник не смог разрешить.
  Выбор нельзя делать вслепую: доступная политика зависит от того, что у приёмника
  уже есть — темы нет (создать), тема есть и управляема (все три политики), тема
  есть и чужая (только новая тема).
  
  Права темы разрешаются штатным canManageTopicContent через порт, а не выдумываются
  заново: форма не должна предлагать того, в чём apply откажет.
  
  Хранилище пакета между шагами повторяет форму хранилища отладочного плеера —
  память, TTL 30 минут, LRU 50, владелец. План и применение обязаны идти от одних
  и тех же байтов, поэтому файл не перезагружается на каждом шаге.

- **feat**(transfer): формат .tbtest и выгрузка теста целиком (2026-08-12) [`a9ebfeb27ff789d1cd8bb0e1db56fc64749ce9e4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a9ebfeb27ff789d1cd8bb0e1db56fc64749ce9e4)
  Пакет — zip с манифестом test.json и медиа по контрольной сумме. Полнота по
  построению: содержимое сериализуется строками таблиц (основа —
  TestSnapshotContent, уже собирающий тест целиком для публикации), а не перечнем
  полей, поэтому новая колонка едет сама. Перечень остался в одном месте — списке
  таблиц теста, и его стережёт гард полноты.
  
  Перенумерация вынесена в чистую функцию: она подменяет строки развёрткой всего
  дерева, включая ключи объектов, а не по списку полей-ссылок — забытое поле
  означало бы успешный импорт и молча сломанный тест. Ключи (шкал, тем) не
  трогаются: на них ссылаются формулы показателей, ради которых перенос и затеян.
  
  Адрес медиа в манифесте хранится вербатим, в отличие от упаковщика SCORM:
  импорту нужно исходное написание, чтобы найти вхождения и переписать их.

- **feat**(transfer): скрипт расшивки настроек теста между базами (2026-08-12) [`2844aa8093d7436a9dce8f3ffde924f57eac4f45`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2844aa8093d7436a9dce8f3ffde924f57eac4f45)
  Временная мера для теста, уже уехавшего в прод книгой: переносит между двумя
  инсталляциями ровно то, что книга теряет — config_json показателей, intro_json,
  design_settings_json, report_settings_json, overall_pass_rule_json и контентные
  страницы. Сопоставление по ключам: показатели по name, страницы по kind, потому
  что sort_order после импорта книгой у всех страниц нулевой и идентичностью не
  является. Сухой прогон по умолчанию, запись только с --apply.
  
  Удаляется, когда штатный импорт научится обновлять существующий тест.

- **feat**(home): все руководства скачиваются из блока «Материалы» (2026-08-12) [`df2bb2037cf12f688b57cfe34a4c76903426a778`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/df2bb2037cf12f688b57cfe34a4c76903426a778)
  Руководства были разбросаны: гайд по шаблонам отдавался из «Шаблонов»,
  гайд по импорту — с «Импорта», а руководство автора не отдавалось ниоткуда.
  Блок «Материалы» и был задуман как полка «чтобы не помнить, где лежит
  руководство», но гейтился правом на управление шаблонами — автор его вообще
  не видел.
  
  Теперь реестр документов один (server/services/doc-downloads): рядом с
  именем файла лежит требуемое право, поэтому список на главной и роут
  скачивания не могут разойтись в том, кому что доступно. Право проверяется
  НА ДОКУМЕНТ, а не на роут: автору доступны руководства автора и по импорту,
  на спецификацию шаблонов он получит 403. Секция показывается всякому, у
  кого есть хотя бы один документ; список активных шаблонов внутри остался
  только у того, кто ими управляет — «Активных шаблонов нет» автору было бы
  утверждением о том, чего он не видит.
  
  Прежние секционные роуты сохранили URL (кнопки в «Шаблонах» и «Импорте») и
  читают тот же реестр вместо собственных копий имён файлов. resolveDocPath
  вызывается из роутов, а не изнутри отправки: внутримодульный вызов нечем
  подменить в тестах, да и решение о коде ответа принадлежит роуту.

- **feat**(results): шкалы печатаются перед показателями на итогах и в отчёте (2026-08-12) [`06ccc1070a135a267c1a467fe5f46a7213dff8e0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/06ccc1070a135a267c1a467fe5f46a7213dff8e0)
  Шкала — измерение, показатель — вывод из измерений, и вывод читается после
  того, из чего он сделан. Порядок переставлен во всех четырёх раскладках
  стандартного шаблона (обычные и адаптивные итоги, обычный и адаптивный
  отчёт), поэтому оба хоста — веб и SCORM — меняются одной правкой.
  
  На экране итогов разделитель ставит только НЕ первый блок, поэтому гейт
  «нет сводки баллов» переехал с показателей на шкалы, а показателям
  добавлено второе условие: после блока шкал черта нужна всегда, без шкал
  они сами становятся первым блоком. Заодно закрыта старая дырка —
  методика без сводки баллов и без показателей рисовала шкалам ведущую
  черту в пустоту.
  
  Сертификационный шаблон не тронут: паритет между шаблонами держится
  вручную, и там порядок прежний.

### Fixes

- **fix**(prd-28): снимок миграции 0018 пересобран поверх ствола (2026-08-13) [`749feca70c308810abe751f640e0aa67bb4d6151`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/749feca70c308810abe751f640e0aa67bb4d6151)
  Снимок был отпочкован от 0015 и не знал про колонки политики «Тест
  пройден, если». Теперь drizzle-kit generate отвечает «No changes».
  Текст самой миграции не изменился: он уже применён к dev-базе, и
  drizzle сверяет файл по хешу.

- **fix**(scorm): распределение и баллы тем доезжают в отчёт LMS (2026-08-13) [`0f1213ac72e40d6476bd837d682548d6e7647c6c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0f1213ac72e40d6476bd837d682548d6e7647c6c)
  Формат ответа вопроса-распределения (PRD-44 FR-54) лежал в теле
  `finishScorm`, которую пакет не вызывает: отправка идёт через
  `finishScormLmsOnly`, а её собственная копия `formatResponse` типа
  `allocation` не знала и возвращала пустую строку — WebTutor показывал
  каждый ответ опросника ЧИЛ пустым. Тест этого не видел: он извлекал
  функцию по имени и находил ПЕРВУЮ копию, то есть мёртвую.
  
  Сборка взаимодействия сведена в одну `buildQuestionInteraction` поверх
  единственных копий `mapScormType`/`formatResponse`/`getCorrectAnswerFor`,
  мёртвая `finishScorm` удалена. Заодно выправился адаптивный путь: он
  собирал взаимодействия третьей копией цикла и отправлял любой вопрос как
  `other` с ответом в виде JSON и без эталона.
  
  Цель темы (`cmi.objectives.n.*`) несла процент в `score.raw` без шкалы —
  число, на которое отчёт не может ответить «из скольких», — и не несла
  имени темы: раздел опознавался только по UUID. Теперь цель собирает общая
  `buildTopicObjective`: баллы с `min`/`max`/`scaled`, описание с именем
  темы, `unknown` там, где вердикта нет. У темы без оцениваемых вопросов
  блок баллов не пишется вовсе — ноль из нуля читается как «ничего не
  набрал», хотя оценивать было нечего. Запись попытки, сохранённая старой
  версией пакета, несёт только процент и деградирует к процентной шкале,
  чтобы восстановленная лучшая попытка не потеряла результат.
  
  Тесты проверяют не только поведение, но и что копия каждой функции одна,
  а зовут её оба пути отправки: расхождение копий здесь случалось дважды.

- **fix**(report): маркеры авторских списков и текст растра на своих местах (2026-08-13) [`0d65caeb18e8154177a170afc20e9d941957fca6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0d65caeb18e8154177a170afc20e9d941957fca6)
  Отчёт печатается в документе ПРИЛОЖЕНИЯ, и глобальный сброс веб-хоста
  (`client/src/styles/preflight.css`, адаптация Tailwind preflight) достаёт его
  дважды. Оба дефекта видел слушатель, оба выглядели независимыми.
  
  СПИСКИ. `ol, ul { list-style: none }` плюс `* { padding: 0 }` гасили маркеры;
  отступ оставался, потому что его `report.css` задавал сам. Автор писал
  маркированный список, видел его в редакторе и в предпросмотре, а в PDF
  приезжали одни отступы — ни точек, ни номеров. Браузерных умолчаний здесь
  ждать нельзя ни в одном хосте (§6.3), поэтому маркер и отступ объявлены сами,
  для всей авторской разметки внутри `.tb-report` — вводного блока, обратной
  связи, толкования уровня. Паритет шаблонов закрыт гардом.
  
  ТЕКСТ СНИМКА. html2canvas ищет базовую линию шрифта скрытой пробой: строка из
  `&lt;span&gt;` и картинки 1×1 с `vertical-align: baseline`, подъём шрифта равен
  `img.offsetTop - span.offsetTop`. Сброс объявляет `img { display: block }`,
  картинка выпадает из строки, «подъём» вырастает на пол-строки — и весь текст
  снимка съезжает вниз примерно на 0.5em, тогда как карточки, фон и рамки
  остаются на месте. Замер: строка, лежащая в DOM на 685.7..701.7, печаталась
  чернилами на 700..708.
  
  Для постраничного отчёта это не косметика: разрывы считаются по ОТРИСОВАННЫМ
  строкам, а на бумагу их переносит растеризатор. Съехавший текст пересекал
  границу окна страницы — нижняя строка листа резалась пополам, и её вторая
  половина всплывала вверху следующего. Раскладка при этом была верна: в DOM ни
  одна строка границу окна не пересекала.
  
  Заплатка адресована ровно этой пробе — по однопиксельному GIF, зашитому в код
  html2canvas, — и уезжает вместе с контейнером отчёта. Проба живёт в ГЛАВНОМ
  документе, а не в клоне, поэтому `onclone` её не достаёт.
  
  Пакет SCORM не задет: в его документе такого сброса нет. Правки всё равно
  общие — отчёт не вправе зависеть от хоста.

- **fix**(prd-28): срок ссылок в выгрузке и русский текст отказа по потолку (2026-08-13) [`5c68ab1272a1e5a7811cdc63cedbbd298664eb55`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5c68ab1272a1e5a7811cdc63cedbbd298664eb55)
  Отчёт прогона несёт фактический срок выпущенных ссылок (linksExpireAt),
  и книга со ссылками печатает его. Раньше клиент подставлял то, что оператор
  ввёл на форме, и при пустом поле колонка «Действует до» была пуста во всех
  строках, хотя срок у ссылок есть — умолчание +30 дней ставит сервер.
  
  Отказы разбора книги (потолок строк, пустой файл) уходят оператору
  по-русски, с числом из настройки и машиночитаемым code рядом; сообщения
  сервиса остаются английскими и живут в журнале.

- **fix**(prd-28): отказ по занятому имени группы опознаётся по коду (2026-08-13) [`e32be7be81f653cf5393cdf33819ddf2a30ecf79`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e32be7be81f653cf5393cdf33819ddf2a30ecf79)
  Экран сверял русскую фразу из тела ответа. Теперь роут отдаёт рядом
  машиночитаемый code, а фраза остаётся человеку и может меняться.

- **fix**(prd-28): поле имени группы остаётся на предпросмотре при переименовании (2026-08-13) [`f686f98fc380fa5560b58eff9cf9b05e0cad71e6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f686f98fc380fa5560b58eff9cf9b05e0cad71e6)

- **fix**(auth): убран роут проверки существования адреса (2026-08-13) [`4350d6fdac533bd86a48ad06680a7848aae44249`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4350d6fdac533bd86a48ad06680a7848aae44249)
  `POST /api/auth/check-email` отвечал `{exists}` любому анонимному
  запросу без ограничения частоты — готовый перебор учётных записей,
  сводивший на нет нейтральный ответ `forgot-password` рядом.
  
  Экран восстановления пароля больше не раскрывает существование
  учётной записи: он не опрашивает адрес ни при загрузке, ни по уходу
  из поля, а после отправки всегда показывает один и тот же нейтральный
  ответ «Если аккаунт с таким email существует…» — ветки «такого адреса
  нет» и подстановки маскированного адреса убраны.
  
  Из ответа `forgot-password` убрана подсказка `hint` с маскированным
  адресом: поле присутствовало только для существующей учётки и потому
  раскрывало её существование ровно так же, как удалённый роут.
  Потребителей у поля не было. Маскированный адрес остаётся там, где
  владение почтой уже доказано, — в ответе `verify-reset-token`.

- **fix**(prd-28): осадок частичного отказа виден в отчёте, ошибки конвейера типизированы (2026-08-13) [`bbabc967a4e4fe42f04d786454f62feaab7e9752`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bbabc967a4e4fe42f04d786454f62feaab7e9752)

- **fix**(prd-28): ссылка не течёт в журнал и у участника остаётся одна живая ссылка (2026-08-13) [`74694fdb0ada87297926e6c761d330b77f2a09f8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/74694fdb0ada87297926e6c761d330b77f2a09f8)

- **fix**(prd-28): перевод в штатные не рвётся посередине, выпуск приглашения в одном хелпере (2026-08-13) [`80f5fee208fc1b49fc460ab0a2f6b96cc501c4d9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/80f5fee208fc1b49fc460ab0a2f6b96cc501c4d9)

- **fix**(prd-28): признак внешнего читается строго как true (2026-08-13) [`6597d701a775e664d999645a9d090ef771cabf54`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6597d701a775e664d999645a9d090ef771cabf54)
  Строка "false" в теле запроса истинна, и чтение по truthiness завело бы
  беспарольную учётку там, где просили обычную.

- **fix**(prd-28): фикстуры insert проверяются типом через satisfies (2026-08-13) [`abda8b868b6a9ddfdbf9d3c59a952e44265e5e54`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/abda8b868b6a9ddfdbf9d3c59a952e44265e5e54)

- **fix**(prd-28): тест сторожит выравнивание времени, комментарии приведены к фактам (2026-08-13) [`4e4b4416d5892c2927f28f79c9374dc9471d713e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e4b4416d5892c2927f28f79c9374dc9471d713e)

- **fix**(prd-49): порядок подблоков объявляет шаблон, надписи не роняют экран (2026-08-12) [`a727025e29b43439cf3295cbe14a077ae1ca396d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a727025e29b43439cf3295cbe14a077ae1ca396d)
  А. Порядок и СОСТАВ подблоков различаются по экранам: у адаптивных итогов сводки
  баллов нет вовсе, а последовательность — темы, шкалы, показатели. Константа вторым
  аргументом `resolveBlockOrder` молча переставила бы эти экраны при переходе макетов
  на цикл и дорисовала бы адаптивным сводку, которой там никогда не было.
  
  Манифест «Стандартного» объявляет порядок ПО ЭКРАНАМ (`default` + ключ экрана — тот же
  принцип, что у надписей), `templateBlockOrder()` читает объявление, `buildResultContext`
  принимает готовый список опцией `templateBlockOrder`. Состав едет тем же списком:
  `resolveBlockOrder` отбрасывает неизвестные шаблону ключи, поэтому отсутствие `summary`
  и есть «на этом экране сводки не бывает».
  
  Б. `labelsTree` больше не бросает на взаимно-префиксных ключах: он строится в контексте
  экрана итогов на обоих хостах, и сторонний шаблон уронил бы ученику выдачу — в пакете
  SCORM молча, пустым экраном. Конфликтующий ключ пропускается, остальные надписи целы.
  Кричать об этом — дело валидатора манифеста: `validateLabelDeclarations` теперь ловит
  такую пару при загрузке шаблона, называя оба ключа.
  
  В. `BLOCK_FLAG` типизирован по `CtxResultBlock`, а не по `string`: опечатка в имени
  флага — это блок, беззвучно исчезнувший с экрана. Добавлен тест на все четыре флага
  сразу (шкалы и показатели видимы одновременно).

- **fix**(prd-49): слой переопределений надписей принадлежит вызову (2026-08-12) [`aa365ea13cbfd3cd6e3f72c617feb6e511b16071`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/aa365ea13cbfd3cd6e3f72c617feb6e511b16071)

- **fix**(docs): сборка PDF ловит файл, который не был перезаписан (2026-08-12) [`ebb15e330ce409474c7f01015d2d31e7b07e9f2a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ebb15e330ce409474c7f01015d2d31e7b07e9f2a)
  Chrome выходит с нулевым кодом и тогда, когда `--print-to-pdf` не смог записать
  файл: он открыт в просмотрщике, доступен только для чтения или кончилось место.
  Проверка «файл существует» в этом случае проходит по ПРЕДЫДУЩЕЙ сборке, и прогон
  рапортует OK о документе, который не пересобирался, — так и уехало устаревшее
  руководство. Признак настоящей записи — время изменения новее, чем до запуска.
  
  Три руководства в docs/dist пересобраны.

- **fix**(transfer): раздел удаляется только при полной замене темы (2026-08-12) [`9fdf1f92b2daca165fd389a0bc52daefca971cd6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9fdf1f92b2daca165fd389a0bc52daefca971cd6)
  Решение владельца: та же логика, что везде, — upsert не удаляет, удаляет только
  явная замена. Раздел не имеет своего переключателя (спека §4: двух переключателей
  быть не должно), поэтому единственная «замена», которой он подчинён, — политика
  ЕГО темы.
  
  Раньше лишний раздел приёмника исчезал при любой взятой части «Структура и
  вопросы», даже когда все темы стояли на самом безопасном «обновить и долить», —
  единственное место, где удаление происходило без выбранного режима.
  
  Раздел темы, которой в пакете нет вовсе, теперь сохраняется: молчание пакета не
  указание удалять. Оба случая закрыты тестами, которых раньше не было.

- **fix**(transfer): ядро решений знает настоящие колонки и адаптивную часть (2026-08-12) [`7ae26abe30801f2c2968c522bca445b2d49f1fc3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7ae26abe30801f2c2968c522bca445b2d49f1fc3)
  Фикстура теста выдумывала имена колонок (text, title) — в схеме это prompt и
  label, и на настоящих строках подписи в плане вышли бы пустыми. Фикстура
  приведена к схеме, подписи читаются откуда следует.
  
  Адаптивная конфигурация (настройки тем, уровни, связи уровней) переносится
  вместе со структурой и не удаляется ни при какой политике: это конфигурация
  теста, а не содержание, которым политика темы распоряжается. Без этого
  выборочный импорт молча терял бы адаптивный тест — ровно тот дефект, ради
  которого затеян перенос.

- **fix**(editor): списки в поле форматированного текста показывают маркеры (2026-08-12) [`a6c5f2db13ad08e7203f8d83c0d17ee34cb7be6b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a6c5f2db13ad08e7203f8d83c0d17ee34cb7be6b)
  Глобальный сброс preflight.css гасит list-style у всех ul/ol, а правила
  .ou-rte__area / .tb-rte__area возвращали только отступ — автор видел сдвиг
  без точек и цифр, хотя при выдаче (Shadow DOM, куда preflight не проникает)
  список рисовался правильно. Маркеры восстановлены на уровне компонента,
  значения повторяют браузерные умолчания, включая вложенные уровни.
  Та же правка для превью обратной связи, которое показывает тот же HTML.

- **fix**(learner): последний вопрос без обзора завершает тест, а не упирается в мёртвую «Далее» (2026-08-12) [`2afb66f2c51ebb3f58d9d22c889d04b9d0398580`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2afb66f2c51ebb3f58d9d22c889d04b9d0398580)
  В гибком режиме кнопка последнего вопроса всегда «Далее»: завершение отдано экрану
  обзора (PRD-19 FR-16). Но ворота обзора прячут его, когда действовать там нечем —
  всё отвечено и либо правка запрещена, либо автор включил «когда отвечено всё, обзор
  не нужен». Тогда веб-хост только сохранял прогресс, и учащийся оставался на последнем
  вопросе с живой кнопкой, которая ничего не делает: пройденный тест невозможно сдать.
  
  Рантайм SCORM тот же конец последовательности закрывает через submit, так что это
  было расхождение хостов, а не общее правило. Веб получил такую же терминальную ветку.
  
  Карты ответов и статусов передаются в отправку явно: завершение вызывается из того
  же обработчика, который только что пометил вопрос отвеченным, а состояние React там
  ещё старое — в гибком режиме ответ последнего вопроса ушёл бы на проверку выброшенным.

- **fix**(import): нечитаемый файл — ошибка автора, а не отказ сервера (2026-08-11) [`939f500fa31fb4d5a3e1a3a324b6d127c5f1f9ab`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/939f500fa31fb4d5a3e1a3a324b6d127c5f1f9ab)
  Все маршруты, читающие загруженную книгу, роняли неудачу чтения в свой общий
  `500`: автор с испорченным или просто не тем файлом видел отказ сервера, хотя
  причина целиком в его входных данных и лечится выбором другого файла. Код
  причины был проведён только в `/inspect`, а он один из пяти.
  
  Теперь такая неудача отвечает `400` с полем `code` на всех пяти: `/inspect`,
  `import-new`, импорт в существующий тест, импорт вопросов и предпросмотр
  массовой загрузки пользователей. Последний назван не был, но зовёт тот же
  `readWorkbookFromBuffer` и страдал тем же.
  
  Перевод вынесен в один `respondWorkbookReadError` и лежит рядом с уже
  существующим переводом переполнения в `413`: оба превращают проблему уровня
  загрузки в ответ, с которым клиент может что-то сделать, и держать их врозь
  значило бы разъезжаться формой ответа. Помощник возвращает false на всё, что не
  про чтение, поэтому прежний `500` каждого маршрута остаётся на месте для
  настоящих сбоев — со своим текстом, единый я не вводил.
  
  Тост импорта на клиенте остался общим сознательно: кнопка «Импортировать»
  недоступна без успешного `/inspect`, так что нечитаемый файл до этого мутатора
  не доходит и разбор кода там был бы мёртвым.

- **fix**(import): книга с префиксными пространствами имён читается импортом (2026-08-11) [`39e49cba28839962b850b4be7d628ce7d63271af`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/39e49cba28839962b850b4be7d628ce7d63271af)
  Книга, у которой все элементы OOXML стоят на префиксе — `&lt;x:workbook&gt;` вместо
  `&lt;workbook xmlns="…"&gt;`, — не читалась вовсе: автор получал «Не удалось прочитать
  файл. Проверьте формат .xlsx» и никакого пути дальше. Файл при этом валиден,
  такие книги выдают сторонние генераторы, не Excel.
  
  Причина в exceljs: парсер создаётся как `new SaxesParser()`, без поддержки
  пространств имён, поэтому они не разрешаются нигде и каждый xform сравнивает
  сырое имя с префиксом (`'workbook'`, `'sheetData'`, `'c'`). Патчить это негде —
  сравнение по `node.name` рассыпано по десяткам xform-ов, включая горячий разбор
  ячеек, а менять библиотеку чтения значит задеть все пять маршрутов загрузки и
  экспорт заодно.
  
  Поэтому префикс снимается перед передачей пакета, и только РЕЗЕРВНЫМ путём:
  обычная загрузка идёт как раньше, распаковка с переписыванием случается лишь
  после её провала — на нормальных книгах не выполняется ни одной лишней операции.
  Префикс определяется по частям и только по привязке к spreadsheetml-main, а не
  как «префикс x:»: в `xl/theme/theme1.xml` drawingml стоит на `a:`, и снятие
  префиксов подряд сломало бы разбор темы. Переписываются только имена элементов —
  префиксные атрибуты (`r:id`) и их объявления остаются на месте.
  
  Заодно причина отказа перестала быть одной на всё. `WorkbookReadError` несёт
  `reason`, `/inspect` отдаёт его полем `code`, и автор видит, что делать: «файл не
  является книгой .xlsx» — брать другой файл, «содержимое разобрать не удалось» —
  пересохранить книгу в Excel. Раньше `onError` тело ответа не смотрел вовсе.
  
  Загрузка книги получила потолок 25 МБ с ответом 413: весь файл лежит в памяти, а
  при нормализации — дважды. Лимит переведён в ответ явно, потому что multer
  сообщает о переполнении через `next(err)` мимо обработчика маршрута. Zip
  дизайн-шаблонов остался без лимита — это другой по размеру вид загрузки.

- **fix**(template): та же правка картинки для сертификационного шаблона (2026-08-10) [`a078b8834cc6d45c1b7f558d53c679a845587fe6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a078b8834cc6d45c1b7f558d53c679a845587fe6)
  Сертификационный шаблон держит медиа-правила своей копией, поэтому картинка
  обложки и информационного слайда выходила за поле ровно так же, как в
  стандартном (4958d203). Правила перенесены один в один, а гард на них добавлен
  в паритетный тест — расхождение копий ловится там же, где ловятся раскладки.

- **fix**(template): картинка стартового и информационного слайда не выходит за поле (2026-08-10) [`4958d2039ab3e3a2a9ff99072de13220b62b6a30`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4958d2039ab3e3a2a9ff99072de13220b62b6a30)
  Иллюстрация лежала в потоке и сама задавала высоту панели: у обложки строка
  грида `auto`, у информационной страницы высота идёт от `aspect-ratio`, и ни к
  чему из этого `height: 100%` не привязывается. Процент схлопывался в `auto`,
  картинка вставала в натуральных пропорциях, `object-fit: cover` не работал —
  портретная иллюстрация растила панель за границы поля и уезжала под подвал, а
  в рамке 4:3 обрезалась по низу. Панель позиционируется, картинка выводится из
  потока: высоту задаёт раскладка, `cover` получает коробку для заполнения.

### Documentation

- **docs**(prd-28): статус реализован, замечания приёмки в долгах (2026-08-13) [`b7a3fa8ba1c72f8c131c51ccb0b49093193c50db`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b7a3fa8ba1c72f8c131c51ccb0b49093193c50db)
  Приёмка в браузере пройдена по всем десяти критериям. Пять замечаний,
  не относящихся к охвату, добавлены к списку известных долгов.

- **docs**(prd-28): длина строки критерия приёмки (2026-08-13) [`081faa9a13b4adcc1becbf729043d55733af43e4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/081faa9a13b4adcc1becbf729043d55733af43e4)

- **docs**(prd-28): ярлык роли приведён к тому, что видит пользователь (2026-08-13) [`b860e32b9d06b787c4416da9cdc4d65fb83ce8b7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b860e32b9d06b787c4416da9cdc4d65fb83ce8b7)
  Спека называла роль «Ученик», интерфейс — «Обычный пользователь».
  Расхождение вскрылось на эскизах.

- **docs**(roadmap): долг — устаревшие подмены классов ДС в wf-utils.css (2026-08-13) [`6eae513e6825c34bd643ccc599ef0d79ec1bf39e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6eae513e6825c34bd643ccc599ef0d79ec1bf39e)
  Файл переопределяет настоящие классы дизайн-системы токенами, которых
  в ней больше нет, и подключается последним. Эскизы PRD-28 обошли это
  порядком подключения, но следующий эскиз наступит на те же грабли.

- **docs**(prd-28): эскизы вкладки приглашения участников списком (2026-08-13) [`78d1deb30c02891716adb0212b44eef968ce3cc9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/78d1deb30c02891716adb0212b44eef968ce3cc9)

- **docs**(prd-28): известные долги, выявленные при реализации (2026-08-13) [`ce889614c2a89e4e56d501e7fee976f50692f1b0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ce889614c2a89e4e56d501e7fee976f50692f1b0)
  Шесть пунктов: осиротевшие токены после выхода из группы, ссылки в
  журнале у двух других отправителей писем, оракул через 429, devLink,
  узкий случай пустой группы и устаревший пункт про check-email в плане
  архитектурных требований.

- **docs**(analytics): эскиз окна «Детализация попытки» (2026-08-13) [`ed4ee07d51394c79280e59af062fcfdf01ec5541`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ed4ee07d51394c79280e59af062fcfdf01ec5541)
  Экраны аналитики не были нарисованы никогда, поэтому дополнять окно было
  не от чего. Эскиз берёт существующее окно 1:1 и описывает дельту: две
  оценочные плитки меняют содержимое, когда оценивать нечего; появляются
  блоки «Показатели» и «По шкалам» в том же порядке, что у ученика; строка
  ответа наконец показывает сам ответ, эталон и вклад в шкалы.
  
  Согласован 2026-08-13. Итерация при согласовании: в «Показателях» убрана
  колонка «Толкование» — окно фиксирует результат, а авторский текст исхода
  на референсной методике идёт на полторы страницы и разносит таблицу.
  
  Подключает ЖИВОЙ DS и ставит его последним: снимок для эскизов отстал на
  23 КБ и не содержит примитива Box вовсе, а wf-utils красит тег «Пройден»
  токенами, которых в текущем DS нет.

- **docs**(prd-28): шаг слияния для снимка миграции и долги задачи 1 (2026-08-13) [`3f0eebfa20babf69669aa91f7da5787753a93ad1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3f0eebfa20babf69669aa91f7da5787753a93ad1)
  Снимок 0018 отпочкован от 0015, поэтому перед слиянием его надо
  перегенерировать поверх 0016/0017 соседней сессии. CHECK-ограничение
  «пароль или внешний» отклонено: оно запрещает учётку, только что
  переведённую в штатные. В задачу 2 добавлен долг по bcrypt-residual.

- **docs**(prd-28): план реализации внешнего участника (2026-08-12) [`73eff3afe712ea6a2ef2227105b63c827bfcc819`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/73eff3afe712ea6a2ef2227105b63c827bfcc819)
  17 задач в пяти этапах: схема и запреты для учётки без пароля, настройки,
  конвейер загрузки списком с отчётом и выгрузкой ссылок, интерфейс после
  эскизов, приёмка в браузере.

- **docs**(prd-28): открытые вопросы закрыты, добавлено заведение списком (2026-08-12) [`37a199062ad3ba72cc11250074b81ac66ff6c0dc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/37a199062ad3ba72cc11250074b81ac66ff6c0dc)
  Спека PRD-50 слита в PRD-28: это один сценарий. Закрыты пять открытых
  вопросов (заведение списком и поштучно, видимость наравне со штатными,
  перевод только внешний -&gt; штатный, поведение при исчерпании попыток и
  истечении ссылки оставлено как есть).
  
  Добавлены: вкладка «Списком из файла» в диалоге назначения теста с
  предпросмотром, отчётом о рассылке и выгрузкой ссылок; необязательное
  создание группы из списка; запреты входа, восстановления доступа и
  приглашения для внешней учётной записи; потолки в настройки системы.

- **docs**(prd-50): спека приглашения участников теста списком (2026-08-12) [`6ea1d6b84f1e85a8479f751453e0cec0f4ac7cef`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ea1d6b84f1e85a8479f751453e0cec0f4ac7cef)
  Сквозной сценарий: файл со списком -&gt; предпросмотр -&gt; назначение теста
  и рассылка разовых ссылок -&gt; отчёт с выгрузкой ссылок. Ограничение
  доступа переиспользует существующую область magic-link, отдельная роль
  не заводится. Потолок строк и почасовой предел писем выносятся в
  настройки системы.

- **docs**: контракт настройки вердикта и образцы отчётов (2026-08-12) [`edb2382ce1fe9cbe398b47462dfb528d4f4520a5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/edb2382ce1fe9cbe398b47462dfb528d4f4520a5)
  Описание параметра приведено к тому, что теперь хранится в базе, а не
  выводится из правил тем. В справочные материалы добавлены два печатных
  отчёта — образцы успешной и неуспешной попытки.

- **docs**(prd-49): статус спеки — реализация выполнена, приёмка впереди (2026-08-12) [`0d4a2cf18efa3f6f129db1b2b9aecb7c7ca4824d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0d4a2cf18efa3f6f129db1b2b9aecb7c7ca4824d)

- **docs**(prd-49): формат шаблона 1.5.0 и руководство автора (2026-08-12) [`3113f1d86de34e2dbea1bca88d309dacd05dbb5d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3113f1d86de34e2dbea1bca88d309dacd05dbb5d)
  Надписи интерфейса итогов объявляет шаблон разделом labels[], порядок и
  состав подблоков — resultsBlockOrder по экранам, контекст несёт дерево
  labels.* и массив result.blocks. Описаны три состояния надписи, статические
  проверки манифеста и правило тотальности сборки контекста.
  
  Номер версии 1.5.0, а не 1.4.0 из спеки PRD-49: 1.4.0 занята вводным блоком
  итогов. Номер в спеке и плане исправлен.
  
  Руководство автора получило раздел «Заголовки и подписи» и описание
  будущих тумблеров слотов карточки.

- **docs**(prd-49): план реализации настраиваемых заголовков итогов (2026-08-12) [`1a2db15d3c1239a75749d730df63dca3f2737ed7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1a2db15d3c1239a75749d730df63dca3f2737ed7)
  Шестнадцать задач с шагами TDD: резолвер надписей и порядка подблоков,
  раздел labels[] в манифесте и его проверка, подблоки в контексте, макеты
  итогов на {{#each result.blocks}}, проводка в веб, пакет SCORM и отчёт,
  слоты карточки показателя, панель «Итоги» в «Оформлении», паритет
  «Сертификации» и документация.
  
  Имя поля слота приведено к showName и в спеке: в config_json, в MeasureInput
  и в редакторах оно теперь пишется одинаково.

- **docs**(prd-49): спека настраиваемых заголовков блоков итогов (2026-08-12) [`ab7e4f23a0d38a18e8cd7e638dfd05cbef1aef00`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ab7e4f23a0d38a18e8cd7e638dfd05cbef1aef00)
  Заголовки блоков зашиты в макет; автор не может ни переформулировать их,
  ни убрать. Плюс карточка показателя вынуждает занимать подводкой два слота
  сразу, отчего на экране появляются два заголовка об одном и том же.
  
  Надпись объявляет шаблон (labels[] манифеста), значение хранит тест,
  разрешает одна функция на все хосты. Слоты карточки получают независимое
  управление показом, данные при этом не стираются.

- **docs**(prd-48): все шесть решений владельца закрыты, долгов нет (2026-08-12) [`211cb0f953ba65d3d58b3e0fe9ed9f39637835df`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/211cb0f953ba65d3d58b3e0fe9ed9f39637835df)
  Полный npm run test:it прогнан: 16 файлов, 72 теста, зелёные — регенерация
  tests/it/schema.sql ничего не сломала. Следы приёмки (тест-копия и тема
  «… (импорт 2)» с 14 вопросами) удалены из dev.
  
  Записана гоча уборки: API тестов в этот момент отдавал 500, потому что другая
  сессия добавила в shared/schema.ts колонку pass_decision_policy, которой в
  dev-базе ещё нет. Удаление выполнено SQL напрямую по порядку из
  TestsRepository.deleteTest, зависимости темы проверены заранее.

- **docs**(prd-48): документ передачи и спека приведены к завершённому состоянию (2026-08-12) [`3f17fca07b4d35953aa4a652b5f7893006e84009`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3f17fca07b4d35953aa4a652b5f7893006e84009)
  Работа закрыта: задачи 1-8, кроме двух долгов, требующих решения владельца.
  Записаны итоги проверок, приёмка формы в браузере и четыре отступления, которые
  ждут подтверждения.

- **docs**(prd-48): задачи 4 и 5 закрыты, документ передачи обновлён (2026-08-12) [`ac32361cf894e8485384139b7694ddac1685f1b4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ac32361cf894e8485384139b7694ddac1685f1b4)
  Серверная часть выборочного импорта готова к приёмке: разбор, решения,
  применение и три роута. Записаны живая проверка по HTTP, восемь гоч и три
  вопроса, которые ждут решения владельца.

- **docs**(prd-48): задачи 2 и 3 выполнены, отступления записаны (2026-08-12) [`570e05f7697618dcd035e1583050c292c44f54ab`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/570e05f7697618dcd035e1583050c292c44f54ab)
  Четыре решения приняты сверх буквы спеки и вынесены в план на подтверждение:
  частичная запись строки теста, удаление лишнего раздела приёмника, запасное
  сопоставление по ручке автора и новые идентификаторы вопросов при политике
  «создать новую тему».

- **docs**(prd-48): спецификация переноса теста без потерь и планы работ (2026-08-12) [`a47f292b1d82532a288d96759e184ab5076ee5de`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a47f292b1d82532a288d96759e184ab5076ee5de)
  Книга Excel — формат содержания: она не несёт jsonb, в котором живут исходы
  показателей, вводный блок, оформление, настройки отчёта и правило прохождения.
  Тест, перенесённый ею из dev в прод, напечатал сырой ключ шкалы вместо названия
  стиля, потерял диаграмму и получил дефолтный порог — молча, без единой ошибки
  импорта.
  
  Спека задаёт формат .tbtest и форму выборочного импорта: пять частей,
  режим upsert/replace только там, где возможна потеря, политика по каждой теме
  и обязательный показ удаляемого до применения.

- **docs**(guide): руководство автора по созданию тестов со снимками экранов (2026-08-12) [`bb9304270317e0854ce36e3c79ae2c9b06a67301`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bb9304270317e0854ce36e3c79ae2c9b06a67301)
  Автору негде было прочитать, как собрать тест: документация покрывала
  только заполнение книги импорта и разработку шаблонов. Руководство ведёт
  весь путь — от темы и банка вопросов до публикации, назначения и выгрузки
  SCORM, — а раздел «Быстрый старт» даёт кратчайший маршрут до рабочего
  теста.
  
  Иллюстрации сняты с живого интерфейса. Чтобы их можно было переснять,
  добавлены два вспомогательных скрипта: seed-guide-demo заводит на стенде
  демонстрационный набор (dev-БД — копия боевой, снимать её «как есть»
  нельзя), make-guide-placeholders рисует заглушку для ещё не снятого места.
  Поиск Chrome вынесен в общий chrome.mjs, а сборка PDF научилась
  разворачивать относительные пути картинок — временный HTML лежит не рядом
  с исходником, и без этого все снимки в PDF были бы битыми.

## [2.14.0](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.13.0...v2.14.0)

### Fixes

- **fix**(report): измерительный тест не печатает «Лучший результат за N попыток» (2026-08-10) [`3348a0a4b816ea37216a40ac7d273c906f988390`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3348a0a4b816ea37216a40ac7d273c906f988390)
  Строка сравнивает прогоны по баллам, а у методики баллов нет ни у одной
  попытки: документ утверждал то, чего экран, с которого его скачали, не
  утверждает. Подпись гасится тем же признаком, что вердикт и сводку
  (PRD-29 §6.7), и целиком — число попыток к содержанию измерительного
  документа отношения не имеет; дата прохождения в шапке остаётся.
  
  Гейт стоит на УЗЛЕ обоих макетов: пустой подписи мало, блок несёт отступ
  и оставил бы разрыв между шапкой и строкой слушателя.
  
  Предпросмотры шаблонов пересобраны — заодно догнали правку страницы
  отчёта из 592e4780, которая до них не доехала.

### Documentation

- **docs**(changelog): раздел 2.13.0 (2026-08-10) [`78009ab8de0a19208bd1169276098ccb5f24bb96`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/78009ab8de0a19208bd1169276098ccb5f24bb96)

## [2.13.0](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.12.0...v2.13.0)

### Features

- **feat**(results): метка шкалы и её значение печатаются одной строкой, линейка доехала до отчёта (2026-08-10) [`6df384408438c90bd73e72d4839fd381d37f165f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6df384408438c90bd73e72d4839fd381d37f165f)
  Значение шкалы стояло отдельной строкой под именем — на списке из четырёх шкал это
  четыре лишних ряда, а числа не выстраивались в столбец, по которому глаз идёт сверху
  вниз. Теперь имя и «значение из максимума» делят один ряд: имя слева, число у правого
  края линейки. Пустая шапка слайдера из разметки ушла, значение печатают все виды кроме
  кольца — у него число стоит в центре фигуры, и второе было бы дублем.
  
  Отчёт линейку не рисовал вовсе, хотя зоны и границы приходят в его контекст тем же
  построителем, что и на экран: разметки в раскладке не было — след переноса страницы из
  кода, где линеек не существовало. Гейт тот же, что на экране, — наличие зон.

- **feat**(prd-19): автор может убрать обзор, когда отвечено всё (2026-08-10) [`5e7a40a4d04c3a9ada44064d72012b8a52c28b55`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5e7a40a4d04c3a9ada44064d72012b8a52c28b55)
  Показ обзора выводился ТОЛЬКО из прав навигации, и по ним при разрешённой
  правке ответов экран остаётся полезным всегда — он единственный путь к уже
  отвеченному вопросу. Но полезен ли он — суждение о методике, а не о правах: у
  опросника, который проходят подряд и ни к чему не возвращаются, список
  отвеченных вопросов стоит между учащимся и результатом. Вынести это может
  только автор, поэтому признак отдельный, а не вывод из существующих двух.
  
  Правило прежнее и остаётся умолчанием: `skip_review_when_complete` по умолчанию
  выключен, ни один настроенный тест выдачу не меняет. Новый признак срабатывает
  ТОЛЬКО при полностью отвеченном объёме — пока что-то пропущено, обзор
  показывается всегда, потому что путь к пропущенному отнимать нельзя.
  
  Настройка проведена до обоих хостов: веб читает её из ответа старта попытки,
  пакет — из TEST_DATA; запасное правило рантайма пакета повторяет ядро на случай
  отсутствия общего модуля. В редакторе — третий переключатель в «Правилах
  прохождения». Фикстуры редактора дополнены обязательным полем модели.

### Fixes

- **fix**(report): страница отчёта соблюдает поля, а строки не рвутся на стыке (2026-08-10) [`592e47801d7313765f0fd992bf9a8974219d008c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/592e47801d7313765f0fd992bf9a8974219d008c)
  Лист был не элементом, а куском общего растра, и все беды стыка росли отсюда:
  фон продолжался сквозь границу, будто страницу оторвали от рулона, а неполный
  кусок дотягивался повтором нижней строки пикселей — вместе с буквами.
  
  Приём, который это лечит, в продукте уже был, но жил ТОЛЬКО в предпросмотре
  редактора: страница отдельным элементом, содержимое обрезано окном. Конвейер
  PDF при этом резал растр — две реализации одного документа и разошлись. Теперь
  страницы строит общая `buildReportPages`, и предпросмотр с файлом обязаны
  совпадать по построению, а не по договорённости.
  
  Три правки геометрии, каждая по своей причине:
  
  \- режем по ПОЛЕЗНОЙ высоте, а не по листу: кусок печатается в окне между
    полями, и кусок ростом в целую страницу туда не помещался — лист показывал
    лишнее, обрывал строку на середине, а следующий начинал с неё же;
  \- высота окна равна ЭТОМУ куску, а не всей полезной площади: иначе лист
    показывал хвост, уже отданный следующей странице;
  \- высота содержимого считается по детям: у корня `min-height` в целый лист, и
    шапка в две строки считалась переростком — вторая её половина уходила в файл
    пустой страницей.
  
  Копия отдаёт свои вертикальные поля окну, иначе на первой странице отступ
  складывался дважды; сдвиг продолжения поправлен на высоту поля.
  
  Резать и дотягивать растр больше нечего — `sliceCanvas` удалён.

- **fix**(report): название теста печатается первой строкой документа (2026-08-10) [`5d50beed5937ed3cd7e268a6739bec699d2a41b8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5d50beed5937ed3cd7e268a6739bec699d2a41b8)
  Название лежало ВНУТРИ карточки счёта, то есть после шапки с вердиктом и после
  вводного слова автора — читатель узнавал, что за бумага у него в руках, где-то в
  середине первой страницы. Теперь оно стоит выше всего: документ обязан назвать
  себя раньше, чем вынести вердикт.
  
  Заодно исчезла вырожденная карточка. У методики без баллов сводка скрыта, и от
  карточки счёта оставалась плашка с одним названием; после переезда печатать в
  ней стало нечего, поэтому гейт `hideScoreSummary` накрыл её целиком. На отчёте
  ЧИЛ это убрало две страницы из десяти.
  
  Порядок печати закреплён тестом, и он обновлён вместе с макетом — прежний
  порядок был таким же намеренным, и менять его молча нельзя.

- **fix**(templates): «Сертификация» догнала экраны и отчёт «Стандартного» (2026-08-10) [`66dc4d88068d546ae333b6623f970442fb18f8a2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/66dc4d88068d546ae333b6623f970442fb18f8a2)
  Ревизия экранов, вынесшая значение шкалы в строку с именем и доведшая линейку до
  отчёта, осталась в стандартном шаблоне: макеты внешнего шаблона её не получили,
  и гард паритета встал на трёх файлах. Ради этого он и стоит — шаблон, чьи
  раскладки отстали от контракта, рисует МЁРТВЫЙ экран, а структурные валидаторы
  при этом молчат.
  
  Макеты `report.html`, `results.html` и `results.adaptive.html` снова побайтовые
  копии стандартных: дельты объявлены только для обложек, и ничего своего у этих
  трёх файлов быть не должно.
  
  Стили перенесены отдельно — паритет их не проверяет, а без них новая разметка
  осталась бы неоформленной: добавлено правило строки «имя + значение»
  (`.tb-measure__head`) и убрано осиротевшее правило шапки слайдера, узла которой
  в разметке больше нет.

- **fix**(report): страницы отчёта перестали быть полупустыми и белыми снизу (2026-08-10) [`9fea1db68bf18da2645dd2191720ea2aff6050f6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9fea1db68bf18da2645dd2191720ea2aff6050f6)
  Фон отчёта живёт ВНУТРИ снимка листа, а снимок ложился на A4 с высотой по
  пропорции: где он кончался, начиналась белая бумага. В отчёте на девять страниц
  четыре получались недобранными — 102, 257, 279 и 119 мм из 297.
  
  Короткие снимки давали карточки выше страницы: одна карточка на все показатели
  вырастала до полутора листов, резалась растром по строке, и хвост в полторы
  сотни пикселей открывал почти пустую страницу.
  
  Что изменено:
  
  \- лист печатается на полную высоту A4 — короткий снимок дополняется продолжением
    собственного фона (нижняя строка пикселей), поэтому знание о цвете шаблона
    конвейеру не нужно и внешний шаблон получает свою подложку, а не выдуманную;
  \- разрез не оставляет висячих строк: хвост несёт не меньше двух строк и не
    меньше четверти листа — иначе последняя страница выглядит обрезком;
  \- разрез предпочитает границу вложенного блока, когда она рядом с краем листа:
    потерять десяток пикселей дешевле, чем разорвать карточку показателя;
  \- показатель стал СВОЕЙ карточкой в обоих шаблонах — блок верхнего уровня, по
    которому раскладка умеет резать, вместо строки внутри общей карточки;
  \- фон повторяется каждый лист (`background-size: 100% 842px`), а не растягивается
    на весь документ: иначе страницы получали разные куски градиента.
  
  Проверено пересборкой отчёта тем же путём: все листы 1190x1684 (297.2 мм),
  недоборов ноль.

- **fix**(results): полоса шкалы больше не выносит вердикт цветом и берёт цвет шкалы (2026-08-10) [`a549e560313abbb5b2088c7252385f3e1bd4f3c9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a549e560313abbb5b2088c7252385f3e1bd4f3c9)
  `gradient_bar` красил полосу светофорной рампой, не глядя на валентность: у
  типологии, где направление не объявлено, «17 из 98» выходило красным, то есть
  оценкой, которой методика не выносит. Соседний `zoneColors` эту политику
  соблюдал с самого начала — расхождение жило внутри одного модуля.
  
  Цвет полосы теперь решается в два шага, оба — правило PRD-46 §7. Где
  направление объявлено, цвет высказывает вердикт и назначается рампой. Где его
  нет, цвет несёт ИДЕНТИЧНОСТЬ шкалы, поэтому берётся заданный автором в
  «Оформлении шкал» — до сих пор эту карту читала только роза, и карточка
  шкалы расходилась с её же сектором. Цвет не задан — нейтральная схема.
  
  Карта разбирается в `fillMeasureBlocks` ДО построения диаграммы: выключенная
  диаграмма не должна забирать у карточки цвет шкалы.

- **fix**(scorm): экспорт отчёта из пакета перестал падать на «TB is not defined» (2026-08-10) [`a250087b6f5c517432ca5c9513c33d9dbc9634d3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a250087b6f5c517432ca5c9513c33d9dbc9634d3)
  `pdfReportMeta` обращалась к `TB`, объявленной ЛОКАЛЬНО в другой функции файла
  (`exportResultsToPDF`), поэтому каждый вызов бросал ReferenceError и «Скачать
  отчёт» в пакете не работало ни у одного теста — независимо от того, задан ли
  вводный блок, ради которого обращение и появилось. Проверка `TB && …` от
  необъявленного имени не спасает.
  
  Общий модуль берётся тем же образцом, что во всех прочих функциях рантайма
  пакета: своей локальной ссылкой на `window.TBTemplate`. Файл не был покрыт
  ничем — отсюда и тест, держащий два факта: функция не падает без общего модуля
  и читает через него ветвь отчёта, когда он есть.

### Documentation

- **docs**(references): книга импорта опросника ЧИЛ версии 3.6 (2026-08-10) [`0c6129d16526bf1e7d1c0801559c6a9ffb6d25ca`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0c6129d16526bf1e7d1c0801559c6a9ffb6d25ca)
  Опросник, собранный из финальной книги-источника в формат импорта продукта:
  листы «Настройки», «Вопросы» (14 блоков типа allocation, бюджет 7),
  «Структура», «Шкалы» (четыре стиля, домен 0..98), «Вклады вопросов» (56 строк)
  и «Показатели». Загружается разделом «Импорт» в любом контуре.
  
  Тексты отчёта книга не несёт: исходы показателей и вводный блок формат импорта
  не переносит, они правятся в редакторе после загрузки.

- **docs**(changelog): разделы 2.11.0 и 2.12.0 (2026-08-10) [`871c1e32cb3c67ee0b4223f127fae0f568be0ef4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/871c1e32cb3c67ee0b4223f127fae0f568be0ef4)
  Журнал отставал на два релиза: последним разделом значился 2.10.0-beta, а
  работа за август лежала двумя абзацами, дописанными вручную ПОД его заголовок —
  то есть под релизом, который вышел раньше описанных изменений.
  
  Разделы собраны заново из истории: каждая запись со своим коммитом и ссылкой,
  абзацы prd-31 и prd-40 из ручной вставки заменены полными пер-коммитными
  записями в тех релизах, куда они действительно попали.

## [2.12.0](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.11.0...v2.12.0)

### Features

- **feat**(editor): настройки отчёта разложены по смыслу, добавлен вводный текст (2026-08-10) [`225f6efac7ce6fc16ccd7944abf30ff73f3d4daf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/225f6efac7ce6fc16ccd7944abf30ff73f3d4daf)
  Автор искал настройки отчёта не там, где они лежали: PRD-27 §7.1 требовал
  держать их в блоке обратной связи, а PRD-47 §6.2 перенёс всё в «Оформление» —
  оба крайних варианта разводили по разным экранам вопросы, которые решаются
  вместе. Теперь граница проходит по СМЫСЛУ поля, и проводит её сам шаблон
  признаком `scope`:
  
  \- «Настройки» → обратная связь: вводный текст, выдавать ли отчёт, что в нём
    показывать, предпросмотр;
  \- «Оформление» → «Отчёт»: вид отчёта, подложка, логотип, предпросмотр.
  
  Карточка осталась ОДНА (проп `scope`): каталог видов, перенос значений при смене
  вида и предпросмотр у обеих сторон общие, и две копии пришлось бы чинить дважды.
  
  Вводных текстов два — экран и отчёт, — плюс переключатель «в отчёте тот же
  текст». Он СВЯЗЫВАЕТ выдачи, а не копирует: правка в одном месте меняет обе, а
  собственный текст отчёта не стирается и возвращается при выключении. Пишутся они
  тем же редактором, что обратная связь, но без вложений, мероприятий и курсов:
  вводное слово — это текст, материалы вешают на обратную связь.
  
  Предпросмотр отчёта показывает ЛИСТЫ A4 той же раскладкой, что режет PDF, и
  объявляет их число в подзаголовке — автор согласовывает документ, который
  получит слушатель, а не ленту.

- **feat**(server): вводный блок и выключатель отчёта доезжают до обоих хостов (2026-08-10) [`bee85271109c08fbb463765b0ac8b6c69d2390a9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bee85271109c08fbb463765b0ac8b6c69d2390a9)
  Веб-маршрут результата и сборка пакета отдают выдаче то, что автор задал:
  вводный текст своей ветви (у экрана и у отчёта они разные) и признак «выдавать
  ли отчёт». Раньше кнопка «Скачать отчёт» стояла в коде константой
  `canReport: true` — теперь её решает настройка теста, и в LMS тот же ответ даёт
  рантайм, потому что признак едет в пакет вместе с выбором вида отчёта.
  
  Переключатель «в отчёте тот же текст, что в итогах» разрешается общим
  `resolveReportIntro` — своя копия правила в рантайме разошлась бы при первой же
  правке и выдала бы слушателю документ с чужим вводным словом.

- **feat**(prd-27): отчёт печатается страницами A4, а не одной лентой (2026-08-10) [`4e2cd433f9d40f01071bbb333a02f341dc3fd0de`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e2cd433f9d40f01071bbb333a02f341dc3fd0de)
  Документ выходил ОДНИМ листом произвольной высоты — «колбасой», которую нельзя
  ни распечатать, ни пролистать. Теперь лист всегда 210×297 мм, а разрыв проходит
  между карточками: «Ваш результат», разрезанный посередине уровня, — брак, а не
  страница.
  
  Правило разбито надвое сознательно. Арифметика над координатами — чистый
  `paginate` (проверяется на числах, без браузера); измерения, клоны листов и
  нарезка — браузерный `paginate-dom`, и его зовут ОБА места: конвейер PDF и
  предпросмотр в редакторе. Своя раскладка в предпросмотре означала бы, что автор
  согласовывает не тот документ, который получит слушатель.
  
  Карточка, которая выше листа сама по себе, режется по НИЖНЕЙ ГРАНИЦЕ СТРОКИ
  (границы берутся у браузера через `Range.getClientRects`): строка, разрезанная
  пополам, нечитаема на обеих половинах, и недобранные сантиметры внизу листа —
  меньшее зло. Ссылки остаются на своём листе.
  
  Измерения идут по `offsetTop`/`offsetHeight`, а не по `getBoundingClientRect`:
  предпросмотр показывает документ уменьшенным, и раскладка по rect считала, что
  на лист влезает больше — лист потом дорезался растром вместо разрыва между
  карточками.

- **feat**(templates): формат авторского текста доведён до выдачи (2026-08-10) [`8ef3141e4001bcba73a5b4b1e02eab2802218104`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8ef3141e4001bcba73a5b4b1e02eab2802218104)
  Редактор обратной связи с PRD-7 предлагает «Обычный / Форматированный / HTML»,
  формат ложился в `feedback_json.format` — и на этом путь заканчивался: выдача
  везла голый текст, макеты печатали его экранированным, и слушатель получал
  методичку сплошным полотном.
  
  \- `FeedbackBlock.format` переносит `normalizeFeedback`, разметку строит новый
    `rich-text` (обычный текст экранируется и `\n` → `&lt;br&gt;`, `richText`/`html`
    печатаются как есть — политика та же, что у полей контентной страницы);
  \- контекст несёт разметку ПАРНЫМ полем (`textHtml` рядом с `text`, `textsHtml`
    рядом с `texts`): подмена строки вывела бы теги текстом в макетах внешних
    шаблонов;
  \- в DSL добавлен `{{& путь }}` — единственный канал разметки внутри `{{#each}}`,
    где и печатаются толкования уровней и тексты рекомендаций. `{{{ }}}` остаётся
    запрещённым: у канала одна форма записи.
  
  Здесь же — вводный блок (`result.introHtml`) и два правила, которые обязаны
  быть одним на все хосты: назначение поля вида (`report-field-scope`) и выбор
  вводного текста отчёта (`report-intro`). Второе вынесено из схемы в чистый
  модуль: тот же ответ нужен внутри SCORM-пакета, а схему туда не затащить.

- **feat**(prd-27): вводный блок и выдача отчёта — колонка и контракт (2026-08-10) [`f9d332c857820c6397f33596f68b203c728f7734`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f9d332c857820c6397f33596f68b203c728f7734)
  Отчёт и экран итогов получают вводный текст автора, а сам отчёт — выключатель
  выдачи. Обе настройки описаны схемой и хранятся отдельно от настроек вида:
  
  \- `tests.intro_json` (миграция 0014) — своя колонка, а не ветвь
    `report_settings_json`: текст экрана к настройкам отчёта отношения не имеет, и
    общая колонка связала бы два независимых черновика редактора;
  \- `report_settings_json.enabled` — общая, вне ветвей режима: документ либо
    положен слушателю этого теста, либо нет, от режима выдачи это не зависит.
  
  Оба умолчания выбраны так, чтобы уже существующие тесты не изменились:
  отсутствие признака означает «выдавать», отсутствие ветви — «блока нет».

- **feat**(prd-47): карточка отчёта переехала на вкладку «Оформление» (2026-08-09) [`15f4df8e9f3b2052ab0df6a22fbd16260feeaf03`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/15f4df8e9f3b2052ab0df6a22fbd16260feeaf03)
  Настройки отчёта были внизу «Настройки → Основное», за разделителем, между
  сценарием прохождения и общей обратной связью. Отчёт — часть шаблона, его поля
  объявляет манифест ровно как параметры оформления, поэтому место им рядом с
  «Макетом» (решение владельца, PRD-47 §6.2).
  
  Переехал ТОЛЬКО интерфейс. Хранение осталось отдельной колонкой: PRD-27 §4.2
  отказался складывать поля отчёта в design_settings_json, чтобы не связывать
  вкладки порядком сохранения, — и это решение в силе. Поэтому model.report
  по-прежнему часть модели теста, а раздел оформления получает её пропсами.
  
  Побочно вычищены два мёртвых пропса: draftTemplateId и draftDesignParams
  приходили в общие настройки ИСКЛЮЧИТЕЛЬНО ради карточки отчёта — то есть
  черновик соседней вкладки тёк в раздел, которому он был не нужен. Теперь эти
  поля у карточки СВОИ, своей же вкладки, и лишняя связь между вкладками исчезла.
  
  Контракт самой карточки не тронут: переезд не смешивается с правкой пропсов,
  иначе разбор регрессии перестанет отличать одно от другого.

- **feat**(prd-47): пункт рейла «Отчёт о результатах» на вкладке «Оформление» (2026-08-09) [`3df4334ef25e7665a16dab808599c770211df38d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3df4334ef25e7665a16dab808599c770211df38d)

- **feat**(prd-47): предпросмотр страницы итогов показывает демо-измерения (2026-08-09) [`81116ee828051acde7ebe33ed3bee2a0f4c2b14c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/81116ee828051acde7ebe33ed3bee2a0f4c2b14c)

- **feat**(prd-47): смоук шаблона и предпросмотр отчёта показывают измерения (2026-08-09) [`3215cf62b82080e6f2171f0fabf6b07690edbd27`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3215cf62b82080e6f2171f0fabf6b07690edbd27)

- **feat**(prd-47): демо-набор шаблона несёт измерения для предпросмотра (2026-08-09) [`77a46e61bb6a918d6de911640568e90da83b86bf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/77a46e61bb6a918d6de911640568e90da83b86bf)

- **feat**(prd-47): вход отчёта делается из входа экрана одним сборщиком (2026-08-08) [`04e83340a4c3498c4f5fec380b21811cd0ac1fe3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/04e83340a4c3498c4f5fec380b21811cd0ac1fe3)

- **feat**(workbook): книга возит границы шкалы, предел показа и благоприятное направление (2026-08-08) [`79c10b60cc6a5cff940ea574ef614903f7e5f7dd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/79c10b60cc6a5cff940ea574ef614903f7e5f7dd)
  Экспорт выгружал из конфигурации только «Диапазоны», поэтому три поля жили
  исключительно в редакторе: автор не мог ни увидеть их в книге, ни задать. Теперь
  у каждого есть колонка, и книга полна: «Границы шкалы», «Предел показа»,
  «Благоприятное направление».
  
  Колонка валентности названа «Благоприятное направление» — так её зовёт карточка
  шкалы. План предлагал «Направление», но это имя на листе уже занято колонкой
  direction, которая ПЕРЕВОРАЧИВАЕТ значение при подсчёте, тогда как валентность
  числа не меняет, а говорит, как его читать. Разница объяснена в Справке.
  
  Границы идут ОДНОЙ ячейкой «мин..макс», как «Диапазоны»: половина домена — не
  домен, а лишний ноль на результатах ученика печатается как «18 из 0». Пустая
  ячейка = не задано, потому что ноль — законная граница.
  
  Наличие колонки решается по строке ЗАГОЛОВКОВ, а не по объекту строки:
  sheetToObjects выбрасывает пустые ячейки, и по нему «колонки нет» неотличимо от
  «автор очистил», хотя значат они противоположное. Книги прежнего формата от
  этого читаются по-старому — слияние оставляет сохранённое.
  
  Экспорт читает конфигурацию СЫРОЙ, а не через parseScaleInterpretation: тот при
  отсутствии домена подставляет охват уровней, и выгрузка сделала бы придуманный
  домен явным на следующей же загрузке.

- **feat**(prd-46): предел оси радара выбирает автор (2026-08-08) [`e31eb6f334fde744f3cbc7be87c88c066156e7cf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e31eb6f334fde744f3cbc7be87c88c066156e7cf)
  Раздел 6 спеки не был реализован вовсе: полный луч всегда означал `domainMax`.
  Для методики, чей домен недостижим, это рисует фигуру, скомканную в трети
  радиуса, — и различия между шкалами, ради которых профиль и читают, пропадают
  вместе с ней.
  
  Три значения отвечают на один вопрос разной ценой. «Домен» — прежнее
  поведение: сетка совпадает с границами интервалов в карточке рядом. «Заданный»
  берёт `displayMax` из настроек шкалы и меняет только масштаб рисунка, не трогая
  того, что шкала измеряет. «По попытке» растягивает фигуру на весь круг ценой
  сравнимости: масштаб начинает зависеть от ответов, поэтому под фигурой печатается
  подпись — без неё две несравнимые картинки выглядят одинаково убедительно.
  
  Подпись — это масштаб рисунка, а не балл ученика, поэтому запрет на числа из
  PRD-35 §4.3 не нарушается. При «домене» она пуста: рассказывать не о чем.
  
  Балл выше предела упирается в кольцо и получает признак «за пределом». Молча
  обрезать нельзя: занижена окажется как раз та шкала, которая выделяется сильнее
  прочих, а «ровно на пределе» и «за пределом» стали бы неразличимы.
  
  `displayMax` живёт в конфигурации ШКАЛЫ, а не рядом с доменом: домен говорит,
  что шкала измеряет, и правит линейку и границы интервалов, а этот предел не
  меняет ничего, кроме размера рисунка. Включается переключателем, как и ручной
  домен, — отсутствие значения это состояние, а поле, заполненное доменом по
  умолчанию, делало бы вид, что предел задан осознанно.
  
  «По попытке» не берёт предел со шкалы со скрытым баллом: иначе спрятанное число
  вернулось бы к читателю через геометрию всей фигуры.

- **feat**(prd-46): пиктограмма шкалы доезжает до диаграммы контурами (2026-08-08) [`3c7c05255d34e5fb3e14d8ed190e13533ff8aad1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3c7c05255d34e5fb3e14d8ed190e13533ff8aad1)
  Шкале мало цвета: на розе она ещё и опознаётся, и подпись с глифом читается
  быстрее подписи без него. Ядро принимало `iconPaths` с самого начала, но
  разрешать имя в геометрию было нечем.
  
  Разрешать приходится НА ХОСТЕ. Пакет — папка статических файлов: ни React, ни
  шрифта иконок, ни библиотеки, в которой можно посмотреть имя. Поэтому веб
  разрешает при отдаче контекста, упаковщик — в бейке, и оба идут через одну
  функцию: иначе одно имя означало бы два разных глифа.
  
  Сама библиотека для этого не годится: `lucide-react` отдаёт глифы React-
  компонентами, геометрия заперта в замыкании `createLucideIcon`. Объявления есть
  в поставляемых ES-модулях, поэтому они читаются один раз генератором
  (`npm run icons:gen`) и лежат данными. Псевдонимы отброшены: это устаревшие
  написания уже имеющихся глифов, и автор получил бы два имени на одну картинку.
  
  Каждый узел нормализуется к пути: окружность — двумя полудугами, потому что
  дуга с совпадающими концами вырождена и рендерер её выбрасывает. Цикл в языке
  шаблонов не умеет менять тег, поэтому `&lt;circle&gt;` в контексте не переживёт.
  
  Селектор пиктограмм берёт ту же таблицу, что и разрешитель: выбирать из другого
  набора значило бы предлагать глиф, который диаграмма молча пропустит. Таблица
  грузится лениво — четверть мегабайта не должна платиться тем, кто пиктограммы
  не ставит.
  
  Хранится ИМЯ, не контуры: выбор автора переживает обновление библиотеки, а
  геометрия прошлой сборки не переживает смену имени.

- **feat**(prd-46): цвет шкалы задаёт автор, а не только палитра (2026-08-08) [`f1e19e35b8e21d1a9965f5da65aae0acad14c959`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f1e19e35b8e21d1a9965f5da65aae0acad14c959)
  Роза красит секторы по идентичности шкалы, но задать этот цвет было нечем:
  ядро принимало `color` на оси, а проводки не существовало ни на одном хосте —
  рисовалась выведенная категориальная палитра, что бы автор ни выбрал.
  
  Облик хранится картой в настройках варианта «Итоги», ключ — ключ шкалы: шкала
  описывает суть, оформление приносит шаблон, а стабильный ключ переживает
  переименование. Одно поле со значением-картой, а не поле на шкалу: набор шкал
  заранее неизвестен.
  
  Применение — в `buildScalesChart`, а не в двух хостах. Оба уже отдают настройки
  варианта, в которых карта и едет, поэтому одна точка применения — единственный
  способ, которым игроки не смогут разойтись в цвете (PRD-29 §9): паритет по
  построению вместо двух зеркальных правок, синхронных вручную.
  
  Контрол по согласованному эскизу: строка на шкалу и превью НАБОРА рядом —
  соседние секторы смыкаются, и близкие оттенки видно только вместе. При
  объявленном направлении цвет ВЫКЛЮЧЕН и объяснён, а не спрятан: там цвет
  высказывает вердикт и совпадает с линейкой в карточке, а исчезнувшее поле
  читается как дефект. Правило «всем или никому» держит ядро — редактор лишь
  не предлагает того, что рендерер проигнорирует.
  
  Разбор карты защитный: значение приходит из `settings_json`, который никто не
  переписывает миграциями, поэтому испорченная запись падает обратно на палитру,
  а не в сломанный `fill`.

- **feat**(prd-26): веб-инспектор показывает «не оценивается» вместо «неверно» (2026-08-08) [`1bb90abd5a895b9b22b4dc7f69ac03674407141f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1bb90abd5a895b9b22b4dc7f69ac03674407141f)
  Вердикт `measure` и признак `measurement` объявлены в контракте снимка и приходят
  из вычислялки готовыми — страница их только рисует. Выводить признак в UI значило
  бы завести второе мнение о том, что считать измерительным вопросом, и разойтись с
  CLI-плеером, который читает ту же вычислялку.

- **feat**(prd-46): ипсативность доезжает до обоих хостов (2026-08-08) [`9a5f8794623d30fc47cbc774bfbb75a8f0e7162d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9a5f8794623d30fc47cbc774bfbb75a8f0e7162d)
  Предикат был написан и покрыт тестами, но его никто не звал: признак всегда
  оставался ложным, поэтому режим «авто» всегда давал радар, а роза рисовалась
  только явным выбором вида. Проводка закрыта разом на обеих сторонах — половина
  её создала бы расхождение хостов, которое спека (§5) считает дефектом.
  
  Пакет считает при сборке выгрузки и печёт в TEST_DATA. Безусловно: цена
  платится один раз на сборку, а не на каждый рендер, и ответ обязан лежать в
  пакете до того, как станет известно, под какой настройкой его прочтут. Ключ
  пишется ТОЛЬКО при истине и внутри блока шкал — отсутствие рантайм и так
  читает как «не ипсативна», значит false менял бы байты каждого пакета с
  измерениями, ничего не меняя на экране.
  
  Веб считает при сборке материала итогов и только в режиме «авто»: при явном
  виде ответ ничего не изменит, а экран итогов рисуется на каждый заход. Читает
  из того же источника, что и шкалы, поэтому у попытки со снапшотом модель
  берётся из снапшота и поздняя правка теста не сменит вид диаграммы у старой
  попытки. Отчёт получает признак тем же объектом measures.
  
  drawnScaleKeys вынесен в одно место: скрытая шкала не рисуется, а доля целого,
  которого ученику не показали, — доля другого целого, и оба хоста обязаны
  судить по тому же набору, который рисует ядро.
  
  Известный предел: у отчёта свой переключатель, а признак ему достаётся
  посчитанный для экрана. Тест с явным видом на экране и «авто» в отчёте получит
  в отчёте радар. Направление безопасное, расхождение записано в коде.

- **feat**(prd-46): адаптер ипсативности для хостов (2026-08-08) [`b2495bf194ca505ef20975f981e1454ca995f913`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b2495bf194ca505ef20975f981e1454ca995f913)
  Перевод строк БД во вход предиката: scaleId в ключ шкалы, типы вопросов,
  бюджеты распределения. Перевод переиспользован из scale-domain, а не
  написан заново — две копии были бы двумя шансами разойтись в том, какие
  строки какой шкале принадлежат.
  
  Флаг НЕ сохраняется. Попытка приколота к снапшоту, а снапшот уже несёт
  шкалы, вклады и вопросы, поэтому вычисление из него воспроизводимо по
  построению; сохранённый флаг был бы вторым экземпляром того же факта, а
  вторые экземпляры расходятся. Домен хранится по обратной причине: он
  авторская величина с переопределением, там есть что сохранять.
  
  Считать полагается ТОЛЬКО в режиме «авто»: при явном виде ответ ничего
  не меняет, и чтения были бы потрачены впустую. Этот guard и делает
  вычисление на лету дешёвым.

- **feat**(prd-46): блок розы в макетах и стили обоих шаблонов (2026-08-08) [`faadb17bf895a0a799adb1fb1a5b5f0ff6d0448f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/faadb17bf895a0a799adb1fb1a5b5f0ff6d0448f)
  ОДИН блок на обе диаграммы: ядро отдаёт их одной формой, и неподходящие
  списки приходят пустыми, а пустой цикл не рисует ничего — ветвиться по
  виду разметке не требуется, в DSL и нечем. Общие части переехали с
  tb-radar__* на tb-chart__*, радарные детали остались под своим именем.
  
  Порядок слоёв: заливки розы непрозрачны, поэтому сетка рисуется ПОВЕРХ
  секторов — под ними она видна только в промежутках между короткими
  секторами, то есть там, где говорит меньше всего.
  
  Зазор между заливками сделан обводкой цветом ФОНА, а не линией: так он
  одинаково работает и на светлой, и на тёмной сцене. Сектор, не
  поместившийся в поле, получает пунктирный край — иначе обрезанная доля
  читается как «шкала ровно на пределе».
  
  Проверка поймала, что DSL не знает комментариев {{! }} — пояснение
  переведено в HTML-форму.

- **feat**(prd-46): выбор вида диаграммы объявлен в манифестах обоих шаблонов (2026-08-07) [`f78bf77c3cdaae8a7d973aa9a9e7aa1b0496ef32`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f78bf77c3cdaae8a7d973aa9a9e7aa1b0496ef32)
  Своего UI не требует: обобщённый контрол настроек уже рисует тип select
  из манифеста, поэтому достаточно объявления. Встаёт там же, где жила
  галочка радара — в варианте итогов и в обоих вариантах отчёта, у
  которого свой независимый переключатель.
  
  Умолчание «нет», и это сторожится тестом: «авто» РИСУЕТ, и умолчание,
  которое рисует, поставило бы диаграмму каждому существующему тесту,
  автор которого её не просил.
  
  Галочка PRD-35 остаётся объявленной и помечена устаревшей: её читают
  тесты, настроенные до появления выбора, и только когда новый ключ не
  задан.

- **feat**(prd-46): единый контракт result.scalesChart и проводка диспетчера (2026-08-07) [`02315bd122c97c443e321a4914ed18b985745a89`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/02315bd122c97c443e321a4914ed18b985745a89)
  Радар и роза приходят в макет ОДНОЙ формой. Размеченный union был бы
  чище в типах и бесполезен в разметке: DSL — подмножество mustache, без
  сравнения и без ветвления по значению, поэтому рядом с kind стоят
  isRadar/isRose, а неподходящие списки ПУСТЫ, а не отсутствуют — пустой
  цикл не рисует ничего, и обе диаграммы проходят через один блок макета.
  
  Отказ построителя не пробуется другой диаграммой: автор, попросивший
  розу и получивший радар, увидел бы о своих данных не то утверждение,
  которое выбрал. Выбирает только «авто», и выбирает до построения.
  
  Шов showRadar заменён на chartSettings, несущий не флаг, а настройки: у
  отчёта СВОЙ переключатель в полях варианта отчёта, поэтому хосты
  передают разные настройки, а правило решения одно. Пакет отдаёт
  настройки как есть и сам ничего не решает — миграция булева ключа PRD-35
  живёт в ядре.

- **feat**(prd-46): поле розы настраивается, пиктограмма необязательна (2026-08-07) [`d7625bb24b3335ff35737772ee8060cf68632e2b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d7625bb24b3335ff35737772ee8060cf68632e2b)
  Поле = 100% целого означало «один стиль забрал весь бюджет» — состояние,
  в ипсативной методике недостижимое. Средняя доля 1/N, у лидера треть, и
  корень от неё давал 0.59 радиуса: внешняя треть пустовала при любых
  ответах, а платили за неё каждую попытку.
  
  Появился режим поля: полное, сбалансированное (край = 2/N) и по попытке.
  Умолчание сбалансированное — умолчание обязано давать читаемую картинку
  без вмешательства. Доля выше края упирается в кольцо и помечается: молча
  обрезать доминирующую шкалу нельзя именно там, где это важнее всего.
  
  Пиктограмма необязательна: у «Командного» в эскизе её нет, и подпись
  просто не резервирует под неё строку. Заодно снята невозможная строка
  эскиза — цвет по идентичности доступен либо всем шкалам, либо никому,
  потому что одна шкала с направлением переводит всю розу на схему уровней.

- **feat**(prd-46): диспетчер вида диаграммы и миграция настройки (2026-08-07) [`5b8baa2878fb6428fb95f8772861aa7703365a1f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5b8baa2878fb6428fb95f8772861aa7703365a1f)
  Решение отделено от обеих геометрий: каждый построитель уже отказывается
  рисовать то, чего не может, и эти отказы про фигуру. Диспетчер отвечает
  на вопрос выше — что автор хочет сказать об этих шкалах и позволяют ли
  данные это сказать.
  
  Два отказа явно выбранной розе разной природы. Неипсативные шкалы всё
  равно рисуются: ипсативность выводится из модели, а автор может знать о
  методике то, чего вывод не видит, поэтому редактор предупреждает, а не
  запрещает. Скрытый балл не рисуется — это не суждение, а невозможность:
  доля, квантованная к середине зоны, уже не та доля.
  
  Отсутствие настройки означает «нет», а не «авто»: «авто» рисует, и
  умолчание, которое рисует, поставило бы диаграмму каждому существующему
  тесту, автор которого её не просил.

- **feat**(prd-46): эскиз облика шкалы — пиктограмма и цвет (2026-08-07) [`01419a1505d0099fddfcc0ff8c386cf00c5b8a94`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/01419a1505d0099fddfcc0ff8c386cf00c5b8a94)
  Оба поля встают в форму шкалы, рядом с направлением и видимостью: облик
  такое же свойство шкалы, как домен и интервалы, и отдельный экран развёл
  бы связанные решения. Под полями — превью сектора ровно таким, каким он
  выйдет на диаграмму, иначе цвет подбирается вслепую.
  
  Выбор цвета — компонент ДС ColorPicker, свой пикер не заводится. Отсюда
  шов: пикер работает в hex, а цветовой контракт рендерера — тройки HSL,
  поэтому конвертация делается на сохранении и в данных остаётся один
  формат.
  
  Пиктограмма выбирается из полного набора lucide с поиском, а не из
  короткой подборки: методики приходят разные, угадать за автора нельзя.
  
  При объявленном направлении поле цвета ВЫКЛЮЧЕНО и объяснено, а не
  спрятано: цвет там высказывает вердикт и совпадает с линейкой в карточке,
  а исчезнувшее поле читается как дефект. Пиктограмма остаётся доступной —
  она несёт идентичность, а не оценку.

- **feat**(prd-46): цвет шкалы, заданный автором (2026-08-07) [`d236e370ede9b16e76976e30112809c02e536be6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d236e370ede9b16e76976e30112809c02e536be6)
  Читается ТОЛЬКО там, где цвет несёт идентичность, то есть у типологии, где
  ни один уровень не лучше другого. Где направление объявлено, цвет
  высказывает вердикт, и авторская закраска поверх него поставила бы один
  цвет на диаграмме и другой на том же значении в карточке рядом.
  
  Слот палитры за шкалой сохраняется в любом случае, поэтому закраска одной
  шкалы не сдвигает оттенки соседних между попытками.

- **feat**(prd-46): пиктограмма шкалы в подписи диаграммы (2026-08-07) [`a505940988ed38707231f404e9ec650a9c69fe6e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a505940988ed38707231f404e9ec650a9c69fe6e)
  Пиктограмма приходит в контекст КОНТУРАМИ, а не именем: в SCORM-пакете
  нет ни React, ни шрифта иконок, а веб и пакет обязаны нарисовать один и
  тот же глиф. Имя, выбранное автором, разрешает в геометрию хост.
  
  Каждый узел lucide нормализуется к пути: окружности записываются двумя
  полудугами, потому что цикл DSL не умеет менять тег, который выдаёт.
  
  Пиктограмма занимает в блоке подписи собственную строку, поэтому луч с
  ней и без неё сохраняет один ритм, а подъём блока над кругом считается
  как прежде.
  
  Для контрольного теста ЧИЛ подобраны по смыслу: цель — целеустремлённому,
  искры — вдохновляющему, люди — командному, шестерёнка — процессному.

- **feat**(prd-46): оси розы вернулись в сетку (2026-08-07) [`cd56c69ec13c507499931ad3618487e53d25af48`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cd56c69ec13c507499931ad3618487e53d25af48)
  Лучи идут по ГРАНИЦАМ секторов, а не по их серединам: граница — это
  место, где одна шкала кончается и начинается следующая, поэтому луч
  продолжает ребро, которое читатель уже видит на заливке, и выносит его
  к краю поля. Рисуются поверх заливок, как и кольца.

- **feat**(prd-46): эскиз розы на выведенной ядром геометрии (2026-08-07) [`bc537405256a76127504270739cff03cf8ece7c0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bc537405256a76127504270739cff03cf8ece7c0)
  Эскиз собран из настоящего SVG, а не нарисован: скрипт печатает разметку
  розы прямо из rose-view, поэтому согласовывается то, что нарисует
  рендерер. Карточки шкал и слои стилей взяты из согласованного эскиза
  PRD-35 без правок, данные заменены на контрольную выкладку ЧИЛ.
  
  Эскиз сразу нашёл две вещи, которые тестами не ловятся: при четырёх
  шкалах эталонное кольцо совпадало с кольцом сетки 25 процентов и
  рисовалось дважды, и оно же было целиком скрыто под заливками секторов.
  Дублирующее кольцо теперь не выдаётся ядром, эталонное рисуется поверх
  заливок.

- **feat**(prd-46): категориальная палитра розы для типологий (2026-08-07) [`a9f2e66116bb69fa5db461ed16f5d57cd107ccbf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a9f2e66116bb69fa5db461ed16f5d57cd107ccbf)
  У типологии нет лучшего и худшего уровня, поэтому цвет сектора несёт
  идентичность шкалы, а не состояние. Иначе рампа уровней отвечает на
  valence: none нейтральным серым, и четыре стиля ЧИЛ различались бы
  только положением.
  
  Палитра выведена из токенов --ou-cat-*, а не скопирована: исходный
  набор проваливает три измеряемые проверки — digital светлее общей
  полосы, b2b читается серым, пара digital/b2c даёт ΔE 12 при пороге 15.
  Сохранён тон OKLCH, светлота и насыщенность пересчитаны в полосу,
  общую для светлой и тёмной темы, серый слот выброшен, порядок подобран
  по худшей паре соседей с учётом смыкания последнего сектора с первым.
  
  Все проверки пройдены в обеих темах: худшая пара соседей ΔE 24.5 при
  нормальном зрении, 10.9 при дейтеранопии. Отсюда предел розы — шесть
  шкал вместо семи.

- **feat**(prd-46): ядро розы Найтингейл и предикат ипсативности (2026-08-07) [`e28ab5b36621613113583df175f5b7c5d99c854d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e28ab5b36621613113583df175f5b7c5d99c854d)
  Роза показывает расклад одного целого: секторы равных углов, радиус —
  корень доли от суммы, поэтому площадь пропорциональна значению. Радар
  для ипсативных методик вроде опросника ЧИЛ утверждает независимость
  осей, которой нет, и рисует фигуру в трети радиуса.
  
  Предикат ипсативности выводится из модели вкладов, а не объявляется
  автором: вклад приходит только от allocation-вопросов и внутри вопроса
  каждый вариант вносит одинаковую сумму по шкалам.
  
  Общая рамка диаграммы вынесена в chart-frame, чтобы радар и роза не
  разошлись в вьюпорте и раскладке подписей.

- **feat**(prd-45): обе вкладки переведены на редактор уровней (2026-08-07) [`144b1d17fcc4612286cad0be160711a16c53c0f9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/144b1d17fcc4612286cad0be160711a16c53c0f9)

- **feat**(prd-45): карточки уровней и лента покрытия вместо таблицы диапазонов (2026-08-07) [`13c98ad836928623f80179d8d0120ef12507f256`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/13c98ad836928623f80179d8d0120ef12507f256)

- **feat**(prd-45): оценка уровня рядом чипов вместо селекта (2026-08-07) [`e44ca6a5404641a29fe2ebb170808b818b6e3e81`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e44ca6a5404641a29fe2ebb170808b818b6e3e81)

- **feat**(prd-45): добавление, удаление и перестановка уровня (2026-08-07) [`7bf609aea60793cf554e06fec6c6edb163b00ce5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7bf609aea60793cf554e06fec6c6edb163b00ce5)

- **feat**(prd-45): проверки порядка границ, дыры легаси и сегменты покрытия (2026-08-07) [`ed25da027389d3fcf6b3a4099d5ce40dcde56480`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ed25da027389d3fcf6b3a4099d5ce40dcde56480)

- **feat**(prd-45): свёртка и развёртка границ уровней шкалы (2026-08-07) [`d511881dcbaefa0da522de6c4f6aa059f80679b5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d511881dcbaefa0da522de6c4f6aa059f80679b5)

- **feat**(prd-44): распределение подчиняется перемешиванию вариантов (2026-08-07) [`9424f300aec06ef720476f9d8a1f27defcbb018a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9424f300aec06ef720476f9d8a1f27defcbb018a)
  Утверждения распределения выдавались в авторском порядке, мимо карты
  перемешивания (FR-07), которой уже подчиняются остальные типы.
  
  Перемешивается только ПОРЯДОК ВЫДАЧИ строк; внутри строки остаётся авторский
  индекс — на него ключуются и ответ учащегося, и вклады в шкалы, поэтому
  перемешать сами индексы значило бы перенести баллы на чужую шкалу. Оба хоста
  передают одну и ту же карту в общий отрисовщик.

- **feat**(prd-44): обложка измерительного теста не обещает проходной балл (2026-08-07) [`642aef7785447b397b1eec579916f12510856d0b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/642aef7785447b397b1eec579916f12510856d0b)
  Порог не является заявлением намерения: он ставится по умолчанию при создании
  любого теста, и автор опросника его не открывает. PRD-29 §6.7 закрыл тот же
  вопрос экраном позже — сводка и вердикт стоят на двух признаках, «порог задан И
  есть что оценивать», — но обложки это не касалось, и опросник без единого
  верного ответа встречал ученика «проходным баллом 70%».
  
  `StartInfo.hasGradedContent` в общем сборщике `buildStartState` обнуляет
  `course.passPercent`, раскладки гасят факт своим же условием — правило одно на
  все шаблоны, включая будущие. Признак считается по СОДЕРЖИМОМУ, а не по баллам
  попытки: на старте попытки ещё нет. Пакет печёт признак только когда ответ «нет»
  — пакеты контрольных тестов остаются прежними, а чтение `!== false` сохраняет
  поведение уже собранных. Веб отвечает одним запросом на весь список тестов
  ученика. Смешанный тест оценивает и порог сохраняет.

- **feat**(prd-44): аналитика, инспектор и предпросмотр шаблона (2026-08-07) [`bb7e0ae487dec21e73ab5897e3dfb5119c5e6070`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bb7e0ae487dec21e73ab5897e3dfb5119c5e6070)
  Выгрузка попытки и текстовые форматтеры аналитики показывают распределение
  ПОЛНОСТЬЮ, вместе с нулями: ноль здесь содержателен — он отличает
  «рассмотрел и не дал веса» от «не дошёл до варианта». Эталон пуст:
  правильного распределения у типа нет.
  
  Инспектор PRD-18 получил свою подпись типа, вектор ответа целиком, верную
  величину вклада (присвоенный балл, а не фиксированная величина — иначе
  инспектор показывал бы не то, что посчитал движок) и строгий признак
  «отвечен»: частичное распределение в протоколе должно читаться так же, как
  в самом прохождении.
  
  Демо-набор шаблона получил вопрос-распределение, чтобы администратор видел
  новый интерактив в проверке работоспособности.

- **feat**(prd-44): взаимодействие SCORM и готовность в адаптивном потоке (2026-08-07) [`64ed95136c8cd9447929aff5577ffbab7168abc3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/64ed95136c8cd9447929aff5577ffbab7168abc3)
  Ответ у типа векторный, поэтому взаимодействие пишется как `other` со
  строкой «индекс[.]балл» через запятую: `numeric` описывает одно число, а
  `matching` семантически ложен — пар в ответе нет. Нули в строку попадают:
  аналитике важно отличать «поставил ноль» от «не дошёл до варианта».
  
  Адаптивный поток проверяет готовность своей функцией, и без отдельной
  ветки он пускал бы дальше с недобором там, где обычный поток не пускает.
  
  Тесты берут функции ИЗ рантайма пакета, а не пересказывают их.

- **feat**(prd-44): матрица вкладов, предпросмотр и фильтры для распределения (2026-08-07) [`a2d0abdd6bd62985a4d0b1b7f64719336ec19e3c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a2d0abdd6bd62985a4d0b1b7f64719336ec19e3c)
  Утверждения перечисляются как единицы измерения с источником
  `option_allocation` — ключ тот же индекс, что у варианта, но верных единиц
  у типа нет вовсе, поэтому отметка верности всегда пуста.
  
  Закрыт долг фазы 1: «Рассчитать по вкладам» получает спецификации бюджета.
  Без них домен вопроса-распределения схлопывался в ноль, и редактор
  предлагал бы автору заниженную границу — а домен решает, под какой полосой
  интерпретации встанет маркер.
  
  Предупреждение о связанности шкал (FR-48) стоит у ВОПРОСА, а не в списке
  шкал: связанность создаёт именно он. Учащийся делит один бюджет, поэтому
  сумма вкладов постоянна и прибавка одной шкале означает убыль другой.
  
  Градуированная цена заменена объяснением, а не показана отключённой:
  отключённый конструктор намекал бы, что настройка существует и чем-то
  заблокирована, тогда как её у типа нет.
  
  Пиктограмма типа `sliders-horizontal` доведена до отладочного плеера — там
  распределение получало иконку ранжирования по остаточному принципу.

- **feat**(prd-44): карточка вопроса-распределения в редакторе (2026-08-07) [`cf5906d1ce50b20803fdd3a41081415fe73baf93`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cf5906d1ce50b20803fdd3a41081415fe73baf93)
  Тип в списке, список утверждений и три поля — бюджет, минимум и максимум
  на вариант. Блока верного ответа у типа НЕТ и быть не может: эталонного
  распределения не существует, поэтому переключатель «есть правильный
  ответ», который несёт шкала, здесь отсутствует.
  
  Валидация называет ЧИСЛА: «распределение невыполнимо» само по себе
  оставляет автора гадать, какое из трёх полей менять. Референсный случай —
  четыре утверждения по минимуму 2 при бюджете 7 — сообщает, что нужно 8
  баллов, а бюджет 7.
  
  Та же проверка добавлена на СЕРВЕРЕ, на создании и на правке: правка
  может сделать конфигурацию невыполнимой (поднять минимум, урезать
  бюджет), и без серверной проверки о ней первым узнал бы учащийся, а не
  автор. Остальные типы намеренно не тронуты — их конфигурация невыполнимой
  стать не может.
  
  Смена типа переносит подписи и забывает бюджет с доменом (FR-49): вход на
  тип всегда начинается с умолчаний, а не с чужих чисел.

- **feat**(prd-44): экран распределения на обоих хостах (2026-08-06) [`b0449591ebe59ccc13bd08e5492e7f45f48c67dc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b0449591ebe59ccc13bd08e5492e7f45f48c67dc)
  Веб-хост и рантайм пакета зовут одну и ту же разметку и один и тот же
  живой ввод. Пакет получил обёртку без собственной разметки, ветку
  диспетчера, привязку ввода к документу и включение файла в сборку.
  
  `TemplateScreen` получил шов `onShadowReady`: жест нельзя провести через
  делегирование `data-action`, потому что ответ не должен доходить до React
  до отпускания пальца — иначе перерисовка заменит удерживаемый узел.
  
  Предзаполнение минимумом (FR-30) сделано на ОБОИХ хостах: вопрос с
  ненулевым минимумом стартует со значениями, иначе учащийся распределит
  весь бюджет и застрянет с утверждением, которое уже нечем поднять.
  
  Подсказка типа теперь получает вопрос, а не только тип, — иначе бюджет в
  ней не появится. Утверждения перемешиваются общим механизмом: ротацию,
  которую референс ведёт вручную ключом, обеспечивает настройка
  «Следование вариантов ответов».

- **feat**(prd-44): живой ввод распределения без пересборки узлов (2026-08-06) [`0cbda70b9e9245f05ab2e354b00b4d4af9dd4a67`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0cbda70b9e9245f05ab2e354b00b4d4af9dd4a67)
  Ползунок дизайн-системы — не нативный `input[type=range]`, а div с
  обработчиками указателя, и оба хоста рисуют сцену, записывая HTML в слот.
  Отдай ответ хосту на каждом pointermove — перерисовка заменит тот самый
  узел, который держит палец, и жест умрёт на первом пикселе. Поэтому жест
  правит DOM НА МЕСТЕ и молчит до отпускания, а дискретный ввод (степпер,
  поле, клавиатура) отдаёт ответ сразу: там нечего беречь.
  
  Тесты проверяют не только числа, но и ТОЖДЕСТВО узлов сквозь жест —
  свойство, которое снимком не увидеть.
  
  Арифметика вся из общей модели: потолок строки, срезание ввода по остатку
  и готовность считаются одним кодом на обоих хостах.

- **feat**(prd-44): строгая готовность ответа на обоих хостах (2026-08-06) [`f3e9661e9540f7b080a676453bb92d537d5973e1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f3e9661e9540f7b080a676453bb92d537d5973e1)
  Вопрос-распределение отвечен ТОЛЬКО когда сумма ровно равна бюджету.
  Ветка задана явно, а не оставлена на `default`, и тест это подтвердил: до
  правки `hasAnswer` возвращал `true` для любого объекта, то есть частичное
  распределение проходило как готовый ответ — вопрос выпадал из счётчика
  пропущенных и из возврата к незавершённым. Ровно тот молчаливый провал,
  о котором предупреждает риск R-1 спеки.
  
  Рантайм пакета зеркалит правило и считает его по ОБЩЕЙ модели, а не по
  своей копии арифметики: модель распределения и `renderAllocation`
  выведены в глобал `TBTemplate`, набор экспортов сторожит гард.

- **feat**(prd-44): renderAllocation в общей эмиссии интерактива (2026-08-06) [`507deb461768ac3cc1c703b7d27d0073054d5415`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/507deb461768ac3cc1c703b7d27d0073054d5415)
  Разметку группы порождает ОДНА функция, которую зовут оба хоста, поэтому
  веб-сцена и SCORM-пакет не могут разойтись.
  
  Два свойства разметки несущие. Превышение бюджета невозможно по
  построению: каждый элемент управления публикует ТЕКУЩИЙ потолок своей
  строки, поэтому состояния «перебор» нет и сообщения о нём тоже. Шкала
  ползунка при этом равна бюджету, а `aria-valuemax` — потолку: дорожка,
  растянутая до потолка, заставила бы строку прыгать при изменении
  СОСЕДНЕЙ строки, а `valuemax` в размер бюджета обещал бы клавиатурному
  пользователю место, которого нет. Недостижимый хвост нарисован, чтобы
  стоп был виден, а не нащупывался.
  
  Подсказка типа теперь зависит от вопроса, а не только от типа: она
  называет бюджет — единственное число, которое нужно знать до первого
  касания. Аргумент необязателен, остальные типы не затронуты.
  
  Экранирование в атрибутах ПОЛНОЕ, а не только кавычки: авторский
  «&lt;img onerror=…&gt;» не должен доехать разметкой и через `aria-label`.
  Поймано собственным тестом.

- **feat**(ui-kit): составной компонент BudgetAllocation (2026-08-06) [`f4521b14aa34bbe705444ccfce238106b8bfb9b5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f4521b14aa34bbe705444ccfce238106b8bfb9b5)
  Распределение бюджета между утверждениями, собранное из существующих
  примитивов: строка это Slider плюс NumberInput, новое в дизайн-системе —
  только группа с общим бюджетом.
  
  Почему группа, а не два примитива рядом: доступный максимум КАЖДОЙ строки
  равен `min(максимум на вариант, своё значение + остаток)`, то есть зависит
  от соседних строк. Это состояние группы; владей им вызывающий экран,
  синхронизацию пришлось бы повторять на каждом экране заново.
  
  Превышение бюджета невозможно ПО ПОСТРОЕНИЮ, а не отклоняется после
  ввода: ползунок и поле публикуют текущий доступный максимум. Шкала
  ползунка при этом зафиксирована бюджетом — растяни её до текущего
  потолка, и значение строки визуально прыгало бы при изменении СОСЕДНЕЙ
  строки. Подвижный стоп показан глушёной зоной, иначе ползунок упирается в
  невидимую стену.
  
  Правила добавлены в ОБЕ копии бандла: `vendor/ui-kit/css` — источник,
  `client/src/styles/vendor` — загружаемая копия.
  
  Попутно исправлена несуществующая переменная `--ou-text-title-s`, взятая
  из эскиза: объявление `font:` с неизвестным токеном отбрасывается целиком,
  поэтому счётчик остатка молча наследовал шрифт. В дизайн-системе шкала
  типографики называется `--ou-text-heading-*`. Правка внесена и в эскиз.

- **feat**(prd-44): распределение баллов в книге импорта и экспорта (2026-08-06) [`61d6ad0904db4263db8fe6189236bde8657b8c5c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/61d6ad0904db4263db8fe6189236bde8657b8c5c)
  Лист «Вопросы» принимает тип `allocation` и три новые колонки — «Бюджет
  распределения», «Минимум на вариант», «Максимум на вариант». Пустые
  минимум и максимум означают умолчания (0 и весь бюджет), заполненные у
  ДРУГОГО типа дают предупреждение: значения некуда положить, но автор явно
  ждал поведения, которого не будет.
  
  Колонка «Номера правильных ответов» у типа обязана быть пустой.
  Заполненная — ошибка строки, а не молча выброшенное значение: эталонного
  распределения у метода нет вовсе, поэтому заполненная ячейка означает,
  что автор неверно понял тип.
  
  Невыполнимая конфигурация отвергается с ЧИСЛАМИ в сообщении — сколько
  требуется и сколько доступно. Референсный случай: четыре утверждения по
  минимуму 2 при бюджете 7 требуют 8 баллов из 7.
  
  Лист «Вклады вопросов» получает источник «распределение». Ключ — индекс
  утверждения, как у «варианта»: утверждения лежат в том же списке. Пустой
  коэффициент теперь означает 1 — в опроснике из 56 строк вкладов единица
  стоит в каждой, и требовать её явно значит требовать 56 одинаковых ячеек.
  
  Градуированная цена к типу неприменима и говорит об этом прямо, а не
  позволяет грамматике разобраться и молча ничего не посчитать.
  Предупреждение о цене на измерительном вопросе стало типозависимым: у
  шкалы причина в отсутствии правильной градации (появится — цена оживёт),
  у распределения в самой природе типа — оживать нечему.
  
  Приёмка A-03 закрыта тестом: опросник ЧИЛ собирается книгой целиком — 14
  вопросов по 4 утверждения, четыре шкалы, 56 строк вкладов, ни одной
  ошибки.
  
  Попутно исправлено: `QUESTION_WIDTHS` позиционно параллелен заголовкам, и
  три новые колонки требовали трёх ширин; строки-примеры шаблона несут все
  колонки, потому что книга берёт набор колонок из первой строки — пропуск
  ключа стирал колонку целиком. Тест «примеры покрывают все типы» больше не
  хранит рукописный список из пяти.

- **feat**(prd-44): ведущая и слабая шкала в языке формул (2026-08-06) [`527f49f37a98fc553ad8d00c30ebc23f63d1fe46`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/527f49f37a98fc553ad8d00c30ebc23f63d1fe46)
  Источники `topScale(["ключи"], место).свойство` и `bottomScale(...)`.
  Форма повторяет существующий `countScales`, расширенный доступом к
  свойству: `key`, `label`, `value`, `margin`, `tiedCount`.
  
  Три правила, и каждое продиктовано референсом, а не вкусом:
  
  \- рейтинг строится по НОРМАЛИЗОВАННОМУ значению. Сырые значения шкал с
    разными доменами несравнимы, а обратное направление уже учтено в
    нормализованном;
  \- ничья решается АВТОРСКИМ порядком шкал теста, а не порядком ключей в
    формуле, — иначе один и тот же ответ при пересчёте в вебе, в пакете и
    из снапшота дал бы разного лидера;
  \- ничья видима. В контрольном заполнении опросника два стиля набрали по
    34, и файл разрешает это молча: `MATCH(MAX(...))` отдаёт победу той
    колонке, что левее. `tiedCount` и `margin` позволяют отчёту сказать
    «два равно выраженных стиля» вместо выдуманного победителя.
  
  Отрыв считается до следующего ОТЛИЧАЮЩЕГОСЯ значения: при ничьей сосед по
  списку — это сама делящая место шкала, и нулевой отрыв читался бы как
  «выражены одинаково» у места, которое на деле оторвалось от остальных.
  
  Попутно исправлено сравнение с `null` в языке формул. Оно уходило в числа,
  где `toNum(null)` и нечисловая строка одинаково дают 0, поэтому
  `topScale(...).key = "cel"` отвечал ИСТИНА на ПУСТОМ рейтинге — отчёт
  назвал бы ведущую шкалу, которой нет. Теперь `null` равен только себе.
  Ошибка была и до PRD-44 (тот же путь у `var()`), но проявиться могла
  только с источником, который возвращает null рядом с кодом исхода.
  
  Всё повторено в JS-двойнике; golden-корпус вырос на 15 случаев, включая
  ничью, шкалу без значения, пустой рейтинг и место за пределами рейтинга.
  Тест паритета признаков обновлён: измерительных типов теперь два.

- **feat**(prd-44): паритет JS-двойника движка шкал (2026-08-06) [`6916d3741faadac2585224fc6a34420311d80f07`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6916d3741faadac2585224fc6a34420311d80f07)
  Правила вклада и домена перенесены в плоский двойник рантайма пакета
  дословно. Golden-тест паритета получил пять сценариев распределения —
  без них расхождение было бы невидимым: двойник со старым правилом
  `value * weight` молча оценивал бы любое распределение как «один балл за
  каждое тронутое утверждение», и ни один существующий сценарий этого не
  ловил.

- **feat**(prd-44): вклад распределения в шкалы и домен шкалы (2026-08-06) [`c0e211370acf7d0a926b4d6132198d24fb8ea0c4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c0e211370acf7d0a926b4d6132198d24fb8ea0c4)
  Источник `option_allocation`: единица активна, когда учащийся присвоил
  утверждению ненулевой балл, а её вклад равен `балл * коэффициент * вес`.
  Это первый случай, когда величину вклада задаёт учащийся, поэтому
  «сработала» и «внесла» перестали быть одним фактом: обе точки расчёта
  (выгрузка по ответу и агрегат) теперь спрашивают одну функцию
  `unitContribution`, иначе разошлись бы.
  
  Домен шкалы считается отдельной ветвью. Наивная сумма максимумов
  вариантов завышена и завышена опасно: утверждения делят ОБЩИЙ бюджет и
  не достигают своих потолков одновременно. На референсе наивная величина
  даёт 392 балла на стиль там, где методика говорит 98, а домен читают и
  нормализация в проценты, и полосы интерпретации PRD-29 — то есть ошибка
  сместила бы ВЕРДИКТ, а не число. Точные границы берутся из жадного
  решения: старт с минимума, пул льётся в наибольший коэффициент, потолок
  суммы ограничен и бюджетом, и тем, что остальные утверждения обязаны
  получить свой минимум.
  
  Бюджеты едут в составе конфигурации оценивания, а не отдельным
  аргументом: бюджет ограничивает домен, значит он такая же часть «как
  считается этот тест», как и вклады. Читаются они ЧЕРЕЗ источник
  конфигурации, поэтому попытка по снапшоту нормализуется бюджетом, с
  которым тест был опубликован.
  
  Приёмка A-02: контрольное заполнение опросника ЧИЛ, снятое с листа
  «Механика подсчета результатов», даёт 34/16/14/34 при сумме 98 — полное
  совпадение с итогами файла, а домен одного стиля равен 0..98, как и
  сказано в примечании файла. Ничья двух стилей по 34 в фикстуре реальна:
  файл разрешает её молча, отдавая победу левой колонке.

- **feat**(prd-44): dataJson распределения и тип в перечислениях схемы (2026-08-06) [`cf06f5c59667c8926631bfb0ea573da1c2f46c39`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cf06f5c59667c8926631bfb0ea573da1c2f46c39)
  `allocation` добавлен в четыре перечисления типов вопроса и в перечень
  источников вклада (`question_measurements.source_type` получает
  `option_allocation`). Миграция базы НЕ требуется: все три колонки —
  `text NOT NULL` без ограничения `CHECK`, перечисление живёт только в
  TypeScript и Zod, поэтому его сторожат тесты, а не база.
  
  `allocationDataSchema` проверяет границы полей и выполнимость
  конфигурации на сохранении: без этой проверки автор сохраняет вопрос,
  который не заполнить ни одним способом. Сообщение называет числа —
  сколько требуется и сколько доступно.
  
  Компилятор поймал пять карт, ключованных типом вопроса (иконки и подписи
  в дереве содержимого, вкладках «Оценка», «Вклады вопросов» и вариантов).
  Ровно ради этого перечень типов остаётся исчерпывающим `Record`, а не
  свободным индексом: новый тип обязан быть объявлен явно.

- **feat**(prd-44): чистая модель распределения бюджета (2026-08-06) [`2544a4fe58de39ff87d78d19c2e4db6abca3a419`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2544a4fe58de39ff87d78d19c2e4db6abca3a419)
  Единственный источник арифметики распределения: спецификация из
  `dataJson`, выполнимость конфигурации, предзаполнение минимумом, остаток,
  доступный максимум варианта, ввод значения и строгая готовность ответа.
  
  Два правила задают форму модуля. Превышение бюджета невозможно ПО
  ПОСТРОЕНИЮ: доступный максимум варианта равен `min(maxPerOption,
  текущее + остаток)`, поэтому состояния «перебор» не существует ни у
  ползунка, ни у клавиатурного ввода, ни у вставки. Тронутый ответ несёт
  запись для КАЖДОГО утверждения, включая нулевые: аналитике нужно отличать
  «поставил ноль» от «не дошёл до варианта».
  
  Выполнимость возвращает числа вместе с вердиктом, чтобы сообщение автору
  называло, сколько требуется и сколько доступно.

- **feat**(prd-44): тип allocation и признак distributesBudget (2026-08-06) [`aa1c329bd3d37d4dd831179065fad3801ab8ce4d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/aa1c329bd3d37d4dd831179065fad3801ab8ce4d)
  Классификатор типов вопроса получает `allocation` и новый признак
  `distributesBudget`. `isMeasurementOnly` возвращает для типа `true`
  безусловно: у метода нет эталонного распределения, поэтому случайный
  `correctIndex`, оставшийся от смены типа в редакторе, не должен делать
  вопрос проверяемым.
  
  Мера против риска R-1 спеки: тест-матрица «тип x признак» проверяет
  КАЖДЫЙ признак на КАЖДОМ типе, поэтому тип, добавленный без решения по
  признаку, падает тестом, а не молча уходит в ветку `default`.
  
  Правки повторены в ES5-зеркале рантайма пакета (app/utils/qtype.js) и в
  публичной поверхности TBTemplate.
  
  Добавлен план реализации с инвентарём точек ветвления по типу вопроса.

- **feat**(results): шкалы и показатели на адаптивном экране итогов и в отчёте (2026-08-06) [`b64347a74b20f56ff8e9c4432a34969948cb91ab`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b64347a74b20f56ff8e9c4432a34969948cb91ab)
  Закрывает issue #33. Шкалы (PRD-5) и показатели (PRD-2) адаптивной попытки
  считались и уезжали в LMS псевдовзаимодействиями scale_*/var_*, но ученику не
  показывались нигде: контекст адаптивного экрана их не нёс, а в вебе они и вовсе
  не считались.
  
  Экран итогов (оба хоста, оба шаблона): уровни тем → показатели → шкалы (с
  радаром) → рекомендации. Состав решают те же два факта, что и в обычном режиме,
  — видимость измерения и трёхпозиционные настройки вида «Итоги»; настройка сводки
  баллов на этом экране не читается, сводки у него нет.
  
  Веб считает значения при завершении адаптивной попытки и хранит их вместе с ней
  (adaptiveAttemptResultSchema.scaleResults/resultVariables) — пересчёта на чтении
  нет, как и в обычном режиме. Формулы показателей получают адаптивный результат
  через одно общее правило adaptiveResultAsStandard, которым уже пользовался пакет:
  один отвеченный вопрос — один балл, тема «пройдена» при подтверждённом уровне.
  controls_status адаптивный вердикт не переопределяет — его выносит лестница.
  
  PDF-отчёт: блок «Ваш результат» добавлен в макеты обоих режимов, адаптивный
  контекст отчёта наполняет свёрстанный, но мёртвый блок «По шкалам», а SCORM-пакет
  начал передавать измерения сборщику отчёта — он не передавал их НИ В ОДНОМ
  режиме, поэтому в LMS документ выходил без измерений и у обычного теста.
  
  Приёмка: экран проверен в браузере на живом адаптивном тесте с тремя шкалами и
  показателем (веб) и в локальном плеере SCORM; контекст отчёта пакета отдаёт
  шкалы, показатели и радар на 3 оси.

- **feat**(users): приглашение на активацию уходит при создании пользователя (2026-08-06) [`a94ebc2a64b41220c04d1d229252a89e10018d6f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a94ebc2a64b41220c04d1d229252a89e10018d6f)
  Одиночное создание учётной записи не слало ничего: письмо-приглашение было
  доступно только массовому импорту и ручному пункту меню для pending-аккаунта,
  поэтому заведённый по одному человек не узнавал, что у него есть доступ.
  
  В форму добавлен чек-бокс «Отправить приглашение» (включён по умолчанию, как в
  мастере импорта): POST /api/users принимает sendInvite, выпускает тот же
  семидневный токен установки пароля, что и повторная отправка, и возвращает
  inviteSent. Сбой почты не роняет запрос — учётная запись уже создана, автор
  видит предупреждение и может повторить отправку из меню строки.

- **feat**(tests-list): вынести «Выполнить отладку» из меню в пиктограмму строки (2026-08-05) [`a67b9970788c1f3108d33c48c29c6fa2cea58e54`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a67b9970788c1f3108d33c48c29c6fa2cea58e54)
  Прямой доступ к отладочному прогону теста рядом с редактированием — не
  нужно открывать «Ещё действия» ради частой операции.

### Fixes

- **fix**(templates): измерительный отчёт больше не печатает баллы и вердикт (2026-08-10) [`9da58b202d49c71bb1417849ca82189eb8fa377e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9da58b202d49c71bb1417849ca82189eb8fa377e)
  Опросник без правильных ответов выдавал документ с зелёным «Тест пройден» и
  сводкой «0 вопросов / 0.0 баллов / 0 %». Правило PRD-29 §6.7 («порог задан И
  есть что оценивать») экран уважал, а отчёт — нет, хотя §5.2 запрещает ему
  показывать иной вердикт, чем экран, с которого его скачали. Сводка теперь под
  тем же гейтом, шапка без вердикта печатает «Результаты теста» — формулировку,
  которую уже использует адаптивный отчёт, — а плашка исчезает целиком.
  
  Здесь же:
  
  \- узкая колонка толкований: сетка «диаграмма слева, список справа» включается
    МОДИФИКАТОРОМ по тому же признаку, каким гейтится сама фигура. Без него
    единственный ребёнок — список толкований — занимал колонку под диаграмму,
    240 px из 545 доступных, и текст уходил вниз лентой;
  \- вводный блок первым в обеих выдачах и печать авторской разметки через
    `{{& … }}`;
  \- переводы строк авторских текстов держатся (`pre-wrap`): абзацы, которыми
    автор разделил объяснение, доходят до слушателя;
  \- манифесты помечают содержательные поля вида `scope: "content"` — по этому
    признаку редактор раскладывает настройки отчёта по двум экранам.
  
  Паритет макетов обоих шаблонов сохранён побайтово, превью пересобраны.

- **fix**(templates): затемнение оверлея токеном ДС, паритет макетов, сторож палитры (2026-08-09) [`4e1f0f79d8629615b702dfb73977b01d606cc27f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e1f0f79d8629615b702dfb73977b01d606cc27f)
  Слой сцены обязан брать палитру только из --ou-*, а в theme.css с PRD-38 жил
  литерал rgb(0 0 0 / 0.85) — затемнение полноэкранного просмотра медиа. Взят
  --ou-bg-backdrop, которым ДС затемняет фон модалки и ящика: затемнение стало
  легче (0.66 тёмная, 0.48 светлая), зато следует теме.
  
  results.html «Сертификации» нёс старую редакцию комментария PRD-46 — приведён
  к «Стандартному», паритет этих макетов держится вручную.
  
  Сторож порядка палитр (PRD-23) требовал, чтобы блок темы был последним листом
  в теневом корне; последним стал лист защиты, палитры не несущий. Проверка
  сужена до листов, которые палитру несут.

- **fix**(prd-34): смоук снова ловит пустой экран (2026-08-09) [`3b2c96dae720297a856eef822563b35980da4436`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3b2c96dae720297a856eef822563b35980da4436)
  applyProtection вставляет свой &lt;style&gt; в каждый отрисованный корень безусловно,
  поэтому root.innerHTML не бывает пустым — и проверка «Экран отрисован пустым»
  замолчала. Молча: экран, не отрисовавший ничего, выглядел для гейта активации
  ровно как отрисовавший всё, и ломаный макет прошёл бы в LMS.
  
  Перед вердиктом из корня вычитается то, что добавил рантайм; атрибут листа
  защиты вынесен в экспорт, чтобы строка не жила копиями в двух модулях.
  
  Тем же коммитом поправлена арифметика ожидаемых строк: проверка добавляет ещё
  строку якоря водяного знака (PRD-34), в ожидании её не было.

- **fix**(tests): фикстура pglite догнала схему — 83 падения ствола (2026-08-09) [`a5e6b5f573c5354301b80378c3af195df1e8326c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a5e6b5f573c5354301b80378c3af195df1e8326c)
  У таблицы tests в tests/it/schema.sql не было question_order (PRD-30,
  drizzle/0011) и quick_advance (PRD-43, drizzle/0012), поэтому любой insert
  в pglite падал и уносил все репозиторные наборы. Настоящие миграции в порядке —
  отставала только фикстура.
  
  Заодно снят DEFAULT/NOT NULL с test_sections.question_order: та же миграция
  0011 сделала колонку nullable (NULL = «как в тесте»), а сторож сверяет наличие
  колонок, не их обязательность, и поймать это не мог.

- **fix**(prd-46): редактор читает ветку отчёта без ключа варианта (2026-08-09) [`db6c29d145512ac78281f809c772aaf64b599386`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/db6c29d145512ac78281f809c772aaf64b599386)
  Настройки отчёта существовали до появления вариантов (PRD-27), и такие ветки
  лежат в базе без variantKey — в dev-БД это две записи из трёх. Сервер их читает
  (отсутствие ключа разрешается вариантом с isDefault), а редактор отбрасывал,
  поэтому автор видел пустую карточку там, где настройка есть и работает.

- **fix**(prd-46): галочка радара действует, пока новое поле не тронуто (2026-08-09) [`fc030346f0c73330105d298bdd7200b3974563a1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fc030346f0c73330105d298bdd7200b3974563a1)
  Отчёт теста, настроенного переключателем PRD-35, перестал печатать диаграмму,
  когда PRD-46 добавил варианту поле scalesChartKind: resolveReportValues
  материализует умолчание манифеста ("none"), а явная строка старше булева флага.
  К моменту чтения вида «автор не трогал» и «автор выбрал нет» уже неразличимы,
  поэтому перенос считается там, где авторские значения ещё на руках.
  
  Экрана это не касалось: настройки страницы хранят только сохранённое автором.

- **fix**(prd-47): предпросмотр отчёта показывает блок измерений (2026-08-09) [`1d790aeb6bda97a924f169d23f9613708f8dc006`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1d790aeb6bda97a924f169d23f9613708f8dc006)
  Пробел нашла приёмка в браузере, а не тесты: план и спека называли двух
  потребителей контекста отчёта — предпросмотр из shared/report и смоук шаблона, —
  а модальное окно «Предпросмотр отчёта» строит контекст СВОЙ, третьим местом. Оно
  и осталось без измерений: автор видел страницу без блока шкал и без диаграммы,
  хотя обучающемуся они уже приезжали.
  
  Демо-измерения берутся из демо-набора шаблона, который окно и так загружает
  вместе с макетом и стилями, а вид диаграммы — из полей ОТЧЁТА через общий
  сборщик: подмена видом с экрана показала бы автору не тот документ, что уйдёт в
  PDF. Шаблон без демо-измерений рисует отчёт как раньше — это отдельный тест.
  
  Заодно поправлена сама проверка: она искала классы блока по ВСЕЙ странице и
  находила их в редакторе за модалкой, то есть давала ложное «да». Теперь смотрит
  только внутрь сцены предпросмотра.

- **fix**(prd-47): отчёт в SCORM-пакете получает измерения и диаграмму (2026-08-08) [`f666fe88cb0e841ba4adcd3afe9938b6a1b3bf20`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f666fe88cb0e841ba4adcd3afe9938b6a1b3bf20)
  opts отчёта в пакете собирался из design и values — измерений там не было вовсе,
  поэтому в LMS печатался документ без блока измерений ЦЕЛИКОМ: без карточек шкал,
  без показателей, без профиля. Дело было не в настройке вида, а в отсутствии данных
  в контексте.
  
  Вход экрана рантайм уже собирает (currentAttemptMeasures в viewResults.js);
  отчётным его делает ТОТ ЖЕ сборщик, что на вебе, — он добавлен в экспорты бандла,
  потому что файлы template/app ходят к ядру только через window.TBTemplate.
  
  Проверено на собранном пакете: в app.js есть и экспорт в карте бандла, и
  определение сборщика, и вызов из pdfExport.js. Файлы приложения склеиваются в один
  скрипт без обёрток, поэтому currentAttemptMeasures виден из pdfExport; вызов всё
  равно закрыт проверкой typeof — пакет собирается и без адаптивных файлов.

- **fix**(prd-47): «авто» в отчёте получает признак ипсативности (2026-08-08) [`e52acf58e67e3026248be9a0905c761cbdf4cbcd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e52acf58e67e3026248be9a0905c761cbdf4cbcd)

- **fix**(prd-47): облик шкал доезжает из экрана в веб-отчёт (2026-08-08) [`823e6e2d6697a311b5a0e30fac3bb38469eea908`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/823e6e2d6697a311b5a0e30fac3bb38469eea908)

- **fix**(media): загрузка книги переиндексирует вложения шкал и показателей (2026-08-08) [`4ac53e9bcf8b2814cfc886a80765b96acb70ee81`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4ac53e9bcf8b2814cfc886a80765b96acb70ee81)
  Индекс использования медиа держат маршруты шкал и показателей, а импорт книги
  пишет в хранилище напрямую и мимо них. Пока импорт сносил конфигурацию шкалы
  целиком, пункт был безобиден. Он стал заметен ровно наоборот: теперь книга
  удаляет ОТДЕЛЬНЫЙ уровень, вложение уходит вместе с ним, а строка media_usages
  остаётся и продолжает выдавать ученику файл, которого в тесте больше нет. Индекс
  не запрещает, а разрешает — в этом направлении расхождение и опасно.
  
  Оба помощника переехали из маршрутов в usage-index.ts, к остальной работе с
  индексом, и вызываются всеми, кто пишет эти сущности. Копия помощника на каждого
  писателя — это и есть механизм, которым один из писателей оказывается забыт;
  здесь им оказался импорт.
  
  Переиндексация набора самоисцеляющая: строки уровня, шкалы или показателя,
  которых больше нет, просто не возвращаются. Предпросмотр книги индекс не трогает
  — он вообще ничего не пишет.
  
  Показатель книгой пока не меняется (parseResultVariableRow не кладёт configJson),
  но книга их СОЗДАЁТ, и правило не должно зависеть от того, для каких полей у листа
  сегодня есть колонка.
  
  Тест сверен на красноту: без вызова из импорта он падает.

- **fix**(scales): у уровня шкалы обязан быть код — он и есть его опознание (2026-08-08) [`7c9c74d3680766fe12176e4ee51bb421766d7e9f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7c9c74d3680766fe12176e4ee51bb421766d7e9f)
  Правило уже жило в этом же модуле, но только для соседней структуры: asOutcomes
  отбрасывает исход без кода, а asBands уровень без кода пропускал, хотя уровень
  без границ отбрасывал. Одна защита, два противоположных ответа на один вопрос.
  
  Пустой код — не «поле, которое автор не заполнил», а запись, которую нельзя
  опознать. Такой уровень не помечается текущим на линейке (band.level ===
  currentLevel вырождается, и оговорка currentLevel !== "" гасит подсветку у всех),
  на него не сослаться из формулы показателя, в LMS он уезжает пустым
  scale.&lt;ключ&gt;.level, и при загрузке книги за ним не удержать толкование. Метка при
  этом остаётся необязательной: каждый читатель падает на label ?? level.
  
  Отсюда четыре правки:
  
  1. asBands отбрасывает уровень без кода — зеркало asOutcomes.
  2. Гейт сохранения даёт band_code на уровень без кода, поэтому автор не может
     сохранить то, что путь показа выбросит.
  3. Карточка показывает «Укажите код уровня» под самим полем и объясняющий баннер;
     читатель редактора остаётся терпимым, чтобы легаси-уровень был виден и
     починяем, а не исчезал под автором.
  4. Добавление уровня подставляет свободный level1/level2 — обязательность не
     должна встречать автора ошибкой под полем, до которого он ещё не дошёл.
  
  Плюс грамматика книги: код больше не может начинаться с кавычки. Уровень без кода
  выгружался как «0..15  «Низкий»», и жадный разбор читал подпись как КОД, молча
  переименовывая уровень в его собственную подпись. Выгрузка читает config.bands
  напрямую, мимо asBands, поэтому страховка нужна независимо от пункта 1.
  
  Приёмка в браузере (dev из worktree, тест ЧИЛ): стёртый код даёт красное поле,
  баннер шкал, отметку на вкладке и заблокированное сохранение; добавленный уровень
  приходит с кодом level1 и ошибки не даёт. Уровней без кода в базе ноль.

- **fix**(workbook): импорт книги больше не стирает интерпретацию шкалы (2026-08-08) [`97bd6aa5e5fc866164f5e5b9af5116cecb4677a2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/97bd6aa5e5fc866164f5e5b9af5116cecb4677a2)
  Загрузка листа «Шкалы» замещала config_json значением { bands }, поэтому круг
  «выгрузил → загрузил» без единой правки сносил домен, валентность, предел показа
  и тексты, тональности и вложения обратной связи уровней. Показатель в том же
  импорте вёл себя правильно и служил образцом: configJson он не кладёт вовсе.
  
  Правило: книга задаёт целиком те поля, у которых есть колонка, и не трогает те,
  у которых её нет. Отсюда слияние в Pass 2 импорта, а не в разборе строки —
  парсер видит одну строку и не знает сохранённого, а existing уже в руках.
  
  Уровни сшиваются по КОДУ уровня: границы и подпись книга выражает, а текст,
  тональность и обратная связь колонки не имеют и переносятся с сохранённого
  уровня. Код, которого в книге больше нет, теряет содержимое намеренно.
  
  Различие «нет колонки» и «пустая ячейка» несёт смысл: ключ, отсутствующий в
  разобранной строке, означает «книга это поле не задаёт», а пустая ячейка
  существующей колонки — «удалить». Поэтому bands кладётся ВСЕГДА, в том числе
  пустым массивом: иначе очищенная «Диапазоны» молча возвращала бы старые уровни,
  а это единственный способ автора снять уровни.

- **fix**(prd-46): предел показа шкалы доезжает в SCORM-пакет (2026-08-08) [`546db613a3c37e6fc4418d66e8196e68d7b1286f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/546db613a3c37e6fc4418d66e8196e68d7b1286f)
  Поймано приёмкой в пакете: шкала печётся ПЛОСКОЙ — домен, направление и уровни
  ложатся полями рядом с ключом, а рантайм перечитывает их тем же общим парсером.
  `displayMax` в этот список не попал, поэтому пакет молча рисовал бы другую
  фигуру, чем веб, при одинаковых настройках теста — ровно то расхождение, которое
  PRD-29 §9 называет дефектом.
  
  Ключ пишется только когда автор задал предел: `null` на каждой шкале менял бы
  байты каждого пакета с измерениями ради поля, которым почти никто не пользуется.

- **fix**(prd-46): облик настраивается и при «авто», а не только при явной розе (2026-08-08) [`f8dd8e824268143227d09f81354a50ecece2b0fd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f8dd8e824268143227d09f81354a50ecece2b0fd)
  Ограничение пришло из пункта 7 плана и таблицы согласованного эскиза, но его
  собственное обоснование — «при радаре настраивать нечего» — разбирает два
  значения настройки из четырёх. Третье, `авто`, на ипсативной методике даёт
  розу: это ровно тот сценарий, ради которого PRD-46 и написан (опросник ЧИЛ).
  
  Автор такого теста видел розу у ученика и не мог её оформить: чтобы добраться
  до блока, приходилось приколоть вид явной «розой» — а это отказ от
  автоматического отката на радар, если модель перестанет быть ипсативной.
  
  При `нет` и `радар` блок по-прежнему не появляется, поэтому обоснование пункта
  сохраняется целиком. Там, где `авто` разрешается в радар, карта просто не
  читается — это ничего не стоит.
  
  Правило вынесено под тест: случаев вида четыре, и решает не «выбрана ли роза»,
  а «может ли роза быть нарисована».

- **fix**(prd-46): реестр типов и гард литералов приведены в согласие (2026-08-08) [`21d0394edc8df3292821bac1bc374b4d50edfa84`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/21d0394edc8df3292821bac1bc374b4d50edfa84)
  Полный прогон поймал два расхождения, которых не видели точечные сьюты.
  
  Реестр типов поля объявлен списком в тесте — новый тип обязан быть добавлен
  туда явно, иначе закрытость реестра ничем не сторожится.
  
  Нейтральный цвет для выключенного поля был hex-литералом и уронил гард «цвет
  приходит из токенов». Литерал не нужен вовсе: у ДС есть собственное состояние
  «без цвета», и оно точнее серого — там, где цвет назначает схема уровней или
  слот палитры кончился, цвета просто НЕТ.

- **fix**(prd-46): чип цвета говорит правду о том, что нарисует роза (2026-08-08) [`04bfe459600d1253ebff41325cc72ff25921a3f7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/04bfe459600d1253ebff41325cc72ff25921a3f7)
  Найдено приёмкой в браузере: у всех четырёх непокрашенных шкал чип показывал
  один и тот же #8F6AE6, тогда как превью рядом рисовало четыре разных сектора.
  Умолчание чипа — это утверждение о цвете, который получится, поэтому оно
  обязано быть СЛОТОМ палитры: слот закреплён за позицией, и именно его рисует
  рендерер, пока автор не задал своего.
  
  Там же второй случай того же рода: при объявленном направлении поле выключено,
  но образец продолжал показывать оттенок идентичности — единственный цвет,
  которого на розе точно не будет. Выключенный чип берёт нейтральный.

- **fix**(prd-26): измерительный вопрос не получает вердикта в протоколе (2026-08-08) [`02baa909d55fd4565d4a0c75c0c789338071e363`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/02baa909d55fd4565d4a0c75c0c789338071e363)
  Протокол ставил такому вопросу «неверно» и «0 / 1». Эталона у него нет, поэтому
  читалось это как ошибка ученика — и расходилось с агрегатом, который такой вопрос
  не считает ни заработанным, ни возможным баллом (FR-08). Единственный его
  результат — вклад в шкалы, он в строке и остаётся.
  
  Правило «что считается измерительным» живёт в общей вычислялке, а не в
  рендерерах: её читают и веб, и CLI-плеер, и второй экземпляр правила разошёлся бы
  с первым. Ключ ответа принимается и как `correct` (запечённый payload), и как
  `correctJson` (сервер), чтобы вызывающему не приходилось переделывать вопрос.

- **fix**(prd-45): гард контраста не зависит от окончаний строк (2026-08-08) [`fe8c0bc91aa8439b0721d285c0660b430f4ffba0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fe8c0bc91aa8439b0721d285c0660b430f4ffba0)
  THEME_BLOCKS описывает многострочные селекторы через \n, а на Windows при
  core.autocrlf=true таблица стилей выкладывается с CRLF. Гард не находил блок,
  который лежит прямо перед ним, и падал с «структура токенов изменилась» — то
  есть был красным на любом свежем клоне, а вина падала на то, что влилось
  последним. Обнаружено при слиянии PRD-45 и PRD-46 в main: в главной рабочей
  копии файл лежит с LF, в свежем worktree — с CRLF, и гард вёл себя по-разному
  на одном и том же коммите.

- **fix**(prd-46): чипы облика выстроены в колонки по левому краю (2026-08-07) [`a85fc9cc2312df6e2441147cc7f7e1872d5e0214`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a85fc9cc2312df6e2441147cc7f7e1872d5e0214)
  Во flex-строке чипы уезжали к правому краю и вставали на разной
  вертикали: ширина чипа зависит от текста — «#8F6AE6» против «По схеме
  уровней», «target» против «users-round», — и колонка переставала
  читаться колонкой. Сетка с фиксированными ширинами под самый длинный
  вариант каждого столбца и прижатием влево.

- **fix**(prd-46): концы осей выходят за внешнее кольцо (2026-08-07) [`237b6f56eb5147f03699bf49d35aa8368325ae50`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/237b6f56eb5147f03699bf49d35aa8368325ae50)
  Луч, обрывающийся ровно на кольце, читается так, будто кольцо его
  срезало, и обе линии дерутся за один пиксель в точке встречи. Короткий
  выпуск говорит другое: луч — граница между шкалами, кольцо — шкала
  целого, и они просто пересекаются. Выпуск оставлен далеко внутри кольца
  подписей, чтобы ничего не упиралось в текст.

- **fix**(prd-46): кольца сетки вернулись и рисуются поверх секторов (2026-08-07) [`fd97381502f953baafccc7aa14cf83d26a304427`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fd97381502f953baafccc7aa14cf83d26a304427)
  Поверх, а не под: заливки розы непрозрачны, поэтому кольца под ними
  видны только в промежутках между короткими секторами — там, где говорят
  меньше всего. Над заливками они читаются как одна шкала, положенная на
  всю фигуру.
  
  Кольцо подписей вернулось на «поле + отбивка»: раз сетка снова доходит
  до края поля, подписи обязаны стоять за ней, иначе внешняя окружность
  идёт через их строки.

- **fix**(prd-46): роза без колец сетки (2026-08-07) [`ddd02e140c96f4fe828ac8a0fe68c5dddd474416`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ddd02e140c96f4fe828ac8a0fe68c5dddd474416)
  Подписать их нельзя — у разных шкал разные интервалы, общая подписанная
  сетка солгала бы о большинстве. Внешнее кольцо отмечает состояние, в
  ипсативной методике недостижимое: одна шкала забрала весь бюджет. А
  после переноса подписей к фигуре оно пошло прямо через их строки.
  
  Заодно ореол пустоты до края поля перестал делать фигуру визуально
  мелкой: остаются секторы, сравниваемые друг с другом, ради чего
  диаграмма и рисуется.

- **fix**(prd-46): эталонное кольцо убрано, фигура укрупнена (2026-08-07) [`3e7f617ebc1a31e21ba06fc859a9c3adc2d5c522`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3e7f617ebc1a31e21ba06fc859a9c3adc2d5c522)
  Пунктирное кольцо не читалось даже при знании замысла и предлагало
  сравнивать долю с механической величиной 1/N, у которой методического
  смысла нет. Вывод уже несут метки уровня, взятые из интервалов, которые
  задал автор методики, — знак дублировал их худшим способом.
  
  Подписи переехали с кольца радиуса ПОЛЯ на кольцо самой фигуры:
  max(радиус сектора) + отбивка. Сектора реального профиля до края поля не
  дорастают (треть целого даёт 0.59 радиуса), поэтому между фигурой и её
  собственными подписями висел пустой пояс шириной в треть чертежа.
  
  Колонка диаграммы расширена с 360 до 480: вьюпорт в 360 единиц нужен
  подписям шестиосевой диаграммы, а сама фигура — круг радиуса 100 в его
  середине, и в колонке 360px она выходила около 120px.
  
  Радар не изменился ни на пиксель: радиус кольца подписей стал параметром
  placeCaption, радар передаёт прежнее значение.

- **fix**(prd-46): карточка шкалы больше не наследует сетку формы (2026-08-07) [`40d03dc4bcf420ed1db99203cee00968a570428e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/40d03dc4bcf420ed1db99203cee00968a570428e)
  ou-formsection — разметка ФОРМЫ: первая колонка 280px задумана под
  подпись и описание поля, элементы управления стоят рядом. Карточка шкалы
  не форма, а показание: слева две короткие строки (имя и метка уровня),
  справа высокая колонка с линейкой, засечками и толкованием, поэтому
  280px плюс 48px отбивки простаивают вдоль всей строки, и чем шире экран,
  тем больше пустоты.
  
  Карточка становится одноколоночной всегда, а не только на узкой сцене.
  Контейнерный запрос на 700px удалён — схлопывать больше нечего. Линейка
  получает полную ширину блока, что показанию и нужно.
  
  Правка внесена в компонентную CSS обоих шаблонов, поэтому действует
  разом в вебе, в SCORM-пакете и в PDF-отчёте.

- **fix**(prd-46): эталонное кольцо подписано (2026-08-07) [`fbf32c6d784705d94ede2879523ec857460a11cd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fbf32c6d784705d94ede2879523ec857460a11cd)
  Пунктирная окружность без подписи — знак, смысл которого с картинки не
  восстановить: первый же читатель эскиза спросил, что это. Подпись
  «пунктир — ровная доля» приходит из ядра, цифр не содержит, поэтому
  запрет на числа на диаграмме не нарушается.

- **fix**(prd-46): эскиз рендерится — токены ДС требуют класса ou на body (2026-08-07) [`e2ea3fb05d4cb5b20ce67d26f45dc8f236590319`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e2ea3fb05d4cb5b20ce67d26f45dc8f236590319)
  Семантические токены дизайн-системы объявлены под селектором .ou.ou--dark,
  а не в :root. Без класса на body КАЖДОЕ var(--ou-*) невалидно и молча
  отбрасывается: страница уходила в засечный шрифт, рамки, плашки и рельсы
  слайдера исчезали, а уцелевали только правила без токенов — отсюда
  видимые заливки секторов при полностью развалившемся остальном.
  
  Заодно: отступ состояний считается от высоты навигации с кнопками, иначе
  шапка сцены уходит под неё; подпись предела оси убрана в угол — под
  нижней осью она вплотную подходила к подписи шкалы.

- **fix**(prd-45): дробный порог не убивает сохранение молча, полосы показателя проходят шлюз (2026-08-07) [`a7d9ab5ffa92f2356f71e9c01dd0bbad3b4b5b9b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a7d9ab5ffa92f2356f71e9c01dd0bbad3b4b5b9b)

- **fix**(prd-35): подписи осей радара не обрезаются краем холста (2026-08-07) [`c017bb792de7ad6540a43ee57f8e5639c9806781`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c017bb792de7ad6540a43ee57f8e5639c9806781)

- **fix**(prd-45): лента покрытия показывает тон, выведенный по направлению шкалы (2026-08-07) [`efe5001682e63a23b3f26cf0ab6697edf95cfa98`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/efe5001682e63a23b3f26cf0ab6697edf95cfa98)

- **fix**(prd-45): подписи ленты не режутся посередине слова, терминология шлюза сохранения (2026-08-07) [`46c055daf5d71643d71408f27096f669d839913a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/46c055daf5d71643d71408f27096f669d839913a)

- **fix**(prd-45): границы сегментов ленты видны, сообщение о порядке честное, терминология уровней (2026-08-07) [`68c6eb1a4d2c0bdbb11176bb9970e7dde9af2091`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/68c6eb1a4d2c0bdbb11176bb9970e7dde9af2091)

- **fix**(prd-45): карточка шкалы не заявляет о блокировке, которой нет (2026-08-07) [`d76397a6fb284b61ea8a35b71a9b807fabecd5cc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d76397a6fb284b61ea8a35b71a9b807fabecd5cc)

- **fix**(prd-45): смежные границы уровней больше не считаются пересечением (2026-08-07) [`d8ea404284a1c37199c805a214432320286a981b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d8ea404284a1c37199c805a214432320286a981b)

- **fix**(prd-45): контраст ленты, честный текст о смыкании, один источник имени уровня (2026-08-07) [`16f68264bd29ed8960117181e75b5f7d748f3d93`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/16f68264bd29ed8960117181e75b5f7d748f3d93)

- **fix**(prd-45): статус покрытия склоняет слово «уровень» (2026-08-07) [`415ad53eff7cb9c1502e7b9e371764d94a17ac39`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/415ad53eff7cb9c1502e7b9e371764d94a17ac39)

- **fix**(prd-45): редактор уровней приведён к согласованному эскизу (2026-08-07) [`4ebb107cea754ac5b0dc6a69bc53aea2c4311e96`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4ebb107cea754ac5b0dc6a69bc53aea2c4311e96)

- **fix**(prd-45): заполненное толкование видно сразу, копия CSS для эскизов синхронна (2026-08-07) [`b8a9536977a3073a8aaeb63d38df23c8864c0e87`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b8a9536977a3073a8aaeb63d38df23c8864c0e87)

- **fix**: публикация теста больше не запирает редактор (2026-08-07) [`f0e9c6b66f496ad6de948835f7ffd3c0fb52b579`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f0e9c6b66f496ad6de948835f7ffd3c0fb52b579)
  Дровер уходил в режим «только чтение» при статусе «опубликован» — замок из PRD-7
  (эскиз `s-readonly`), нарисованного до появления снапшотов. BRD раздел 4.7 и
  BRC-20 говорят обратное: публикуется снапшот, доставка обслуживается им, а
  рабочая версия редактируема всегда, и правки не влияют на выдачу до
  переопубликации.
  
  Замок отбирал у автора правку шкал, показателей, оценки и структуры уже
  опубликованной методики. Признак «опубликован, есть неопубликованные изменения»
  и действие «Опубликовать изменения» (PRD-15 FR-12) для этого уже есть на сервере
  и в списке тестов. Флаг «только чтение» у вкладки структуры сохранён — он нужен
  хостам, у которых прав на правку нет по-настоящему.

- **fix**(prd-44): вид вклада «распределение» переживает загрузку в редактор (2026-08-07) [`2018c4618d26a980b08f07d441b33f3e87904a2d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2018c4618d26a980b08f07d441b33f3e87904a2d)
  Загрузчик редактора ветвится по ВИДУ ИСТОЧНИКА вклада и держит собственный белый
  список — перечисления схемы он не знает. Вида `option_allocation` в списке не
  было, а неизвестный вид не отбраковывался, а молча подменялся на `question`:
  матрица «Вклады вопросов» рисовалась пустой, хотя строки в базе есть.
  
  Хуже показа была запись: правка любой ячейки такого вопроса переписала бы весь
  его набор строк видом `question` — фиксированным вкладом за факт ответа вместо
  вклада, равного присвоенному баллу. Закрыто тестом на КАЖДЫЙ вид из перечисления
  схемы, чтобы следующий новый вид не пропал так же тихо.

- **fix**(prd-44): исход `neutral` доезжает до живого пути отправки в LMS (2026-08-07) [`229f3d12be45230ed72418c67b0d9839a190c3f5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/229f3d12be45230ed72418c67b0d9839a190c3f5)
  Правило «измерительный вопрос не бывает верным или неверным» (8ee66c4f) легло в
  `finishScorm`, которую никто не вызывает: пакет отправляет результат через
  `finishScormLmsOnly`, и оттуда измерительный вопрос по-прежнему уходил как
  `incorrect`. Адаптивный путь собирает взаимодействия своей копией цикла и вёл
  себя так же.
  
  Правило вынесено в общую `interactionResultFor`, её зовут все три пути. Тест
  проверял НАЛИЧИЕ ТЕКСТА во всём файле и потому находил правило в мёртвой копии —
  теперь он проверяет поведение функции и её вызов в живых путях.

- **fix**(prd-29): числовой показатель без полос показывает значение, а не пустую пилюлю (2026-08-07) [`ea321b5af1db07c9b76a74fa0b798704be517ba6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ea321b5af1db07c9b76a74fa0b798704be517ba6)
  Карточка числового показателя выходила учащемуся пустой цветной пилюлей, хотя
  значение посчитано. Причин две, и обе нужны разом.
  
  Вид карточки гасил значение: `showValue` требовал `renderKind !== "label"`, а
  «метка» — умолчание параметра «Вид показателей» в шаблоне. Вид отвечает за ФОРМУ
  значения (линейка, кольцо, голое число), видимость — за сам факт показа, поэтому
  признак теперь числовой природы значения. У строкового исхода значение и уровень
  — одно и то же, и там по-прежнему печатается только исход.
  
  Разметка карточки несла ТОЛЬКО слот уровня. Полос интерпретации у такого
  показателя нет, уровня тоже нет — оставался пустой баннер. Добавлен слот значения
  той же разметкой, что у карточки шкалы без домена; баннер закрыт по наличию
  уровня. Правка внесена в ОБА поставляемых `results.html` — паритет раскладок
  держится вручную.
  
  Принято в браузере на 1280 и 430 px: «18» и «2» читаются, строковый показатель
  рядом не изменился.

- **fix**(prd-29): незаполненный домен шкалы — это отсутствие домена, а не ноль (2026-08-07) [`894c01fb42134a1b4c34f540c7f53f816507b724`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/894c01fb42134a1b4c34f540c7f53f816507b724)
  Редактор сохраняет незаполненный домен как `domainMin: null`, а не отсутствием
  ключа. `Number(null)` конечен и равен нулю, поэтому «домена нет» превращалось в
  отрезок 0..0: учащийся видел «18 из 0», а признак наличия домена пускал кольцо и
  «значение из максимума» туда, где области определения нет вовсе.
  
  `null` и пустая строка (пустое числовое поле формы) отбиваются ДО `Number()`.
  Интервал без границы теперь отбрасывается, а не сползает в ноль — придуманный
  ноль молча сдвинул бы границу соседнего уровня.

- **fix**(prd-44): кружок ползунка распределения не режется стопом (2026-08-07) [`97675b9ead344d8c495109d1c66224390d9668a4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/97675b9ead344d8c495109d1c66224390d9668a4)
  Недоступный хвост дорожки идёт в разметке после ползунка, оба позиционированы
  без z-index — значит рисуются в порядке DOM, и хвост ложится поверх кружка.
  Стоп стоит на «значение + остаток», поэтому при полностью распределённом бюджете
  он совпадает с центром кружка и закрашивал его правую половину фоном строки.
  Поднимаем кружок на верхний слой в обеих копиях DS-стилей; правило покрывает и
  разметку сцены, и React-компонент BudgetAllocation — .ou-alloc__slider своего
  контекста наложения не создаёт.

- **fix**(prd-44): измерительный вопрос не «неверный» в отчёте LMS (2026-08-07) [`8ee66c4fda5f5ec4afbf5fb77afb979c5965995a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8ee66c4fda5f5ec4afbf5fb77afb979c5965995a)
  Взаимодействие измерительного вопроса писалось как `incorrect`, потому
  что верного ответа у него нет и проверка возвращала ноль. В отчёте LMS
  это выглядит ошибкой ученика там, где ошибаться не в чем. SCORM 2004
  имеет для этого отдельный исход — `neutral`; правило опирается на признак
  типа, поэтому чинит и шкалу без верной градации (PRD-26 FR-08), и
  распределение (PRD-44 FR-09).
  
  Найдено при браузерной приёмке пакета: образец SCORM получил
  вопрос-распределение, чтобы приёмка проверяла ЖИВОЙ интерактив, а не
  только разметку.

- **fix**(prd-44): видимый стоп ползунка в светлой теме (2026-08-06) [`2864613eaaa0fd2e528fef6b1ba663cf00aaea8a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2864613eaaa0fd2e528fef6b1ba663cf00aaea8a)
  Недоступный хвост был закрашен `--ou-bg-surface-2`, и в светлой теме это
  почти совпало с полупрозрачной дорожкой: стоп не читался вовсе. Найдено
  браузерной приёмкой, а не глазами по эскизу — эскиз согласован в тёмной
  теме, где разница была.
  
  Теперь хвост берёт фон СТРОКИ, поэтому дорожка визуально заканчивается на
  потолке, а не меняет оттенок. Правило работает в обеих темах, потому что
  опорой служит поверхность, на которой строка нарисована.
  
  Правка внесена в обе копии бандла и в эскиз, чтобы копии не разошлись.

- **fix**(prd-44): подписи утверждений на общем проходе подгонки шрифта (2026-08-06) [`8611856f6b19e7e6a994d8bc0ae573e8bd8c871f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8611856f6b19e7e6a994d8bc0ae573e8bd8c871f)
  В эскизе подписи узкого экрана несли жёсткие 17px. Это ломает механизм:
  рантаймовый проход по высоте (`fitQuestionScene`) умеет только менять
  размер шрифта, и литерал в разметке отнимает у него единственный рычаг —
  длинный список перестал бы влезать в поле и получил бы прокрутку.
  
  Теперь размер приходит ТОЛЬКО из `--tb-answer-fs`, как у вариантов
  остальных типов. Длиновой проход (`optionFont` по самому длинному
  утверждению) уже видит утверждения, потому что они лежат в
  `dataJson.options` — это следствие FR-02, и оно молча отвалилось бы, дай
  кто-нибудь типу собственную форму `dataJson`. Заперто тестом.

- **fix**(prd-44): эскиз распределения на ступенях сцены шаблона (2026-08-06) [`20725b525cf1731fa7d869aa61d62ba8bc83cf8d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/20725b525cf1731fa7d869aa61d62ba8bc83cf8d)
  Пороги адаптации я выдумал (1100px) вместо того, чтобы взять
  действующие. Из-за этого раскладка 75% пропадала уже на обычном оконном
  экране, хотя правило шаблона переводит колонку на всю ширину только на
  ступени S1.
  
  Теперь эскиз повторяет ступени theme.css (`@container tbscene`):
  
  \- S1, сцена 900px — колонка вопроса с 75% на всю ширину. Это
    существующее правило `.tb-scene__col`, а не решение эскиза;
  \- S2, сцена 700px — ползунок уезжает под текст утверждения. Та же
    ступень, на которой у шкалы PRD-26 дорожка встаёт вертикально;
  \- S3, сцена 520px — карта вопросов в шапке скрывается, как у остальных
    типов.
  
  Заодно карта вопросов сжимается по правилам `.tb-scene__map`: ряд
  номеров переносится внутри себя, легенда уходит на свою строку на
  ступени шапки 1000px. В эскизе шапка скопирована инлайнами, среди
  которых `flex:none` на ряду номеров, — из-за него страница уезжала
  вбок уже на 760px.
  
  Проверено в браузере: 1280, 920, 760, 375.

- **fix**(prd-44): эскиз распределения на экране телефона (2026-08-06) [`1c5e9c87c80186e384e1fd18bd223dbfd7c411c8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1c5e9c87c80186e384e1fd18bd223dbfd7c411c8)
  На 375x667 экран был нерабочим: сцена держала высоту окна и прятала
  переполнение, поэтому вопрос обрезался подвалом, а до блока ответа было
  не добраться; ряд номеров вопросов уезжал вбок и тянул за собой
  горизонтальную прокрутку.
  
  Ниже 1100px сцена растёт по содержимому и прокручивается СТРАНИЦА:
  посадка «сцена = окно» верна на настольном экране, но на телефоне шапка
  с подвалом съедают всю высоту. Ниже 700px шапка отдаёт место вопросу —
  легенда карты и счётчик скрыты, ряд номеров переносится (ему инлайном
  запрещено сжиматься, отсюда и уезжала страница), заголовки берут
  телефонные размеры вместо скопированных настольных 30px.
  
  Проверено в браузере: 375x667 и вся высота содержимого на 375x1500.

- **fix**(prd-44): ползунок уезжает под текст, колонка на всю ширину (2026-08-06) [`ef2f2ac26124d51239533cb23d344e85d0efb303`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ef2f2ac26124d51239533cb23d344e85d0efb303)
  Ниже 1100px строка утверждения переходит в стек: ползунок встаёт ПОД
  текст, а не ужимается рядом с ним. Без этого подпись рвалась на пять
  строк рядом с полупустой дорожкой. Переход задаёт медиазапрос, а не
  отдельное состояние: узкой строка бывает не только на телефоне.
  
  Тем же порогом колонка вопроса занимает всю ширину вместо 75%. Порог
  общий намеренно: бессмысленно экономить место, ставя ползунок под текст,
  и одновременно выбрасывать четверть ширины.
  
  Проверено в браузере на 1912, 1045 и 430.

- **fix**(prd-44): одна прокрутка и узкий экран в эскизе распределения (2026-08-06) [`c36a946f111cbbcfec0975c0e3e37429e76f0cdf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c36a946f111cbbcfec0975c0e3e37429e76f0cdf)
  Холст взят с согласованного экрана вопроса (approved/question-scale.html),
  но я сам сломал его базовое поведение: добавил блоку вариантов
  собственную прокрутку, и на экране появилась вторая полоса вокруг куска
  формы вместо одной на всей области вопроса. Убрано; вместо этого колонка
  вопроса растёт по содержимому (`min-height` вместо `height`), и
  переполнение отдаётся области `main` — ровно как у остальных типов.
  
  Узкий экран показан колонкой телефонной ширины во всю высоту сцены.
  Раньше телефонная раскладка не помещалась в область ответа настольной
  сцены: блок вылезал поверх заголовка вопроса. Настольные шапка и подвал
  в этом состоянии скрыты намеренно — PRD-44 меняет блок ответа, а
  мобильная адаптация сцены целиком идёт отдельным треком.
  
  Правило подсветки строки изменено с «не ноль» на «выше минимума». При
  ненулевом минимуме поля предзаполнены, и прежнее правило подсвечивало все
  строки сразу — подсветка говорила «вы это выбрали» там, где выбор сделала
  система.
  
  Проверено в браузере: шесть состояний на 1912x1080 и переполнение на
  1400x620 — полоса прокрутки одна и принадлежит области вопроса.

- **fix**(prd-31): привязка попытки к назначению и паритет барьеров с пакетом (2026-08-06) [`b537f31b918728c55313a48bd8ae179c18d57962`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b537f31b918728c55313a48bd8ae179c18d57962)
  createAttempt не писал assignment_id, хотя роут его передавал: на dev-БД
  0 привязок при 35 назначениях. Весь скоуп по назначению молча схлопывался —
  maxAttempts не срабатывал для назначенных тестов, барьер B не срабатывал
  никогда, барьер A срабатывал между КАЖДОЙ попыткой, то есть ровно то
  расхождение, которое PRD-31 устранял.
  
  Брошенная попытка больше не даёт новой выборки: старт переносит состав и
  порядок выдачи открытой попытки того же снапшота, сбрасывает прогресс и
  аннулирует старую строку (annulInProgressAttempts получил userId). Иначе
  «старт — посмотрел — бросил — старт» перебирал пул даром: оба барьера и
  счётчик попыток читают только завершённые попытки. Побочно закрыто
  накопление незавершённых строк, из которых resume брал произвольную.
  
  canRetake на экране итогов теперь учитывает decideRetake — пакет так делает
  через attemptIntervalState, а веб предлагал кнопку, которую старт отбивал 403.
  
  PRD-19 FR-20: «Скачать отчёт» по прошлой попытке на стартовом экране веба.
  У пакета это есть, а комментарий про «нет клиентского PDF» устарел после
  PRD-27/35; данные отчёта тянутся по клику, а не на каждый вход в тест.

- **fix**(shell): шапка не растягивается на учётке с несколькими ролями (2026-08-06) [`8eaede45924099b139cb0175bfff3d48b001c6d0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8eaede45924099b139cb0175bfff3d48b001c6d0)
  Подпись под именем перечисляла все роли через запятую, и на учётке с
  четырьмя ролями блок пользователя занимал ~380px, выдавливая остальное
  содержимое шапки: у `.ou-shell__user-meta` нет ограничения ширины, а
  `.ou-shell__actions` не сжимается.
  
  Теперь в шапке — только сильнейшая роль, через `primaryRole()` и тот же
  `ROLE_PRIORITY`, по которому PRD-13 FR-30 выбирает стартовый раздел, так
  что подпись называет ту область, куда пользователя реально пускают.
  Полный набор ролей никуда не делся — он в заголовке меню под аватаром.

- **fix**(feedback): условная обратная связь доходит до ученика на вебе (2026-08-06) [`86147b789f2145220673272d40fbc458a36ce80e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/86147b789f2145220673272d40fbc458a36ce80e)
  Вопрос с условной обратной связью не показывал на вебе НИКАКОГО пояснения —
  ни ветки, ни общего текста. Редактор при переключении в этот режим обнуляет
  общее поле, а веб-хост читал только его, не глядя на feedbackMode: и
  стандартный режим (handleStandardConfirm), и адаптивный (ответ маршрута
  answer-adaptive) отдавали пустоту. Пакет при этом ветку показывал, разбирая
  режим тремя собственными копиями правила.
  
  Правило стало общим — feedbackTextFor в shared/template/feedback-banner,
  доступное рантайму пакета через TBTemplate. Три копии в пакете удалены, оба
  веб-пути зовут то же самое, поэтому четвёртой копии появиться неоткуда.
  
  Общий текст намеренно НЕ подставляется как запасной для незаполненной ветки:
  автор убрал его из этого режима, и показывать легаси-остаток значило бы выдать
  ученику текст, которого в вопросе уже нет. Частичный балл идёт по ветке
  неверного ответа — тот же раздел, что и в пакете.

- **fix**(results): блок «Результаты по темам» молчит, когда темам нечего сказать (2026-08-06) [`54155b40a026de67bca71cf9b13f2f6f1b777ac2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/54155b40a026de67bca71cf9b13f2f6f1b777ac2)
  Слоты карточки темы гасились поодиночке, а сама карточка и заголовок над ней
  держались на простом НАЛИЧИИ темы в topicResults. У чисто измерительного теста
  (опросник Маслач: измерительные вопросы не идут в total темы) это оставляло на
  экране заголовок, разделитель и карточку с названием темы и вечно пустой полосой
  прогресса — блок без единого факта. Предыдущая правка (issue #30) убрала строки,
  но не коробку.
  
  Тема попадает в контекст, только если ей есть что сказать: оцениваемые вопросы,
  ВЫНЕСЕННЫЙ вердикт либо собственные курсы и мероприятия темы (карточка — их
  единственный путь к ученику). Иначе тема выбывает целиком: пустой массив в DSL
  ложный, поэтому условие на result.topicResults уносит и заголовок, и сетку — в
  том числе в сторонних шаблонах из реестра PRD-3, куда правкой макета не
  дотянуться.
  
  Полоса прогресса — под условием на total: карточка может выжить ради курсов, но
  без оцениваемых вопросов полоса пуста и означать ничего не может.
  
  Отчёт фильтрует исходный список тем тем же правилом: его строки дополняются по
  ИНДЕКСУ в паре с ним, и несогласованные списки приписали бы теме чужие счётчики
  (плюс §5.2 — отчёт не вправе показать иное, чем экран).

- **fix**(debug-player): эталон находит карточки по индексам, а не по тексту (2026-08-06) [`84a303bc6f22ab49c942ed3b4821f4d4a0294deb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/84a303bc6f22ab49c942ed3b4821f4d4a0294deb)
  Оверлей «Эталон» связывал элемент на экране с записью данных по совпадению
  textContent с сырой строкой из TEST_DATA. Рендер прогоняет каждый ответ через
  renderInlineMarkdown с типографикой (NBSP после предлогов, «ёлочки», тире,
  \n в &lt;br&gt;), поэтому текст в DOM не равен исходному: в сопоставлении буквы пар
  вставали только на короткие латинские термины и не вставали на русские
  формулировки, а в ранжировании эталон не показывался вовсе.
  
  Теперь обе разметки ключуются по индексам, которые уже есть в DOM:
  data-drop=r&lt;right&gt; на подсказке, data-drag=&lt;left&gt; на фишке. Строке
  ранжирования добавлен data-item с индексом ЭЛЕМЕНТА — data-drag/data-drop там
  хранят позицию показа, которую меняет перетаскивание, а эталон задан индексами.
  
  Тесты оверлея строят DOM настоящим рендерером на тексте, который типографика
  переписывает: фикстуры из чистых ASCII-строк и прятали этот дефект.

- **fix**(take-test): частичный балл доходит до мгновенного фидбека на вебе (2026-08-06) [`f67b92736f3b7caf53a982a2b28c339727e39347`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f67b92736f3b7caf53a982a2b28c339727e39347)
  Вердикт стандартного (не адаптивного) прогона считался на клиенте своей
  захардкоженной логикой точного совпадения, поэтому мог быть только
  верно/неверно. Итоги попытки при этом уже учитывали ступенчатую и весовую
  цену ответа через общий движок — ученик видел «Неверно» сразу после ответа
  и частичный балл только на экране результатов.
  
  scoreAnswerLocally делегирует @shared/scoring/engine и возвращает долю
  s/sMax, вердикт стал трёхпозиционным — тот же тон и та же формулировка,
  что у SCORM-пакета. Эффективный конфиг оценивания приходит с попыткой
  полем scoring рядом с correctJson (start и resume), под тем же гейтом
  «показывать правильность ответа»: нет ключа ответа — нет и конфига.
  Адаптивный режим scoreRatio не получает и остаётся бинарным намеренно.

- **fix**(report): вердикт темы в PDF-отчёте трёхпозиционный, как на экране (2026-08-06) [`336eedb463cc92518b3dc2eecba5eb5d2ec60ba5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/336eedb463cc92518b3dc2eecba5eb5d2ec60ba5)
  Отчёт печатал вердикт темы булевым — `src.passed === true` в
  `buildReportContext`, — тогда как экран печатает его тремя состояниями
  (`topicView`: да / нет / метки нет вовсе). Тема без разрешённого правила
  прохождения получает `passed: null` (`aggregateStandardResult` при
  отсутствии правила), и отчёт объявлял её проваленной: экран и скачанный
  с него документ утверждали про одну и ту же тему разное.
  
  \- Вердикт в контексте отчёта вернул третье состояние.
  \- Макет `report.html` обоих поставляемых шаблонов не печатает узел плашки
    при пустой метке. Пустой строки было мало: у `.tb-report__topic-verdict`
    свой фон и отступы — остался бы красный прямоугольник без текста.
  \- Той же логике подчинены два цвета в `report.css`: полоса карточки и
    заливка прогресса были красными ПО УМОЛЧАНИЮ и красили тему без вердикта
    в провал мимо метки. База стала нейтральной, красное даёт `is-fail` —
    он приходит тем же `passClass`, что и `is-pass`.
  
  Счётчики и баллы у такой темы остаются на месте: снято утверждение о
  прохождении, а не данные.
  
  Правки в «Сертификацию» внесены дословно теми же — паритет с эталоном
  держится вручную, гард стили не сверяет (пункт техдолга от 2026-08-02).
  
  Закрывает пункт техдолга от 2026-08-03, вскрытый приёмкой консолидации
  текстов обратной связи.
  
  Приёмка: макет прогнан через тот же `renderScreenInto`, что и экспорт PDF,
  и снят в реальном браузере на трёх темах разом — пройдена (зелёные плашка
  и полоса), не пройдена (красные), без вердикта (плашки нет, полоса и
  прогресс серые). Точечные прогоны: report-context, report-html,
  report-layout-parity, report-export-pdf, report-package-bake,
  template-layout-parity, report-preview, result-context — 157 тестов.
  
  Превью шаблонов перегенерированы (`npm run scorm:previews`).

- **fix**(results): вердикт гаснет у любого теста, которому нечего оценивать (2026-08-06) [`6637acd752446af780d17fdb726e3ebf10f20ffc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6637acd752446af780d17fdb726e3ebf10f20ffc)
  Тест с порогом, но без оцениваемых вопросов печатал зелёную шапку «Пройден»
  и одновременно блок обратной связи. Оба правила отвечают на один вопрос —
  «оценивает ли этот тест», — но гашение вердикта лежало ВНУТРИ ветки измерений
  `buildResultContext`, а туда заходит только тест со шкалами или показателями.
  Контрольный тест (самая частая конфигурация продукта) шёл мимо, и его вердикт
  оставался булевым: `aggregateStandardResult` при нулевом числе оцениваемых
  вопросов возвращает `overallPassed: true` намеренно, рассчитывая, что хост
  вердикт не покажет.
  
  Продуктового решения пункт не требовал: правило задано PRD-29 §6.7 («порог
  задан И есть что оценивать») и не ограничено измерительными методиками.
  
  \- Гашение вынесено в общий путь построителя, до ветки `measures`.
  \- Неизвестный признак порога вердикт НЕ гасит. Сводка вправе считать
    неизвестность отрицанием — она лишь прячет блок; вердикт же заголовок
    экрана, и хост, не научившийся слать признак, иначе потерял бы шапку
    у каждого оцениваемого теста. Снимаем только по знанию: возможных баллов
    ноль либо порог явно не задан (`hasPassThreshold === false`).
  \- Тест «контрольный тест вердикт сохраняет» проверял вердикт на фикстуре
    измерительного опросника (`possiblePoints: 0`), то есть фиксировал сам
    дефект — фикстуре добавлены баллы.
  
  Закрывает дефект приёмки PRD-29 D-4 и одноимённый пункт техдолга от
  2026-08-03; формулировки в ROADMAP, спеке и приёмке актуализированы.
  
  Приёмка: SCORM-пакет в реальном браузере (локальный плеер, тест из одного
  вопроса с ценой 0) — плашки в шапке нет, «Рекомендации» на месте. Точечные
  прогоны: 30 файлов, 366 тестов.
  
  Превью шаблонов перегенерированы (`npm run scorm:previews`) — они отставали
  и от `a9b1628` (issue #30), эта правка приезжает в них вместе с текущей.

- **fix**(results): скрыть бессмысленные показатели по теме на экране итогов (2026-08-05) [`a9b162873976f44795092de9422c6b583c712500`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a9b162873976f44795092de9422c6b583c712500)
  Строка «Баллов» темы игнорировала настройку «Сводка по баллам»: выключение
  сводки гасило только общий блок теста, а построчный «Баллов» в карточке
  темы оставался в SCORM-пакете при любом раскладе (issue #30). PDF-отчёт
  сознательно не подчиняется этой настройке — это скачанный документ без
  переключателя, поэтому у него собственный флаг topicPointsIgnoreScoreSummary.
  
  Заодно погашена строка «Правильно» (и «Баллов») там, где у темы нет ни
  одного оцениваемого вопроса — та же бессмыслица «0 из 0», которую PRD-29
  убрал из сводки теста, просачивалась на уровень темы.

### Documentation

- **docs**(references): книга опросника ЧИЛ версии 3.6 (2026-08-10) [`03260643a0a03877b0b90e810c0738d77ddb7fcb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/03260643a0a03877b0b90e810c0738d77ddb7fcb)
  Исходная методика, по которой в системе собран опросник ведущего стиля: состав
  блоков, распределение баллов и толкования стилей. Лежит рядом с прочими
  референсами методик — по ним сверяют настройку теста, когда расчёт расходится с
  ожиданием автора.

- **docs**(prd-27): формат шаблонов 1.4.0, охват отчёта приведён к реальности (2026-08-10) [`75ef952f4669055df15963c94b2f2bfa598301a8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/75ef952f4669055df15963c94b2f2bfa598301a8)
  Спека формата описывает три расширения контракта: канал контролируемого HTML
  `{{& путь }}` (§9) с парными полями `textHtml`/`textsHtml` (§10.4), назначение
  поля вида `settings[].scope` (§8.4.1) и вводный блок `result.introHtml` (§10.3).
  Все три обратно совместимы: шаблон, ничего о них не знающий, работает как
  прежде. Руководство разработчика догнало до 1.3.0 с примерами.
  
  PRD-27 приведён в соответствие с продуктом:
  
  \- §6.5 и решение D-6 описывают постраничную раскладку A4, а из §9 «Вне охвата»
    ушли разрывы страниц — там остались только колонтитулы и нумерация;
  \- §7.1 вместо «всё в блоке обратной связи» несёт таблицу размещения по двум
    экранам: спека и реализация расходились в разные стороны;
  \- FR-27 и FR-28 — вводный блок и переключатель «в отчёте тот же текст».

- **docs**: разбор красного ствола и зелёный полный прогон (2026-08-09) [`796a38c14fa6870742d95458a14a4c027e0f112d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/796a38c14fa6870742d95458a14a4c027e0f112d)
  87 падений в 12 файлах разложены на четыре причины; две из них были живыми
  дефектами, а не устаревшими проверками. После починки полный test:cov зелёный:
  479 файлов, 7650 тестов, покрытие 88.63 / 80.79 / 86.49 / 90.38 при пороге 80.

- **docs**(prd-46): правило переноса легаси-переключателя и отчёт о правке (2026-08-09) [`3d628b369bc840648db5869e3b9a16694ec098ba`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3d628b369bc840648db5869e3b9a16694ec098ba)
  Спека §4.1 доопределена: «не тронуто» — отсутствие ключа, а не значение «нет»,
  и почему у отчёта это решается до слияния с умолчаниями. Отчёт о приёмке PRD-47
  §8 переведён из «не чинился» в «исправлен».

- **docs**(prd-47): «Отменить» проверено на двух черновиках — приёмка закрыта (2026-08-09) [`1f8c0af2f21cc79c446479e2fb3e65517d0ecaa5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1f8c0af2f21cc79c446479e2fb3e65517d0ecaa5)
  Главный риск переезда карточки: на одной вкладке теперь правятся поля из РАЗНЫХ
  черновиков — design_settings_json вкладки «Оформление» и model.report модели теста.
  Проверено живьём: тронуты оба поля подряд, «Отменить» вернул оба, ящик закрылся,
  при переоткрытии метка не осталась, в базе не осело ничего.
  
  Устройство это подтверждает: handleExitWithoutSave зовёт editor.reset(),
  design.revert() и contentPages.discard() одной кнопкой, поэтому переезд карточки
  связку не ломает.
  
  Из «не проверено живьём» осталась только печать — она вне рамок трека по решению
  владельца.

- **docs**(prd-47): приёмка доведена на реальных тестах со шкалами (2026-08-09) [`1e7ad68ec63be4a1de6624762ead4bc04900e01b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1e7ad68ec63be4a1de6624762ead4bc04900e01b)
  Отчёт из ПАКЕТА проверен живьём, а не по проводке: 22 вопроса «PRD-29 Маслач
  (восстановление)» в отладочном проигрывателе, страница отчёта поймана перед
  растеризацией — блок шкал, диаграмма и названия всех трёх шкал на месте.
  
  Облик шкал сверен на живой попытке ЧИЛ: экран и отчёт дают одну фигуру, те же
  четыре цвета в том же порядке (включая заданный автором вручную) и те же три
  пиктограммы. Настройки тестов, которые для этого временно менялись, возвращены
  байт в байт.
  
  Отдельным разделом записан дефект, найденный по ходу и НЕ относящийся к PRD-47:
  умолчание манифеста scalesChartKind='none' маскирует легаси-переключатель
  showCompetencyRadar, поэтому отчёт теста, настроенного до PRD-46, молча перестал
  рисовать диаграмму. Экрана это не касается — там нетронутое поле отсутствует, и
  миграция работает.
  
  Два вывода об инструменте: chrome-headless-shell падает на этом пакете вместе со
  всем соединением CDP (полный chromium проходит), а функции бандла TBTemplate не
  подменяются — esbuild отдаёт их геттерами, поэтому наблюдать надо результат, а не
  вызов. Обе ловушки стоили времени и записаны, чтобы не стоили снова.

- **docs**(prd-47): трек закрыт — статус, отчёт о приёмке, роадмап (2026-08-09) [`81ec06ff18db71d9217669cf3f391362ee1395d9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/81ec06ff18db71d9217669cf3f391362ee1395d9)
  Спека помечена реализованной и принятой в браузере, с диапазоном коммитов. Раздел
  «чего не хватало» оставлен как описание дефектов — по нему видно, что именно
  чинилось, — но переименован в прошедшее время, чтобы не читался как открытый.
  
  Дописан §4.5 про находку приёмки: потребителей контекста отчёта оказалось ТРИ, а
  не два, как называла спека, — модальное окно предпросмотра строит контекст своим
  вызовом. Оттуда правило для похожих работ: искать всех вызывающих построитель, а
  не доверять списку из требований.
  
  В отчёте о приёмке отдельным разделом перечислено, что НЕ проверено живьём:
  диаграмма в пакете (у образцового пакета нет шкал), печать (вне рамок), совпадение
  цвета шкал с экраном (закрыто модульно) и поведение «Отменить» при двух черновиках.
  
  В роадмапе PRD-47 переехал из «не начатых» в статусную таблицу как реализованный,
  строка PRD-35 сужена ещё раз: предпросмотр отчёта больше не «собирается без
  измерений».

- **docs**(prd-47): план реализации — измерения и диаграмма в отчёте (2026-08-08) [`d2bc636b543ee44f5303694d44130e7a57ab5225`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d2bc636b543ee44f5303694d44130e7a57ab5225)
  Четыре среза, каждый самодостаточен и коммитится отдельно. A даёт ядро и чинит веб:
  появляется buildReportMeasures — единственное место, где вход экрана превращается во
  вход отчёта, и там же перестаёт теряться облик шкал. B тянет то же в пакет: сегодня
  opts отчёта собирается из design и values, поэтому в LMS печатается документ без
  блока измерений целиком. C возвращает предпросмотр, D двигает карточку настроек на
  вкладку «Оформление».
  
  Самопроверка нашла в плане три неточности, и все исправлены по коду, а не по памяти:
  tests.report_settings_json ветвится по РЕЖИМУ теста, а не хранит values плоско;
  трёх файлов тестов, на которые план ссылался как на существующие, в репозитории нет
  — теперь они «создать» и приведены целиком; два шага описывали правку словами вместо
  кода — заменены на точные места (preview-context.ts:576 и ветка рейла в
  design-section.tsx).
  
  Отдельно записано, чего план НЕ делает, со ссылками на решения владельца: печать,
  своя настройка блока шкал и перенос хранения полей отчёта.
  
  В финале явно оговорено, что полный test:cov запускается ТОЛЬКО с разрешения
  владельца и что ствол на 2026-08-08 красный — своё падение надо отделять от чужого.

- **docs**(prd-47): закрыты оба открытых вопроса — блок шкал и демо-измерения (2026-08-08) [`d5552a22e953b62889f4112319e21620101e980b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d5552a22e953b62889f4112319e21620101e980b)
  Своей настройки «показывать блок шкал» у отчёта не будет: блок печатается, когда
  измерения есть. Решение 2.1 про набор настроек ДИАГРАММЫ, а не про буквальное
  повторение каждого переключателя экрана.
  
  Демо-измерения предпросмотр берёт из демо-набора шаблона — того же
  PreviewDemoDataset (demo/course.json), из которого предпросмотр страниц берёт
  всё остальное. Никакой второй демо-выдумки специально для отчёта, которая
  разошлась бы со страничной.
  
  Разбор источника вскрыл следствие шире отчёта: слова measures в контракте
  предпросмотра нет вовсе, а runtime.result демо-набора несёт только балл, статус и
  вердикт. Значит автор не видит блок измерений и диаграмму НИ В ОДНОМ
  предпросмотре — ни страничном, ни отчётном. Это же объясняет открытый вопрос
  PRD-46 §10 «нужен ли автору предпросмотр диаграммы». Отсюда работа: секция
  измерений появляется в демо-наборе и его контракте, а читают её оба предпросмотра
  — поэтому в приёмку добавлена и страница итогов, иначе демо-данные разойдутся
  между двумя предпросмотрами.
  
  Открытых вопросов в спеке больше нет.

- **docs**(prd-47): настройки отчёта переезжают на вкладку «Оформление» (2026-08-08) [`2146cf5aa3ccaa30f025388d38a0f1f33eaa5142`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2146cf5aa3ccaa30f025388d38a0f1f33eaa5142)
  Пятое решение владельца: карточка «Отчёт о результатах» уходит из «Настройки →
  Основное» в свой пункт рейла вкладки «Оформление». Основание — отчёт есть часть
  шаблона, его поля объявляет манифест ровно как параметры оформления, и место им
  рядом с «Макетом», а не под правилами прохождения.
  
  Переезжает ТОЛЬКО интерфейс. Хранение остаётся отдельной колонкой: PRD-27 §4.2
  отказался складывать поля отчёта в design_settings_json, чтобы не связывать
  вкладки порядком сохранения, и это решение не отменяется. В PRD-27 поставлено
  уточнение, иначе следующий читатель решит, что раздел устарел целиком.
  
  Следствие названо честно и вынесено в приёмку: на одной вкладке окажутся поля
  двух черновиков, поэтому «Отменить» обязано вести себя предсказуемо — автор не
  должен обнаружить, что откатилась только часть тронутого. Наполовину это уже так:
  карточка и сегодня читает черновой templateId соседней вкладки (риск R-5).
  
  Пункт рейла показывается ВСЕГДА, в отличие от остальных. Прочие прячутся при
  отсутствии параметров, и это осмысленно — «Цвета» без цветовых параметров пустой
  экран. Отчёт же есть у любого теста, и при шаблоне без объявленных видов карточка
  объясняет, что он соберётся видом «Стандартный»; спрятать пункт значит спрятать
  это объяснение.
  
  Эскиз не требуется: состав карточки не меняется, добавляется строка в рейл.

- **docs**(prd-47): требования к диаграмме в отчёте собраны (2026-08-08) [`c2836a65465b268c96496f8e576bfc1c6776507f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c2836a65465b268c96496f8e576bfc1c6776507f)
  Спека фиксировала только повод. Теперь в ней четыре решения владельца, сверенное
  с кодом состояние и решение, из которого можно писать план.
  
  Решения: отчёт рисует по своим настройкам, а их набор повторяет набор страницы
  итогов; цвет и пиктограмма шкалы берутся С ЭКРАНА, потому что покраска — не
  свойство страницы и профиль не может быть в двух документах разного цвета;
  печать вне рамок; паритет с пакетом входит в трек.
  
  Раздел «что уже есть» переписан по факту: три настройки у варианта отчёта
  объявлены, умолчания совпадают с экранными, веб их читает, роза в раскладке
  отчёта есть. Отсюда важное следствие: «базово соответствует» выполняется по
  построению, и включать что-либо задним числом не требуется.
  
  Главная находка — отчёт в пакете не получает measures ВООБЩЕ: opts собирается из
  design и values, поэтому в LMS печатается документ без блока измерений целиком, а
  не без одной фигуры. Это шире исходной формулировки трека.
  
  Решение — один сборщик входа отчёта в shared/report и два поставщика данных.
  Альтернатива «каждый хост собирает сам» отвергнута с обоснованием: две сборки
  одного входа уже дали в этом продукте три дефекта.
  
  Guard ипсативности расширяется до «auto в любой из двух настроек» не ради
  экономии, а по необходимости: в пакете отчёт собирается на клиенте из запечённых
  данных, и посчитать признак там нечем.

- **docs**: документы сведены с фактом — три листа книги, три трека, строка PRD-35 (2026-08-08) [`2f41e611bd2a4d74c7f853bf9230aea2cab88bb1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2f41e611bd2a4d74c7f853bf9230aea2cab88bb1)
  Проверка велась по коду и git-истории, а не по чужим отчётам; всё, что ниже,
  подтверждено запросом или командой.
  
  Лист «Шкалы» описан в трёх местах — руководство методиста, руководство по
  шаблону книги и спецификация формата, — и все три отстали. Дописаны три колонки,
  появившиеся сегодня («Границы шкалы», «Предел показа», «Благоприятное
  направление»), и объяснена пара, которую легко перепутать: «Направление»
  переворачивает значение при подсчёте, «Благоприятное направление» числа не
  меняет. Отдельно сказано, чего в книге НЕТ: толкование уровня, тональность и
  вложения живут в редакторе, загрузка сохраняет их по КОДУ уровня, а уровень без
  кода книга не примет.
  
  Там же исправлена ошибка старше сегодняшней работы: «Показывать ученику»
  описывалось как «да / нет», хотя со времён PRD-29 у колонки ТРИ значения. Книга,
  написанная по такому описанию, раскрывала ученику сырой балл, который методист
  закрыл, — ровно та ловушка, от которой защищается разбор ячейки.
  
  В ROADMAP не было ВОВСЕ трёх треков: PRD-45 (влит слиянием 1fb9b283), PRD-46
  (девять коммитов, реализация целиком, приёмка не проводилась) и PRD-47 в
  статусной таблице. Документ при этом утверждает «всё, чего в сводке нет,
  закрыто», то есть молчание читалось как готовность.
  
  Строка PRD-35 сужена по факту: «в dev-БД нет попыток по методике» неверно —
  восемь завершённых попыток по методике ЧИЛ от 2026-08-06, проверено запросом.
  Остались непроверенными растеризация PDF и предпросмотр отчёта.
  
  Даты в заголовках §0 и §0.0 сдвинуты на 2026-08-08 вместе с якорями: смена
  заголовка ломает ссылки, все внутренние якоря после правки сверены скриптом.
  
  Шапка PRD-45 больше не говорит «ветка не влита» — слияние проверено как предок
  main; исходное состояние оставлено ниже, оно объясняет, почему вливание требовало
  разбора истории. Шапка PRD-46 больше не говорит «реализация идёт».

- **docs**(workbook): в план дописан хвост про обязательный код уровня (2026-08-08) [`b977346ca5ee4021b0d4b72f61666b427a57d05c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b977346ca5ee4021b0d4b72f61666b427a57d05c)

- **docs**(workbook): долг по конфигурации шкалы закрыт, план сведён с фактом (2026-08-08) [`10cd809ff4c738e98797b0fda14e4f6848b42812`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/10cd809ff4c738e98797b0fda14e4f6848b42812)
  Строка §0.3 разделена: перезапись configJson закрыта, а вторая половина пункта —
  переиндексация медиа при записи в обход маршрутов — осталась открытой и сужена до
  неё. В закрывающей записи перечислено, что именно теряло старое поведение.
  
  В план дописан раздел 6: слияния верхнего уровня оказалось мало (обратная связь
  живёт внутри уровня, поэтому уровни сшиваются по коду), имена двух колонок из трёх
  взяты из редактора вместо предложенных планом, наличие колонки определяется по
  заголовкам, и приёмка — круг на трёх реальных тестах, десять шкал, потерь нет.

- **docs**(prd-46): план по книге Excel и передача работы (2026-08-08) [`ab983512f9257e86e14e8559d594033cca7a41dc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ab983512f9257e86e14e8559d594033cca7a41dc)
  План: импорт книги замещает `config_json` шкалы значением `{ bands }`, а экспорт
  выгружает из конфигурации только «Диапазоны» — круг «выгрузил → загрузил»
  разрушает домен, направление, тексты уровней, вложения и предел показа, даже
  если в файле ничего не править. Показатель в том же импорте ведёт себя
  правильно и служит образцом.
  
  План режет работу на две части разной цены: сначала прекратить потерю данных
  слиянием конфигурации, потом добавить книге недостающие колонки. Правило,
  которое их связывает: книга задаёт целиком те поля, у которых есть колонка, и
  не трогает те, у которых её нет.
  
  Передача: состояние восьми коммитов PRD-46, чего нет и почему, базовая линия
  падений ствола (88 в 13 файлах — чтобы преемник не искал их причину в этой
  работе) и грабли стенда, включая глобальную перезапись `templates.source_path`
  при запуске dev из worktree.

- **docs**(prd-46): план облика шкалы на диаграмме (2026-08-08) [`e228257d0919051a328d63c9fc565812ca591ac2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e228257d0919051a328d63c9fc565812ca591ac2)
  Разведка показала, что облика в коде нет вовсе: ядро принимает и цвет, и контуры
  пиктограммы, но ни один хост их не заполняет, поэтому роза всегда рисуется
  выведенной палитрой. При этом коммиты «эскиз облика», «оформление переехало в
  настройки итогов» и «чипы облика в колонки» трогали ТОЛЬКО файл эскиза — по
  названиям легко решить обратное, и это записано в плане первым делом.
  
  План режет работу по сквозной пользе, а не по слоям: сначала цвет целиком от
  поля манифеста до оси диаграммы, потом пиктограмма. Поле, которое никто не
  читает, хуже отсутствующего.
  
  Перечислены семь решений, которые пересматривать нельзя, точки подключения с
  адресами и грабли, известные заранее, — включая то, что запуск dev из worktree
  глобально переписывает путь к макетам шаблона.

- **docs**(prd-47): диаграмма профиля в отчёте выделена в отдельный трек (2026-08-08) [`9ea0e084f9e6a1dd61cdf18157213af65396c0ba`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9ea0e084f9e6a1dd61cdf18157213af65396c0ba)
  У отчёта СВОЙ переключатель вида — это решение PRD-35 §9, а не недоделка: документ
  уносят специалисту, и профиль там уместен даже когда ученику на экране его не
  показывают. Значит отчёт не «то же самое в PDF», а свой набор решений, и держать
  его внутри PRD-46 значило бы принимать эти решения молча.
  
  Документ фиксирует только повод, состояние смежных механизмов и пять открытых
  вопросов; требования не собирались, к реализации не приступать. Отдельно записано
  известное расхождение: признак ипсативности приходит в отчёт посчитанным для
  ЭКРАНА, поэтому тест с явным видом на экране и «авто» в отчёте получит там радар.
  
  PRD-46 сужен соответственно — отчёт убран из объёма и из приёмки, чтобы два
  документа не претендовали на одно.

- **docs**(prd-46): итоги приёмки ипсативности на обоих хостах (2026-08-08) [`e68505ed1a3f769e27f7da798342cd6c8c5b228e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e68505ed1a3f769e27f7da798342cd6c8c5b228e)
  Проверено на содержимом dev-БД, а не на выдуманных данных. «Авто» на ипсативной
  модели даёт розу, та же модель со скрытой шкалой — радар, возврат шкалы возвращает
  розу. Радиусы сходятся с r = F·sqrt(доля) при F = 100/sqrt(0,5): 92,6/53,5/53,5/75,6
  на баллах 9/3/3/6 в вебе и 81,6/75,6/75,6/43,6 на баллах 7/6/6/2 в пакете. Цвета
  секторов совпадают у хостов до значения RGB, так что паритет подтверждён числами.
  
  Записаны две находки, стоившие времени. Первая: без домена радар не строится вовсе,
  и экран остаётся без диаграммы — штатный отказ, который при разборе легко принять за
  дефект (роза домена не требует, она опирается на сумму). Вторая: запуск dev из
  worktree переписывает templates.default.source_path на своё дерево, то есть переводит
  на свои макеты все чужие сессии; путь возвращён.

- **docs**(prd-46): передача состояния ветки (2026-08-08) [`2af3abb5f5e983c358edb8fcc6132ee3d987aaf8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2af3abb5f5e983c358edb8fcc6132ee3d987aaf8)
  Что сделано и что осталось, с точными адресами правок, состоянием проверок и
  граблями, которые стоили времени: база ветки не main, приёмка из worktree
  рисует старый макет из-за абсолютного templates.source_path, палитра розы
  ограничена шестью шкалами, опора розы — сумма вкладов, а не домен.
  
  Отдельно записан предел guard'а веба: у отчёта свой переключатель вида, а
  признак ипсативности достаётся ему посчитанным для экрана.

- **docs**(prd-46): эскизы согласованы и перенесены в approved (2026-08-08) [`d52130050b7479ad08bae03f6418234a9fe855e9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d52130050b7479ad08bae03f6418234a9fe855e9)
  Оба эскиза — диаграмма на экране итогов и оформление шкал в настройках
  варианта — согласованы 2026-08-08. Гейт wireframes-first пройден:
  написанный под них код перестаёт быть опережающим.

- **docs**(prd-46): спека и эскиз без эталонного кольца (2026-08-07) [`b24ca229a442d782a7adae6f9e5f98382976da22`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b24ca229a442d782a7adae6f9e5f98382976da22)
  Тексты приведены в соответствие с кодом: кольца ровного расклада нет,
  подписи описаны как кольцо max(радиус сектора) + отбивка, из подписей
  состояний и таблиц убраны ссылки на снятый знак.

- **docs**(prd-45): статус приёмки, итоги браузерной проверки и состояние ветки (2026-08-07) [`26c8272e5d38f0e62fecc360c5412f0c0a8a6889`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/26c8272e5d38f0e62fecc360c5412f0c0a8a6889)

- **docs**(prd-45): задача 8 снята из плана, ссылки приведены в соответствие (2026-08-07) [`da8934c02fdf34a54223bf19cfacc54f838effc5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/da8934c02fdf34a54223bf19cfacc54f838effc5)

- **docs**(prd-45): перестановка уровней снята из охвата в технический долг (2026-08-07) [`418591553f4d7b2270bae084de29b5a4f96aab09`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/418591553f4d7b2270bae084de29b5a4f96aab09)

- **docs**(prd-44): найденные по ходу дефекты и правило проходного балла на обложке (2026-08-07) [`f023f0fb19d9df99669e909d9c04ba8f381a42f6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f023f0fb19d9df99669e909d9c04ba8f381a42f6)
  В план PRD-44 записаны три находки старше самого типа вопроса: обложка
  измерительного теста с «проходным баллом», второе перечисление (вид источника
  вклада), которого не было в инвентаре ветвлений, и замок редактора после
  публикации против BRD.
  
  В руководстве по шаблонам у `course.passPercent` описано, когда он приходит
  пустым и почему раскладка обязана гасить факт своим условием.

- **docs**(prd-45): план реализации редактора уровней шкалы (2026-08-07) [`5e80e62bd1ca348d213aaa96cdc31ba1f56d5575`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5e80e62bd1ca348d213aaa96cdc31ba1f56d5575)

- **docs**(prd-45): редактор уровней шкалы — пороги вместо пар границ (2026-08-07) [`bc6e58c06e6caa8c15ea575730c7c3cc4a2595ae`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bc6e58c06e6caa8c15ea575730c7c3cc4a2595ae)
  Разбор экрана «Диапазоны (пороги) → уровень»: таблица теряет подписи полей при
  прокрутке, режет содержимое и переносит селект оценки на три строки, а парный
  ввод min/max допускает молчаливые дыры между уровнями при дробном балле.
  
  Спека заменяет таблицу списком карточек уровней с вводом по точкам-разрезам:
  N+1 число вместо 2N, дыры и пересечения становятся невозможны конструктивно.
  Модель bands и findBand не меняются — порог достаётся нижнему уровню, что
  сохраняет поведение уже опубликованных тестов.

- **docs**(prd-44): стенд WebTutor снят из приёмки A-05 (2026-08-07) [`00bc274b255402396eb6c81ce57dcbe1bf104330`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/00bc274b255402396eb6c81ce57dcbe1bf104330)
  Решение владельца продукта 2026-08-07: состоявшегося прохождения в
  локальном эмуляторе достаточно. Основание записано рядом с самим
  пунктом, а не только в переписке: тип не вводит новых обращений к LMS —
  взаимодействие пишется существующим механизмом, а его строка закрыта
  тестом на функциях рантайма.
  
  Без этой записи следующая сессия увидела бы незакрытый пункт приёмки и
  пошла бы искать доступ к стенду.

- **docs**(prd-44): состояние плана после фаз 2-4 и 7 (2026-08-07) [`d904e495986f70fe8ea7ffb837459e22c010afbc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d904e495986f70fe8ea7ffb837459e22c010afbc)

- **docs**(prd-44): эскиз распределения баллов согласован (2026-08-06) [`80dc3c3c5fbd2b93d18a7eff95428b11df8d1175`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/80dc3c3c5fbd2b93d18a7eff95428b11df8d1175)

- **docs**(prd-44): состояние плана после фаз 5 и 6 (2026-08-06) [`02963b44a2b01afa74dd789c7a187c062352d4de`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/02963b44a2b01afa74dd789c7a187c062352d4de)

- **docs**(prd-44): эскиз экрана распределения баллов (2026-08-06) [`63aa92ef5d97581688af56a5a9f02b5e8ab28798`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/63aa92ef5d97581688af56a5a9f02b5e8ab28798)
  Шесть состояний на холсте согласованного экрана вопроса: нетронутый,
  частичное распределение, весь бюджет, ненулевой минимум, узкий экран и
  обзор ответов. Дельта относительно согласованного экрана ровно одна —
  блок ответа.
  
  Пара «ползунок и числовое поле» собрана из существующих примитивов
  (Slider, NumberInput). Новым в дизайн-системе становится только их группа
  с общим бюджетом: доступный максимум КАЖДОЙ строки зависит от значений
  соседних строк, то есть это состояние группы, и владеть им должен один
  компонент.
  
  Решение, которого не было в спеке: шкала ползунка ФИКСИРОВАНА бюджетом, а
  подвижный стоп показан глушёной зоной с чертой. Иначе одно из двух —
  либо значение строки визуально прыгает при изменении соседней строки,
  либо учащийся упирается в невидимую стену и не понимает, почему ползунок
  дальше не идёт.
  
  Проверено в браузере, все шесть состояний. Три дефекта найдены и
  исправлены по скриншотам: обёртка аннотации схлопывала ползунок до точки,
  строка с нулевым баллом была помечена как «с весом», счётчик остатка не
  читался с одного взгляда.

- **docs**(prd-44): состояние плана после фазы 1 и принятые отклонения (2026-08-06) [`3c62507b554e9ed42b7bd9e8d7dbdd58ba12fc0e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3c62507b554e9ed42b7bd9e8d7dbdd58ba12fc0e)
  Записаны три решения, отличающиеся от первоначального плана: бюджеты
  едут в конфигурации оценивания, точная арифметика домена сложнее формулы
  FR-15, и долг фазы 4 по клиентской кнопке «Рассчитать по вкладам».

- **docs**(wireframes): подтянуть эскизы под новую иконку отладочного прогона (2026-08-06) [`89a3ab9bce7d11f07eaad957e371331395eca464`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/89a3ab9bce7d11f07eaad957e371331395eca464)
  Эскизы принимаются как эталон UI, поэтому оставшаяся в них колба
  означала бы расхождение с кодом уже на следующей сверке. Спрайты
  `i-flask` заменены на `i-bug-play` в эскизах домашней страницы (PRD-25)
  и окна отладки (PRD-18).

- **docs**(prd-44): развернуть скелет в PRD типа «распределение баллов» (2026-08-06) [`4b1e3876daa5f65306862a1f58f98493b945c819`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4b1e3876daa5f65306862a1f58f98493b945c819)
  Требования собраны на референсе «Опросник ЧИЛ V3.6»: механика проверена
  численно — контрольное заполнение через закладываемое правило вклада даёт
  34/16/14/34 при сумме 98, совпадая с итогами файла.
  
  Закрыты все восемь развилок скелета: тип всегда измерительный (эталон с
  частичным баллом вынесен в будущий PRD), бюджет с единым доменом на вариант,
  строгая готовность «сумма равна бюджету», привязка к шкалам через вклады, а не
  в карточке вопроса. Вклад впервые задаёт учащийся — отсюда новый источник
  option_allocation и отдельная ветвь домена шкалы, иначе нормализация в проценты
  считалась бы от завышенного максимума.
  
  В охват добавлена ведущая и слабая шкала: у ipsative-методики ничья реальна и в
  референсе разрешается порядком колонок, поэтому правило ничьей задано явно, а
  отчёту даны tiedCount и margin.
  
  Ревизия классификатора типов вопроса записана в технический долг: риск
  молчаливого пропуска нового типа вскрылся третий раз подряд.
  
  Референсный файл опросника добавлен в docs/references — на него ссылается
  приёмка.

- **docs**(specs): привести статус PRD-16 к факту — реализован, кроме отложенного FR-90 (2026-08-06) [`5a81efcfad69591a394aaf71f084855e788eb594`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5a81efcfad69591a394aaf71f084855e788eb594)
  Шапка спеки и ROADMAP утверждали «частично реализован, остаток: валидация
  конструкторов и порядок/перемешивание вариантов», хотя блоки D и E закрыты
  коммитами от 2026-06-16 и 2026-07-15. Расхождение уводило планирование:
  закрытая работа числилась в разделе «треки в работе — что осталось».
  
  Статус выведен сверкой кода, а не шапок: маршрут раздела и редиректы,
  FileUploader без ввода URL, валидация с баннером, ручки перетаскивания и
  переключатель, а также выдача на обоих хостах. Добавлена карта реализации
  (раздел 6.1) блок -&gt; FR -&gt; коммиты -&gt; код, чтобы следующая сверка шла по
  ссылкам. Незакрытым остаётся только сознательно отложенный FR-90.

- **docs**(roadmap): собрать незакрытое в один раздел «Что не закрыто» (2026-08-06) [`7739b259149b197ded3d3cdd847e6cc1cafb9cdc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7739b259149b197ded3d3cdd847e6cc1cafb9cdc)
  Ответ на вопрос «что ещё предстоит» приходилось собирать из трёх мест:
  статус трека — в §0, повод и зависимости — в §0.2, долг — в §0.3, а внутри
  строк закрытое и открытое шли вперемешку. Добавлен §0.0: треки в работе с
  остатком по каждому, не начатые с готовностью требований и блокером, хвосты
  закрытых треков, открытые пункты техдолга, документационный долг и открытые
  задачи трекера. Нормативным источником статуса остаётся §0.
  
  Тем же проходом строки §0 приведены к факту — сводка по неверным строкам
  была бы хуже её отсутствия:
  
  \- PRD-34 значился «НЕ НАЧАТ» при влитом в main треке;
  \- PRD-23, PRD-29 и PRD-31 держали остатки, закрытые ещё в июле-августе;
  \- пометки «спека своего статуса не отражает» у PRD-39/40/42 устарели: эти
    спеки уже актуальны, устаревшими были сами пометки;
  \- PRD-20 и PRD-31 закрыты (см. предыдущий коммит).
  
  Из техдолга снят пункт «в PDF-отчёте тема с неопределённым вердиктом
  печатается с меткой "Не пройден"» — исправлен коммитом 336eedb.
  
  Открытые задачи трекера сверены с `gh issue list`: #24 закрыт, открыты
  #33/#32/#31/#28/#27/#26/#12.

- **docs**(specs): закрыть PRD-20 и PRD-31, привести статусы PRD-23 и PRD-34 к факту (2026-08-06) [`a51f498aa35d1c1b6e75a76c229c1fb35edcb10a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a51f498aa35d1c1b6e75a76c229c1fb35edcb10a)
  Заголовки четырёх спек расходились с состоянием кода и с принятыми
  решениями, а заголовок — первое, по чему сверяют статус трека.
  
  Закрыты:
  
  \- PRD-20. Фаза 3 закрыта целиком: 5.15 (выкат) — решением владельца
    продукта, новые тесты уходят с новым рантаймом сразу, действующие
    переиздаются на границе цикла; разовой миграции и уведомления учащихся
    не требуется. Записано ограничение принятой политики: технического гарда
    под ней нет — ре-экспорт доступен автору в любой момент, признака «идёт
    цикл аттестации» в модели не существует, соблюдение границы
    организационное. Попутно в §11 закрыты вопросы 2 (решён моделью 5.13)
    и 5 (та же политика выката).
  \- PRD-31. Остаток приёмки — барьер B при разведённых часах — подтверждён
    владельцем продукта; добавлен §12.2 с оговоркой, что протокол прогона в
    документ не прикладывался и закрытие опирается на подтверждение, а не на
    отдельный отчёт.
  
  Приведены к факту:
  
  \- PRD-23. Стояло «В РАБОТЕ, закрыты Э0-Э1» при закрытых Э0-Э9: код в main
    (`shared/template/themes.ts`, `theme-css.ts`), «Сертификация» объявляет
    `themes[]` и с версии Э7 ушла дальше по номерам.
  \- PRD-34. Стояло «код не начат, эскизов нет» при влитом в main треке
    (merge 029a292, миграция 038, `shared/template/protection/`) и пройденной
    браузерной приёмке. Приёмка ЧАСТИЧНАЯ — непроверенное перечислено.

- **docs**: закрыть техдолг и блокеры, державшиеся на недоступности WebTutor (2026-08-06) [`5a53d2e2ac06935535c10e127acf4c16b806cc35`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5a53d2e2ac06935535c10e127acf4c16b806cc35)
  Стенд WebTutor стал доступен, и на нём приняты SCORM-пакет, retake gate и
  таймер (reload посередине попытки, kill вкладки, семантика cmi.total_time).
  Это снимает ограничение, из-за которого LMS-специфичные механики годами
  принимались локальным плеером и висели непроверенными.
  
  \- PRD-20 / BR-14-07: гейт «валидация на целевой LMS до выката» пройден,
    техдолг снят.
  \- PRD-21: блокер старта снят — контракт webtutor_cooldown подтверждён на
    цели; сам трек по-прежнему не начат. Закрыт OQ-01: временный режим
    проверки на мок-данных не нужен.
  \- PRD-31 (R-3) и PRD-37: риски пересмотрены. По PRD-37 формулировка
    смягчена, но не снята — целевая платформа профиля 1.2 это LMS заказчика,
    и одна платформа готовности профиля не доказывает.
  \- BR-07-20 и BR-27-04: зафиксирована полевая приёмка гейта и возможность
    получить реальные образцы выгрузки как эталоны приёмки обмена.
  
  Попутно исправлены два пункта приёмки PRD-20, ссылавшихся на механизмы,
  которые сама спека потом отменила: watchdog отката часов (5.8, не
  реализуется) и счётчики reload / single-active-session (5.9, отложены).
  Критерии пережили отменившее их решение, и проверять там было нечего.
  Остаточный вопрос параллельных окон переквалифицирован: он не про обход
  отсчёта, а про затирание общего suspend_data двумя окнами, то есть ближе
  к PRD-36.
  
  Не отыграна ручная правка suspend_data (tamper-подпись) — помечена как
  открытая, но гейт не держит: проверяется локально и от LMS не зависит.

- **docs**(prd-10): развести доставленное и реально отложенное в статусе «Цены ответа» (2026-08-06) [`1748420215bcb75f7a910c7168dbd70298e4923a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1748420215bcb75f7a910c7168dbd70298e4923a)
  Статус PRD-10 с 2026-06-06 числил нереализованными три вещи, из которых две
  на деле были доставлены попутно с PRD-15 блоком D: серверный градуированный
  check-answer.ts для веб-попыток и scoreRatio/«Частично правильно» в SCORM CMI.
  Из-за этого документ занижал готовность трека и мешал понять, что осталось.
  
  Реально открытым остаётся другое, и это записано явно: preview-модалка балла
  и «Частично правильно» в мгновенном фидбеке веб-хоста для стандартного режима,
  где take-test.tsx до сих пор считает верно/неверно собственной логикой точного
  совпадения мимо @shared/scoring/engine. Итоговый балл при этом считается верно,
  поэтому пробел незаметен. Оба пункта отслеживаются issue #31.
  
  Заодно актуализировано место хранения конфига: цена ответа переехала с вопроса
  на тест (миграция 028), сам движок не менялся.

- **docs**(brd): актуализировать BRD до состояния треков на 2026-08-05 (2026-08-06) [`c9c86893e722b6b24036d2642370bf41b9933de2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c9c86893e722b6b24036d2642370bf41b9933de2)
  BRD не пересматривался с 2026-08-01, за это время ROADMAP ушёл вперёд на
  десять треков, а по одному из них решение было пересмотрено полностью.
  
  \- BR-20 переписан: «доставка вложений, требования не собирались» больше не
    соответствует действительности. Зафиксированы оба прохода — файл в
    медиатеке (PRD-32) и последующий отказ от загрузки в пользу внешней
    ссылки (PRD-42), потому что относительный адрес не резолвится ни в
    PDF-отчёте, ни вне SCORM-пакета. Легаси-дескрипторы с битой ссылкой
    помечены принятым техдолгом, а не дефектом.
  \- BR-18 переведён в реализованные: приёмка пройдена на трёх путях.
  \- Заведены BR-28..BR-31 (листинг кода, защита от копирования,
    компактность состояния прогона, профиль SCORM 1.2) — треки существовали
    только в ROADMAP, из-за чего бизнес-требования по ним нигде не были
    сформулированы.
  \- Треки, уточняющие уже принятые требования (PRD-38/39/40/41/43),
    добавлены в таблицу расширений; идея PRD-44 внесена без номера этапа,
    как и позиции BR-21 — решения по ней не приняты.
  
  Синхронизированы модель данных, приоритизация этапов, диапазон этапов с
  критериями в PRD и таблица рисков.

- **docs**(roadmap): актуализировать статус PRD-38..44 и закрыть решённый техдолг (2026-08-05) [`925d6170bc666a19afc0fb77bd740cf4a6e3abba`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/925d6170bc666a19afc0fb77bd740cf4a6e3abba)
  Roadmap не знал о PRD-38..44 (специфицированы и частично уже влиты в main),
  показывал PRD-35 нереализованным вопреки нормативной таблице §0, и держал
  два пункта техдолга, закрытых issue #30 (a9b1628) и коммитом 85426a0.
  Заодно поправлены собственные строки «Статус» в спеках PRD-38/39/40/42 —
  отставали от факта после мержа в main.

## [2.11.0](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.10.0-beta...v2.11.0)

### Features

- **feat**(prd-43): wire quickAdvance into the web learner runtime + tests (2026-08-05) [`79a6f8554b0e47443f8d52db72eba965f8c87744`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/79a6f8554b0e47443f8d52db72eba965f8c87744)

- **feat**(prd-43): add the independent quick-advance toggle to the test editor (2026-08-05) [`889c16ea520d76ed7e836cd390123deeac4992d2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/889c16ea520d76ed7e836cd390123deeac4992d2)

- **feat**(prd-43): add quickAdvance to the editor model and its API mappers (2026-08-05) [`6680cf46e70e02108bfb71d8babce82a78ab8286`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6680cf46e70e02108bfb71d8babce82a78ab8286)

- **feat**(ds): уменьшить круги-деления шкалы Ликерта через новый размер ou-stepper--s (2026-08-05) [`fa365ed10e923f79eddc226669427996255b4bf2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fa365ed10e923f79eddc226669427996255b4bf2)
  Булиты 44px в ou-stepper--choice визуально доминировали над подписями; добавлен
  размерный DS-модификатор (32px, DS-линейка --ou-size-control-s) по аналогии с
  ou-switch/ou-btn/ou-avatar, вместо точечного патча в шаблоне. renderScale
  подключает его на горизонтальной ветке шкалы (вертикальная уже 28px).

- **feat**(prd-43): thread quickAdvance into the SCORM runtime's nav-state builder (2026-08-05) [`c63d1c9cfc0a141d53f83f0aa7331b9d49334b55`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c63d1c9cfc0a141d53f83f0aa7331b9d49334b55)

- **feat**(prd-43): propagate quickAdvance through settings service, routes, SCORM export data (2026-08-05) [`f02e54d42a13c7e953a9660c2cb27977e4fa04fd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f02e54d42a13c7e953a9660c2cb27977e4fa04fd)

- **feat**(prd-43): split quickAdvance from flexible in buildQuestionNav (2026-08-05) [`2a673236d1cb3803c7c1e3aec230a7478c8898d1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2a673236d1cb3803c7c1e3aec230a7478c8898d1)

- **feat**(prd-42): переименовать «Документы» в «Материалы» в превью обратной связи (2026-08-05) [`1c3d9606715574c187eb4d35db70d1a83651c0e1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1c3d9606715574c187eb4d35db70d1a83651c0e1)
  Финальный обзор ветки поймал разъезд меток: редактор (Task 2) уже
  называет секцию «Материалы», а карточка предпросмотра feedback-preview.tsx,
  которая открывает этот самый редактор, всё ещё показывала «Документы» —
  для автора выглядело как незавершённое переименование одного и того же
  списка вложений.

- **feat**(prd-43): add tests.quick_advance column + backfill migration (2026-08-05) [`e4c30750328b7ee11f01cbc1aab50f574e835ba8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e4c30750328b7ee11f01cbc1aab50f574e835ba8)

- **feat**(prd-42): удалить назначение purpose=feedback-asset из /api/media/upload (2026-08-05) [`3e92944521c9a5e9a14d76294b7f53ea7826a919`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3e92944521c9a5e9a14d76294b7f53ea7826a919)
  Единственный вызывающий (загрузка PDF-вложения обратной связи) удалён
  вместе с секцией «Материалы» — маршрут стал мёртвой веткой без клиента.

- **feat**(prd-42): секция «Материалы» — ссылка вместо загрузки файла (2026-08-05) [`e648027b092dd38b75885c8eba0c22f9ec8962d7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e648027b092dd38b75885c8eba0c22f9ec8962d7)
  Вложение обратной связи теперь редактируемая строка {title, url}, по
  образцу «Курсы» — без выбора файла, без обращения к медиатеке. Легаси-
  строки (PRD-32, url вида /api/media/&lt;id&gt;) отображаются и правятся так
  же, их адрес никак не помечен и не блокирует сохранение.

- **feat**(prd-42): fileName/mimeType вложения обратной связи — опциональные (2026-08-05) [`7786fb50c55de0328677415de3e2f200921f8ff8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7786fb50c55de0328677415de3e2f200921f8ff8)
  Материал (PRD-42) — это title+url, без файла. fileName/mimeType остаются
  только для чтения legacy-дескрипторов, загруженных через отменённый
  аплоад (PRD-32).

- **feat**(prd-40): author UI — outcome-split cooldown switch + fields, drop best-effort banner (2026-08-05) [`9a42825a3ad34751aff2186c6a1ea0456720aa56`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9a42825a3ad34751aff2186c6a1ea0456720aa56)

- **feat**(prd-40): bake cooldownByOutcome + split periods into the SCORM TEST_DATA (2026-08-04) [`05a7b5ac9247560d75e86c229f2e7fb7640c8239`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/05a7b5ac9247560d75e86c229f2e7fb7640c8239)

- **feat**(prd-40): thread attempt outcome into AttemptFact at every construction site (2026-08-04) [`5970d58d9dba840bef453de9d47974bc5d479087`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5970d58d9dba840bef453de9d47974bc5d479087)

- **feat**(prd-40): web barrier A resolves the cooldown period by last attempt's outcome (2026-08-04) [`864720da72a938d6b99d12564b7c602ad652d652`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/864720da72a938d6b99d12564b7c602ad652d652)

- **feat**(prd-29): домен и валентность числового показателя, как у шкалы (2026-08-04) [`c68a85f29422b6103285cc5fb16c634fb0dc1707`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c68a85f29422b6103285cc5fb16c634fb0dc1707)
  ResultVariableModel получает domainMin/domainMax/valence — то же значение и
  round-trip, что у ScaleModel, нужно для радара компетенций (PRD-35): радиус
  считается от домена показателя, а не только от границ полос.

- **feat**(prd-40): seed passedStateIn default, remove the unused suspend_data_cooldown plugin (2026-08-04) [`e38502f40a5cebd38dc96ce7e953b5d8e2366cd5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e38502f40a5cebd38dc96ce7e953b5d8e2366cd5)

- **feat**(prd-40): webtutor_cooldown resolves the period by attempt outcome (passedStateIn) (2026-08-04) [`8776d34a8785598513d0058173d6831a3493b78f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8776d34a8785598513d0058173d6831a3493b78f)

- **feat**(prd-40): resolveCooldownDays resolver + buildRetakeState shows the resolved period (2026-08-04) [`c3b241a6bf9f5131fb2e6da98e0dbbbe7a696f4d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c3b241a6bf9f5131fb2e6da98e0dbbbe7a696f4d)

- **feat**(prd-40): EligibilityContext.retakePolicy carries the outcome-split fields (2026-08-04) [`7ed086b357f0ab9f4c5645598317ed8f7a514bf1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7ed086b357f0ab9f4c5645598317ed8f7a514bf1)

- **feat**(prd-40): cooldownByOutcome switch + split cooldown fields in retakePolicySchema (2026-08-04) [`bdae989ac040b707f1a04ac08075e10601944799`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bdae989ac040b707f1a04ac08075e10601944799)

- **feat**(prd-39): Certification — зазор и вердикт слитой панели сопоставления (2026-08-04) [`69efba2b9e672dbea2d942403a684e1b7749f7de`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/69efba2b9e672dbea2d942403a684e1b7749f7de)

- **feat**(prd-39): Стандартный шаблон — зазор и вердикт слитой панели сопоставления (2026-08-04) [`f6c99dc5fe131feb735cb4ced8cd524020ee2331`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f6c99dc5fe131feb735cb4ced8cd524020ee2331)

- **feat**(prd-39): renderMatching печатает режим слияния вместо стрелки (2026-08-04) [`e09d5bf4274138918fc826309947a9ffb5b48992`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e09d5bf4274138918fc826309947a9ffb5b48992)

- **feat**(prd-30): лист «Настройки» книги Excel — правило выдачи теста (Э14) (2026-08-04) [`e176370c0a16fd8c249e92500734ffb3e29baa53`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e176370c0a16fd8c249e92500734ffb3e29baa53)
  Новый лист «параметр/значение», первым в книге (описывает саму книгу, а не
  её содержимое): единственный поддержанный параметр «Порядок выдачи
  вопросов» с тремя значениями, незнакомое имя параметра — ошибка импорта.
  На листе «Структура» пустая ячейка «Случайный порядок вопросов» теперь
  читается как «как в тесте» (раньше — как «да»). Книга без листа «Настройки»
  импортируется как раньше (обратимость экспорт -&gt; импорт). Руководство и его
  PDF обновлены под новый лист и сдвиг нумерации разделов.

- **feat**(prd-30): контрол порядка выдачи в редакторе теста (Э13) (2026-08-04) [`5796a8cd139585e2d69e9d992c82aaf63b4b7ae4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5796a8cd139585e2d69e9d992c82aaf63b4b7ae4)
  Контрол теста по утверждённому эскизу: три позиции в сплошном режиме, две в
  секционном (переход в секционный переписывает shuffle_all в random, FR-17).
  Контрол темы — три позиции, умолчание «как в тесте» (null). Zod-приём
  и test-settings.ts несут tests.question_order и nullable questionOrder
  раздела. Стили строки контрола теста в паре с существующей строкой темы —
  единая ширина подписи и триггера селекта.

- **feat**(prd-30): SCORM-пакет — question_order в манифесте и JS-двойник сборки (2026-08-04) [`c411f106c87e681071fed7a2148121d8ab0d265a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c411f106c87e681071fed7a2148121d8ab0d265a)
  test-json.ts печёт questionOrder теста и наследование темы; поля опускаются,
  если тест настройку не трогал — пакет остаётся байт-идентичным прежнему
  (FR-23). effectiveSectionOrder/assembleDelivery перенесены в плоский JS
  рантайма (generateVariant) рядом с orderQuestions, паритет с TS-движком
  закрыт golden-тестом на одинаковом входе.
  
  Предыстория: снят безусловный второй проход перемешивания в ветке
  linear_flat — он стирал уже выстроенный порядок темы, а веб-хост такого
  прохода не имел вовсе, из-за чего один тест шёл в двух хостах по-разному.
  Перемешивание теперь ровно в одном месте, shuffle_all возвращает смешивание
  поперёк тем именно туда. docs/specs/prd-4 обновлена: global_shuffle остаётся
  нереализованным намерением, ссылка на PRD-30 §6.4.

- **feat**(prd-30): веб-сборка попытки использует assembleDelivery (FR-19) (2026-08-04) [`1a069802f571803cf2a29334a70c09575525da90`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1a069802f571803cf2a29334a70c09575525da90)
  server/routes/attempts.ts зовёт assembleDelivery вместо поштучного
  orderQuestions при построении варианта (сплошной и адаптивный режимы);
  форма variant.sections[].questionIds сохраняется — от неё зависят экраны
  разделов, обзор и итоги, при shuffle_all меняется только порядок потока.
  На клиенте отдельный variant.deliveryOrder восстанавливает тот же поток при
  возобновлении попытки: секции хранят состав, вариант — поток, и они
  расходятся только при «полном перемешивании».

- **feat**(prd-30): assembleDelivery — сборка потока выдачи в shared/draw (2026-08-04) [`b9f70ecd98e2655a177c7f9647d395f75ae75ae1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b9f70ecd98e2655a177c7f9647d395f75ae75ae1)
  effectiveSectionOrder разрешает переопределение темы поверх правила теста
  (FR-18: null темы = fixed теста -&gt; fixed, иначе random). assembleDelivery
  собирает темы блоками при fixed/random; при shuffle_all перемешивает единицы
  потока — тема с действующим random разбирается на вопросы, тема с fixed
  остаётся неразрывным блоком. В секционном режиме shuffle_all гасится до
  random. Вход — уже отобранные вопросы, отбор и квоты PRD-11 не тронуты.

- **feat**(prd-30): tests.question_order + nullable test_sections.question_order (2026-08-04) [`cdb6eca1aa2f11c65cacbfcd007c481086458efd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cdb6eca1aa2f11c65cacbfcd007c481086458efd)
  Миграция 0011: колонка тестов question_order (fixed/random/shuffle_all,
  умолчание random — сегодняшнее поведение), question_order раздела теряет
  NOT NULL/DEFAULT — существующие явные fixed остаются переопределением,
  прочие мигрируют на NULL («как в тесте»). Zod-типы обновлены под nullable.

- **feat**(prd-38): плашка аудио-плеера и стили полноэкранного просмотра (2026-08-04) [`01f7e309b6c1bfdc858338366f1247ababcfb6e5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/01f7e309b6c1bfdc858338366f1247ababcfb6e5)

- **feat**(prd-38): медиа встроено в блок вопроса, ответы во всю ширину (2026-08-04) [`ee59d728685d0bc37bda48396eec325b0697d853`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ee59d728685d0bc37bda48396eec325b0697d853)

- **feat**(prd-38): слот медиа переехал в блок вопроса в обоих шаблонах (2026-08-04) [`eb63912b857bfa1b051ccdd6ae3da8a07cb7b00c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eb63912b857bfa1b051ccdd6ae3da8a07cb7b00c)

- **feat**(prd-38): рантайм пакета делегирует медиа общему рендереру (2026-08-04) [`e95900a7ab60cbb5779e124f5862cdc398ec1514`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e95900a7ab60cbb5779e124f5862cdc398ec1514)

- **feat**(prd-38): веб-хост печатает медиа общим рендерером (2026-08-04) [`780b5527249b5249ce0939c08add415478c1f86b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/780b5527249b5249ce0939c08add415478c1f86b)

- **feat**(prd-38): полноэкранный просмотр медиа переехал в общий код (2026-08-03) [`3149ed9bd4eb49d488fbf5688b1343c5f38df25e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3149ed9bd4eb49d488fbf5688b1343c5f38df25e)

- **feat**(prd-38): общий рендерер медиа вопроса для обоих хостов (2026-08-03) [`bb5237ce81e4a456b8c1e9ba153e827e8c82e8b2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bb5237ce81e4a456b8c1e9ba153e827e8c82e8b2)

- **feat**(report): отчёт печатает консолидированные тексты обратной связи (2026-08-03) [`e5c2cffe83a33c2c79facb172ff99de7ff62885c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e5c2cffe83a33c2c79facb172ff99de7ff62885c)

- **feat**(results): адаптивный режим питает консолидированный блок на обоих хостах (2026-08-03) [`8494c871072963461282d8425ec14bbeed88c41e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8494c871072963461282d8425ec14bbeed88c41e)
  Разметка блока в адаптивных макетах уже была, общий сборщик его уже собирал — не хватало источников. Теперь их подают оба хоста.
  
  Пакет: адаптивный рендер отдаёт в общий сборщик обратную связь теста
  (vrTestFeedback) и материалы тем (vrTopicFeedbackTexts / vrTopicAssets) —
  те же чтения, что у стандартных экранов итогов, а не третья копия логики.
  
  Веб: адаптивный результат попытки получил те же два поля, что стандартный
  (feedbackTexts / recommendedAssets, .default([]) — сохранённые попытки
  остаются валидными); заполняются на завершении адаптивной попытки теми же
  общими правилами (topicFeedbackTexts / feedbackAssets). Экран итогов
  адаптивной попытки берёт материал результата так же, как стандартный, и
  читает из него обратную связь теста.
  
  Правило видимости не дублируется: оно живёт в общем сборщике.

- **feat**(results): адаптивный экран итогов показывает блок рекомендаций (2026-08-02) [`2bf3e2e4e308106fcfcc32a30bf8b065dd565402`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2bf3e2e4e308106fcfcc32a30bf8b065dd565402)
  Блок «Рекомендации» существовал только в стандартных итогах: адаптивный экран не
  показывал ученику ни текстов обратной связи, ни курсов, ни мероприятий, ни вложений
  ни у одной темы. Причин было две — сборщик адаптивного контекста блок не собирал, а
  в адаптивных макетах обоих шаблонов не было самой разметки.
  
  Сборка идёт ТОЙ ЖЕ функцией collectRecommendations и из тех же источников, что в
  стандартном режиме: обратная связь теста, затем материалы тем. Гейт по вердикту темы
  вынесен в общий topicRecommendationSources — режимы различаются только тем, как
  называется провал темы: в стандартном это passed === false, в адаптивном — ни один
  уровень не подтверждён (та самая ветка, где движок берёт failureFeedback и гасит
  overallPassed, и по которой пакет отображает адаптивную тему на стандартную).
  
  Разметка перенесена из results.html дословно, в оба шаблона.

- **feat**(results): тексты тем и разделов вливаются в консолидированный блок (2026-08-02) [`e0df9e42c8d2a12987d03afa6a7943f6c01cbc39`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e0df9e42c8d2a12987d03afa6a7943f6c01cbc39)

- **feat**(results): текст обратной связи темы сохраняется вместе с попыткой (2026-08-02) [`34c8665d5307a3277b267456bbd4ad91340c222a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/34c8665d5307a3277b267456bbd4ad91340c222a)

- **feat**(media): полная пересборка индекса покрывает обратную связь (2026-08-02) [`471cab65520d1e7ec21ed0ed6c9a4a414f63a8dc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/471cab65520d1e7ec21ed0ed6c9a4a414f63a8dc)
  Обход в reindexAllUsages дополнен четырьмя типами PRD-32: test_feedback,
  topic_feedback и наборы scale_feedback/variable_feedback, ключуемые
  идентификатором ТЕСТА (спека §6.1). Блок обратной связи теста обходится
  отдельно от оформления, чтобы один файл не учитывался дважды.
  
  Очистка строк исчезнувших сущностей расширена теми же типами; приём с
  перечитыванием идентификаторов сохранён, четыре теста-ключевых типа делят
  один свежий запрос. Оговорка про «зарезервированные, не подключённые ни к
  одному писателю» типы снята: писатели появились.

- **feat**(media): индекс использования для обратной связи шкал и показателей (2026-08-02) [`a831c05fb45535dfc3195bfbe9f0feaa407f6af9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a831c05fb45535dfc3195bfbe9f0feaa407f6af9)

- **feat**(media): индекс использования для обратной связи темы (2026-08-02) [`85fc5defe7504863aee03ef1759273f5f3801fe6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85fc5defe7504863aee03ef1759273f5f3801fe6)

- **feat**(media): индекс использования для обратной связи теста (2026-08-02) [`753f5a58b5f5a7839d51b36f1e1fdb7de687b259`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/753f5a58b5f5a7839d51b36f1e1fdb7de687b259)

- **feat**(media): доступ ученика к вложению обратной связи темы, шкалы и показателя (2026-08-02) [`bd85bcfd4a30ff9e5da87460eba46ebb2d3e3f90`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bd85bcfd4a30ff9e5da87460eba46ebb2d3e3f90)

- **feat**(scorm): упаковка медиа по адресу реестра, включая вложения обратной связи (2026-08-02) [`ab48f0e70bf3864b1eedea07d953a0222d0d5230`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ab48f0e70bf3864b1eedea07d953a0222d0d5230)

- **feat**(scorm): резолвер ссылок медиатеки для сборки пакета (2026-08-02) [`ef30195b84a74de909a4a58070f1b559ac670057`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ef30195b84a74de909a4a58070f1b559ac670057)

- **feat**(prd-32): вложение без файла помечено и заменяемо в редакторе (2026-08-02) [`7b07b8ee15a7ca5698462f9f8167225d95084e38`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7b07b8ee15a7ca5698462f9f8167225d95084e38)

- **feat**(prd-32): редактор загружает вложение сразу при выборе файла (2026-08-02) [`3c46c1bbdfea65ef3874b5ed5889ec331b38c8dc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3c46c1bbdfea65ef3874b5ed5889ec331b38c8dc)
  Выбранный PDF уезжает в медиатеку немедленно
  (POST /api/media/upload?purpose=feedback-asset), а в дескриптор попадает
  канонический адрес из ответа сервера. Черновик больше не держит File.
  Отказ сервера показывается баннером, «Сохранить» заблокировано на время
  загрузки.

- **feat**(prd-32): строгая проверка вложения обратной связи на сервере (2026-08-02) [`ea34bd872f2711ca49639578f8b7d3c721f92f12`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ea34bd872f2711ca49639578f8b7d3c721f92f12)

- **feat**(prd-32): дескриптор вложения несёт канонический адрес медиатеки (2026-08-02) [`652050fc3c5bc6793440a2a720fb42e75eca5c81`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/652050fc3c5bc6793440a2a720fb42e75eca5c81)

- **feat**(templates): радар компетенций в «Сертификации» — паритет с эталоном (PRD-35) (2026-08-02) [`14db7225175d3030cfc3774f390b14ae210aa348`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/14db7225175d3030cfc3774f390b14ae210aa348)
  Внешний шаблон возит побайтовые копии раскладок «Стандартного» плюс форк стилей, и
  паритет держится вручную: радар приехал в эталон, а сюда — нет. Догнаны экран итогов
  и оба отчёта (обычный и адаптивный), плюс настройка `showCompetencyRadar` в трёх
  местах манифеста, без которой автор не может включить диаграмму в этом шаблоне вовсе.
  
  Двухколоночная раскладка блока шкал включается классом, который готовит ЯДРО
  (`result.scalesBlockClass`), а не селектором `:has(.tb-radar)`: пакет рендерится
  внутри того движка, который подставит LMS, и полагаться на поддержку `:has` там нельзя.
  
  Стили разведены по назначению. В `theme.css` каждый цвет — токен ДС, поэтому палитра
  бренда затекает сама и дельты не требуется. В `report.css` палитра своя: отчёт
  печатается и растеризуется без ДС, контейнерных запросов и интерактива, ширина A4
  известна заранее. Там же вручную заданы тона уровня в списке шкал — теми же оттенками,
  что даёт светофорная рампа ядра; без этого метка «Высокий» ушла бы в документ обычным
  текстом, а цвет вершины на диаграмме остался бы без пары в списке.

- **feat**(media): канонизация легаси-адресов при сохранении контента (2026-08-02) [`181797d977987463b9bc3ba4d466c44d63b3066f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/181797d977987463b9bc3ba4d466c44d63b3066f)
  Задача 15: сохранение вопроса приводит адреса /uploads/media/... в
  целых значениях полей (mediaUrl) к каноническому /api/media/&lt;id&gt; до
  записи сущности в хранилище — индекс из Задачи 10 пишется уже по
  каноническому виду. Неразрешимый адрес остаётся как есть, разметка
  (HTML-поля) не переписывается — parseMediaRef требует, чтобы адресом
  было ВСЁ значение поля.
  
  rewriteMediaRefs (media-refs.ts) — чистый обходчик-перезаписыватель,
  canonicalizeEntityMedia (usage-index.ts) — асинхронная обёртка с
  резолвом через реестр. Мок хранилища routes.questions.coverage.test.ts
  дополнен getMediaAssetByStorageKey — без него канонизация валила один
  ранее зелёный кейс с legacy mediaUrl.

- **feat**(prd-34): скрытие задания при потере окном фокуса (2026-08-02) [`a43bac3023ce60d630ba645793010caa419e9b26`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a43bac3023ce60d630ba645793010caa419e9b26)
  Основной сигнал — visibilitychange; потеря фокуса окном берётся с задержкой
  300 мс и отменяется мгновенным возвратом: в рамке LMS blur срабатывает на
  каждый клик по родительской странице. Скрытие отложено, показ — нет: заглушка
  снимается сама и немедленно, задержка на возврате читалась бы как подвисание.
  
  Кнопки на заглушке нет. Обязательный щелчок был бы платой за каждое
  переключение окна, включая всплывающие окна самой LMS, то есть маленьким
  наказанием — а трек наказаний не вводит.
  
  Найдено при проверке в живом пакете: вуаль ставится на каждый регион
  периметра отдельно, и на коротком заголовке вопроса сообщение обрезалось по
  границе региона, а на соседнем регионе дублировалось. Теперь сообщение
  печатается один раз, на самом крупном из закрываемых регионов; площадь
  измеряется по факту, а не выводится из структуры разметки.

- **feat**(media): выключение публичной статики uploads и совместимостный алиас (2026-08-02) [`f9dc62c14cb07e909b3eabcd919d756aa4e12fc0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f9dc62c14cb07e909b3eabcd919d756aa4e12fc0)
  Раздача /uploads/media/... идёт через legacyUploadsAlias с тем же правилом доступа,
  что и канонический маршрут /api/media/:id — общая функция deliverAsset исключает
  дублирование заголовков, диапазонов и обработки ошибки потока. §11 спецификации
  приведён в соответствие фактическому порядку этапов (Э2/Э3 поменяны местами,
  выключение статики выделено в Э3.1).

- **feat**(prd-34): водяной знак на экранах попытки и итогов (2026-08-02) [`0e72585c9529b95f34d3d11e5450b461a275eca5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0e72585c9529b95f34d3d11e5450b461a275eca5)
  Знак строит ядро, а МЕСТО объявляет макет пустым якорем protection-mark.
  Поставляемый шаблон объявляет его на всех четырёх экранах знака: на вопросе —
  в строке подсказки справа, на обзоре и итогах — в начале тела сцены. Якоря
  нет — знак встаёт строкой в начало сцены; мера не отключается никогда.
  
  Разделение ответственности не стилистическое: если бы знак писал шаблон,
  внешний шаблон получил бы власть над читаемостью атрибуции — кегль в четыре
  пикселя, и знак формально есть, а на снимке его нет.
  
  Экран итогов знак несёт, но от копирования по FR-09 не защищается; настройка
  для него отдаётся отдельным полем ответа результата.
  
  В проверку работоспособности шаблона (PRD-3) добавлена информационная строка
  о наличии якоря — предупреждение, не ошибка: пусть «знак уехал наверх»
  открывается администратору при загрузке, а не участнику на приёмке.
  
  Демо-пакет собирается со включённым знаком и скрытием, иначе локальная
  приёмка этих мер требует ручной правки бейка.

- **feat**(media): удаление актива с отчётом «где используется» (2026-08-02) [`f780efa5ae179366a0063f9b001bc5f5596d06f5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f780efa5ae179366a0063f9b001bc5f5596d06f5)
  Отказ по образцу PRD-15 content-guard: 409 media_in_use со списком мест,
  пока актив используется (включая опубликованные снимки). Гонка между
  проверкой использований и удалением закрыта — нарушение внешнего ключа
  media_usages (23503) превращается в тот же 409, а не в 500. Физические
  байты удаляются по счётчику ссылок на контрольную сумму.

- **feat**(media): полная пересборка индекса использования и отчёт о сиротах (2026-08-02) [`ae5047b0ee6b2f50bcef3b5c23b4e5a5c935c832`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ae5047b0ee6b2f50bcef3b5c23b4e5a5c935c832)
  POST /api/media/reindex гоняет тот же обходчик syncEntityUsages по всем
  вопросам, всем контентным страницам и оформлению всех тестов (test_design),
  чтобы закрыть пути мимо записи индекса: импорт вопросов из Excel,
  дублирование вопроса/темы, согласование системных страниц при смене режима
  прохождения. Оформление индексируется как объект designSettingsJson, а не
  весь тест — тот же выбор, что в PUT /:id/design.
  
  Для полного прохода добавлен ContentPagesRepository.getAllContentPages()
  (по образцу getAllAssignments), IStorage.getTests() уже существовал.

- **feat**(prd-34): защита в пакете и её отключение в отладочном прогоне (2026-08-02) [`50a60831dbc599600bd20919b68f8980e30c4402`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/50a60831dbc599600bd20919b68f8980e30c4402)
  Настройки запекаются в TEST_DATA, признак сборки едет отдельным полем
  protectionActive: в отладочном прогоне (source: debug) защита и скрытие
  выключены, а водяной знак остаётся — автор должен видеть то, что увидит
  участник, но копировать текст своего вопроса ему никто не мешает.
  
  Сборщик решения объявлен ОДИН раз, отдельной утилитой, подключённой до
  render-модулей: части рантайма склеиваются плоско, и дубль имени в файле,
  идущем позже, молча перекрыл бы реализацию.
  
  Периметр в рантайме не перечисляется — его считает тот же общий построитель,
  что и на веб-хосте, поэтому хосты не могут разойтись.

- **feat**(prd-34): защита на ученических экранах веб-хоста (2026-08-02) [`7d79f25a30360d5c5055fddb2753c0f0a91a6ea5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7d79f25a30360d5c5055fddb2753c0f0a91a6ea5)
  Экран вопроса и экран обзора получают решение общего построителя, тот же,
  что и пакет. Отметка знака обезличена: идентификатор попытки, укороченный
  до шести знаков.
  
  Снято безусловное подавление. На веб-хосте стартовый экран и экран вопроса
  гасили copy/cut/contextmenu и запрещали выделение ВСЕГДА — наследие чистки
  Tailwind. Настройка теста этим не управляла, автор не мог скопировать текст
  в отладочном прогоне, стартовый экран был защищён вопреки FR-09, а в пакете
  ничего этого нет — то есть хосты уже расходились. Теперь мерой управляет
  один механизм на обоих хостах.
  
  Тест, закреплявший безусловное поведение, переписан под новый контракт: без
  protection экран не гасит ничего, с protection гасит внутри периметра.

- **feat**(media): индекс использования для контентных страниц и оформления теста (2026-08-02) [`0bf2dbd42ed880df0e03c96bb4a55b87a624be65`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0bf2dbd42ed880df0e03c96bb4a55b87a624be65)
  Задача 10б: закрывает пробел Задачи 10 — индексировались только вопросы,
  хотя content_page и test_design тоже перечислены в MediaEntityType. Без
  этого правило доступа откажет учащемуся в файле контентной страницы или
  оформления после отключения публичной статики /uploads (Задача 14).
  
  \- content-pages.ts: syncEntityUsages на создании, обновлении, смене варианта
    (replace-variant меняет valuesJson так же, как обычный PUT) и удалении
    страницы
  \- tests.ts: syncEntityUsages на сохранении и сбросе настроек оформления
    (PUT /:id/design) и на удалении теста (media_usages не каскадируется);
    индексируется сам объект designSettings, а не весь тест — иначе media из
    test_feedback задваивалась бы под test_design
  \- Синхронизация всегда после фиксации сущности, вне транзакции, в try/catch
    с логированием — по образцу вопросов: недостающая строка индекса безопасна
    (отказывает в доступе) и лечится пересборкой (Задача 11), а потерянное
    сохранение — нет

- **feat**(prd-34): блок «Защита» в настройках теста (2026-08-02) [`925fa26b93690fe08012febbe9b3d9ce9b944182`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/925fa26b93690fe08012febbe9b3d9ce9b944182)
  Три переключателя в разделе «Правила прохождения», за отбивкой от правил
  PRD-19: защита текста, водяной знак, скрытие при уходе из окна. Подчинённости
  между ними нет — FR-02, поэтому и в модели редактора они независимы.
  
  Загрузка существующего теста читает отсутствие поля как умолчание (FR-05):
  тест, созданный до PRD-34, открывается с включённой защитой — ровно так же,
  как его читает выдача попытки.
  
  Фикстуры тестов редактора дополнены тремя полями: runtime собирается в них
  литералом, и tsc не пропускал неполный объект.

- **feat**(media): индекс использования обновляется при сохранении вопроса (2026-08-02) [`66dfa72433df64bd7f1bf880dd9ee7905f573380`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/66dfa72433df64bd7f1bf880dd9ee7905f573380)

- **feat**(media): индексация существующих файлов в реестр (2026-08-02) [`d4e8a6d52dbd8b7c17aaa2e0f57ec0c3b911f75f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d4e8a6d52dbd8b7c17aaa2e0f57ec0c3b911f75f)

- **feat**(prd-34): три настройки защиты в схеме, сервисе и роутах (2026-08-02) [`996d17fe9776f3209c943a458b836c91d2e3f68b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/996d17fe9776f3209c943a458b836c91d2e3f68b)
  copy_protection (умолчание ВКЛ), protection_watermark и
  protection_hide_on_blur (ВЫКЛ) — независимы, ни одна не управляет другой.
  
  Умолчание ВКЛ означает, что защиту получают и уже существующие тесты:
  это принятое решение (FR-03), поэтому backfill в false в миграции НЕТ.
  
  Снимок публикации доработки не потребовал — он хранит строку теста целиком,
  поэтому новые колонки попадают в него сами. Отсутствие поля в СТАРОМ снимке
  читается как умолчание (FR-05), это и делает чтение в attempts.
  
  Миграция применена к dev-БД напрямую, без drizzle-kit push: push приводит
  базу к схеме ЭТОЙ рабочей копии и снёс бы колонки, добавленные соседними
  сессиями. ADD COLUMN IF NOT EXISTS аддитивен и безопасен.

- **feat**(media): защищённая раздача файла с диапазонами и ETag (2026-08-02) [`b3e0b31ea364d7cb6a2cebb4fde4adaa89f0122d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b3e0b31ea364d7cb6a2cebb4fde4adaa89f0122d)
  GET /api/media/:id заменяет публичную статику: право доступа проверяется до
  отдачи байт, Range/206, ETag/304, Cache-Control: private, обработка обрыва
  потока чтения без падения процесса.

- **feat**(prd-34): поле protection в контракте рендерера, экспорт в TBTemplate (2026-08-02) [`ce1d6c4ce0185bf8f9253e101df345dd98dfe030`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ce1d6c4ce0185bf8f9253e101df345dd98dfe030)
  renderScreenInto применяет защиту ПОСЛЕДНИМ шагом, после заполнения слотов
  и плейсхолдеров: разметка слота иначе затирает проставленные пометки.
  
  Отсутствие поля protection означает, что экран не защищается — это верный
  ответ для всех экранов вне периметра (FR-09) и для предпросмотра, поэтому
  call-site'ы, которым защита не нужна, не трогаются вовсе.
  
  Прогон shared/template целиком: 320 тестов, 34 файла — зелёные.

- **feat**(prd-34): применение защиты — инлайн, слушатели, впрыснутое правило печати (2026-08-02) [`25676e73e2d7426f27ae38b83ba626e27302e79b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/25676e73e2d7426f27ae38b83ba626e27302e79b)
  Запрет выделения ставится инлайн-свойствами: рантайм сам печатает часть
  правил инлайном (подбор кегля, разметка сопоставления), и правило из
  таблицы стилей он бы перебил, а инлайн — нет.
  
  Печатное правило и вид служебных элементов впрыскиваются в корень сцены,
  а не лежат в файле шаблона: правило в файле зависело бы от того, какой
  шаблон загружен, и внешний шаблон по PRD-3 молча остался бы без защиты.
  Правило пишется по служебному атрибуту ядра, поэтому PDF-отчёт, который
  этого атрибута не несёт, защищён признаком, а не аккуратностью.
  
  Предупреждение показывается только на copy и cut; на контекстное меню и
  перетаскивание молчит, иначе обычный правый клик превращается в шум.

- **feat**(prd-34): построитель периметра защиты — единственный источник (2026-08-02) [`42eb32452b12e56494850cf20bc851534f8b9422`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/42eb32452b12e56494850cf20bc851534f8b9422)
  Чистый модуль без DOM: решает, что экран защищает, что прячет и чем
  помечается. Периметр экрана вопроса перечислен поимённо и адресуется
  слотами и привязками ЯДРА, а не классами шаблона, поэтому внешний шаблон
  (PRD-3) получает защиту, ничего о ней не объявляя.
  
  Перечень экранов знака НЕ выводится из периметра защиты: экран итогов знак
  несёт, а от копирования не защищается. Место знака здесь не решается — его
  объявляет макет якорем.
  
  Отладочный прогон снимает защиту и скрытие, но не знак: автор должен видеть
  то, что увидит участник.

- **feat**(media): загрузка пишет реестр и отдаёт канонический адрес (2026-08-02) [`e8e13941df20e5fd4c3624e3a3e2fb3f75c39dd0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e8e13941df20e5fd4c3624e3a3e2fb3f75c39dd0)
  POST /api/media/upload теперь пишет строку реестра (или переиспользует
  существующую по паре владелец+checksum) через MediaStore/IStorage вместо
  безымянного файла в uploads/media. Ответ сохраняет старые поля
  (url/mime/originalName/size), url — теперь /api/media/&lt;id&gt;. Видимость по
  умолчанию — shared. Фильтр загрузки расширен на PDF (PRD-32).

- **feat**(media): правило доступа к активу с кешем решения (2026-08-02) [`85a7e314e8c06f6bed40a86f2a4d9f3b6db4d1fe`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85a7e314e8c06f6bed40a86f2a4d9f3b6db4d1fe)

- **feat**(media): служба индекса использования (2026-08-02) [`3e185a7fd1fedcff251bdd54f3e758c2f3986b9c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3e185a7fd1fedcff251bdd54f3e758c2f3986b9c)

- **feat**(media): чистый обходчик ссылок на медиа (2026-08-02) [`50e99126407256fd56accc680a704846d6ddf9a9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/50e99126407256fd56accc680a704846d6ddf9a9)

- **feat**(media): репозиторий реестра и индекса, подключение к IStorage (2026-08-02) [`94506f079773d69f7dde2c23f94e9e188793d351`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/94506f079773d69f7dde2c23f94e9e188793d351)

- **feat**(media): порт MediaStore и файловая реализация с дедупликацией (2026-08-02) [`87dc3d1112021ecd9f3742b4761185d36dbff1fa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/87dc3d1112021ecd9f3742b4761185d36dbff1fa)

- **feat**(media): право media.manage для управления реестром (2026-08-02) [`b46f30d911283dc4017eaf8749421f55c8d12a2a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b46f30d911283dc4017eaf8749421f55c8d12a2a)
  Управляет пересборкой индекса использования и удалением актива;
  чтение файла отдельным правом не описано — его решает правило
  доступа к активу (Задача 7). Держат Автор, Разработчик,
  Администратор. Golden-тест каталога прав и role-model.md приведены
  в соответствие.

- **feat**(media): таблицы реестра медиа и индекса использования (2026-08-02) [`9aea42424d49b6b450cf6baf45050332e9a0eafb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9aea42424d49b6b450cf6baf45050332e9a0eafb)

- **feat**(prd-35): радар и шкалы в PDF-отчёте — измерения в контексте отчёта, разметка, стили (2026-08-01) [`f7d7e0bc724f0f60c689af779a51d77bd0b0f240`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f7d7e0bc724f0f60c689af779a51d77bd0b0f240)

- **feat**(prd-35): свойство страницы показывает автору условие своей работы (2026-08-01) [`5dc57161c74294931e4d58e7cf3c53cbbaab83ea`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5dc57161c74294931e4d58e7cf3c53cbbaab83ea)

- **feat**(prd-35): материализация домена шкалы при сохранении и публикации (2026-08-01) [`f4b89d4e109b6610a01095a0ffbec8a0bd2419d1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f4b89d4e109b6610a01095a0ffbec8a0bd2419d1)

- **feat**(prd-35): радар в SCORM-пакете и тест паритета хостов (2026-08-01) [`d5617a605463259369496ce2189774e095bb7f4b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d5617a605463259369496ce2189774e095bb7f4b)

- **feat**(prd-35): радар на экране итогов веб-хоста — настройка варианта, разметка, стили (2026-08-01) [`cb7368858d811f71243f9db805758bd34502a02d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cb7368858d811f71243f9db805758bd34502a02d)

- **feat**(prd-31): панель интервала между попытками и приёмка обоих хостов (фазы 5-6) (2026-08-01) [`e7095e8b70129646c212143276e844ff8e720bb3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e7095e8b70129646c212143276e844ff8e720bb3)
  Автор получил вторую группу в пейне «Повторное прохождение»: переключатель
  «Ограничение между попытками» и поле «Интервал, часов» (1-8760). Барьеры
  независимы — интервал настраивается и при выключенном периоде охлаждения, ради
  чего `cooldownPeriodDays` и стал условно-обязательным. Часы сохраняются при
  выключении переключателя: автор, снявший ограничение на время, не должен вводить
  значение заново. Пояснение под группой меняет текст в зависимости от того,
  включён ли кулдаун: без этого два похожих поля читаются как дубли.
  
  Зажим диапазона вынесен в маппер и переиспользован формой, чтобы поле и значение,
  прочитанное с сервера, не разошлись в границах. Разметка взята из согласованного
  эскиза; выдуманный класс `ou-divider` заменён на существующий `ou-card__divider`
  после сверки с таблицей стилей DS — контролёр несуществующие `ou-*` не ловит.
  
  Стенд: непарсимое значение `--server-date` больше не игнорируется молча. Во время
  приёмки это неотличимо от «доверенная дата не работает», хотя виноват аргумент.
  Полный момент (`2026-08-01T14:30:00Z`) стенд принимал и раньше — он нужен барьеру
  B, который меряет часами.
  
  Приёмка. Веб — в реальном браузере: три состояния панели, включая случай «только
  интервал». SCORM — пакет `_build-interval-scorm.ts` с обоими барьерами на стенде с
  пиненными часами: повторный вход в СВОЁ назначение кулдауном не блокируется,
  очередная попытка ждёт интервал и видит момент открытия с датой и временем, кнопка
  старта неактивна, счётчик показывает «Попытка 2 из 3». Арифметика сходится по часам
  стенда, а не машины. Результаты обеих приёмок — в §12.1 спеки.
  
  Смена поведения веба вынесена в CHANGELOG отдельным абзацем: период охлаждения
  больше не блокирует повтор внутри назначения, а `maxAttempts` считается внутри
  назначения, и компенсирующей миграции нет.

- **feat**(prd-35): контракт result.scalesChart и сборка радара в контексте итогов (2026-08-01) [`d2a48cc1f9a21cbb84a1202cf0607244ccec31f1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d2a48cc1f9a21cbb84a1202cf0607244ccec31f1)

- **feat**(prd-35): ядро радара компетенций — геометрия, цвет, квантование (2026-08-01) [`29bd776dab958573ad89338abc172dceda0ee734`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/29bd776dab958573ad89338abc172dceda0ee734)

- **feat**(prd-31): назначение как единица допуска и интервал между попытками (фазы 1-3) (2026-08-01) [`eeac9f39762f60eeaf43c9b7bff87689c17a9142`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eeac9f39762f60eeaf43c9b7bff87689c17a9142)
  Веб переведён на общую модель допуска. Назначение — единица допуска: барьер A
  (кулдаун, календарные дни) действует между назначениями, барьер B (интервал,
  часы) — внутри назначения, и они применяются в непересекающиеся моменты.
  Раньше один и тот же переключатель означал на хостах разное: в SCORM гейт решал
  по записям обучения LMS, то есть по границе назначения, а в вебе — по границе
  попытки, потому что связи попытки с назначением не существовало.
  
  Движок: attemptIntervalDecision в shared/eligibility/engine.ts плюс JS-твин для
  пакета, паритет закреплён существующим golden-тестом. Часы, а не календарь:
  автор просит «раз в сутки» и получает 24 часа. Часы, сообщающие время раньше
  предыдущей попытки, не заслуживают доверия — интервал отсчитывается заново, как
  это уже делает кулдаун.
  
  Схема: ветка attemptInterval в retake_policy_json; cooldownPeriodDays стал
  необязательным и требуется только при включённом барьере A, иначе тест с одним
  барьером B пришлось бы снабжать выдуманным значением для выключенного
  переключателя. Колонка attempts.assignment_id (миграция 037 и drizzle 0008)
  без индекса: потребители и так грузят попытки по (user_id, test_id) и делят их
  в памяти, а лишний индекс на самой быстрорастущей таблице стоил бы записей.
  Старые строки остаются с NULL — принадлежность задним числом не восстановить, а
  домысливать нельзя; все они ведут себя как одно легаси-назначение.
  
  maxAttempts тоже считается внутри назначения, как в пакете всегда и было. Это
  осознанная смена поведения веба без компенсирующей миграции: кулдаун больше не
  блокирует повтор внутри назначения, а повторное назначение выдаёт новый комплект
  попыток.
  
  Тесты, утверждавшие прежнюю семантику, переписаны под модель, а не подогнаны:
  барьер A проверяется там, где прошлая попытка принадлежит другому назначению.
  Добавлены случаи на суть правки — кулдаун не блокирует повтор внутри назначения
  и попытка пиннится к назначению. Попутно закрыта утечка между тестами:
  clearAllMocks чистит вызовы, но не реализации, поэтому переопределение текущего
  назначения протекало в последующие случаи и меняло их бакет.

- **feat**(prd-30): порядок выдачи вопросов в теме — движок, оба хоста, редактор, книга (Э2-Э7) (2026-08-01) [`a2c5b27819a824939a5c90e5717febacad6e1076`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a2c5b27819a824939a5c90e5717febacad6e1076)
  Автор задаёт индекс вопроса внутри темы, тема теста решает, перемешивать её
  вопросы или выдавать по индексу. Отбор при этом не меняется: квоты PRD-11 и
  случайная выборка работают как раньше, порядок применяется поверх результата.
  
  Движок (Э2) — shared/draw/order-questions.ts: возрастание индекса, вопросы без
  индекса в конец, равные индексы перемешиваются ВНУТРИ своей группы. Последнее —
  суть решения: автор говорит «сначала эти три, потом те пять», а внутри группы
  порядок не определён, и стабильный tie-break по id изображал бы очередь,
  которую никто не задавал. Неизвестный режим откатывается к random, чтобы пакет
  из нового редактора не заморозил порядок в старом рантайме.
  
  Веб (Э3): drawSection не тронут, порядок накладывается на его результат; в
  режиме вариантов selectForm перестаёт перемешивать (порядок = список варианта);
  в адаптиве сортировка идёт ВНУТРИ уровня, состав и порядок уровней прежние.
  
  Пакет (Э4): плоский JS-двойник orderQuestions в assets/app.js плюс пятый
  параметр порядка у selectForm (включая ветку закреплённого варианта отладчика —
  иначе отладка показывала бы не то, что играет ученик). questionOrder и
  orderIndex пекутся в TEST_DATA только при fixed, поэтому пакеты нетронутых
  тестов остаются байт-идентичными. Паритет закрыт golden-тестом на 11 сценариев
  x 2 перестановки.
  
  Редактор (Э5): свитч «Случайный порядок вопросов» в теле темы, поле «Индекс в
  теме» под «Сложностью», колонка «Индекс» в дереве банка, предупреждение для
  linear_flat (там перемешиваются вопросы всех тем, порядок внутри одной не
  выживает). Приём полей в API вопроса и в цепочке сохранения теста: Zod-схема
  маршрута, SectionPayload, вставка секций, типы и мапперы — незаявленный ключ
  Zod срезает молча. Живая приёмка поймала, что дерево читает банк через
  getQuestions(), где ORDER BY не было, — добавлен и закрыт тестом DAL.
  
  Книга Excel (Э6): «Индекс в теме» на листе «Вопросы» и «Случайный порядок
  вопросов» на листе «Структура» (колонка «Порядок» там уже занята порядком
  разделов). Пустая ячейка и книга без колонок читаются как сегодняшнее
  поведение. Заполнена справка шаблона; заодно найдена пропущенная запятая,
  из-за которой две строки справки склеивались в индексирование массива —
  синтаксически валидное, поэтому tsc молчал.
  
  Приёмка (Э7): экспортированный ZIP в локальном плеере и отладочный плеер
  выдают 10 -&gt; 20 -&gt; 30 -&gt; 40 -&gt; без индекса. Живое веб-прохождение отложено: в
  dev-БД нет колонки attempts.assignment_id из миграции 0008, старт попытки
  падает независимо от этого PRD; веб-путь закрыт тестами маршрута.
  
  Новых тестов 79, регрессия по 15 затронутым наборам зелёная, tsc 0 ошибок.

- **feat**(template): читаемый слой 12 -> 14px на всех ширинах (2026-08-01) [`a8c84da060ed65442c62098949e7a00a30625fd4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a8c84da060ed65442c62098949e7a00a30625fd4)
  Мобильная ступень поднимала подсказку к вопросу, подзаголовок, «требуется
  N%», текст обратной связи и подписи чипов до `body-m`, а база оставалась
  на `body-s`. Довод не зависит от ширины: 12px — размер подписи, а перечисленное
  читают, и на десктопе дистанция до экрана больше, а не меньше.
  
  Правила перенесены на базовый уровень, блок в ступени S3 удалён как
  избыточный. `body-s` 12/16 против `body-m` 14/20 — шаг даёт ещё и четверть
  межстрочного, что на абзаце обратной связи важнее размера.
  
  Метки, которые сканируют, остаются на 12px: подписи под цифрами фактов,
  надзаголовки, теги, счётчики «отвечено/пропущено».
  
  Гард переписан с проверки правила в S3 на проверку базовых правил и заодно
  фиксирует, что метки НЕ поднимаются.
  
  Приёмка: на 1440 и 390 читаемый слой 14px/20px, метки 12px/16px,
  горизонтального переполнения нет, сетка тем на десктопе прежняя.

- **feat**(prd-30): индекс вопроса в теме и порядок выдачи — схема и банк (Э1) (2026-08-01) [`f97b167e2d7b4ed528c4387f548b7db64c92c278`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f97b167e2d7b4ed528c4387f548b7db64c92c278)
  Порядка вопросов в банке темы не существовало физически: у questions не было
  колонки порядка, а getQuestionsByTopic читал строки вообще без ORDER BY —
  PostgreSQL возвращал их произвольно, и это был вход обоих движков выдачи.
  
  \- questions.order_index (integer, nullable, БЕЗ умолчания): любое ненулевое
    умолчание загнало бы все вопросы каждой темы в одну группу равных, и порядок
    не появился бы ни у кого. NULL = «индекс не задан» — такие вопросы идут
    последними. Имя order_index, а не sort_order: у test_sections.sort_order
    другая семантика — плотный технический порядок разделов, переписываемый на
    каждом сохранении
  \- test_sections.question_order = random (умолчание) | fixed: перечисление, а не
    булево, чтобы будущая позиция «по возрастанию сложности» не требовала второй
    миграции; умолчание сохраняет сегодняшнее поведение всех тестов
  \- getQuestionsByTopic: ORDER BY order_index ASC NULLS LAST, id — банк читается
    в авторском порядке, чтение детерминировано
  \- createQuestion прокидывает индекс через ?? (0 — валидный индекс, не «пусто»),
    duplicateQuestion переносит индекс в копию: она встаёт рядом с исходником
  
  Миграция 0007, tests/it/schema.sql перегенерирован (дельта — две колонки).
  6 интеграционных тестов на pglite: умолчание NULL, сохранение, очистка через
  null, перенос при дублировании, порядок с хвостом без индекса, группа равных.
  
  Приём поля в POST/PUT /api/questions намеренно НЕ добавлен — маршрут ведёт
  явный список полей, это правка Э5 вместе с UI.
  
  tsc 0 ошибок, test:it 40/40, unit 6292/6292.

- **feat**(template): читаемый слой на телефоне переведён с 12 на 14px (2026-08-01) [`67bb67c8590ab67fadcc3a7b1724e57a4b233ea3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/67bb67c8590ab67fadcc3a7b1724e57a4b233ea3)
  12px — размер подписи, а не текста для чтения, и на нём была набрана
  как раз содержательная часть: подсказка к вопросу, обратная связь по
  теме, «требуется N%» и подписи чипов-рекомендаций. Обратная связь и
  рекомендации — то, ради чего экран итогов существует, и они оказывались
  самым мелким текстом на экране, вдвое мельче вариантов ответа (15-17px),
  которые они объясняют.
  
  DS `body-s` — 12/16, `body-m` — 14/20, поэтому шаг даёт не только два
  пикселя кегля, но и четверть межстрочного: на абзаце обратной связи это
  заметнее размера.
  
  Метки, которые сканируют, а не читают, намеренно остались на 12px:
  счётчики «отвечено/пропущено», подписи под цифрами фактов, надзаголовки,
  теги.
  
  Правка только в ступени S3 — база (десктоп и планшет) не тронута.
  Тот же довод применим и к ней, но это отдельное решение по уже принятому
  десктопному виду.

- **feat**(prd-29): шкалы и показатели на экране итогов SCORM-пакета (2026-08-01) [`1281e0ff8a9a53c9031761a2f5787fb41a85b284`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1281e0ff8a9a53c9031761a2f5787fb41a85b284)
  Пакет собирает для общего построителя тот же вход, что собирает веб-хост:
  запечены домен, направление и полные толкования шкал, толкование показателя
  и блок обратной связи теста; рантайм разрешает параметры оформления, рампу,
  настройки блоков и нормализует обратную связь теста ровно один раз.

- **feat**(prd-29): стили строки измерения поверх ou-slider (2026-08-01) [`4c4f02e741aa55191046bb744d1a760fca661607`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4c4f02e741aa55191046bb744d1a760fca661607)

- **feat**(prd-29): блоки показателей, шкал и рекомендаций в макете итогов (2026-08-01) [`19ac3bd8fcef8d16b3c006825c51463c92bd6b8b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/19ac3bd8fcef8d16b3c006825c51463c92bd6b8b)

- **feat**(prd-29): проброс измерений на веб-хосте (2026-08-01) [`1b502f321eb86a0cb369d6e4dc6f8f6f0192b6f2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1b502f321eb86a0cb369d6e4dc6f8f6f0192b6f2)

- **feat**(prd-29): шкалы, показатели и рекомендации в контексте итогов (2026-08-01) [`ee14f63c397a478060d1691380d64f767c8cd0b2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ee14f63c397a478060d1691380d64f767c8cd0b2)

- **feat**(prd-29): drizzle-миграция 0006 с переносом данных (2026-08-01) [`293731484e03273620bdba16ea5b98f87f4db270`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/293731484e03273620bdba16ea5b98f87f4db270)
  drizzle-kit generate выпускает только DDL: три ADD COLUMN и два DROP COLUMN.
  Смысловой перенос значений он выразить не умеет, поэтому вставлен вручную
  между добавлением и удалением — ровно то, ради чего файлы миграций drizzle
  остаются обычным SQL. Без него выкатка на прод молча закрыла бы от учеников
  все шкалы и показатели, которые были им открыты.
  
  Заодно убраны проверочные ограничения на перечень, которые я добавил в 036 и
  применил к dev-базе. Это было отступление от конвенции проекта: `check`
  объявлен только для двух регулярок-идентификаторов, а все 31 текстовое
  перечисление живут без ограничения в базе — значения держит тип в схеме и
  разбор zod. Хуже того, drizzle проверки ОТСЛЕЖИВАЕТ (в снимке есть
  scales_key_check), а этих двух в снимке нет — очередная генерация увидела бы
  их как дрейф. Ограничения сняты и с dev-базы, данные не пострадали.

- **feat**(template): пиктограммы в чипах обратной связи (2026-07-31) [`dae894a6e76e32f92a4d214ef55d926ab8f020cc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dae894a6e76e32f92a4d214ef55d926ab8f020cc)
  На экране итогов курсы и мероприятия стоят в ОДНОМ ряду без подписей —
  в отличие от отчёта, где у каждого вида своя секция с заголовком. Значок
  там единственное, что их различает.
  
  Глифы взяты из авторского предпросмотра обратной связи, который решает
  ту же задачу для групп «Документы / Курсы / Мероприятия»: ссылка для
  курса, календарь для мероприятия. В шаблон они встроены инлайновым SVG с
  теми же атрибутами, что уже используют другие значки layout'ов, — рантайм
  шаблона фреймворка не знает.
  
  Заодно исправлено расхождение в самом референсе: `CalendarDays` был
  импортирован под группу «Мероприятия», но подставлялась иконка ссылки,
  и оба вида помечались одинаково. Авторская и ученическая поверхности
  обязаны показывать одно и то же.
  
  Тест ловит три сценария потери смысла: значок пропал, уехал за подпись
  и — главное — стал одинаковым у обоих видов.

- **feat**(template): мобильная адаптация шаблонов «Стандартный» и «Сертификация» (2026-07-31) [`fa9d14d24b26a508600a8d8f5feb969e1e99c42a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fa9d14d24b26a508600a8d8f5feb969e1e99c42a)
  Телефон в портрете (360/390) и планшет (768). Все размерные условия —
  контейнерные (`@container tbscene` на `.tb-scene`), вьюпортных `@media`
  по ширине в обоих theme.css не осталось: в Shadow DOM веб-хоста и в
  превью-модалках viewport меряет окно, а не сцену.
  
  Ступени: 900 (порт прежних правил, десктоп не меняется), 700 (развал
  сеток «медиа/ответы» и «картинка/текст»; ниже 768, чтобы планшет остался
  двухколоночным), 520 (телефон).
  
  Замер пакета на 360 до правок: подвалу вопроса нужно 383px при 312
  доступных — кнопка «Отправить ответ» уходила за край экрана; сетка тем
  `minmax(340px)` не ужималась и давала горизонтальную прокрутку тела;
  шапка занимала 210px из 740; кегль оставался 32/22px.
  
  Сделано:
  \* поля 24 -&gt; 16, кегль через clamp() 22/17px, сетки в одну колонку;
  \* шапка перестроена: обёртки brandrow и map растворяются, таймер DS
    ложится в строку (он колонка «подпись над числом» — отсюда этажерка),
    карта вопросов скрыта, навигация по вопросам остаётся на обзоре.
    210 -&gt; 77px;
  \* подвал переносится естественно, основная кнопка последняя (`order`),
    цели не мельче 44px;
  \* fitQuestionScene получил профиль по ширине поля и четвёртую фазу —
    потолок высоты вопроса, чтобы гарантированно было видно три варианта.
    Раньше алгоритм умел только уменьшать кегль и на телефоне упирался в
    пол, а вопрос занимал 77% поля;
  \* тач-плотность по `pointer: coarse`; токены объявлены на `.tb-scene`, а
    не на `.ou`/`:host` — правило документа побеждает теневое по правилу
    encapsulation context;
  \* `touch-action: none` на `.ou-rank__grip` в обеих копиях DS: без него
    тач-жест уходил в прокрутку и перетаскивание в ранжировании на
    телефоне не работало вовсе. На всю строку ставить нельзя — отняло бы
    прокрутку списка;
  \* порог старта перетаскивания 4 -&gt; 8px, явный `cardSelector`.
  
  Разметка не тронута: template-layout-parity требует побайтового
  совпадения layout'ов шаблонов.
  
  Приёмка в браузере на обоих хостах: на 1440 и 768 метрики совпадают с
  замером до правок, на 360 переполнений не осталось. Ключевая проверка —
  при окне 1600px ступени срабатывают по ширине сцены (900/700/520/360),
  чего вьюпортные медиа не дали бы.

- **feat**(prd-29): сверка литералов формулы с перечнем исходов (2026-07-31) [`b4d36e7e67b8d8b60c43d05b13461a27470f7067`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b4d36e7e67b8d8b60c43d05b13461a27470f7067)

- **feat**(prd-29): толкования интервалов и перечень исходов в редакторе (2026-07-31) [`e890cdfd5ecde8eaccf50bb5fa9818e62bc31b4e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e890cdfd5ecde8eaccf50bb5fa9818e62bc31b4e)
  Интервал шкалы и исход показателя несут метку уровня, текст толкования,
  необязательную оценку уровня (закрытый перечень методологических состояний,
  не цвет) и необязательные рекомендации.
  
  Существующий BandsEditor расширен колонкой «Оценка» и строкой-продолжением
  с толкованием; он же переиспользован для числового показателя. Для строковых
  и булевых показателей добавлен зеркальный OutcomesEditor: у них интервалов
  нет, формула возвращает код исхода. Рекомендации на интервале и на исходе
  открывает существующий FeedbackEditorModal.
  
  Круговой обход через config_json доработан с обеих сторон — и запись
  (bandsToPayload / outcomesToPayload), и чтение (buildScaleBands /
  buildOutcomes), иначе первое же сохранение стирало бы толкования.

- **feat**(prd-29): домен, направление и видимость в редакторе шкал (2026-07-31) [`dcd41910b6b694f3cfde822b48e23fa84ef8982f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dcd41910b6b694f3cfde822b48e23fa84ef8982f)
  Снята константа сокрытия тогла показа шкалы ученику: вместо двухпозиционного
  переключателя — выбор из трёх положений (не показывать / уровень и толкование /
  уровень, толкование и значение). Средняя позиция — рабочая потребность
  психодиагностики: уровень раскрывается, сырой балл остаётся закрытым.
  
  Добавлены границы шкалы (domainMin/domainMax) и благоприятное направление
  (valence) — оба хранятся в config_json. Отсутствие границ выражено отдельным
  переключателем, а не нулём-сигналом: ноль здесь законная граница. Кнопка
  «Рассчитать по вкладам» предзаполняет домен через achievableRange с реальными
  типами вопросов; расхождение с сохранённым доменом показывается баннером,
  тихого пересчёта нет.
  
  Колонка книги «Показывать ученику» переведена на три значения с чтением старых
  книг («да» = уровень и значение), чтобы экспорт с импортом не раскрывали
  ученику число там, где методолог его закрыл.

- **feat**(prd-29): параметры схемы уровней, видов рендера и блоков итогов (2026-07-31) [`25b3f755853f44bae403d75b2a20fb9c1b3e8053`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/25b3f755853f44bae403d75b2a20fb9c1b3e8053)

- **feat**(prd-29): сбор рекомендаций из трёх источников в один блок (2026-07-31) [`64d2a8a44f099c41213cd6bc7c0fcb00290c3779`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/64d2a8a44f099c41213cd6bc7c0fcb00290c3779)

- **feat**(prd-29): сборка карточки шкалы и показателя (2026-07-31) [`1a6d7fdfc9bd74cc3408cc91ceaf2fa2fe6f5d0d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1a6d7fdfc9bd74cc3408cc91ceaf2fa2fe6f5d0d)

- **feat**(prd-29): рампа цветов уровней по образцу условного форматирования (2026-07-31) [`4e3a9ed047ded0d419308efa77a65e8c710706e6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e3a9ed047ded0d419308efa77a65e8c710706e6)

- **feat**(prd-29): достижимый диапазон шкалы вынесен из rawRange (2026-07-31) [`2c9e7d4928d623c0387e5be72b02457156b6e8c9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2c9e7d4928d623c0387e5be72b02457156b6e8c9)

- **feat**(prd-29): модель толкования для шкал и показателей (2026-07-31) [`91719f910a3aa0441a9f8778de0827d63aeb7d82`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/91719f910a3aa0441a9f8778de0827d63aeb7d82)

- **feat**(prd-27): картинки отчёта — файлы шаблона, а не ассеты ядра (2026-07-31) [`ce5e2d1e6d9f159c6427d0f8b2a948b2864e958c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ce5e2d1e6d9f159c6427d0f8b2a948b2864e958c)
  FR-05. Подложка, логотип и прочая графика страницы отчёта переезжают из кода
  продукта в шаблон: вариант объявляет их полем `settings[]` типа `image`, чей
  `default` — путь внутри шаблона, а сами файлы перечислены в `assets.images`.
  Ядро больше не знает ни `pdf-bg-*.png`, ни `logo-light.png`: до этого отчёт
  всегда печатался на одной из трёх зашитых подложек с чужим логотипом, и
  сертификационный тест выдавал документ под чужим брендом.
  
  Значение поля разрешает ОДИН общий модуль (`shared/report/report-assets.ts`)
  против базы хоста — `template/` в пакете, `template-default/` при деградации,
  `/api/templates/&lt;id&gt;/assets/` на вебе; картинка автора берётся как есть. Перед
  растеризацией значения инлайнятся в data-URL: растеризатор снимает то, что уже
  в документе, и ничего не догружает, а нечитаемая картинка даёт пустое значение,
  на котором макет гейтит свою строку.
  
  Из контекста удалены `report.backgroundUrl`/`logoUrl`/`hasLogo` — макет читает
  `report.values.&lt;key&gt;`. Вместе с ними удалены `loadReportAssets`, тип
  `ReportAssets`, роут `/api/report/asset/:file` и сами PNG.
  
  Две вещи всплыли только на живой приёмке:
  
  \- область сессии по ссылке-приглашению знала удалённый ассетный роут, а новый
    нет — ученик по magic link получал 403 и скачал бы отчёт без оформления;
    добавлено правило `GET /api/templates/:templateId/assets/*`;
  \- умолчания полей не доходили до интерфейса автора: карточка показывала пустое
    поле, а предпросмотр — страницу без подложки, хотя ученик получал её. Теперь
    предпросмотр строится на значениях, слитых с умолчаниями варианта, а само
    умолчание показано заполнителем и в черновик не пишется.
  
  Случайный выбор подложки из трёх убран: подложка одна, автор вправе заменить её
  своей картинкой. Версии поставляемых шаблонов подняты (default 1.4.0,
  certification 1.5.0).

- **feat**(users): повторная отправка приглашения и палитра DS в письмах (2026-07-31) [`878d3f5b6d0ddc9319a5561f55b45e018753884a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/878d3f5b6d0ddc9319a5561f55b45e018753884a)
  Письмо-приглашение раньше уходило только из массового импорта и только
  для вновь создаваемых строк: учётка, застрявшая в статусе «Ожидает»,
  получить его повторно не могла.
  
  \- POST /api/users/:id/invite: право users.manage, только статус pending,
    токен на 7 дней (INVITE_TTL_MS, его же использует массовый импорт),
    общий с forgot-password лимит 3 письма в час, ответ { success, sent },
    событие аудита user.invite;
  \- пункт «Отправить приглашение» в меню строки — только у pending;
    при sent=false предупреждающий тост (почта не настроена, ссылка в журнале);
  \- server/email-theme.ts: значения токенов DS, развёрнутые в литералы
    (почтовые клиенты не понимают ни --ou-*, ни color-mix), мягкие подложки
    считаются тем же oklch-смешением, что и рампа ui-kit; все три письма
    переведены на эту палитру;
  \- tests/email-ds-colors: сверка палитры писем с university-rt.css и запрет
    произвольных цветов в email.ts.

### Fixes

- **fix**(scorm): защитный стиль подключается до снятия старой разметки (2026-08-05) [`facfa1c7b7beed568fc8b4d33c59fe5022324039`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/facfa1c7b7beed568fc8b4d33c59fe5022324039)
  ensureStyle(root) вызывался после querySelectorAll/unmark — при первом
  вызове applyProtection защитный &lt;style&gt; ещё не существовал, unmark
  искал несуществующие классы впустую. Стиль теперь гарантированно на
  месте до любых операций над разметкой.

- **fix**(scorm): счётчик пропущенных вопросов держится рядом с «отвечено» (2026-08-05) [`1f187736dab61be9c2117bfb02655cf326c2f5a9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1f187736dab61be9c2117bfb02655cf326c2f5a9)
  Строка счётчиков карты вопросов уходила на отдельный ряд раньше легенды
  из-за flex-grow на индикаторе точек; теперь индикатор не растягивается
  без нужды, а «отвечено N из M · пропущено K» остаются одной строкой,
  пока хватает места, и переходят в двухстрочный стек только на узкой шапке.

- **fix**(prd-43): disambiguate banner-text assertion after quickAdvance banner wording alignment (2026-08-05) [`a44951094573e874b28c8b2e06e87c490b11d269`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a44951094573e874b28c8b2e06e87c490b11d269)

- **fix**(tests): report-export-pdf — учесть глобальный стиль защиты копирования (2026-08-05) [`205fe2bbf0e5b72420458eefc7b281b840492a21`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/205fe2bbf0e5b72420458eefc7b281b840492a21)
  Тест «CSS варианта... уходят вместе с контейнером» падал на main
  независимо от PRD-42: renderScreenInto (общий рендерер, которым
  пользуется и exportReportPdf) с появлением PRD-34 безусловно инжектит
  идемпотентный [data-tb-protection] &lt;style&gt; в document.head при КАЖДОМ
  рендере сцены (apply.ts, applyProtection). Это не утечка страницы
  отчёта, а сознательный общий побочный эффект, появившийся ПОСЛЕ того,
  как тест был написан. Проверка сужена до «своих» стилей варианта.

- **fix**(prd-43): plan — Task 2's legacy strict-mode tests need quickAdvance:true override (2026-08-05) [`14a056f4619f8b9563eaec40a26da6d1f0c30656`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/14a056f4619f8b9563eaec40a26da6d1f0c30656)
  Found by the Task 2 implementer subagent: the base `flexible` fixture carries
  quickAdvance:false (needed for the flexible two-step tests), but the two
  pre-existing strict-mode tests spread it without overriding quickAdvance,
  so they inherited false and collided with the new "strict without quick
  advance" combination test that expects the opposite outcome for the same
  input shape.

- **fix**(scorm): легенда карты вопросов вплотную к индикатору, счётчики в одну строку (2026-08-05) [`729bdf312090184df6941638def611645f7e6a40`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/729bdf312090184df6941638def611645f7e6a40)
  .ou-quiz__dots держал flex-grow:1 — индикатор растягивался на всё свободное
  место в строке, а сами точки оставались прижаты влево, из-за чего между ними
  и легендой открывался пустой зазор. Заодно счётчики «отвечено»/«пропущено»
  были безусловно двухстрочными даже там, где помещались в одну строку.
  Рост убран (flex:0 1 auto, сжатие и перенос по числу точек сохранены), счётчики
  снова в одну строку с точкой-разделителем; на узкой шапке (&lt;=820px) «пропущено»
  опускается второй строкой строго под «отвечено», не уезжая под индикатор.

- **fix**(prd-24): подсветка и переход к ошибке порога темы в «Правилах прохождения» (2026-08-05) [`fff313b14ea79b7f75049722e7af9eb58eb528a0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fff313b14ea79b7f75049722e7af9eb58eb528a0)
  NumberInput порога у «Индивидуального правила» темы не получал ни
  error, ни data-field: реальные ошибки валидации (FR-15c/f) не
  подсвечивались на странице и «Перейти к ошибкам» не мог найти якорь
  для прокрутки — баннер писал число ошибок, а на экране ничего не
  менялось. Тот же пробел с data-field закрыт и в пороге «По вариантам».

- **fix**(prd-34): водяной знак получает стиль и в отладочном прогоне (2026-08-05) [`496550433b2d30791606f25a31fe57ca93b580ca`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/496550433b2d30791606f25a31fe57ca93b580ca)
  ensureStyle() внедряла .tb-protection-mark/.tb-protection-veil только
  при активной защите копирования (target != null). В отладке (FR-25,
  active=false) copy всегда null, а знак всё равно рисуется (FR-19) —
  получался неоформленный текст вместо тонкой приглушённой строки по
  эскизу. Стиль теперь подключается на каждый рендер безусловно.

- **fix**(scorm): карта вопросов переносится по числу точек, а не по ширине шапки (2026-08-05) [`e0246473b2b7c344c05a0e19df0f2ddd472eec6e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e0246473b2b7c344c05a0e19df0f2ddd472eec6e)
  .ou-quiz__dots держал flex:0 0 auto и умел сжиматься/переноситься только
  ниже контейнерного брейкпоинта 640px, поэтому длинный список вопросов
  (60+) переполнял шапку любой ширины вместо переноса в несколько строк.
  Заодно «отвечено»/«пропущено» сведены в один блок-столбец, чтобы
  «пропущено» всегда было второй строкой строго под «отвечено X из Y»
  с выравниванием вправо, а не уезжало на строку легенды.

- **fix**(scorm): кнопка «Завершить тест» блокируется после клика (2026-08-04) [`d1d3d5a7451390c6e8143fe606006ee0d232dfce`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d1d3d5a7451390c6e8143fe606006ee0d232dfce)
  finishAndClose() выставлял только внутренний флаг scormFinished, не трогая
  DOM — на хостах, где window.close() не срабатывает (Moodle встраивает SCO
  без script-открытого popup), кнопка выглядела активной вечно и провоцировала
  повторные клики. Гард отладочного плеера тоже не ловил эту кнопку на
  стандартном экране результатов (data-action="results-finish", подпись без
  слова «тест»), поэтому баг воспроизводился и там.

- **fix**(ui-kit): narrow-match accent tint учитывает generic review-классы (2026-08-04) [`a069c3237da50159371204566cf534ef1da46125`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a069c3237da50159371204566cf534ef1da46125)

- **fix**(auth): устранить молчаливый сбой валидации пароля из-за Zod v4 (2026-08-04) [`41ce41e79a5ac2789b40921551114b76e821d0f1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/41ce41e79a5ac2789b40921551114b76e821d0f1)
  @hookform/resolvers 3.10.0 определял ZodError по устаревшему полю
  error.errors (формат Zod v3); в Zod v4 у ошибки только .issues, поэтому
  проверка не срабатывала и резолвер прокидывал необработанный ZodError
  вместо ошибок полей — формы смены пароля (reset-password, first-login
  и другие на zodResolver) блокировали сабмит без единого сообщения
  пользователю. Поднят @hookform/resolvers до 5.7.1 (детектирует схемы
  Zod v4 и читает .issues); добавлен регресс-тест на инлайн-ошибку при
  недопустимых символах в пароле.

- **fix**(prd-31): пауза перед закрытием SCO снимает гонку барьера B (2026-08-04) [`f05dec1e32ac918376c72060160a04fbd9fbc6b1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f05dec1e32ac918376c72060160a04fbd9fbc6b1)
  Commit()/Terminate() возвращаются, как только LMS-адаптер принял вызов, а
  запись cmi.suspend_data в БД портала может идти асинхронно. Немедленный
  window.close() после них race'ился с этой записью: на testuniver.rt.ru
  портал пересоздавал сессию сразу после «Завершить и закрыть», и вторая
  попытка стартовала без completedAt только что завершённой первой —
  attemptIntervalState() не находил квалифицирующую попытку и часовой
  интервал молча пропускал учащегося в роутер вместо блокировки.

- **fix**(prd-38): пути к стилям в эскизе после переноса в approved (2026-08-04) [`4cc6c04663bbca98adb8b7b94a5915760ef36642`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4cc6c04663bbca98adb8b7b94a5915760ef36642)

- **fix**(prd-38): оверлей монтируется в переданный корень, а не в document.body (2026-08-03) [`f4ddf16970d521a453448e4bbd78002ddc85a337`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f4ddf16970d521a453448e4bbd78002ddc85a337)

- **fix**(report): веб отдаёт измерения в форме, которую принимает сборщик отчёта (2026-08-03) [`8584bdc6182f7d57cd1692b6521b3c3a237b3115`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8584bdc6182f7d57cd1692b6521b3c3a237b3115)

- **fix**(results): обратная связь молчит только при явном успехе (2026-08-03) [`9e3f8df9583db42f2398d5e9e66987cd4969a227`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9e3f8df9583db42f2398d5e9e66987cd4969a227)
  Материалы и тексты показываются во всех состояниях, кроме заведомого прохождения —
  на двух уровнях сразу.
  
  Тема: гейт «кроме passed === true» вместо прежнего «только passed === false». Потемные
  пороги задают редко, у такой темы вердикта нет вовсе, и прежнее правило молча съедало
  бы материал автора во всех тестах без потемных порогов. Одно правило теперь у всех
  четырёх ресурсов темы: текст, вложения (общий сборщик), курсы и мероприятия
  (`vrRecommended` в пакете, фильтр в веб-адаптере, отчёт).
  
  Тест: своя обратная связь теста больше не показывается при явном успехе — пройденный
  тест не показывает работу над ошибками. «Явный успех» = порог объявлен И есть что
  оценивать И вердикт «пройден». У измерительного теста без порога вердикт не выносился,
  и его обратная связь показывается при любом passed: она и есть результат метода.
  
  Признак порога доезжает до общего сборщика новой опцией `hasPassThreshold`, отдельной
  от копии внутри `measures` (та ходит только у теста со шкалами или показателями).
  Данные уже были на обоих хостах: веб берёт признак из `MeasuresSource`, который маршрут
  читает для каждой попытки, пакет — из запечённого `TEST_DATA.overallPassRule`. Ничего
  нового в пакет и в маршрут не поехало. Отсутствие признака = «неизвестно» = показываем.

- **fix**(results): обратная связь темы показывается по одному правилу (2026-08-02) [`b0653979d16ec09b945ccabef68f11647bf54ee1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b0653979d16ec09b945ccabef68f11647bf54ee1)
  Всё, что автор повесил на тему — текст, курсы, мероприятия, вложения PRD-32, —
  уходит ученику только при непройденной теме. До консолидации курсы и мероприятия
  уже гейтились провалом, а тексты и вложения показывались всем: после сведения трёх
  ресурсов в один блок разные правила стали бы видны на экране.
  
  Гейт стоит в общем сборщике `buildResultContext` — единственном месте, через
  которое ходят оба хоста, поэтому веб и пакет не могут разойтись составом блока.
  Тема без вердикта (`passed: null`, у темы не задан порог) молчит: провала не было,
  и правило то же, что у курсов (`tr.passed !== false`).

- **fix**(scorm): пакет читает текст обратной связи темы из действующего источника (2026-08-02) [`85426a0384f6fa8fdcbc75dd6f1d803c83b6dc41`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85426a0384f6fa8fdcbc75dd6f1d803c83b6dc41)
  Сборка печёт `topics.feedback_json.text` (с откатом на легаси-колонку) и текст
  раздела этого теста, а рантайм отдаёт их общему сборщику под тем же именем
  `feedbackTexts`, каким их кладёт веб. Прежнее поле `topicFeedback` снято:
  оно везло ТОЛЬКО легаси-колонку под именем, которого сборщик не читал.

- **fix**(results): веб читает легаси-текст темы и держит комментарии на английском (2026-08-02) [`33cf94bfcd0a575024c23e5ac8f04ab7af294e13`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/33cf94bfcd0a575024c23e5ac8f04ab7af294e13)

- **fix**(results): веб-хост отдаёт обратную связь теста без измерений (2026-08-02) [`933cd9052c4d13ef85cfc9a3f297a8cbd4226d6d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/933cd9052c4d13ef85cfc9a3f297a8cbd4226d6d)
  Продолжение Д-3. Опция сборщика была заведена верно, но на вебе `testFeedback` до
  сборщика не доезжал: `measuresForAttempt` выходила по `return undefined` РАНЬШЕ, чем
  читала `deliveredTest.feedbackJson`, дальше `readResultsRenderPayload` при отсутствии
  материала звала двухаргументный `buildResultContext`, и передать опцию было неоткуда.
  В пакете дефект уже закрыт, веб приведён к тому же поведению.
  
  Материал экрана итогов собирается теперь для ЛЮБОЙ стандартной попытки: функция
  переименована в `resultsMaterialForAttempt` — она только читает, а решение «есть ли
  что показывать из измерений» осталось ровно одно, в `buildResultContext` по пустоте
  массивов шкал и показателей. Развилка на два вызова в `readResultsRenderPayload`
  убрана: сборщик и так понимает отсутствующий третий аргумент, а развилка молча
  теряла всё, что материал несёт помимо измерений.
  
  Ответ маршрута не изменился: поле `measures` по-прежнему уходит клиенту только у
  теста, который шкалы или показатели действительно объявляет, — по нему клиентский
  отчёт печатает шкалы и радар, и пустой набор заставил бы его рисовать лишний блок.
  
  Тест поставлен на уровень МАРШРУТА (`GET /api/attempts/:id/result`), а не адаптера:
  прежний кейс был зелёным при живом дефекте именно потому, что проверял слой ниже
  места поломки.

- **fix**(results): обратная связь теста доезжает до «Материалов» без шкал и показателей (2026-08-02) [`bc2dc002d064049285e62d90f591df5aec6dced6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bc2dc002d064049285e62d90f591df5aec6dced6)
  Приёмочный дефект Д-3. Сбор блока рекомендаций был вынесен из ветки измерений, но
  сам `testFeedback` остался внутри неё: на вебе он попадал в общий сборщик только при
  `hasMeasures`, в пакете `buildResultsMeasures` возвращает null у теста без шкал и
  показателей — и обратная связь терялась вместе со всем объектом. Контрольный тест —
  самая массовая конфигурация продукта, и обратная связь в нём существует ровно затем,
  чтобы её показали.
  
  Обратная связь теста никогда не была частью измерений по смыслу: она оказалась там
  потому, что PRD-29 собирал весь блок внутри своей ветки. Поэтому она вынесена из
  `MeasuresInput` в собственную опцию `ResultContextOptions.testFeedback` — одну на оба
  хоста. Порядок источников прежний: тест (самый общий) → показатели → шкалы →
  темы/разделы; дедупликация по-прежнему оставляет первое вхождение.
  
  Веб берёт блок из своего `MeasuresSource` (он собирается независимо от наличия шкал),
  пакет — из `TEST_DATA.testFeedbackJson` через `vrTestFeedback`, читаемый ОТДЕЛЬНО от
  `buildResultsMeasures`. Адрес вложения по-прежнему нормализует единственный
  `normalizeFeedback`.

- **fix**(tests): чинит стенд экранов итогов после PRD-34 — `buildScormProtection` в глобалях (2026-08-02) [`2ae698851ed8be8171a4862048109068891f0d92`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2ae698851ed8be8171a4862048109068891f0d92)
  Коммит 50a6083 добавил в `renderViewResultsTemplated`/`renderResultsTemplated` вызов
  `buildScormProtection`, но стенд `results-report-action` подставляет рантайму глобали
  поимённо и об этой не знал — все семь его проверок падали с ReferenceError ещё до
  настоящей работы. Правка чисто стендовая: заглушка защиты в списке параметров.

- **fix**(results): вложения обратной связи темы и раздела доезжают до «Материалов» (PRD-32) (2026-08-02) [`ff07a5885f886ec806d5f7ca6f2e5a04cf3fb743`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ff07a5885f886ec806d5f7ca6f2e5a04cf3fb743)
  Вложение темы (`topics.feedback_json`) индексировалось и отдавалось по прямой
  ссылке, но на экране итогов его не было ни в вебе, ни в пакете: блок «Материалы»
  собирался из трёх источников — обратная связь теста, исход показателя, диапазон
  шкалы. Тема и раздел довозили до итогов только курсы и мероприятия.
  
  Блок структурирован ПО ТИПУ РЕСУРСА, а не по причине, поэтому вложения темы и
  раздела вливаются в тот же список, без разделения по темам. Нормализованный вход
  темы получил поле `recommendedAssets` (по образцу `recommendedCourses`), а сбор
  рекомендаций вынесен из ветки измерений: контрольный тест без шкал и показателей
  раньше не получал блока вовсе. Порядок источников: тест → показатели → шкалы →
  темы/разделы, дедупликация оставляет первое вхождение, поэтому один файл,
  приложенный и к тесту, и к теме, не даёт дубля.
  
  Оба хоста заполняют поле из ДВУХ мест — `topics.feedback_json` и
  `test_sections.feedback_json`:
  \- веб: `attempts.ts` при подсчёте кладёт их в результат попытки (экран рисуется
    из сохранённого результата, как и курсы);
  \- пакет: `test-json.ts` запекает `section.recommendedAssets` (медиа-упаковщик сам
    переписывает адреса в путь внутри ZIP), рантайм читает их из TEST_DATA.
  
  Адрес выбирается ЕДИНОЙ функцией `normalizeFeedback` через новый общий помощник
  `feedbackAssets` — второй копии правила не заводится.

- **fix**(media): вложение обратной связи РАЗДЕЛА доезжает до индекса использования (PRD-32) (2026-08-02) [`b9fe26b94a50df8d467528cf05acd306d78c7cd5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b9fe26b94a50df8d467528cf05acd306d78c7cd5)
  Редактор пишет дескриптор вложения в `test_sections.feedback_json`, но индекс
  использования знал только `tests.feedback_json` — ученику `GET /api/media/&lt;id&gt;`
  отвечал 403, а в SCORM-пакет файл не попадал.
  
  Раздел принадлежит тесту и своего ключа в `media_usages` не имеет, поэтому едет
  под тем же `test_feedback` с идентификатором ТЕСТА. Ключевое: `syncEntityUsages`
  переписывает строки пары (тип, идентификатор) ЦЕЛИКОМ, так что два раздельных
  вызова затёрли бы строки друг друга. Обе части индексируются ОДНИМ вызовом на
  объекте `{ test, sections }` (`testFeedbackUsageEntity`) — заодно пути в поле
  `field` становятся говорящими: `test.assets.0.url` / `sections.0.assets.0.url`.
  
  Правки на всех трёх путях: POST /api/tests, PUT /api/tests/:id (разделы
  перечитываются ПОСЛЕ сохранения — служба пересоздаёт их строки) и полная
  пересборка `reindexAllUsages`.

- **fix**(scorm): упаковщик не портит абсолютные адреса и вычищает отвергнутые ссылки (2026-08-02) [`b04dc84b62bf35a31dc2585edc58dd9468fc5abc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b04dc84b62bf35a31dc2585edc58dd9468fc5abc)

- **fix**(prd-32): баннер отказа и метка строки говорят о разном (2026-08-02) [`67606506104ca3158609884bf2fcdad3a216c617`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/67606506104ca3158609884bf2fcdad3a216c617)

- **fix**(prd-32): свой ключ строки вложения и защита от позднего ответа загрузки (2026-08-02) [`4975dab6752f8292f1cb26a859c29ce85b2c334d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4975dab6752f8292f1cb26a859c29ce85b2c334d)
  Строка списка вложений получила черновой идентификатор: сервер
  дедуплицирует по контрольной сумме, поэтому один и тот же PDF, приложенный
  дважды, возвращает один id — две строки шли с одинаковым React-ключом.
  Идентификатор актива остаётся в дескрипторе, но больше не ключ списка;
  дескрипторам из props тоже выдаётся идентификатор при инициализации.
  
  Токен визита в useRef отсекает ответ, пришедший после переоткрытия
  модалки: она не размонтируется, и результат прошлого визита иначе
  подшивал вложение или сообщение об отказе в чужую форму.

- **fix**(prd-32): модалка вложений сбрасывает состояние и не теряет сообщения об отказе (2026-08-02) [`fdf66e48376de4f4df7c75370aeb13ed6984e24e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fdf66e48376de4f4df7c75370aeb13ed6984e24e)
  Переоткрытие модалки очищает баннеры и счётчик активных загрузок: она
  отрендерена безусловно и не размонтируется, поэтому состояние прошлого
  визита переживало цикл закрыть/открыть. Выбор новой пачки файлов больше
  не стирает сообщение об отказе — его закрывает крестик баннера.
  
  Результат загрузчика размечен явным союзом, счётчик назван
  uploadsInFlight, занятость кнопки берётся из пропа loading дизайн-системы,
  идентификатор актива из ответа сервера попадает в дескриптор и в ключ
  списка. Подмена fetch в тестах приведена к vi.stubGlobal.

- **fix**(prd-32): предпросмотр и контракт редактора читают канонический адрес вложения (2026-08-02) [`faefa278c18c99712a029cb2712f434801b28c39`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/faefa278c18c99712a029cb2712f434801b28c39)

- **fix**(prd-32): приоритет канонического адреса вложения над легаси-полем (2026-08-02) [`2bcd5bdc33a6fafcf2f0d098755e790b7b4e1eb1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2bcd5bdc33a6fafcf2f0d098755e790b7b4e1eb1)

- **fix**(templates): «Сертификация» догоняет якорь водяного знака (PRD-34) (2026-08-02) [`efe6effc3490199e3bac940d4ce7ea46a6855b2f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/efe6effc3490199e3bac940d4ce7ea46a6855b2f)
  Второе расхождение, вскрытое слиянием PRD-34: ветка добавила в эталонные макеты
  пустой якорь `data-slot="protection-mark"` (question, results, review,
  section-results), а внешний шаблон возит их побайтовые копии и потому остался без
  места для знака — водяной знак в «Сертификации» просто не отрисовался бы, при этом
  валидатор PRD-3 молчит, а гард раскладок краснеет только ПОСЛЕ слияния двух зелёных
  по отдельности веток.
  
  Раскладки перенесены копированием — контракт гарда требует побайтового совпадения, и
  объявленных дельт у этих четырёх файлов нет.
  
  В форк `theme.css` перенесено правило строки `.tb-scene__qmeta`: на экране вопроса
  якорь делит ряд с подсказкой и прижат к правому краю, чтобы знак не забирал
  собственной высоты у фиксированного кадра. Вид самого знака задаёт ядро, шаблон
  отвечает только за место.

- **fix**(it): экспортированный DDL догоняет миграцию 038 — три колонки защиты (PRD-34) (2026-08-02) [`cc12ce5b96541f3df991f0e6de85779aae22f844`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cc12ce5b96541f3df991f0e6de85779aae22f844)
  Расхождение вскрылось при слиянии PRD-34: ветка добавила колонки в `shared/schema.ts`
  и миграцию 038, но `tests/it/schema.sql` не тронула — ровно тот пропуск, который
  гард `tests/storage/schema-export-parity.test.ts` и написан ловить. Обе правки жили в
  разных ветках и по отдельности были зелёными, поэтому поймать это раньше слияния
  было негде.

- **fix**(it): экспортированный DDL догоняет схему — assignment_id в attempts (PRD-31) (2026-08-02) [`43a6a8740380153d66f3c2a853d6ef059ff3af89`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/43a6a8740380153d66f3c2a853d6ef059ff3af89)
  Миграция 037 добавила колонку и в `shared/schema.ts`, но `tests/it/schema.sql` —
  DDL, который применяет pglite-стенд, — ведётся вручную, одной правкой на миграцию,
  и эту правку пропустил.
  
  Сама по себе рассинхронизация ничем не проявляется: схема остаётся валидной. Ломается
  посторонний DAL-тест и глубоко внутри drizzle — список колонок INSERT тот строит из
  `schema.ts`, тогда как база собрана из отставшего файла, и наружу выходит
  `column "assignment_id" of relation "attempts" does not exist`.

- **fix**(media): Vary: Cookie — кеш браузера не отдаёт файл следующему пользователю (2026-08-02) [`9942c8a8c8c252077488ce62ab2d04cfc25e6d20`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9942c8a8c8c252077488ce62ab2d04cfc25e6d20)

- **fix**(media): чистка индекса при массовом удалении и неразрушающая пересборка (2026-08-02) [`7718049275efad1ea28f8d6edbe8270c7c76ba32`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7718049275efad1ea28f8d6edbe8270c7c76ba32)
  Каскадное удаление темы/папки и POST /api/questions/bulk-delete больше не
  оставляют в media_usages строки удалённых вопросов и страниц: deleteTopic/
  deleteTopicsBulk теперь возвращают id вопросов и страниц, снятых каскадом
  (RETURNING в той же транзакции), а маршруты передают их clearCascadedUsages
  (новый помощник в usage-index.ts, лучшие усилия — сбой не рушит запрос).
  
  reindexAllUsages больше не вызывает clearAllMediaUsages перед проходом:
  каждая сущность синхронизируется по отдельности (как раньше), а строки
  исчезнувших сущностей удаляются ОДНИМ проходом в конце по каждому типу
  через новый storage.deleteMediaUsagesExcept/MediaRepository.deleteUsagesExcept
  (id держатся ОДНИМ параметром-массивом text[], не по одному биндингу на id —
  иначе лимит Postgres в 65535 параметров). Keep-списки перечитываются
  непосредственно перед удалением, а не переиспользуются из обхода, — иначе
  вопрос, сохранённый во время пересборки, читался бы как «исчезнувший».
  clearAllMediaUsages/clearAllUsages удалены как мёртвый код.

- **fix**(media): индекс использования на массовых путях импорта, дублирования и согласования страниц (2026-08-02) [`eb93f8760e02c44f2f37ed39d2bbdd2806787a61`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eb93f8760e02c44f2f37ed39d2bbdd2806787a61)
  Синхронизация media_usages добавлена в четырёх местах, идущих мимо неё:
  \- импорт вопросов из Excel (questions-import.ts) — после createQuestion/updateQuestion
  \- POST /api/questions/:id/duplicate — под id копии, не оригинала
  \- POST /api/topics/:id/duplicate — под id каждого продублированного вопроса
  \- согласование системных content_pages при смене flowMode/шаблона
    (test-settings.ts) — создание индексируется, удаление получает null
  
  Все вызовы после фиксации в БД, вне транзакции сохранения, обёрнуты в
  try/catch (сбой пишется в журнал и не роняет операцию).

- **fix**(media): индексация опубликованных снимков — защита выданной версии от потери файлов (2026-08-02) [`3522d61760738e39f24853dc5bd2e407f24312ab`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3522d61760738e39f24853dc5bd2e407f24312ab)

- **fix**(media): ответ 416 на диапазон за пределами файла (2026-08-02) [`9345f42d8ac40cab4c842e39d458a6b1f63d3bf2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9345f42d8ac40cab4c842e39d458a6b1f63d3bf2)

- **fix**(media): уникальность пары владелец+сумма как барьер против гонки загрузки (2026-08-02) [`85f01df3def0b9e0833fd8808fb630d0a8c883ee`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85f01df3def0b9e0833fd8808fb630d0a8c883ee)
  Частичный уникальный индекс (owner_id, checksum) WHERE owner_id IS NOT NULL
  закрывает гонку между SELECT-дедупом и INSERT при параллельной загрузке
  одинаковых байтов одним автором; NULL-владельцы (разовая индексация)
  исключены, т.к. Postgres не считает NULL равными. Загрузка ловит 23505 и
  перечитывает выигравшую строку вместо 500. Заодно поправлен устаревший
  образец адреса в сообщении об отказе base64.

- **fix**(prd-34): относительные пути к стилям в эскизе (2026-08-02) [`dbf8d06444484c8a02d9cd73ca63ff6b628a956a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dbf8d06444484c8a02d9cd73ca63ff6b628a956a)
  Абсолютные пути от корня репозитория работают только когда файл отдаётся
  сервером из корня; открытый по file:// эскиз оставался без стилей.
  Относительные пути работают в обоих случаях.

- **fix**(media): предел времени и размера у кеша решений о доступе (2026-08-02) [`c537610300007f38003879fc6700889136fcf611`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c537610300007f38003879fc6700889136fcf611)

- **fix**(media): обходчик срезает пунктуацию и отбрасывает ключи с выходом за каталог (2026-08-02) [`3ce0dc5bc6f9fbe007701cfcdaeafe51df5173db`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3ce0dc5bc6f9fbe007701cfcdaeafe51df5173db)

- **fix**(media): обходчик находит адреса внутри разметки, а не только целиком в поле (2026-08-02) [`914b38ad6c5202ea7b3308a951459379df6f3213`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/914b38ad6c5202ea7b3308a951459379df6f3213)

- **fix**(media): поиск сирот антисоединением, тесты легаси-корзины и границ (2026-08-02) [`e48b4e5a6da1c4e72b742c0f5faa61e1956819f5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e48b4e5a6da1c4e72b742c0f5faa61e1956819f5)

- **fix**(media): предсказуемый отказ порта на отсутствующем ключе, тесты границ (2026-08-02) [`02d906d6bf749a7acbe8eab67bc4d2c50fed256b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/02d906d6bf749a7acbe8eab67bc4d2c50fed256b)

- **fix**(media): внешний ключ индекса использования, без дублирующего индекса (2026-08-02) [`ff2bff7a5d6e3fe204895cfcf933714539bfc0a1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ff2bff7a5d6e3fe204895cfcf933714539bfc0a1)

- **fix**(prd-29): нечего оценивать — нет ни сводки, ни вердикта (2026-08-01) [`60af76b6e3e22e5ac54c02744e5cf35f6d5e8bb5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/60af76b6e3e22e5ac54c02744e5cf35f6d5e8bb5)

- **fix**(prd-29): сцена итогов ограничена окном на веб-хосте (2026-08-01) [`3dbb6fe9608a4fc2433f3de527848069db527fa4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3dbb6fe9608a4fc2433f3de527848069db527fa4)

- **fix**(prd-29): нет порога — нет вердикта, пустая плашка не рисуется (2026-08-01) [`020681b4c219634c5c1bd2f482c2f2082767001f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/020681b4c219634c5c1bd2f482c2f2082767001f)

- **fix**(prd-29): видимость показателя в редакторе (2026-08-01) [`f5b054bf0e41aead51a5171e326c577bea43c86e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f5b054bf0e41aead51a5171e326c577bea43c86e)
  Блокирующий дефект приёмки D-1: у показателя есть learnerVisibility в модели,
  мапперах, запекании пакета и книге, но контрола в форме не было — значение
  навсегда оставалось hidden. Контрол добавлен в блок «Вывод»; список значений
  экспортирован из секции шкал и переиспользован, чтобы формулировки на двух
  вкладках не разошлись.

- **fix**(template): подсказка сопоставления перестала называть положение карточки (2026-08-01) [`3df6540a8a9e9a864eb8855916084c0c9de39905`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3df6540a8a9e9a864eb8855916084c0c9de39905)
  «Перетащите карточку справа на нужную строку» верно только там, где
  колонки стоят рядом. На узкой сцене сетка сопоставления разворачивается
  в одну колонку (ступень S2), и карточка оказывается ПОД своей строкой —
  подсказка сбивает с толку ровно там, где помощь нужнее всего.
  
  Текст называет объект, а не его место: «Перетащите карточку на нужную
  строку». Верно на любой ширине и на обоих хостах.
  
  Эскизы экрана сопоставления обновлены тем же текстом, чтобы утверждённый
  референс не разошёлся с поставляемой копией. Запись в CHANGELOG о прежней
  формулировке оставлена как есть — это история, а не действующий текст.

- **fix**(template): пол clamp() на планшетной ступени не обрезает подгонку снизу (2026-08-01) [`e2f1fa82be95a09ad5d5f520c0322db26f9a617b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e2f1fa82be95a09ad5d5f520c0322db26f9a617b)
  На ступени S2 активен ШИРОКИЙ профиль подгонки с полом 16/14, а `clamp()`
  держал пол 20/15 — нижняя часть диапазона была недостижима. Подгонка
  считала 16px, CSS печатал 20, и цикл ужимания работал вхолостую ровно
  там, где был нужен: на длинном вопросе.
  
  Замер на сцене 640px, вопрос в 300 слов: прокрутка тела 97px -&gt; 7px.
  
  Потолки (26 и 19) не тронуты — их задача ограничивать сверху.
  
  Добавлен гард: пол `clamp()` каждой ступени обязан совпадать с полом
  профиля, активного на этой ширине — S3 с узким (15/15), S2 с широким
  (16/14). Тест разбирает оба файла и сверяет числа.

- **fix**(prd-29): скругление правого края рельса в эскизе (2026-08-01) [`439450411d2b69c5b17e5fb3b81e4d03eb3d6006`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/439450411d2b69c5b17e5fb3b81e4d03eb3d6006)
  Исполнитель Task 10 нашёл ошибку, которая жила и в задании, и в утверждённом
  эскизе: .tb-zone:last-of-type не совпадает ни с чем. Псевдокласс отбирает по
  типу ЭЛЕМЕНТА, а последний span внутри дорожки — это маркер, не зона. Правый
  край рельса оставался бы квадратным поверх скруглённой дорожки при скруглённом
  левом.
  
  В шаблоне исправлено на :not(:has(~ .tb-zone)); эскиз приведён к тому же, иначе
  ошибка уехала бы в следующую задачу, которая возьмёт его за образец. Первый
  край переведён с :first-of-type на :first-child — там совпадение было
  случайным.

- **fix**(template): пол кегля вопроса на телефоне приравнен к полу вариантов (2026-08-01) [`0fd346358af0b44c39ebce0369d26a1c358e7644`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0fd346358af0b44c39ebce0369d26a1c358e7644)
  У подгонки один рычаг — размер шрифта, и чем ниже он может опуститься,
  тем реже длинный вопрос упирается в потолок высоты и требует отдельной
  прокрутки. Пол вопроса был 18px при поле вариантов 15px; теперь оба 15.
  
  Замер на вопросе в 300 слов (поле 559px): естественная высота блока
  вопроса 383px -&gt; 263px, то есть с 69% поля до 47%. Потолок на таком
  вопросе всё ещё срабатывает, но прячет около 55px вместо 175. Вопросы
  короче ~250px потолок больше не задевает вовсе.
  
  Иерархия «вопрос над вариантом» держится насыщенностью (600 против 400),
  а не размером, поэтому совпавшие полы её не ломают. 15px — та же нижняя
  граница читаемого текста, на которой уже стоят варианты.
  
  Границы в профиле и в `clamp()` ступени S3 обязаны совпадать, иначе CSS
  обрежет то, что посчитала подгонка; это проверяется тестом.

- **fix**(template): убраны дублирующий чип и декоративная иконка на итогах адаптивного теста (2026-08-01) [`cc1c90397183bd6644cde3cd992593dfa7a8f48d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cc1c90397183bd6644cde3cd992593dfa7a8f48d)
  Чип в шапке и заголовок в теле несли ОДИН И ТОТ ЖЕ жёстко зашитый текст
  «Тест завершён» — не разные формулировки одного смысла, а буквальный
  дубль. Чип убран; заголовок остаётся.
  
  Иконка-шапочка над заголовком ничего не кодировала и просто занимала
  место. Убрана вместе со ставшими мёртвыми правилами `.tb-adaptive-hero__icon`.
  
  Соседние экраны проверены и не тронуты: у перехода между уровнями чип
  подписывает тип экрана, а заголовок динамический; у стены блокировки в
  теле три ветки с разными заголовками. Их иконки несут смысл — стрелки
  вверх/вниз кодируют смену уровня.

- **fix**(prd-29): кольцо опознаётся признаком, линейка ушла внутрь условия (2026-08-01) [`532086068e7ac8d5d483fcb60cb8892dcb4f0687`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/532086068e7ac8d5d483fcb60cb8892dcb4f0687)
  Два дефекта, оба найдены исполнителем Task 9, оба мои.
  
  D9. Ведущий разделитель блока показателей стоял СНАРУЖИ условия. На
  контрольном тесте показателей нет, но hideScoreSummary отсутствует, значит
  условие истинно и линейка рисуется в пустоту — между сводкой и «Результатами
  по темам» встали бы две линейки вместо одной. Тот же класс ошибки, что уже
  ловили, только теперь на контрольном экране. Линейка внесена внутрь блока
  первым потомком: она есть тогда и только тогда, когда блок существует и над ним
  есть сводка.
  
  D10. Кольцо опознавалось по ringDashoffset. На максимуме шкалы смещение дуги
  равно нулю, то есть кольцо пропало бы ровно у того результата, который его
  заполняет — снова подготовленное ЧИСЛО в роли булева признака, ровно как со
  сводкой. Ядро отдаёт явный isRing рядом с percent; сравнений в разметке нет и
  быть не может, движок их не поддерживает. Два теста.
  
  Заодно закрыты два наблюдения исполнителя для Task 10: типографика значения
  переехала с селектора слайдера на строку меры (шкала без домена рисует значение
  вне обёртки слайдера и осталась бы мелкой рядом с крупной), а дуга кольца
  получила тон меры — иначе критический уровень рисовался бы акцентным цветом
  рядом с красной плашкой.

- **fix**(prd-29): признак сводки инвертирован, у рельса появилась подпись (2026-08-01) [`a17f661a01501561ea46c720d2965b22e0013e97`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a17f661a01501561ea46c720d2965b22e0013e97)
  Исполнитель Task 9 остановился перед катастрофой и был прав.
  
  showScoreSummary выставлялся ТОЛЬКО внутри ветки измерений, а измерения не
  передаёт ни SCORM-рантайм, ни предпросмотр, ни веб-хост контрольного теста.
  Разметка вида {{#if result.showScoreSummary}} убрала бы кольцо процентов и
  «вопросов / верно / баллов» у КАЖДОГО контрольного теста во всех выгруженных
  пакетах и в предпросмотре. Ошибка моя: положительный признак при отсутствующем
  значении означает «скрыть».
  
  Признак инвертирован в hideScoreSummary: записывается только подавление,
  отсутствие означает «показывать». Контрольный контекст не меняется вовсе.
  
  Заодно закрыт восьмой дефект: рельс собран из span-ов и для скринридера нем, а
  собрать фразу из связываемых частей разметка не может. CtxMeasureView получил
  ariaLabel, который готовит ядро — «Имя: 27 из 45, уровень: Высокий».

- **fix**(template): статус незавершённой попытки — из подвала в тело старта (2026-08-01) [`0ce4c12c442e6145f3a793fe8f3a1312193a4550`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0ce4c12c442e6145f3a793fe8f3a1312193a4550)
  «Незавершённый тест — вопрос N из M» — это состояние попытки, а не
  действие, и в панели действий ему не место: в разметке строка стояла
  между кнопками и рвала ряд там, где стояла.
  
  Перенесена в тело баннером `ou-banner--sm` рядом с «Прошлой попыткой» и
  «Повторное прохождение недоступно» — тот же класс информации уже
  оформлен так же, новый элемент не изобретался. Порядок чтения: факты -&gt;
  как проходить -&gt; прошлая попытка -&gt; незавершённая попытка -&gt; кнопки,
  то есть статус стоит прямо над кнопкой, которую объясняет.
  
  Правка в разметке, а не в CSS, поэтому действует на обоих хостах и на
  всех ширинах: на десктопе заметка тоже больше не втискивается между
  кнопками. Подвал старта на телефоне: 4 ряда и 183px -&gt; 2 ряда и 111px.
  
  Правило подвала для `.tb-scene__note` из мобильного слоя убрано —
  вешать его больше не на что.

- **fix**(template): порядок подвала старта и заглушка обложки на телефоне (2026-08-01) [`7e96ae946a8e76e1035871f4ddb310ec14bf7fe9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7e96ae946a8e76e1035871f4ddb310ec14bf7fe9)
  Подвал старта в состоянии «есть незавершённая попытка» разваливался на
  четыре ряда (183px): в разметке строка статуса стоит между кнопками, а
  `flex-basis: 100%` рвёт ряд там, где она стоит. Одинокая ghost-кнопка
  «К списку тестов» оказывалась сверху и читалась как заголовок секции.
  
  Статус вынесен вперёд (`order: -1`) и приглушён до подписи — дальше
  перенос группирует сам: статус, затем две альтернативы рядом, затем
  основное действие во всю ширину. 4 ряда -&gt; 3, 183 -&gt; 133px.
  
  Обложка варианта «изображение справа» без заданной картинки рисовала
  пустую рамку — серый прямоугольник с иконкой картинки, читавшийся как
  сломанное изображение. Теперь это абстрактная градиентная панель, тем же
  приёмом, каким шаблон «Сертификация» уже оформляет свой безкартиночный
  вариант обложки. Иконка убрана: панели, которая ничего не сообщает, не
  нужен значок, объясняющий, что она ничего не сообщает.
  
  На телефоне панель-заглушка не рисуется вовсе — без изображения она не
  несёт ничего и забирала около трети экрана у фактов и кнопки старта.
  Заданная автором картинка показывается как прежде.

- **fix**(prd-29): подсказка кодов работает на пустом перечне исходов (2026-07-31) [`84542ff1a88eec274c75a4827c283914b8c70386`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/84542ff1a88eec274c75a4827c283914b8c70386)
  Исполнитель Task 15 назвал побочный эффект своей реализации, и он оказался
  дефектом моего контракта. Банер-предупреждение и подсказка кодов брали один и
  тот же findUnknownOutcomes, а он по контракту молчит на пустом перечне —
  чтобы не шуметь, пока автор ещё не начал заполнять список.
  
  Для предупреждения это верно, для подсказки — ровно наоборот. Засеять перечень
  из готовой формулы одним нажатием (PRD-29 §5.1) нужно именно тогда, когда не
  объявлено ничего: методику импортировали, формула есть, список пуст. Общий
  источник выключал возможность в тот момент, когда она нужнее всего.
  
  Потребители разведены: предупреждение остаётся на findUnknownOutcomes с
  подавлением, подсказка считает литералы формулы за вычетом объявленных, без
  подавления. Добавлены два теста, включая прямую фиксацию разного поведения.

- **fix**(prd-29): параметры уровней отзеркалены в шаблон сертификации (2026-07-31) [`f9383bcb1254bc3353f3e1f65fe6be9c60275370`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f9383bcb1254bc3353f3e1f65fe6be9c60275370)
  Регресс от 25b3f75: шесть параметров PRD-29 были добавлены только в
  стандартный шаблон, а tests/template-layout-parity.test.ts требует, чтобы оба
  поставляемых шаблона предлагали автору один набор параметров дизайна — иначе
  тест, переключённый на другой шаблон, теряет настройки.
  
  Пропустил при проверке я: прогнал только два новых файла тестов Task 11 и не
  тронул соседние. Найдено исполнителем Task 13 на полном прогоне.
  
  Настройки блоков (scoreSummary/indicators/scales) у вида «Итоги» сертификации
  пока НЕ добавлены сознательно: его макет получит блоки измерения в Task 9,
  которая сейчас заблокирована параллельной работой в layouts и theme.css.
  Добавлять переключатели к блокам, которых в макете нет, значило бы показать
  автору настройку без действия.

- **fix**(prd-25): не предлагать тест, который пройти нельзя (2026-07-31) [`bfd428a059667333a2bafd932d022f1cf7eabdd0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bfd428a059667333a2bafd932d022f1cf7eabdd0)
  Главная. Секция «Мне назначено» показывала тест с исчерпанными попытками с
  активной кнопкой «Начать»: решение о кнопке принимал ОДИН факт — открыт ли
  cooldown, а счётчик «использовано 1 из 1» ехал рядом просто текстом. Старт
  сервер отклоняет (ATTEMPTS_EXHAUSTED), то есть кнопка вела в тупик. Теперь
  секция отдаёт только то, что ещё можно пройти, и total считает тот же список.
  Незавершённая попытка сильнее исчерпания — начатое дают завершить. Закрытый
  cooldown исключением НЕ является: такой тест проходится позже, и его карточка
  вместо кнопки показывает дату открытия.
  
  Итоги адаптивного теста в вебе. Кнопка «Пройти снова» стояла под
  {{#unless result.hasScormActions}}, то есть «в вебе — всегда», без оглядки на
  попытки, а клик гасился в result.tsx: мёртвая кнопка. Подвал получил ВТОРОЙ
  факт о прогоне — canRetake («попытки остались») рядом с canRetry («не сдал И
  попытки остались»). Разделены они не ради симметрии: обычные итоги предлагают
  пересдачу как средство исправить незачёт, адаптивные — как повторный прогон,
  который осмыслен и после зачёта. buildResultsNav выводит canRetake из canRetry,
  поэтому хост вправе заполнить любой один факт. Пакет SCORM не затронут: его
  адаптивный подвал живёт на своих флагах.
  
  Приёмка в браузере: A/B на одном тесте (попытки 1 из 5 и 1 из 1, попытка
  зачётная) — строка на главной и кнопка в подвале появляются и исчезают вместе
  с попытками, выход «К списку тестов» остаётся всегда.

- **fix**(scales): заголовки колонок матрицы вкладов больше не слипаются (2026-07-31) [`eb54a28ed10082f511d3d64ee323a3633d60c6da`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eb54a28ed10082f511d3d64ee323a3633d60c6da)
  Ключ шкалы — одно неразрывное слово: подчёркивание не даёт точки переноса,
  поэтому в колонке фиксированной ширины (7rem, table-layout: fixed) заголовок
  вида EMOTIONAL_EXHAUSTION выходил за границы ячейки и наезжал на соседние.
  
  Разметка отдаёт &lt;wbr&gt; после каждого подчёркивания, чтобы ключ переносился по
  своим сегментам; CSS разрешает перенос как запасной вариант для длинного ключа
  без подчёркиваний и выравнивает многострочные шапки по низу. Подсказка th
  падает на ключ, когда у шкалы нет названия.
  
  Приёмка в браузере: светлая и тёмная темы, горизонтальная прокрутка с
  закреплённой колонкой «Вариант ответа» — наложений между колонками шкал нет.

- **fix**(prd-18): эталон в адаптивном тесте показывал ключ чужого вопроса (2026-07-31) [`878a3518481bb5396f9cbf381cf707da928de870`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/878a3518481bb5396f9cbf381cf707da928de870)
  Оверлей брал текущий вопрос как flatQuestions[currentIndex] — это верно
  только для стандартной выдачи. В адаптиве экран ведёт adaptiveState
  (getCurrentAdaptiveQuestion по TEST_DATA.adaptiveTopics), а flatQuestions
  либо пуст (legacy linear_flat: initAdaptiveTest вместо generateVariant),
  либо держит чужой вариант выдачи. В итоге на одиночном вопросе появлялось
  несколько отметок, а на вопросе с двумя верными — одна.
  
  Резолвер currentScreenQuestion зовёт ту же функцию пакета, что и рендер,
  с фолбэком по currentQuestionId; если вопрос не резолвится — оверлей не
  рисуется вовсе. Побочно закрыт случай legacy-адаптива, где эталон не
  показывался совсем.

- **fix**(email): нечитаемая кнопка в письмах (2026-07-31) [`a3fd305fb9a189f5e2de42a9cc7b229b93cee468`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a3fd305fb9a189f5e2de42a9cc7b229b93cee468)
  Цвет текста кнопки задавался только правилом `.button` в блоке `&lt;style&gt;`.
  Почтовые клиенты навязывают ссылке свой цвет, и он перебивал классовое
  правило: подпись выходила фиолетовой на фиолетовой подложке.
  
  Общий сборщик `ctaButton()` заявляет цвет инлайн, с `!important` и ещё раз
  на вложенном `&lt;span&gt;` — для клиентов, которые перекрашивают `&lt;a&gt;`, но не
  его детей. Правило `.button` осталось запасным вариантом. Кнопка теперь
  одна на все три письма (сброс пароля, приглашение, назначение теста).

### Documentation

- **docs**(prd-43): mark the spec implemented and merged (2026-08-05) [`7c3b6807bcfa821fbc738617e6f2c21dec3b575b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7c3b6807bcfa821fbc738617e6f2c21dec3b575b)

- **docs**(prd-43): explain why handleNext/handleStandardContinue aren't interchangeable (2026-08-05) [`bc5d090bd40ca4e5c25f75c33a5dd8a232718a13`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bc5d090bd40ca4e5c25f75c33a5dd8a232718a13)

- **docs**(prd-43): fix misattributed comment in apiToEditorModel (2026-08-05) [`e670919113aaff03e79a72fc3edab62634d77d58`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e670919113aaff03e79a72fc3edab62634d77d58)

- **docs**(prd-44): скелет спеки — тип вопроса «распределение баллов между шкалами» (2026-08-05) [`f6031615159116f599ca11c3b46f798be740b179`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f6031615159116f599ca11c3b46f798be740b179)

- **docs**(prd-42): убрать третью копию мёртвых правил tb-feedback-editor__asset* (2026-08-05) [`04eecbdee6bb2d67877d36c5c7635132f7498ea9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/04eecbdee6bb2d67877d36c5c7635132f7498ea9)
  У этого же файла есть собственный inline &lt;style&gt; — отдельная, третья
  копия правил .tb-feedback-editor__asset*/__upload*, которую не задел
  коммит 03d764c (он чистил только две внешние копии tb-components.css).
  Разметка, использовавшая эти классы, уже заменена на «Материалы» в
  этом же файле — правила осиротели и здесь.

- **docs**(prd-42): убрать упоминания «PDF-assets» из заметок эскиза (2026-08-05) [`ccd0e8caa9c8eb98a5e660d14b0a522884f81de1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ccd0e8caa9c8eb98a5e660d14b0a522884f81de1)
  Текст таблиц заметок и DS-mapping в prd7-editor-drawer.html описывал
  секцию s-feedback-edit старыми словами (PDF-assets) уже после того,
  как сама разметка блока стала «Материалы». Превью-бейдж «1 файл» в
  списке тем (s-default/s-default-adaptive) НЕ трогаю: это другое
  состояние эскиза и оно соответствует не изменённому в этом PRD виджету
  feedback-preview.tsx («Документы»), где различение курс/файл остаётся.

- **docs**(prd-42): эскиз — секция «Материалы» вместо загрузки PDF (2026-08-05) [`03d764c8a442d8300adbe1f944bd181a0d3833f8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/03d764c8a442d8300adbe1f944bd181a0d3833f8)
  Синхронизирует s-feedback-edit с фактической реализацией
  feedback-editor-modal.tsx после PRD-42. Заодно чистка мёртвого кода,
  найденного код-ревью Task 2: неиспользуемый импорт userEvent в тесте,
  осиротевшие правила .tb-feedback-editor__asset*/__upload* в обеих
  копиях tb-components.css.

- **docs**(prd-43): implementation plan for the quick-advance toggle (2026-08-05) [`ecf0db5ce901fd742122331607d29b416ee16ee1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ecf0db5ce901fd742122331607d29b416ee16ee1)

- **docs**(prd-43): исправить раздел миграции — актуальный тракт drizzle-kit generate, не legacy migrations/ (2026-08-05) [`a584ee6ea9640843aa689dd6b1dfab0c9b19d2fc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a584ee6ea9640843aa689dd6b1dfab0c9b19d2fc)

- **docs**(prd-43): спека — быстрый переход к следующему вопросу отдельным переключателем (2026-08-05) [`abb2619278b66c199c78aac78ccd520529d9ca76`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/abb2619278b66c199c78aac78ccd520529d9ca76)

- **docs**(prd-42): план реализации — материалы обратной связи только ссылкой (2026-08-05) [`c393f0af5ee432f667a9fbf9d149f88506c06e4e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c393f0af5ee432f667a9fbf9d149f88506c06e4e)
  5 задач: модель данных (fileName/mimeType опциональны), редактор
  (секция «Материалы» вместо загрузки), сервер (удалить
  purpose=feedback-asset), эскиз prd7-editor-drawer.html, итоговая
  проверка включая ручную сверку рабочей ссылки в скачанном PDF.

- **docs**(prd-42): спека — материалы обратной связи только ссылкой, без загрузки (2026-08-05) [`73a00f7279bc39533e9ac26815476e948b79a9e9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/73a00f7279bc39533e9ac26815476e948b79a9e9)
  PDF-отчёт печатает ссылки на вложения буквально (pdf.link), а адрес вложения
  (/api/media/&lt;id&gt;) относительный — на вебе без origin, в SCORM-пакете вообще
  переписан на путь внутри zip. Вместо резолвинга адреса меняем модель: новые
  вложения — сразу настоящий внешний URL, как у «Курсы»/«Мероприятия».

- **docs**(prd-40): план и спека периода охлаждения по исходу попытки (2026-08-05) [`e85ee0eb4276d3053c62b34e5534a5dc9003c03c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e85ee0eb4276d3053c62b34e5534a5dc9003c03c)

- **docs**(prd-40): retire suspend_data_cooldown from PRD-6 spec, changelog entry (2026-08-05) [`6edf9b24f15ffe85639bd1ceea6f238d1ed97f4c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6edf9b24f15ffe85639bd1ceea6f238d1ed97f4c)

- **docs**(prd-41): скелет PRD Moodle cooldown plugin с открытыми вопросами (2026-08-04) [`32ad7b04dbce65f6a26003fd25f358eb8b15322e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/32ad7b04dbce65f6a26003fd25f358eb8b15322e)
  Cooldown-гейт (PRD-6) сейчас поддерживает только WebTutor; для курсов в
  Moodle нужен отдельный eligibility-plugin. Архитектура исполнения
  (браузер/сервер), источник данных о попытке и аутентификация к Moodle
  Web Services пока не выяснены — фиксируем вопросы для проверки на живом
  Moodle перед проектированием требований.

- **docs**(prd-40): tighten passedStateIn comment reference (2026-08-04) [`5e1405c165e9fcf67cf068139e8a75f2a2a98b91`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5e1405c165e9fcf67cf068139e8a75f2a2a98b91)
  Code review flagged the doc pointer to "PRD-6 §3.6 header note" as
  imprecise (no such header note exists there) - point at the actual
  adjacent comment instead.

- **docs**(prd-39): план реализации слияния карточек сопоставления (2026-08-04) [`5c2c455677fc9c2fe395ff544682b8d080640b30`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5c2c455677fc9c2fe395ff544682b8d080640b30)

- **docs**(prd-39): спека слияния карточек сопоставления вместо стрелки (2026-08-04) [`0e05d4805d5e8935dd6fb0ddfa6b29b9a857e2b7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0e05d4805d5e8935dd6fb0ddfa6b29b9a857e2b7)

- **docs**(prd-39): спека слияния карточек сопоставления вместо стрелки (2026-08-04) [`98551167b414818197145f073bd5ea614e017c4a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/98551167b414818197145f073bd5ea614e017c4a)

- **docs**(prd-30): раздел 14 спеки расширен, ROADMAP отражает статус линии (2026-08-04) [`aa3869b19847841fb71258fe0564dfe8bc7b61d8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/aa3869b19847841fb71258fe0564dfe8bc7b61d8)
  Спека дополнена разделом 14 (порядок на уровне теста: контракт имён,
  Э8-Э15, зависимости задач). ROADMAP: запись PRD-30 дополнена фиксом
  linear_flat от 2026-08-03. spec-template-platform.pdf и
  template-development.pdf пересобраны заодно единым прогоном npm run docs:pdf
  (содержимое источников не менялось).

- **docs**(prd-30): план и эскиз расширения «порядок выдачи на уровне теста» (2026-08-04) [`a19078a2f7a5d236c38c9ae5bb1c9661f64f1853`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a19078a2f7a5d236c38c9ae5bb1c9661f64f1853)
  Этап Э8: контрол темы получает третью позицию «как в тесте» поверх
  утверждённого prd30-question-order.html, без переделки остальной анатомии
  строки. План разбивает работу на 9 задач по этапам спеки Э8-Э15.

- **docs**(prd-38): дефект оверлея в теневом корне зафиксирован в плане (2026-08-04) [`6aecbea8ac8da400d28a5aa57d7f22d12585643b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6aecbea8ac8da400d28a5aa57d7f22d12585643b)
  Ревью нашло, что оверлей монтировался в document.body, а theme.css в вебе
  инжектится внутрь теневого корня — правила .qm-overlay до него не дошли бы.
  Черновик плана размечен как устаревший, источник истины — реализация в f4ddf16.

- **docs**(prd-38): поправлены ссылки на тесты защиты, техдолг примера SCORM (2026-08-04) [`fc5717c90cea2bf106e3eb849b40d6272c5daba6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fc5717c90cea2bf106e3eb849b40d6272c5daba6)

- **docs**(prd-38): отчёт приёмки композиции медиа вопроса (2026-08-04) [`bbc66f32a2b41ed121d71a8cd15dc3b9a4f20b10`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bbc66f32a2b41ed121d71a8cd15dc3b9a4f20b10)

- **docs**(prd-38): эскиз композиции медиа вопроса согласован (2026-08-03) [`d760b080f19e6569635a1b9248e93796d92f67a6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d760b080f19e6569635a1b9248e93796d92f67a6)

- **docs**(prd-38): CSS плана заменён на проверенный в браузере (2026-08-03) [`ffd0b6704889033e9a28a779c7e09dbed8a746df`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ffd0b6704889033e9a28a779c7e09dbed8a746df)

- **docs**(prd-38): план реализации и общий оверлей медиа в требованиях (2026-08-03) [`d912a935a044f850937ade03965f0bb5fbcecd85`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d912a935a044f850937ade03965f0bb5fbcecd85)

- **docs**(prd-38): спека композиции медиа вопроса перенесена в docs/specs (2026-08-03) [`c1a958ccf332c3151302621e626c4d1aa832d52b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c1a958ccf332c3151302621e626c4d1aa832d52b)

- **docs**(question-media): спека композиции медиа вопроса и вида аудио-вопроса (2026-08-03) [`095c19564fccf0462b9f80c519f85d446b0f652c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/095c19564fccf0462b9f80c519f85d446b0f652c)

- **docs**(scorm): PRD-36 компактность состояния прогона и PRD-37 профиль SCORM 1.2 (2026-08-03) [`996438078a2d657e73eaf8cab29ed5e33b3face7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/996438078a2d657e73eaf8cab29ed5e33b3face7)
  Разбор профиля SCORM 1.2 вскрыл самостоятельный дефект профиля 2004:
  cmi.suspend_data хранит полное содержимое выданных вопросов по копии на
  каждую попытку, потолка у массива попыток нет, и предел 64000 пробивается
  на первой же попытке длинного теста — молча, с одновременной потерей
  счётчика попыток, истории, якоря таймера и обеих дат барьеров.
  
  PRD-36 фиксирует целевую модель: содержимого вопросов в состоянии нет,
  вопрос адресуется позицией в TEST_DATA, массив попыток заменяется на
  счётчик, лучшую и последнюю. Инвентаризация потребителей показала, что
  массив не нужен ни одному из них, а содержимое вопросов читает единственное
  место — сборка interactions по лучшей попытке. Правило «в LMS уезжает
  лучшая попытка» намеренно не меняется: требования на него нет ни в одном
  PRD, но менять правило оценки заодно с оптимизацией хранения нельзя.
  
  PRD-37 фиксирует объём профиля 1.2 и перечень потерь как часть требований,
  подлежащую согласованию, а не как будущий дефект. PRD-36 записан жёстким
  условием входа: предел suspend_data в 1.2 равен 4096 при нынешних 35-75
  тысячах, и сжатие этот разрыв не закрывает.
  
  Строка техдолга о suspend_data сохранена как след происхождения и помечена
  переводом в трек.

- **docs**(results): приёмка консолидации текстов и три пункта техдолга (2026-08-03) [`c75540dc884eafb2e94a5efc39427c769bacd206`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c75540dc884eafb2e94a5efc39427c769bacd206)
  Работа принята: тексты теста, темы и раздела показываются одним блоком на
  обоих хостах, в обоих режимах и в отчёте. Приёмка вскрыла тупик завершения
  попытки в пакете (PRD-19) и два расхождения вердикта — вынесены в техдолг.

- **docs**(results): план — правило видимости уточнено владельцем (2026-08-02) [`cd30ea3d7bea76e03874246cfc2ec78b45b47591`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cd30ea3d7bea76e03874246cfc2ec78b45b47591)

- **docs**(results): план консолидации текстов обратной связи (2026-08-02) [`f4ace67a1fbcb011ddab0a5eeb2fc2e308cac460`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f4ace67a1fbcb011ddab0a5eeb2fc2e308cac460)
  Тексты теста и тем показываются одним блоком на экране итогов и в отчёте, без
  повторов, на обоих хостах и в обоих режимах. Согласовано: всё по теме видно
  только при непройденной теме, адаптивный режим входит в объём.

- **docs**(prd-32): приёмка принята — четвёртый заход и статус спеки (2026-08-02) [`9538e49466981489dc5057f87cdfa16633ba7d5e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9538e49466981489dc5057f87cdfa16633ba7d5e)

- **docs**(prd-32): дорожная карта и отчёт приёмки (2026-08-02) [`77e97e62ef94babe75afb18e8a2bd38ba6fae39f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/77e97e62ef94babe75afb18e8a2bd38ba6fae39f)
  PRD-32 закрыт вместе с этапом Э4 медиатеки: упаковщик SCORM резолвит адрес
  реестра, поэтому медиа вопросов, страниц и оформления снова попадает в пакет.
  Три находки за пределами работы вынесены в технический долг.

- **docs**(prd-32): спека — пятая поверхность вложений и показ на итогах (2026-08-02) [`ce634fda10b9dcf885121a123c179a79f43a04c8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ce634fda10b9dcf885121a123c179a79f43a04c8)
  Приёмка вскрыла, что вложение можно приложить и разделу теста
  (`test_sections.feedback_json`), причём поле было write-only. Плюс
  зафиксирован показ вложений темы и раздела в блоке «Материалы».

- **docs**(prd-32): техдолг — импорт книги перезаписывает конфигурацию шкалы (2026-08-02) [`013db51f801d3e69ce88efb882bd605786514c15`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/013db51f801d3e69ce88efb882bd605786514c15)

- **docs**(prd-32): техдолг — зависимости эффекта сброса модалки вложений (2026-08-02) [`8526c342ca70336140e989677a885d2167cf167f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8526c342ca70336140e989677a885d2167cf167f)

- **docs**(prd-32): план — перегенерация превью шаблонов после смены правила адреса (2026-08-02) [`565477bf784abaec525a544710103c80b3fdf19d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/565477bf784abaec525a544710103c80b3fdf19d)

- **docs**(prd-32): план — приоритет url над scormHref и уборка комментариев о неидемпотентности (2026-08-02) [`e9dad72b440d7e68fa127c13d0fcc31fc58016d6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e9dad72b440d7e68fa127c13d0fcc31fc58016d6)

- **docs**(prd-32): план реализации доставки вложений и упаковки медиа из реестра (2026-08-02) [`017285e5171b5630ea1d7d7511c5b8f8a62c2ff0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/017285e5171b5630ea1d7d7511c5b8f8a62c2ff0)
  16 задач: контракт дескриптора, строгая проверка загрузки, немедленная загрузка
  в редакторе, резолвер реестра и переписанный упаковщик, гард автономности пакета,
  индекс использования и права для четырёх мест хранения обратной связи, приёмка.
  
  Спека уточнена: шкалы и показатели индексируются набором теста, а не поштучно —
  выборки одной шкалы по идентификатору в контракте хранилища нет.

- **docs**(prd-32): спека доставки вложений обратной связи и упаковки медиа из реестра (2026-08-02) [`358d83d788eb4e9b59a543f4809e8e178fc808f4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/358d83d788eb4e9b59a543f4809e8e178fc808f4)
  Работа закрывает PRD-32 и этап Э4 медиатеки: оба упираются в одну механику —
  упаковщик SCORM обязан резолвить канонический адрес реестра.
  
  Вскрыт второй, более крупный повод: после включения медиатеки загрузка отдаёт
  /api/media/&lt;id&gt;, а упаковщик знает только data: и /uploads/ — медиа вопросов,
  страниц и оформления в ZIP не попадает, пакет теряет автономность.

- **docs**(roadmap): два пункта техдолга, всплывших хвостом паритета «Сертификации» (2026-08-02) [`ae24d85feafa7a8aea5936ff1da71e036cd4bd03`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ae24d85feafa7a8aea5936ff1da71e036cd4bd03)
  Оба всплыли при догоне радара и оба отложены сознательно, чтобы не ехать хвостом
  чужой задачи.
  
  Генератор превью вшивает содержимое макетов в `preview.html` строковыми литералами
  через `JSON.stringify`, не приводя переводы строк к LF: на Windows-копии в литералы
  попадает `\r\n` против `\n` в закоммиченных превью, и перегенерация даёт дифф на
  тысячи невидимых отличий поверх содержательной правки. Правка тулинга требует своей
  приёмки и пересечётся с незакоммиченной правкой DSL в том же скрипте.
  
  Паритет внешнего шаблона держится вручную, а гард сверяет только РАЗМЕТКУ: ни стили,
  ни `contentTemplates[].settings` он не смотрит. Замер показал живые расхождения — в
  `theme.css` нет пяти классов степпера PRD-26, у `results.standard` нет трёх настроек
  PRD-29, остался мёртвый `has-slide-bg`. Записан и выход из класса ошибок:
  не расширять гард, а убрать двойственность через `manifest.extends`, разделив резолв
  раскладки и стилей (сейчас фолбэк-экран тянет `theme.css` эталона целиком и теряет
  бренд).

- **docs**(prd-34): приёмка ученических экранов веб-хоста (2026-08-02) [`ca25299aba74db0dcfc59491b8117ad04c9e045d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ca25299aba74db0dcfc59491b8117ad04c9e045d)
  Проверено на реальной сессии ученика: заведены учётка, тема с тремя вопросами,
  опубликованный тест с включённой защитой и назначение. Данные аддитивны.
  
  Экран вопроса: пометки и user-select none, copy/меню/перетаскивание погашены
  внутри периметра и НЕ погашены на футере, сообщение показано, печатное правило
  в Shadow DOM сцены, знак в объявленном якоре.
  
  Экран итогов (AC-02): защищённых регионов ноль, копирование проходит, знак при
  этом на месте — ровно FR-09 плюс FR-16.
  
  Выключенные настройки (AC-03): ни защиты, ни знака, ни заглушки.
  
  Отмечено: dev-сервер синхронизирует встроенный шаблон при запуске, поэтому
  правка макета не подхватывается живым процессом — до перезапуска знак вставал
  запасным размещением над шапкой.

- **docs**(prd-34): отчёт о браузерной приёмке защиты от копирования (2026-08-02) [`32650811c386cadb60ce342b0623f21ec2ccadcd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/32650811c386cadb60ce342b0623f21ec2ccadcd)
  Проверка велась на собранном SCORM-пакете в локальном плеере, то есть на том
  коде, который уходит в LMS: экран вопроса, экран обзора, водяной знак,
  скрытие при потере фокуса, выключенная настройка и отладочный признак.
  
  Отдельным разделом перечислено НЕпроверенное с причинами: полный прогон и
  покрытие (запрещены без разрешения), ученические экраны веб-хоста живьём и
  полный круг настроек (нет доступных тестовых данных в общей dev-базе),
  мобильный callout iOS (десктопный Chrome свойство не принимает), печать,
  PDF-отчёт и загрузка стороннего шаблона через реестр.

- **docs**(prd-34): знак в строке подсказки, автовозврат заглушки, сообщения по DS (2026-08-02) [`a74959b731e9a278b98fb4c238f5d373c8da8410`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a74959b731e9a278b98fb4c238f5d373c8da8410)
  Знак переехал в строку подсказки «Выберите…», прижатый вправо, кегль 10 px,
  непрозрачность 0.35. Собственной высоты он больше не занимает — делит строку
  с уже существующей подсказкой и не сдвигает содержимое.
  
  Заглушка снимается САМА по возврату фокуса или видимости, кнопки на ней нет.
  Обязательный щелчок был платой за каждое переключение окна, включая
  всплывающие окна самой LMS, то есть маленьким наказанием — а трек наказаний
  не вводит (FR-14, FR-15, FR-23).
  
  Сообщения приведены к DS. Предупреждение о копировании — ou-toast в штатной
  стойке ou-toast-stack: баннер ДС объявлен inline-уведомлением и тостом НЕ
  является, а стойка задаёт и позицию, поэтому свой класс с выдуманным
  расположением убран. Заглушка — ou-empty (--inline + --horizontal): это
  компонент для «здесь намеренно ничего нет, вот почему». Вертикальный вариант
  не годится — вуаль накрывает регион в две строки, и блок обрезался по его
  границе; пиктограмма уменьшена до размера строки и выровнена по средней.

- **docs**(prd-34): эскиз водяного знака, заглушки и предупреждения (2026-08-02) [`4f53395062b19ac067839405000d073818384694`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4f53395062b19ac067839405000d073818384694)
  Шесть состояний на реальной разметке: сцена вопроса взята дословно из
  layouts/question.html, сцена итогов — из layouts/results.html, theme.css
  шаблона и ДС подключены как есть. Дельта — три элемента, которые строит
  ядро: tb-protection-mark, tb-protection-veil, tb-protection-toast.
  
  Эскиз утверждает ровно два параметра знака — кегль 11 px и контраст
  --ou-fg-muted при 0.6. Место знака эскизом не утверждается: его объявляет
  макет якорем.
  
  Найдено при сверке скриншотов: запасное размещение ставит знак ПЕРЕД
  шапкой, и на экране итогов это выглядит как случайная надпись у края
  кадра. Поэтому поставляемый шаблон объявляет якорь на всех четырёх
  экранах знака, а запасное размещение остаётся деградацией для чужого
  шаблона. Спека и план поправлены.

- **docs**(media): план — предел времени и размера у кеша решений о доступе (2026-08-02) [`97b3c065e880c44e7499d228d406b1060cd84b6c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/97b3c065e880c44e7499d228d406b1060cd84b6c)

- **docs**(prd-34): место знака объявляет макет якорем, а не угадывает ядро (2026-08-02) [`aaa601a549f432b7003a7836fa757d5085c1453c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/aaa601a549f432b7003a7836fa757d5085c1453c)
  Прежний способ — подъём от слота вариантов до общего предка со слотом
  задания — верен только для вертикальной раскладки. На двухколоночном
  шаблоне строка становится третьей колонкой и ломает раскладку, а шаблон
  по PRD-3 произволен. Угадывание запрещено явно.
  
  Теперь макет объявляет пустой якорь [data-slot="protection-mark"], а ядро
  даёт разметку, кегль и контраст. Разделение не стилистическое: если бы знак
  писал шаблон, внешний шаблон получил бы власть над читаемостью атрибуции.
  Якоря нет — знак встаёт строкой в начало сцены; мера не отключается никогда.
  
  Следствие-упрощение: экран для размещения знать не нужно, поэтому тип
  WatermarkAnchor и таблица «экран → якорь» из плана убраны, ProtectionSpec
  вернулся к watermarkText. Наличие якоря показывается администратору в
  проверке шаблона (PRD-3) — информационно, загрузку не блокирует.

- **docs**(media): план — сбой индексации не роняет сохранение сущности (2026-08-02) [`ff99d71cda71564c3ba20573a14d73a37ff20f37`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ff99d71cda71564c3ba20573a14d73a37ff20f37)

- **docs**(prd-34): кегль знака фиксированный минимальный, полоса не резервируется (2026-08-02) [`c68b450b1187723d373a2a2643557e4ff31ef73a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c68b450b1187723d373a2a2643557e4ff31ef73a)
  Знак не подчиняется автоподбору по длине содержимого, которому подчиняются
  задание и варианты: кегль — константа 11 px, одинаковая на всех экранах.
  Отдельного места под знак не резервируется, он встраивается в существующий
  просвет и занимает высоту одной своей строки, без отступов.
  
  Следствие для приёмки: проверяется, что кегль знака одинаков на коротком и
  на длинном задании. Если сцена перестала укладываться — чинится укладка,
  а не знак.

- **docs**(prd-34): водяной знак — читаемая строка в просвете, не диагональная плитка (2026-08-02) [`300603b06cac5f0217be89ccc48499399e9108ad`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/300603b06cac5f0217be89ccc48499399e9108ad)
  Форма знака изменена: одна горизонтальная надпись, не перекрывающая ни текст
  задания, ни варианты. На экране вопроса она встраивается в просвет между
  заданием и вариантами; на обзоре и итогах такого просвета и якорей ядра нет,
  поэтому там знак становится полосой в начале сцены. Оба размещения ядро
  вычисляет по своим слотам и корню сцены, без обращения к классам шаблона.
  
  Просвет резервируется, а не отыскивается: сцена — фиксированный кадр с
  автоподбором кегля, и при длинном задании имеющийся зазор схлопывается.
  
  Принятое ограничение записано прямо: читаемый знак в одном месте снимается
  кадрированием, поэтому AC-05 требует читаемости на снимке ЭКРАНА и не
  обещает атрибуции произвольного фрагмента.
  
  В плане переписаны контракт (watermark: {text, anchor}), CSS, тесты и
  реализация applyWatermark; эскиз обязан показать оба размещения.

- **docs**(prd-34): план реализации защиты от копирования (2026-08-02) [`0cf7fa1b9ae8d85f1ad7619fa9dac7abbb7ca0e1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0cf7fa1b9ae8d85f1ad7619fa9dac7abbb7ca0e1)
  Одиннадцать задач: эскизы, чистое ядро периметра, применение к DOM,
  контракт рендерера, схема с миграцией 038, редактор, веб-хост, паритет
  со SCORM и отладкой, водяной знак, скрытие при потере фокуса, приёмка.
  
  Ядро разложено на четыре модуля с одной ответственностью каждый:
  spec (чистый построитель), apply (инлайн, слушатели, впрыснутое правило
  печати), watermark, blur-guard. Каждый закрывается юнит-тестом в jsdom;
  экранная часть принимается только в реальном браузере на обоих хостах.
  
  Отдельным разделом перечислены места, которые всплывут при реализации:
  позиционирование слоёв только инлайном, плоская склейка рантайма пакета
  и печать внутри Shadow DOM.

- **docs**(media): план — срез пунктуации и отброс traversal-ключей в обходчике (2026-08-02) [`861a2c9456af74496d4f5d28cce74321e56b7ee9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/861a2c9456af74496d4f5d28cce74321e56b7ee9)

- **docs**(media): план — обходчик ищет адреса внутри разметки, оговорка к канонизации (2026-08-02) [`98c4bb8c38b98364d8a24cf1f037900d404bcbb2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/98c4bb8c38b98364d8a24cf1f037900d404bcbb2)

- **docs**(prd-34): книга Excel вне FR-04, печатное правило впрыскивается ядром (2026-08-02) [`4f558755fa7cd227318a052a33d996662f06f965`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4f558755fa7cd227318a052a33d996662f06f965)
  Проверка по коду перед планом реализации выявила два расхождения.
  
  Книга Excel не несёт НИ ОДНОЙ настройки уровня теста — ни timeLimitMinutes,
  ни maxAttempts, ни showCorrectAnswers, ни переключателей PRD-19. Пункт про
  перенос в книгу опирался на несуществующий образец и убран; общий пробел
  вынесен в возможный отдельный трек по PRD-14. AC-08 переписан под тот же круг.
  
  Снимок публикации хранит строку теста целиком, поэтому новые колонки попадают
  в него сами — работы там нет, и это сказано прямо.
  
  FR-33: печатное правило впрыскивается ядром в корень сцены, а не лежит в
  theme.css или в сборке styles.css — так независимость от загруженного шаблона
  (FR-34) держится конструкцией.

- **docs**(prd-34): водяной знак обязателен на экране итогов теста (2026-08-02) [`6fe4fc56303d95ae3dd273b59dc6e81f25e4b55e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6fe4fc56303d95ae3dd273b59dc6e81f25e4b55e)
  Перечень экранов водяного знака и периметр защиты от копирования разделены:
  знак показывается на вопросе, обзоре, итогах раздела и ОБЯЗАТЕЛЬНО на итогах
  теста (обычный и адаптивный режим), тогда как экран итогов от копирования по
  FR-09 не защищается.
  
  Обоснование: при включённом showCorrectAnswers экран итогов показывает и текст
  задания, и ключ — это самый ценный лист для утечки, и атрибуция остаётся на нём
  единственной мерой.
  
  Построитель периметра возвращает два независимых перечня, выводить один из
  другого при реализации запрещено. PDF-отчёт знака по-прежнему не несёт.

- **docs**(media): план — поиск сирот антисоединением, гонка удаления в задаче 13 (2026-08-02) [`ea8cd88899c935ec70b809d23bc6857cef247b18`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ea8cd88899c935ec70b809d23bc6857cef247b18)

- **docs**(prd-34): требования защиты от копирования согласованы (2026-08-02) [`4d76be23ce46a7172cca6252e50a820a6756cfb5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4d76be23ce46a7172cca6252e50a820a6756cfb5)
  Документ переведён из постановки повода в спецификацию: девять открытых
  вопросов закрыты владельцем продукта, добавлены FR-01..FR-34, критерии
  приёмки AC-01..AC-10 и журнал согласования.
  
  Ключевые решения: периметр — экран вопроса и экран обзора (разбор ответов
  не защищается); три независимые настройки теста (защита ВКЛ, водяной знак
  и скрытие при потере фокуса ВЫКЛ); пять мер без перехвата горячих клавиш;
  предупреждение участнику без какой-либо записи; водяной знак обезличенный.
  
  Устройство: общий построитель периметра в shared/template и применение
  инлайн-свойствами в renderScreenInto — так §4.9 (внешние шаблоны) и §4.6
  (инлайн-стили рантайма) закрываются конструкцией, а не дисциплиной.
  
  Актуализировано по коду: экран обзора печатает текст сам макетом, а не
  слотом; шаблон сейчас один и base.css в нём нет; PDF-отчёт растеризуется
  по своему поддереву и текстов заданий не содержит; скрытие при потере
  фокуса защищает от ухода в поиск, а не от съёмки.

- **docs**(media): план — контракт отказа порта, тесты границ, обработка ошибки потока (2026-08-02) [`61016ba27aac82f5d0591cd4b3a970cd09b6268e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/61016ba27aac82f5d0591cd4b3a970cd09b6268e)

- **docs**(media): импорт прав в плане через алиас @shared (2026-08-02) [`704ff71459b545dd917fcc6ba8e2a40212734778`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/704ff71459b545dd917fcc6ba8e2a40212734778)

- **docs**: импорт наблюдений питает и учёт экспозиции заданий (BR-26), отдельного обмена не заводится (2026-08-02) [`8972cf1b76a89b73c356fc2a08184dea236715bc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8972cf1b76a89b73c356fc2a08184dea236715bc)

- **docs**(prd-35): приёмка пакета реальной методики — состав TEST_DATA проверен (2026-08-02) [`d2f48ce4c2d3b29fa6a5d699d4858645797aa64d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d2f48ce4c2d3b29fa6a5d699d4858645797aa64d)

- **docs**(prd-29): статус «реализован и принят», решение по признаку оценивания (2026-08-02) [`fc05479417d9dbc7a05f3588655322f38f862dff`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fc05479417d9dbc7a05f3588655322f38f862dff)
  Спека всё ещё говорила «реализация не начата», хотя работа закрыта на обоих
  хостах. Статус переписан на фактический, с тремя подтверждениями приёмки:
  домен по вкладам дал 45/25/40 — верхние границы интервалов методолога; третья
  шкала окрашена зеркально двум другим при одной схеме на тест; вход построителя
  контекста в пакете побайтно совпал с веб-хостом.
  
  Добавлен раздел 6.7 с решением по дефекту приёмки: автоматика сводки и вердикта
  опирается на ДВА признака — порог задан И есть что оценивать. Там же записано,
  почему признак взят из возможных баллов, а не из числа оцениваемых вопросов:
  последнее не доезжает никуда и потребовало бы менять формат уже сохранённых
  результатов, у которых поля не появится никогда.
  
  Строка дорожной карты приведена к тому же.

- **docs**(prd-35): приёмка на модельных данных методики в dev-БД (2026-08-02) [`7f442c126ecac949a7c56e0118dd8689389c8324`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7f442c126ecac949a7c56e0118dd8689389c8324)

- **docs**(prd-34): защита текста задания от копирования — повод и граница достижимого (2026-08-01) [`d9d57ea21e645e84a8b96569dc2367e232ca9278`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d9d57ea21e645e84a8b96569dc2367e232ca9278)
  Запрет копирования недостижим как гарантия (клиент обязан получить текст, чтобы
  его нарисовать; пакет запекает банк вместе с ключами), а запрет снимка экрана
  недостижим в браузере вовсе — требование переформулировано до начала
  проектирования, чтобы трек не стартовал с невыполнимого критерия приёмки.
  Строки ROADMAP ушли предыдущим коммитом.

- **docs**(prd-35): BR-24 «Радар компетенций», Этап 29 и статус трека (2026-08-02) [`fe36d381f26aa37e965511559e2ccdef2e5369bd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fe36d381f26aa37e965511559e2ccdef2e5369bd)

- **docs**: требования BR-21…BR-23 и BR-25…BR-27 — психодиагностический охват, SSO, согласие, IRT, экспозиция, QTI (2026-08-02) [`c31d572ffa2786c2a0dd5372fd03a71d4747ee57`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c31d572ffa2786c2a0dd5372fd03a71d4747ee57)

- **docs**: план реализации ядра медиатеки (реестр, индекс, защищённая раздача) (2026-08-02) [`a74236e16603f4acc88b0a87b52f259d52346b49`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a74236e16603f4acc88b0a87b52f259d52346b49)

- **docs**(prd-35): отчёт о приёмке радара; тона уровней в печатных стилях (2026-08-01) [`70d7fed2660d85faf7d1a83982f14c30ba3bf7c9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/70d7fed2660d85faf7d1a83982f14c30ba3bf7c9)

- **docs**: спецификация медиатеки — реестр, защищённая раздача и переиспользование файлов (2026-08-01) [`967f4f5705f99866755c8a8b14a72b0435e515ac`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/967f4f5705f99866755c8a8b14a72b0435e515ac)

- **docs**(prd-29): задача по D-A — признак «есть что оценивать» (2026-08-01) [`588fe81e0c50f61847e1522390cc6a421ed3f7aa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/588fe81e0c50f61847e1522390cc6a421ed3f7aa)
  Измерительный тест несёт порог по умолчанию 70%, и на нём экран итогов выдаёт
  кольцо 0%, «0 из 0 верно» и зелёную плашку «Пройден». Ровно та бессмыслица,
  ради устранения которой писался PRD. Моя ошибка: для автоматики блоков я взял
  признак «задан ли порог», хотя автор-методолог о правиле «без порога» не
  догадается и не должен.
  
  В движке есть scoredQuestions, и комментарий рядом описывает контракт «хосты
  читают его, чтобы скрыть процент и вердикт», но он не доезжает никуда: поля нет
  в attemptResultSchema, сервер его не сохраняет, построитель не принимает.
  Тянуть через схему значило бы менять формат СОХРАНЁННЫХ результатов, а у уже
  пройденных попыток поля не появится никогда — потребовался бы запасной вариант,
  и он был бы ровно тем, что взято сейчас.
  
  possiblePoints уже во входе построителя и уже передаётся обоими хостами. Ноль
  возможных баллов и есть «нечего оценивать»: измерительный вопрос не приносит и
  не может принести баллов. Работает и для старых попыток, без миграции.

- **docs**: пробел по IMS QTI из разбора WebTutor заведён требованием BR-27 (2026-08-01) [`d93a862b6d87afca3e694677428704462c9ed6f5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d93a862b6d87afca3e694677428704462c9ed6f5)
  Разбор фиксировал обмен заданиями по IMS QTI как единственную строку, где
  встроенный конструктор WebTutor объективно сильнее, и помечал её кандидатом
  в отдельный трек. Кандидат стал требованием — записка теперь ведёт к нему,
  чтобы находка не осталась висеть без адресата.
  
  Границы первой очереди отмечены сразу: обмен заданиями и банком без
  переноса сценария теста, явно объявленный профиль соответствия, приёмка
  на реальных файлах сторонних систем.

- **docs**: конкурентный анализ рынка и мирового эталона, PRD-33 (листинг кода) (2026-08-01) [`99d78a4e10d45a08a453324978b703355e185e7c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/99d78a4e10d45a08a453324978b703355e185e7c)
  Продукт сравнивался с конкурентами только на слух, поэтому приоритеты
  опирались на догадки. Разбор шести соперников и отраслевого эталона даёт
  основание для очередности работ и вскрыл пробелы, которых не было ни в
  одном треке.
  
  Ключевые находки, влияющие на решения:
  \- автономность SCORM-пакета отличает нас от облачных сервисов, но НЕ от
    iSpring, у которого та же модель и пять форматов выгрузки против одного;
  \- отсекающие критерии закупки (SSO, согласие на ПДн, прокторинг, открытые
    задания) снимают продукт с рассмотрения раньше сравнения по существу;
  \- по сборке теста мы ближе к эталону, чем к рынку: квоты по тегам это
    blueprint, фиксированные варианты — параллельные формы, случайная выдача
    по квотам — упрощённый LOFT;
  \- по доказательности вне эталона полностью: экспозиция не измеряется,
    эквивалентность форм не проверяется, порог назначается волевым решением;
  \- уровневый адаптивный режим не является CAT — термин занят другим.
  
  PRD-33 заводит листинг кода в тексте задания: markdown-подмножество
  shared/text кода не покрывает, а типографический проход подменяет в нём
  кавычки и дефисы.

- **docs**(prd-35): убраны числовые значения с радара — цифры печатает карточка шкалы (2026-08-01) [`e1f12d4a24a49902479915925884f59b5db1f404`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e1f12d4a24a49902479915925884f59b5db1f404)

- **docs**(prd-35): эскиз экрана итогов с радаром компетенций; план уточнён по эскизу (2026-08-01) [`a5b5ac5305fae9f2a1d6e643eb65161aca5b9bea`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a5b5ac5305fae9f2a1d6e643eb65161aca5b9bea)

- **docs**(prd-30): веб-приёмка пройдена, оговорка снята (2026-08-01) [`e631748ee54eae968558322612b05c5fe95d5834`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e631748ee54eae968558322612b05c5fe95d5834)
  Оставалась незакрытой третья часть приёмки: ученик проходит тест в браузере.
  Мешала посторонняя причина — в dev-БД не было колонки attempts.assignment_id
  (миграция 0008), из-за чего падал любой старт попытки независимо от порядка
  выдачи. База догнала миграции, проверка выполнена.
  
  Тема из пяти вопросов (индексы 10/20/30/40 и один без индекса) выдаётся
  одинаково на всех трёх путях: веб-прохождение, экспортированный ZIP в локальном
  плеере и отладочный плеер — 10 -&gt; 20 -&gt; 30 -&gt; 40 -&gt; без индекса. Экран обзора
  раздела перечисляет вопросы в той же последовательности.

- **docs**(prd-35): план реализации радара компетенций (2026-08-01) [`3d3da048e52db1c222e5deab1e9c976e8ec13908`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3d3da048e52db1c222e5deab1e9c976e8ec13908)

- **docs**(prd-29): задача по D-2 с разобранной причиной (2026-08-01) [`391b383c4c685d45dd42a0fb09b00f124d154af3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/391b383c4c685d45dd42a0fb09b00f124d154af3)
  Диагноз доведён до строки. Оболочка ученика — Stack minH=screen, а модификатор
  даёт min-height: 100dvh, то есть МИНИМАЛЬНУЮ высоту: колонка растёт с
  содержимым, main растёт за ней, .tbh-inset-screen со своим flex: 1 1 auto
  растягивается, и сцена не ограничена ничем.
  
  Существующее правило main:has(&gt; .tbh-inset-screen) ограничивает только main, но
  не саму колонку — цепочка рвётся на уровень выше. До PRD-29 это не проявлялось:
  экран итогов был коротким и окно не переполнял.
  
  Правка в одну строку, ценность в браузерной проверке. Главный риск — задеть
  страницы, которым рост нужен: список тестов и история.

- **docs**(prd-29): две задачи по дефектам приёмки (2026-08-01) [`c1e39b8947f3f722f321b03e23e181dc34e1116a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c1e39b8947f3f722f321b03e23e181dc34e1116a)
  Task 17 — блокирующий D-1: у показателя нет контрола видимости. Поле есть в
  модели, мапперах, запекании пакета и книге, а в форме его нет, поэтому значение
  навсегда «скрыт» и автор не может показать профиль ученику. Это прямая цель
  PRD: главный вывод методики оказался единственным, чего нельзя вывести.
  Причина организационная — Task 6 отложила интерфейс на Task 13, а та касалась
  только шкал, и показатель выпал между заданиями.
  
  Task 18 — D-4 и D-3, обе про экран итогов. Вердикт булев, поэтому у теста без
  порога в шапке висит «Не пройден», хотя сводка баллов по тому же признаку уже
  скрыта; третье состояние в проекте есть, строки тем им пользуются. И плашка
  уровня рисуется без условия, оставляя окрашенный прямоугольник без текста при
  пустой метке.
  
  D-2 (прокрутка на вебе) не включён: нужен браузер, чтобы найти место разрыва, и
  корень, судя по коду, в оболочке приложения, а не в PRD-29. D-5 закрыт как не
  дефект — разный набор строк в карточке темы задан флагом намеренно.

- **docs**(prd-29): инцидент при уборке стенда в матрице приёмки (2026-08-01) [`a978e3f3f672d40ca2a2b4722d6f55d919b1c8a3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a978e3f3f672d40ca2a2b4722d6f55d919b1c8a3)

- **docs**(prd-29): матрица приёмки (2026-08-01) [`9de7eaff745ec0d18ff9cd2534f43be435852546`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9de7eaff745ec0d18ff9cd2534f43be435852546)

- **docs**(prd-35): спецификация радара компетенций (этап 1 — завершённая попытка) (2026-08-01) [`b22b26beb99913e1a834e4399756497a7de88ea5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b22b26beb99913e1a834e4399756497a7de88ea5)

- **docs**: актуализация BRD, README и ROADMAP до PRD-32 (2026-08-01) [`00b974739492d13f11367151c80ae949cd63c829`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/00b974739492d13f11367151c80ae949cd63c829)
  Документы отстали от кода настолько, что вводили в заблуждение.
  
  ROADMAP (1.14). Таблица статусов обрывалась на PRD-20, а PRD-25/26/27 не
  упоминались вовсе. Теперь §0 покрывает все треки PRD-1…PRD-32 и объявлен
  нормативным источником статуса: иначе пометки в §0.2 и в шапках самих PRD
  продолжают расходиться между собой (сейчас, например, PRD-24 и PRD-27
  называют себя неначатыми, хотя закоммичены). В §0.2 добавлены повод и
  принятое решение по PRD-25/26/27, сняты устаревшие «код не начат» у
  PRD-29/30/31.
  
  README. Расхождения с кодом: пять ролей вместо шести — нет «Разработчика»,
  за которым закреплён экспорт SCORM, тогда как отладочный прогон остался у
  автора; четыре типа вопросов вместо пяти; таблица переменных окружения
  вместо двухслойной конфигурации config/*.jsonc + секреты в .env; db:push
  как модель деплоя вместо drizzle generate/migrate. Описаны домашняя
  страница, поля контентной страницы, темы шаблона, пороги по вариантам,
  отчёт из шаблона, итоги измерительного теста и индекс вопроса в теме.
  
  BRD (2.8). Заведены BR-15…BR-20: шкальный вопрос, отчёт как часть шаблона,
  итоги измерительного теста, авторский порядок выдачи, интервал между
  попытками и доставка вложений обратной связи. BR-20 оформлен рамкой
  вопросов, а не требованиями, потому что по PRD-32 они не собирались.
  Треки, которые новых бизнес-требований не заводят (PRD-19/22/23/24),
  сведены таблицей расширений — им не нужен свой BR, они уточняют принятые.
  Исправлена фактура §6: questions.scoring_json больше не существует, оценка
  резолвится через test_question_scoring; типы страниц заменены системными
  узлами границ раздела. Добавлены NFR-20…NFR-23, этапы 15-25 и девять
  рисков по новым трекам.

- **docs**(prd-31): интервал между попытками и общая модель допуска (2026-08-01) [`9d5bcd03925720978de56fa04aaf4e408865129b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9d5bcd03925720978de56fa04aaf4e408865129b)
  Назначение объявляется единицей допуска. Барьер A (кулдаун, календарные дни)
  действует между назначениями, новый барьер B (интервал между попытками, часы) —
  внутри назначения; барьеры независимы и применяются в непересекающиеся моменты.
  Решение принимает общий движок, хосты поставляют только факты.
  
  Повод — расхождение хостов: в SCORM гейт решает по записям обучения LMS, то есть
  стоит на границе назначения, а в вебе тот же переключатель проверяется на старте
  попытки, потому что связи попытки с назначением не существует. Тем же страдает
  maxAttempts.
  
  Пробник wt-assignment-boundary-probe.js на портале подтвердил дефект: запись
  становится завершённой сразу после первой попытки, а перезапуск завершённого
  назначения WebTutor разрешает, поэтому при включённом кулдауне многопопыточное
  назначение в SCORM сломано уже сегодня. Опознать текущее назначение по
  идентификатору записи нельзя (отвергнуто пробниками 16.07 и 01.08), поэтому
  вердикт барьера A переносится за Initialize с немедленным Terminate без записей
  cmi.*; NFR-01 PRD-6 переформулирован через результат, а не через порядок вызовов
  (§9.1).
  
  Тем же прогоном снят открытый риск §8 спеки доверенной даты: фрейм курса и
  оболочка плеера одного origin.

- **docs**(roadmap): строка PRD-32 внесена внутрь таблицы (2026-08-01) [`8e0a0e9f2ec2efba3982416e93a73d8ea4bfeef6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8e0a0e9f2ec2efba3982416e93a73d8ea4bfeef6)
  Пустая строка перед записью разрывала таблицу, и markdownlint считал строку
  обычным абзацем длиной 1097 символов. Перенесена вплотную к PRD-31.

- **docs**(prd-32): доставка вложений обратной связи — необходимость зафиксирована (2026-08-01) [`6e0efb122f340359ec0fcd6695e90fd3665518ca`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6e0efb122f340359ec0fcd6695e90fd3665518ca)
  Разбор показал, что дыра глубже, чем я записал вчера. Дело не в незаполненном
  адресе: загрузка вложений не реализована вовсе. Редактор говорит это в
  собственной шапке — список PDF живёт только на клиенте, а при сохранении файл
  выбрасывается, в модель уходит один дескриптор. Файл не сохраняется нигде, ни
  на диске, ни в базе. Автор прикладывает документ, видит его в списке, сохраняет
  тест — и файл исчезает.
  
  Заведён PRD-32 с фиксацией проблемы, таблицей состояния всех звеньев цепочки и
  вопросами, с которых начинать. Требования не собирались, форма решения не
  согласована — по решению владельца в этой сессии не проектируем.
  
  Формулировка в PRD-29 §12.1 исправлена на точную и сослана на новый документ.
  Для PRD-29 не меняется ничего: рендер к пустоте готов, вложение без адреса
  отбрасывается вместо мёртвой ссылки, паритет хостов не нарушен.

- **docs**(prd-29): материалы обратной связи не доезжают до выдачи (2026-08-01) [`b05520ed4c481cb7f29d1a67107daa0337ce1f27`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b05520ed4c481cb7f29d1a67107daa0337ce1f27)
  Вскрыто исполнителем Task 12 и подтверждено: scormHref заполнять некому, а
  сборщик медиа SCORM-пакета про обратную связь не знает вовсе. Автор может
  приложить PDF в редакторе, файл сохранится, но ученик не увидит его нигде.
  
  Причина не в PRD-29 — вложения обратной связи никогда не доезжали до выдачи.
  Наша работа первой пытается их показать, поэтому дыра и проявилась. Рендер к
  ней готов: вложение без адреса отбрасывается, а не рисуется мёртвой ссылкой,
  поэтому подсекция просто не появляется, и паритет хостов не нарушен.
  
  Записано в известные ограничения с указанием, что закрывать надо отдельной
  работой (раздача на вебе, упаковка в ZIP, заполнение scormHref при экспорте), а
  до неё материалы класть ссылкой.

- **docs**(prd-30): эскиз Э0 согласован, термин «Индекс» вместо «номера» (2026-08-01) [`24a196cea156170d9794b68b4a2a5f592709efe4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/24a196cea156170d9794b68b4a2a5f592709efe4)
  Эскиз собран на реальной разметке: строка темы взята из утверждённого
  prd17-test-variants.html, поле «Сложность» и дерево банка — из
  content-bank-explorer.html. Дельта — один класс раскладки на токенах ДС,
  собственных компонентов нет. Три места правки: свитч «Случайный порядок
  вопросов» в теле темы, поле «Индекс в теме» под «Сложностью», колонка
  «Индекс» в дереве банка.
  
  Уточнения модели по итогам согласования:
  \- поле переименовано в «Индекс в теме»: «номер» обещал бы уникальность и
    плотную нумерацию, которых модель не требует (допустимы дубли и NULL)
  \- колонка questions.order_index, а НЕ sort_order: у test_sections.sort_order
    другая семантика — плотный порядок разделов, переписываемый на каждом
    сохранении
  \- контрол темы — свитч, а не селект: у той же семантики уровнем ниже
    («Случайный порядок вариантов») в проекте уже принят свитч; модель осталась
    перечислением, третья позиция заменит контрол без миграции
  \- колонка книги на листе «Структура» — булевы значения serBool (да/нет)
  
  При переносе в approved убраны литеральные px из fallback-значений var()
  (контролёр там строже); собственный отступ под двухстрочный навбар эскиза
  обёрнут в gate-skip. check:wireframes:ds по файлу — 0 нарушений.

- **docs**(prd-29): стили измерения нужны обоим шаблонам (2026-08-01) [`184905db48ff4abbaf622b6083bd5e8a5e6e82c7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/184905db48ff4abbaf622b6083bd5e8a5e6e82c7)
  Task 9 положила макет измерения в обе копии results.html — побайтово, этого
  требует тест паритета. Значит и стили нужны обеим, иначе на шаблоне
  сертификации блоки отрисуются неоформленными.
  
  Побайтового совпадения для стилей НЕ требуется: паритет стережёт только макеты
  и параметры, а у сертификации своя палитра. Переносится структурная часть;
  всё, что берётся из токенов дизайн-системы, подхватится палитрой шаблона само.

- **docs**(prd-29): копия сертификации в списке файлов, в кольце значение (2026-08-01) [`cbee6542f699f47f1f5cc89b6275ba8360b2bded`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cbee6542f699f47f1f5cc89b6275ba8360b2bded)
  Одиннадцатая находка исполнителя. tests/template-layout-parity.test.ts требует
  побайтового совпадения results.html обоих поставляемых шаблонов, исключений для
  него не объявлено, а в списке файлов Task 9 копия сертификации отсутствовала.
  Гейт допускает ровно один вариант: копировать. Объявить исключение значило бы
  сказать, что измерительный тест на шаблоне сертификации не показывает ничего.
  
  Принято и его замечание про кольцо: в центре печатался процент, а percent — это
  доля позиции в домене, не оценка. У шкалы с normalization: none такая подпись
  читается как балл — ровно тот процент, которого методика не даёт и ради отказа
  от которого затевался PRD. Дуга долю кодирует и так, поэтому в центре теперь
  значение.

- **docs**(prd-29): разметка итогов приведена к эскизу и исправленному ядру (2026-08-01) [`7c8e454b9d53b4bc89c195a7f7cca735e3ed2483`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7c8e454b9d53b4bc89c195a7f7cca735e3ed2483)
  Исполнитель Task 9 нашёл восемь расхождений и отказался писать разметку — верно.
  Разбор подтвердил все восемь.
  
  Классы блока рекомендаций я выдумал: tb-recos* не существует ни в шаблоне, ни в
  каталоге DS. Эскиз даёт tb-recs-block / tb-recs-group / tb-recs-group__text и
  кладёт чипы в существующий tb-recs, который и даёт им перенос с зазором. Взяты
  имена и правила эскиза, заголовок блока — «Рекомендации», плашка уровня —
  ou-tag--l, как принималось.
  
  Схема разделителей давала двойную линейку: у блока шкал был и ведущий, и
  хвостовой. Хвостовой снят, у блока показателей появился ведущий, открытый
  признаком сводки — блоку показателей может предшествовать только она.
  
  Вид «значение» печатал «27 из » при отсутствии домена: шапка безусловно выводила
  максимум, которого нет. Слот значения ветвится по виду, и виды без рельса
  печатают готовый valueLabel — он заведён контрактом ровно для них.
  
  Вид «кольцо» не рисовался вовсе: ядро готовило percent и ringDashoffset, а ветки
  в разметке не было, и выбранное автором кольцо молча вырождалось в строку.
  Ветка добавлена.
  
  Признак сводки и подпись рельса поправлены в ядре коммитом a17f661; текст плана
  приведён к ним.

- **docs**(prd-29): своя схема цветов требует закреплённой темы (2026-08-01) [`70106f583051fb6c1f85f70aa9eff89cb665c84a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/70106f583051fb6c1f85f70aa9eff89cb665c84a)
  Исполнитель Task 8b нашёл столкновение с PRD-23. Шаблоны темизированы,
  параметры типа color живут ПО ПАЛИТРАМ, а вид темы тест может оставить auto —
  тогда палитру выбирает браузер. Рампа уровней при этом считается в ядре, и это
  сознательное решение: SCORM-пакет отрисовывается движком, который встроит LMS.
  Следствие: при auto сервер не знает выбранной палитры и не может запечь одну
  пару своих цветов.
  
  Записано решение: при auto действует именованная схема, она от палитры не
  зависит; своя схема применяется только при закреплённой теме. Именованные
  схемы и виды рендера работают всегда. Редактор предупреждает автора вместо
  молчаливого отката на светофорную.
  
  В открытые вопросы добавлен перенос интерполяции в CSS color-mix: он снял бы
  ограничение, но вернул бы зависимость от движка LMS — ровно то, от чего расчёт
  в ядре и уходил.

- **docs**(prd-29): недостающая задача — проброс измерений на веб-хосте (2026-08-01) [`cb6e89a89c85ac59bc9ad530f83b78e2a93005c6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cb6e89a89c85ac59bc9ad530f83b78e2a93005c6)
  Исполнитель Task 8 доложил, что Step 9 неисполним в рамках названного списка
  файлов, и не стал выходить за него. Проверил: buildResultContext зовётся из
  template-render двумя аргументами, третий не передаётся никогда. Значит ядро
  считает, контекст умеет, а на живом вебе экран итогов не меняется ни на байт.
  
  Пробел мой: я считал, что проброс укладывается в один файл. Реальная цепочка
  идёт routes/attempts.ts → template-render → result-context, и ни одно звено не
  несёт ни строк шкал, ни эффективных параметров дизайна (их считает приватная
  readScreenTemplate и наружу не отдаёт), ни настроек варианта «Итоги» (они
  лежат в content_pages, а веб-хост вариант итогов вовсе не разрешает).
  
  Добавлена Task 8b с явным перечнем источников, ловушкой снимка PRD-15 (попытка
  со snapshot_id обязана читать толкования из снимка, иначе правка задним числом
  изменит уже пройденную попытку) и запретом на второе разрешение параметров.
  
  Тем же шагом закрывается пятое наблюдение исполнителя: testFeedback объявлен
  как один текст и уходит в сборщик без нормализации, поэтому ссылки и материалы
  уровня ТЕСТА не попадут в блок никогда — хотя PRD-29 §7.1 называет тест
  равноправным источником.

- **docs**(prd-29): задачи экрана согласованы с мобильной адаптацией и пиктограммами (2026-07-31) [`cb70bc3d4c4133713eb57c66b4b564b05b085b72`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cb70bc3d4c4133713eb57c66b4b564b05b085b72)
  Параллельная работа закоммичена (fa9d14d, dae894a) и меняет две мои задачи.
  
  Task 9: чипы обратной связи получили пиктограммы tb-rec__ico — ссылка у курса
  и материала, календарь у мероприятия. Блок рекомендаций измерительного экрана
  взял разметку иконок ДОСЛОВНО оттуда же, иначе на одном экране оказались бы
  голые чипы рядом с иконованными.
  
  Task 10: мобильная адаптация шаблона построена на контейнерных запросах
  @container tbscene со ступенями 900/700/520, а не на медиазапросах по окну.
  Мой @media (max-width: 640px) заменён на @container tbscene (max-width: 700px)
  — на той же ступени, где остальная сцена уже переходит в один столбец.

- **docs**(prd-29): вложения приводятся к ссылке на границе контекста (2026-07-31) [`26cc00b5221cb0243dc28513f0e2db0dc7438380`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/26cc00b5221cb0243dc28513f0e2db0dc7438380)
  Исполнитель Task 14 нашёл расхождение форм. Редактор хранит канонический
  feedbackContentSchema, где вложение — дескриптор PDF
  {title, fileName, mimeType, scormHref?}, и адрес лежит в scormHref. Мой
  collectRecommendations и разметка ждут RecommendationLink {title, url?},
  поэтому блок «Материалы» отрисовал бы ссылки с пустым адресом.
  
  Хранить значение редактора дословно — правильно, это канонический формат.
  Приводить формы должен адаптер на границе контекста, ровно как это уже
  сделано для курсов и мероприятий тем. В Task 8 добавлен normalizeFeedback:
  курсы и мероприятия проходят как есть, вложение превращается в ссылку по
  scormHref, а вложение без адреса ОТБРАСЫВАЕТСЯ — файл не загружен, открывать
  нечего, и мёртвая ссылка хуже её отсутствия. Добавлен тест.

- **docs**(prd-29): круговой обход толкований — отдельный шаг Task 14 (2026-07-31) [`542ddaea33119d8a9faafbb04439f7a8bb585eb6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/542ddaea33119d8a9faafbb04439f7a8bb585eb6)
  Исполнитель Task 13 предупредил: toConfigJson собирает config_json заново из
  модели редактора, то есть сохранение переписывает конфиг ЦЕЛИКОМ. Пока модель
  не несла text/tone/feedback, терять было нечего — но Task 14 их и добавляет,
  и без доработки ОБЕИХ сторон любое сохранение шкалы стёрло бы толкования.
  Заметно это стало бы только после перезагрузки страницы.
  
  Добавлен явный шаг: запись в bandsToPayload (scales-api.ts), чтение в
  buildScalesFromApi (test-editor.mappers.ts, файла не было в списке задачи) и
  обязательный тест на круговой обход.
  
  Команда коммита переведена на поимённое добавление: git add по каталогу
  sections/ утащил бы в индекс чужой незакоммиченный feedback-preview.tsx — на
  этих же граблях чуть не поскользнулась Task 13.

- **docs**(prd-29): тест подписей пишется против реального SettingControl (2026-07-31) [`db15fdd5e6fe6098446a9e0915d419c0a799d376`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/db15fdd5e6fe6098446a9e0915d419c0a799d376)
  Девятое расхождение плана с кодом, найдено исполнителем. Я написал тест против
  свободной функции renderSettingControl(setting, value, onChange), которой в
  проекте нет и никогда не было. Настоящий контрол — компонент SettingControl с
  одним объектом пропсов, где кроме setting/value/onChange обязательны
  sequenceIds, sequenceTotal и testId для ветки sequence.
  
  Исполнитель правильно отказался выбирать между «переделать боевой код под
  выдуманный тест» и «переписать тест». Верно второе: тест рендерит компонент под
  его собственным именем, недостающие пропсы передаёт заглушками — ветка select
  их не читает. Компонент экспортируется, сигнатура не меняется.
  
  Шаг реализации уточнён до трёх точечных правок: поле optionLabels в тип
  ContentTemplateSetting (его там нет, подписи некуда класть), подстановка
  подписи в ветке select, экспорт компонента.

- **docs**(prd-29): долг после Task 6 — drizzle-миграция и три значения в книге (2026-07-31) [`c85fe2e841d0aaf0dcfdf6551b85e42f03b93726`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c85fe2e841d0aaf0dcfdf6551b85e42f03b93726)
  Реализация вскрыла два пробела плана.
  
  Д-1. migrations/036 документирует изменение, но боевой деплой идёт через
  drizzle-kit generate в каталог drizzle/ (там сейчас до 0005). Без
  сгенерированной 0006 схема на прод не поедет. Генерация за владельцем:
  drizzle-kit подключается к базе, а dev-база — копия боевых данных.
  
  Д-2. Колонка книги Excel осталась парой да/нет. Пока видимость была булевой,
  это было точно; со средней позицией пара становится ловушкой — экспорт с
  последующим импортом схлопывают level в level_and_value, то есть раскрывают
  ученику число там, где методолог его закрыл. Сегодня безвредно (задать level
  негде), но ровно с Task 13 возможность появляется, поэтому перевод колонки на
  три значения включён В Task 13 отдельным шагом, с тестом на круговой обход.
  
  Также подтверждена и принята правка вне списка файлов Task 6: на zod 4
  .partial() не гасит .default(), поэтому предписанный планом configJson с
  умолчанием затирал бы толкование при правке одной метки. Хелпер onlyProvided
  в маршрутах шкал и показателей оставляет только присланные клиентом поля.

- **docs**(prd-29): домен задаётся NumberInput с явным переключателем (2026-07-31) [`afcc40773e6a18f6a0c44f60cb0d12a68aa3faf3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/afcc40773e6a18f6a0c44f60cb0d12a68aa3faf3)
  Седьмая находка сверки. Я поставил Input type="number", тогда как весь
  редактор собран на NumberInput из ui-kit со степпером.
  
  Дальше вскрылась ловушка посерьёзнее. NumberInput.value обязательно число, и
  в проекте «не задано» выражают нулём-сигналом с подсказкой (так сделаны
  maxAttempts и timeLimitMinutes). Для домена шкалы это неверно: ноль —
  ЗАКОННОЕ значение, все три домена референсной методики начинаются с нуля, и
  сигнальный ноль сделал бы «шкала от нуля» неотличимым от «границы не заданы».
  
  Поэтому «не задан» выражается явным переключателем: выключен — домен выводится
  из охвата интервалов, ровно как это делает parseScaleInterpretation; включён —
  появляются два поля, засеянные текущим доменом. Тесты дополнены случаем, где
  ноль остаётся значением, а не признаком отсутствия.

- **docs**(prd-25): назначение без доступных попыток — не назначение (2026-07-31) [`0cd7ccdbbca2a01fcf793cf7cd9c85b1b56572e0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0cd7ccdbbca2a01fcf793cf7cd9c85b1b56572e0)
  Пустое состояние «Мне назначено» у ученика с исчерпанными назначениями читается
  как «Тестов пока не назначено», и это НЕ дефект формулировки: назначение, попытки
  по которому кончились, для учащегося назначением не является. Записано, чтобы
  текст не «починили» обратно отдельной формулировкой.

- **docs**(prd-29): редактор толкований расширяет существующий, а не дублирует (2026-07-31) [`474f99c88a46e162e32350e873055093b21606c4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/474f99c88a46e162e32350e873055093b21606c4)
  Сверка с кодом вскрыла самую дорогую ошибку плана: я велел создать
  interpretation-bands-editor.tsx, тогда как BandsEditor уже существует в
  scales-section.tsx — таблица с колонками min/max/метка/уровень, Input size="s",
  IconButton с корзиной, фабрика emptyBand и стабильный clientKey. Исполнитель
  построил бы второй редактор рядом с настоящим.
  
  Task 14 переписана на расширение существующего: добавляются колонка «Оценка» и
  строка-продолжение с толкованием, потому что абзац в ячейку таблицы не лезет.
  Рекомендации открываются готовым FeedbackEditorModal (текст, курсы,
  мероприятия, вложения с флагами скрытия), а не новым инлайновым блоком. Новый
  компонент остаётся ровно один — редактор перечня исходов, для показателей в
  проекте аналога нет; он зеркалит идиому BandsEditor.
  
  Заодно убран выдуманный класс ou-formrow: настоящий примитив раскладки —
  ou-formgroup с модификаторами --one/--two/--three.

- **docs**(prd-29): формат select в манифесте приведён к фактическому (2026-07-31) [`1a13ba3e0800b56265e5b9c9308c39853dc75f79`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1a13ba3e0800b56265e5b9c9308c39853dc75f79)
  Четвёртая правка того же рода, снова пойманная сверкой с кодом до отправки
  исполнителю. Я объявил options массивом объектов {value,label}, а реальный
  формат — массив СТРОК плюс отдельный словарь optionLabels. Так уже объявлен
  progressMode в этом же манифесте, и так его читает design-section.tsx.
  
  Заодно вскрылась асимметрия: у параметров дизайна optionLabels поддержан, а у
  настроек страницы нет — start-pages-section.tsx подставляет в подпись само
  значение. Автор увидел бы в списке английские коды auto/show/hide. Переводить
  сами значения нельзя, их сравнивает resolveResultsBlocks, поэтому пробел
  закрывается в общем механизме: подписи добавляются в отрисовку настроек, с
  тестом на подписи и на откат к значению при их отсутствии.

- **docs**(prd-29): обход формулы типизирован по реальному дереву разбора (2026-07-31) [`ea594b49087590648ed431d516c38030b44281bf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ea594b49087590648ed431d516c38030b44281bf)
  Третий случай той же ошибки, но пойман до отправки исполнителю. Я писал обход
  по памяти и предполагал узел вида { type: "call", name, args } со списком
  имён функций-исключений. Такого узла в дереве нет.
  
  Проверено на живом парсере: scaleById("ee").raw даёт
  { type: "accessor", fn, arg: "ee", prop: "raw" } — ключ шкалы лежит ПОЛЕМ, а
  не дочерним строковым узлом. Так же устроены var и count. Значит строковым
  узлом оказывается только литерал-значение, то есть ровно код исхода, и отсев
  получается структурным: список исключений не нужен вовсе.
  
  Обход переписан исчерпывающим switch по объединению Ast вместо обхода
  произвольного объекта — новый тип узла теперь даёт ошибку компиляции, а не
  молча пропущенную ветвь. Тесты приведены к фактическому поведению разбора,
  добавлены случаи вложенных ветвей и полностью объявленного перечня.

- **docs**(prd-29): откат вида рендера — цепочка на каждый вид, не общая лестница (2026-07-31) [`7c6a6e866ed0807cec924b99868d65fe38fe5a73`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7c6a6e866ed0807cec924b99868d65fe38fe5a73)
  Агент-исполнитель нашёл противоречие внутри Task 5: тест ждал отката линейки
  к «значению из максимума», а единый список FALLBACK_ORDER отдавал кольцо,
  потому что оно стоит в нём раньше и исполнимо при том же условии.
  
  Верен тест. Кольцо печатает ПРОЦЕНТ, а при normalization: none процент не
  определён — это исходная посылка PRD-29. Как явный выбор автора кольцо
  допустимо, как автоматическая замена линейке — нет: иначе инструмент сам
  придумает число, которого методика не даёт.
  
  Единый линейный список этого не выражает, потому что вырождение не
  одномерно. Введены цепочки на каждый вид: линейный индикатор вырождается в
  другой линейный, кольцо и градиент недостижимы автозаменой. Градиент как
  автозамена тоже запрещён — без интервалов нет ни уровня, ни тона, ни текста,
  и цветовой континуум подразумевал бы оценку, которой автор не давал.
  
  Добавлен тест-страж на то, что кольцо не подставляется автоматически.

- **docs**(prd-29): домен считает движок, а не второй экземпляр той же логики (2026-07-31) [`ac30cfbf3750513b3995fb61a87c1a5cc1ca25d1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ac30cfbf3750513b3995fb61a87c1a5cc1ca25d1)
  Агент-исполнитель нашёл противоречие внутри Task 3: тест ждал зажатую нулём
  нижнюю границу, реализация брала буквальный минимум вкладов. Разбор показал,
  что неверны оба: движок уже считает достижимый диапазон в rawRange, и считает
  иначе, чем мой computeScaleDomain — с зажимом нулём для одноиндексных вопросов,
  суммой вкладов для множественного выбора и поддержкой weighted_avg.
  
  Второй экземпляр этой математики разъехался бы с первым: домен, посчитанный
  не так, как движок нормирует percent, поставил бы маркер линейки не туда, где
  стоит его уровень.
  
  Task 3 переписана: achievableRange выносится из rawRange и экспортируется,
  rawRange сокращается до фильтра по выданным вопросам плюс вызов. Отдельный
  модуль shared/scales/domain.ts не заводится. Тестов стало 10, добавлены случаи
  множественного выбора и weighted_avg; заведён отдельный тест на то, что тесты
  движка остаются зелёными без правок.

- **docs**(prd-29): прокрутка на экране итогов принята как норма (2026-07-31) [`c0060bae31723e5de6f10e7b31da91f561cfa80a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c0060bae31723e5de6f10e7b31da91f561cfa80a)
  Решение владельца 2026-07-31. Замеры эскиза: при 1280x800 тело 650px против
  1058px содержимого — видны профиль, две шкалы целиком и начало третьей; при
  1024x640 профиль и одна шкала. Требования «все шкалы в первом экране» нет:
  при пяти-семи шкалах прокрутка неизбежна в любом окне.
  
  Приёмка проверяет другое: первый экран даёт ориентировку, обрыв приходится на
  середину секции, подвал доступен без прокрутки.

- **docs**(prd-29): отбивка секции 16px, иерархия начертанием (2026-07-31) [`57356180efb825b45407be253b05cf4359e9ffb6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/57356180efb825b45407be253b05cf4359e9ffb6)

- **docs**(prd-29): ужать отбивку секции и развести иерархию весом (2026-07-31) [`26b024dd62097cf4b449ada82a2f449c5c8b9f37`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/26b024dd62097cf4b449ada82a2f449c5c8b9f37)
  Честный кадр в окне 1280x800 показал, что третья шкала уходит под сгиб:
  тело сцены 650px против 1186px содержимого. Четыре секции ou-formsection
  тратят 256px только на отбивку 32px сверху и снизу — почти половину видимой
  области. Отбивка ужата до 16px: показатель и все три шкалы влезают целиком,
  сетка, линии и размеры шрифтов не меняются.
  
  Иерархия заголовка блока и имени сущности разведена НАЧЕРТАНИЕМ, а не
  размером: поднять заголовок значило бы поднять tb-scene__subhead везде,
  включая «Результаты по темам» на контрольном экране, который заморожен §11.

- **docs**(prd-29): сцена эскиза в окне фиксированной высоты (2026-07-31) [`f61aa37f84b947e8533b84890d3a8eec02a6d7c5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f61aa37f84b947e8533b84890d3a8eec02a6d7c5)

- **docs**(prd-29): приёмка каркаса экрана в окне фиксированной высоты (2026-07-31) [`bbf15786bb0f04fb35b4c36eca5acec1f8569ecf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bbf15786bb0f04fb35b4c36eca5acec1f8569ecf)
  Контракт сцены (шапка сверху, подвал внизу, прокручивается только тело) уже
  задан в theme.css, но проверять его на кадре, растянутом по содержимому,
  бессмысленно: такой кадр скрывает, сколько занимает измерительный блок и что
  видно без прокрутки.
  
  Добавлен шаг приёмки на 1280x800 и 1024x640 с записью в матрицу, сколько
  блоков видно без прокрутки; если показатель съедает первый экран целиком —
  размеры пересмотреть.

- **docs**(prd-29): структура блока шкал через ou-formsection, единый уровень заголовков блоков, крупное значение (2026-07-31) [`a8d1a9eea43950465939fe5055a4972caa2d8bdd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a8d1a9eea43950465939fe5055a4972caa2d8bdd)

- **docs**(prd-29): единый уровень заголовков блоков, крупное значение шкалы (2026-07-31) [`3d23d854533812a8203a24b7f5f32847fc831e50`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3d23d854533812a8203a24b7f5f32847fc831e50)
  Имя показателя стояло надзаголовком tb-eyebrow и попадало на то же место и в
  тот же вес, что заголовок блока «По шкалам» — два разных по уровню элемента
  выглядели одинаково значимыми.
  
  Строка показателя приведена к строке шкалы: ou-formsection, слева имя, справа
  баннер. Заголовок блока теперь ВСЕГДА tb-scene__subhead, имя сущности — ВСЕГДА
  левая колонка секции.
  
  Значение шкалы поднято с body-s (12px) до display-s (24px): рядом с названием
  в 20px оно читалось сноской, хотя это и есть та цифра, ради которой человек
  пришёл. Домен «из 45» остаётся body-m и приглушённым.

- **docs**(prd-29): блок шкал структурируется через ou-formsection (2026-07-31) [`f7c5948ed2782f3fbedc4a3673b9433aabd6c156`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f7c5948ed2782f3fbedc4a3673b9433aabd6c156)
  Плоский список слипался: внутренний ритм строки равен межстрочному,
  разделителей нет, всё тянется во всю ширину — конец одной шкалы неотличим
  от начала следующей.
  
  ou-formsection даёт из коробки сетку «подпись 280px плюс содержимое»,
  отбивку 32px и волосяную линию между секциями. Слева название шкалы и
  плашка уровня, справа рельс, границы и толкование. Заодно снимается разнос
  значения по всей ширине сцены: рельс живёт в своей колонке.
  
  Собственная дельта сократилась до трёх правил: выравнивание плашки,
  поправки к ou-slider (снятая мера, значение справа, ручка без перетаскивания)
  и переключение на один столбец ниже 640px.

- **docs**(prd-27): формат шаблонов 1.1.0 — отчёт как часть шаблона (2026-07-31) [`040dccbfd2d652fd2eba7aaeae39d7e406194fc5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/040dccbfd2d652fd2eba7aaeae39d7e406194fc5)
  Спецификация платформы описывала все ученические экраны, кроме отчёта: он
  попал в модель шаблона позже и в документе не значился вовсе. Версия формата
  поднята до 1.1.0 (обратно-совместимое расширение) с записью в истории версий.
  
  Добавлено: §8.4 — виды `report` и `report.adaptive`, объявление варианта,
  картинки как файлы шаблона (§8.4.1.1), путь выбора автора до выдачи по трём
  хостам, среда стилей вне сцены с корневым `tb-report`, деградация на макет
  стандартного шаблона, контракт `pdf-link-btn`; §10.2 — публичный контекст
  `report.*`; §16 — почему выбор варианта хранится отдельно от настроек дизайна;
  §17 — статические проверки варианта и рендер отчёта в smoke-проверке. По месту
  поправлены структура ZIP, пример манифеста, оговорка в §8.2 и §18.
  
  Руководство разработчика приведено к той же версии: раздел про отчёт был
  изолированной вставкой, теперь отчёт проходит через введение, структуру ZIP,
  пример манифеста, таблицу опциональных макетов, валидацию, чек-лист и типичные
  ошибки. Таблица контекста `report.*` была неполной — дополнена фактическим
  контрактом (вердикт, счётчики, геометрия дуги, рекомендации, поля строк тем).
  Заодно исправлена ссылка на несуществующий раздел и убраны ссылки на PRD,
  недоступные внешнему разработчику.
  
  PDF в docs/dist пересобраны.

- **docs**(prd-29): эскиз итогов на примитивах DS (2026-07-31) [`44aafa4d30b94a36053c009f6e5a7bda8f31e2d4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/44aafa4d30b94a36053c009f6e5a7bda8f31e2d4)

- **docs**(prd-29): визуал шкал на примитивах DS вместо самодельных классов (2026-07-31) [`dc07a8cf125dfe5cf6e1967ff4b996e75ddb2849`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dc07a8cf125dfe5cf6e1967ff4b996e75ddb2849)
  Эскиз не согласован: блоки завёрнуты в скруглённые прямоугольники, шкалы не
  бьются с дизайн-системой. Причина в плане, а не только в эскизе — я выдумал
  tb-ruler, хотя в DS уже есть ou-slider ровно с этой анатомией (__header,
  __rail, __fill, __thumb, __marks + __mark-lbl).
  
  \- шкала = плоская строка на сцене, ни одной вложенной карточки;
  \- рельс собирается из ou-slider, зоны — ou-slider__fill с цветом ядра;
  \- показатель = ou-banner--subtle, варианты success/info/warning/error
    ложатся на четыре тона один в один;
  \- под рельсом только ЧИСЛОВЫЕ границы, название уровня словом стоит в ou-tag —
    это снимает тесноту подписей и заодно узкий экран;
  \- контекст получает valueText/maxText (для ou-slider__val со &lt;strong&gt;), marks[],
    toneClass и bannerVariant: разметка биндит готовое и ничего не вычисляет.
  
  Отвергнут вариант со сводным BarChart: у шкал разные домены (45/25/40), общая
  ось врёт, а нормировать в проценты нельзя при normalization: none.

- **docs**(prd-29): тон уровня выводится по рампе, а не отдельной лестницей (2026-07-31) [`4b1c34a75163494d2e2b82d4eb0308c8396a3e54`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4b1c34a75163494d2e2b82d4eb0308c8396a3e54)
  Сверка эскиза с кодом плана: середина рампы окрашена жёлтым, а toneOf
  отдавал на ней neutral — плашка уровня получилась бы синей рядом с жёлтой
  зоной. Порог смещён так, чтобы тон совпадал с цветом своей зоны; neutral
  остаётся только за valence: none. Добавлены два теста.

- **docs**(prd-29): эскиз экрана итогов измерительного теста (2026-07-31) [`90de28dc8008126e327ee438fd91f0be1bacfc05`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/90de28dc8008126e327ee438fd91f0be1bacfc05)

- **docs**(prd-30): спека «Порядок выдачи вопросов в теме» (2026-07-31) [`d131c4d7e293596b07421de9a1facac14041940b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d131c4d7e293596b07421de9a1facac14041940b)
  Повод: порядок вопросов внутри темы всегда случайный и не отключается —
  перемешивание зашито в drawSection (shared/draw/blueprint.ts) и в selectForm
  (shared/draw/forms.ts, где ломается даже авторский список варианта PRD-17).
  Авторского порядка в банке нет физически: у questions нет колонки порядка,
  а getQuestionsByTopic читает без ORDER BY.
  
  Согласованная форма: nullable questions.sort_order («Порядок в теме») плюс
  test_sections.question_order = random/fixed (перечисление, а не булево, —
  под будущее «по возрастанию сложности»). Сортировка по возрастанию, NULL
  в конец, равные значения перемешиваются внутри группы.
  
  Ключевые развилки:
  \- поле влияет ТОЛЬКО на последовательность; отбор и квоты PRD-11 не трогаются
  \- в режиме вариантов порядок берётся из списка варианта, sort_order не применяется
  \- sort_order НЕ входит в content_hash, иначе перестановка ложно протухала бы
    pinned_content_hash (PRD-15 блок D)
  \- в адаптиве сортировка внутри уровня, в linear_flat настройка не действует
  
  15 требований FR-01..FR-15, план тестов, этапы Э0-Э7 (эскизы до кода).
  Вынесено: перетаскивание с перенумерацией, разный порядок в разных тестах.

- **docs**(prd-29): план реализации итогов измерительного теста (2026-07-30) [`b105a46c4017bab8acc172e5054d614af2a03391`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b105a46c4017bab8acc172e5054d614af2a03391)
  16 задач в девяти этапах: эскиз, чистое ядро в shared, хранение и миграция,
  сбор рекомендаций, контекст итогов с разрешением блоков, разметка и стили,
  параметры варианта дизайна, SCORM, редакторы, приёмка.
  
  Ядро покрыто TDD-шагами с полным кодом: модель толкования, расчёт домена по
  вкладам, рампа цветов уровней, сборка карточки с откатом вида, сбор
  рекомендаций, разрешение auto/show/hide, сверка литералов формулы с перечнем
  исходов.

- **docs**(prd-29): требования к итогам измерительного теста (2026-07-30) [`81df38e0ac42e46ec8a1c4f06aa4ed0333f803b1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/81df38e0ac42e46ec8a1c4f06aa4ed0333f803b1)
  Шкалы (PRD-5) и показатели (PRD-2) считаются, но не выводятся: разметка обоих
  шаблонов к ним не обращается, тогл показа у шкал скрыт константой с пометкой
  «отдельный PRD», у показателей контрола нет. Для измерительной методики экран
  итогов выдаёт бессмысленное «верно 22 из 22».
  
  Согласованная форма решения:
  \- общая модель толкования для шкал и показателей: интервалы для чисел, перечень
    исходов с кодами для строк (формула возвращает код, валидатор сверяет литералы
    формулы с перечнем);
  \- хранимый домен шкалы с предзаполнением по вкладам вопросов;
  \- благоприятное направление отдельным полем от существующего direction;
  \- видимость три позиции вместо булева флага;
  \- визуал целиком в параметрах варианта дизайна: цветовая схема уровней по
    образцу условного форматирования Excel, вид шкал и показателей с откатом по
    выполнимости; в данных цветов нет;
  \- окраска зон дискретная, нюанс внутри зоны маркером;
  \- один макет results с настройками auto/show/hide на блок;
  \- рекомендации из трёх источников в единый блок, структурированный по типу
    ресурса.
  
  Итоги раздела не меняются. Вынесено в отдельные спеки: PDF-отчёт, таблица
  решений по уровням с хранимым источником формулы, удобства редактора DSL,
  перенос новых полей в книгу Excel.

- **docs**(changelog): 2.10.0-beta (2026-07-30) [`f829000a6a7e8630a436b7c2d8cf7a5394948888`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f829000a6a7e8630a436b7c2d8cf7a5394948888)

## [2.10.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.9.0-beta...v2.10.0-beta)

### Features

- **feat**(prd-27): выгрузка отчёта, снапшоты, проверка шаблона (2026-07-30) [`27413cf53cf44a16b10fe2504aae6577793aa086`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/27413cf53cf44a16b10fe2504aae6577793aa086)
  Фаза 5, FR-22/24/26/27.
  
  Выгрузка. Выбор варианта разрешается ОДНОЙ функцией `resolveReportBake`, которой
  пользуются все хосты: иначе отчёт в LMS расходился бы с предпросмотром. Сборщик
  запекает результат в `TEST_DATA.designSettings.report` и вкладывает CSS выбранного
  варианта в `styles.css` — к моменту растеризации читать файлы из рантайма поздно.
  Рантайм берёт макет варианта, а не канонический вид; пакеты старой сборки, где
  запекания нет, продолжают работать по прежнему пути.
  
  Деградация. `report`/`report.adaptive` добавлены в список системных экранов,
  падающих на вложенный «Стандартный». Отдельно чинится потеря СТИЛЯ: при деградации
  у несуществующего варианта нет своего `styleFile`, и страница собиралась без
  оформления — теперь и макет, и стиль берутся из «Стандартного».
  
  Снапшоты. Отчёт по попытке со `snapshot_id` собирается вариантом ИЗ ВЫДАЧИ, а не из
  живого черновика: смена вида автором не переписывает задним числом документы по уже
  выданным попыткам.
  
  Проверка работоспособности рендерит по одному варианту каждого объявленного вида
  отчёта наравне с экранами — иначе сломанный макет всплывал бы только у обучающегося,
  который не смог скачать PDF. Данные те же, что у предпросмотра в настройках теста.
  
  Отладчик отдельного пути не потребовал: он играет настоящий пакет, собранный тем же
  сборщиком.
  
  Заодно перегенерирована `tests/it/schema.sql` — колонка `report_settings_json` из
  Фазы 3 не была туда внесена, из-за чего падали 83 теста слоя доступа к данным.

- **feat**(prd-27): предпросмотр отчёта в настройках теста (2026-07-30) [`b97d6a5c907700526614d64ed8c3d5d888ea1827`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b97d6a5c907700526614d64ed8c3d5d888ea1827)
  Фаза 4: окно «Предпросмотр отчёта» из блока обратной связи (FR-17..FR-21).
  Страница рисуется ТЕМ ЖЕ рендерером и ТЕМ ЖЕ макетом, что уйдёт обучающемуся,
  на структуре редактируемого теста: реальные название и разделы, числа и
  вердикты демонстрационные, слушатель обозначен нейтрально. Переключатель
  показывает оба исхода; значения полей берутся из НЕсохранённого черновика.
  Это страница, а не PDF: ни растеризации, ни скачивания.
  
  Демонстрационные числа считаются ОТ ПОРОГА, а не от доли: доля с округлением
  сваливается через порог на малой выдаче, и «пройденный» отчёт получал красную
  строку — автор прочёл бы это как дефект макета.
  
  TemplateScreen получил `fill`: цепочка растягивания существует для экрана
  ученика (сцена обязана доставать до низа плеера) и делает это через
  `flex: 1 1 auto; min-height: 0` на корне. Отчёт — документ формата A4 со своей
  высотой, и растянутым он показывал не те пропорции, что уйдут в PDF.
  
  Секция «Настройки» теперь ходит в API за каталогом видов, поэтому её тесты
  обёрнуты провайдером запросов в одном месте, а не поштучно.

- **feat**(template): сцена, подгонка текста и состав пакета (2026-07-30) [`667c02b94cf5f25394c1cce9e332cb318f32778a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/667c02b94cf5f25394c1cce9e332cb318f32778a)
  Рендер шаблона: тема сцены, подгонка шрифта и вопроса под область, таймеры
  сцены; макеты стандартного шаблона и оба предпросмотра приведены к ним.
  Рантайм cooldown-плагина запекается в пакет только при включённом ограничении.

- **feat**(access): роль «разработчик» и отдельное право на отладочный прогон (2026-07-30) [`fbd23f6d9ce39e3b94d27a2ca1714f1b4f83bfa4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fbd23f6d9ce39e3b94d27a2ca1714f1b4f83bfa4)
  Отладчик и выгрузка SCORM перестали быть одним правом: tests.debug.play
  получает автор, tests.export.scorm — только разработчик. Область видимости
  у обоих прежняя (владелец теста или грант), меняется лишь набор ролей.

- **feat**(prd-27): выбор вида отчёта в настройках теста (2026-07-30) [`6af6e5ccfebc7aa7fc9e5de06d4f93d54ae06d68`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6af6e5ccfebc7aa7fc9e5de06d4f93d54ae06d68)
  Фаза 3: блок «Отчёт о результатах» на вкладке «Настройки» — селектор вида,
  поля `settings[]` выбранного варианта и предупреждение о теряемых значениях
  при смене (FR-11..FR-15). Выбор хранится в `tests.report_settings_json`
  по режиму теста; пустой выбор персистится как `null`, а не как `{}`.
  
  Каталог видов считается на ЧЕРНОВОМ `templateId` вкладки «Оформление»
  (§4.2, риск R-5): иначе автор выбирает из списка, которого после сохранения
  не будет. Виды другого режима не предлагаются (D-5). Шаблон без видов
  объясняет деградацию к «Стандартному» (FR-15) вместо пустого селектора.
  
  Ключ запроса каталога — свой (`"record"`), не общий с `use-content-pages`:
  тому нужен голый массив вариантов, а подсказке под селектором ещё и название
  шаблона. Общий ключ на две разные формы означал бы, что содержимое кэша
  определяет тот из хуков, кто сходил первым.

- **feat**(prd-27): оба хоста строят отчёт из макета; хранение выбора (Фазы 2-3) (2026-07-30) [`270affa2c9bb21028be9a9c52812b17c1afabfe9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/270affa2c9bb21028be9a9c52812b17c1afabfe9)
  Конвейер `exportReportPdf` принимает МАКЕТ, CSS и контекст вместо готового
  HTML: рендерит общим `renderScreenInto`, кладёт стили варианта и токены темы
  на контейнер отчёта, растеризует отрисованный корень. Строители HTML удалены —
  `report-html.ts` сжался с 400+ строк до 92 и несёт только входные типы и
  подписи, которые считает ядро.
  
  Пакету CSS не передаётся: `styles/report.css` вкладывается в собранный
  `styles.css` при сборке, потому что читать файл из рантайма к моменту
  растеризации поздно; протечь он не может — скоуплен в `.tb-report`. Веб
  получает макет, CSS, вариант и токены полем `reportRender`. Деградация FR-10:
  в вебе через `resolveTemplateDir` + `default`, в пакете через `systemLayout` —
  те же пути, что у системных экранов.
  
  Хранение (Фаза 3): миграция 0004 добавляет `tests.report_settings_json`,
  схема хранит выбор по РЕЖИМУ теста, значения полей чужого варианта не
  переносятся. Маршрут результата читает выбор автора; при деградации на
  «Стандартный» берётся его `isDefault`, а чужие значения не тащатся.
  
  Приёмка в браузере на обоих хостах: пакет — макет и 56 правил `.tb-report` в
  styles.css, страница отрисована и совпадает с прежней, PDF собирается; веб —
  payload с вариантом `report.standard`, PDF 214 КБ за 3 секунды без ошибок.
  
  Два дефекта поймали тесты, оба мои: подложка терялась (в CSS был комментарий
  «ставит хост», а не ставил никто — теперь её ставит макет из
  `report.backgroundUrl`), и CSS отчёта не попадал в пакет (параметр в сборщик
  добавил, а в `return` он не вошёл — проверка собранного пакета показала ноль
  правил).
  
  Эталон приёмки §6.2: побайтное равенство макета и строителя проверено в
  2ac6b2d, где оба существовали. Строитель удалён, поэтому эталоном служат явные
  ожидания — состав и ПОРЯДОК текста, карточки, вердикты, адреса чипов, гейты.
  Содержательные правила из теста удалённых строителей не потеряны: склонение,
  экранирование, дедуп рекомендаций, отсечение пройденных тем и подложка
  перенесены в тест макета.

- **feat**(access): в записи о погашении ссылки есть адрес и клиент (2026-07-30) [`1c98637542b51a968709acbcdbab367cf5f95c63`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1c98637542b51a968709acbcdbab367cf5f95c63)

- **feat**(access): ссылка-вход не выдаётся аккаунтам с правами выше учащегося (2026-07-30) [`5d0c0ec2c9308eb4343f735b1059e83a2af24c88`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5d0c0ec2c9308eb4343f735b1059e83a2af24c88)
  Этап 3 PLAN_MAGIC_LINK_SCOPE.md (D-3): единая точка решения
  mayReceiveAssignmentLink (server/services/access.ts) на базе
  getEffectiveRoles — выдаёт ссылку-вход только чистому учащемуся; ноль
  ролей тоже трактуется как отказ. Подключена во все четыре места выпуска
  токена (создание назначения — прямое и групповое, /resend,
  /resend-group, /resend-user): токен не создаётся, письмо уходит без
  magicLink на обычный вход (server/email.ts — sendAssignmentEmail с
  опциональным magicLink, без намёка на роли/права). Отзыв уже
  выпущенных токенов при resend сохранён без изменений.

- **feat**(prd-27): страница отчёта макетом шаблона (Фаза 2, часть 1) (2026-07-30) [`2ac6b2d74df86ecfa80d0cd3f0c76dad8b274ef1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2ac6b2d74df86ecfa80d0cd3f0c76dad8b274ef1)
  Вёрстка отчёта перенесена из кода в макеты «Стандартного»: layouts/report.html,
  layouts/report.adaptive.html и styles/report.css, целиком скоупленный в
  `.tb-report`. Перенос, а не редизайн (§6.2): те же цвета значениями, та же
  геометрия дуги радиусом 44 (у кольца экрана — 63), плашка вердикта под
  названием темы, перенос длинной подписи уровня.
  
  DSL ничего не считает, поэтому вычисления ушли в контекст: verdictHeadline,
  verdictBadge, verdictClass, ringDasharray/ringDashoffset, correctLabel,
  earnedPointsLabel, courses/events с дедупом по проваленным темам, а в строки
  тем — verdictLabel, countsLabel, pointsFixedLabel, barPercent, showFeedback,
  achievedClass. Адаптивные материалы перечисляются по КАЖДОЙ теме с её
  названием: понятия «проваленная тема» там нет.
  
  Приёмка §6.2 автоматизирована: отрендеренный макет сравнивается с тем, что
  печатает нынешний строитель — подпись всего видимого текста совпадает символ
  в символ, плюс сверяются состав и порядок карточек тем, вердикты, адреса
  кликабельных чипов, дедуп курса и гейты блоков. Отдельно пиннится контракт
  §6.3: корневой класс tb-report, отсутствие tb-scene, скоупленность CSS.
  
  Стражи поймали два упущения, оба исправлены по существу:
  
  \- страж паритета потребовал те же макеты в «Сертификации» — скопированы
    побайтно и объявлены в её манифесте (своё оформление отчёта для RTK —
    решение отдельного трека её ревизии, здесь не выдумывается);
  \- валидатор манифеста потребовал placeholders у каждого варианта — добавлен
    пустой массив, что для отчёта и означает «содержимого нет».
  
  Два теста пришлось уточнить, потому что они утверждали больше нужного:
  manifest-variant-kind дублировал список видов (заменён на
  variantKindSchema.options, разойтись больше не может), а
  template-layout-parity требовал «ровно один лист стилей», хотя по своему
  заголовку проверяет отсутствие отставленного base.css — сужен до этого.
  
  Остаток Фазы 2: переключение обоих хостов на макет вместо готового HTML,
  доставка CSS, деградация FR-10 и удаление строителей.

- **feat**(access): шапка ученика без переходов, недоступных по ссылке (2026-07-30) [`7b79fc5bd4355d29cdc1bdab807e4142508e839a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7b79fc5bd4355d29cdc1bdab807e4142508e839a)

- **feat**(prd-27): контракт вида отчёта в манифесте и его контекст (Фаза 1) (2026-07-30) [`0a5dbe85574bd7fe3110a8e5ab0822474caa9334`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0a5dbe85574bd7fe3110a8e5ab0822474caa9334)
  Виды `report` и `report.adaptive` объявляются как обычные записи
  contentTemplates[]: `layoutFile` обязателен (общего макета у отчёта нет),
  `styleFile` необязателен, редактируемые поля — те же settings[] из PRD-22.
  Виды РАЗНЫЕ, а не варианты одного: баллы против подтверждённых уровней, и
  вариант одного режима нельзя выбрать для другого.
  
  Валидация (FR-25) проверяет по файлам пакета то, что иначе всплыло бы только
  на живой приёмке: CSS шаблона в пакете лежит в главном документе, а на вебе
  внедряется внутрь Shadow DOM экрана, тогда как отчёт рендерится вне сцены.
  Поэтому макет, опирающийся на классы `tb-scene*` или на селекторы документа,
  работает в LMS и ломается в браузере. Отсюда требования: корневой класс
  `tb-report`, вложенность селекторов styleFile в `.tb-report`, запрет слоя
  сцены и `:root`/`html`/`body`, плюс запрет типа `sequence` и placeholders[]
  и ровно один isDefault на вид. Активационный гейт перепроверяет ОБЪЯВЛЕНИЕ
  без файлов пакета — та же схема, что у проверки тем.
  
  Контекст отчёта (§5) строит `result.*` ТЕМ ЖЕ построителем, что экран
  результатов: отчёт не вправе показать иной вердикт, чем экран, с которого его
  скачали. Блок `report.*` несёт только готовые значения — дату, склонённое
  число попыток, колонки сетки, гейты слушателя и логотипа, значения settings[].
  Дубли вычислений из report-html убраны в общий модуль, чтобы контекст не
  разошёлся с тем, что реально печатается.
  
  Руководство разработчика шаблона описывает вид, поля, среду стилей, контракт
  кликабельных ссылок и деградацию на «Стандартный».

- **feat**(access): ограниченная сессия ходит только по маршрутам своего теста (2026-07-30) [`2f33d662943bf7b209a98088629e114d0a512c28`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2f33d662943bf7b209a98088629e114d0a512c28)

- **feat**(access): клиентский признак выхода за границы ссылки (2026-07-30) [`652482ed7c5d58b8dc0aeff5a7f8ce61526c28c1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/652482ed7c5d58b8dc0aeff5a7f8ce61526c28c1)

- **feat**(access): guard области включён в цепочку до роутеров (2026-07-30) [`842a6f61327096fadd7434a6f06d8cf89c02b29e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/842a6f61327096fadd7434a6f06d8cf89c02b29e)

- **feat**(access): список тестов ученика урезается до теста ссылки (2026-07-30) [`98f1048e200a7dc63b60749bd24195c979e02f67`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/98f1048e200a7dc63b60749bd24195c979e02f67)

- **feat**(access): вход по паролю снимает ограничение области, /me отдаёт magicScope (2026-07-30) [`ea71d3c6c773d5c2d47c1a36479cd9fc3551e45a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ea71d3c6c773d5c2d47c1a36479cd9fc3551e45a)

- **feat**(access): ссылка помечает сессию областью теста и не отдаёт токен наружу (2026-07-30) [`975919ee4a74baee4b26bf916c8e093ed6ae1d44`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/975919ee4a74baee4b26bf916c8e093ed6ae1d44)

- **feat**(access): guard области для сессии, открытой по ссылке (2026-07-30) [`08a5482f8e280824a99a23f52867cab75550caf0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/08a5482f8e280824a99a23f52867cab75550caf0)

- **feat**(learner): строка попытки печатается только при известном лимите (2026-07-30) [`63f48ce7b763e045b9a1752632d2a94c96c8dd57`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/63f48ce7b763e045b9a1752632d2a94c96c8dd57)
  Счётчик показывался и без ограничения попыток — «Попытка 3» без «из
  скольких». Это факт, с которым ученику нечего делать: бюджета нет,
  считать не к чему. Теперь общий билдер отдаёт строку, только когда
  известны ОБА числа — позиция попытки и предел, — иначе пусто, и макет
  печатает шапку без подзаголовка. То же правило уже действовало для
  метки прошлой попытки на стартовом экране, теперь они не расходятся.
  
  Следствие для обложки старта: обе части строки-eyebrow (счётчик и тег
  «Попытки исчерпаны») условные, и на тесте без лимита строка остаётся
  пустой — нулевой высоты, но забирает row-gap шапки и сдвигает заголовок.
  Пустой ряд скрыт правилом в теме; `:empty` тут не работает, после
  раскрытия шаблона в блоке остаются пробельные узлы.

- **feat**(template): общие модули состояния экранов, на которые опираются итоги (2026-07-30) [`cba54cedeb6cff2faeb8f5fb7d62ed88d15c2e88`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cba54cedeb6cff2faeb8f5fb7d62ed88d15c2e88)
  Четыре самодостаточных модуля ревизии, без которых собранное дерево не
  компилируется: результаты и стартовый экран уже импортируют их, а сами
  файлы оставались незакоммиченными.
  
  \- results-nav — состояние подвала экрана результатов (отчёт / повтор /
    замыкающее действие). Раньше пакет ПОДМЕНЯЛ подвал во время исполнения,
    а веб рисовал макет как написано, и один тест предлагал разное;
  \- question-nav — состояние навигационной строки вопроса;
  \- start-image — правило выбора иллюстрации стартового экрана;
  \- section-budget — бюджет раздела.
  
  Логически предшествует трём коммитам отчёта; вынесено отдельно, потому что
  это чужой по происхождению код ревизии, а не часть работы над отчётом.

- **feat**(report): отчёт о результатах общим модулем для веба и пакета (2026-07-30) [`458f46ea1e78a3c7b2f6e882b6d756cacc91000d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/458f46ea1e78a3c7b2f6e882b6d756cacc91000d)
  PDF-отчёт существовал только внутри SCORM-пакета, поэтому у ученика на
  вебе его не было вовсе (canReport: false). Вёрстка страницы отчёта и
  конвейер экспорта вынесены в shared/report: report-html чистый, export-pdf
  получает библиотеки инъекцией. Пакет берёт их через TBTemplate, и
  app/utils/pdfExport стал тонким адаптером (636 -&gt; 145 строк); веб
  импортирует напрямую.
  
  Веб не получает новых npm-зависимостей: jsPDF и html2canvas отдаются из
  тех же вендоренных сборок, что уходят в пакет (/api/report/lib/:file),
  подложки и логотип — /api/report/asset/:file. Одна версия библиотек на оба
  хоста, ~560 КБ грузятся только по нажатию кнопки. Роут под сессией и с
  белым списком имён файлов.
  
  Данные отчёта собирает сервер (buildReportInput): нужны СЫРЫЕ числа по
  темам и ФИО, чего презентационный render-контекст не несёт.
  
  Заодно исправлены два дефекта самой страницы отчёта, унаследованные из
  пакетной версии: плашка вердикта обрезалась и накрывала название темы в
  трёхколоночной сетке (теперь под названием), а разделитель «|» в
  счётчиках висел на переносе.
  
  Приёмка: в браузере на реальной попытке скачивается PDF 205 КБ, страницы
  отчёта сверены в обоих режимах; в SCORM-плеере отчёт по-прежнему
  собирается. Вместе с result.tsx в коммит попала незакоммиченная ревизия
  экрана результатов (showBack / themed).

- **feat**(access): таблица разрешений и матчер пути для ограниченной сессии (2026-07-30) [`292f2d39c4ad5536552a0a980c2d5d1edc338aaa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/292f2d39c4ad5536552a0a980c2d5d1edc338aaa)

- **feat**(prd-22): иллюстрация старта — свойство страницы, а не параметр оформления (2026-07-30) [`3e9b47bc2b607d404ebc25c29fe190a343c1249f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3e9b47bc2b607d404ebc25c29fe190a343c1249f)
  Вариант «Старт: изображение справа» показывал картинку, но задать её на самой
  странице было нечем: форма страницы строится только по манифесту, а вариант не
  объявлял ни одного поля — строка «Старт» в «Структуре» даже не раскрывалась,
  потому что раскрываемость считалась по placeholders и игнорировала settings.
  Картинку задавали параметром оформления, то есть в другой вкладке и на весь
  тест, хотя выбор варианта — свойство страницы.
  
  Теперь вариант объявляет свойство `image`, и картинка живёт там же, где выбран
  вариант. Параметр оформления убран: одна настройка в двух местах и приводила к
  тому, что её искали не там. Сохранившееся значение параметра ещё читается как
  запасное — задать его в редакторе уже нельзя (в базе таких тестов нет, миграция
  не нужна).
  
  Иллюстрацию получает ТОЛЬКО вариант, объявивший свойство. Пока картинку отдавали
  всем вариантам старта, «Старт: стандартный» рисовал ту же колонку, и переключение
  в форме смены варианта ничего видимо не меняло — превью выглядело зависшим.
  
  Правило разрешения одно на все поверхности (`shared/template/start-image.ts`):
  рантайм пакета, веб-хост и оба предпросмотра редактора зовут его же, поэтому
  выбранное автором и увиденное учеником не расходятся.

- **feat**(templates): палитра и дерево экранов в обоих предпросмотрах шаблона (2026-07-30) [`913cb32def6069387189d47727e293721f5b2b33`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/913cb32def6069387189d47727e293721f5b2b33)
  В галерее шаблонов предпросмотр не давал выбрать палитру: сцена рисовалась
  той, что случайно выбрала системная тема, и вторую нельзя было ни увидеть,
  ни прогнать проверкой. Переключатель взят из предпросмотра «Оформления»
  дословно — тот же SegmentedControl, тот же класс, та же позиция над сценой;
  показывается только когда шаблон объявляет две палитры.
  
  Рейл предпросмотра в редакторе теста был плоским списком всех демо-экранов
  (24 строки у стандартного шаблона), хотя в галерее он давно дерево
  Раздел -&gt; Вариант -&gt; демонстрации. Разметка дерева вынесена в общий
  TemplatePreviewRail: у галереи к ней добавляются точки статуса проверки
  (слот), у редактора их нет. Однотипные экраны собираются в ветку; тип с
  единственной демонстрацией остаётся листом.

- **feat**(prd-25): маршрут / рендерит главную, пункт «Главная» в обеих навигациях (2026-07-30) [`d1dfe2a9bed83828ac53c8dec57e45560beaad51`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d1dfe2a9bed83828ac53c8dec57e45560beaad51)

- **feat**(prd-26): шкала в аналитике, инспекторе отладчика и предпросмотре шаблона (2026-07-30) [`ac707756de17f4659e3afb1b9b8f2aac692944de`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ac707756de17f4659e3afb1b9b8f2aac692944de)
  Аналитика форматирует шкалу как одиночный выбор: список градаций, выбранная
  градация текстом, правильная — только если она задана (у измерительной вернётся
  пустая строка, а не «1) …»).
  
  Инспектор отладчика знает подпись типа, читает ответ и вклад в шкалы по индексу
  градации, а оверлей «Эталон» ставит галочку на .ou-stepper__step — у шкалы
  другая разметка строк, чем у карточек, поэтому без своей ветки эталон просто
  не отрисовался бы. У измерительной шкалы оверлей молчит: эталона нет.
  
  inspector-compute.js грузится отдельным скриптом в странице отладчика и до
  TBQType не дотягивается, поэтому признак продублирован в нём локально с
  указателем на источник.
  
  Предпросмотр шаблона: демо-вопрос со шкалой в наборе, маршрут «Шкала» в
  manifest и перегенерированный preview.html — администратор видит новый
  интерактив в проверке работоспособности шаблона.
  
  Примечание к границам коммита: manifest.json уже нёс незакоммиченную работу
  трека тем (themes[] и обнуление цветовых умолчаний); моя правка в нём —
  четыре строки маршрута предпросмотра.

- **feat**(prd-25): раскладка домашней страницы и шаблон main-aside в ui-kit (2026-07-30) [`fc56a7ce29908b217cc8e3a6e1f1fcdb51aae2eb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fc56a7ce29908b217cc8e3a6e1f1fcdb51aae2eb)

- **feat**(prd-25): компоненты секций главной страницы (2026-07-30) [`6abccbde791f90938d0c4f09a9f0f45c2e77ac25`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6abccbde791f90938d0c4f09a9f0f45c2e77ac25)

- **feat**(prd-26): шкала в остальном авторском интерфейсе (2026-07-30) [`03d0cff6d086e13ac408b8424b97eecf823c749b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/03d0cff6d086e13ac408b8424b97eecf823c749b)
  Предпросмотр вопроса, фильтр по типу и дерево содержимого знают шкалу.
  В измерительном режиме предпросмотр не помечает НИ ОДНУ градацию верной —
  зелёная отметка там была бы прямой ложью автору.
  
  Редактор теста: вклады читают градации как единицы одноиндексного ответа,
  демо-ответ шкалы — тот же список, что у одиночного выбора; конструктор
  градуированной оценки предлагает шкале «Веса опций», а не «Ступенчато».
  
  Пять локальных перечислений QuestionType заменены импортом из общего модуля.
  Это сразу окупилось: компилятор нашёл ещё две исчерпывающие карты
  Record&lt;QuestionType, …&gt; в разделе «Шкалы», которые до этого молчали.

- **feat**(prd-20): серверный остаток времени раздела и возврат в раздел (2026-07-30) [`959d2ea64d352799049f5c048c821601ada3ceec`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/959d2ea64d352799049f5c048c821601ada3ceec)
  Остаток времени раздела теперь принадлежит СЕРВЕРУ: клиент лишь пингует
  `POST /api/attempts/:id/section-timer`, а состояние живёт в
  `attempts.section_timer_json` (services/section-timer). Клиентский таймер
  больше не источник истины — перезагрузка, второе окно и правка времени в
  браузере не добавляют минут.
  
  Вместе с этим: позиция возврата по разделам (повторный вход в раздел
  продолжает с того вопроса, где вышли) и разбор бюджета раздела из
  `test_sections.time_limit_minutes` в рантайме пакета.

- **feat**(prd-26): редактор шкального вопроса (2026-07-30) [`acfea87864c89824ec570535b6a6ae1b9a635aab`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/acfea87864c89824ec570535b6a6ae1b9a635aab)
  Тип «Шкала» в списке типов. Редактор градаций — это НЕ копия редактора
  вариантов, а он сам: SingleChoiceBuilder получил два необязательных пропса
  (подпись и плейсхолдер) и признак showCorrect. Иначе два списка подписей
  неизбежно разошлись бы в разметке.
  
  Переключатель «Есть правильный ответ» выключен по умолчанию: опросник без
  верных ответов — это умолчание, а не исключение. Выключен — колонка отметок
  скрыта и сохраняется ПУСТОЙ correctJson; включён — обычная проверяемая шкала.
  Переход НА шкалу переключатель сам не включает, иначе каждый переделанный из
  одиночного выбора вопрос молча стал бы оцениваемым.
  
  Шкала делит состояние с одиночным выбором (dataJson у них идентичен), поэтому
  смена типа single &lt;-&gt; scale сохраняет и подписи, и отметку без отдельной ветки
  переноса. Переключателя перемешивания у шкалы нет — её порядок содержателен.
  
  Правильная градация обязательна только при включённом переключателе.

- **feat**(prd-20): миграция колонки attempts.section_timer_json (2026-07-30) [`fcf0e71d0febbb36a7424884aa678c9f17b9297f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fcf0e71d0febbb36a7424884aa678c9f17b9297f)
  Колонка объявлена в shared/schema.ts (f1d6313), но миграции для неё не
  было: схема на test/прод собирается ТОЛЬКО из drizzle/*.sql, поэтому
  деплой отдавал код, читающий section_timer_json, в БД без этой колонки —
  Drizzle перечисляет колонки явно, и любой запрос к attempts (прохождение
  теста, результаты, аналитика) падал бы с 42703.

- **feat**(prd-26): шкала в листах «Оценка» и «Вклады вопросов» (2026-07-30) [`5d9d4a771b9c9b5f44fcf87548669a2ce89605d4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5d9d4a771b9c9b5f44fcf87548669a2ce89605d4)
  «Вклады вопросов»: ключ источника шкалы — индекс градации, и он проверяется
  по числу градаций. До правки unitCountOfQuestion читал options только у
  одиночного и множественного выбора, поэтому у шкалы выходило 0 единиц и любой
  ключ считался выходящим за пределы — сетка вкладов опросника не импортировалась
  вообще.
  
  «Оценка»: веса по градациям принимаются; цена, выставленная измерительной
  шкале, даёт предупреждение, но значения всё равно сохраняются — они оживают,
  как только автор задаст правильную градацию. Молча хранить мёртвые числа
  нельзя: именно так рождаются обращения «почему не считается».
  
  Признак измерительного режима теперь едет в ResolvedQuestion, поэтому проходы
  книги не перечитывают вопрос ради одного флага.

- **feat**(prd-26): импорт и экспорт шкальных вопросов (2026-07-30) [`cedbf5bc4daabd8eb9f792daa1b128960b73560e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cedbf5bc4daabd8eb9f792daa1b128960b73560e)
  Тип scale принимается листом «Вопросы» и выводится экспортом. Колонка
  «Номера правильных ответов» работает переключателем: пусто — измерительный
  режим (correctJson = {}, не null: колонка NOT NULL), один номер — проверяемая
  шкала, несколько номеров — ошибка строки. Молча брать первый нельзя: это
  запекло бы ошибку автора в банк вопросов.
  
  Экспорт пишет измерительной шкале ПУСТУЮ ячейку — иначе на следующем импорте
  каждое утверждение опросника стало бы проверяемым вопросом. Круговой обход
  экспорт-импорт закреплён тестами для обоих режимов.
  
  Колонка «Следование вариантов ответов» к шкале не применяется: значение
  Random не ошибка, но попадает в предупреждения — автор ждал того, чего не
  произойдёт. Для этого у результата импорта вопросов появился канал warnings,
  проброшенный в книгу рядом с ошибками.

- **feat**(prd-26): измерительный режим в агрегате и вклады шкалы в шкалы PRD-5 (2026-07-30) [`1c28fffd8532cbef5b6b0afc36c042521affb75f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1c28fffd8532cbef5b6b0afc36c042521affb75f)
  Измерительный вопрос (шкала без правильной градации) не участвует в оценке:
  не даёт заработанных и возможных баллов и не входит в число вопросов. Иначе
  опросник из 22 утверждений читался бы как «0 из 22 верно» и обнулял процент
  смешанного теста.
  
  Раздел, в котором нечего оценивать, остаётся НЕ гейтящимся (passed = null),
  а не проваленным на нулевом проценте. Тест целиком из измерительных вопросов
  отдаёт scoredQuestions = 0 — по этому признаку хосты скрывают процент и
  вердикт, — и считается пройденным: промахнуться мимо отсутствующего порога
  нельзя. Иной статус задаётся показателем PRD-2.
  
  Вклад по варианту читается для шкалы как для одиночного выбора в обоих
  движках шкал; диапазон одноиндексного ответа — экстремум, а не сумма.
  Перечисления QuestionType в движках оценки и шкал больше не дублируются,
  а реэкспортируются из shared/questions/question-type.
  
  Централизация типа сразу дала эффект: компилятор нашёл две исчерпывающие
  карты Record&lt;QuestionType, …&gt; в редакторе вариантов, которые до этого молчали.
  
  Веб-хост не показывает измерительному вопросу ни подсветки, ни баннера
  вердикта даже при включённом «показывать правильность ответа».

- **feat**(prd-26): проверка и градуированная оценка шкалы (2026-07-30) [`89e9874c14264bd43aaa22cbfddf844b0fb1a9da`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/89e9874c14264bd43aaa22cbfddf844b0fb1a9da)
  Проверка шкалы тождественна одиночному выбору: ответ — один индекс, значит
  сравнение то же. Ветка exactCorrect теперь ветвится по признаку
  isSingleIndexChoice в обоих движках — в TypeScript-источнике и в ES5-порту.
  
  QuestionType движка оценки больше не дублируется списком, а реэкспортируется
  из shared/questions/question-type: расходиться перечислениям негде.
  
  Парсер листа «Оценка» принимает для шкалы обе формы весов — «веса: …» и ключ
  РТК «%A0B1C2»; «ступени» для шкалы остаются ошибкой, потому что счётчик
  выбранных единиц у одного индекса всегда 0 или 1. Тексты ошибок обновлены.
  
  Режим weighted типонезависим (берёт вес по индексу ответа), поэтому баллы
  0..5 по градациям опросника выгорания работают без правок движка.
  
  Порт-тест и тест перемешивания теперь подгружают qtype.js так же, как это
  делает сборка пакета, и прогоняют шкалу в золотом наборе сценариев.

- **feat**(prd-26): шкала в рантайме SCORM-пакета (2026-07-30) [`5f09315e6a6f1919470f188561ce1641ea436979`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5f09315e6a6f1919470f188561ce1641ea436979)
  Пакет не заводит своего рендера шкалы: render/questions/scale.js —
  тонкая обёртка над общим TBTemplate.renderScale, поэтому разметка в LMS
  побайтово совпадает с вебом. Диспетчер типов маршрутизирует scale к обёртке,
  shuffleMappingFor отказывает шкале (порядок градаций содержателен),
  готовность ответа и выбор идут через признак isSingleIndexChoice,
  клавиатура — через общий nextScaleIndex, а не через свой счёт индексов.
  
  Взаимодействия cmi.interactions: шкала отдаётся типом choice, ответ и ключ
  форматируются как у одиночного выбора (шесть точек ветвления в resultsPage).
  Телеметрия читает подписи градаций через hasOptionList.
  
  Порог вертикальной раскладки по ширине живёт в theme.css шаблона — единственном
  общем источнике оформления сцены, поэтому точка переключения одинакова на обоих
  хостах. Правило по числу градаций применяет renderScale.
  
  Тест паритета tests/scale-host-parity.test.ts пиннит две вещи, способные молча
  разойтись: разметку обёртки против общего рендера и ES5-зеркало признаков типа
  против источника по всем типам сразу.
  
  Примечание к границам коммита: shuffle.js, feedback.js и theme.css на момент
  правки уже несли незакоммиченную работу по другим трекам (в shuffle.js целиком
  не закоммичена сама функция shuffleMappingFor), разделить построчно невозможно.

- **feat**(prd-26): шкала на веб-экране вопроса (2026-07-30) [`f55faaebceb3322a3640514809d55b6f1cd5883d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f55faaebceb3322a3640514809d55b6f1cd5883d)
  Ветка типа в interactionHtml, готовность ответа (индекс 0 — настоящий ответ,
  поэтому проверка по типу значения), проверка ответа как у одиночного выбора
  через isSingleIndexChoice.
  
  deliversShuffledOrder теперь явно отказывает шкале: порядок градаций
  содержателен, и строка с shuffleAnswers=true (наследие импорта или смены
  типа) не должна его ломать.
  
  Клавиатура шкалы вынесена в общий чистый модуль scale-keyboard: стрелки
  двигают и выбирают по обеим осям, Home/End прыгают к полюсам, шаг за край
  не эмитит ответ повторно. Тем же модулем воспользуется рантайм пакета.
  
  Примечание к границам коммита: файлы take-test.tsx, template-screen.tsx,
  answer-gate.ts и два файла тестов на момент правки уже несли незакоммиченную
  работу по другим трекам, разделить её построчно невозможно, поэтому она вошла
  в этот коммит вместе с правками шкалы.

- **feat**(prd-26): общая разметка шкалы renderScale (2026-07-30) [`540eae9c45a845a681fd86105f6210f80c0e5d9a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/540eae9c45a845a681fd86105f6210f80c0e5d9a)
  Шкала порождается тем же модулем, что и остальные типы, поэтому разметка
  не может разойтись между вебом и пакетом. Компонент — существующий Stepper
  дизайн-системы в режиме выбора; собственного компонента шкала не заводит.
  
  Градации до выбранной несут is-done (DS красит коннектор до ответа),
  выбранная — is-current. В проверке правильная градация is-success,
  ошибочно выбранная is-error, корень получает ou-stepper--review.
  Параметра shuffleMapping у функции нет намеренно: порядок градаций
  содержателен и не перемешивается.
  
  Измерительный режим обслуживается тем же вызовом: у review нет correctIndex,
  поэтому классы вердикта просто не появляются.

- **feat**(prd-26): единый источник признаков типа вопроса (2026-07-30) [`edb9c828d7efdc7163eabaf47d64d6b5a059ee92`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/edb9c828d7efdc7163eabaf47d64d6b5a059ee92)
  Ветвление по типу вопроса рассыпано по 25 файлам и не является исчерпывающим:
  это цепочки if (type === '...') с падением в ветку по умолчанию, поэтому
  добавление типа компилируется молча и тихо пропускает логику.
  
  Введены два зеркальных модуля предикатов — shared/questions/question-type.ts
  (источник истины) и app/utils/qtype.js (ES5-зеркало для рантайма пакета,
  подключено в сборку перед всеми частями, ветвящимися по типу). Ветвиться
  следует по признаку: isSingleIndexChoice, hasOptionList, hasFixedOptionOrder,
  isMeasurementOnly.
  
  Ревизия всех 80 точек с разметкой решения по каждой — в заметке
  docs/plans/notes/prd26-type-branch-points.md.

- **feat**(prd-26): тип вопроса scale в схеме и строках интерфейса (2026-07-30) [`f1d6313e0393515c67332f1c597ad3f362a2ec13`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f1d6313e0393515c67332f1c597ad3f362a2ec13)
  Значение scale добавлено в четыре перечисления: questions.type,
  detailedAnswerSchema, questionStatsSchema и scorm_answers.question_type.
  Миграции нет — колонки объявлены как text без CHECK.
  
  Спека поправлена по двум фактам, найденным в коде: question_measurements
  типа вопроса не хранит (только source_type), а correct_json объявлена
  NOT NULL, поэтому измерительный режим кодируется пустым объектом, а не null.
  
  npm run check проходит БЕЗ ошибок — значит ветвление по типу вопроса нигде
  не исчерпывающее и компилятор пропущенные ветки не покажет. Ревизия точек
  ветвления (Task 3) обязательна.

- **feat**(ui-kit): режим выбора и статус success у Stepper (2026-07-30) [`86a20a6eea668746594b182b47859b8a109cb353`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/86a20a6eea668746594b182b47859b8a109cb353)
  Stepper получает пропсы choice и review и статус success. В режиме выбора
  компонент работает как упорядоченная шкала ответа: bullet крупнее и пустой
  (без номера шага и галочки), подписи переносятся и берут размер из
  --tb-answer-fs, у пройденной градации акцентом красится только коннектор,
  а не точка. Модификатор review гасит заливку, когда показан вердикт.
  
  Базовые правила Stepper не тронуты: всё новое живёт под модификаторами.
  Правила добавлены в ОБЕ копии DS-бандла — vendor/ui-kit (источник) и
  client/src/styles/vendor (загружаемая копия).

- **feat**(workbook): выпадающие списки в шаблоне и оформление листа «Пример» (2026-07-29) [`b69ac20deeab671e18ec660c00996f1404f7388a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b69ac20deeab671e18ec660c00996f1404f7388a)
  Перечислимые колонки шаблона получили проверку ввода: значение выбирается
  из списка, а не вписывается руками. Покрыто 18 колонок на семи листах —
  тип вопроса, следование вариантов, режим ОС, тип порога (свой набор на
  «Структуре» и на «Порогах вариантов»), режим квоты, тип/агрегация/
  нормализация/направление/SCORM шкалы, тип и SCORM показателя, управление
  статусом, источник вклада и все «да/нет».
  
  Опечатка в такой ячейке стоила автору цикла «загрузи — прочитай ошибки»,
  а часть значений молча падала в умолчание: нераспознанное «Следование
  вариантов ответов» просто означает «перемешивать», и автор об этом уже не
  узнавал.
  
  Списки не выписаны руками: они выводятся из отображений экспорта (*_TO) и
  из enum'ов схемы через enumValues, поэтому шаблон не может предлагать то,
  что загрузка перестала принимать. Тест проходит каждым предлагаемым
  значением через настоящий разборщик — константы сверять с константами
  смысла нет.
  
  Источник списков — скрытый лист «Значения», а не инлайновая формула:
  инлайновый список обрезан 255 символами и делится ЛОКАЛЬНЫМ разделителем
  (в русской версии Excel это «;»), из-за чего один и тот же файл показал бы
  один пункт со всеми запятыми. Лист добавляется последним, чтобы книга
  по-прежнему открывалась на «Вопросах». Ошибка сделана предупреждением, а
  не запретом: загрузка принимает больше написаний, чем шаблон предлагает,
  и книга из другого редактора должна вставляться без борьбы.
  
  Лист «Пример» перестал быть простынёй: у каждого блока подпись, строка
  заголовков выделена жирным с заливкой и рамкой, данные в рамке с переносом
  по словам. На ролевых листах строка заголовков выделена и закреплена.
  
  Гайд по заполнению дополнен разделом о списках, PDF пересобран.

- **feat**(templates): вариант стартового экрана и изображение старта на обоих плеерах (2026-07-29) [`2fd4481532bc763cd513546b76e655b64c758be8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2fd4481532bc763cd513546b76e655b64c758be8)
  Выбор варианта старта в «Структуре» (например «Изображение справа») ни на что не
  влиял: и SCORM-рантайм, и веб-хост брали generic-макет `start`. Теперь оба
  разрешают макет через `templateKey` стартовой страницы и его contentTemplate,
  откатываясь к `start`, когда вариант не выбран или его макет не поставлен.
  
  \- Манифест стандартного шаблона получил параметр `startImageUrl` — иллюстрацию
    правой колонки; стандартный старт показывает её, когда она задана.
  \- Предпросмотр (модалка страницы и выбор варианта) подставляет `design.*` из
    черновика параметров, поэтому в нём видно выбранное изображение, а не
    заглушку.
  \- Факты «вопросов» и «проходной балл» на стартовых макетах стали условными:
    экран, которому их не дали (адаптивный тест), не рисует пустое место.

- **feat**(workbook): шаблон книги с колоночной справкой и примером, руководство по заполнению (2026-07-29) [`f74021842ae03ad39e1144049efff0826bbd029c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f74021842ae03ad39e1144049efff0826bbd029c)
  Шаблон импорта был восемью пустыми листами и короткой памяткой «какой лист
  зачем» — автор, впервые открывший файл, не знал ни что значит колонка, ни какие
  значения она принимает. Сборка шаблона вынесена в отдельный модуль, который
  кроме ролевых листов даёт:
  
  \- лист «Справка» — строка на каждую (лист, колонка): обязательность, смысл,
    допустимые значения, пример;
  \- лист «Пример» — тот же маленький связный тест, разложенный по блокам и
    готовый к копированию. Не ролевой лист, поэтому импортом не подхватится;
    строки примера прогоняются через настоящий импортёр в тестах, так что то,
    что читатель копирует, заведомо даёт чистый план импорта.
  
  Рядом с «Скачать шаблон» появилось руководство «Как заполнить шаблон импорта»
  в PDF (GET /api/workbook/docs/guide, тот же docs/dist).
  
  Предпросмотр импорта показывает предупреждения импортёра — план мог быть
  безошибочным и всё равно не тем, что автор имел в виду. Первый случай: книга
  несёт оценку и на листе «Оценка», и в колонках «Балл»/«Цена ответа» листа
  «Вопросы»; выигрывает «Оценка», а нетронутый (шапка без строк) лист «Оценка»
  из шаблона ещё и очищает переопределения теста.

- **feat**(workbook): точный разбор книги — «Варианты теста», независимые колонки оценки, ключ «%A2B1» (2026-07-29) [`6f02a1d694946d7f67e8593515541908c95fbb57`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6f02a1d694946d7f67e8593515541908c95fbb57)
  Три находки разбора книги импорта, каждая из которых молча меняла смысл файла.
  
  \- Колонка принадлежности вариантам (PRD-17) называлась просто «Варианты» и
    сталкивалась с ответами: на том же листе есть «Тексты вариантов ответа», а
    «Варианты» — древнее имя самой колонки ответов. Теперь канон — «Варианты
    теста»; старое имя читается как принадлежность только рядом с канонической
    колонкой ответов, а на древнем листе без неё по-прежнему означает ответы.
  \- «Балл», «Цена ответа» и «Сложность» — независимые звенья цепочки: неразобранная
    градуированная цена больше не отменяет всю строку и не утаскивает за собой
    валидные балл и сложность (иначе вопрос молча проваливался к системному
    умолчанию 1 балл / точное совпадение).
  \- Цена ответа принимает буквенный ключ «%A2B1C1D0» — форму, в которой написаны
    исходные банки сертификации. Только латиница и только по порядку A, B, C…:
    кириллические А/В/С неотличимы на вид, но стоят в другом порядке алфавита.
    Экспорт по-прежнему пишет каноническое «веса:».
  
  Плюс лист «Справка»: цена ответа задаётся в книге теста, а не у вопроса —
  сказано прямо, с примерами на все типы вопросов.

- **feat**(templates): скачивание руководства и спецификации PDF из раздела «Шаблоны» (2026-07-29) [`e6fe6b0bf0ae3c8f4292762eb90a4dc0ab765a0f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e6fe6b0bf0ae3c8f4292762eb90a4dc0ab765a0f)
  Разработчик шаблона получает оба документа там же, где загружает шаблон, а не
  ссылкой из переписки. GET /api/admin/templates/docs/:doc отдаёт заранее
  собранный PDF из docs/dist; путь ищется от CWD, затем от модуля — запуск из
  другого рабочего каталога не должен ронять выдачу. Файла нет — 404 с подсказкой
  `npm run docs:pdf`, а не пятисотка.
  
  Роут зарегистрирован до `/:id`, иначе `docs` прочиталось бы как id шаблона.

- **feat**(prd-6): управляемые часы сервера в локальном SCORM-плеере (2026-07-29) [`c5296e6031d300aa6b3252b6bab3ed96aafff1bf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c5296e6031d300aa6b3252b6bab3ed96aafff1bf)

- **feat**(prd-6): «сегодня» в SCORM-гейте берётся из часов сервера LMS (2026-07-29) [`6f922ee815170a4acb02b3bcd64483a02732fec0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6f922ee815170a4acb02b3bcd64483a02732fec0)

- **feat**(prd-6): один мемоизированный запрос портала в retake-гейте (2026-07-29) [`d562fc3b29cdb04e2b33bb87eb1ef82d8948e8d3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d562fc3b29cdb04e2b33bb87eb1ef82d8948e8d3)

- **feat**(prd-6): обратный отсчёт кулдауна считает сервер, клиент не читает часы (2026-07-29) [`c321c9d93df8233ea20d9059fca9c3ebe6d2bdbe`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c321c9d93df8233ea20d9059fca9c3ebe6d2bdbe)
  Отсчёт «через N дн.» на стартовом экране раньше считался браузером по
  локальным часам (daysUntilIsoDate), из-за чего при переведённых часах
  ученика строка расходилась с серверным решением о доступности повтора.
  Теперь сервер считает daysUntil от effectiveToday (санитарная нормализация
  недоверенных часов) через shared/eligibility/engine.daysUntilDate и
  присылает готовое значение в retakeGate; веб-хост больше не читает
  локальные часы.

- **feat**(prd-6): клэмп недоверенного «сегодня» и общая daysUntilDate (2026-07-29) [`058520516ff03dda38988d3a1997431987516566`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/058520516ff03dda38988d3a1997431987516566)

- **feat**(prd-25): агрегатор и роут GET /api/home (2026-07-29) [`bc15c56d9dbaefc5966060c39b9153a412cf24c8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bc15c56d9dbaefc5966060c39b9153a412cf24c8)

- **feat**(prd-25): правила внимания, быстрые действия и малые секции главной (2026-07-29) [`dfc990f94977eb049ad8ab17756142c22e2730c4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dfc990f94977eb049ad8ab17756142c22e2730c4)

- **feat**(prd-25): секции «Мои тесты» и «Мои темы и вопросы» (2026-07-29) [`76222c8785daa142fe9d82ad32184fb5e4e5a93f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/76222c8785daa142fe9d82ad32184fb5e4e5a93f)

- **feat**(prd-25): секции «Мне назначено» и «Мои результаты» (2026-07-29) [`6ed82c38cc2fb16c63f5ea08530e744f4502a550`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ed82c38cc2fb16c63f5ea08530e744f4502a550)

- **feat**(prd-25): контракт ответа /api/home (2026-07-29) [`3f5735bfc8fab81ad8a6fa5911127f57b5696bbb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3f5735bfc8fab81ad8a6fa5911127f57b5696bbb)

- **feat**(prd-25): обновление topics.updated_at при мутациях темы и её вопросов (2026-07-29) [`02919d98cc4e7038fbefffc7311e5764c7442397`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/02919d98cc4e7038fbefffc7311e5764c7442397)

- **feat**(questions): вставка из документа приезжает markdown-ом (2026-07-29) [`7e3f5da8b9539f561b7ce5d52b143b8867cfe517`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7e3f5da8b9539f561b7ce5d52b143b8867cfe517)
  Автор пишет вопросы в Word и вставляет их в редактор — в буфере приезжает
  HTML. Раньше в поле попадал либо сырой HTML (и ученик видел бы теги
  символами), либо plain-версия буфера, то есть жирный и ссылки терялись молча.
  
  Теперь вставка конвертируется ТЕМ ЖЕ htmlToMarkdown, что и ячейки Excel-
  импорта: вставленный вопрос и импортированный дают одинаковый текст. Обработчик
  висит на тексте вопроса и на всех полях вариантов (выбор, сопоставление,
  ранжирование), курсор возвращается на конец вставленного фрагмента.
  
  Если разметки в буфере нет, обработчик не вмешивается — обычная вставка текста
  работает как работала.
  
  Расчёт вынесен в чистую функцию (тестируется без DOM), в компоненте остался
  только шов чтения буфера. Проверено и на уровне функции, и на живом компоненте
  через fireEvent.paste; тест на компонент падает, если снять onPaste.
  
  Плюс подсказка автору о поддерживаемой разметке — через штатный слот hint
  DS-поля, без новой вёрстки.

- **feat**(deploy): бэкофилл текста страниц как шаг деплоя (2026-07-29) [`07dc57c59ec081697d58bad9ca7b8f7c36a5db12`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/07dc57c59ec081697d58bad9ca7b8f7c36a5db12)
  У полей страниц типографика применяется на записи, поэтому страницы,
  сохранённые до конвейера, остались бы с прямыми кавычками и дефисами до
  случайной пересохранки автором — в одном тесте соседние страницы читались бы
  по-разному.
  
  Шаг оформлен скриптом, а не SQL-миграцией: правило — это код (типографика
  обязана знать, где кончается тег и начинается проза), и переписывать его на
  SQL значило бы завести вторую расходящуюся копию. Скрипт собирается тем же
  esbuild рядом с сервером, потому что продовый образ ставится с --omit=dev и
  tsx в нём нет; деплой запускает его сразу после drizzle-kit migrate.
  
  Свойства: идемпотентный, пишет только изменившиеся строки (повторный деплой
  даёт updated: 0), умеет --dry-run, а падение не валит релиз — шаг
  косметический и повторится на следующем деплое.
  
  Проверено на dev-БД: 64 страницы, 3 к изменению, и все три — неразрывные
  пробелы после висячих слов. Соглашение о таких шагах описано в drizzle/README.

- **feat**(content-pages): каноническая форма и типографика для полей страницы (2026-07-29) [`8bcacba71abb4807183830263e0529c7f8c3ad49`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8bcacba71abb4807183830263e0529c7f8c3ad49)
  Поля страницы — такой же авторский текст, как вопрос, и хранятся так же:
  LF, без краевых пробелов, не более одной пустой строки, плюс типографика.
  Разница только в проходе: text/textarea идут простым, richText/html —
  знающим про теги, чтобы атрибуты и CSS остались нетронутыми. Markdown не
  интерпретируется нигде: в этих полях он не поддерживается.
  
  Правило вынесено в отдельный сервис, потому что его зовёт не только запись,
  но и деплойный бэкофилл, а две копии разошлись бы незаметно — отличие тут
  неразрывный пробел, которого в диффе не видно.
  
  Порядок сохранён: сперва XSS-санитайзер, потом этот проход. Чистить то, что
  через строчку удалят, смысла нет.

- **feat**(analytics): в отчётах текст вопроса без маркеров разметки (2026-07-29) [`99a99dd153f25f4d76ac18a1596d32800d7e22fb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/99a99dd153f25f4d76ac18a1596d32800d7e22fb)
  Отчёты и LMS-описания читают люди, а не импортёр: `**жирный**` в ячейке Excel
  или в description интеракции — мусор. Аналитика и SCORM-описания получают
  плоский текст.
  
  Выгрузка банка вопросов НАМЕРЕННО не тронута: там текст обязан уехать ровно
  таким, каким лежит, иначе цикл экспорт-импорт терял бы разметку.

- **feat**(template): markdown и типографика в текстах вопросов на обоих хостах (2026-07-29) [`46a3c8ca2c793787e437a3f2369bbd61ef1c529d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/46a3c8ca2c793787e437a3f2369bbd61ef1c529d)
  Тексты вопросов и вариантов ответа рендерились через esc(): разметку автор
  набрать не мог, кавычки и дефисы доезжали до ученика как есть. Теперь оба
  хоста гонят их через общий конвейер shared/text.
  
  \- варианты ответа всех четырёх типов — одна общая эмиссия, поэтому веб и пакет
    меняются разом и не могут разойтись;
  \- текст вопроса: веб-хост зовёт renderInlineMarkdown, пакет — authorTextHtml
    через TBTemplate с откатом на экранирование (старый пакет без бандла всё
    равно покажет текст, просто без разметки);
  \- предпросмотр в админке идёт тем же путём: автор согласует то, что увидит
    ученик, а не другую отрисовку;
  \- разбор («обзор») получает плоский текст с типографикой: макет биндит
    {{prompt}}, DSL его экранирует, поэтому маркеры там были бы видны;
  \- подбор кегля считает длину ВИДИМОГО текста — маркеры становятся тегами, а
    URL ссылки на экран не попадает вовсе.
  
  Экранирование осталось первым шагом конвейера, до генерации тегов, поэтому
  разметка, набранная автором, по-прежнему не может стать разметкой.
  
  Приёмка: пакет собран и проигран в реальном браузере через CDP — жирный,
  ссылка, ёлочки и неразрывные пробелы на месте, сцена не поехала.

- **feat**(text): проход по разметке — типографика без порчи тегов (2026-07-29) [`c0e7d0efe18db44f9fa3e25eac252e6641b208d1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c0e7d0efe18db44f9fa3e25eac252e6641b208d1)
  Поля страниц с типом richText/html держат HTML, и гнать по нему типографику
  напрямую нельзя: кавычка в class="lead" стала бы ёлочкой и атрибут развалился
  бы, а дефис в margin:0 -1px внутри &lt;style&gt; — длинным тире.
  
  Модуль разбирает значение на теги и текстовые куски и трогает только текст:
  \- кавычки считаются парами СКВОЗЬ теги, поэтому "&lt;strong&gt;x&lt;/strong&gt;" даёт
    «&lt;strong&gt;x&lt;/strong&gt;», а не две непарные метки;
  \- тире и неразрывные пробелы — по каждому текстовому куску;
  \- теги, атрибуты, &lt;style&gt;, &lt;pre&gt; и &lt;code&gt; не трогаются;
  \- markdown не интерпретируется: в этих полях автор пишет HTML, и * — символ.
  
  normalizeAuthorPlain — тот же набор для простых полей страницы: их рендер
  экранирует значение, поэтому типографика обязана быть в хранимом тексте.

- **feat**(text): конвейер текста автора — типографика, markdown, каноническая форма (2026-07-29) [`33ec1962106701063a8173e80a33e6afffc1be8f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/33ec1962106701063a8173e80a33e6afffc1be8f)
  Модуль shared/text: единый источник обработки авторского текста для обоих
  хостов (веб и SCORM-пакет). Порт docs/references/textSanitizer.js без DOM
  и без загрузки словаря по сети.
  
  Фаза 1 — рендер-конвейер:
  \- typography: ёлочки, em-dash, неразрывные пробелы; словарь висячих слов
    встроен константой (149 слов), правило длины 2 вместо 3
  \- markdown: inline (жирный, курсив, ссылки, автоссылки) и блочный (абзацы);
    текст экранируется ДО генерации тегов, ссылки только по whitelist протоколов
    и с target=_blank, чтобы переход не уводил с попытки в LMS
  \- plain: stripMarkdown для машинных путей (выгрузки, хеши, кегль) и
    renderPlainText с типографикой для экранов без разметки
  \- html-to-markdown: конвертация входящей разметки в хранимое подмножество
  
  Фаза 2 — запись:
  \- normalizeAuthorText: каноническая форма (LF, без краевых пробелов, не более
    одной пустой строки); типографика в БД НЕ запекается — применяется на рендере,
    поэтому contentHash меняется только при реальной правке автора
  \- POST/PUT /api/questions: канон для текста вопроса, вариантов и обратной связи;
    поле, отсутствующее в запросе, остаётся undefined и не затирается
  \- импорт Excel: канон + HTML→markdown в ячейках, всё до computeQuestionHash
  \- страницы: канон только для text/textarea; html/richText не трогаем
  
  117 тестов, покрытие 99 процентов по новому коду.

- **feat**(prd-25): колонка topics.updated_at и индекс по ней (2026-07-29) [`93ace18120c1ab0cce4989cf15c12c7d835b6b91`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/93ace18120c1ab0cce4989cf15c12c7d835b6b91)

- **feat**(editor): пикер варианта страницы с живым превью (список + панель) (2026-07-28) [`75f9042af9daf0f50e3a39c882b72a0066a953bb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/75f9042af9daf0f50e3a39c882b72a0066a953bb)
  В редакторе структуры выбор варианта был просто списком. Модалы «Добавить
  страницу» и «Сменить вариант» теперь показывают список слева и живую превью-панель
  справа: выбранный вариант рендерится тем же общим движком, что рантайм и
  «Шаблоны» (TemplateScreen → renderScreenInto) против черновика «Оформления».
  Превью вписывается в фикс-бокс пропорционально (референс-канва 1180×740 +
  равномерный transform:scale, без скролла), поэтому высота модала не «пляшет».
  Спек варианта строится из бандла шаблона (buildScreenInputs по variantKey/route,
  иначе buildContentPageScreen); кривой/частичный бандл деградирует до «нет превью».
  В клиентский тип варианта добавлен pageKind. VariantList вынесен в новый модуль.

- **feat**(templates): варианты старта в предпросмотре — иерархически под «Старт» (2026-07-28) [`b7e0f07de292853d33ec75ec97ae1d4b2249b4d0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b7e0f07de292853d33ec75ec97ae1d4b2249b4d0)
  Добавленный вариант start.image-right показывался в рейле плоским соседом
  (classify давал ему собственный typeKey, а «start» не был в TYPE_GROUPED).
  Теперь classify сводит start и start.* к типу «start», а «start» добавлен в
  TYPE_GROUPED — однотипные варианты старта вкладываются под один узел «Старт»
  (как вопросы/учебные страницы): Старт → [Стандартный | Изображение справа].
  Метки preview-роутов старта переименованы в «Стандартный»/«Изображение справа»,
  чтобы листья читались чисто под родителем «Старт».

- **feat**(scorm): вариант стартового экрана «изображение справа» (2026-07-28) [`5af4f81ad6ab23afc856937264cd552b60eb7597`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5af4f81ad6ab23afc856937264cd552b60eb7597)
  Добавлен layout-вариант старта start.image-right.html: обложка, где иллюстрация
  закреплена в ПРАВОЙ колонке всегда (стандартный старт показывает картинку справа
  лишь при заданном startImageUrl; вариант держит медиа-панель постоянно, с
  плейсхолдером-иконкой при отсутствии изображения). Зарегистрирован в манифесте
  как start.image-right (kind start, isDefault на start.standard) и добавлен в
  preview-рейл; buildOne теперь строит start-контекст и для вариантов старта, так
  что он корректно отрисовывается в предпросмотре. CSS-плейсхолдер .tb-cover__media-ph.
  
  Примечание: рантайм по-прежнему рендерит старт через systemLayout('start') =
  start.html; авто-выбор варианта в прохождении — отдельная доработка (настройка
  теста + редактор), в этот коммит не входит.

- **feat**(scorm): таймер теста/раздела на всех экранах отмеряемого потока (2026-07-28) [`86e15e5fa4e06700aa0967b3b7c06b2cabfda8c2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/86e15e5fa4e06700aa0967b3b7c06b2cabfda8c2)
  Таймер отсчитывался, но был только на экранах вопроса и обзора — на хабе
  маршрутизатора, контент-страницах, введении раздела и итогах раздела его не
  было. Блок таймера (DS ou-timer, скрытый) добавлен в 9 layout потока
  (content + варианты, gallery, section-intro, section-results); start и results
  намеренно без него (до старта / после финиша). Раскрытие+покраска вынесены в
  общий revealSceneTimers(app) и вызываются на каждом экране потока
  (renderContentPage — контент+хаб, section-intro, section-results, вопрос, обзор).
  Присутствие таймеров следует настройкам: тест-таймер — при активном лимите
  (state.remainingSeconds), раздел-таймер — при активном отсчёте раздела
  (state.sectionTimer), поэтому на хабе (вне раздела) виден только тест-таймер.
  Таймер по-прежнему стартует с «Начать тестирование» (initTimer).

- **feat**(scorm,web): обзор — только выданные вопросы, «Назад» к источнику, current-пилл (2026-07-28) [`d9d47424ed8c8b3c094f76ec402505e1ac49837b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d9d47424ed8c8b3c094f76ec402505e1ac49837b)
  Три правки экрана обзора (PRD-19):
  1. В обзор попадают только ВЫДАННЫЕ вопросы (флат-индекс &lt;= текущей позиции или
     уже зафиксированные) — не показываем ещё не выданные. Реализовано флагом
     `delivered` в question-progress/review-context: невыданные исключаются и из
     карты-индикатора, и из списка «Без ответа».
  2. При входе в обзор по кнопке «К обзору»/«Вернуться» (mid-flow) футер даёт
     акцентную «Назад» (возврат к вопросу-источнику) справа, а «Завершить …»
     приглушается до ghost слева — ниже риск случайного завершения. В конце потока
     поведение прежнее: «Завершить» — основное действие.
  3. Индикатор подсвечивает текущий вопрос (is-current), если вход был по кнопке.
  
  Единый билдер review-context + layout review.html → веб и SCORM идентично.

- **feat**(scorm,web): завершённые разделы хаба явно отличаются от незавершённых (2026-07-28) [`b23ea7243265fc0f704acd743a276a36cb68bdb9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b23ea7243265fc0f704acd743a276a36cb68bdb9)
  Завершённая карточка была почти неотличима: в тёмной теме её фон
  (--ou-bg-surface-2) совпадает с фоном свежих карточек (--ou-bg-elevated).
  Теперь завершённая «оседает» — тёмный фон (surface-1), без тени, приглушённый
  заголовок — и несёт маркер в статусе: ✓ для «Завершена»/«Пройдена», ✗ для
  «Не пройдена» (цвет из состояния карточки). Свежие «Не начата» остаются
  светлее, с тенью и accent-кнопкой «начать». Маркер и стили — в общем
  shared/flow/router-hub, работают на обоих хостах.

- **feat**(scorm,web): пошаговая адаптация карты вопросов по ширине (2026-07-28) [`d4df157587670e41fe3c2ec95287cde09b189355`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d4df157587670e41fe3c2ec95287cde09b189355)
  Карта (точки + легенда + счётчики) переставала помещаться на узкой сцене.
  Заголовок сделан size-контейнером (@container, работает и в iframe SCORM, и в
  Shadow DOM веба), карта деградирует по шагам при сужении: (1) легенда уходит под
  индикатор, (2) «пропущено» на свою строку, (3) ячейки индикатора дробятся на
  ряды (счётчик «отвечено» ещё рядом), (4) «отвечено» на свою строку. Счётчик
  разбит на два span (отвечено/пропущено) для раздельного переноса; точкам на
  шаге 3 задан flex:1 1 0, чтобы они обёртывались внутренне, а не выталкивали
  счётчик вниз раньше времени.

- **feat**(scorm,web): доля вопроса 1/3 (ужимается до 1/4 ради опций) (2026-07-28) [`34232cc8da85156927c9eff1c0f6c047a24891ab`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/34232cc8da85156927c9eff1c0f6c047a24891ab)
  Максимальная доля поля под заголовок вопроса — 1/3 (было 1/4): вопрос читается
  крупнее. Если опции не помещаются без скролла, вопрос уступает место, но только
  от 1/3 ДО 1/4 (не ниже) — headerFractionMin. Проверено рендером: длинный вопрос +
  4 опции → вопрос 24px (33% ≈ 1/3), опции 22px, скролла нет.

- **feat**(scorm,web): высотный баланс вопроса и опций без скролла (2026-07-28) [`72604d40e5de33d9feab370134292f3ebb91cfb1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/72604d40e5de33d9feab370134292f3ebb91cfb1)
  Длина текста больше не диктует размер шрифта вопроса: рантайм-пасс (оба хоста,
  после рендера и на ресайз) балансирует площади по ВЫСОТЕ поля:
  1) заголовок вопроса получает до 1/4 поля — крупнейший шрифт, влезающий в четверть
     (не ужимаем, если снизу есть место);
  2) опции получают крупнейший шрифт, при котором все влезают без скролла;
  3) если опции всё равно не помещаются на полу шрифта — заголовок уступает место
     ниже своей четверти (точка баланса).
  Общий fitQuestionScene в shared, вызывается из SCORM-рантайма и веб-хоста
  (template-screen). Проверено рендером: длинный вопрос + 4 опции на узком поле →
  вопрос 20px (24% ≈ 1/4), опции 22px, скролла нет.

- **feat**(scorm,web): матчинг — адаптивное соотношение колонок по длине текста (2026-07-28) [`85eed010a78e59e395f78ef6a2f157e122c84c61`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85eed010a78e59e395f78ef6a2f157e122c84c61)
  Если максимальная длина текстов одной стороны заметно превышает другую, эта
  сторона получает больше места (~66/33) вместо ровных 50/50. «Заметно» = длиннее
  не меньше чем в 1.5x И минимум на 25 символов (пара лишних символов раскладку не
  смещает). Левые тексты длиннее -&gt; 2fr:1fr, правые длиннее -&gt; 1fr:2fr, иначе 1fr:1fr.
  Проверено рендером: длинные описания слева дают 67/33.

- **feat**(scorm,web): поле слайда заполняет 3/4 ширины, адаптивно к экрану (2026-07-28) [`eade3551c26e02c33795c0d26e48d6e2c5a0f6ec`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eade3551c26e02c33795c0d26e48d6e2c5a0f6ec)
  Колонка контента вопроса (.tb-scene__col) была 75%, но с фикс-капом max-width
  960px — на широком стейдже отладчика/веба это ~62% и большая пустая полоса
  справа. Кап снят: поле адаптируется к ширине экрана и заполняет 3/4 (проверено
  1127/1550 = 73% при 1550px). Единый источник → рантайм, превью и отладчик, все
  типы вопросов (одиночный/множественный/сопоставление/ранжирование).

- **feat**(scorm,web): матчинг — коннектор-шеврон, подсветка строки и связи (2026-07-28) [`011ac0789f7e3f74856c29f1f92d4a58cf17ca0e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/011ac0789f7e3f74856c29f1f92d4a58cf17ca0e)
  \- Инструкция по эскизу: «Перетащите карточку справа на нужную строку».
  \- Дроп по всей СТРОКЕ (подсказка + слот = data-drop=r&lt;ri&gt;), поэтому карточка,
    отпущенная на строку, образует пару; правый столбец сам приёмной зоной не
    выглядит.
  \- Подсветка цели при перетаскивании — штатный DS
    (движок теперь ставит is-target на строку), горит ЛЕВАЯ подсказка.
  \- Связанное состояние: в зазоре рисуется индикатор-шеврон «‹» (пунктир→сплошной
    акцент при коннекте, DS ); тонируются ОБЕ карточки строки.
  \- Пустой зазор раньше не давал никакого коннектора — связь не читалась.

- **feat**(scorm,web): светофор по вариантам множественного выбора при разборе (2026-07-28) [`c5fedf8663aa280251a522133f6ce34ba84c36b7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c5fedf8663aa280251a522133f6ce34ba84c36b7)
  Множественный выбор в режиме разбора теперь подсвечивает каждый вариант по
  тому, как ученик им распорядился (не только по ключу):
  \- верный и выбран -&gt; зелёный (галочка);
  \- верный, но пропущен -&gt; жёлтый (галочка) — ошибочно пропущенный;
  \- неверный и выбран -&gt; красный (крестик);
  \- неверный и пропущен -&gt; зелёный без метки — правильно пропущенный.
  Одиночный/ранжирование/сопоставление остаются зелёный/красный. Логика в общем
  reviewClass/reviewMark (оба хоста), цвета — новые классы missed-answer (warning)
  и correct-skip (success) в theme.css.

- **feat**(scorm,web): чуть меньше минимум шрифта вопроса, чуть крупнее максимум опции (2026-07-28) [`31d38fd59161e0781707fe5ce30da6663aeca9b0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/31d38fd59161e0781707fe5ce30da6663aeca9b0)
  Формула fit(): вопрос 20..32 -&gt; 18..32 (длинный вопрос сжимается сильнее),
  вариант ответа 20..14 -&gt; 22..14 (короткая опция читается крупнее).

- **feat**(scorm,web): «Завершить» хаба маршрутизатора в стандартном футере (2026-07-28) [`17829b4776f13928b726674bf1110e8c62f24ec7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/17829b4776f13928b726674bf1110e8c62f24ec7)
  Кнопка «Завершить» переехала из тела хаба в стандартный футер (штатное место
  навигации, справа внизу), на обоих хостах и в превью. Модель:
  \- buildRouterHubHtml больше не рисует встроенную .router-finish;
  \- футер content.html показывает primary-кнопку, неактивную при page.nextDisabled;
  \- готовность (isRouterReadyToFinish) гейтит кнопку: SCORM выставляет disabled
    и onclick=finishRouter императивно, веб — через nextDisabled + onNext.
  Убрано правило, прятавшее футер хаба; на веб-хосте DOMParser round-trip для
  удаления .navigation теперь только при её реальном наличии — иначе он ломал
  mustache-условие внутри тега (переваривал в фиктивные атрибуты).
  Контракт PageContext дополнен полем nextDisabled.

- **feat**(scorm): титульный слайд — отдельная раскладка без header (cover) (2026-07-28) [`d891da72857ebf22a7b7e375fd94f06bd05ec7fd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d891da72857ebf22a7b7e375fd94f06bd05ec7fd)
  Стартовый экран сделан отдельным титульным слайдом, не как прочие экраны:
  \- убран общий header; логотип остаётся в левом верхнем углу (плавающий);
  \- крупный заголовок теста (44px) в основном поле, под ним — описание;
  \- «Попытка N» — тонкий eyebrow над заголовком; факты (вопросы/балл/время)
    скомпонованы под описанием; контент оптически центрирован в поле;
  \- два варианта одной раскладки, переключаются design.startImageUrl:
    без иллюстрации (одна колонка) и с иллюстрацией справа (две колонки,
    картинка object-fit:cover во всю высоту).
  
  Проверено headless-рендером обоих вариантов: нет header, логотип top-left,
  крупный заголовок, описание, факты, сцена заполняет окно, футер снизу.

- **feat**(templates): рейл предпросмотра — 2 группы (система/пользователь), ревизия названий (2026-07-27) [`14ecbac506b93e9862db6480ba808621691a6b2f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/14ecbac506b93e9862db6480ba808621691a6b2f)
  Предпросмотр шаблона в админке: рейл экранов сделан иерархичным и названия
  приведены к сути (по замечанию).
  
  Иерархия (preview-rail.ts): две верхние группы — «Системные экраны» (ядро,
  автор не редактирует) и «Пользовательские страницы» (контент автора). Внутри
  средний уровень — ТИП; варианты одного типа сворачиваются под ним:
  \- «Вопрос» ▸ одиночный/множественный/сопоставление/ранжирование;
  \- «Учебная страница» ▸ текст/медиа-варианты; «Галерея» ▸ слайды.
  
  Ревизия названий:
  \- «Результаты» → «Итоги теста» (в паре с «Итоги раздела»);
  \- «Системная: доступ ограничен» → «Доступ ограничен»;
  \- метки вопросов в манифесте без префикса «Вопрос:» (тип — родитель).
  
  Удалён экран-призрак «Итог: результат» (content.summary): kind summary — легаси,
  его роль забрал вычисляемый section-results («Итоги раздела»), поток его не
  рендерит. Убран из preview.routes И из CONTENT_VARIANT_KINDS (preview-context).
  
  Тесты preview-rail переписаны под новую таксономию (50) + preview-context (82) —
  зелёные. Клиентская иерархия проверена в браузере; манифестные метки/удаление
  маршрута применятся после рестарта dev-сервера (сервер кэширует манифест шаблона).

- **feat**(scorm,web): старт — убран дубль заголовка, плейсхолдер лого, варианты с картинкой (2026-07-27) [`4516a1d00bdaaa544cd3116c813efda6992af066`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4516a1d00bdaaa544cd3116c813efda6992af066)
  По замечаниям к стартовому экрану:
  \- убран дублирующий h2 с названием теста в теле (заголовок остаётся только в
    шапке) — описание теста теперь ведущий блок тела (показывается, если задано);
  \- логотип: слот всегда в левом верхнем углу; при заданном design.logoUrl —
    картинка, иначе — плейсхолдер-иконка (место логотипа видно);
  \- два варианта макета: без картинки (одна колонка) и с иллюстрацией справа
    (две колонки) — переключается наличием design.startImageUrl; поле добавлено в
    CtxDesign и резолвится на обоих хостах (scormDesignContext + template-render,
    тот же разворот media-конверта, что и логотип).
  
  Проверено headless-рендером обоих вариантов: плейсхолдер лого, без h2-дубля,
  описание, plain=1 колонка / media=2 колонки с картинкой справа.

- **feat**(scorm): адаптивное тело вопроса — шрифт по длине, рост области, скролл сцены (2026-07-27) [`c43b009b7cc62fcfc4b30d358206238c8e5f42b5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c43b009b7cc62fcfc4b30d358206238c8e5f42b5)
  Экран вопроса приведён к требованию: шапка сверху, футер снизу (фиксированы),
  между ними — адаптивные поле вопроса и планки ответов.
  
  \- шрифт планок теперь длино-адаптивен: --tb-answer-fs (optionFont 20px→14px)
    применён к .ou-radio-card__title / .ou-rank__text / .ou-match__card-title
    (раньше вычислялся, но нигде не применялся — планки были на дефолте DS);
    заголовок вопроса уже был адаптивен через --tb-question-fs;
  \- .tb-scene__col: height:100% → min-height:100% — колонка заполняет тело при
    коротком контенте (контент сверху) и РАСТЁТ при длинном, вместо зажима;
  \- .tb-qbody: flex:1 → flex:1 0 auto — не сжимается ниже своего контента, поэтому
    карточки растут и тело сцены (overflow:auto) скроллит вместо клиппинга;
  \- удалён мёртвый height-fit fitQuestionText (целил в отсутствующий .question-card).
  
  Модель: шрифт ужимается по длине до минимума; на минимуме область растёт; если
  контент не влезает между шапкой и футером — скроллится тело. Проверено headless
  на 3 длинах: короткий (20px, без скролла), длинный (14px, влезает), переполнение
  (14px, скролл); шапка top / футер bottom фиксированы во всех.

- **feat**(scorm): тело старта по эскизу — крупный h2-заголовок теста над описанием (2026-07-27) [`7ffe46098561300bae2d470a2ee3b749277babe6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7ffe46098561300bae2d470a2ee3b749277babe6)
  Экран старта: в тело добавлен крупный h2 с названием теста над описанием
  (эскиз «Старт»: компактная шапка-бренд + прайм-заголовок в теле). Остальное
  тело уже соответствовало: факты, «Как проходить» (из startPageContent автора),
  баннеры прошлой попытки/кулдауна, футер.

- **feat**(scorm): шапка адаптивных результатов по эскизу — тег «Тест завершён» + подзаголовок (2026-07-27) [`fda344e7db855a14432313bb4ddedfda2d1f18f9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fda344e7db855a14432313bb4ddedfda2d1f18f9)
  Экран adaptive-результатов получает общую шапку: логотип + course.title +
  подзаголовок попытки + тег «Тест завершён». Лид тела заменён с дублирующего
  course.title на фикс-текст эскиза «Адаптивный тест подобрал уровень по каждой
  теме»; hero «Тест завершён» + карточки уровней сохранены. adaptiveRender отдаёт
  course.subtitle (веб — через readResultsRenderPayload). Проверено headless-рендером.

- **feat**(scorm,web): шапка на экранах перехода, блокировки и введения раздела (2026-07-27) [`457691c9b2b078c9f442e703bfe2573673386963`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/457691c9b2b078c9f442e703bfe2573673386963)
  Единая шапка добавлена в оставшиеся системные экраны по эскизам:
  \- переход (адаптив): лого+course.title+подзаголовок + тег «Адаптивный тест»;
    adaptiveRender отдаёт course+design в контекст (buildTransitionContext их не даёт);
  \- блокировка (retake-wall): заголовок+подзаголовок (data-path) + тег «Доступ ограничен»;
    gate.js кладёт course в view (рендер через renderPathOnlyDsl — только data-path,
    без mustache, поэтому логотип на этом экране опущен);
  \- введение раздела: лого+course.title+подзаголовок + тег «Раздел N из M» + прогресс;
    buildSectionIntroContext расширен (courseTitle/subtitle/sectionsTotal → course.title
    теста, eyebrow «Раздел N из M», progressPercent); имя раздела остаётся в h2 тела;
    оба хоста считают позицию раздела; fallback-билдер синхронизирован.
  
  Проверено headless-рендером всех трёх: шапка, тест-заголовок, подзаголовок,
  верные теги, прогресс введения 66%.

- **feat**(scorm,web): подзаголовок попытки в шапке результатов (2026-07-27) [`85440c72ece587fa3eaac42c079f87810cd59f2d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85440c72ece587fa3eaac42c079f87810cd59f2d)
  Экран результатов получает строку «Попытка N из M» под заголовком (единая шапка):
  \- SCORM viewResults.js: ctx.course.subtitle = scormCourseSubtitle();
  \- веб: серверный readResultsRenderPayload принимает готовый subtitle, маршрут
    attempts строит его тем же buildCourseSubtitle из completedAttempts + maxAttempts.
  results.html биндит course.subtitle. Остальная шапка/тело уже соответствовали
  эскизу (тег статуса + прогресс + ring + факты + карточки тем).
  
  Проверено headless-рендером: подзаголовок, тег «Тест пройден», прогресс 86%,
  ring 86%, 3 факта, 3 карточки тем с фидбэком, сцена заполняет окно.

- **feat**(scorm,web): итоги раздела по эскизу — шапка теста + тег «Раздел N из M» + факты (2026-07-27) [`79daf66590f14b6fa5347f6ce380f89712c168ea`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/79daf66590f14b6fa5347f6ce380f89712c168ea)
  Экран «Итоги раздела» приведён к эскизу:
  \- шапка: логотип + course.title ТЕСТА (было topicName) + подзаголовок попытки +
    тег «Раздел N из M» + прогресс по позиции раздела; имя раздела ушло в h2 тела;
  \- тело: eyebrow + h2(раздел) + ring + вердикт + факты (вопросов, верно) —
    «баллов» опущен, данных о баллах в контексте раздела нет;
  \- buildSectionResultContext расширен (courseTitle/subtitle/sectionIndex/total →
    course.title+subtitle, sectionLabel, progressPercent); прогресс через DSL-ширину;
  \- позицию раздела считают оба хоста: SCORM из TEST_DATA.sections, веб из порядка
    тем flatQuestions (паритет).
  
  Проверено headless-рендером: шапка теста, тег «Раздел 1 из 3», прогресс 33%,
  ring 85%, факты 6/«5 / 6», вердикт «Раздел пройден», сцена заполняет окно.

- **feat**(scorm,web): шапка страниц контента по эскизу — тег «Страница N из M» + прогресс (2026-07-27) [`148e4948584e1687fcc32ea09d157dfbde736dc5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/148e4948584e1687fcc32ea09d157dfbde736dc5)
  Страницы контента (content + 6 PRD-22 вариантов content.*/gallery.*) получают
  общую шапку из эскиза «Страница контента»: логотип + course.title + подзаголовок
  попытки + тег «Страница N из M» + полоса прогресса по позиции в последовательности.
  
  \- buildPageContext (общий, оба хоста) отдаёт Core-поля pageLabel + progressPercent
    (layout не умеет арифметику); прогресс задаётся декларативно через DSL-ширину
    style=width:{{page.progressPercent}}% — работает на SCORM и вебе без императива;
  \- brandrow--center: контентная шапка центрирует бренд-ряд (таймеров нет);
  \- SCORM renderContentPage и веб TemplateContentScreen отдают course.subtitle (паритет).
  
  Проверено headless-рендером content.html: шапка, тег, прогресс 50% из контекста,
  контент автора, сцена заполняет окно; 6 вариантов — идентичная шапка, баланс DSL.

- **feat**(scorm): подзаголовок попытки в шапке старта (2026-07-27) [`481b270db94f4f4d13d6f6fd4c7bbfb9ce1ba162`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/481b270db94f4f4d13d6f6fd4c7bbfb9ce1ba162)
  Экран старта получает ту же строку «Попытка N из M» под заголовком, что и
  вопрос/обзор — через buildStartState (общий билдер, оба хоста): предстоящая
  попытка = completedAttempts + 1. Разметка start.html биндит course.subtitle.

- **feat**(scorm): экран обзора ответов по эскизу — единая шапка + ou-stat-row (2026-07-27) [`3e3966813f455ce6757c57f450bfdae658e84c99`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3e3966813f455ce6757c57f450bfdae658e84c99)
  Обзор («Обзор ответов»): шапка приведена к общему паттерну экрана вопроса —
  логотип + course.title + подзаголовок попытки + DS-таймеры тест/раздел +
  компактная карта-ряд со счётчиком (было: h1=scopeLabel, старый .q-timer, без
  подзаголовка). scope раздела ушёл в h2 тела. «Без ответа» — через DS ou-stat-row
  и разделитель ou-separator (было tb-card/tb-unanswered). renderReviewScreen
  отдаёт course.subtitle и ревилит таймеры, как экран вопроса.
  
  Проверено headless-рендером review.html: единая шапка, h2-scope, карта в шапке
  и крупная в теле, 2 ou-stat-row, разделитель, сцена заполняет окно.

- **feat**(scorm): шапка вопроса по эскизу — подзаголовок попытки + DS-таймеры тест/раздел (2026-07-27) [`3e25353257ba898a10f1f6e5d119253153a5c0fb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3e25353257ba898a10f1f6e5d119253153a5c0fb)
  Приведение шапки экрана вопроса к утверждённому wireframe (prohozhdenie):
  \- подзаголовок под заголовком «Попытка N из M» через общий билдер
    buildCourseSubtitle (паритет: SCORM берёт номер у Telemetry, веб — из
    completedAttempts+1); «Обязательный курс» из мока опущен — поля категории
    у теста нет, фабриковать нечестно;
  \- таймеры теста и раздела как DS ou-timer--digital--s (метка+число), скрыты
    по умолчанию, ревилятся рантаймом при активном отсчёте — наличие следует
    настройкам лимитов; секц-таймер получает DS is-critical у порога;
  \- paintTimer теперь красит DS __num и is-critical (без цветового литерала в
    mainRender), с обратной совместимостью для legacy .q-timer.
  
  Проверено headless-рендером question.html: подзаголовок, оба таймера в ряд
  (раздел critical), карта в ряд + счётчик, сцена заполняет окно.

- **feat**(template): единый DS-баннер проверки ответа + метки correct/incorrect (2026-07-26) [`c98dcdea0e7fb9c65437ae3b4aa63ac456081552`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c98dcdea0e7fb9c65437ae3b4aa63ac456081552)
  Проверка ответа на экране вопроса переведена на дизайн-систему. Варианты
  получают завершающую метку (галочка/крест) у правого края карточки — цвет
  наследуется от класса correct-answer/incorrect-answer, литералов нет. Общий
  билдер feedbackBanner собирает DS `.ou-banner` вместо инлайн-hex
  `.feedback-block`, поэтому вердикт единообразен на обоих хостах. Разметка и
  иконки взяты из эскиза `question — проверка`.

- **feat**(scorm): нав-панель вопроса и модалка завершения на DS (ou-btn/ou-modal) (2026-07-26) [`2630032fb8a1afc1c870556b2795aeb642fa13b5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2630032fb8a1afc1c870556b2795aeb642fa13b5)
  Строка навигации вопроса — панель .tb-scene__foot с ou-btn (ghost Назад/Пропустить/
  К обзору, primary Отправить/Далее/Завершить). Модалка завершения (FR-09) переведена
  на ou-modal (warning-иконка, ou-btn), &lt;html&gt; как .ou-провайдер даёт токены. Приёмка
  в плеере: подвал вопроса DS-нативный.

- **feat**(template): динамический размер шрифта вопроса/вариантов (общая fitFont) (2026-07-26) [`f79a84b56265fc19302e323957ecf790b6734f7c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f79a84b56265fc19302e323957ecf790b6734f7c)
  fit-font.ts портирует формулу fit() эталона: вопрос 32-&gt;20, вариант 20-&gt;14 по длине.
  Оба хоста кладут questionFont/optionFont (по длине промпта и самого длинного варианта)
  в state; question.html подставляет --tb-question-fs/--tb-answer-fs инлайном, эмиссия
  наследует. answerTexts извлекает тексты ответов по типу. Экспорт из TBTemplate, гард
  расширен. Приёмка через харнесс: длинный вопрос/вариант ужимаются, зажаты снизу.

- **feat**(template): контент- и галерея-страницы на сцене DS (info.*/gallery.*) (2026-07-26) [`4d995bafd73b2ceeb8c4b09c9928ac747bd78dd9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4d995bafd73b2ceeb8c4b09c9928ac747bd78dd9)
  content.*/gallery.* на сцену: тело ЦЕНТРИРУЕТ блок (читаемая мера 720px для текста,
  сетка «медиа 5/текст 7» с картинкой 4:3 — по замечанию про вытягивание и пустые
  поля); подвал Назад/Далее, у галереи — точки последовательности (page.dots). theme.css:
  tb-content(-text/-image/-image-right), tb-content__media/title/lead/body, tb-dots.
  Приёмка через харнесс: контент сбалансирован, не растянут.

- **feat**(template): адаптивные итоги на сцене DS (results.adaptive) (2026-07-26) [`b6df2d761d701a18f718ad1b9119ab52df529fe2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b6df2d761d701a18f718ad1b9119ab52df529fe2)
  results.adaptive.html на сцену: герой (иконка уровней + «Тест завершён»), сетка тем
  ou-card с тегом уровня (levelClass is-info=accent / is-fail=error) и единым блоком
  обратной связи per-topic (текст + чипы, из 6.1); подвал с PDF/Завершить/Пройти заново.
  theme.css: adaptive-hero, ou-tag.is-info. Тесты на .ou-tag/.tb-topic-card. Приёмка
  через харнесс.

- **feat**(template): экран результатов на сцене DS (Результаты) (2026-07-26) [`6ccc5c48070b12bfcc443e0a2cdb8e9290ccf929`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ccc5c48070b12bfcc443e0a2cdb8e9290ccf929)
  results.html на сцену: шапка (название + вердикт-тег + полоса), полоса счёта
  (кольцо ou-ring + факты), сетка тем ou-card — вердикт-тег, ou-progress по passClass,
  строки ou-stat-row, и ЕДИНЫЙ блок обратной связи per-topic (текст + чипы курсов/
  мероприятий из 6.1); подвал с действием. theme.css: score-strip, topics-grid,
  stat-rows, recs-chips. Тесты на новую разметку. Приёмка через харнесс — финальный
  экран собран (кольцо, темы, фидбэк, рекомендации).

- **feat**(template): экран блокировки повтора на сцене DS (system.blocked) (2026-07-26) [`e54309ff8d9d55c005d1c7e17fa0c198dd229bea`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e54309ff8d9d55c005d1c7e17fa0c198dd229bea)
  system.blocked.html — центрированная стена: предупреждающая иконка + три ветки
  (default/cooldown/error, рантайм показывает одну через [hidden]). theme.css:
  tb-blocked-icon (warning-soft), tb-blocked-branch с [hidden]-переопределением
  (класс flex не должен перебивать hidden). Приёмка через харнесс.

- **feat**(template): итоги раздела на сцене DS (Итоги раздела) (2026-07-26) [`e09e7e870ed16c6ee625f82913dd23d5c5904735`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e09e7e870ed16c6ee625f82913dd23d5c5904735)
  section-results.html — центрированная колонка (эйброу / название раздела / кольцо
  ou-ring / сводка / вердикт-тег) + подвал «Продолжить». Кольцо и тег красятся по
  passClass (is-pass -&gt; success, is-fail -&gt; error) через theme.css. Приёмка через
  харнесс: зачёт (зелёное) и незачёт (красное).

- **feat**(template): экран обзора ответов на сцене DS (Обзор ответов) (2026-07-26) [`cafb2539985a8e12dcd1ecd02d06b2e98c4df6f2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cafb2539985a8e12dcd1ecd02d06b2e98c4df6f2)
  review.html на сцену: шапка без полосы прогресса (flush, отступ снизу — по замечанию),
  тело — подсказка, крупная карта ou-quiz, карточка «без ответа» со строками-переходами;
  подвал с «Завершить». theme.css: header--flush, tb-review-map, tb-unanswered. Приёмка
  через standalone-харнесс.

- **feat**(template): экран введения раздела на сцене DS (intro.standard) (2026-07-26) [`561eea22e68d4f9c46c8ae1f588f69067ddb773f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/561eea22e68d4f9c46c8ae1f588f69067ddb773f)
  section-intro.html — центрированная колонка (эйброу «Раздел N из M» / название /
  описание / точки-мета / карточка инструкции) + подвал с «Начать раздел». theme.css:
  центр-колонка, eyebrow, meta-dots, tb-card, фикс токена дорожки прогресса на
  --ou-bg-surface-3. Приёмка через standalone-харнесс (TBTemplate + styles.css).

- **feat**(template): стартовый экран на сцене DS (Старт.html) (2026-07-26) [`053137734762dfd9c16a11cf87335efe48dfdd21`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/053137734762dfd9c16a11cf87335efe48dfdd21)
  start.html на сцену: шапка (лого/название), тело — лид-описание, полоса фактов
  (вопросы/проходной/время/попытки), инструкция, кулдаун и прошлая попытка через
  ou-banner; подвал-панель с действиями (ghost «К списку» слева, primary справа).
  theme.css получил doc-scene слой (hero/lead/facts/note). Тест обновлён под .ou/сцену,
  машина состояний действий сохранена. Приёмка в плеере.

- **feat**(template): экран вопроса на сцене DS (шапка/тело/панель) (2026-07-26) [`231d7059d840ec252d13d02c5f094de2f8ea9cd2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/231d7059d840ec252d13d02c5f094de2f8ea9cd2)
  question.html переписан на сцену: фикс-шапка (лого/название/таймеры + карта ou-quiz
  с легендой и счётчиком + полоса прогресса), прокручиваемое тело (мета+тег темы,
  заголовок, подсказка-подзаголовок, слоты), подвал host-managed. Пакет делает &lt;html&gt;
  DS-провайдером темы (.ou ou--&lt;theme&gt;), как веб — shadow-host. Подсказка типа вопроса
  унифицирована (questionHint, оба хоста), убран дубль из renderQuestionInput.
  Приёмка в SCORM-плеере: все типы, сцена сбалансирована, панель закреплена.

- **feat**(template): слой сцены на токенах DS (флекс-корень, flex-варианты) (2026-07-26) [`345f52be63b62decc5b3dc3e99da85076912b8df`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/345f52be63b62decc5b3dc3e99da85076912b8df)
  theme.css получил слой сцены: фикс-шапка / прокручиваемое тело / фикс-подвал,
  заполняющие варианты (flex + min-height 52px, зазор 8px), сетка «медиа 5/ответы 7»
  через :has, class-driven галка множественного, ревью по компонентам, маппинг
  ou-quiz статусов. Всё на --ou-* — гард на отсутствие цветовых литералов зелёный.

- **feat**(template): общий рендер ранжирования и сопоставления (ou-rank/ou-match) (2026-07-26) [`bbb7199d53d255d549160694a632b32662a00d0b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bbb7199d53d255d549160694a632b32662a00d0b)
  renderRanking: ou-rank строки с grip/index, DnD по позиции (data-drag/drop) плюс
  клавиатурные кнопки rank-up/rank-down. renderMatching: сетка ou-match (side-r) —
  фиксированная подсказка + перетаскиваемый ответ; пул неразмещённых через общий
  normalizePool, классы ревью. Модель DnD/пула сохранена, сменилась только разметка.

- **feat**(template): общий рендер одиночного/множественного выбора (ou-radio-card/ou-check) (2026-07-26) [`802e0eb7d058695e2e9b56075b82b8dcc0cb0def`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/802e0eb7d058695e2e9b56075b82b8dcc0cb0def)
  Чистые renderSingleChoice/renderMultiple эмитят .ou-разметку эталона: карточка
  ou-radio-card с class-driven is-on, декоративный контрол (радио-кольцо/чек-бокс),
  делегирование select:N, порядок по shuffleMapping, классы ревью correct/incorrect.
  Раскладка сцены (заполнение, зазор 8px, галка множественного) — в theme.css (3.1).

- **feat**(scorm): вендоринг DS и шрифта RostelecomBasis в пакет (2026-07-26) [`e5fdead611f4047d21738d276ebfe49d483ba038`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e5fdead611f4047d21738d276ebfe49d483ba038)
  styles.css пакета собирается как DS (токены+компоненты+@font-face) -&gt; theme.css/
  base.css поверх -&gt; мост палитры. Брендовый шрифт (4 woff2) кладётся в assets/fonts/
  и объявляется в манифесте; @font-face DS переписан на путь пакета (woff2-only).
  Браузерная приёмка в SCORM-плеере: DS применяется к .ou, мост даёт акцент из
  --primary, шрифт грузится, старый экран без регресса. Вес zip 866-&gt;1056 КБ.

- **feat**(template): DS + мост палитры в веб-хосте (Shadow DOM под .ou) (2026-07-26) [`2f5471b95b52e97f880bb150a17d9ad30aa2ea96`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2f5471b95b52e97f880bb150a17d9ad30aa2ea96)
  Веб-хост теперь грузит дизайн-систему в Shadow DOM (persistent-стайл, :root-&gt;:host)
  и делает host DS-провайдером темы (.ou + .ou--&lt;theme&gt;, «Авто» следит за системой).
  template-render подмешивает buildPaletteBridge к css экрана, поэтому DS-акцент
  ученических экранов выводится из --primary теста. Мост экспортирован из TBTemplate
  для второго хоста. ?raw-инлайн DS в бандл подтверждён сборкой.

- **feat**(template): мост палитры теста в токены DS (color-mix рампа из --primary) (2026-07-26) [`9546989005eb9dfc72c694637a4b63814715a294`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9546989005eb9dfc72c694637a4b63814715a294)
  Выводит акцентную рампу --ou-purple-50..900 из одного --primary теста теми же
  долями color-mix(in oklch), что и ui-kit, плюс поверхности из --background/--card/
  --border. Все --ou-accent-* следуют за рампой, поэтому бренд теста применяется в
  обеих темах без правки панели «Оформление». Формула принята браузерно на трёх
  брендах x двух темах (см. docs/plans/notes/uikit-palette-bridge-acceptance.md).

- **feat**(workbook): лист «Пороги вариантов» и колонка «Варианты» в шаблоне книги (PRD-24/PRD-17) (2026-07-26) [`e768d4cd5e3eae6ecd7cab2dded7884baede78c5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e768d4cd5e3eae6ecd7cab2dded7884baede78c5)
  Скачиваемый шаблон книги теперь совпадает с экспортом из теста: в «Вопросы» добавлена колонка «Варианты» (членство в вариантах, PRD-17), добавлен лист «Пороги вариантов» (PRD-24) — иначе автор не мог из шаблона положить вопрос в вариант и задать порог. Порядок листов и справка синхронизированы с экспортом. Шаблон вопросов берёт заголовки из QUESTION_HEADERS, чтобы не отставать от формата.

- **feat**(templates): показывать замечания комплектности в карточке шаблона (PRD-3) (2026-07-26) [`085982f5a489c9ffa996e1b9ffbf885d39290258`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/085982f5a489c9ffa996e1b9ffbf885d39290258)
  Бейдж «Комплектность» держится всю жизнь шаблона, но причины были видны только в диалоге загрузки — автор видел вечное предупреждение без объяснения. IssueList вынесен в общий модуль и теперь рендерит блокирующие ошибки и замечания и в модалке деталей.

- **feat**(content): полный состав папки в дереве и тултип на обрезанных названиях (2026-07-26) [`7e59890d45d658b66af8e098ac690d9641462133`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7e59890d45d658b66af8e098ac690d9641462133)
  Строка папки показывает всё, что лежит в поддереве («2 папки · 3 темы · 40 вопросов»), а не «0 тем» рядом с сотнями вопросов; нулевые части опускаются. Новый TruncatedLabel вешает title на название вопроса только когда оно реально обрезано колонкой.

- **feat**(flow): единый гейт показа обзора ответов (PRD-19) (2026-07-26) [`5a437e0432c641afc9dd187c0c904828c656abe5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5a437e0432c641afc9dd187c0c904828c656abe5)
  Общий модуль shared/flow/review-gate (shouldShowReview): обзор показывается только когда ученику есть что сделать — вернуться к пропущенному или изменить ответ; иначе поток идёт сразу к итогам раздела. Оба хоста читают одно правило (PRD-12 паритет): веб импортирует напрямую, пакет — через TBTemplate. SCORM-рантайм переведён с проверки allowReturnToUnanswered на этот гейт.

- **feat**(analytics): категориальная палитра серий из токенов DS (2026-07-24) [`7201ba619a7f255033da08ad8f7f2a0d3d3b5475`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7201ba619a7f255033da08ad8f7f2a0d3d3b5475)
  Цвета линий считались формулой hsl(i*60, 70%, 50%): контраст и различимость
  соседей никто не гарантировал, тему формула не слышала вовсе. Введён набор
  --tb-chart-1..8 из семантических токенов DS.
  
  Набор объявлен на `.ou`, а не на :root: семантические токены DS живут в блоках
  `.ou, .ou--light` / `.ou--dark`, и объявление в :root ссылалось на переменные,
  которых там нет, — все серии молча схлопывались в цвет текста. Поймано замером в
  браузере, а не тестом.
  
  Замер после правки: 8 различных цветов в светлой и 8 в тёмной теме, значения
  меняются при переключении.

### Fixes

- **fix**(scorm): пакет без телеметрии не несёт ни рантайма, ни висячих вызовов (2026-07-30) [`41b4983c3a58ba2e35710ff2dba44d36199b08e6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/41b4983c3a58ba2e35710ff2dba44d36199b08e6)
  Модуль телеметрии в пакет при выключенном состоянии не попадал и раньше,
  а вот места вызова вырезались регулярками по собранному коду — и
  вырезались не все. `Telemetry.finish(results)` в adaptiveRender шёл с
  переменной, а не с объектным литералом, под шаблон не подходил и
  оставался в бандле ссылкой на несуществующий объект. Адаптивные экраны
  кладутся в каждый пакет независимо от режима теста, поэтому адаптивный
  тест, выгруженный без телеметрии, падал с ReferenceError на результатах.
  
  Правку исходников регулярками заменила заглушка `telemetry-disabled.js`:
  тот же интерфейс, ничего не делающий. Телеметрии в пакете по-прежнему
  нет — ни эндпоинта, ни подписи запросов, ни чтения профиля из LMS, ни
  буфера, — но имя `Telemetry` связано, и класс «пропущенное место вызова»
  стал невозможен по построению. `hasAttemptNumber()` возвращает false:
  номер попытки заглушечный, и подзаголовок «Попытка N из M» показывать
  его не должен.
  
  Заодно убраны три console.log про телеметрию в answers/startPage/
  resultsPage. Они дублировали собственный лог модуля, а в пакете без
  телеметрии второй из них врал — сообщал об отправке, которой не было.
  
  Инвариант закреплён тестом: если бандл упоминает `Telemetry`, он обязан
  его объявлять. Приёмка в браузере: пакет доигран до экрана результатов,
  finishAndClose() отрабатывает, консоль чистая, запросов телеметрии нет.

- **fix**(scorm): результат попытки переживает закрытие страницы (2026-07-30) [`c1ddb13c8bc05ca96c916eb375b9cde63f27b857`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c1ddb13c8bc05ca96c916eb375b9cde63f27b857)
  Запрос завершения попытки уходил обычным fetch и отменялся вместе с
  документом: LMS в этот момент часто уводит со SCO. Потерянный запрос
  попадал в буфер повторов — именно так набирался поток обращений,
  который периметр расценил как повышенную активность.
  
  Теперь `/api/scorm-telemetry/finish` отправляется с `keepalive`: формат
  на проводе прежний, серверных правок не потребовалось. Где поля нет
  (встроенные браузеры части LMS), payload уходит через `sendBeacon`
  блобом `application/json`; маяк сообщает о постановке в очередь, а не о
  доставке, поэтому успех считается отправкой — повтор задвоил бы попытку.
  Остальные события остаются обычными запросами: бюджет `keepalive` общий
  на страницу, тратить его на промежуточные события значит обделить
  единственный payload, обязанный пережить страницу. Результат тяжелее
  60 КБ уходит обычным запросом, иначе браузер отклонил бы вызов.
  
  Приёмка в браузере: рекордер придерживает preflight, вкладка
  закрывается сразу после вызова. POST завершения приходит через 3 с
  после закрытия; контрольный обычный запрос не приходит вовсе.

- **fix**(tags): длина тега до 100 символов и общая нормализация квот (2026-07-30) [`20c66eea3d8e8129f71a2c4e8c2f3298e3e18981`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/20c66eea3d8e8129f71a2c4e8c2f3298e3e18981)
  Формулировки компетенций не помещались в 50 символов и обрезались посреди
  фразы. Квоты выдачи теперь проходят через тот же нормализатор, что и теги
  вопроса: выдача сопоставляет теги строкой, поэтому расхождение в написании
  означало квоту, не находящую ни одного вопроса.

- **fix**(prd-25): кнопки в «Мне назначено» одной ширины; шаблон list-action в ui-kit (2026-07-30) [`a59f9c76f6d6f9ea33860afd44ae751ed2ffbc3b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a59f9c76f6d6f9ea33860afd44ae751ed2ffbc3b)

- **fix**(prd-25): отступы главной как на остальных авторских страницах (2026-07-30) [`57625271b6099b160be00cf87d8e42418925450b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/57625271b6099b160be00cf87d8e42418925450b)

- **fix**(access): ссылка-вход не выдаётся привилегированным и при добавлении в группу (2026-07-30) [`429503fcc50f7451f0716c0930b937a2aba000fa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/429503fcc50f7451f0716c0930b937a2aba000fa)
  Пятый путь выпуска ссылки, пропущенный на Этапе 3: notifyNewGroupMember
  в server/routes/groups.ts минтил токен вручную и не спрашивал
  mayReceiveAssignmentLink — приватная учётная запись, добавленная в
  группу с активным назначением, получала passwordless-вход.
  
  Структурная причина устранена: единая точка server/services/
  assignment-link.ts (deliverAssignmentLink) — решение
  «выдать/придержать» и отправка письма в одном месте вместо четырёх
  копий в assignments.ts плюс отдельной ручной реализации в groups.ts.
  Все пять точек выпуска (создание назначения прямое и групповое,
  /resend, /resend-group, /resend-user, добавление в группу) теперь
  проходят через неё.
  
  Добавлены тесты на придержание ссылки для /resend-group,
  /resend-user и добавления в группу — ранее эти пути не проверялись
  на privileged-получателя.

- **fix**(access): в сессии по ссылке «назад» ведёт к своему тесту, а не в список (2026-07-30) [`bae1bac1bfc65cd0dc3b640f140b4bd847ede5cd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bae1bac1bfc65cd0dc3b640f140b4bd847ede5cd)

- **fix**(learner): при исчерпании попыток факты перечитываются, «Мой результат» ведёт на попытку (2026-07-30) [`c0cb270823613f2f07afd0aa03f0a8fb0ce14917`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c0cb270823613f2f07afd0aa03f0a8fb0ce14917)

- **fix**(learner): исчерпание попыток показывается на стартовом экране, без ухода в кабинет (2026-07-30) [`fd65f5fd5921e2a15ed9ceea6f22d95bec0c9acd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fd65f5fd5921e2a15ed9ceea6f22d95bec0c9acd)

- **fix**(access): свой тест с завершающей косой чертой остаётся в границах (2026-07-30) [`eefd5b8080ffa98bca7efb7e492044cc7b7bcb44`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eefd5b8080ffa98bca7efb7e492044cc7b7bcb44)

- **fix**(access): признак выхода за границы читается из кода ответа, а не из текста (2026-07-30) [`56dd590e7aa6af0aa5691427b86143f4b06e3bc8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/56dd590e7aa6af0aa5691427b86143f4b06e3bc8)

- **fix**(access): guard не обходится регистром пути, неизвестная привязка запрещает (2026-07-30) [`f93a273696aacd91286265a92f1a8edb88eefc63`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f93a273696aacd91286265a92f1a8edb88eefc63)

- **fix**(report): длинная подпись уровня переносится, а не обрезается (2026-07-30) [`4b7549e8981dc7b95c698585580eb4b8b58988fc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4b7549e8981dc7b95c698585580eb4b8b58988fc)
  Приёмка в браузере на реальной адаптивной попытке показала: плашка уровня
  обрезалась по краю карточки — ученик видел «Минимально требуемый уровен…».
  
  Плашка несла white-space: nowrap с тех времён, когда подпись была короткой
  («Не достигнут»). Полная формулировка вердикта — больше сорока символов, и
  в трёхколоночной сетке (колонка около 170 px) она гарантированно выходит за
  карточку, а карточка обрезает (overflow: hidden). То же касается названия
  уровня: оно задаётся автором шаблона и ничем не ограничено.
  
  Плашка адаптивного отчёта теперь переносится и ограничена шириной карточки;
  короткая подпись по-прежнему компактна. Плашка вердикта стандартного
  отчёта («Пройден» / «Не пройден») оставлена без переносов — она короткая, и
  разрыв на две строки ей только вредит.
  
  Приёмка: страница отчёта отрисована на попытке с четырьмя темами, три из
  которых с полным вердиктом — текст читается целиком во всех карточках;
  пакет пересобран, PDF собирается.

- **fix**(prd-12): веб-хост получает макет межуровневого перехода (2026-07-30) [`9db60e88894a2932e0be8759c589b0e2d058f5a4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9db60e88894a2932e0be8759c589b0e2d058f5a4)
  Экран `system.transition` не значился среди экранов, которые отдаёт
  `/screen-template/:screen`, поэтому веб-хост запросить его не мог. Это
  чистый системный макет без вида контент-шаблона — как `blocked`, — и
  файловый fallback ниже закрывает шаблон, который его не поставляет.

- **fix**(scorm): стартовый экран адаптивного пакета рендерится из шаблона (2026-07-30) [`ef403e0eadcd0d1372ee705eab027f34a1fb2737`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ef403e0eadcd0d1372ee705eab027f34a1fb2737)
  Шаблонный путь был закрыт для адаптива условием `mode !== 'adaptive'` —
  когда-то у режима был собственный хром. Хрома давно нет (PRD-12: один
  экран, один шаблон), а условие осталось, и адаптивный пакет открывался
  не стартовым экраном, а заглушкой «Стартовый экран недоступен: шаблон не
  предоставил макет». Общий контекст к адаптиву готов: `canResume` для него
  и так всегда false — адаптивную сессию не возобновляют.

- **fix**(access): матчер пути не падает на некорректном percent-encoding (2026-07-30) [`895dcb8788c6aa21e70ef2593cd39b1243822d7e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/895dcb8788c6aa21e70ef2593cd39b1243822d7e)

- **fix**(scorm): «Мой результат» на общей сцене, кнопка отчёта возвращена (2026-07-30) [`c66ade2be093624d8f5429633ea25f9a17bd312f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c66ade2be093624d8f5429633ea25f9a17bd312f)
  Пакетный app.js — плоская склейка частей рантайма (joinJsParts), и
  assets/app.js идёт в ней ПОСЛЕДНИМ. Поэтому его собственные объявления
  viewSavedResults / renderSavedResults / backToStart перекрывали шаблонные
  из app/render/*: экран «Мой результат» рендерил доревизионную разметку
  .results-page, для которой в текущем styles.css нет ни одного класса —
  пустой экран, а «Скачать отчёт» уезжала за пределы вида.
  
  Легаси-двойники удалены. Шаблонный renderViewResultsTemplated заполняет
  result.nav (отчёт + замыкающее действие) и привязывает подвал: отчёт
  выгружает ЛУЧШУЮ сохранённую попытку — ту, что показана на экране, — а
  замыкающее действие возвращает на стартовый экран, а не закрывает SCO.
  
  Приёмка в SCORM-плеере: оба пути к экрану дают DS-сцену с «Скачать
  отчёт», PDF собирается.

- **fix**(prd-26): пиктограмма шкалы — thermometer-sun (2026-07-30) [`8a3471e06b65734712131a5c0bc839a858ad30f9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8a3471e06b65734712131a5c0bc839a858ad30f9)
  Иконка типа «шкала» заменена во всех четырёх местах, где интерфейс
  показывает пиктограмму вопроса: дерево содержимого, вкладка «Оценка»,
  редактор вариантов и инспектор тестового прогона.

- **fix**(templates): мост палитры DS у внешних шаблонов и в предпросмотрах (2026-07-30) [`0b27938279245760c0b37c0402ebe73082fb3b39`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0b27938279245760c0b37c0402ebe73082fb3b39)
  Ученические экраны — разметка .ou-*, и весь акцент они берут из рампы DS
  --ou-purple-*, а не из --primary; связывает их palette-bridge. Мост вызывался
  в двух местах и вёл себя трояко: в пакете безусловно, на экранах веб-хоста —
  только если автор СОХРАНИЛ параметры оформления, а в bundle, из которого
  рендерят админский реестр и предпросмотры редактора, его не было вовсе.
  
  Из-за этого шаблон, чей бренд живёт в собственном theme.css при default: null
  в параметрах (паттерн PRD-23), выходил оранжевым в выгрузке и фиолетовым в вебе.
  Стандартный шаблон дефект скрывал: его theme.css-primary и есть DS-фиолетовый.
  
  Ломалось при этом обещание, которое даёт сам редактор: незаданная строка цвета
  показывает inheritedColor из extractThemeTokens по этому же theme.css, то есть
  UI говорит «наследуешь оранжевый», а рендер красил фиолетовым, и автору
  приходилось вручную вводить цвет, который ему уже показали.
  
  paletteVar получил четвёртый аргумент templateCss — мост срабатывает и когда
  токен объявлен в собственной таблице стилей шаблона; readTemplateBundle
  дописывает мост к css. Безопасно, потому что мост эмитит живую ссылку
  hsl(var(--primary)): побеждает то, что выше в каскаде — авторский параметр либо
  дефолт темы, — а шаблон без палитры моста по-прежнему не получает.
  
  Закрывает два падавших теста template-render.palette.

- **fix**(prd-25): боковая колонка главной липкая при прокрутке, как в спецификации (2026-07-30) [`4fcaa5d1ba0a19224e497e08f751889ed97fefee`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4fcaa5d1ba0a19224e497e08f751889ed97fefee)

- **fix**(scorm): переключатель «Случайный порядок вариантов» доезжает до пакета (2026-07-30) [`46af6535e5d9475c919497868d83339651953768`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/46af6535e5d9475c919497868d83339651953768)
  Флаг вопроса не запекался в TEST_DATA, поэтому рантайм пакета строил
  перемешивание для каждого вопроса: выключенный автором переключатель работал
  в вебе и молча игнорировался в SCORM-пакете и отладочном плеере, который
  собирает тот же пакет.
  
  Поле кладётся в бейк только когда переключатель выключен (умолчание —
  включён), поэтому пакеты нетронутых тестов остаются байт-идентичными (FR-02).
  Рантайм уже читает его через общий shuffleMappingFor.

- **fix**(prd-20): экспортированная схема pglite знает о section_timer_json (2026-07-30) [`7b99d7e6465cc4b6e88b772f8633c599cbb07c0b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7b99d7e6465cc4b6e88b772f8633c599cbb07c0b)
  `tests/it/schema.sql` — DDL, экспортированный из `shared/schema.ts` и
  применяемый харнессом pglite. После добавления `attempts.section_timer_json`
  его не перегенерировали, поэтому DAL-тесты, вставляющие attempts, падали:
  Drizzle перечисляет колонку в INSERT, а в тестовой БД её не было.
  
  Перегенерировано документированной процедурой из db-harness:
  `npx drizzle-kit export --sql | grep -v '^DATABASE_URL:' &gt; tests/it/schema.sql`
  (диff — ровно одна строка).

- **fix**(prd-3): макеты вариантов страниц учитываются как используемые (2026-07-30) [`3bbf7bee659781eda70b3b46efd9708c3c7d1a59`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3bbf7bee659781eda70b3b46efd9708c3c7d1a59)
  `collectReferences` обходил `layouts`, `partials`, `assets`, `systemPages`,
  но не `contentTemplates[].layoutFile` — а варианты страниц объявляют свой
  макет именно там (PRD-22). Из-за этого каждый макет варианта попадал в
  предупреждения `UNUSED_FILE`, и исправный пакет навсегда получал жёлтый чип
  «Комплектность: Предупреждения»: у «Сертификация (РТК)» так набралось 7
  предупреждений на content.text, content.image-*, gallery.* и
  start.image-right.
  
  Асимметрия была налицо: проверка синтаксиса DSL (`collectTemplateSources`)
  эти пути обходит, а учёт использования — нет. Побочно закрыт второй дефект:
  отсутствующий макет варианта не давал блокирующей FILE_MISSING.
  
  Реальный пакет РТК после правки валидируется как ok=true без предупреждений.
  Сохранённый в БД отчёт устаревший — чип снимется после повторной проверки
  (`POST /api/admin/templates/:id/validate` или загрузка ZIP через «Обновить»).

- **fix**(scorm): DS-стиль и брендовый шрифт пакета берутся из dist (2026-07-30) [`f8ba3e20594559555e2f260c117cf3a81946ca68`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f8ba3e20594559555e2f260c117cf3a81946ca68)
  `readVendorDsCss` читал `vendor/ui-kit/css/university-rt.css` из исходного
  дерева, а `readPackageFontFiles` — `client/public/fonts`. В прод-образе нет
  ни `vendor/`, ни `client/`, и вдобавок корень считался от `__dirname`, тогда
  как в бандле все модули схлопываются в `dist/` — расчёт уводил за пределы
  приложения. Итог на test и проде: `generateScormPackage` падал с ENOENT,
  поэтому SCORM-экспорт и отладчик (PRD-18) отвечали 500 «Failed to build
  debug session», хотя на dev всё собиралось.
  
  Оба источника стали ассетами сборки: `script/build.ts` кладёт их в
  `dist/scorm/assets/{ds,fonts}`, чтение идёт по кандидатам «исходник → dist»
  относительно CWD — та же конвенция, что в assets/read-asset. Правка
  Dockerfile не нужна: `dist` копируется в образ и так.
  
  Побочно закрыт тихий дефект: пропуск шрифта глотался catch'ем, так что
  пакет мог уехать вообще без RostelecomBasis.
  
  Проверено воспроизведением: сборка пакета в файловой раскладке образа
  падала с тем же ENOENT и проходит после правки.

- **fix**(deploy): PDF-документы разделов «Шаблоны» и «Импорт» едут в образ (2026-07-30) [`1e591d4c7e70de751e94fa805433772cec715527`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1e591d4c7e70de751e94fa805433772cec715527)
  Роуты скачивания читают `docs/dist/*.pdf` с диска, но Dockerfile копировал
  только явный список путей и `docs/dist` в образ не попадал — на проде оба
  документа отдавали 404 «Документ не собран», при этом PDF были закоммичены.
  Коммит PDF в git на состав образа не влияет.
  
  `.dockerignore` глушил `docs/` целиком: заменено на `docs/*` + `!docs/dist`
  (Docker не даёт ре-инклюдить путь под исключённым родителем — тот же приём,
  что для server/scorm/templates). Контракт закреплён тестом: все документы,
  объявленные в роутах, собраны, скопированы в образ и не исключены из
  контекста сборки.

- **fix**(learner): стартовый экран адаптивного теста и отказ вместо вечного спиннера (2026-07-29) [`4e259c7a0fddbd8ff266806395ab7ecf7f1aae46`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e259c7a0fddbd8ff266806395ab7ecf7f1aae46)
  Назначенный адаптивный тест на вебе не открывался: бесконечная «Подготовка
  теста...» и пустая консоль. Ветка стартового экрана была ограничена
  testMode === "standard", а легаси-React-старт, который покрывал адаптив,
  удалён вместе с переводом ученических экранов на шаблоны. Адаптивный тест
  не совпадал ни с одной веткой рендера и проваливался в заглушку-спиннер,
  причём молча — отказ загрузки шаблона глотался пустым catch.
  
  Старт теперь рендерится в обоих режимах: макет от режима не зависит.
  У адаптива не передаётся questionCount (в секциях drawCount = 0), а факты
  «вопросов» и «проходной балл» в start.html / start.image-right.html
  обёрнуты в {{#if}} — иначе на экране пустое число и голый «%»; для обычных
  тестов значения ненулевые, вид не меняется. Возобновление у адаптива
  скрыто: handleResumeTest его не восстанавливает и всегда начинает заново,
  а в варианте нет списка вопросов, поэтому предложение читалось как
  «вопрос 1 из 0» и не делало обещанного.
  
  Отказ рендера больше не невидим. Учащийся видит одно нейтральное
  «Ошибка сервиса. Обратитесь к администратору» — техническая формулировка
  ему всё равно неинтерпретируема. Диагностика уходит двумя каналами
  (lib/report-error): console.error и POST /api/logs/client — новый узкий
  приёмник, пишущий в тот же буфер, который читают «Логи». Приёмник открыт
  любому авторизованному (учащийся не имеет logs.*, а он и есть тот, кто
  упирается в сломанный экран), сообщение обрезается до 500 символов,
  схлопывается в одну строку (перевод строки иначе подделывает записи) и
  ограничен 20 отчётами на пользователя в минуту: кольцевой буфер общий и
  конечный. Провал в конец рендера больше не вечен — спиннер держится 10 с
  (есть законные однокадровые провалы при опустошении очереди контента),
  затем тот же экран отказа с отчётом о застрявшем состоянии.
  
  Приёмка в браузере на копии прод-данных: вход штатным magic link под
  назначенным учеником, стартовый экран открылся, тест запустился и выдал
  первый вопрос, 0 ошибок в консоли. Экран отказа проверен подменой ответа
  screen-template/start на 500 — обе записи легли в лог сервера с привязкой
  к пользователю.

- **fix**(prd-6): монотонный бюджет гейта и покрытие диагностики источника даты (2026-07-29) [`1dfa1018b723c71b311b5fdda243480382819c27`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1dfa1018b723c71b311b5fdda243480382819c27)

- **fix**(prd-6): синхронный сбой fetch больше не выносит retake-гейт наружу (2026-07-29) [`197339e3c91b360536c5ca8f269c280e5d198ebb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/197339e3c91b360536c5ca8f269c280e5d198ebb)

- **fix**(prd-6): резолв даты укладывается в общий бюджет retake-гейта (2026-07-29) [`66468a6c26560818d07c47543a9cb094e62541ac`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/66468a6c26560818d07c47543a9cb094e62541ac)

- **fix**(prd-25): черновик без вопросов — предупреждение, как в согласованном эскизе (2026-07-29) [`0c2d6c1c503cda576f37d94b7fd243da59415437`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0c2d6c1c503cda576f37d94b7fd243da59415437)

- **fix**(editor): пустой тег квоты блокирует сохранение и виден заранее (2026-07-28) [`ff92efc4f8f876543ae395e233c52d7cb58c20e5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ff92efc4f8f876543ae395e233c52d7cb58c20e5)
  Квота по тегу с пустым/пробельным (или слишком длинным) тегом уходила на сервер и
  отваливалась сырым HTTP 400 (sections.N.drawBlueprintJson.strata.M.tag) уже после
  нажатия «Сохранить». Теперь клиент зеркалит серверную drawStratumSchema
  (normalizeTag → 1–50 символов; count — целое ≥1) в validateTestEditor: ошибка
  попадает в validation.errors и через существующий канал БЛОКИРУЕТ сохранение
  (кнопка disabled + guard в save()), зажигает точку вкладки «Состав» и верхний
  баннер «Поля с ошибками». Плюс в QuotaEditor — заметный инлайн-баннер «Не выбран
  тег для квоты…» и подсветка невалидной строки (Select tone="error"). Юниты на
  пустой/длинный тег и битый count добавлены.

- **fix**(scorm): экран итогов заполняет ширину, как страница-роутер (2026-07-28) [`6c46c77dfe9ea35da8546b90d1fdb2eddd1c41d7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6c46c77dfe9ea35da8546b90d1fdb2eddd1c41d7)
  .tb-scene__col--wide держал max-width:1120px — на широком стенде оставалась
  пустая полоса справа. Кап снят: поле итогов заполняет всю ширину тела (как
  .tb-content на роутере/контенте), а сетка «Результаты по темам»
  (auto-fit minmax 340) растягивается на больше колонок.

- **fix**(scorm): карта вопросов кликабельна; невыданные пиллы без курсора-пальца (2026-07-28) [`22b8ed3bc7a806b88483a1211d0168d845af5e00`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/22b8ed3bc7a806b88483a1211d0168d845af5e00)
  На экране вопроса клики по карте вешались на устаревший селектор .tb-pill —
  реальные пиллы это .ou-quiz__dot, поэтому навигатор был мёртв. Селектор
  исправлен: выданные (issued) вопросы снова кликабельны и ведут к goToQuestionIndex,
  frontier по-прежнему гейтит недоступные (disabled). Клик уважает allowReturn:
  в строгом режиме все пиллы disabled → навигатор read-only.
  Плюс `.ou-quiz__dot:disabled { cursor: default }` — базовый DS ставит
  cursor:pointer на все точки, из-за чего невыданные (disabled) показывали палец.

- **fix**(scorm,web): обзор — убран дубль индикатора из шапки, счётчики в теле (2026-07-28) [`d1a36b463b0e6f9448d2684cd5ce1b3f5b5206c7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d1a36b463b0e6f9448d2684cd5ce1b3f5b5206c7)
  На экране обзора шапка повторяла карту вопросов, которая уже есть в теле
  (крупный tb-review-map). Индикатор из шапки убран; счётчики «отвечено X из Y ·
  пропущено Z» перенесены в тело — под точки и легенду. Шапка обзора теперь
  несёт только заголовок теста. Layout общий → веб и SCORM одинаково.

- **fix**(scorm,web): «пропущено» переносится под «отвечено», не к левому краю (2026-07-28) [`77d84cf6575e799f8d3ab0a79b8f5d2686b62aaa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/77d84cf6575e799f8d3ab0a79b8f5d2686b62aaa)
  При сужении «пропущено X» уходило full-width к левому краю, разъезжаясь с
  «отвечено Y из Z» справа. Счётчикам задан text-align:right — теперь при
  переносе на свою строку они выстраиваются стопкой по правому краю, «пропущено»
  ровно под «отвечено».

- **fix**(scorm,web): баннер вердикта над опциями, цветная иконка, центровка (2026-07-28) [`41722db055814e538338efdc25bf37df04181883`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/41722db055814e538338efdc25bf37df04181883)
  По эскизу: (1) баннер результата ответа переносится НАД опциями (слот
  question-feedback перед question-interaction); (2) иконка красится по исходу
  (зелёный/жёлтый/красный) — база .ou-banner оставляла её серой, тон дают только
  style-варианты, которых баннер не использует; (3) строка вердикта
  центрируется с иконкой (align-items:center, снят margin-top иконки).
  Отступ слота переведён с top на bottom (баннер теперь сверху).

- **fix**(scorm): итоговый экран через общий layout, а не легаси-разметку (2026-07-28) [`435e22a0a0a784ff0b1989f5ca0d66eeb7783f7d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/435e22a0a0a784ff0b1989f5ca0d66eeb7783f7d)
  Пост-финиш renderResults строил старую разметку results-page (results-hero /
  results-main-card / results-ring), которую ревизия «Стандартный» в theme.css
  больше не стилизует — экран схлопывался в пустую карточку. Теперь итоги
  рендерятся через общий layout results.html + renderScreenInto (та же сцена, что
  у «Мой результат» и веб-хоста): шапка с тегом исхода, кольцо, факты, карточки
  тем. Свои действия финиша (Скачать отчёт / Пройти заново / Завершить|Далее)
  навешиваются на футер после монтирования. Старая разметка осталась
  запасным путём (renderResultsLegacy) для шаблона без layout результатов.

- **fix**(scorm,web): ошибочно пропущенный вариант — красный крест, а не галочка (2026-07-28) [`3a5a511b3fe658a6bd703a5b0bc1b1eae2b5ce56`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3a5a511b3fe658a6bd703a5b0bc1b1eae2b5ce56)
  Жёлтая карточка (правильный ответ, который не выбрали) несла галочку — она
  ложно читалась как «здесь верно». Учащийся ошибся и на этом варианте, поэтому
  метка теперь красный крест (карточка остаётся жёлтой). Галочку получает только
  правильно выбранный вариант; обе ошибки (лишний выбор и пропуск) — крест.
  Плюс отступ над баннером-вердиктом, чтобы он не прилипал к последней опции.

- **fix**(scorm,web): карточки хаба показывают исход раздела, а не мнимую «Пройдена» (2026-07-28) [`c15e673720a797d26992cc24d96383f50ce30033`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c15e673720a797d26992cc24d96383f50ce30033)
  Завершённая секция всегда красилась зелёным «Пройдена», даже когда порог не
  взят. Теперь исход выводится на карту только при включённой демонстрации итогов
  раздела: пройдена → зелёный «Пройдена», не пройдена → красный «Не пройдена».
  Итоги выключены или у раздела нет порога (passed == null) → нейтральный
  «Завершена», чтобы хаб не раскрывал скрытый автором результат. Логика в общем
  shared/flow/router-hub; оба хоста прокидывают showSectionResults и sectionResults.

- **fix**(debug-player): «Эталон» не рвёт перетаскивание в сопоставлении (2026-07-28) [`77f6498ccb843fea4d356b32130892dd211b385a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/77f6498ccb843fea4d356b32130892dd211b385a)
  Оверлей эталона перекрашивался каждые 600ms и мутировал поддерево
  захваченного (setPointerCapture) чипа — Chromium отвечал pointercancel,
  и движок DnD обрывал жест. Перетаскивание работало только при выключенном
  эталоне. applyReference/clearReference теперь простаивают, пока в iframe
  идёт жест (существует [data-drag-ghost]); следующий тик перекрашивает.

- **fix**(scorm,web): убран native draggable у чипов матчинга/ранжирования (2026-07-28) [`a78b2d3a18d014ba0dba368afe4194def6a3a3e4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a78b2d3a18d014ba0dba368afe4194def6a3a3e4)
  Коренная причина, почему мышью не тащилось: чипы несли draggable="true", и на
  настоящем drag браузер запускал НАТИВНЫЙ HTML5-drag (fires pointercancel), который
  обрывал жест общего pointer-движка. Playwright page.mouse нативный drag не поднимает,
  поэтому автотесты это скрывали. Атрибут полностью удалён — нативный drag теперь
  невозможен, движок остаётся единственной механикой. cursor:grab возвращает подсказку
  захвата. Фикс в общем рендере — оба хоста, матчинг и ранжирование.

- **fix**(scorm,web): матчинг/ранжирование — реальное перетаскивание мышью (2026-07-28) [`cdf4a07102199b269b03715ee82d414d18d18f95`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cdf4a07102199b269b03715ee82d414d18d18f95)
  Чипы несут legacy-атрибут draggable="true", из-за чего при настоящем drag мышью
  браузер запускал НАТИВНЫЙ HTML5-drag; он шлёт pointercancel и обрывал жест общего
  pointer-движка — мышью перетаскивание молча не работало (проходили только
  синтетические PointerEvents, которые нативный drag не поднимают). Движок теперь
  гасит dragstart на [data-drag], оставаясь единственной механикой. Фикс в общем
  модуле — оба хоста, оба типа (матчинг и ранжирование).

- **fix**(debug-player): «Эталон» матчинга — лейбл пары не прилипает к тексту (2026-07-28) [`deb2854432de3adcdebae4d4d961d179d9e9057a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/deb2854432de3adcdebae4d4d961d179d9e9057a)
  Гаттер под букву пары увеличен с 40px до 52px: бейдж (~32px от left:10px) больше
  не перекрывает текст карточки (было −2px, стало +10px зазора).

- **fix**(scorm,web): матчинг — восстановлено создание пар перетаскиванием (2026-07-28) [`7723c967be4253435d7feca552a66f731aeb4a86`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7723c967be4253435d7feca552a66f731aeb4a86)
  Открытые строки сопоставления отдавали дроп-зону pool:slot (только переупорядочивание
  пула), а пара создавалась лишь через r&lt;ri&gt;, который был только у УЖЕ соединённых
  строк — первую пару создать было невозможно, интерактив не работал. Слот каждой
  строки выровнен со СВОЕЙ подсказкой, поэтому дроп в него теперь пары́т чип с этой
  подсказкой (data-drop=r&lt;ri&gt;). Общий рендер — фикс на обоих хостах.

- **fix**(scorm): не показывать правильные ответы, если настройка выключена (2026-07-28) [`d93fa277f59c995c9709c726b925a04d2cd1cc62`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d93fa277f59c995c9709c726b925a04d2cd1cc62)
  Подсветка правильного/неверного в SCORM-рантайме гейтилась по locked (feedback
  показан ИЛИ ответ зафиксирован), из-за чего при выключенном showCorrectAnswers,
  но зафиксированном ответе (allowAnswerChange off) ключ утекал ученику. Теперь
  ревью гейтится строго по showCorrectAnswers (как на вебе, take-test reviewMode).
  Редактируемость по-прежнему держат делегированные обработчики (isAnswerLocked).

- **fix**(debug-player): «Эталон» не рисовал маркеры правильных ответов (2026-07-28) [`f0864854c2bc2adeb122841ed11e285a298c2163`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f0864854c2bc2adeb122841ed11e285a298c2163)
  applyReference целиком целился в старую разметку (.option[data-index] с qid из
  onclick, .ranking-board, .matching-board), которой ревизия «Стандартного» уже
  не выдаёт. Переписан под текущую разметку (.ou-radio-card / .ou-rank__item /
  .ou-match__row) и текущий вопрос из state.currentIndex; ranking/matching
  сопоставляются по тексту, т.к. DOM больше не несёт индекс элемента.
  ГОЧА: клиент грузит inspector-compute.js один раз (if !window.TBInspector) —
  открытому окну отладчика нужен хард-релоад, чтобы получить новый ассет.

- **fix**(scorm): «Далее» после отправки ответа не появлялась (2026-07-28) [`49e09ff8a8ad4ebeef904829813b6808001216f3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/49e09ff8a8ad4ebeef904829813b6808001216f3)
  updateNavigationButton искал нав-строку по .navigation, но ревизия
  «Стандартного» сделала футер вопроса .tb-scene__foot -&gt; селектор не находил,
  ранний выход, и «Отправить ответ»/«Принять» не сменялись на «Далее»: ученик
  застревал без кнопки вперёд. Селектор переведён на .tb-scene__foot.

- **fix**(scorm): хаб маршрутизатора — без подзаголовка попытки, карточки по верху (2026-07-28) [`07f5835e59ee100ffcf9792d56c3f4a08ae62b38`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/07f5835e59ee100ffcf9792d56c3f4a08ae62b38)
  \- Убран подзаголовок «Попытка N» из шапки хаба разделов: он относится к
    экранам внутри попытки, а не к меню разделов.
  \- Карточки разделов выравниваются по верху поля с отступом тела, а не
    центрируются по вертикали (tb-content-body justify-content для хаба).

- **fix**(scorm): отступ шапки от полосы прогресса (2026-07-28) [`55ba06ddb08ef89e8896ff8ecfbe6683a658aed2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/55ba06ddb08ef89e8896ff8ecfbe6683a658aed2)
  Шапка со стандартным header имела padding-bottom:0, из-за чего заголовок
  и граница прилипали к полосе прогресса (прибита к нижнему краю). Добавлен
  нижний отступ, чтобы контент шапки не касался линейки прогресса.

- **fix**(scorm): логотип в шапке страниц контента и адаптивных итогов (2026-07-28) [`6a1bc6148be8cf3a82f9a025ef0fc8eb57b2d70a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6a1bc6148be8cf3a82f9a025ef0fc8eb57b2d70a)
  Общая шапка биндит {{#if design.logoUrl}}, но два рендера НЕ клали design в
  контекст, из-за чего логотип пропадал на этих экранах при заданном лого:
  \- renderContentPage (страницы контента + роутер-хаб «Меню карточек», который
    монтируется в page-content контентной обёртки);
  \- renderAdaptiveResultsTemplated (адаптивные итоги).
  Добавлен design: scormDesignContext() в оба. Прочие экраны со стандартной
  шапкой (старт, вопрос, обзор, итоги раздела/теста, введение, переход) design
  уже передавали.

- **fix**(scorm): лого в пакете 404 + мохибаке имени файла при загрузке (2026-07-28) [`56c12df1d35463ffd60d0f11c9d343bd45e1b7f9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/56c12df1d35463ffd60d0f11c9d343bd45e1b7f9)
  Два бага загрузки/рендера логотипа:
  
  1. Лого 404 в пакете. Шапка содержит &lt;img src="{{ design.logoUrl }}"&gt;, а
     templateLoader применяет withTemplateAssetBase(layout, 'template/') к СЫРОМУ
     layout'у — и префиксил плейсхолдер: src="template/{{ design.logoUrl }}".
     После подстановки упакованного медиа (media-assets кладёт файл в assets/media/,
     URL = assets/media/X.png) выходило template/assets/media/X.png → файла там нет.
     Фикс: isAbsoluteRef пропускает URL с '{{' — значение плейсхолдера подставляется
     на рендере уже с корректной базой (упакованное медиа — от корня пакета). Статичные
     шаблонные ассеты (images/…) базу по-прежнему получают.
  
  2. Мохибаке имени файла. POST /api/media/upload возвращал req.file.originalname,
     который busboy декодирует как latin1 → кириллица приходит как «Ð Ð¼Ñ…». Декодирую
     обратно в UTF-8 (Buffer.from(name,'latin1').toString('utf8')).
  
  Проверено: withTemplateAssetBase не трогает плейсхолдер, но префиксит images/…;
  asset-base тесты (11) зелёные, tsc чист.

- **fix**(scorm): дефолтная палитра шаблона = DS (акцент фиолетовый #7700FF) (2026-07-27) [`c09525a9d8879419cf6259f11673ec2aa3989a56`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c09525a9d8879419cf6259f11673ec2aa3989a56)
  Дефолтные значения палитры «Стандартного» не совпадали с DS: акцент по
  умолчанию был 217° синий (легаси-дефолт в :root и manifest primaryColor), и
  через мост палитры он перекрывал родной DS-фиолетовый #7700FF.
  
  Дефолты приведены к DS (точные HSL из vendor/ui-kit токенов):
  \- primaryColor/--primary: 217 91% 42% (синий) → 268 100% 50% (#7700FF, DS-акцент)
  \- backgroundColor/--background: 225 7% 7% → 222 28% 9% (--ou-bg-page тёмная)
  \- cardColor/--card: 225 14% 14% → 220 23% 18% (--ou-bg-elevated тёмная)
  \- borderColor/--border: 0 0% 22% → 222 18% 22% (--ou-border-soft тёмная)
  
  Значение --primary я НЕ менял в ходе ревизии — 217° синий был дефолтом ДО неё;
  это исправление приводит незаданную палитру к DS. Автор по-прежнему может
  переопределить цвета в «Оформление → Цвета» (мост наложит бренд теста).
  
  Проверено сборкой пакета: --primary=268°, --ou-purple-500=hsl(var(--primary)),
  акцент кнопки/логотипа фиолетовый.

- **fix**(templates): рейл — «Введение раздела» и «Меню карточек» в системные (2026-07-27) [`4b12503280d72781c4302c5dedfe0bb108956e10`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4b12503280d72781c4302c5dedfe0bb108956e10)
  По замечанию: делить по РОЛИ в потоке, а не по редактируемости. «Введение
  раздела» (граница раздела) и роутер «Меню карточек» (выбор темы) — экраны
  фиксированного назначения (каркас прохождения), даже если автор заполняет
  инструкцию/список тем. Перенесены из «Страниц контента» в «Системные экраны».
  Вторая группа переименована «Пользовательские страницы» → «Страницы контента»
  и содержит только свободные страницы автора (Учебная страница, Галерея).
  
  Проверено в браузере: обе группы читаются однозначно. Тест обновлён (31).

- **fix**(web): сцена ученических экранов заполняет окно (Shadow DOM fill-chain) (2026-07-27) [`d1f469038fb92e12ece6195befdc4ff48f8f1bf0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d1f469038fb92e12ece6195befdc4ff48f8f1bf0)
  Веб-приёмка выявила: на веб-хосте сцена НЕ заполняла окно — футер поджимался к
  верху под пустоту (та же исходная жалоба, что чинилась для SCORM). Причина: в
  Shadow DOM цепочка height:100% рвётся — light-DOM обёртка ученика использует
  min-height (tbh-minh-screen/full), а не definite height, поэтому .tb-scene
  {height:100%} схлопывалась до высоты контента.
  
  \- template-screen.tsx (non-shell): host → display:flex column, промежуточный
    screen-div и сама сцена → flex:1 min-height:0 — воспроизводит foundation SCORM
    (#app&gt;*{flex:1}). No-op для превью (хост без constraint высоты = по контенту);
  \- take-test.tsx: обёртка старта была tbh-minh-screen + tbh-wfull (без колонки/
    fill, в отличие от обзора/итогов/результатов) → tbh-col + tbh-fill.
  
  Проверено вживую (Playwright, :8081): старт и вопрос — сцена=окно, футер по нижнему
  краю (footAtBottom), шапка сверху.

- **fix**(scorm): карта вопросов по эскизу — ряд, легенда, счётчик, is-flagged (2026-07-26) [`b2ca315eabc54e6df7a8af47892bb07e9476d396`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b2ca315eabc54e6df7a8af47892bb07e9476d396)
  Карта вопросов (индикатор прогресса) в шапке была столбцом с легендой под точками,
  порядок/метки не по эскизу, счётчик без «из {всего}». Привёл к эскизу: один ряд
  (точки + легенда + счётчик в одну строку), легенда «пропущен · текущий · не отвечен ·
  отвечен», счётчик «отвечено N из {total} · пропущено K». Статус «пропущен» — DS
  is-flagged (маркер), а не is-skipped; легенда review синхронизирована.

- **fix**(scorm,web): сцена заполняет окно — вернуть опору высоты (2026-07-26) [`add3d6c4e37f7b857d803669e80fa646d7df70a4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/add3d6c4e37f7b857d803669e80fa646d7df70a4)
  Сцена (.tb-scene{height:100%}) сжималась по контенту — футер уезжал вверх, под ним
  пустота, скролл на внешнем элементе — потому что цепочка html→body→.container→#app
  потеряла height:100% (была в удалённом base.css). Вернул фундамент в theme.css
  (#app — флекс-колонка на 100%, тело растягивается) и поправил :host в
  template-screen.tsx (min-height→height) для веб-хоста и превью. Проверено в плеере:
  scene=viewport, футер внизу, скролл уходит внутрь тела.

- **fix**(scorm): вернуть page-content слот + стили роутер-хаба/скелета в theme.css (2026-07-26) [`67aecb9023f909c5ed1f5665ddabee6600a10c65`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/67aecb9023f909c5ed1f5665ddabee6600a10c65)
  Генерик-макет `content.html` — это page-content шелл, куда рантайм и превью
  вставляют карточки роутера и скелет контент-страницы; моя сцен-ревизия выкинула
  слот, из-за чего роутер-хаб не монтировался (fallback в #app) и предпросмотр
  «Меню карточек» был пуст. Вернул сцен-шелл со слотом. Живые стили shared-билдеров
  (.router-* хаба, .content-page/.content-placeholder скелета, .card/.question-text)
  перенёс в theme.css на токенах DS; кнопку «Завершить» роутера — с .btn на .ou-btn.

- **fix**(preview): вопросы предпросмотра рендерятся сцен-эмиссией, а не легаси (2026-07-26) [`3595a9c620ed82157dcfe7b9ca6094ce30815b08`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3595a9c620ed82157dcfe7b9ca6094ce30815b08)
  `buildInteraction` в preview-context эмитил до-сценовую разметку
  (.option/.matching-board/.rank-item) — рантайм давно на сцене `.ou-*`; base.css
  это маскировала, после её удаления «Сопоставление»/«Ранжирование» в предпросмотре
  осыпались в сырой текст. Теперь превью делегирует в ту же shared сцен-эмиссию
  (renderSingleChoice/renderMultiple/renderRanking/renderMatching) — предпросмотр
  совпадает с рантаймом.

- **fix**(web): не дублировать сцену при перерендере template-screen (2026-07-26) [`7a0fbebcfbbe8eaec8189ce6e4f9829a82692d53`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7a0fbebcfbbe8eaec8189ce6e4f9829a82692d53)
  `querySelectorAll(":scope &gt; …")` на ShadowRoot не находит ничего — у `:scope`
  нет опорного элемента на DocumentFragment, поэтому очистка предыдущего рендера
  не срабатывала: каждый перерендер (новый объект контекста, выбор ответа, смена
  темы) добавлял ещё одну копию сцены, и на живом веб-хосте экран задваивался.
  Очистка теперь перебирает дочерние узлы напрямую, сохраняя только постоянный
  таблицу стилей DS. Регрессионный тест: три перерендера оставляют одну сцену
  (jsdom воспроизводит поведение :scope на ShadowRoot).

- **fix**(template): экран перехода — смена уровня в теме, без вердикта ответа (2026-07-26) [`0bfbb062de4380a45eeec5264bb6d93ecef7cf27`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0bfbb062de4380a45eeec5264bb6d93ecef7cf27)
  system.transition теперь констатирует СМЕНУ УРОВНЯ в текущей теме: эйброу «Тема
  «...»», заголовок «Сложность повышена/понижена/Уровень зафиксирован», подпись —
  не вердикт ответа и не переход между темами (flat-адаптив отложен). buildTransitionContext
  переписан (topicName, title=смена уровня), CtxTransition обновлён, макет на сцену
  (центр-колонка + иконка уровня), SCORM отдаёт topicName. Приёмка через харнесс.

- **fix**(template): единый состав обратной связи в итогах (стандарт <-> адаптив) (2026-07-26) [`453954f5c6ce94ca143cecc746d6b23ba112da15`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/453954f5c6ce94ca143cecc746d6b23ba112da15)
  Per-topic состав фидбэка сведён к одной форме {feedback, recommendedCourses,
  recommendedEvents} + флаги для обоих режимов (обратная связь — свойство настроек
  теста, не режима). context.ts: CtxTopicFeedback, оба вью наследуют. result-context:
  buildTopicFeedbackView, TopicInput/AdaptiveTopicInput наследуют TopicFeedbackInput.
  Веб-адаптер отдаёт per-topic recs, SCORM-адаптив: recommendedLinks-&gt;Courses + events.
  Макеты и тесты — на новые поля. Гард 6.1: составы совпадают.

- **fix**(scorm): gate setShown переключает и атрибут hidden, тесты стены на сцену (2026-07-26) [`42881edba047c5f0198a2f793ee33810a5b44aaa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/42881edba047c5f0198a2f793ee33810a5b44aaa)
  Ветки system.blocked прячутся атрибутом hidden (и scene-правилом [hidden]); один
  style.display мог оставить показанную ветку скрытой атрибутом. setShown теперь
  снимает/ставит hidden. Тесты блок-стены — селекторы на .tb-scene/[data-testid].

- **fix**(docker): не терять владельца .env при деплое (2026-07-26) [`90805511ded87a86567e29629a0a2e9c270e76a6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/90805511ded87a86567e29629a0a2e9c270e76a6)
  upsert_env писал через mv — env-файл становился новым inode с владельцем root, и приложение (UID 1500, 0640) теряло чтение; ломало каждый тестовый деплой, где выравнивание клона переписывает DATABASE_URL/ENCRYPTION_*. Теперь запись идёт сквозь оригинал (cat &gt;), а seal_env_file фиксирует владельца и режим после каждого шага, пишущего файл. README — как чинить руками.

- **fix**(ds): центрировать подпись загрузчика по вертикали (2026-07-26) [`da7344c92797af66c8d4cf11fcf29299958ab3bf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/da7344c92797af66c8d4cf11fcf29299958ab3bf)
  .ou-uploader__cta — span (кликом служит вся зона), сам текст не центрировался в фиксированной высоте и прилипал к верхней строке. Правка в обе копии DS-CSS.

- **fix**(learner): обвязка ученических экранов берёт цвет у шаблона, а не литералом (2026-07-24) [`f59c1a32fd1f7a575cba27fb47c9451d1e255971`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f59c1a32fd1f7a575cba27fb47c9451d1e255971)
  Кнопки хоста были синими (#2563eb) инлайном, а блок обратной связи —
  зелёным/красным литералом: ни то ни другое не следовало ни за палитрой DS, ни за
  палитрой шаблона, и под брендированным экраном читалось как чужое. Теперь цвет
  приходит из палитры шаблона (cssVars на внешней обёртке) с токенами DS в
  качестве фолбэка.
  
  Побочно: у адаптивных кнопок className был строкой утилит Tailwind, удалённого из
  проекта, — весь их вид держался на том самом инлайновом фоне. Заменено на
  .tbh-primarybtn.
  
  Замер в браузере: фон кнопки rgb(119,0,255) = --primary шаблона (268 100% 50%),
  текст из --primary-foreground; инлайнового синего в DOM не осталось.
  
  В take-test.tsx и его тесты попала также правка PRD-19 из этой же сессии: обзор
  раздела показывается по общему правилу (shared/flow/review-gate) и в роутер-режиме
  тоже, а когда действовать в обзоре нечем — раздел уходит сразу в свои итоги.

- **fix**(debug-player): поверхности сцены из токенов DS, а не белым литералом (2026-07-24) [`da6371c0af04009cff06bd1ab832881cfc7bb55b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/da6371c0af04009cff06bd1ab832881cfc7bb55b)
  Тему приложение переключает классом .ou--dark на body — литерал #fff его не
  слышал, и в тёмной теме карточка пакета с iframe оставались белыми пятнами.
  Замер в браузере: светлая rgb(255,255,255), тёмная rgb(26,31,43).

- **fix**(prd-22): показывать недоступный вариант и на системных страницах (2026-07-24) [`a64c36f17ab174eeab2f77d16a3b2a1b12bb1f82`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a64c36f17ab174eeab2f77d16a3b2a1b12bb1f82)
  Список тестов считает страницы ЛЮБОГО вида, а в дровере признак был сужен до
  авторских: точка на вкладке «Структура» проверяла kind === "info", метку в строке
  рисовал только AuthorPageRow. Из-за этого тест с системной страницей, привязанной
  к выпавшему варианту, светил треугольником в списке и выглядел чистым в редакторе
  — автору нечего было открыть по предупреждению.
  
  Живой пример: тест со страницей вида questions на ключе questions.e-migr-4,
  которого стандартный шаблон не объявляет. Теперь его видно во всех трёх местах —
  точка, метка в строке и баннер сверху — с одинаковой формулировкой.

- **fix**(prd-24): печатать потолок баллов под каждым вариантом (2026-07-24) [`40f1132479502d18575a722926868329f5a95cbd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/40f1132479502d18575a722926868329f5a95cbd)
  Подсказка «макс. N баллов» была одна на весь блок «По вариантам» и стояла после
  последней строки — читалась как относящаяся к последнему варианту и не говорила,
  чей это потолок. Ветка «перечислить поимённо, если максимумы разные» проблему не
  решала: при совпадающих максимумах имена не печатались вовсе.
  
  Теперь строка своя у каждого варианта, в колонке «Порог» под его полем, и только
  там, где потолок имеет смысл — у типа «Сумма баллов». У процента её нет.
  
  Аннотация утверждённого эскиза (prd7-editor-settings-tab, «подсказка с достижимым
  максимумом баллов ИМЕННО этого варианта») требовала именно этого — расходился код,
  поэтому эскиз не менялся, только CSS-раскладка подсказки в обеих копиях.

### Documentation

- **docs**(prd-28): внешний участник — признак учётной записи, не роль (2026-07-30) [`6bf53013f78b263cfc98aeff38c0dc2f8faca81b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6bf53013f78b263cfc98aeff38c0dc2f8faca81b)

- **docs**(roadmap): раздел технического долга, первым пунктом пересоздание сессии (2026-07-30) [`e95fe414fac002d544e7e342b4c3cd6ed3d5b19b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e95fe414fac002d544e7e342b4c3cd6ed3d5b19b)

- **docs**(access): этапы 3 и 4 плана ссылки-приглашения завершены (2026-07-30) [`b8afda83919141ae4779b44159539969ade01229`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b8afda83919141ae4779b44159539969ade01229)

- **docs**(access): приёмка нашла выходы за границы в действиях «назад» (2026-07-30) [`7baf0c6f70954d84edfeb6f389d9c0f4299a3645`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7baf0c6f70954d84edfeb6f389d9c0f4299a3645)

- **docs**(prd27): эскиз отчёта согласован — перенос в approved (2026-07-30) [`6391508484f57317b4b260874b551279d1893411`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6391508484f57317b4b260874b551279d1893411)
  Ссылки внутри поправлены под новый уровень вложенности.

- **docs**(access): выход за границы ссылки ведёт на форму входа, экран-заглушка снят (2026-07-30) [`89d067eba765aa214bb35f352073a173d63312ce`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/89d067eba765aa214bb35f352073a173d63312ce)

- **docs**(wireframes): эскиз экрана «ссылка открывает только этот тест» (2026-07-30) [`2e75fbe514982e7b570232249d4a1a0ed0bc839b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2e75fbe514982e7b570232249d4a1a0ed0bc839b)

- **docs**(prd27): эскиз выбора варианта отчёта и его предпросмотра (Фаза 0) (2026-07-30) [`badcfddcd6065388627dbac829c2e44dae2d74bc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/badcfddcd6065388627dbac829c2e44dae2d74bc)
  Дельта — одна карточка в блоке обратной связи вкладки «Настройки» и одно
  модальное окно. Окружение (карточка «Общая обратная связь теста», оболочка
  редактора, вкладки, подвал) перенесено 1:1 из approved/prd7-editor-settings-tab
  и не меняется; `tb-feedback-block` и `tb-link-row*` не переобъявляются — они
  приходят из подключённого tb-components.css.
  
  Шесть состояний: вариант и поля, смена варианта с перечнем теряемых значений,
  шаблон без вариантов (деградация на «Стандартный»), предпросмотр в двух
  исходах, адаптивный тест со своим типом. Таблицы пояснений связывают каждый
  элемент с FR и с моделью данных.
  
  Страница отчёта внутри предпросмотра — эскизный фрейм: её вёрстка принадлежит
  ШАБЛОНУ и эскизом не задаётся (спецификация §6.2 требует воспроизвести
  текущий вид при переносе в макеты «Стандартного»).
  
  Приёмка в браузере: DS-контролёр без замечаний, все шесть состояний
  отрисованы и сверены, переключатели состояний и тем работают. По пути
  исправлено: пять выдуманных мной DS-классов заменены на существующие
  (ou-card__subtitle, ou-formfield__desc, ou-banner__ico, ou-banner__desc),
  несуществующие размеры --m у textarea и banner убраны, ou-skeleton в DS нет —
  заменён эскизным фреймом, открытое меню селектора перекрывало предупреждение
  ради которого состояние и существует.

- **docs**(prd-27): среда стилей отчёта — контракт вместо запрета (2026-07-30) [`a6722370a91bcd8e0dc456ba79a00496e8fdd1e9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a6722370a91bcd8e0dc456ba79a00496e8fdd1e9)
  В §6.3 был запрет («макет не вправе опираться на theme.css и tb-scene»)
  без ответа, чем тогда верстать. Заменён на исчерпывающий контракт.
  
  Разбор фактического положения: дизайн-система загружена в ГЛАВНЫЙ документ
  у обоих хостов (веб импортирует vendor/university-rt.css глобально, пакет
  вкладывает вендоренную DS в styles.css), поэтому .ou-* в контейнере отчёта
  работают везде. Расходится только CSS ШАБЛОНА: в пакете он в главном
  документе, а на вебе внедряется внутрь Shadow DOM с переписыванием
  :root/body/.ou в :host. Правила сцены при этом неймспейснуты под .tb-scene
  и для отчёта инертны, пока макет сам их не позовёт.
  
  Отсюда контракт: доступны компоненты DS, токены и брендинг теста
  переменными на контейнере (тот же мост палитры, что у сцены) и собственный
  styleFile варианта; запрещены слой сцены и селекторы документа. Запрет
  сделан исполнимым: корневой класс tb-report, вложенность селекторов
  styleFile в .tb-report и отсутствие tb-scene проверяются СТАТИЧЕСКИ при
  валидации манифеста (FR-25) — иначе такой макет заработает в LMS и
  сломается в браузере, что видно только на живой приёмке (риск R-1a).

- **docs**(prd-27): шаблон отчёта о результатах — часть шаблона теста (2026-07-30) [`f9e200839f62aee5cec33025bc8f4253486f9e60`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f9e200839f62aee5cec33025bc8f4253486f9e60)
  Отчёт остался единственным ученическим артефактом вне модели шаблонов:
  вёрстка страницы живёт в коде (shared/report/report-html.ts) инлайн-стилями
  с зашитыми цветами и подложками, поэтому разработчик шаблона не может
  изменить структуру отчёта, а автор теста — ничего в нём выбрать.
  
  PRD вводит два вида варианта в манифесте (report и report.adaptive —
  разные, а не варианты одного вида), макет варианта через общий рендерер,
  редактируемые поля через settings[] PRD-22, выбор варианта и правку полей
  в блоке обратной связи настроек теста, предпросмотр на структуре
  редактируемого теста и деградацию на макет «Стандартного» с брендингом
  активного шаблона.
  
  Три определяющих решения согласованы (D-1..D-3), альтернативы и причины
  отклонения записаны. Реализация не начата; UI — только после эскизов.

- **docs**(wireframes): эскизы итогов приведены к реализации (2026-07-30) [`0fd4ac145ae045d934327dfb99e2fdada7da9589`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0fd4ac145ae045d934327dfb99e2fdada7da9589)
  Две расходимости после правок этой сессии:
  
  \- в шапке всех трёх экранов итогов стояло «Обязательный курс · Попытка 1
    из 2»; счётчик попытки из шапки итогов убран (параметрам прохождения там
    не место), а «Обязательный курс» продукт никогда и не печатал — поля
    категории у теста нет;
  \- в адаптивных итогах три пилюли шли тремя цветами по высоте уровня.
    Теперь подтверждённые уровни одного тона, а третья карточка показывает
    второе состояние — «Минимально требуемый уровень не подтверждён». Её
    содержимое (текст плюс две рекомендации) и есть ветка непройденной темы,
    так что карточка встала на место и по смыслу.

- **docs**(prd-26): статус спецификации — реализован, результаты проверок в отчёте (2026-07-30) [`9b86657269252fcc83066e6d6805f130c2d391bd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9b86657269252fcc83066e6d6805f130c2d391bd)
  npm run check чист, npm run test:it 34 из 34, полный набор прошёл.
  
  Порог покрытия по ветвям НЕ достигнут: 79.59% при норме 80%. Дефицит
  атрибутирован построчно и не относится к этой задаче: собственные модули
  PRD-26 покрыты на 95-100%, а из строк, дописанных внутрь чужих больших
  файлов, непокрытыми остались ДВЕ ветви (иконка типа в отладчике). Крупнейшие
  непокрытые поверхности — start-pages-section.tsx и variant-preview-picker.tsx
  из трека PRD-22. Решение владельца: закрыть PRD-26 как есть, покрытие чужих
  поверхностей ведётся своим треком.

- **docs**(prd-25): статус — реализовано, приёмка пройдена (2026-07-30) [`87a522ea2859d4ee53700a552365029f511d1522`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/87a522ea2859d4ee53700a552365029f511d1522)

- **docs**(prd-26): отчёт о приёмке типа вопроса «шкала» (2026-07-30) [`ea111b0a1ea01840ec49e55264bbaede963bae62`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ea111b0a1ea01840ec49e55264bbaede963bae62)
  Импорт настоящей книги опросника Маслача сервисом против dev-базы: 22 вопроса
  типа scale, 3 шкалы, показатель, 132 вклада, ноль ошибок. Пакет собран тем же
  ассемблером, что использует роут выгрузки, и проигран в полном Chromium через
  CDP; веб-хост пройден учащимся по штатному magic link.
  
  Ключевое доказательство сквозной работы: индекс выбранной градации доехал до
  движка шкал PRD-5 — reduced_personal_accomplishment.raw = 2 после ответа
  «Редко». Адаптив проверен на двух ширинах: 620px даёт вертикальную раскладку,
  1440px горизонтальную.
  
  Токен и назначение, созданные для прогона, удалены; тест возвращён в черновик.

- **docs**(prd-25): состав секций менеджера по факту приёмки; FR-02 нормативен (2026-07-30) [`9320a22368f19f2139557b06eaba1e384dc9f423`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9320a22368f19f2139557b06eaba1e384dc9f423)

- **docs**(templates): план ревизии шаблона «Сертификация» под модель стандартного (2026-07-30) [`f7f9f82396a7857522b39edc980ea6b336a65c02`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f7f9f82396a7857522b39edc980ea6b336a65c02)
  Шаблон был написан под контракт, действовавший до ревизии стандартного шаблона
  на DS и модель «сцена», и за ней не пошёл. План фиксирует разбор 11 дефектов —
  главный из них непроходимый экран вопроса: строку навигации теперь строит
  РАСКЛАДКА из state.nav, а рантайм только привязывает обработчики к
  .tb-scene__foot, которого в раскладке нет.
  
  Целевое решение: раскладки становятся побайтовыми копиями стандартных, вся
  дизайн-специфика уезжает в theme.css, а от повторного отставания страхует
  юнит-тест паритета.

- **docs**(prd-26): шкала в шаблоне книги и справке импорта (2026-07-30) [`6a55ad2ba89bd862d5619b76cb3fb881deda45a3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6a55ad2ba89bd862d5619b76cb3fb881deda45a3)
  Лист-справка книги: тип scale в перечне типов, формат градаций, правило
  пустой ячейки правильных ответов, неприменимость колонки следования
  вариантов, «веса» для шкалы и запрет «ступеней» с объяснением причины.
  
  Лист «Пример» получил рабочий опросник целиком, а не одну строку: вопрос-шкалу
  q7 в разделе «Самооценка» без порога, шкалу-накопитель «Нагрузка» с диапазонами
  и пять строк вкладов по градациям. Ячейка правильного ответа ПУСТАЯ — заполнив
  её в примере, мы научили бы автора ровно противоположному.
  
  Подсказки роута выгрузки вопросов дополнены строкой примера для scale.
  
  Тест «строки-примеры проходят настоящий импорт без единой ошибки» покрывает
  теперь и опросник; добавлена проверка связности примера — градации ссылаются
  на существующую шкалу.

- **docs**(prd-26): спецификация, эскиз и план типа вопроса «шкала» (2026-07-30) [`21e20427a740bd39431a140c290cb0c808acf642`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/21e20427a740bd39431a140c290cb0c808acf642)
  Новый тип вопроса scale: упорядоченные градации, один выбор, опциональный
  правильный ответ (выключен — измерительный режим без баллов, только вклад
  в шкалы PRD-5). Рендер — существующий Stepper дизайн-системы в новом режиме
  выбора; новый компонент не вводится.
  
  Миграция базы не требуется: questions.type объявлена как text без CHECK.

- **docs**(references): исходный textSanitizer как референс типографики (2026-07-29) [`322d3f6a7a87116983f15a2fe93b070134ddb0e1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/322d3f6a7a87116983f15a2fe93b070134ddb0e1)
  Модуль обработки текста и его словарь «висячих» слов — исходник, по которому
  сверяется наша канонизация и типографика полей страницы. Лежит в референсах:
  кодом не подключается, но правила берутся дословно из него.

- **docs**(templates): вводная часть и быстрый старт руководства по шаблонам (2026-07-29) [`133c18b1527ceea17ac8e9fc8b5518f8dd802cd6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/133c18b1527ceea17ac8e9fc8b5518f8dd802cd6)
  Руководство и спецификация уходят наружу — разработчику шаблона, который
  не видел ни кода, ни внутренних PRD. Поэтому:
  
  \- руководство открывается объяснением, что делает сервис, что такое шаблон,
    как ядро рисует один экран и из каких экранов состоит прохождение, и ведёт
    читателя через сборку минимального шаблона с нуля (12 шагов до ZIP);
  \- ссылки на номера PRD и фазы поставки убраны из обоих документов: снаружи
    они ничего не значат;
  \- в спецификации появились история версий и раздел «Перспективные изменения»
    вместо описания фаз поставки.

- **docs**(security): решения по области и жизни ссылки-приглашения (2026-07-29) [`38c52885b61ee070686dd454b9ff22105431bf7d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/38c52885b61ee070686dd454b9ff22105431bf7d)
  Зафиксированы ответы на две развилки плана.
  
  Жизнь токена — не одноразовость и не привязка к устройству, а три
  ограничения одновременно: не истёк срок действия, не исчерпаны попытки,
  тест не пройден успешно. Нарушено любое — ссылка больше не открывается.
  Многоразовое погашение в пределах жизни разрешено намеренно: возврат к
  тесту и возобновление незавершённой попытки — штатные сценарии. Срок
  действия остаётся обязательным предохранителем: у теста может не быть ни
  лимита попыток, ни правила прохождения, и тогда два других условия не
  наступят никогда.
  
  Действующие ссылки, выданные привилегированным аккаунтам, точечно не
  отзываем — закрываем кодом. Отмечено следствие порядка работ: экспозицию
  снимает не этап 3, а этап 1: после ограничения области такая ссылка
  перестаёт быть ключом от аккаунта и становится ключом от одного теста.
  Этап 3 прекращает выдачу новых.
  
  Добавлена проверка данных по копии прод-БД: через группы эскалации нет
  (единственная группа состоит только из учащихся), но ссылки выдавались
  привилегированным аккаунтам прямым назначением — одна администраторская
  действует. Поэтому дефект переформулирован: фильтр по ролям получателя
  нужен при любом назначении, а не только групповом.

- **docs**(security): план ограничения области ссылки-приглашения (2026-07-29) [`436fe8786dc8f33fbf170f09ff1ff3f3e2904487`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/436fe8786dc8f33fbf170f09ff1ff3f3e2904487)
  Ссылка из письма о назначении (/access/&lt;token&gt;) выполняет полноценный
  вход: ставит session.userId и ничем сессию не помечает. Обрезав у ссылки
  хвост learner/test/&lt;id&gt;, получатель оказывается в кабинете пользователя.
  
  План фиксирует пять дефектов (неограниченная область сессии; токен
  многоразовый — при погашении не выставляется ни revoked_at, ни used_at;
  срок по умолчанию 30 дней либо до due_date; назначение на группу выдаёт
  вход в привилегированный аккаунт, если участник не только learner;
  погашение логируется без IP/UA, отзыв не выведен в интерфейс), целевое
  правило «ссылка — не вход в систему, а доступ к одному назначению» и
  четыре этапа с порядком.
  
  Не согласован. Требуют решения: проверка на проде назначений на группы с
  привилегированными участниками (приоритет этапа 3) и развилка этапа 2 —
  строгая одноразовость против повторного погашения с того же устройства.

- **docs**(prd-6): same-origin — ожидаемый режим, а не проверяемый инвариант (2026-07-29) [`cacdfa20030301d496e1a298db8d7a13b01776ae`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cacdfa20030301d496e1a298db8d7a13b01776ae)

- **docs**(prd-6): effectiveToday в контракте состояния и уточнение веток деградации (2026-07-29) [`5f4b7ef159205bf6ef6b19f6cde04b8ff86c955f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5f4b7ef159205bf6ef6b19f6cde04b8ff86c955f)

- **docs**(prd-6): результат приёмки доверенной даты на локальном стенде (2026-07-29) [`b8d5de5780916770241cf9412c5a1258822ef067`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b8d5de5780916770241cf9412c5a1258822ef067)

- **docs**(prd-6): уточнение спеки — общий бюджет гейта и запрос портала для suspend_data (2026-07-29) [`ef2d5c9f82499963ce7cd42727c475cb0fc7800c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ef2d5c9f82499963ce7cd42727c475cb0fc7800c)

- **docs**(prd-6): спека и план доверенного источника даты кулдауна (2026-07-29) [`1a37e197c090af7cd05644a895ea7572022b776e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1a37e197c090af7cd05644a895ea7572022b776e)

- **docs**(drizzle): как чинить журнал миграций, отставший больше чем на бейзлайн (2026-07-29) [`c09df9878e953c4e5057e3466620c89f5b91709c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c09df9878e953c4e5057e3466620c89f5b91709c)

- **docs**(prd-25): эскиз главной согласован (2026-07-29) [`4470ac1dcb4f8f94d3812afdf3592be0cd5de235`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4470ac1dcb4f8f94d3812afdf3592be0cd5de235)

- **docs**(prd-25): эскиз домашней страницы (черновик) + уточнение FR-17 (2026-07-28) [`b16d12f4ec90b0601cf0ce67053c8d1d4fad2bd0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b16d12f4ec90b0601cf0ce67053c8d1d4fad2bd0)

- **docs**(prd-25): план реализации домашней страницы (2026-07-28) [`484f1eb4bdea8e64961b4fb52db24d60e26db201`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/484f1eb4bdea8e64961b4fb52db24d60e26db201)

- **docs**(prd-25): спецификация домашней страницы (2026-07-28) [`02a21bb8a9df206e4841f001b1c3cc66e87b5406`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/02a21bb8a9df206e4841f001b1c3cc66e87b5406)

- **docs**(plan): план приведения стандартного шаблона к эскизам (2026-07-28) [`89040a40d3a81a94819e24752e468c933bf0c21e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/89040a40d3a81a94819e24752e468c933bf0c21e)
  План поэкранной сверки реализации «Стандартного» шаблона с согласованными
  эскизами (docs/wireframes/prohozhdenie) — источник истины, перечень универсальных
  дефектов и по-экранные правки. Рабочий документ трека ревизии шаблона.

- **docs**(plan): базлайн пакета до вендоринга DS (845 КБ, без ou-/шрифта) (2026-07-26) [`6f4242dbe0b99a3f449085d4497eb91b85f9eeab`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6f4242dbe0b99a3f449085d4497eb91b85f9eeab)

- **docs**(plan): план реализации ревизии шаблона на ui-kit (фазы 0-6) (2026-07-26) [`67c42a4854e1391678cca68d83185c8b6cd02e71`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/67c42a4854e1391678cca68d83185c8b6cd02e71)

- **docs**(template): эскизы прохождения теста на ui-kit + спецификация ревизии (2026-07-26) [`0e084642023984b49eabbdecb2c3146aaba98ef1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0e084642023984b49eabbdecb2c3146aaba98ef1)
  Ревизия шаблона «Стандартный»: перевод ученических экранов на дизайн-систему и модель «сцена». В docs/wireframes/prohozhdenie — эталонная разметка ui-kit (из архивов заказчика) плюс отрендеренные статикой все экраны потока (5 типов вопросов, роутер, старт/обзор/итоги/результаты, контент info/gallery, введение раздела, системные blocked/transition, адаптивные итоги). Спека spec-standard-template-uikit-revision фиксирует канонический состав из движка (система vs автор), модель сцены, мост палитры в токены DS и выявленные несогласованности рантайма. Референсы — исходные материалы заказчика.

- **docs**(ds): правило источника цвета и как оно проверяется (2026-07-24) [`38cd290e2578e9b12ca784084441bac7e7a24367`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/38cd290e2578e9b12ca784084441bac7e7a24367)
  Раздел «Цвет» в руководстве по дизайну: два источника (токены DS для авторского
  интерфейса, палитра шаблона для ученических экранов и обвязки), когда допустим
  литерал и чем это проверяется. Отдельно записана гоча, стоившая отладки: алиасы
  объявляются на `.ou`, а не на :root, иначе ссылки на семантические токены DS не
  разрешаются и цвет молча становится цветом текста.
  
  Здесь же план, по которому шла работа.

- **docs**(changelog): 2.9.0-beta (2026-07-24) [`e8ee38be494bdb0d96411f1c3b0820246848055c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e8ee38be494bdb0d96411f1c3b0820246848055c)
  Сгенерировано по тегам. Заодно заполнены пропущенные секции 2.7.1-beta и
  2.8.0-beta — их релизы прошли без обновления файла.

## [2.9.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.8.0-beta...v2.9.0-beta)

### Features

- **feat**(editor): отделить в баннере ограниченные стили от удалённой разметки (2026-07-24) [`10ec5178dcde9d1cf46fd34fc5cd880a3452000b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/10ec5178dcde9d1cf46fd34fc5cd880a3452000b)
  Диагностика после сохранения говорила одно: «следующие элементы были удалены
  как небезопасные». Для скоупленного CSS это неправда — стили работают, они лишь
  не выходят за блок страницы, и в списке удалённого читались бы как потеря
  данных. Баннер вынесен из формы в отдельный компонент (вёрстка и классы
  прежние) и показывает два раздела: удалённое и ограниченное, с числом правил.
  
  Вставка в редакторе нормализуется тем же скоупом, что применяют сохранение и
  сборка, — автор видит итоговый CSS сразу, а не узнаёт о нём из пакета.

- **feat**(prd-24): отладчик показывает выданный вариант и применённый порог (2026-07-24) [`2160fbd2bd08d685a8405f3469823e78edd542b8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2160fbd2bd08d685a8405f3469823e78edd542b8)
  Инспектор угадывал выпавший вариант, сверяя набор выданных идентификаторов с
  составом форм. Догадка неоднозначна: два варианта могут состоять из одних и тех
  же вопросов, и тогда «Выдача» показывала первый подходящий — не тот, что
  действительно выпал. Теперь берётся пин, который рантайм пишет в состояние;
  вывод по набору остался запасным путём для состояний, снятых до пина.
  
  В результаты по разделам добавлена колонка с порогом, которым тема РЕАЛЬНО
  гейтилась, и меткой выданного варианта под ним. Без этого вердикт темы с
  правилом «по вариантам» нечитаем: видно «не пройден», но не видно, какой из
  порогов применился.

- **feat**(prd-24): лист книги «Пороги вариантов» (2026-07-24) [`5fb386f2fca30a15796f1db2213f194e636b04ac`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5fb386f2fca30a15796f1db2213f194e636b04ac)
  Правило by_variant не помещалось в книгу: на листе «Структура» под порог темы
  отведена одна пара колонок, поэтому экспорт молча вырождал бы правило в «Как у
  теста», а импорт терял пороги.
  
  Пороги вынесены на отдельный лист по образцу «Квоты» — та же кардинальность
  «раздел → N записей» и та же привязка по имени раздела. Ключ варианта в книге —
  НОМЕР, а не formId: вариантов как сущностей в книге нет, они позиционные, и та
  же нумерация уже используется колонкой «Варианты» листа «Вопросы». Экспорт
  переводит formId в номер, импорт — обратно, но ПОСЛЕ сборки набора вариантов:
  раньше номер просто не к чему привязать.
  
  На листе «Структура» тип порога принимает значение «По вариантам» с пустым
  «Порогом». Ошибки: неизвестный номер варианта и раздел не в режиме вариантов —
  ошибки строки; неполное покрытие вариантов — ошибка раздела, чтобы книга не
  служила обходом валидации редактора. Книга без листа импортируется как прежде.

- **feat**(prd-24): раздел «Правила прохождения» умеет пороги по вариантам (2026-07-24) [`7ce810c59336fd31b76c868eb6a30d9bb95955a2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7ce810c59336fd31b76c868eb6a30d9bb95955a2)
  Список правила темы получает четвёртый пункт «По вариантам» — но только у
  темы, которая выдаётся вариантами; у остальных список прежний. При выборе
  пункта пороги сразу сеются на каждый вариант (из прежнего индивидуального
  правила либо из общего правила теста), иначе правило рождалось бы заведомо
  невалидным.
  
  Раскрытый блок — сетка [метка | Тип | Порог] по согласованному эскизу: колонка
  порога тянется до правого края ячейки, поэтому правый край контролов совпадает
  с полем правила темы, а колонка типа выравнивается по самому широкому значению.
  Подсказка показывает достижимый максимум ИМЕННО этого варианта и считается той
  же цепочкой эффективной цены, что и валидация, — иначе подсказка и запрет
  разошлись бы.
  
  Правка набора вариантов теперь идёт через общий переход, который одним
  обновлением модели правит и состав, и правило: новому варианту заводится порог,
  удалённому — убирается, а выключение режима возвращает «Как у теста». Падение
  набора до одного варианта — промежуточное состояние правки, введённые пороги
  при нём сохраняются.

- **feat**(prd-24): модель и валидация правила «По вариантам» в редакторе (2026-07-24) [`150eb8273e537904f899f488aa94a139889421db`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/150eb8273e537904f899f488aa94a139889421db)
  Правило темы получило четвёртый источник by_variant с картой порогов по
  formId; маппер читает его из API и отбрасывает битые записи, чтобы неверная
  пара {тип, значение} не превратилась в гейт, которого автор не задавал.
  
  Валидация закрывает четыре дыры: источник доступен только теме в режиме
  вариантов; покрыт должен быть КАЖДЫЙ вариант (непокрытый молча деградировал бы
  к общему правилу при выдаче); порог не может ссылаться на удалённый вариант;
  процент держится в 0..100.
  
  Абсолютный порог ограничивается максимумом ИМЕННО ЭТОГО варианта, посчитанным
  точно — по той же цепочке эффективной цены, что рисует вкладка «Оценка»
  (переопределение → умолчание секции → умолчание теста → системная 1). Оценка
  «по числу вопросов» была бы заведомо заниженной и отвергала бы законные пороги
  там, где у вопросов есть переопределения цены.

- **feat**(prd-24): манифест объявляет минимальный порог по вариантам (2026-07-24) [`a9f4d2eb7217ac35bf4b13625ccea3bc960af567`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a9f4d2eb7217ac35bf4b13625ccea3bc960af567)
  Сборка приводила правило темы к типу PassRule и читала из него type/value.
  У правила by_variant их нет: порог лежит на каждом варианте, поэтому тема
  получала бы NaN в minNormalizedMeasure и невалидный манифест.
  
  Для такой темы объявляется НАИМЕНЬШИЙ нормализованный порог по её вариантам
  (percent/100 либо value / сумма баллов варианта). Метаданные секвенсинга не
  должны быть строже реально применяемого правила — иначе LMS заблокирует
  обучающегося, который прошёл по порогу своего варианта. Достижимые баллы
  варианта берутся из эффективной цены, а не из вопроса: цена — свойство теста.
  
  Каст в bake снят — правило уезжает в TEST_DATA как авторское, а рантайм
  резолвит его общим движком.

- **feat**(prd-24): SCORM-рантайм пиннит выданный вариант и судит по нему (2026-07-24) [`7f38831165fcf392b44359a6d2ba78eec97ed817`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7f38831165fcf392b44359a6d2ba78eec97ed817)
  Пакет выбирал вариант, но нигде не сохранял, какой именно — из состояния это
  было не восстановить, отладчик и вовсе угадывал вариант сопоставлением набора
  идентификаторов. Теперь выпавший formId пиннится в state.variant.sections и
  доезжает до обоих расчётов (итог и результат раздела), так что тема гейтится
  порогом своего варианта — тем же движком, что и на вебе.
  
  Подпись «Требуется…» больше не читает сырое правило: оно не несёт порога ни
  для by_variant, ни для наследования, поэтому подпись там просто пропадала.
  Источник — resolvedPassRule из результата темы; для попыток, сохранённых до
  этой правки, остался запасной путь на сырое правило.
  
  Состояние сессии без пина (сохранённое раньше) читается под guard и деградирует
  к общему правилу, а не падает.

- **feat**(prd-24): веб-оценка учитывает выданный вариант темы (2026-07-24) [`de44a3de40275a54035002a0396236e7daeab69d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/de44a3de40275a54035002a0396236e7daeab69d)
  Обе точки расчёта (итог попытки и результат раздела) передают движку formId
  из variant_json — тот самый пин, который PRD-17 уже делает при построении
  попытки. Без него тема с правилом by_variant оценивалась по общему правилу
  теста, то есть порог варианта не работал вовсе.
  
  Тесты закрывают три случая: тема зачтена по порогу своего варианта, тема НЕ
  зачтена там, где соседний вариант дал бы зачёт, и попытка без пина (созданная
  до PRD-17) деградирует к общему правилу.

- **feat**(prd-24): агрегатор гейтит тему по выданному варианту (2026-07-24) [`3c005d0651a448adb0507d05a5e1c5a73e4e02bd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3c005d0651a448adb0507d05a5e1c5a73e4e02bd)
  Движок расчёта не знал, какой вариант получил обучающийся, поэтому не мог
  применить порог by_variant. Секция расчёта получила опциональный formId, и
  резолвер вызывается с контекстом выдачи; для тем без вариантов и попыток без
  пина поведение прежнее.
  
  Заодно тема теперь сообщает resolvedPassRule — правило, которое РЕАЛЬНО её
  гейтило (порог выданного варианта, общее правило при наследовании, null у
  негейтированной). Сырое passRule остаётся как было. Это нужно, чтобы подпись
  «Требуется…» строилась из фактически применённого порога и переживала
  сохранение попытки: запись попытки хранит topicResults, но не состав выдачи,
  поэтому пересчитать порог задним числом было бы не по чему.

- **feat**(prd-24): резолвер правила темы учитывает выданный вариант (2026-07-24) [`665f74ac201bdb276f7ec19fb6f73fa4b2e681e9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/665f74ac201bdb276f7ec19fb6f73fa4b2e681e9)
  Правило прохождения темы резолвилось без знания того, какой вариант выпал
  обучающемуся, поэтому один порог применялся ко всем вариантам сразу. Для
  типа «сумма баллов» это прямая несправедливость: варианты расходятся по
  достижимому максимуму (цена вопроса — свойство теста, начисление
  градуированное), и общий абсолютный порог даёт разные шансы.
  
  Добавлен четвёртый источник by_variant: порог на каждый вариант, ключ —
  стабильный formId (не позиция, иначе правка набора переадресует пороги).
  Резолвер принимает необязательный контекст выдачи и возвращает прежний
  ResolvedRule, поэтому за его пределами ни один потребитель не узнаёт о
  новом источнике — оба хоста продолжают звать один и тот же движок.
  
  Неизвестный или отсутствующий вариант (попытка до PRD-17, состояние SCORM
  без пина, удалённый вариант) деградирует к ОБЩЕМУ правилу теста, а не к
  «нет гейта»: тема должна остаться под проверкой, а редактор не даст
  сохранить конфигурацию с непокрытым вариантом.
  
  Схема хранения объявлена в shared/schema.ts — миграция не нужна, правило
  живёт в существующем jsonb-поле topic_pass_rule_json.

- **feat**(scorm-tools): собирать приёмочный пакет под выбранную палитру (2026-07-23) [`670831409d454ac73fdc86ebd48e10471734c8e6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/670831409d454ac73fdc86ebd48e10471734c8e6)
  Без флагов поведение прежнее. `--theme` закрепляет палитру, с которой пакет
  открывается, `--theme-colors` подставляет заведомо чужой цвет на каждую тему:
  без него приёмка бессмысленна — обе палитры были бы просто шаблонными, и по
  экрану не сказать, дошли ли переопределения до отрисовки.

- **feat**(templates): «Сертификация» 1.3.0 объявляет обе палитры (2026-07-23) [`ddf537f9309269d63fee30c897e3e28d44c94413`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ddf537f9309269d63fee30c897e3e28d44c94413)
  Шаблон поставлял светлую и тёмную с самого начала, но не объявлял их, поэтому
  автор теста не мог ни зафиксировать вид теста, ни задать цвет на палитру.
  
  Заодно снята давняя претензия к обложке: `.cover-brand` была залита фиксированным
  оранжевым мимо токена, и стоило автору сменить цвет кнопок, как плашка
  расходилась с остальным экраном. Теперь градиент ведётся от `var(--primary)`,
  объём даёт разная светлота.
  
  Версия поднята: пакет 1.2.1 уже импортирован на стенде, и выпускать другое
  содержимое под тем же номером нельзя. Превью обоих шаблонов перегенерированы
  под изменённый рантайм.

- **feat**(editor): раздел «Цвета» с выбором вида теста и палитрой на каждую тему (2026-07-23) [`1ba3ea0fbeab2642504cd52f8dcf6687c877eac3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1ba3ea0fbeab2642504cd52f8dcf6687c877eac3)
  Цвета выносятся из «Брендирования» в свой раздел: у них логика сложнее переноса
  значения в рендер — наследование из палитры шаблона, формат хранения, значение
  на каждую тему. Отбор идёт по ТИПУ параметра, а не по объявленному в манифесте
  разделу, иначе каждый уже загруженный шаблон пришлось бы переиздавать.
  
  Форму раздела задаёт шаблон, а не выбор автора: одна палитра — плоский список,
  несколько — переключатель «Светлая/Тёмная/Авто» и таблица «параметр × тема».
  Таблица не сворачивается при закреплении темы: иначе подобранная для второй
  палитры раскладка исчезает из виду, хотя ученик её увидит.
  
  Раздел, в который шаблон не положил ни одного параметра, больше не выводится —
  раньше это был пункт рельсы, открывавшийся баннером «здесь ничего нет».
  
  Предпросмотр получил переключатель палитры: закреплённая тема открывается
  своей, «Авто» — первой, остальные смотрятся переключением.
  
  Разбор палитр из стилей переехал в shared — прежняя локальная копия удалена.

- **feat**(ui-kit): заголовок строки в Table (2026-07-23) [`33dcc5e77b08c6192e206560fbbe7ca02fa37a81`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/33dcc5e77b08c6192e206560fbbe7ca02fa37a81)
  Матричной таблице «параметр × значение» первая колонка не данные, а имя
  строки. Без `&lt;th scope="row"&gt;` скринридер зачитывает ячейку, не называя
  параметр, к которому она относится.
  
  Опция колонки `rowHeader` — единственный способ получить это, не собирая
  таблицу руками мимо дизайн-системы.

- **feat**(learner): паритет тем на веб-хосте (2026-07-23) [`5e8111ef9a0b61402a446b4e1651da892c175e3d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5e8111ef9a0b61402a446b4e1651da892c175e3d)
  Серверная сборка экрана делит брендирование надвое: параметры, не зависящие от
  палитры, остаются инлайновыми переменными, цвета уезжают в CSS-блок, а рядом
  едет закреплённая палитра. Оба поля проброшены через все ученические экраны.
  
  Ключевая правка — форма селектора. В теневом корне хост совпадает только с
  функциональной записью `:host(...)`; суффиксная `:host[data-theme="dark"]`
  невалидна, и браузер выбрасывает правило целиком. Наивная замена `:root` на
  `:host` ломала это и для собственных стилей шаблона: «Сертификация» теряла
  тёмную палитру на вебе с самого начала, просто до появления тем это было
  некому заметить. Поймано в реальном браузере, закрыто тестами.

- **feat**(scorm): применять выбранную палитру в рантайме пакета (2026-07-23) [`4657615eeec8e4210704557775f1d436a1ca206a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4657615eeec8e4210704557775f1d436a1ca206a)
  Тема и цвета по палитрам запекаются в TEST_DATA.designSettings, иначе пакет
  перекрашивался бы умолчаниями шаблона и выглядел не так, как автор видел в
  редакторе. У шаблона без тем форма данных прежняя.
  
  В рантайме закреплённая палитра ставится на `&lt;html data-theme&gt;`, а
  переопределения печатаются отдельным `&lt;style id="tb-theme-vars"&gt;` — последним,
  чтобы на равной специфичности выигрывал порядок. При «Авто» атрибут не
  ставится: иначе медиа-запрос шаблона не получит своей очереди.
  
  Цвета темизированного шаблона больше не идут инлайн-свойствами: инлайн бьёт
  любой стиль и намертво фиксирует одну палитру. Инлайн остаётся за тем, что от
  палитры не зависит, — шрифтом и логотипом.
  
  `_init` принимает манифест целиком: без `themes[]` рантайму нечем отличить
  шаблон с палитрами от обычного.

- **feat**(api): хранить вид теста и цвета по палитрам, проверять объявление тем (2026-07-23) [`5fa63fad2fa6b43a89be98cd95c14fd2d6b73de7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5fa63fad2fa6b43a89be98cd95c14fd2d6b73de7)
  Гейт шаблона: `themes[]` проверяется и при загрузке пакета, и при активации
  (PRD-3 NFR-05/06). При загрузке объявление дополнительно сверяется со стилями,
  а шаблон со второй палитрой, но без объявления, получает предупреждение —
  иначе автор о ней просто не узнает.
  
  Хранение теста: `theme` и `paramsByTheme` в design_settings_json. Колонка
  типа JSON, миграция не нужна; у шаблона без тем форма записи не меняется
  вовсе, поэтому сохранённые тесты трогать не пришлось.
  
  Непонятное в запросе отклоняется с 422 и названной причиной — неизвестная
  тема, палитра вне объявленных, нецветовой параметр внутри палитры. Тихое
  отбрасывание опаснее отказа: редактор считал бы палитру сохранённой, а ученик
  видел бы другую.

- **feat**(shared): ядро тем шаблона — реестр, резолвер значений и печать в CSS (2026-07-23) [`ed8ab6357ffd71c1389dd19fa2b6e9d05defa430`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ed8ab6357ffd71c1389dd19fa2b6e9d05defa430)
  Шаблон может поставлять две палитры, но платформе неоткуда было о них узнать:
  одно значение цвета молча перекрашивало обе, и белый фон ложился под почти
  белый текст тёмной палитры.
  
  Закладывается общее ядро PRD-23:
  
  \- themes.ts — закрытый реестр `light`/`dark` и валидатор объявления, включая
    сверку со стилями шаблона: заявленная палитра без токенов дала бы автору
    колонку цветов, которая ничего не красит;
  \- theme-tokens.ts — разбор палитр из стилей, поднят из редактора в shared,
    так как им теперь пользуется и валидатор;
  \- theme-params.ts — деление настроек на «красит одинаково» и «красит по темам»
    плюс правило переезда плоского цвета (FR-17): значение, сохранённое до
    PRD-23, читается как значение обеих палитр и не теряется;
  \- theme-css.ts — печать переопределений правилами той же структуры, что и
    theme.css шаблона. Инлайн-свойства не подходят принципиально: их нельзя
    ограничить медиа-запросом, а именно это и нужно теме.
  
  Оба хоста печатают одним модулем, поэтому расхождению взяться неоткуда.

- **feat**(templates): сетка из 12 раскладок контентной страницы в обоих шаблонах (2026-07-23) [`b7378b0b5deaa2105fdba49324a522ea2a76e9b7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b7378b0b5deaa2105fdba49324a522ea2a76e9b7)
  Повод трека: вариант «Галерея: карточка» был автору недоступен. Отдельного вида
  страницы «галерея» нет — это вариант info со своим макетом, поэтому у обоих
  поставляемых шаблонов теперь одна и та же сетка: обычная страница и страница
  последовательности, каждая в раскладках «текст», «изображение слева» и
  «изображение справа».
  
  Поля описаны в манифесте: placeholders[] дают содержимое, settings[] — свойства
  (идентификатор последовательности, подпись «Далее», фон). Ручные pillsTotal /
  pillCurrent / showBack ушли: точки считает ядро по структуре теста.
  
  Демо-наборы дополнены страницами последовательности, чтобы предпросмотр
  показывал индикатор, а проверка работоспособности видела его отрисовку.

- **feat**(scorm-tools): последовательности в приёмочном пакете и экран перехода в предпросмотре (2026-07-23) [`374ca39e831065888a70234af62bb26b83f5b51a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/374ca39e831065888a70234af62bb26b83f5b51a)
  Вариант с настройкой sequence получает в приёмочном пакете ТРИ соседние страницы
  с одним идентификатором — иначе индикатору нечего считать и проверить его на
  локальном плеере было нельзя.
  
  Офлайн-генератор preview.html научился экрану «Правильно/Неправильно + смена
  уровня»: он связывает верхнеуровневое пространство transition.*, которого нет в
  общих данных, поэтому каждый {{#if}} макета оказывался ложным и экран рисовался
  голой иконкой с пустым заголовком — дефект шаблона, которого нет. Контекст
  строится тем же общим сборщиком, что и в бою.

- **feat**(learner): паритет веб-прогона по последовательностям и файлам шаблона (2026-07-23) [`5424782553b8c160fdd70750825c665fbf8f4c59`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5424782553b8c160fdd70750825c665fbf8f4c59)
  Веб-хост считает точки индикатора тем же общим ядром, что и SCORM-рантайм, и
  получает от сервера макеты всех вариантов, а не только общую обёртку — раньше
  вариант со своим макетом рисовался в вебе обёрткой, и все варианты выглядели
  одинаково. Относительные ссылки в авторском содержимом разрешаются базой,
  которую отдал сервер.

- **feat**(tests): отметка теста со страницами, требующими сопоставления варианта (2026-07-23) [`190d3e39f1d26b6ed08ede5d1f53c82d26044d25`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/190d3e39f1d26b6ed08ede5d1f53c82d26044d25)
  Страницы с недоступным вариантом рисуются через подмену, поэтому в прогоне их не
  отличить, и автор не узнавал, что решение за ним. Пиктограмма в строке теста
  показывает число таких страниц подсказкой и по нажатию открывает редактор сразу
  на «Структуре» — по самой строке автор попал бы на «Состав».

- **feat**(templates): рельс предпросмотра от вариантов манифеста (2026-07-23) [`78b7b33c2bc98ba167b6c6aeea6ce5f9350d36c8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/78b7b33c2bc98ba167b6c6aeea6ce5f9350d36c8)
  Средним уровнем рельса был маршрут, а автор в «Структуре» выбирает ВАРИАНТ.
  Теперь группировка идёт по contentTemplates[], имя берётся из манифеста, и
  несколько демо-страниц одного варианта (так устроена галерея certification)
  показываются как его демонстрации, а не как разные варианты. Экраны, за которыми
  варианта нет (старт, вопросы, результаты, системные), остаются на группировке по
  маршруту.
  
  Точка состояния появилась и на самом варианте — худший статус его экранов; пока
  хоть один экран не проверен, группа остаётся нейтральной, а не объявляет вердикт,
  которого у неё нет.

- **feat**(editor): поля и свойства контентной страницы из манифеста (PRD-22) (2026-07-23) [`279339a98c1a6516788d15f0ce5a3b97e72e604c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/279339a98c1a6516788d15f0ce5a3b97e72e604c)
  Состав полей страницы задаёт исключительно манифест: placeholders[] дают
  содержимое, settings[] — свойства страницы (идентификатор последовательности,
  подпись «Далее», фон). Контрол выбирается по типу из общего реестра, поэтому
  пропущенный тип теперь ошибка компиляции, а не однострочный ввод исподтишка.
  
  Автор задаёт только идентификатор последовательности — число точек и позицию
  считает ядро, и предпросмотр страницы получает её место в последовательности,
  рассчитанное по всей структуре теста.
  
  Редактор открывается на нужной вкладке (initialTab), чтобы список тестов мог
  привести автора прямо в «Структуру», где делается сопоставление варианта.

- **feat**(editor): честная вкладка «Оформление» — цвета шаблона, пояснения, обложки (2026-07-23) [`dc17caf0e6bbc32de5984c2eac66cb223a479814`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dc17caf0e6bbc32de5984c2eac66cb223a479814)
  Шаблон вправе держать палитру целиком в theme.css с default: null (так делает
  certification, чтобы следовать светлой или тёмной теме зрителя). В этом случае
  редактору нечего было показать, и он откатывался на #000000 — все поля читались
  чёрными, пока ученик видел оранжевый бренд. Токены читаются из таблицы стилей
  шаблона, и строка цвета показывает либо «из шаблона», либо действие, которое
  возвращает цвет шаблону.
  
  Формат цвета выводится из МАНИФЕСТА, а не из редактируемого значения: угадывание
  HEX там, где вся палитра объявлена как null, писало #7700FF в шаблон, чей CSS
  собирает hsl(var(--primary)); браузер отбрасывал hsl(#7700FF) как недопустимый, и
  элемент терял цвет целиком. Умолчание — HSL, соглашение платформы.
  
  Параметр показывает описание из манифеста: цвет по имени «Акцентный» ничего не
  говорит о том, где он проявится, и автор теста был вынужден гадать.
  
  Карточка шаблона показывает его СОБСТВЕННУЮ обложку (manifest.assets.preview) —
  схематичный набросок остался запасным вариантом для шаблона без обложки.

- **feat**(ui-kit): RichTextEditor, переворот Popover и палитра цвета на общем слое (2026-07-23) [`0144769e561b608fe22a8ae0cb0177d56e1823e1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0144769e561b608fe22a8ae0cb0177d56e1823e1)
  RichTextEditor — поле с режимами ввода (plain / rich / html): типы полей PRD-22
  требуют от редактора богатого ввода, а санитайзер приходит пропсом, чтобы
  компонент не знал о правилах продукта. Вставленный фрагмент нормализуется сразу,
  иначе автору сообщали бы при сохранении о разметке, которую он не набирал и не
  видит.
  
  Popover получил flip (у нижнего края окна попап уезжал за границу) и chrome:
  слой с готовым оформлением — палитра цвета, градиент — переезжает на общее
  позиционирование, не получая вторую рамку поверх своей.
  
  ColorPicker снят с собственного позиционирования: нарисованный на месте, он
  считался в координатах СТРАНИЦЫ внутри произвольного позиционированного или
  обрезающего предка формы, поэтому уплывал от своей кнопки и обрезался краями
  формы.
  
  Бандл DS пересобран; обе копии (vendor/ui-kit/css и client/src/styles/vendor)
  приведены к одному источнику — сломанный терминатор комментария, из-за которого
  раньше держали локальную заплату, исправлен в источнике.

- **feat**(api): помечать тесты со страницами, чей вариант недоступен (2026-07-23) [`4d9153801d6cd0b17e26a1174c4eaf434395d46d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4d9153801d6cd0b17e26a1174c4eaf434395d46d)
  Подмена варианта вариантом по умолчанию делает прогон исправным, но и незаметным:
  ничто в тесте больше не выдаёт, что решение автора не принято. Список тестов
  теперь считает такие страницы и отдаёт их число строкой, чтобы автор узнал о
  висящем сопоставлении там, где он выбирает тест.
  
  Привязки страниц берутся ОДНИМ запросом на всю страницу списка: запрос на каждую
  строку стоил бы списку скорости.
  
  Экран контента для веб-хоста заодно отдаёт макеты вариантов и базу файлов:
  вариант с собственным layoutFile обязан рисоваться через ИМЕННО его макет, а
  раньше веб получал только общую обёртку — и все варианты сетки выглядели
  одинаково.

- **feat**(api): отдавать файлы шаблона вебу и проверять типы полей при активации (2026-07-23) [`0005d73d08ecdbbd62dbebaf763019732aa1d936`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0005d73d08ecdbbd62dbebaf763019732aa1d936)
  Авторское содержимое вправе ссылаться на файлы шаблона относительным путём. В
  SCORM-пакете эти файлы лежат рядом со страницей, а в вебе им некуда было
  указывать: статикой отдаются только /uploads и /docs, а встроенные шаблоны не
  лежат ни там, ни там. Маршрут /api/templates/:id/assets/* закрывает пробел для
  обоих видов шаблонов; выход за каталог шаблона отклоняется, манифест и макеты
  через него не отдаются — у них свои эндпоинты. Читать может любой вошедший:
  ученику нужны картинки, а администратор смотрит шаблон ДО активации, и
  ограничение активными оставило бы каждый черновой предпросмотр с битыми медиа.
  База подставляется в макеты в предпросмотре, в bundle и на экране контента.
  
  Типы полей перепроверяются при АКТИВАЦИИ, а не только при загрузке: шаблон,
  установленный до появления закрытого реестра, несёт validation_json своей эпохи,
  а встроенные шаблоны путь загрузки не проходят вовсе — без этого шлюза они
  активировались бы с полем, которое редактор не умеет отрисовать.

- **feat**(api): хранить и отдавать настройки контентной страницы (PRD-22) (2026-07-23) [`51eea7367757d1392fa8cf7c2b569c4a4706cfce`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/51eea7367757d1392fa8cf7c2b569c4a4706cfce)
  Настройки нормализуются против settings[] варианта: недекларированный ключ в базу
  не попадает, значения приводятся к объявленному типу, объявленный default
  заполняет отсутствующее, значение вне перечня select отбрасывается, а текстовые
  настройки санируются — они доезжают до макета как подпись кнопки.
  
  Идентификатор последовательности переживает вариант, который его больше не
  объявляет (FR-29): автор, переключивший страницу на другой вариант и обратно,
  сохраняет её место в последовательности.
  
  В поток веб-прогона settingsJson тоже поехал: SCORM-пакет вёз их с FR-20, а веб
  без них не считал последовательность вовсе, и галерея теряла индикатор.

- **feat**(shared): предпросмотр и проверка работоспособности по вариантам (PRD-22) (2026-07-23) [`e12a54bfe1eeba72d12d72752f456f2906f98914`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e12a54bfe1eeba72d12d72752f456f2906f98914)
  Галерея была отдельной веткой предпросмотра (buildGalleryParts) с собственным
  контекстом gallery.* и вручную заданными точками — то есть второй копией правил
  отрисовки. Ветка убрана: слайд предпросматривается как обычная контентная
  страница, а точки считает общее ядро последовательностей по демо-набору, так что
  шаблон, не объявивший последовательность, точек не получит — ровно как в бою.
  
  Экран теперь несёт ключ и название варианта, через который отрисован: в
  «Структуре» автор выбирает именно вариант, и рельс предпросмотра должен
  группировать по нему, а не по маршруту.
  
  Проверка работоспособности перестаёт требовать слот page-content от варианта с
  собственным макетом: он размечает поля через data-placeholder и слот ему не
  нужен — предупреждение выдавалось на экран, который рисуется безупречно. Взамен
  проверяется то, что действительно важно: объявленные поля должны иметь куда
  попасть, иначе автор заполняет их в «Структуре» и до ученика они не доезжают.
  Формулировка «экран будет отрисован из стандартного шаблона» тоже исправлена —
  это верно только при отсутствии макета.
  
  Через runtime-entry новые модули (последовательности, база файлов, реестр типов)
  уезжают в бандл SCORM-пакета.

- **feat**(shared): подменять недоступный вариант страницы вариантом по умолчанию (2026-07-23) [`156bb4add9745d31d068ccc103596829387cb137`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/156bb4add9745d31d068ccc103596829387cb137)
  Страница, чей вариант текущий шаблон оформления не объявляет, проваливалась в
  buildFallbackContentHtml — нешаблонная карточка, не похожая ни на что вокруг,
  которая скорее прятала проблему, чем показывала её. Теперь такая страница
  рисуется через вариант по умолчанию своего вида (PRD-1 §4.3.2).
  
  Подмена — решение только на отрисовку: templateKey в БД не переписывается,
  поэтому привязка автора переживает возврат к прежнему шаблону, а «Структура»
  продолжает отмечать страницу как требующую сопоставления.

- **feat**(shared): база для относительных ссылок на файлы шаблона (PRD-22 FR-36) (2026-07-23) [`69182af937981c5630579d41803762e3aea244d2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/69182af937981c5630579d41803762e3aea244d2)
  Автор вправе сослаться на файл шаблона ровно так, как он лежит в пакете
  (&lt;img src="images/diagram.png"&gt;). Один и тот же путь не может работать на обоих
  хостах как есть: в SCORM-пакете шаблон лежит под template/, а веб отдаёт его со
  своего маршрута. Поэтому значение хранится дословно, а база подставляется при
  отрисовке — своя на каждом хосте.
  
  Трогаются только относительные ссылки: абсолютные пути к авторским медиа, data:,
  якоря и протокольные ссылки остаются как есть.

- **feat**(shared): ядро последовательностей контентных страниц (PRD-22 FR-17..FR-19) (2026-07-23) [`9b58144fb2ce8e0e745488157fe246dec95267a1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9b58144fb2ce8e0e745488157fe246dec95267a1)
  Количество точек индикатора и текущая позиция были двумя числами, которые автор
  вбивал руками, поэтому любая правка структуры молча делала их неверными. Теперь
  автор задаёт только идентификатор последовательности, всё остальное выводится:
  участок структуры между системными элементами, внутри него — максимальный ряд
  СОСЕДНИХ страниц с одинаковым непустым идентификатором, и не меньше двух страниц
  (индикатор из одной точки никуда не ведёт).
  
  Зона «После теста» делится границей результатов: счётчик не должен переживать
  экран результатов. canGoBack сюда не входит — это состояние прогона, его
  передаёт хост. Модуль без зависимостей от фреймворка: тот же код считает точки в
  SCORM-пакете, в вебе и в предпросмотре.

- **feat**(db): колонка content_pages.settings_json и контракт полей манифеста (PRD-22) (2026-07-23) [`6208d6225ea4315a799357b76da79a3167eeccd7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6208d6225ea4315a799357b76da79a3167eeccd7)
  Свойства страницы (идентификатор последовательности, подпись кнопки, фон) нельзя
  хранить вместе с авторским содержимым: у них разные правила жизни. Содержимое
  переносится по общим ключам плейсхолдеров при смене варианта, а идентификатор
  последовательности обязан пережить даже вариант, который его больше не
  объявляет. Отсюда отдельная колонка, а не ещё один ключ в values_json.
  
  Заодно схема манифеста перестаёт быть z.unknown(): placeholders[] и новый
  settings[] проверяются против реестра типов, поэтому опечатка в типе больше не
  доезжает до редактора. Вид страницы gallery убран из variantKindSchema — слайд
  галереи это вариант info со своим макетом, а не отдельный вид.

- **feat**(shared): единый закрытый реестр типов полей шаблона (PRD-22) (2026-07-23) [`7bb9d51ce6a75eecf1be9a5ee0fc8c7931f269e5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7bb9d51ce6a75eecf1be9a5ee0fc8c7931f269e5)
  Перечень допустимых типов жил в двух независимых switch (контрол редактора и
  правило отрисовки) плюс прозой в спеке, а манифест не валидировался вовсе.
  Наборы уже разошлись (select имел контрол, но не имел правила отрисовки), и
  опечатка вроде richtext молча превращалась в однострочный ввод без диагностики.
  
  Реестр разделяет поля по природе: placeholders[] — содержимое, попадающее в
  макет, settings[] — свойства страницы. Оба списка закрыты; потребители
  (валидатор манифеста, редактор, рендерер) выводят поведение отсюда, поэтому
  пропущенный контрол становится ошибкой компиляции.

- **feat**(deploy): инструмент create-admin для создания/повышения администратора (2026-07-21) [`6dd3df8441a87efe6c9a04365cf84c7b5c17c96c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6dd3df8441a87efe6c9a04365cf84c7b5c17c96c)
  set-password умеет только сбрасывать пароль существующему аккаунту. Для
  поднятия входа на свежем стенде (клон прода без известных кред) нужен способ
  СОЗДАТЬ администратора. create-admin по тем же лекалам: `.mjs` работает внутри
  контейнера (та же @vvlad1973/crypto, шифрование email и scrypt-хеш, что у
  приложения), `.bat` доставляет его по SSH и запускает через docker exec.
  Идемпотентно: существующий email повышается до administrator и пароль
  сбрасывается. Пароль передаётся base64 end-to-end.

### Fixes

- **fix**(content): скоупить CSS при сохранении страницы и сборке пакета (2026-07-24) [`09018aa0863245127e03acc2e70b69033c0ed8af`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/09018aa0863245127e03acc2e70b69033c0ed8af)
  Скоуп применяется в тех же точках, где уже работает санитайзер, поэтому
  хранимое значение и запечённое в пакет совпадают. Страница режима HTML
  рендерится целиком, её область — .content-page--html, а не плейсхолдер.
  
  Пересанитайз при каждой сборке важен отдельно: он чинит страницы, сохранённые
  ДО этой правки, без миграции. Повторный проход ничего не наращивает —
  обработанный блок помечен data-tb-scoped.

- **fix**(content): ограничить CSS авторской вставки блоком её поля (2026-07-24) [`00f4e36260ff9ffc6775db8d4fcc2e04bab8e23a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/00f4e36260ff9ffc6775db8d4fcc2e04bab8e23a)
  Автор вставляет в поле не фрагмент, а целый HTML-документ вместе со &lt;style&gt;,
  где есть правила для body и *. Санитайзер &lt;style&gt; не удаляет — тег не опасен.
  В предпросмотре такая вставка выглядит правильно: веб-хост рендерит экран в
  Shadow DOM, где селектор body не совпадает ни с чем. В пакете и отладчике та же
  разметка попадает в НАСТОЯЩИЙ документ: body{display:flex;align-items:center}
  сжимал .container до 35 px и схлопывал сцену 16:9 сертификационного шаблона до
  3x2 px — экран выглядел пустым при полностью живом DOM. Заодно авторский .btn
  переопределял кнопку «Далее» шаблона.
  
  Селекторы авторского CSS переписываются так, чтобы совпадать только внутри
  своего блока: body/html/:root становятся самим блоком, остальное получает
  префикс, @media обходится рекурсивно, @keyframes и @font-face не трогаются,
  @import удаляется (внешний ресурс ломает автономность пакета). После этого
  правила body не существует ни в одном хосте — расхождение предпросмотра с
  пакетом исчезает по построению, а не залечивается в одном из них.
  
  Сканер написан руками, а не регуляркой: списки селекторов, вложенные at-правила,
  строки и комментарии регуляркой не переписать, а модуль обязан работать без DOM
  и в Node, и в браузере. Ошибка разбора безопасна — правило перестаёт совпадать,
  испортить документ оно уже не может.

- **fix**(prd-24): не терять применённое правило при возврате результата в рантайм (2026-07-24) [`93ab120cdd21589f4fd928b72205afceab75c7c2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/93ab120cdd21589f4fd928b72205afceab75c7c2)
  Живой прогон собранного пакета вскрыл дефект, которого не видели юнит-тесты:
  calculateResults и computeSectionResult перекладывают результат общего движка
  в свою форму вручную, и поле с применённым правилом там просто не переносилось.
  Из-за этого подпись «Требуется…» на экране результатов оставалась пустой у
  темы с правилом «по вариантам» — и осталась бы пустой в сохранённых попытках,
  где пересчитать порог уже не по чему.
  
  Приёмочный сборщик получил режим TB_BY_VARIANT: тема выдаётся вариантами и
  гейтится порогом на каждый (100% против 40%), так что пакет можно проиграть и
  увидеть разницу. Проверено на живом пакете: при одном и том же результате в 50%
  вариант с порогом 100% даёт незачёт, вариант с 40% — зачёт, а состояние без
  пина деградирует к общему правилу теста.
  
  Тестами закрыты и сам пропуск поля, и краевые случаи манифеста (вариант без
  заданного порога, вариант без вопросов, правило без набора вариантов) и
  парсера листа порогов. Превью шаблонов пересобраны — в их бандл входит
  резолвер с контекстом выдачи.

- **fix**(prd-24): привести блок порогов по вариантам к эскизу (2026-07-24) [`f365acd8e8a9709bbb615957c51ae7eb2c0e2b83`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f365acd8e8a9709bbb615957c51ae7eb2c0e2b83)
  Живой прогон в браузере показал два расхождения с согласованным эскизом,
  которых не видели jsdom-тесты.
  
  Метка «Тип» вставала НАД полем, ломая строки сетки: DS-компонент Select — это
  flex-колонка и рендерит подпись внутри себя как .ou-field__lbl, тогда как в
  эскизе разметка писалась руками и подпись была соседом в .ou-formfield.
  Строчная раскладка теперь применяется к самому контролу.
  
  Подсказка о максимуме печаталась на каждом варианте и разрывала сетку. По
  эскизу она одна под блоком: показывает общее число, когда максимум у вариантов
  одинаковый, и перечисляет по вариантам только при различии.
  
  Заодно в плане снято неверное допущение, будто редактор не знает поштучных цен:
  цепочка эффективной цены доступна из модели целиком, поэтому и подсказка, и
  валидация считают максимум варианта точно.

- **fix**(wireframes): не показывать меню закрытых селектов (2026-07-23) [`f4297c3b222f4133123b2271483bd086997247da`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f4297c3b222f4133123b2271483bd086997247da)
  В эскизах все option-меню отрисовывались одновременно и накладывались друг
  на друга. Причина не в конкретном эскизе: DS-класс .ou-select__menu не имеет
  правила для закрытого состояния, потому что настоящий React-компонент
  монтирует меню в DOM только пока оно открыто. Инициализатор эскизов
  (initSelect в prd7-shared.js) вместо этого вшивает меню в DOM навсегда —
  без гейта они видны всегда.
  
  Добавлен гейт видимости для эскизов: меню показывается только при .is-open.
  Чинит все эскизы, где селект попадает на видимый экран (36 файлов грузят
  общий инициализатор). Portal-меню, которые отдельные эскизы инжектят в
  document.body, не затронуты — они не потомки .ou-select.

- **fix**(tests): вернуть аналитике зелёный прогон (2026-07-23) [`4075c8e0a91000f4457516da8580f023c3ec2738`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4075c8e0a91000f4457516da8580f023c3ec2738)
  Маршруты деталей попытки отвечали 500 на любой попытке с пакетом: аналитика
  пересчитывает вклады в шкалы и показатели по текущей конфигурации теста, а в
  моках хранилища не было getScales/getQuestionMeasurements/getResultVariables,
  на которые опирается loadScoringConfig.
  
  Продукт исправен — ломались только фикстуры, но девять падений висели в дереве
  с середины июля и маскировали бы настоящую регрессию.

- **fix**(deploy): выдавать CREATE на БД тестового клона под drizzle-kit migrate (2026-07-20) [`d1643f7f1660643c01c15dfbd0c9d93f01deb2c8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d1643f7f1660643c01c15dfbd0c9d93f01deb2c8)
  Деплой на тестовый стенд падал ещё до бейзлайна: `permission denied for
  database test_builder_test`. Провижининг клона (deploy-test.sh) выдаёт
  приложенческой роли CREATE на СХЕМУ public и владение таблицами, но не
  CREATE на саму БАЗУ — а `migrate` держит журнал в ОТДЕЛЬНОЙ схеме `drizzle`,
  и `CREATE SCHEMA` требует database-level CREATE. Базу создаёт `createdb` от
  postgres, поэтому владелец — postgres, и роли этого права не хватает. На то же
  спотыкается и разовый baseline-existing-db.sql.
  
  Пока схему приводил `push`, пробел не проявлялся — он не создавал схем.
  Всплыло после перехода push-&gt;migrate. Добавлен `GRANT CREATE ON DATABASE`
  в блок провижининга, рядом с грантом на схему public.
  
  Плюс подсказка в build-test.bat переписана: прежняя указывала только на
  «таблица уже существует» (бейзлайн), а первым делом стенд упирается в
  нехватку прав — теперь названы оба случая.
  
  Существующий клон, созданный до этой правки, разблокировать разово:
    sudo -u postgres psql -c 'GRANT CREATE ON DATABASE test_builder_test TO "scormDbUser";'
  затем baseline (клон старше бейзлайна прода), затем повторить деплой.

- **fix**(deploy): не терять текст ошибки drizzle-kit migrate и на тестовом стенде (2026-07-20) [`14a5ed54092bb285486aa72b7fa265f6ee1237da`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/14a5ed54092bb285486aa72b7fa265f6ee1237da)
  Продолжение b6b425e: там потерю сообщения об ошибке миграции вылечили в
  deploy.sh (боевой контур), а build-test.bat несёт СВОЮ команду миграции и
  страдал тем же — деплой обрывался на спиннере `applying migrations...` без
  единой строки о причине.
  
  drizzle-kit завершает упавший прогон через process.exit(1), обрывая запись в
  канал (в Node запись в pipe асинхронна). Поэтому migrate теперь пишет в файл
  внутри контейнера (запись в файл синхронна и переживает выход) и печатается
  после. Плюс явная ветка разбора: если БД тестового стенда — это клон, снятый
  ДО бейзлайна прода, migrate падает на CREATE уже существующих таблиц;
  сообщение подсказывает разовый baseline-existing-db.sql.

### Documentation

- **docs**(templates): описать ограничение авторского CSS блоком поля (2026-07-24) [`6b7fde843eb6e345fe4af4aa27e721f8de248c88`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6b7fde843eb6e345fe4af4aa27e721f8de248c88)
  Гайд утверждал обратное: «инлайновый &lt;style&gt; может повлиять на оформление всего
  экрана — макет должен быть устойчив к чужим правилам». Теперь это неверно.
  Добавлен раздел с таблицей преобразований и, что важнее для разработчика
  шаблона, честные границы: вьюпортные единицы и position: fixed не скоупятся
  (переписывается совпадение селектора, а не значение свойства), а пиксельная
  вставка не масштабируется вместе со сценой на cqh/cqw.

- **docs**: привести рунбуки БД к объединённому деплою (2026-07-24) [`14d8eaac1cdff44158ace89f97447856ab2909ac`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/14d8eaac1cdff44158ace89f97447856ab2909ac)
  Оба документа описывали эпоху `drizzle-kit push --force` и два разных скрипта
  выкладки. Прежний текст сохранён как исторический контекст, сверху добавлен
  статус: миграции версионированные, скрипт один, применяются в одноразовом
  контейнере до старта приложения.

- **docs**(prd-24): поправить в плане расположение тестов резолвера (2026-07-24) [`7d3c30c0c003dc2f27a39b9ae4fb2fab0bf584ef`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7d3c30c0c003dc2f27a39b9ae4fb2fab0bf584ef)
  План отправлял тесты resolveTopicRule в tests/scoring-pass-rule.test.ts, но
  там проверяется легаси-функция checkPassRuleWithPartial, извлечённая из
  resultsPage.js. Фактически резолвер покрыт в tests/scoring-aggregate.test.ts —
  туда и добавляются кейсы by_variant. Обнаружено при исполнении Task 1.

- **docs**(prd-24): эскиз правила «По вариантам» в настройках теста (2026-07-23) [`e423ae6a6f5d6d9435e9fc9ad34825824a619ab8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e423ae6a6f5d6d9435e9fc9ad34825824a619ab8)
  Состояние s-pass-by-variant добавлено в существующий эскиз вкладки
  «Настройки», а не отдельным файлом: иначе получился бы дубликат Drawer
  более чем на 90% (шапка, вкладки, рельса, карточка теста, таблица тем).
  Оно встаёт в уже сложившееся семейство состояний s-pass-*.
  
  Дельта к утверждённому UI минимальна: четвёртый пункт «По вариантам» в
  списке правила темы (только у темы в режиме вариантов) и раскрывающийся
  блок со строкой на вариант «Вариант N · Тип · Порог». Блок собран сеткой
  [метка | Тип | Порог]: колонка «Порог» тянется до правого края ячейки,
  чтобы правый край контролов совпадал с полем правила темы, а колонка
  «Тип» выравнивается по самому широкому значению между строками.
  
  Аннотации добавлены в acceptance-таблицу и в таблицу условной видимости
  (доступность пункта, синхронизация записей с набором вариантов,
  валидация порога против максимума своего варианта).

- **docs**(prd-24): пороги прохождения по вариантам — требования и план (2026-07-23) [`eee6020817270b126fbe47109d4ad240f15dd587`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eee6020817270b126fbe47109d4ad240f15dd587)
  Правило прохождения темы резолвится без знания выпавшего варианта, поэтому
  на тему приходится один порог на все варианты. При разном достижимом
  максимуме баллов это даёт неравные шансы (риски PRD-17 R-8/R-9) — тот самый
  долг, который PRD-17 вынес в отдельный PRD (D-6).
  
  Вводит четвёртый источник правила by_variant: порог на каждый вариант,
  применяемый к ВЫДАННОМУ. Хранение — в существующем topic_pass_rule_json
  (миграции нет), ключ — стабильный formId; вся новая логика заперта в
  resolveTopicRule, поэтому оба хоста читают её единым движком. Обмен — через
  новый лист книги «Пороги вариантов» по образцу листа «Квоты».
  
  Синхронизированы PRD-17 (FR-12, §9, D-6, R-8/R-9), спецификация формата
  Excel (§12.8), BRD психометрики (отчёт по проходимости вариантов — там) и
  ROADMAP. Калибровочные коэффициенты по-прежнему вне охвата.

- **docs**(prd-23): требования, эскиз и план по темам шаблона (2026-07-23) [`a57ba99b15daa3925d7af195c926e795a799b1ad`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a57ba99b15daa3925d7af195c926e795a799b1ad)
  PRD-23 фиксирует: палитры объявляет ИСКЛЮЧИТЕЛЬНО манифест (перечень закрыт,
  проверка при загрузке и при активации), тест хранит вид и цвет на каждую
  палитру, отрисовка ставит `data-theme` на корень сцены и печатает
  переопределения последними в каскаде, цвета живут в своём разделе рельсы.
  
  Отдельно записана форма селектора для теневого корня: условие обязано идти
  функциональной записью `:host(...)`, иначе браузер отбрасывает правило и тема
  молча перестаёт применяться. Это стоило одного дефекта, найденного только в
  браузере.
  
  Спека платформы получила раздел «Темы шаблона», руководство разработчика —
  пример двух палитр, пункты чек-листа и коды ошибок валидатора, PRD-3 — гейт и
  предупреждение о необъявленной палитре.
  
  Эскиз согласован и перенесён в approved. Заявленные в нём классы `.ou-table`
  в дизайн-системе не существуют — разметка приведена к фактическому компоненту
  `Table`.

- **docs**(prd-22): требования, эскизы и план по полям страницы и последовательностям (2026-07-23) [`8508f19cbb018b806b79e9e10d1dfac0fe769d24`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8508f19cbb018b806b79e9e10d1dfac0fe769d24)
  PRD-22 фиксирует: отдельного вида страницы «галерея» нет, состав полей задаёт
  ИСКЛЮЧИТЕЛЬНО манифест (placeholders[] — содержимое, settings[] — свойства),
  перечень типов закрыт и проверяется при загрузке И при активации,
  последовательности вычисляются по участку структуры.
  
  Спека платформы, PRD-1 и PRD-3 приведены в соответствие; руководство разработчика
  шаблонов описывает settings[], последовательности и относительные ссылки на файлы
  шаблона. План доработок предпросмотра и вариантов ведёт остаток трека.

## [2.8.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.7.1-beta...v2.8.0-beta)

### Features

- **feat**(ui): показывать версию сборки в подвале сайдбара + бамп 2.8.0-beta (2026-07-21) [`74d135928e7709ae49772ae2af5e2664e9e7d8f6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/74d135928e7709ae49772ae2af5e2664e9e7d8f6)
  Повторяющийся вопрос «задеплоилось ли то, что я собрал» решается тем, что
  инстанс сам сообщает, что на нём крутится. В подвал авторского сайдбара
  выведен build stamp: semver отвечает «какая версия», короткий git-SHA —
  «какая именно сборка» (суффикс `-dirty` метит сборку с незакоммиченными
  правками). Значения инъектятся Vite `define` из package.json и `git
  rev-parse` во время сборки; при отсутствии git SHA просто опускается.
  
  Версия поднята 2.7.1-beta -&gt; 2.8.0-beta (минор: ушла фича PRD-12 FR-6 —
  контент-страницы и хаб-маршрутизатор в веб-хосте).

- **feat**(learner): показывать контент-страницы и хаб-маршрутизатор в вебе (PRD-12 FR-6) (2026-07-20) [`3713a60227d5f98efb345d287cf1483d05e4f27b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3713a60227d5f98efb345d287cf1483d05e4f27b)
  Веб-прохождение выдавало только вопросы: все четыре зоны размещения и хаб
  существовали в «Структуре», но не существовали в вебе как код. Теперь обе
  среды рендерят их из одних макетов через один `renderScreenInto`.
  
  Порядок выдачи разрезан по границе раздела: страницы «после темы» принадлежат
  уходящему разделу и идут ДО его обзора/итогов, «перед темой» — приходящему и
  идут ПОСЛЕ. Иначе введение следующего раздела опережало итоги предыдущего.
  «Перед темой» доставляются по факту прибытия на вопрос — каким бы путём
  учащийся туда ни попал.
  
  Отдельные случаи, каждый пойман живым прогоном:
  
  \- раздел с пустой жеребьёвкой в конце теста терял свои страницы: их держали
    для «прибытия», которого не будет. У конца теста прибытия нет — остаток
    промежутка выдаётся сразу;
  \- возобновление попытки не восстанавливало структуру, из-за чего пропадали
    ВСЕ оставшиеся зоны, а не только пройденные. Уже пройденные страницы
    намеренно не переигрываются: попытка хранит индекс вопроса, не позицию
    страницы;
  \- «Введение раздела» рисовалось общей обёрткой вместо своего макета — пустой
    экран с одной кнопкой; и его кнопки (`section-intro-continue` / `-back`) не
    были подключены;
  \- в router-режиме сохранение прогресса шло через общий advance, который на
    границе раздела поднимает обзор PRD-19 — флаг переживал возврат в хаб, и
    следующий раздел открывался на чужом обзоре;
  \- на хабе оставалась мёртвая «Далее» из обёртки макета.
  
  `TemplateScreen` дополнительно делегирует `data-nav` как `nav:&lt;value&gt;` —
  контент-макеты выражают «Далее» именно так, и без этого веб пришлось бы
  переписывать разметку шаблона, то есть расходиться с тем, что играет пакет.
  
  Проверено в браузере: линейный проход всех четырёх зон до экрана итогов,
  полный router-проход (хаб → раздел → возврат → «Завершить»), возобновление,
  раздел без вопросов.

- **feat**(api): доставлять структуру теста и макеты контент-страниц веб-хосту (2026-07-20) [`2f87210d5b604ba1efada5874b63490a29c01885`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2f87210d5b604ba1efada5874b63490a29c01885)
  Веб-хост не мог показать ни одной авторской страницы просто потому, что до
  него не доезжали данные: ответы старта и возобновления попытки несли только
  выданные вопросы, а `/screen-template` знал лишь пять системных экранов.
  
  \- `contentPages` + `flowMode` в ответах старта и возобновления. Читаются через
    источник попытки, поэтому попытка, приколотая к снапшоту (PRD-15), получает
    ОПУБЛИКОВАННУЮ структуру, а не сегодняшние правки автора;
  \- экраны `content` и `section-intro` в `/screen-template`, причём для `content`
    отдаются ещё и `contentTemplates` из манифеста — из объявленных там
    плейсхолдеров веб строит тот же каркас страницы, что и SCORM.

- **feat**(shared): единый источник правил прохождения для обоих хостов (2026-07-20) [`0a910f653e04aae2c23b482dd62fc3b01ca4ad00`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0a910f653e04aae2c23b482dd62fc3b01ca4ad00)
  Что и в каком порядке видит учащийся, как собирается контент-страница и по
  каким правилам открыт раздел — эти правила жили ТОЛЬКО в SCORM-рантайме, и
  веб-хост о них не знал. Две реализации там, где нужна одна, и разошлись:
  «Структура» обещала экраны, которых веб не показывал (PRD-12 FR-6).
  
  Три модуля, framework-free и browser-safe (бандлятся в SCORM-пакет как есть):
  
  \- `flow/page-sequence` — последовательность прохождения: четыре размещения
    контент-страниц × три режима потока, граница `summary` в зоне «После теста»,
    фильтр системных kind.
  \- `flow/router-hub` — правила хаба (открытость раздела, политика завершения) и
    его разметка.
  \- `template/content-page` — каркас страницы по плейсхолдерам её варианта.
  
  Попутно закрыты два дефекта сборки последовательности:
  
  \- хаб-маршрутизатор вытеснял авторские страницы зоны «До теста»: он и они
    нумеруются из ОДНОГО пространства `sortOrder`, но разными сторонами (сервер
    ставит хабу 0, редактор нумерует авторские с 0, исключив хаб из списка). При
    коллизии хаб вставал первым, а всё за ним недостижимо — выбор темы заменяет
    последовательность целиком. Теперь хаб замыкает зону независимо от значений
    в базе, миграция данных не нужна;
  \- раздел с пустой жеребьёвкой терял страницы «перед темой» и «после темы»:
    они были привязаны к первому и последнему ВЫДАННОМУ вопросу, а якоря нет.
    В режиме «по темам» сборка идёт от разделов — якорем стал сам раздел.
  
  39 тестов: матрица размещений и правила хаба.

- **feat**(db): ввести версионированные миграции drizzle (generate) с бейзлайном (2026-07-18) [`6ec64b888645b937baf74f90b9b930f75409c3a7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ec64b888645b937baf74f90b9b930f75409c3a7)
  Готовит переход с `drizzle-kit push` на `generate`+`migrate`. push вёл схему
  без журнала (stateless diff schema.ts -&gt; БД), поэтому здесь заводится сам
  механизм версионирования:
  \- drizzle.config.ts: out -&gt; ./drizzle, явная таблица журнала
    drizzle.__drizzle_migrations;
  \- drizzle/0000_baseline.sql + meta/: снимок текущей схемы (generate подтвердил
    «No schema changes»);
  \- drizzle/baseline-existing-db.sql: разовый идемпотентный бейзлайн существующей
    БД — помечает 0000 применённым (created_at = journal when), чтобы migrate не
    пытался создать уже существующие таблицы. Проверено на одноразовой БД: свежая
    получает схему через migrate, существующая с бейзлайном — 0000 пропускается;
  \- drizzle/README.md: рунбук процесса;
  \- .gitattributes: drizzle/** eol=lf (детерминизм байтов образа и хэша).
  
  Легаси migrations/ остаются как история; переключение деплоя — отдельным коммитом.

### Fixes

- **fix**(deploy): не терять текст ошибки drizzle-kit migrate (2026-07-20) [`b6b425e385afb24a8063067d9ec746f15be9c703`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b6b425e385afb24a8063067d9ec746f15be9c703)
  Сбой миграции выглядел как беспричинный обрыв деплоя: баннер drizzle,
  пустая строка, закрытие ssh-сессии — ни строчки о причине. Сообщение
  терялось дважды, по двум разным механизмам:
  
  \- `docker compose run` без `-T` подключается к терминалу (его даёт
    `ssh -tt`) и сворачивает attach-поток в момент выхода контейнера,
    не дождавшись последнего stderr;
  \- под `-T` stdout становится каналом, а запись в канал в Node асинхронна,
    и `process.exit(1)` внутри drizzle-kit обрывает её на полуслове.
  
  Поэтому вывод пишется в файл внутри контейнера (запись в файл синхронна,
  выход её не рвёт) и печатается уже после завершения. Плюс явная ветка
  разбора: `set -euo pipefail` обрывал скрипт молча, теперь падение
  называет себя и подсказывает разовый бейзлайн существующей БД — именно
  на нём и споткнулся первый деплой после перехода с push на migrate.

- **fix**(deploy): защитить выкатку на тестовый стенд от того же остатка в /tmp (2026-07-20) [`f58d24692cb049182ca91f6accc3d1d785942225`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f58d24692cb049182ca91f6accc3d1d785942225)
  Дефект здесь латентный, но реальный: финальный `rm -f` образа выполняется
  только когда весь удалённый шаг прошёл успешно, так что после любого сбоя
  tar остаётся в /tmp — и если он root-овский, следующий scp упирается в
  sticky-бит ровно так же, как на боевом контуре.

- **fix**(deploy): сносить root-овский остаток пакета в /tmp перед scp (2026-07-20) [`cdbe6959a5e794eee07571368ad673a96ce67574`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cdbe6959a5e794eee07571368ad673a96ce67574)
  /tmp имеет sticky-бит, поэтому оставшийся от прежнего запуска под root
  файл /tmp/deploy-&lt;project&gt;.tar непривилегированный пользователь не может
  ни перезаписать, ни удалить — scp падает с "dest open ... Permission
  denied", и деплой не стартует вовсе. Тот же случай, что и со staging-
  каталогом (6ef5e9f), только там вычистили каталог, а лежащий рядом tar
  оставили.

- **fix**(deploy): чистить staging-каталог под sudo — root-овский остаток ломал повторный деплой (2026-07-18) [`6ef5e9fdd0ed8960eb10b1b4ae7687e259fa8b88`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ef5e9fdd0ed8960eb10b1b4ae7687e259fa8b88)
  Прежний ручной запуск под root оставлял /tmp/deploy-&lt;project&gt; с владельцем root;
  из-за sticky-бита на /tmp повторный автозапуск под vvlad1973 не мог его удалить
  (rm: Operation not permitted), и деплой падал на очистке. Деплой и так
  привилегированный (run-deploy.sh -&gt; sudo bash), поэтому чистим staging через
  sudo rm — работает независимо от владельца остатка.

- **fix**(deploy): выполнять деплой на сервере по SSH, а не печатать команду для ручного запуска (2026-07-17) [`05aa498da8a8c5b0b6c785f7e8d41152b2fa5c6b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/05aa498da8a8c5b0b6c785f7e8d41152b2fa5c6b)
  build-docker.bat и deploy-docker.bat после scp лишь печатали "Run on server:
  bash -c ..." — оператор должен был заходить на сервер и вставлять её руками.
  build-test.bat (тест) же делает `ssh -tt` и прогоняет весь сценарий сам. Отсюда
  асимметрия «тест разворачивается сам, прод заставляет гонять команды». Заменяю
  echo-инструкцию на `ssh -tt`, распаковывающий пакет и запускающий run-deploy.sh
  на сервере (флаг -tt — для интерактивных промптов drizzle-kit push --force).
  Теперь прод-флоу самодостаточен так же, как тестовый.

- **fix**(deploy): целить pre-push команды на сервис по имени проекта, а не app (2026-07-17) [`65f0fa863f619b685855342aa83e4fe461880355`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/65f0fa863f619b685855342aa83e4fe461880355)
  Сгенерированный docker-compose.yml называет сервис по имени проекта
  (PROJECT_NAME_PLACEHOLDER -&gt; test_builder), а команды миграций, пре-пуш-гейта и
  drizzle push жёстко обращались к сервису "app", которого в compose нет. Деплой
  падал на первом `docker compose run ... app` с "no such service: app" — ДО
  миграций и push, оставляя развёртывание недоделанным. deploy-test.sh этого не
  ловил, потому что использует одну переменную и как имя сервиса, и как аргумент
  run. Меняю три хардкода app на ${PROJECT_NAME}.

- **fix**(db): гардить бэкфилл PRD-15 по каждой колонке отдельно, а не по одной points (2026-07-17) [`99da255a0c879c79746279e9124c8dfa245196a5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/99da255a0c879c79746279e9124c8dfa245196a5)
  Миграции 027/028 охранялись проверкой существования только questions.points,
  но тело бэкфилла читает И points, И scoring_json. Эти колонки дропаются
  НЕЗАВИСИМО: drizzle-kit push --force синхронит схему с shared/schema.ts (там
  уже нет ни той, ни другой), поэтому прошлый push мог снести scoring_json,
  оставив points. На таком гибридном состоянии гард по points пропускал ветку, и
  INSERT падал с "column q.scoring_json does not exist" (поймано на проде).
  
  Теперь гард раздельный (has_points / has_scoring) с тремя ветками: обе колонки —
  исходный совмещённый бэкфилл; только points — бэкфилл цены; только scoring_json —
  бэкфилл градуированной конфигурации. plpgsql планирует лишь входимую ветку, так
  что ветка с отсутствующей колонкой не планируется и не падает. Данные
  сохраняются в любом из состояний.

- **fix**(deps): вылечить дрейф package-lock — из-за peer:true на прямых зависимостях падал npm ci на чистой машине (2026-07-17) [`5479c29c27c8159e20fc50ec0958736cfc4be536`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5479c29c27c8159e20fc50ec0958736cfc4be536)
  Апдейт dev-зависимостей (ec74d58) перегенерировал lock и переклассифицировал
  кучу ПРЯМЫХ зависимостей в "peer": true (@dnd-kit/core, drizzle-orm, pg, postcss,
  vite-node, vitest, react, react-dom, react-hook-form, zod и др.), а также потерял
  top-level @emnapi/core|runtime. На машине с уже заполненным node_modules это не
  всплывало, но на ЧИСТОЙ машине npm ci следовал дрейфнутому lock и не раскладывал
  peer-помеченные прямые пакеты -&gt; vite/Rollup не резолвил import "@dnd-kit/core"
  (result-variables-section.tsx / start-pages-section.tsx) и сборка падала.
  
  Регенерация через npm install --package-lock-only: снят ошибочный peer:true,
  восстановлены @emnapi, БЕЗ смены версий (правились только флаги peer/dev/optional).
  Проверено: npm ci --dry-run зелёный, npm ls --package-lock-only резолвит @dnd-kit
  как прямые деп, npm run build проходит.

- **fix**(templates-ui): русские подписи узлов PRD-19 и галереи в рельсе превью (2026-07-17) [`fcd3e35792adeb28389d456f3a6fb362350a757e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fcd3e35792adeb28389d456f3a6fb362350a757e)
  Экраны review, section-results и content.gallery получили подписи в TYPE_LABELS — раньше рельс превью показывал их сырыми идентификаторами латиницей. page-preview-modal обновлён под переименование requiredSlots -&gt; expectedSlots.

- **fix**(web-host): fallback системных экранов на стандартный шаблон + fail-safe (2026-07-17) [`e3d297054421711c9f0199aaac2a031c12abb191`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e3d297054421711c9f0199aaac2a031c12abb191)
  Веб-хост доведён до паритета со SCORM по fallback: виды review/section-results добавлены в SCREEN_KIND, а экраны start/question/results/blocked получили файловый fallback на стандартный шаблон вместо 404 при отсутствии макета. templateOwnsKind переведён с fail-open на fail-safe: при ошибке БД или пустых contentTemplates экран отрисуется из стандартного шаблона, а не из шаблона, который вид не объявляет.

- **fix**(scorm): единый fallback-набор системных экранов + рендер контентных страниц по layoutFile (2026-07-17) [`4ce33321fa12c8fefb05955d6c8d999537971336`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4ce33321fa12c8fefb05955d6c8d999537971336)
  computeFallbackKinds расширен со start/results до всех системных экранов (карта FALLBACK_KIND_LAYOUT), с ключами МАКЕТОВ, а не видов (kind questions -&gt; layout question, intro -&gt; section-intro). Экраны вопроса, обзора, итогов раздела и вводной проведены через systemLayout с подменой стилей на стандартные. Загрузчик подтягивает макеты по contentTemplates[].layoutFile, а renderContentPage предпочитает макет варианта: без этого шаблон с моделью «один макет на страницу» рендерил все контентные страницы одним layouts["content"]. preview.html обоих шаблонов перегенерированы из исходников рантайма.

- **fix**(templates): превью рендерит дизайн шаблона, а не подменённый экран (2026-07-17) [`b726ba77e045f9ba01f1cf4c0c8af369d78a9116`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b726ba77e045f9ba01f1cf4c0c8af369d78a9116)
  Резолвер макета (resolveLayoutKey) приведён к рантайму: экран берёт макет из contentTemplates[].layoutFile, «Введение раздела» — из фиксированного ключа section-intro, галерея — из ключа варианта. Вводная раздела и слайды галереи строятся тем же конвейером, что рантайм (context + слот), а не скелетом page-content — иначе рендерились пустыми. Сборщики набора (readTemplateBundle, smoke-bundle) больше не выбрасывают shell и добавляют макеты по layoutFile: без оболочки шаблон с фикс-сценой (mountShell) превьюился вне своей сцены. Идентичность экрана берётся от привязанной страницы (pageId), поэтому два экрана на одном варианте (галерея certification) не коллизят.

- **fix**(templates): отсутствие макета или слота не блокирует активацию (2026-07-17) [`b18b1cfd6a001a613caaeedb5629efe4764de256`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b18b1cfd6a001a613caaeedb5629efe4764de256)
  SHELL_CONTRACT, QUESTION_CONTRACT и отсутствие обязательного макета переведены из блокирующих ошибок в предупреждения (PRD-1 §4.3.2, PRD-3 NFR-06): движок штатно переживает отсутствие слота, а хосты рендерят такой экран из стандартного шаблона. Smoke-раннер получил вход fallbackLayouts — экран без своего макета рендерится из стандартного шаблона, а не проваливается.

### Documentation

- **docs**(guide): описать резолвинг макета и mountShell для авторов шаблонов (2026-07-17) [`069b0dbe0dc5f3017c97079fe1c70fdc99891499`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/069b0dbe0dc5f3017c97079fe1c70fdc99891499)
  Гайд утверждал единое «макет из layoutFile» и приводил пример intro.hero, который
  в реальности так не резолвится. Переписан §4.3.1 под три класса резолвинга,
  пример заменён на info.text. Добавлен абзац про mountShell в §4.1 (фикс-сцена,
  типовой симптом «предпросмотр не похож на дизайн») и строка про галерею; уточнён
  fallback старта на стандартный шаблон вместо content.

- **docs**(templates): свести контракт резолвинга макета и fallback к фактическому рантайму (2026-07-17) [`79cae0c6250d983636b8f573ccd8d5ae738ee5d0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/79cae0c6250d983636b8f573ccd8d5ae738ee5d0)
  Спека и PRD-1 описывали резолвинг как единое «макет из layoutFile варианта», а
  рантайм резолвит по трём разным механизмам. Приведено к коду:
  
  \- три класса резолвинга (spec §8.2): системные экраны — по фиксированному ключу
    layouts[], галерея — по ключу варианта, прочие контентные (info/summary/router/
    html) — по layoutFile, иначе общий content;
  \- «Введение раздела» читается по ключу section-intro, а НЕ по layoutFile варианта
    intro (частая ошибка авторов, ломавшая certification2);
  \- fallback на стандартный шаблон — только у экранов с выделенным макетом; router и
    summary его не получают (рендерятся общим content), спека их больше не обещает;
  \- задокументирован флаг mountShell (spec §7.1): монтирование фикс-сцены, без
    которого шаблон рендерится вне своей сцены.

- **docs**(templates): fallback на стандартный шаблон вместо блокировки, контракт layoutFile (2026-07-17) [`f8955e000a5e1ea8d9468530bf9870753cf659f6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f8955e000a5e1ea8d9468530bf9870753cf659f6)
  Разрешён конфликт спек по резервному поведению шаблона: PRD-1 §4.3.2 (fallback на стандартный шаблон с предупреждением) признан источником истины, PRD-3 NFR-05/NFR-06 переформулированы под фактическое поведение. Отсутствие макета или слота больше не блокирует активацию — блокируют только целостность пакета и безопасность. Привязка экрана к макету задаётся полем contentTemplates[].layoutFile (§8.2), ключи вида layouts["content.intro"] убраны из контракта. Полный разбор конфликтов и план — в docs/reports/AUDIT_TEMPLATE_LAYOUT_FALLBACK.md.

## [2.7.1-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.7.0-beta...v2.7.1-beta)

### Features

- **feat**(analytics): расчётная информация в CSV-выгрузке протокола попытки (2026-07-15) [`a6ab29602d1f941ce44a7b14df4cee18d66d64ca`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a6ab29602d1f941ce44a7b14df4cee18d66d64ca)
  В выгрузке попытки (аналитика, web + SCORM) не было расчётных данных, кроме баллов.
  Теперь по каждому вопросу добавлены сложность, доля верности и вклады в шкалы, а в
  конце — сводка по шкалам (значение + уровень) и показателям. Считается при выгрузке
  из сохранённых ответов + конфигурации теста (пересчёт, без миграции БД). Процент
  шкал в выгрузку не идёт — это вспомогательное значение для формул показателей, а не
  результат шкалы (результат = абсолютное значение + уровень из диапазонов).

- **feat**(learner): гейт «Отправить ответ» по готовности ответа и неугадываемый порядок ранжирования (2026-07-15) [`54d22acb30d35d4bebd35430be0a54f529482971`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/54d22acb30d35d4bebd35430be0a54f529482971)
  Чистые хелперы вынесены в answer-gate.ts (юнит-тестируемо, паритет со SCORM):
  hasAnswer гейтит кнопку отправки — пустой ответ отправить нельзя. Ранжирование
  больше не засевается как ответ: выдаваемый порядок гарантированно не равен
  верному (иначе нетронутая раскладка засчиталась бы верной с P=1/N!), поэтому
  кнопка ждёт реальной перестановки.

- **feat**(templates): сертификационный RTK-шаблон (фикс-сцена 16:9) (2026-07-15) [`894a155dc9283d8846e6357c1398375710405b55`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/894a155dc9283d8846e6357c1398375710405b55)
  Новый регистрируемый шаблон оформления по эскизам РТК: системная тема
  (светлая и тёмная) с переопределяемыми брендовыми цветами, фиксированная
  сцена 16:9 (cqh/cqw), страница-роутер, галерея, экран обзора, кнопки
  навигации с пропуском и возвратом к неотвеченным. Добавлен эскиз
  rtk-template-wireframes.html. ZIP-пакеты сборки не коммитятся (артефакты).

- **feat**(templates): монтирование экранов в shell фикс-сцены (mountShell) (2026-07-15) [`9ede4fcb54f6b4088f2c9050ff905201406061f6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9ede4fcb54f6b4088f2c9050ff905201406061f6)
  TemplateScreen получает проп shell: для шаблонов с mountShell экран
  рендерится внутри shell.html (container-type:size 16:9), чтобы cqh/cqw
  масштабировались по ширине хоста, а не по вьюпорту. Модалки превью
  (проверка/страница/шаблон) прокидывают shell, когда манифест объявил
  mountShell.

- **feat**(tooling): офлайн-генератор превью на общем dsl и поддержка shell фикс-сцены (2026-07-15) [`4579304524fa6da114edd653821d2592180c1401`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4579304524fa6da114edd653821d2592180c1401)
  Генератор бандлит настоящий dsl.ts (вместо наивного regex, ломавшего
  вложенные {{#if}}) и монтирует экраны в shell шаблона при mountShell, чтобы
  превью совпадало с рантаймом. Перегенерировано preview.html дефолтного
  шаблона под обновлённый рендер.

- **feat**(scorm): рендер под фикс-сцену, гейт отправки ответа и редизайн карточек роутера (2026-07-15) [`85190b522724bb1ea0877efb2d3346beb75f6ae9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85190b522724bb1ea0877efb2d3346beb75f6ae9)
  Экраны монтируются прямо в #app (без обёртки), чтобы .tb-pad фиксированной
  сцены заполнял её и нижняя навигация якорилась внизу; добавлен ступенчатый
  подгон текста вопроса (--q-fit) со скроллом как крайним вариантом.
  «Отправить ответ»/«Принять» блокируется, пока у текущего вопроса нет
  пригодного ответа (паритет с веб-хостом через hasAnswer). Карточки роутера
  получают изображение, чипы (кол-во/лимит), кнопку «начать» и блок сроков.

- **feat**(template): иллюстрация введения раздела, склонение минут и вид варианта gallery (2026-07-15) [`8455e849d798d72c025df336b4e4ed65890870b9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8455e849d798d72c025df336b4e4ed65890870b9)
  Экран «Введение раздела» получает колонку авторской иллюстрации
  (illustrationUrl/hasIllustration гейтит её), а лимит времени склоняется
  по-русски (1 минута / 2 минуты / 5 минут). В variantKind добавлен вид
  gallery для шаблонов с фикс-сценой.

- **feat**(auth): скрипт учёта остатка bcrypt-хешей — гейт PRD-9 Этап 3 (2026-07-05) [`27742983bc8aca33d10dcad2bb75868e81ba8e8f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/27742983bc8aca33d10dcad2bb75868e81ba8e8f)
  Метрика auth.legacy_bcrypt_rehash in-process и обнуляется при рестарте, поэтому не отвечает на вопрос «остались ли bcrypt-хеши». Скрипт делает прямой read-only подсчёт по БД с разбивкой по статусу/активности и вердиктом SAFE/HOLD — на него и завязывается удаление bcryptjs. Спека PRD-9 актуализирована под реализованные Этапы 1-2.

- **feat**(auth): PRD-9 — хеширование паролей scrypt (@vvlad1973/crypto), ленивый rehash bcrypt→scrypt (2026-07-04) [`b650ac4c34f67a21994bbe4bae24cd621bd19284`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b650ac4c34f67a21994bbe4bae24cd621bd19284)
  \- server/utils/crypto: hashPassword/verifyPassword на scrypt через @vvlad1973/crypto@2.4.0;
    isLegacyBcryptHash + изолированная verifyLegacyBcrypt (dynamic import bcryptjs) для legacy-совместимости
  \- users-repository.validatePassword: перехеширование legacy bcrypt в scrypt при успешном логине
    (счётчик auth.legacy_bcrypt_rehash + info-лог); server/metrics — реестр счётчиков
  \- create-admin/set-password.mjs теперь минтят scrypt; тесты обновлены под scrypt + legacy-путь
  \- зависимость @vvlad1973/crypto поднята до ^2.4.0
  \- bcryptjs осознанно оставлен временной external-зависимостью (удаление — Этап 3 после обнуления метрики)

- **feat**(logging): tree-логирование pino с уровнями из конфигурации (2026-07-03) [`fc8de91cbcac7de2e35b4174ca59e3652752ff10`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fc8de91cbcac7de2e35b4174ca59e3652752ff10)
  Логгер переведён на @vvlad1973/pino-logger-tree + pino-pretty: базовый уровень,
  отдельные уровни консоли/файла и пер-подсистемные оверрайды (db/scorm/auth/...)
  берутся из config.log; трассировка SQL — это db на уровне trace. Роут и страница
  просмотра логов приведены в соответствие.

- **feat**(config): внешний загрузчик конфигурации (JSONC + секреты только в env) (2026-07-03) [`680401415fd1b2b39cc7fc1d8f2bc5384e99afe3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/680401415fd1b2b39cc7fc1d8f2bc5384e99afe3)
  Единый источник настроек — config/&lt;NODE_ENV&gt;.config.jsonc (несекретные значения),
  секреты вынесены в .env и подставляются через { env: VAR } загрузчиком
  (server/config-loader.mjs на базе @vvlad1973/utils getConfig). Сервер, скрипты и
  drizzle читают типизированный config вместо разрозненных process.env; суперадмины
  (PRD-13) переехали из SUPERADMIN_EMAILS в config.access.superadminEmails
  (server/superadmin.ts, резолв по SHA-256 хешу). .env.example сведён к секретам,
  .gitignore игнорирует локальные config/*.local.jsonc.

- **feat**(debug-player): фиксация вариантов тем на вкладке «Выдача» (2026-07-03) [`ba2a9f1d069a72963807150dcb3022e08f2946e4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ba2a9f1d069a72963807150dcb3022e08f2946e4)
  Методолог может закрепить, какой вариант темы (PRD-17 formSet) выдать до
  старта прогона, вместо случайного выбора — чтобы осмотреть конкретный вариант
  без перезапусков «на удачу». Пин { topicId: formId } едет в стейдж через hash
  launch-URL (#tbff=); рантайм читает его в generateVariant под guard'ом и
  инертен в проде (WebTutor запускает пакет без hash, на другом origin). У каждой
  темы в режиме вариантов — свой независимый селект; после старта он заблокирован
  (варианты фиксируются в момент выдачи), «Сброс» возвращает выбор. Эскиз вкладки
  «Выдача» обновлён и согласован.

### Fixes

- **fix**(prd-6): экран блокировки cooldown — рендер в актуальный #app после mountShell (2026-07-16) [`11ae0c833bfdb579aa0552bb736344dd7360d6ad`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/11ae0c833bfdb579aa0552bb736344dd7360d6ad)
  Боевой прогон: гейт логировал «cooldown state rendered on start page», но экран
  оставался на «Загрузка теста…». Корень: шаблоны с mountShell:true (напр.
  certification) в applyTemplateShell ЗАМЕНЯЮТ узел #app на #app оболочки. А
  renderCooldownStart/renderBlockWall брали ссылку на #app ДО загрузки шаблона и
  писали в старый, открепившийся узел — контент рендерился вне видимого DOM, а
  перенесённый в новый #app загрузчик #loading оставался виден.
  
  Теперь #app берётся заново ВНУТРИ .then(ensureTemplate) — после mountShell.
  Fallback-стена и таймаут-сетка тоже берут #app свежим на момент записи.
  
  Проверено локально на shell-mount сборке (mountShell включён в default): контент
  в правильном #app, загрузчик убран. Default без mountShell не затронут.

- **fix**(prd-6): не зависать на экране блокировки cooldown при висящем fetch шаблона (2026-07-16) [`d9dd934fded08c1417391ac1b65ec8830d643522`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d9dd934fded08c1417391ac1b65ec8830d643522)
  Боевой прогон на WebTutor: решение гейта отрабатывает верно (blocked), но экран
  блокировки не отрисовывался — учащийся оставался на «Загрузка теста…». Причина:
  на cplayer2 (SCO в sandboxed iframe) package-local fetch шаблона может ЗАВИСНУТЬ
  (pending без ответа и без ошибки), а у loadDesignTemplate есть .catch, но нет
  таймаута — Promise.all не резолвится, рендер не наступает.
  
  Два предохранителя: таймаут в ensureTemplate (4с -&gt; null -&gt; встроенная стена) и
  защитная сетка в renderCooldownStart (если за 4.5с ничего не отрисовано или
  рендер бросил исключение — показываем встроенную стену, чистый innerHTML без
  fetch). Диагностика рендера добавлена. `settled`-флаг гасит ложный timeout-лог,
  когда шаблон успел загрузиться.

- **fix**(prd-6): cooldown по учебным записям WebTutor вместо карточки — любой исход (2026-07-16) [`b0b0c14a76ee132c672031683cd60e3e130784c9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b0b0c14a76ee132c672031683cd60e3e130784c9)
  Регрессия: боевой источник webtutor_cooldown был переключён на скрап карточки
  курса (get_metadata, маркер best_learn_step_success), которая несёт дату только
  для «Пройден». Из-за этого cooldown не срабатывал после проваленной попытки.
  
  Восстановлен корректный источник — учебные записи учащегося из ExtJS-коллекции
  (POST /pp/Ext5/extjs_json_collection_data.html: collection_code + parameters +
  secid), контракт подтверждён на живом портале. Дата берётся из last_usage_date,
  завершённость определяется полем state («Пройден»/«Не пройден» — оба исхода),
  дата-заглушка 31.12.9999 отбрасывается. Разные назначения одного курса
  считаются одним курсом: матчинг по name (= заголовок теста), берётся самая
  поздняя дата. secid скрапится со страницы; cur_person_id не нужен — endpoint
  привязан к сессии.
  
  Автора предупреждает баннер в редакторе: ограничение сработает только если
  название курса в LMS совпадает с названием теста.
  
  Добавлен тест-барьер: проваленная завершённая попытка обязана блокировать
  повтор; агрегация назначений; отбрасывание заглушки. Диагностика гейта развёрнута
  (причина решения, ошибки failPolicy, скрап-цепочка) — прежде молчала.

- **fix**(scales): нормализация процента по выданным вопросам, а не по всему банку (2026-07-15) [`69030b911d250fd52b8c942e2e9582df55732ddb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/69030b911d250fd52b8c942e2e9582df55732ddb)
  percent считался с диапазоном minPossible/maxPossible по ВСЕМУ банку измеряемых
  вопросов, а raw — только по выданной выборке. Невыданные вопросы дают 0 в raw, но
  их положительный минимум суммировался в min → raw &lt; min → отрицательный процент на
  100%-верной попытке (каскадило на показатели вида scale.X.percent &gt;= 70). По
  PRD-5 §5.2 диапазон считается по фактически выданным вопросам (те, что в answers):
  single допускает 0 (неверный ответ = 0), multiple/matching/ranking суммируют
  активные единицы; при вырожденном диапазоне — диагностика, а не число (§5.2, стр.329).
  Правка синхронно в обоих движках (shared + package), golden-parity держится.
  
  Плюс: чистый хелпер computeAnswerContributions (вклады одного ответа по шкалам) —
  используется выгрузкой протокола аналитики.

- **fix**(scales): ввод дробных вкладов через запятую и сохранение отрицательных (2026-07-15) [`0253dfa33eeed3408efbabaf2188fdac45a98cbc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0253dfa33eeed3408efbabaf2188fdac45a98cbc)
  Дробный вклад вводился только через точку: Number("0,5") = NaN, поэтому
  фиксировалась лишь целая часть и значение сохранялось как 0. Отрицательный
  вклад терял знак из-за ре-синхронизации, переписывавшей поле в String(-0)
  = "0". Общий хелпер numeric-input парсит запятую (точка принимается и
  показывается запятой), сохраняет отрицательные, а ре-синк больше не затирает
  незавершённый ввод. Та же правка применена к порогам диапазонов шкал.

- **fix**(security): вынести сидер из DAL в script/seed-db.ts, не сеять в проде (2026-07-04) [`2e684a28748a5f63ca229d8c352ba1c4f7d2662d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2e684a28748a5f63ca229d8c352ba1c4f7d2662d)
  seedDatabase жил в server/storage.ts и вызывался БЕЗУСЛОВНО на старте сервера
  (server/index.ts) — на свежей продовой БД он создавал демо-аккаунты admin@test.com
  /admin123 и learner@test.com/learner123, реальная дыра. Функция вынесена в ручной
  dev-скрипт script/seed-db.ts (npm run seed, рядом с create-admin): отказывается
  запускаться при NODE_ENV=production (exit 1 до касания БД), креды переопределяемы
  через env (SEED_ADMIN_EMAIL/PASSWORD, SEED_LEARNER_EMAIL/PASSWORD) с dev-дефолтами,
  хеширование через шов hashPassword. Авто-вызов со старта сервера убран — прод-БД
  демо-аккаунтов больше не получает; в dev сид запускается вручную.

- **fix**(auth): убрать timing-enumeration в validatePassword (2026-07-04) [`bcdf792247b387372467415dfc6ddb87c00f3a6c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bcdf792247b387372467415dfc6ddb87c00f3a6c)
  При неизвестном e-mail validatePassword возвращал null мгновенно, тогда как для
  существующего аккаунта тратил время на bcrypt-сверку — разница во времени ответа
  позволяла перечислять зарегистрированные адреса. В шов crypto.ts добавлен
  dummyVerifyPassword: сверка против фиксированного хеша той же стоимости
  (вычисляется один раз, лениво), всегда резолвится. Ветка «пользователь не
  найден» вызывает его перед return null, поэтому время ответа не зависит от
  существования аккаунта. Тест storage.test проверяет вызов фиктивной сверки.

- **fix**(db): белый список записываемых колонок в update-методах (2026-07-04) [`9b0cec7de62203b74ba7e4676b481295b1c92e50`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9b0cec7de62203b74ba7e4676b481295b1c92e50)
  updateUser/updateGroup/updateAttempt принимали широкий Partial&lt;Row&gt; и клали его
  в .set() (updateUser вдобавок через any-каст), поэтому вызывающий с расширенным
  телом мог перезаписать чувствительные колонки: passwordHash, emailHash, id,
  аудит-поля, а у попытки — userId/testId/startedAt. Введён хелпер pickDefined,
  копирующий только разрешённые присутствующие ключи; каждый метод объявляет свой
  whitelist (user: name/status/mustChangePassword/gdprConsent(+At), email отдельно
  с шифрованием и производным emailHash; group: name/description; attempt:
  variantJson/answersJson/resultJson/finishedAt). Пустой набор -&gt; no-op (возврат
  текущей строки). any-каст убран. Мок-тест updateAttempt переведён с
  несуществующей колонки score на finishedAt.

- **fix**(db): инварианты копии темы и полная атомарность мутаций DAL (2026-07-04) [`39e46057d9786b820775602436cad6251100fc50`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/39e46057d9786b820775602436cad6251100fc50)
  duplicateTopicWithQuestions больше не оставляла у копии темы NULL в
  nameNormalized/ownerId/visibility/folderId (расхождение с контрактом владения и
  одноимённости) и была многошаговой без транзакции. Теперь копия строится общим
  билдером topicInsertValues (единый источник инвариантов с createTopic): владелец
  = дублирующий, приватная видимость, сохранённый folderId; код не переносится
  (per-test алиас формул); имя уникально в пределах владельца, поэтому повторное
  дублирование не конфликтует с owner-scoped индексом; вопросы копируются с
  сохранением contentHash/difficulty/createdBy. Topic и вопросы вставляются одной
  транзакцией.
  
  Полный аудит транзакций закрыл ещё три многошаговые мутации: renameTopicInFormulas
  (пакет update по формулам одной транзакцией), deleteAdaptiveLevelsByTest и
  deleteAdaptiveLevelLinksByTest (циклы по уровням заменены одним subquery-delete,
  levels+links атомарно). Маршрут POST /:id/duplicate передаёт дублирующего
  пользователя. Мок-заглушки storage.test/storage2.test приведены к новой
  реализации (transaction как vi.fn, adaptive-удаления без итерации).

- **fix**(db): единый атомарный deleteTest без сирот (2026-07-04) [`1c08ba67fe24a807e888a9a5ceacd199c5ef8234`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1c08ba67fe24a807e888a9a5ceacd199c5ef8234)
  Удаление теста было размазано по трём слоям (adaptive-очистка в маршруте,
  test_sections в deleteTest, FK-каскад для пяти таблиц) и оставляло сиротами
  attempts, test_assignments, test_access_grants, test_snapshots — строки со
  ссылкой на несуществующий тест (attempts.testId notNull не может «висеть»).
  
  deleteTest стал единственным атомарным владельцем удаления: в одной транзакции
  чистит adaptive (links через подзапрос по их levels), test_sections,
  test_assignments, test_access_grants, attempts и test_snapshots; FK ON DELETE
  CASCADE уносит content_pages/scales/result_variables/question_measurements/
  test_question_scoring. Из DELETE /:id убрана ручная adaptive-очистка.
  
  Retention: attempts и test_snapshots удаляются — жёсткое удаление необратимо
  (archive — отдельный обратимый путь), осиротевшие попытки не нужны. SCORM
  (scorm_packages/attempts/answers) сохраняется — scorm_packages.testId nullable
  by design, экспортированный пакет живёт в LMS независимо от теста.

- **fix**(db): атомарность многошаговых мутаций DAL (2026-07-04) [`81817b8547b215f4f6f6490a79f876f2297b7b54`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/81817b8547b215f4f6f6490a79f876f2297b7b54)
  Удаления/replace-методы делали несколько изменяющих запросов подряд вне
  транзакции — сбой на середине оставлял БД в противоречивом состоянии (например,
  setUserGroups успевал удалить старые группы, но падал на вставке новых, оставляя
  пользователя без групп). Восемь таких методов обёрнуты в db.transaction:
  deleteTopic, deleteTopicsBulk, deleteGroup, deleteFolder, deleteTestFolder,
  deleteTestFolderCascade, setUserGroups, setUserRoles — теперь частичный сбой
  откатывается целиком.
  
  deleteGroup переведён с result.rowCount на .returning().length (доминирующая
  идиома проекта, портируемо между драйверами). Атомарность и happy-path покрыты
  интеграционными тестами на pglite (tests/it/dal-transactions.it.test.ts);
  мок-заглушки storage.test/storage2.test дополнены transaction. Харнесс: помечено,
  что pglite не заполняет rowCount для DELETE/UPDATE без RETURNING.

- **fix**(db): восстановить снесённые push-ом констрейнты корректности показателей/шкал (2026-07-04) [`f2647f2719b1a851efd39a7ffb464c62c7368cac`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f2647f2719b1a851efd39a7ffb464c62c7368cac)
  drizzle-kit push приводит БД к schema.ts и сносит объекты, объявленные только в
  SQL-миграциях. Так из миграций 008/009 пропали инварианты корректности
  result_variables/scales: партиальные контроллеры (не более одного контроллера
  success и одного completion на тест), natural-unique (test_id,name)/(test_id,key)
  (имя/ключ адресуются в формулах DSL, дубль неоднозначен) и regex-CHECK на
  name/key. Их отсутствие давало, в частности, неоднозначный cmi.success_status.
  
  Объявлены в schema.ts через check() и uniqueIndex().on(...).where(...) (доступны
  в drizzle-orm 0.45) — теперь push их поддерживает. Применяется через db:push
  (dev-таблицы пусты, ALTER безопасен). Enum/JSON-CHECK не восстанавливались —
  дублируют валидацию Zod. Прод: скан нарушений обязателен ДО выкатки (данные
  могли накопить нарушения за период без констрейнтов). Схема харнесса
  tests/it/schema.sql регенерирована.

- **fix**(scales): строка диапазонов умещается в панель дровера (2026-07-04) [`a08e5c41792758a394e631b71232ee4877054b9c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a08e5c41792758a394e631b71232ee4877054b9c)
  На вкладке «Шкалы» строка «Диапазоны (пороги)» (min/max/метка/уровень + удаление)
  вылезала за правый край: DS-Input вне .ou-formfield держит intrinsic min-width, из-за
  чего таблица (auto-layout, width:100%) распухала шире панели, а панель скроллится
  только по вертикали. Перевёл таблицу на table-layout:fixed (+ colgroup, узкая колонка
  действий) и сделал инпуты на всю ширину ячейки со min-width:0 — колонки делят
  доступную ширину и умещаются на широкой и узкой панели.

- **fix**(tests): не терять section.formSetJson при сохранении (PRD-17) (2026-07-04) [`759f3fce96e8e007f3636b3a5bdfbaeda568d5ec`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/759f3fce96e8e007f3636b3a5bdfbaeda568d5ec)
  sectionBodySchema в роуте не перечисляла formSetJson, а Zod по умолчанию вырезает
  неизвестные поля — поэтому набор вариантов темы молча выкидывался до записи в БД:
  ответ 200 и тост «сохранено», но в БД пусто, при переоткрытии изменений нет (и в
  отладчике не появлялся селектор варианта — данных не было). Добавил formSetJson в
  схему секции + регресс-тесты (round-trip до сервиса сохранения; отклонение набора
  &lt; 2 вариантов).

- **fix**(config): суперадмины из SUPERADMIN_EMAILS (per-deployment), а не пустой список (2026-07-04) [`4548296d0885e388fda6ba2f6e32d11c4c1783cf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4548296d0885e388fda6ba2f6e32d11c4c1783cf)
  Рефакторинг конфигурации оставил access.superadminEmails=[] во всех config/*.jsonc
  и убрал SUPERADMIN_EMAILS из .env.example. Итог: после деплоя аккаунт суперадмина
  терял все роли — экран «не назначено ни одной роли» (роли суперадмина не хранятся в
  БД, а вычисляются из этого списка). Список суперадминов — per-deployment значение,
  поэтому берём его из env (как DATABASE_URL/SESSION_SECRET) через { env:
  SUPERADMIN_EMAILS }; test.config.jsonc несёт явных тестовых админов; вернул
  SUPERADMIN_EMAILS в .env.example.

### Documentation

- **docs**: снятие движка правил курса из требований (2026-07-05) [`81bbf132bc37a29330295b716c34fa8a98f30c11`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/81bbf132bc37a29330295b716c34fa8a98f30c11)
  Продукт — конструктор тестов, а не курсов: универсальный движок правил
  (course_rules, события/условия/действия) и графовый режим section_graph выведены
  из охвата. Логика теста задаётся сценарием (flowPolicy: flowMode/completionPolicy/
  sectionUnlockRules/passDecisionPolicy), результат — шкалами и показателями. Снято в
  BRD (BR-03, Этап 3), PRD-4 и PRD-8; зачищены ссылки в PRD-2/PRD-3 и в архитектурной
  заметке о структуре настроек. Сценарные pass/unlock-политики и section.* сохранены.

- **docs**(templates): актуализация руководства и спецификации платформы шаблонов (2026-07-05) [`e680d98a7c66bad651066d86dc19896fc31c8032`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e680d98a7c66bad651066d86dc19896fc31c8032)
  Сверка docs/guides/template-development.md и docs/specs/spec-template-platform.md
  с фактическим кодом рендера/валидатора: врезки «Реализация», недостающие системные
  узлы раздела PRD-19 (review/section-results), полный публичный контекст, коды
  валидатора (LAYOUT_TEMPLATE_SYNTAX), поправлены слоты вопроса и природа ошибок DSL.
  Движок правил, переменные и правило-ориентированный Runtime API вынесены как «вне
  охвата»: логика теста задаётся сценарием (flowPolicy).

- **docs**: актуализация аудита и плана архитектурных требований на 2026-07-05 (2026-07-05) [`2c964a935b698281f482ae37faa2f74819c31eeb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2c964a935b698281f482ae37faa2f74819c31eeb)
  Статусы задач и находок перепроверены по коду версии 2.7.0-beta: закрыта Фаза 0
  (секреты вне репозитория), учтены переход на 5 ролей (PRD-13), React 18-&gt;19 + DS,
  config-слой, graceful shutdown/heartbeat, конфигурируемый cookie.secure. В аудите
  добавлен раздел «Что изменилось с первичного аудита». Заодно поправлены ссылки под
  перенос runbook'ов в docs/runbooks/.

- **docs**(prd-6): закрытие на Phase 1, вынос администрируемого реестра в PRD-21 (2026-07-05) [`ce556a0a2845e7ffc2840e0b35242a02cb9c9307`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ce556a0a2845e7ffc2840e0b35242a02cb9c9307)
  Рантайм-ценность cooldown (гейт до Initialize, block-wall, ленивый источник даты через
  ClientBridge get_metadata) поставлена и работает на обоих ученических хостах — PRD-6
  закрывается на Phase 1. Администрируемый реестр eligibility-плагинов и их конфигураций
  (редактирование endpoint'ов/фильтров, выбор из &gt;=2 конфигураций, диагностика) выделен в
  самостоятельный PRD-21: у него отдельный объём (админ-UI + персистентность) и свой
  LMS-зависимый риск приёмки (нет живого WebTutor). BR-07-19 в BRD фиксирует перенос.

- **docs**: нормализация структуры docs/, удаление завершённых процессных документов (2026-07-05) [`ca682f8e4e8cc2af5e8f47c7d1fcbcc1829f6059`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ca682f8e4e8cc2af5e8f47c7d1fcbcc1829f6059)
  Приведение документации в соответствие с DOCUMENTATION-GUIDELINES:
  \- runbook'и перенесены в docs/runbooks/, гайд — в kebab-case (design-guidelines.md);
  \- удалены завершённые процессные документы (правило «план/todo/handoff/„как мы сделали"
    удаляется после завершения»): PLAN_appshell_migration, PLAN_storage_refactor(+todo),
    access-control/implementation-plan, prd-15/implementation-plan, prd-19/HANDOFF;
  \- ROADMAP: история релизов вынесена из поля «Последняя актуализация» (дублировала CHANGELOG
    и таблицу §0) — шапка схлопнута до даты + указателей на единые источники правды;
  \- починены перекрёстные ссылки под новые пути; README актуализирован под структуру docs/.

- **docs**(architecture): актуализация service-architecture под фасад DAL и scrypt (2026-07-05) [`d00e310dc210a4ab707781de054a15ce71bbaf91`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d00e310dc210a4ab707781de054a15ce71bbaf91)
  DatabaseStorage описан как тонкий делегирующий фасад над 14 доменными репозиториями (server/storage/*); отражены транзакции многошаговых мутаций, белый список записываемых колонок и переход хеширования паролей на scrypt (PRD-9).

- **docs**: BRD аппарата психометрической аналитики тестов (2026-07-05) [`fc8fa1aa1f5c7a5b7bb9d6e863d03ca73d625174`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fc8fa1aa1f5c7a5b7bb9d6e863d03ca73d625174)
  CTT + надёжность + дистракторы; источники — телеметрия SCORM, импорт обезличенных
  Excel-выгрузок из LMS, веб-прохождения; версионирование содержания (psychoHash).
  Требования на согласование; IRT вне охвата итерации.

- **docs**(storage): статус финальной чистки импортов фасада (2026-07-04) [`a1bbdcb86fafa8269ded335c3f1730299a3ad205`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a1bbdcb86fafa8269ded335c3f1730299a3ad205)
  Отмечен завершающий пункт: все 14 доменов за репозиториями, тело фасада —
  чистая делегация, мёртвые импорты удалены. Рефакторинг storage.ts закрыт.

- **docs**(storage): статус извлечения токенов сброса в UsersRepository (12.14) (2026-07-04) [`634e32ddaa4d15a8b5955c7ac07e3c7c010fec5b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/634e32ddaa4d15a8b5955c7ac07e3c7c010fec5b)
  Пункт 12.14 отмечен выполненным; тело фасада стало чистой делегацией во всех
  доменах. Осталась финальная чистка мёртвых импортов фасада.

- **docs**(storage): статус извлечения FoldersRepository (12.13) (2026-07-04) [`3cceed995420eb8323cdee6dc70d4d3ae8b7a39b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3cceed995420eb8323cdee6dc70d4d3ae8b7a39b)
  Пункт 12.13 отмечен выполненным (оба дерева папок за фасадом,
  каскады папок атомарны).

- **docs**(storage): статус извлечения AssignmentsRepository (12.12) (2026-07-04) [`53dc41baa36cd9f0b0a24fec58c4a9df4f34f4bc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/53dc41baa36cd9f0b0a24fec58c4a9df4f34f4bc)
  Пункт 12.12 отмечен выполненным (кросс-доменные чтения инлайнены,
  фасад по назначениям стал чистой делегацией).

- **docs**(storage): статус извлечения ContentPagesRepository (12.11) (2026-07-04) [`f17594a1478232ca15b696d215a39335a6499d92`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f17594a1478232ca15b696d215a39335a6499d92)
  Пункт 12.11 отмечен выполненным (первый из хвостовой четвёрки полного сплита).

- **docs**(storage): статус извлечения TestsRepository (12.6) (2026-07-04) [`2f4532b8227f282cd83561614e3de995f9767cde`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2f4532b8227f282cd83561614e3de995f9767cde)
  Пункт 12.6 отмечен выполненным; добавлены хвостовые подпункты полного сплита
  (12.11 ContentPages, 12.12 Assignments, 12.13 Folders, 12.14 PasswordReset
  в UsersRepository), после которых фасад становится чистой делегацией.

- **docs**(storage): статус извлечения ScalesVariablesRepository (12.10) (2026-07-04) [`70ea9b0816d6488b6bdd29b49ef97f4fc9b954fc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/70ea9b0816d6488b6bdd29b49ef97f4fc9b954fc)

- **docs**(storage): статус извлечения AttemptsRepository (12.7) (2026-07-04) [`09388524ec7f3662972e71911f52e8d4e4634e9e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/09388524ec7f3662972e71911f52e8d4e4634e9e)

- **docs**(storage): статус извлечения AdaptiveRepository (12.9) (2026-07-04) [`ff409ffc599795aefd5d05cd241da9baf876e5b2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ff409ffc599795aefd5d05cd241da9baf876e5b2)

- **docs**(storage): смена порядка (Tests последним) + статус ScormRepository (12.8) (2026-07-04) [`1d70b801291d2b7e89b27d036ecc57828fce451e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1d70b801291d2b7e89b27d036ecc57828fce451e)

- **docs**(storage): статус извлечения QuestionsRepository (12.5) (2026-07-04) [`5033b13753c2ff6f4212bb758fbbce51bbcf92b9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5033b13753c2ff6f4212bb758fbbce51bbcf92b9)

- **docs**(storage): правило границы (каскады корневого агрегата) + статус Topics (12.4) (2026-07-04) [`7044f41130e931c93fb5daf57ba8d95417cb8154`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7044f41130e931c93fb5daf57ba8d95417cb8154)

- **docs**(storage): статус извлечения AccessRepository (12.3) (2026-07-04) [`fd1e2c9919b2b36f53f095dc4691796e39bcae8e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fd1e2c9919b2b36f53f095dc4691796e39bcae8e)

- **docs**(storage): статус извлечения GroupsRepository (12.2) (2026-07-04) [`45887b2eac0b3933227c30b8fe67031395a88a3c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/45887b2eac0b3933227c30b8fe67031395a88a3c)

- **docs**(storage): паттерн разбиения на репозитории + статус пилота Users (12.1) (2026-07-04) [`bd364980423a69eac7c845d2ca4e46b2cac22f83`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bd364980423a69eac7c845d2ca4e46b2cac22f83)
  Раздел 6.1: композиция-делегирование, раскладка каталог/фасад, статус пилота;
  чек-лист: пункт 12 разбит на подпункты 12.1-12.10, Users закрыт.

- **docs**(storage): статус пункта 13 (мёртвый код + единый JSDoc) (2026-07-04) [`3cde0d33491637a119c70d2f037f5740b259ecb6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3cde0d33491637a119c70d2f037f5740b259ecb6)
  Разделы 6.3/6.4 и чек-лист: аудит вызовов кандидатов, удаление getTopicByName,
  сохранение mapLegacyTest с уточнением что getMigrationHealth его не гейтит.

- **docs**(storage): статус достройки контракта IStorage (пункт 11) (2026-07-04) [`043f2045b15b4e523b031fd5e231281fef313fe8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/043f2045b15b4e523b031fd5e231281fef313fe8)
  Раздел 6.2 и чек-лист: 12 методов добавлены, getTopicByName отложен в удаление
  мёртвого кода, касты сняты.

- **docs**(storage): статус выноса сидера (пункт 10) (2026-07-04) [`1ba0a3a55c10de90b46bf845b49930aeb73b0486`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1ba0a3a55c10de90b46bf845b49930aeb73b0486)
  Раздел 5.4 и чек-лист: seedDatabase вынесен в script/seed-db.ts с гейтом против
  production; авто-сид со старта сервера убран. Приоритет безопасности завершён.

- **docs**(storage): статус анти-enumeration в validatePassword (пункт 9) (2026-07-04) [`04f9cfae782ae90f4ea372f087bd6601b7a974b6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/04f9cfae782ae90f4ea372f087bd6601b7a974b6)
  Раздел 5.3 и чек-лист: dummyVerifyPassword в ветке «пользователь не найден».

- **docs**(storage): статус централизации хеширования пароля (пункт 8) (2026-07-04) [`67a76ffc6e408497f3661d731e7181a816fe2631`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/67a76ffc6e408497f3661d731e7181a816fe2631)
  Раздел 5.2 и чек-лист: единый шов crypto.ts, DAL без bcryptjs, тест обёрток;
  пункт 9 сужен до фиктивной сверки (verifyPassword-шов уже из пункта 8).

- **docs**(storage): статус белого списка колонок (пункт 7) (2026-07-04) [`f6194d2ce278c2d76d1b0582df6d4ee4671c1529`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f6194d2ce278c2d76d1b0582df6d4ee4671c1529)
  Раздел 5.1 и чек-лист: pickDefined + whitelist в update-методах, тест
  mass-assignment; закрыт первый пункт приоритета безопасности.

- **docs**(storage): статус консолидации писателя секций (пункт 6) (2026-07-04) [`a56f8497ae1c49ecf3aeca9bf9f8801c3e6d9ac7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a56f8497ae1c49ecf3aeca9bf9f8801c3e6d9ac7)
  Раздел 3.4 и чек-лист: единственный живой писатель = TestSettingsService,
  консолидация выполнена удалением двух мёртвых копий; round-trip на живом пути.

- **docs**(storage): актуализация планов — транзакции и дубль темы (2026-07-04) [`fdf041850d9e4d8a9eaa07943968d81747ff213a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fdf041850d9e4d8a9eaa07943968d81747ff213a)
  Раздел 3.1: отмечен полный охват транзакций (8 по списку + 4 найденных аудитом).
  Раздел 3.3 и чек-лист: дубль темы через общий билдер инвариантов и атомарно.
  Чек-лист приведён в актуальное состояние (закрыты пункты 1-5, версия 1.1).

- **docs**(storage): статус атомарного удаления теста (2026-07-04) [`ba407c6371fa0f93f797b033603589e771bdfa68`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ba407c6371fa0f93f797b033603589e771bdfa68)
  Раздел 3.2 и чек-лист отмечены выполненными: deleteTest как единственный
  владелец, политика retention (attempts/snapshots удаляются, SCORM сохраняется),
  тест «ноль сирот» и вынос интеграционных тестов в test:it.

- **docs**(storage): статус реконсиляции констрейнтов корректности (2026-07-04) [`c1b3f34c27947da85cb7851da5d9004f507b1b34`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c1b3f34c27947da85cb7851da5d9004f507b1b34)
  Раздел 2.1 дополнен natural-unique (test_id,name)/(test_id,key); отмечено
  выполнение на dev (объявление в schema.ts, push, интеграционный тест) и
  оставшийся deploy-time шаг — скан нарушений на проде перед выкаткой.

- **docs**(storage): план рефакторинга DAL и реконсиляции констрейнтов (2026-07-04) [`f16d39c9e56ef47ff0a4fddc7e387dfc81cad7bd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f16d39c9e56ef47ff0a4fddc7e387dfc81cad7bd)
  Аналитический план рефакторинга server/storage.ts (целостность,
  производительность, безопасность, поддерживаемость) + чек-лист прогресса и
  отдельная спека реконсиляции констрейнтов корректности, снесённых push-ом
  (партиальные контроллеры result_variables, regex/enum CHECK миграций 008/009).

- **docs**: разложить docs по каталогам plans/ и reports/ (2026-07-04) [`a75cb27da93ee385d1027efa845b221c8dfe2fbe`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a75cb27da93ee385d1027efa845b221c8dfe2fbe)
  Планы (PLAN_*) переехали в docs/plans/, аудиты (AUDIT_*) — в docs/reports/.
  Чистое перемещение без изменения содержимого.

- **docs**(prd-20): актуализация roadmap и плана таймера (Фазы 1-2) (2026-07-04) [`d3f8789af7327dca248b9e989e2ad3900f595a12`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d3f8789af7327dca248b9e989e2ad3900f595a12)
  ROADMAP 1.13 и план PRD-20 приведены к состоянию реализованных Фаз 1-2
  «Устойчивость таймера и возобновление timed-тестов»: монотонный таймер,
  baseline-anchor кросс-reload целостности, единообразное возобновление по режимам.

- **docs**(changelog): 2.7.0-beta (2026-07-02) [`7ea2040b912e72aa440517ddd9d3fe7ea3d908d0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7ea2040b912e72aa440517ddd9d3fe7ea3d908d0)

## [2.7.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.6.0-beta...v2.7.0-beta)

### Features

- **feat**(scorm): PRD-20 — устойчивый таймер и возобновление timed-тестов (2026-07-02) [`2632991b6ccc923ac8f51bf959d34e3d23be9ca3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2632991b6ccc923ac8f51bf959d34e3d23be9ca3)
  Таймер экзамена переведён на монотонный performance.now(): устранён дрейф в фоне и выигрыш от перевода часов в сессии. Кросс-reload восстановление на модели активного времени (cmi.total_time + baseline-anchor в suspend_data) — timed-тесты снова резюмятся (снят технический guard, adaptive остаётся исключением); периодический commit session_time даёт kill-устойчивость; tamper-evidence на anchor превращает правку suspend_data в потерю resume, а не «фри тайм»; router продолжает незавершённую тему с точной позиции. Любой сбой персистентности деградирует к прежнему поведению. LMS-валидация — в техдолге.

### Documentation

- **docs**(prd-20): актуализация требований под реализацию (2026-07-02) [`f6f69d358fb058179dff17f308f7c96ceef4188a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f6f69d358fb058179dff17f308f7c96ceef4188a)
  Требования приведены в соответствие с кодом: tamper-evidence — keyed djb2-checksum на anchor (не HMAC на весь блоб); watchdog отката часов снят (модель активного времени не держит клиентские часы в контуре доверия) → флаг аномалии при регрессе total_time; счётчики reload/сессий отложены; Фаза 3 — 5.12/5.14 реализованы, 5.13 закрыта моделью (активное время = «пауза при закрытии»); runtime-таблица дополнена timerCommitInterval/timerAnchorTampered; привязка к BR-14.

- **docs**(brd): BR-14 — устойчивость таймера и возобновление timed-тестов (2026-07-02) [`fd69693ccc9ce93a5af6ba1050931571f48f15d3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fd69693ccc9ce93a5af6ba1050931571f48f15d3)
  Добавлено бизнес-требование BR-14 (Этап 15, PRD-20): реальное время без выигрыша от троттлинга/перевода часов, восстановление таймера и прогресса timed-тестов на reload по активному времени, единообразие по режимам BR-06, kill-устойчивость, tamper-evidence (дезтеррент), деградация и приёмка на целевой LMS (техдолг). Плюс два риска: LMS-специфичность total_time и подделываемость клиентского учёта (уровень B).

- **docs**: актуализация документации под релиз 2.6.0-beta (2026-07-02) [`48faa82364d707464e4188f96f7b082b29b65308`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/48faa82364d707464e4188f96f7b082b29b65308)
  CHANGELOG 2.6.0-beta; README/ROADMAP/architecture/AUDIT и PRD-16/19 приведены в соответствие с осью контента и групповыми операциями «Папки и темы» (модульные роутеры, bulk-эндпоинты topics/folders, двухрежимное удаление папки, partial-batch с защитой контента).

- **docs**(prd-20): спецификация и план реализации устойчивого таймера (2026-07-02) [`e159d1e5b0c69f583bccaf109cf1fe4e39532575`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e159d1e5b0c69f583bccaf109cf1fe4e39532575)
  PRD-20 (Фазы 1-2) и поэтапный план реализации Фазы 2: модель активного времени (cmi.total_time + performance.now), tamper-evidence на anchor, единообразное возобновление по режимам PRD-4, деградация и чек-лист LMS-валидации (техдолг перед включением в прод).

## [2.6.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.5.1-beta...v2.6.0-beta)

### Features

- **feat**(tests): открытие редактора теста двойным кликом по строке (2026-07-01) [`25fd4aa913b8054ea6d16628b240a1a4242f736f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/25fd4aa913b8054ea6d16628b240a1a4242f736f)
  Одинарный клик по строке больше не открывает дровер; кнопка «Редактировать» (и клавиатура) сохраняют одинарный клик. Курсор строки → default.

- **feat**(content): групповые операции «Папки и темы» в дереве контента (2026-07-01) [`8fb870546cd39c213bd4c013704c1d98e4cc3860`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8fb870546cd39c213bd4c013704c1d98e4cc3860)
  Два взаимоисключающих режима выделения; выбранная папка захватывает свои темы транзитивно. Бар групповых действий: перенос (темы→папка, папки→reparent), доступ (выдать/отозвать/видимость/владелец, у папок доступа нет — действует на темы), удаление через сводную защиту. Одиночное удаление темы теперь тоже через подтверждение/защиту (не мгновенно).

- **feat**(content): серверные групповые операции с темами и двухрежимное удаление папки (2026-07-01) [`ae0dbf3001a592e6232483407a2b356fe369ed24`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ae0dbf3001a592e6232483407a2b356fe369ed24)
  Темы: bulk-delete (partial-batch — незаблокированные удаляются, используемые в опубликованных тестах пропускаются, admin ?force), bulk-move/visibility/owner/grant/revoke (soft+hard). Папки: DELETE в режимах «перенести содержимое»/«каскад» через content-guard, bulk-reparent с защитой от цикла. Путь выдачи/оценки права на тему не проверяет (инвариант PRD-15).

### Fixes

- **fix**(content): дропдаун в модалках перемещения не обрезается краем модалки (2026-07-01) [`64a2062209ed03b8d429bec70cb345ebd35894a3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/64a2062209ed03b8d429bec70cb345ebd35894a3)
  «Переместить тему в папку»: DS Select (абсолютный .ou-select__menu клиппился телом модалки, overflow-y:auto) заменён на FolderTreeSelect — его поповер портируется в &lt;body&gt;, как в соседней модалке создания папки; заодно UX-дерево вместо плоского списка. «Переместить вопросы в тему»: там выбор темы (не папки), поэтому точечный overflow-оверрайд tb-select-modal — меню выходит за тело короткой модалки (контейнер не клиппит, z-index меню 50 &gt; футера).

### Documentation

- **docs**(wireframes): групповые операции с папками и темами в эскизе «Темы и вопросы» (2026-07-01) [`8785151ca7040f897dcd5d965ba1bd8007aefaee`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8785151ca7040f897dcd5d965ba1bd8007aefaee)
  Два режима выделения (Папки+Темы / Вопросы), групповой доступ, двухрежимное удаление папки, сводная защита partial-batch и жёсткий отзыв.

## [2.5.1-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.5.0-beta...v2.5.1-beta)

### Fixes

- **fix**(content): фильтр контента по владельцу — «Владелец» вместо «Автор» + имя/email вместо UUID (2026-07-01) [`88a884a2330f7e485bf24fcb16077bc3581b018a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/88a884a2330f7e485bf24fcb16077bc3581b018a)
  Подпись бралась через несуществующие поля firstName/lastName (у модели одно поле name) и сваливалась в сырой UUID; теперь name || email || id. Заодно фильтр переименован «Автор» → «Владелец» (подпись, aria-label, чип активного условия).

- **fix**(static): кеш-политика SPA — no-cache на index.html, immutable на хеш-ассеты (2026-07-01) [`0abf79afff975c432a1ad4f9804dacfb332d0510`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0abf79afff975c432a1ad4f9804dacfb332d0510)
  Без явного no-cache Express отдавал index.html как public,max-age=0, и устаревший index.html в кеше браузера/прокси продолжал указывать на старые хеш-бандлы — «другой пользователь после деплоя видит старый UI». Теперь index.html всегда ревалидируется, хешированные ассеты кешируются как immutable.

## [2.5.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.4.0-beta...v2.5.0-beta)

### Features

- **feat**(prd-19): системные узлы границ раздела + фиксы навигации, инспектора и сборки отладчика (2026-07-01) [`833978506d0f30cde68c68941445c580b7ec587e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/833978506d0f30cde68c68941445c580b7ec587e)
  Структура / шаблон:
  \- Введение/Обзор/Итоги раздела как системные узлы шаблона (intro per-topic,
    review/section-results singletons); убраны legacy intro/summary — чинит пустую
    страницу «Далее» в начале раздела и дублирующиеся «Итоги раздела»
    (миграции 034/035, идемпотентны)
  \- предпросмотр страниц «Структуры» из реальных данных; страница-роутер:
    счётчик «Разделы X/Y» + «Перейти к результатам», карточки тем
  \- единый размер контролов вариантов; грип сопоставления по центру строки;
    подсветка зафиксированного ответа matching/ranking
  
  Навигация прохождения:
  \- кнопка «К обзору» в карточке вопроса при заходе из обзора
  \- пилюли: отвечённые впереди текущей позиции больше не гаснут как «не выдан»
    (фронтир по статусу answered/skipped, а не по currentIndex)
  
  Отладчик (PRD-18):
  \- протокол / «выдан» / итог считаются по статусу answered/skipped, а не по
    currentIndex — отвечённые на текущей позиции и после возврата не теряются;
    единица измерения «баллов» в футере протокола
  \- метки пар «Эталона» слева и жёстко по серединной горизонтали
  
  Сборка:
  \- dev собирает бандл shared/template из исходников и не кеширует — устаревший
    dist/scorm/assets/shared-runtime.js больше не теневит правки
  \- build копирует ассеты отладчика (shim.js / inspector-compute.js) в dist,
    reader резолвит их и в проде — чинит /debug/shim.js в контейнере

- **feat**(scales): хинт о нереализованном источнике «Другие шкалы» (2026-06-30) [`89b255111dcc8f0e45a8138ac81aa791c50ae8c6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/89b255111dcc8f0e45a8138ac81aa791c50ae8c6)
  Переключатель «Источник» с опцией «Другие шкалы» отключён, так как
  композитные шкалы (расчёт шкалы из других шкал) отложены. Добавлена
  подсказка, чтобы недоступность опции не выглядела как дефект.

- **feat**(scoring): пиктограммы типов вопросов в таблице «Оценка» (2026-06-30) [`722ab46bd85fdf8689cb07c684b4d3000a50a3fb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/722ab46bd85fdf8689cb07c684b4d3000a50a3fb)
  Иконки одиночный/множественный/сопоставление/ранжирование (как в дереве контента) с i18n-подписями.

- **feat**(prd-14): импорт книги — оценка из легаси-колонок «Вопросы» при отсутствии листа «Оценка»; импорт с владельцем (FR-28/FR-36) (2026-06-30) [`82282183a85a3a9ca5e25ab3f7125cf8030c3ce9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/82282183a85a3a9ca5e25ab3f7125cf8030c3ce9)
  Когда лист «Оценка» отсутствует, переопределения оценки выводятся из колонок «Балл»/«Цена ответа» листа «Вопросы». Через importWorkbook/importQuestionRows протянут actor — темы/вопросы импорта владеет импортирующий.

- **feat**(prd-2): пересборка конструктора показателей + поле «Id» темы (2026-06-30) [`23324bdd70b0b1043933f752bc0f449417c58ba5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/23324bdd70b0b1043933f752bc0f449417c58ba5)
  Карточка в 3 блока (Имя·Метка / Формула / Вывод). 4 шаблона (Порог/Категория/Взвеш.сумма/Вердикт) на одном условие-примитиве Элемент·Свойство·Оператор·Значение; группированный «Элемент» (Общий/Темы/Шкалы); тип выводится из шаблона; формула пишется сразу; читаемые ссылки на тему (topicById(код)/topicByName(имя), без UUID); справочник «Функции» + вставка по курсору. Метка опциональна, data-field-якоря для перехода к ошибке. Редактор темы получил поле «Id (код)».

- **feat**(ui-kit): Select — опциональные группы опций (optgroup-заголовки) (2026-06-30) [`9bf1a27b57af2495a6581ba4de8ae6869896044e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9bf1a27b57af2495a6581ba4de8ae6869896044e)
  Поле group у SelectOption: соседние опции с одним group рендерятся под нерасклик­иваемым заголовком. Обратносовместимо. Стиль .ou-select__optgroup в обеих копиях DS-CSS.

- **feat**(prd-2): рантайм/роуты — резолв тем по коду/имени + общий балл; код темы и rewrite topicByName при переименовании (2026-06-30) [`794363e38218b8d3e4906ab7d7233d8a62b53dd5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/794363e38218b8d3e4906ab7d7233d8a62b53dd5)
  Оба рантайма индексируют темы по uuid+код+имя (topicsByName) и считают общий балл. Валидатор формулы знает коды и имена тем. Роут темы валидирует/сохраняет код и при переименовании переписывает topicByName в живых показателях тестов, использующих тему (снапшоты не трогаются).

- **feat**(prd-2): DSL — общий балл (score), topicByName, адресация темы по коду + целостность при переименовании (2026-06-30) [`3417d297b9b0643d67c21d1a13898fe64607d50d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3417d297b9b0643d67c21d1a13898fe64607d50d)
  Аксессоры score и topicByName (TS + JS-порт, golden-парити). topicById принимает код-или-UUID. Чистый хелпер renameTopicByNameInFormula для rewrite ссылок при переименовании темы.

- **feat**(prd-2): колонка topics.code + опциональная метка показателя (схема + миграция 032) (2026-06-30) [`7bd9a64dfbbd53ef645fd2a8a2a1ef0c93479aae`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7bd9a64dfbbd53ef645fd2a8a2a1ef0c93479aae)
  Читаемый авторский id темы для topicById("код") (иначе UUID). Метка показателя становится необязательной — паритет со шкалами; пустая подставляется именем.

- **feat**(prd-19): статусы пропуска/возврата в отладчике PRD-18 (FR-24) (2026-06-29) [`06174a997b98338951f8d859693d926a8da1eb2a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/06174a997b98338951f8d859693d926a8da1eb2a)
  В «Протоколе» отладчика добавлена колонка «Статус» (отвечен/пропущен/не отвечен),
  читаемая из живой карты state.questionStatuses. Статус отличается от сырого
  наличия ответа: пропуск с черновиком — answered=true, но status='skipped', так
  что методолог видит навигацию отдельно от вердикта. Изменения единым потоком:
  общий inspector-compute.js (поле status в строке протокола), тип ProtocolRow +
  колонка в React-таблице, столбец «Статус» в CSV, CLI inspector.js (тег + CSV) —
  паритет, т.к. CLI и веб делят один compute. «Состояние» уже показывало
  questionStatuses в дереве state.

- **feat**(prd-19): веб-паритет cooldown — состояние охлаждения на стартовой странице (FR-19/20) (2026-06-29) [`38a627b45fea2489f79b05491165943750f8a2a8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/38a627b45fea2489f79b05491165943750f8a2a8)
  Веб-хост теперь рендерит cooldown через тот же общий шаблон start.html
  (buildStartState), без отдельной блок-стены — паритет со SCORM-гейтом
  renderCooldownStart. GET /api/learner/tests считает decideRetake (источник даты —
  собственные завершённые попытки сервера) и отдаёт retakeGate + priorResult из
  resultJson последней попытки (на неё же указывает «Мой результат»). Стартовый
  экран показывает дату ДД.ММ.ГГГГ + «через N дн.» (хелперы cooldown-format,
  зеркало SCORM fmtDateHuman/daysUntilIso) и сводку прошлой попытки; кнопка старта
  disabled. «Скачать отчёт» на вебе не предлагается (нет клиентского PDF). На
  403-гонке cooldown сворачивается на стартовую, блок-стена — только fallback.

- **feat**(prd-19): SCORM eligible-повтор — сводка прошлой попытки + «Скачать отчёт» на старте (FR-19) (2026-06-29) [`4075a201b438b6c64c7501396c25864ee3b3ad32`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4075a201b438b6c64c7501396c25864ee3b3ad32)
  «Повтор: можно»: buildScormStartContext отдаёт priorResult (балл/вердикт/попытка из getBestAttempt) и canDownloadReport ПОСЛЕ Initialize. На start.html карточка прошлой попытки + «Мой результат» (view-results) + «Скачать отчёт» (download-report -&gt; downloadPDF(true): лучший сохранённый attempt, а не пустая текущая попытка). Приёмка на плеере: prior-карточка, «Прошлая попытка 65% не пройдено попытка 1 из 3», PDF лучшей попытки скачивается. Контракт/layout/CSS из коммита 75941a6.

- **feat**(prd-19): cooldown-состояние на стартовой странице + снятие гейт-шелла (FR-19/20) (2026-06-29) [`75941a69cb049088e871d0961be93b74696e0584`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/75941a69cb049088e871d0961be93b74696e0584)
  FR-19: при eligible гейт ведёт сразу на обычный старт (renderStartShell удалён) — повтор неотличим от обычного теста. FR-20: при блокировке cooldown рендерится НА start.html (дата ДД.ММ.ГГГГ + «через N дн.» + disabled-кнопка), а не отдельной блок-стеной; prior-результат/«Скачать отчёт» гейтятся по state.* (на SCORM до Initialize недоступны — только дата). buildStartState получил cooldown/priorResult/canDownloadReport. Фикс латентного бага ensureTemplate: пустой templateLayouts={} считался загруженным — гейт рендерил встроенную стену вместо шаблона. Приёмка на standalone-плеере (:5050): eligible -&gt; обычный старт; cooldown -&gt; карточка на start.html, Initialize не вызван (NFR-01/02).

- **feat**(prd-19): поэтапное завершение D5/D6, итоги раздела, строгий режим, неотвеченный=неверный (E) (2026-06-29) [`36026bf4f0a5fd0841708526ea99bc0059c1c153`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/36026bf4f0a5fd0841708526ea99bc0059c1c153)
  Завершение по разделам (D5): перехват границы раздела -&gt; обзор -&gt; модал -&gt; заморозка -&gt; вычисляемые итоги раздела (section-results, новый шаблонный layout + builder) -&gt; следующий раздел/роутер; строгий режим показывает итоги между разделами без обзора. Редактор структуры (D6): виртуальная строка «Обзор», гейт по allowReturnToUnanswered. Блок E (FR-13/14): неотвеченный/черновик/пропуск = 0 баллов и вердикт «неверно» в едином движке (gradedAnswerFor), включая SCORM-интеракции (раньше интеракции брали сырой ответ и могли дать correct при балле 0). Веб-эндпоинт section-result грейдит раздел тем же aggregateStandardResult. Общие контракты/стили (context.ts, base.css), на которых далее строится cooldown-старт, добавлены здесь — файлы делят D5 и F построчно.

- **feat**(prd-19): навигация прохождения A–D + ревизия оформления/структуры (2026-06-29) [`adbc29d92f904d15d113c1f009b949d0ee43503e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/adbc29d92f904d15d113c1f009b949d0ee43503e)
  PRD-19 «Навигация прохождения» (пропуск/возврат): настройки (блок A), рантайм
  статусов вопроса/пропуска/возврата (B), кликабельные прогресс-пиллы вместо
  линейного бара (C), экран обзора неотвеченных + кнопка «Вернуться» + модал
  подтверждения завершения FR-09 (D — обзор/возврат/модал; поэтапное завершение
  и итоги раздела ещё впереди). Пиллы и обзор — framework-free через общий шаблон
  (.tb-pills/.tb-unanswered, builder question-progress/review-context), рендер на
  ОБОИХ хостах (web + SCORM), паритет PRD-12.
  
  Плюс ревизия оформления/структуры (Этапы 1-3): производный dirty-флаг контент-
  страниц, загрузчик изображений в свойствах страницы, проводка design_settings
  (цвета/шрифт на вебе + логотип на обоих хостах) через общий buildTemplateCssVars,
  cssVars из манифеста активного шаблона.
  
  Содержит также накопленные правки редактора (настройки/секции). tsc чист,
  2807 тестов зелёные.

- **feat**(debug-player): доводка встроенного плеера отладки PRD-18 (окно/инспектор/эталон + DS-примитивы) (2026-06-29) [`186f6abb1c5f698dc0b7f90c3706df4f1620a6e0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/186f6abb1c5f698dc0b7f90c3706df4f1620a6e0)

- **feat**(debug-player): инспектор по утверждённым эскизам PRD-18 (2026-06-28) [`a96df04f0a464139ba616d0b943401465c37be63`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a96df04f0a464139ba616d0b943401465c37be63)
  Bring the in-service debug player chrome + every inspector tab into full
  conformance with docs/wireframes/approved/prd18-debug-player.html:
  
  \- toolbar / status bar over the stage / stage ribbon + «Эталон» switch /
    edge collapse FAB / tab overflow scroll arrows.
  \- «Результаты» ou-kpi grid + threshold ProgressBar + per-section table.
  \- «Выдача»: per-tag composition table + «По типам» breakdown + «из банка N»
    \+ итог; no internal formId on the canvas.
  \- «Протокол»: method-aware «цена» note (веса опций · / тиры · c,x → ratio /
    точное совпадение) via the shared explainAnswer; before-start gating.
  \- «LMS»: structured Вызов · Ключ · Значение table + raw-log details.
  \- «Состояние»: DS ou-tree with state/suspend_data/cmi sub-tabs + filter.

- **feat**(scoring): единый движок агрегации результата + резолвер pass-rule (2026-06-28) [`4e3ec6f6fb871f58e3464a055ac61891f10fb371`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e3ec6f6fb871f58e3464a055ac61891f10fb371)
  THE single implementation of standard AND adaptive result aggregation
  (aggregateStandardResult / aggregateAdaptiveResult) plus the pass-rule
  resolver (resolveOverallRule / resolveTopicRule / checkPassRule), consumed
  by both the web grader and the SCORM runtime via the TBTemplate bundle.
  
  \- Fixes the inherit_overall / none topic rules that were treated as
    unsatisfiable count rules (100% read as «не пройден»).
  \- Adaptive aggregation resolves the achieved level POSITIONALLY (the
    reference SCORM convention) with a bounds guard instead of an unguarded
    throw, fixing the web value-match off-by-one on non-contiguous levelIndex.
  \- Adds explainAnswer (method + tallies) for the debug player «цена» note,
    with a plain-JS twin kept in golden parity.

- **feat**(web): окно встроенного плеера тестирования и отладки (PRD-18) (2026-06-27) [`8974a798a54eebfe2bd24af796c81ba7bebfdab1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8974a798a54eebfe2bd24af796c81ba7bebfdab1)
  Full-screen окно /author/tests/:id/debug (пункт меню «Тестовый прогон» → отдельное окно): статус-панель над стейджем, iframe прогона на применённом шаблоне, сворачивающийся DS-инспектор с 7 вкладками (Результаты/Протокол/Выдача/Шкалы/Показатели/Состояние/LMS на Tabs+tb-table), тумблер «Эталон» (подсветка правильных ответов ✓/№/буквы-пар на реальном рендере), экспорт CSV, сброс прогона; экран «недоступно» при отсутствии edit. Чистый адаптер buildSnapshot поверх window.TBInspector держит React-панели тонкими.

- **feat**(api): роутер встроенного отладочного плеера (PRD-18) (2026-06-27) [`5f870a8ae2878397f055a2184295ef2c5a7aac7a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5f870a8ae2878397f055a2184295ef2c5a7aac7a)
  POST/GET/DELETE /api/tests/:id/debug/session|play|... + shim.js/inspector-compute.js. Пакет собирается из ЖИВОГО состояния теста с telemetry:null и отдаётся VERBATIM same-origin под одноразовым токеном (in-memory стор: TTL 30 мин, LRU 50, владение по userId). Шим хостится в окне-плеере — SCO находит API_1484_11 по window.parent (R-7). Гейт = edit-scope теста (как у экспорта SCORM). Прогон изолирован: ни attempts, ни телеметрии (R-2).

- **feat**(scorm): вынести общий buildScormExportData для экспорта и отладки (PRD-18) (2026-06-27) [`028da02944638660e15024f010853d38cdb1aa43`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/028da02944638660e15024f010853d38cdb1aa43)
  Единый сборщик SCORM ExportData (live или snapshot-источник) — production-экспорт и встроенный отладочный плеер собирают ОДИН и тот же артефакт тем же пайплайном (NFR-18, R-4: «отлаживаешь то, что отгрузится»). Export-роут делегирует ему; ScormBuildError маппится на 404/422.

### Fixes

- **fix**(owner): владелец теста при импорте суперадмином + атомарное назначение (2026-07-01) [`ced30d682b653926c29667be9c61bdece6049a96`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ced30d682b653926c29667be9c61bdece6049a96)
  Симптом: тест, импортированный на инстансе, где автор залогинен как
  конфиг-суперадмин, показывал «Владелец» = «—» (у обычного администратора всё ок).
  
  Причина: владелец назначается корректно (owner_id = id импортёра), но
  конфиг-суперадмин провижинится с name=null (provisionSuperadmins), а колонка
  «Владелец» резолвит ИМЯ владельца → null → «—». То есть пробел отображения
  безымянного пользователя, а не сбой назначения.
  
  \- server/routes/tests.ts: колонка «Владелец» падает на email, если у пользователя
    нет имени (u.name || u.email) — тесты суперадмина больше не выглядят как «—».
  \- server/services/access.ts: будущие суперадмины провижинятся с name=email,
    чтобы не быть безымянными нигде.
  \- server/services/test-settings.ts + routes/tests.ts + routes/workbook.ts:
    create() пишет owner_id прямо в INSERT (атомарно, в одной транзакции с тестом);
    import-new и обычное создание передают id создателя/импортёра. Владелец больше
    не зависит от отдельного post-insert setTestOwner (оставлен подстраховкой),
    который мог не отработать на старом образе.

- **fix**(tests-list): одно открытое меню действий + стабильный жёлоб скроллбара (2026-06-30) [`2913a15874771b91ac9e8421dae63b3b082c4057`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2913a15874771b91ac9e8421dae63b3b082c4057)
  Меню теста и меню папки взаимоисключающи, повторный клик закрывает; scrollbar-gutter:stable, чтобы открытие нижнего меню не сдвигало сетку.

- **fix**(analytics): градуированная цена ответа в детализации и SCORM-роллапе (2026-06-28) [`2071c0cff96521c9de1f9d0d5b43bdf1df9b0e23`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2071c0cff96521c9de1f9d0d5b43bdf1df9b0e23)
  The per-attempt detail and the SCORM topic rollup re-derived points
  independently of the graded engine and dropped partial credit:
  
  \- analytics/attempts.ts: use the graded checkAnswer ratio (× points) instead
    of a binary === 1 collapse with all-or-nothing points, so per-row earned
    reconciles with the stored attempt total for weighted/tiered questions.
  \- analytics/scorm.ts: accrue the baked partial points unconditionally instead
    of gating earnedPoints behind isCorrect, so a tiered/weighted partial row
    (isCorrect=false, points&gt;0) no longer vanishes from the topic rollup.

- **fix**(editor): вписать модал вариантов в форму и шрифт вопросов по WF (PRD-17) (2026-06-26) [`22cd4136d989095664d829a4786ed2144c3bff1e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/22cd4136d989095664d829a4786ed2144c3bff1e)
  В Ф4 при портировании эскиза в ModalDialog + ui-kit TransferList потеряли
  модал-scoped оверрайды трансфера (в prd17-test-variants.html заданы под
  #wf-modal). Из-за дефолтов ui-kit панели расползались за пределы формы
  (.ou-transfer__pane min-height:420px + body display:block без внутреннего
  скролла → панель раздувалась, колонка стрелок-переноса уезжала ниже видимой
  зоны), а текст вопросов рендерился крупнее WF (body-m/nowrap вместо body-s).
  
  Перенесены оверрайды эскиза, scoped под .tb-variants-modal (чтобы не задеть
  другой TransferList): body → flex-колонка с min-height:0, .ou-transfer flex:1
  и minmax(0,1fr) грид, .ou-transfer__pane/__list min-height:0 + overflow:auto,
  .ou-transfer__row-name body-s с переносом. На ModalDialog добавлен
  className=tb-variants-modal как якорь стилей.

### Documentation

- **docs**(prd-2): актуализация — §3/§10/§12 и шапка под реализованную модель (2026-06-30) [`c3806eeb3f69b80a298a63c6a30c22b5f3cdda6c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c3806eeb3f69b80a298a63c6a30c22b5f3cdda6c)
  Окружающие разделы приведены к коду: тип выводится из шаблона (не поле), метка опциональна, тогл «показывать обучающемуся» скрыт, карточка из 3 блоков; score — не «планируемое», а реализован; шаблон «Категория по уровням шкалы»; адресация темы по коду/имени + rewrite topicByName при переименовании. v2.3, дата 2026-06-30. markdownlint чист.

- **docs**(prd-2): модель показателей — объекты/единицы, 4 шаблона, справочник функций, topicByName (2026-06-30) [`5a4403bcac22650f80be5ef788c177b169b3bfef`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5a4403bcac22650f80be5ef788c177b169b3bfef)
  PRD-2 §4: каталог объектов результата и единиц, шаблоны конструктора, справочник DSL, адресация темы по имени/коду. Эскиз prd2-prd5-scoring-tabs обновлён под новую модель (+ group-опции в prd7-shared.js).

- **docs**(prd-19): закрытие трека — ROADMAP до PRD-19 + router-визуал проверен (FR-05b) (2026-06-29) [`8eb0f61a93f432bfa4a13d5d2e516c40ac929770`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8eb0f61a93f432bfa4a13d5d2e516c40ac929770)
  ROADMAP актуализирован до PRD-19 (версия 1.11, блоки A-G, паритет обоих хостов,
  приёмка SCORM-плеер + Playwright). HANDOFF: router-визуал подтверждён на хабе из
  общего шаблона (пройденная тема «Пройдена»+заблокирована, «Завершить тест»
  гейтится). Зафиксированы гочи: router_by_topics нужно собирать через
  testSettingsService.create (иначе нет системной страницы kind:router = хаба);
  router использует отдельную модель routerFlow.js (window.RouterFlow), а не плоский
  state.pageSequence. Трек PRD-19 закрыт.

- **docs**(prd-19): HANDOFF — сессия 4 (веб-паритет cooldown + блок G), визуал в Playwright + гоча dev-БД (2026-06-29) [`4c2bebfd30f1addb131db4e294c8e1726a5b53a9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4c2bebfd30f1addb131db4e294c8e1726a5b53a9)
  Зафиксирована реализация остатка трека (Block F веб-паритет cooldown, Block G
  FR-24) и её визуальная проверка через Playwright (скрины cooldown-старта и колонки
  «Статус»). Отдельно записана гоча окружения: dev-БД test_builder (:55432) — старый
  снимок схемы, текущий код на ней не стартует; визуал делался на изолированной
  prd19qa с override DATABASE_URL.

- **docs**(prd-19): HANDOFF — SCORM eligible-повтор (FR-19) сделан, остаток F = веб-паритет (2026-06-29) [`e21211045bd1bd95d54d1781e63a5c236698d507`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e21211045bd1bd95d54d1781e63a5c236698d507)
  Отмечен коммит 4075a20 (повтор: можно на SCORM); в остатке блока F только веб-паритет cooldown + блок G.

- **docs**(prd-19): актуализация HANDOFF — блоки E и F закоммичены, остаток трека (2026-06-29) [`80cdfcd6cf0997a65b75aaa16321898f295620f1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/80cdfcd6cf0997a65b75aaa16321898f295620f1)
  Зафиксирован статус: весь PRD-19 в git (E/F закоммичены, приёмка F на плеере); обновлён список «Осталось» (веб-паритет cooldown, SCORM eligible-повтор, блок G).

- **docs**(prd-18): описать встроенный плеер отладки в README (2026-06-27) [`64dddf78ddf3cac726899bd0898b0c91dc2335a8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/64dddf78ddf3cac726899bd0898b0c91dc2335a8)
  Возможности → новый раздел «Тестирование и отладка» (PRD-18): тестовый прогон, инспектор реального времени (7 вкладок), «Эталон», изоляция прогонов. API Reference (эндпоинты /debug) и дерево структуры дополнены debug-player/buildScormExportData. Попутно снят устаревший стек: shadcn/Radix/Tailwind удалены → @universityrt/ui-kit; убраны несуществующие tailwind.config.ts и components.json.

- **docs**(prd-18): записать трек встроенного плеера отладки в ROADMAP (2026-06-27) [`dd22cf42dd48505269f816281f19543295b89bc3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dd22cf42dd48505269f816281f19543295b89bc3)
  Трек PRD-18 (Фазы 0-4 реализованы и запушены, приёмка Этап 14 закрыта) — в журнал актуализаций; источник → PRD-1...PRD-18, версия 1.10.

- **docs**(prd-18): спека плеера отладки, эскиз и приёмка BRD (Этап 14) (2026-06-27) [`abfa7f1e57d05a1ea02c51e6a98d4d54cfcd7fb8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/abfa7f1e57d05a1ea02c51e6a98d4d54cfcd7fb8)
  PRD-18 (§11 — Фазы 1–4 закрыты), согласованный эскиз prd18-debug-player.html и чеклист приёмки BR-13 в BRD отмечены как выполненные.

## [2.4.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.3.0-beta...v2.4.0-beta)

### Features

- **feat**(editor): гасить взаимоисключающие контролы выдачи вместо скрытия (PRD-17) (2026-06-26) [`d34a6a465d0261575228fa7de75a6628922dd1b4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d34a6a465d0261575228fa7de75a6628922dd1b4)
  UX-уточнение 1.2: карточка темы больше не «теряет» настройки визуально. Режим вариантов, «Все вопросы темы» и адаптив теперь делают конфликтующие контролы неактивными (disabled) с пояснением, а не прячут их. Добавлены пропсы disabled/disabledReason в QuotaEditor и VariantsEditor; конфиг сохраняется в draft, просто не применяется. Плюс R-9: ненавязчивое предупреждение о неравных размерах вариантов.

- **feat**(scorm): паритет вариантной выдачи в рантайме пакета (PRD-17) (2026-06-25) [`363a40c67d0a89e279e6d574202834d7c6fd5d5f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/363a40c67d0a89e279e6d574202834d7c6fd5d5f)
  Тот же опубликованный тест должен раздавать варианты и в SCORM. В
  рантайм-близнеце app.js добавлен plain-JS порт selectForm и ветка в
  generateVariant (режим вариантов вместо drawSection), а builder
  пробрасывает form_set_json в TEST_DATA (включается только при наличии,
  FR-02). У пакета нет cross-attempt-стора (NFR-17), поэтому ротация
  деградирует до случайного выбора. Golden-тест forms-port держит JS и TS
  движки в паритете; проверено рантайм-плеером — выдаётся ровно один
  вариант целиком.

- **feat**(editor): редактор вариантов теста на вкладке «Состав» (PRD-17) (2026-06-25) [`6cbf073d1e7067fc31fba3adb6259854fb007576`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6cbf073d1e7067fc31fba3adb6259854fb007576)
  Авторам нужен визуальный сбор форм по утверждённому эскизу. В строке
  темы добавлен свитч-режим «Варианты теста»: включение скрывает контролы
  выдачи из всего банка и показывает таблицу размеров форм + строку
  покрытия (FR-18). Модал — табы по вариантам с «+ Вариант» и TransferList
  «банк темы ↔ вариант», с иконкой типа вопроса и бейджами принадлежности
  к другим формам (FR-06). Маппинг formSet &lt;-&gt; formSetJson (load/save) и
  валидация (&gt;= 2 форм, каждая непустая; контроль выдачи не проверяется в
  режиме вариантов).

- **feat**(ui-kit): иконка элемента в TransferList (TransferItem.icon) (2026-06-25) [`a3a0c7611021cd3140a224ff191f106bb640302d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a3a0c7611021cd3140a224ff191f106bb640302d)
  renderRow заменяет строку целиком и ломает выбор/перенос, поэтому для
  произвольной пиктограммы в слоте аватара добавлен необязательный
  TransferItem.icon (фолбэк на буквенный аватар). Нужно редактору
  вариантов теста (PRD-17), чтобы показывать тип вопроса в списках.

- **feat**(workbook): импорт/экспорт колонки «Варианты» книги теста (PRD-17) (2026-06-25) [`96c270d179eb56a220b86c5280c383647224dec5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/96c270d179eb56a220b86c5280c383647224dec5)
  Варианты курируются массово, поэтому состав форм нужен в Excel рядом с
  вопросами, а не только в редакторе. На листе «Вопросы» добавлена колонка
  «Варианты» с НОМЕРАМИ форм (метки авто-нумеруются позиционно, D-10):
  импорт группирует вопросы по номеру в form_set_json per section
  (parseVariantNumbers/buildFormSet), экспорт пишет номер = позиция формы.
  SectionPayload.formSetJson персистит варианты при сохранении секции.
  Меньше двух форм в секции — ошибка импорта. Спецификация §6.4.

- **feat**(feasibility): защита целостности вариантов выдачи (PRD-17) (2026-06-25) [`a923de25839cb526d80edbe4a6b01dc9ba37c33a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a923de25839cb526d80edbe4a6b01dc9ba37c33a)
  Удаление вопроса/темы или публикация с неполным вариантом раньше
  проходили молча: формы (form_set_json) ссылаются на вопросы напрямую,
  и устаревшая форма доставляла бы меньше заявленного. Добавлен issue
  variant_incomplete в общий движок shared/draw/feasibility и проброс
  variantQuestionIds через гейты удаления/публикации (published -&gt; 409,
  draft -&gt; предупреждение) + клиентское зеркало. Снапшот замораживает
  form_set_json целиком (FR-11) — подтверждено тестом.

- **feat**(attempts): выдача темы по фиксированному варианту с ротацией (PRD-17) (2026-06-25) [`a2f1bd916ced01bfd72b15dcf1c9c63906b7e34a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a2f1bd916ced01bfd72b15dcf1c9c63906b7e34a)
  При старте попытки секция с form_set_json выдаётся через selectForm: историю
  ротации собираем из завершённых попыток пользователя (formId по теме), пин
  выпавшего formId в variant_json. Без набора — прежний drawSection. Историю
  грузим лишь когда тест использует варианты. storage пишет form_set_json при
  сохранении секций. Адаптивный режим не затронут (варианты — для стандартной
  выдачи).

- **feat**(draw): движок выбора варианта selectForm (PRD-17) (2026-06-25) [`35233cfa4cb7bcebe96fd5718c4b514dc7f285d7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/35233cfa4cb7bcebe96fd5718c4b514dc7f285d7)
  Чистый selectForm: выбирает один вариант, исключая выпадавшие в прошлых
  завершённых попытках (ротация по formId; при исчерпании — цикл), и выдаёт
  его состав целиком — в случайном порядке, с мягкой нехваткой (вопрос, ушедший
  из банка, отбрасывается без дублирования). shuffle инъектируется, поэтому и
  выбор, и порядок детерминированно тестируемы. В SCORM previousFormIds пуст
  (нет cross-attempt-стора) — выбор сводится к случайному.

- **feat**(schema): колонка form_set_json и схема вариантов выдачи (PRD-17) (2026-06-25) [`c54af2afde471704cf8a5f4ca8634ed285826f8b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c54af2afde471704cf8a5f4ca8634ed285826f8b)
  Добавляет на test_sections опциональный form_set_json: список ≥2 вариантов
  (стабильный id, label, явный questionIds). Наличие переводит секцию в режим
  вариантов — выпавший вариант выдаётся целиком; draw_count/draw_all/квоты
  тогда не применяются. id стабилен, чтобы история ротации не «съезжала» при
  правке набора. В testVariantSchema добавлен formId (пин выпавшего варианта).
  Колонка nullable — все существующие тесты сохраняют поведение.

### Documentation

- **docs**(prd-17): UX-уточнение 1.2 — disabled вместо скрытия контролов (2026-06-26) [`8f36894debb62262bfbb139a50eb0b10c6255bab`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8f36894debb62262bfbb139a50eb0b10c6255bab)
  Спецификация (v1.2) и согласованный эскиз приведены к реализации: взаимоисключающие контролы выдачи показываются неактивными, а не прячутся (FR-03, D-2, фаза 4a). Семантика выдачи не изменилась — только подача в UI.

- **docs**(prd-17): закрытие фаз 2-5 «варианты теста» (2026-06-25) [`26af93001fea96e2b5163cb775bad95c7483d0fd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/26af93001fea96e2b5163cb775bad95c7483d0fd)
  Статус PRD переведён в «реализован полностью»: фазы 2 (снапшоты +
  целостность), 3 (Excel), 4 (редактор) и 5 (SCORM-паритет) отмечены
  закрытыми с итогами проверки (golden-тест + рантайм-плеер).

- **docs**(prd-17): спецификация «варианты теста» (BR-12) + согласованный эскиз (2026-06-25) [`7fed13c70ae13ac4ef17ee3c7cb975e3f81a8d16`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7fed13c70ae13ac4ef17ee3c7cb975e3f81a8d16)
  Корпоративная сертификация (эталон РТК) требует фиксированных вариантов
  выдачи: автор курирует наборы вопросов на тему, при старте выпадает один
  вариант целиком, на повторе — другой. PRD-17 фиксирует модель и границу
  реализуемости (ротация — только веб-хост: WebTutor не даёт cross-attempt
  хранилища, NFR-17), BRD дополнен BR-12 + BR-12-09 (покрытие банка).
  
  Эскиз вкладки «Состав» построен на реальной разметке редактора; режим
  «Варианты теста» при включении скрывает контролы выдачи из всей темы.

## [2.3.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.2.0-beta...v2.3.0-beta)

### Features

- **feat**(content): колонка «Владелец», инлайн-превью вопроса и фикс вложенности (2026-06-17) [`b7c3f8463ab2deba4243ebc676983a311b0b3856`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b7c3f8463ab2deba4243ebc676983a311b0b3856)
  В дерево «Темы и вопросы» добавлены: колонка «Владелец» темы; инлайн-раскрытие
  строки вопроса в компактную read-only карточку (question-preview.tsx) с отметкой
  корректных вариантов значком чек-бокса. Исправлена вложенность на уровне вопросов:
  порядок элементов строки вопроса приведён к строке темы ([чекбокс][твист][тип]),
  из-за чего твист вопроса теперь на шаг правее родителя; вертикальная направляющая
  выровнена под шевроном темы. Страница и шапка таблицы стали sticky (внутренний
  скролл оболочки).

- **feat**(tests): паритет списка «Тесты» с разделом «Темы и вопросы» (2026-06-17) [`2b094c676940c43115d90a6196e65192768cc49b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2b094c676940c43115d90a6196e65192768cc49b)
  Тулбар приведён к языку контент-дерева: PageHeader-заголовок, DS Input-поиск,
  кнопка «Фильтры» с фасетами (Статус/Режим/Сценарий/Владелец/Область) и чипами
  активных условий (tests-filters.tsx), «Развернуть всё/Свернуть всё». Терминология
  сценария совпадает с колонкой («Линейный/По темам/Роутер»). Шапка таблицы и тулбар
  sticky; перенос теста в папку переведён с радио-списка на FolderTreeSelect. Линия
  над шапкой убрана, выровнены отступы и позиция поиска. Тесты обновлены под комбобокс.

- **feat**(content): слой действий раздела «Темы и вопросы» (Фаза 3) — меню, FAB, перенос, экспорт (2026-06-16) [`ca1984e418bdbb226c5c8bb49e74c763f7942038`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ca1984e418bdbb226c5c8bb49e74c763f7942038)
  Раздел доведён до утверждённого эскиза: на каждой строке (папка/тема/вопрос)
  всегда видимое ⋯-меню; чекбоксы вопросов + панель массовых действий; FAB
  speed-dial создания (папка/тема/вопрос); клик по вопросу открывает общий
  QuestionEditorDrawer; пикеры переноса вопроса в тему и темы в папку; удаление/
  перенос вопроса проходят через PRD-15 content guard.
  
  Выбор расположения папки/темы — компактный комбобокс, раскрывающийся деревом
  папок (общий FolderTreeSelect поверх DS Tree): в модалке создания папки и в
  поле «Папка» Drawer темы. В ⋯-меню папки добавлена команда «Добавить папку».
  
  Экспорт вопросов в Excel перенесён в раздел: команда «Экспорт в Excel» в
  ⋯-меню темы (одна тема) и кнопка в панели при выборе тем (чекбоксы тем).
  Терминология типов вопроса в фасет-фильтре — из i18n.

- **feat**(content): фасет-фильтр по эскизу + сложность интервалом/«Не задана» (2026-06-16) [`31be02566fb1245239933772ea7bb93473ee9179`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/31be02566fb1245239933772ea7bb93473ee9179)
  Фильтр приведён к утверждённому эскизу: floating popover, draft + «Применить»
  (батч-перефильтрация), мемоизация дерева и дебаунс поиска. Фасет «Сложность» =
  свитч «Не задана» (гейт) + слайдер-интервал 0–100 вместо фикс-таксономии;
  колонка дерева показывает число, для null — приглушённое «Не задано» (убраны
  выдуманные легко/средне/сложно). facetMatch и чипы — на интервал/unset; ключи
  i18n difficultyUnset/difficultyNotSet. PRD-16 FR-03/11/13.

- **feat**(questions): сложность вопроса опциональна (nullable) — PRD-16 FR-10 (2026-06-16) [`0c03a2ff4ecce2a0aa3e81df1f402acbaf545af7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0c03a2ff4ecce2a0aa3e81df1f402acbaf545af7)
  Сложность — число 0–100; отсутствие (NULL) означает «не задано». Снят
  NOT NULL с questions.difficulty (миграция 029), default 50 сохранён для
  обратной совместимости. Метки/уровни сложности — свойство теста
  (adaptive_levels), не банка.

- **feat**(content): фасет-фильтр «Темы и вопросы» (Фаза 2) (2026-06-16) [`b9af11fc8f77ae2936bffff5a0a1bdd1235db7c9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b9af11fc8f77ae2936bffff5a0a1bdd1235db7c9)
  Панель фасетов (Тип/Сложность/Теги/Медиа/Автор/Область) + чипы активных условий + «Очистить всё»; клиентская фильтрация дерева с авто-раскрытием к совпадениям и счётчиком «найдено/всего» на теме; Теги/Автор скрываются при отсутствии данных (тегов/createdBy/прав users.read). URL-синхронизация фильтров отложена. tsc зелёный; проверено на живом приложении (фильтр по типу → дерево сузилось, чипы, счётчики).

- **feat**(content): table-tree «Темы и вопросы» — чтение (Фаза 1) (2026-06-16) [`c3cc1acbaaba4a301ff5d4dc820065634fd7af11`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c3cc1acbaaba4a301ff5d4dc820065634fd7af11)
  Единое дерево Папка superset Тема superset Вопрос в визуальном языке списка тестов: колонки Название/Сложность/Вопросов, корневой «Все темы (N)», папки «(N тем)», тип вопроса — пиктограмма, сложность — тег в колонке, indent-line, рендер вопросов по раскрытию темы; тулбар поиск + Развернуть/Свернуть всё (поиск фильтрует дерево). Дерево собирается на клиенте из /api/folders + /api/topics + /api/questions. tsc + 2620 тестов зелёные; проверено на живом приложении (реальные данные, светлая/тёмная).

- **feat**(content): каркас раздела «Темы и вопросы» (Фаза 0) (2026-06-16) [`7f465dffd4ce43361530d556dd975dc96b3efec5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7f465dffd4ce43361530d556dd975dc96b3efec5)
  Маршрут /author/content (ProtectedRoute topics.manage), пункт сайдбара «Темы и вопросы» (FolderTree; старые Темы/Вопросы пока сохранены), страница-заготовка content.tsx, ключи i18n navigation.topicsAndQuestions + content. Дерево/фильтры/редакторы — следующие фазы. tsc + 2620 тестов зелёные.

- **feat**(learner): review-режим в общем шаблоне вопроса (PRD-12 паритет) (2026-06-15) [`ec190993e0cd99be702a8c3df4cae8369c3414e7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ec190993e0cd99be702a8c3df4cae8369c3414e7)
  TemplateQuestionScreen + optionsHtml/rankingHtml/matchingHtml получили пропы
  reviewMode + correctAnswer: в режиме просмотра ответов опции/ступени/строки
  соответствия получают классы correct-answer/incorrect-answer — ТЕ ЖЕ, что эмитит
  SCORM-рантайм (server/scorm .../single.js, multiple.js, feedback.js) и которые уже
  стилизованы в общем base.css. Логика correct/wrong зеркалит SCORM feedback.js
  (single: correctIndex; multiple: correctIndices; matching: pairs[{right,left}];
  ranking: correctOrder). Новый CSS не добавлялся, SCORM не затронут.

- **feat**(ui-kit): Grid label-control + утилиты ou-link-reset / ou-list--bulleted (2026-06-15) [`161837d1a9067c072eca79d51f40c07d1a0e0af7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/161837d1a9067c072eca79d51f40c07d1a0e0af7)
  Закрытие пробелов DS для этапа 2d:
  \- Grid: проп template="label-control" + класс ou-lgrid--label-control
    (grid-template-columns: 1fr var(--ou-lgrid-control,6rem); align-items:center)
    — двухколоночный «подпись + фикс-контрол» вместо arbitrary grid-cols-[1fr_6rem].
  \- .ou-link-reset (color:inherit; text-decoration:none) — сброс нативной &lt;a&gt;.
  \- .ou-list--bulleted (list-style:disc; padding-inline-start + li+li gap) —
    маркированный нативный список без сырого Tailwind, сохраняя a11y/роль списка.
  Все классы добавлены в ОБЕ копии university-rt.css.

- **feat**(ui-kit): проп surface на flow-примитивах Stack/Cluster (2026-06-15) [`e51edbb19aa8e1c79466c57507fd90f752f2d9a9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e51edbb19aa8e1c79466c57507fd90f752f2d9a9)
  Stack/Cluster теперь принимают surface ('muted'|'subtle'|'elevated'),
  переиспользуя самостоятельные ou-box--surface-* утилиты (как padding) —
  flex-панель несёт собственный фон без обёртки-Box. Закрывает пробел для
  полупрозрачных Tailwind-фонов (bg-*/30) через DS-токен поверхности.

- **feat**(ui-kit): Box.border принимает 'solid'|'dashed' (+ ou-box--border-dashed) (2026-06-15) [`62abbc12ee983efd3759041a90b8cd98f7bb126e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/62abbc12ee983efd3759041a90b8cd98f7bb126e)
  Проп border расширен до boolean|'solid'|'dashed'; добавлен CSS-класс
  .ou-box--border-dashed (1px dashed var(--ou-border-soft)) в обе копии
  university-rt.css. Закрывает пробел DS: пунктирные рамки-подсказки больше
  не требуют сырого Tailwind border border-dashed.

- **feat**(ui-kit): добавить token-пропсы padding в flow-примитивы Stack/Cluster (2026-06-14) [`f6224da6b796c9adbd96c4b1a7f7b65c6cde64ce`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f6224da6b796c9adbd96c4b1a7f7b65c6cde64ce)
  Stack/Cluster теперь принимают pad/padX/padY/padTop/padBottom/padStart/padEnd
  (как Box), переиспользуя существующие ou-box--pad* утилиты — flex-контейнер
  несёт собственный padding без обёртки-Box. Общий PaddingProps + хелпер
  padClasses для Box и Stack, без дублирования логики.

- **feat**(ui-kit): TagInput — чип-инпут тегов с автодополнением (домен-логика через пропы) (2026-06-14) [`991f7fa50110487a3f4ca4e1780b024e4da188cb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/991f7fa50110487a3f4ca4e1780b024e4da188cb)

- **feat**(ui-kit): примитивы для экранов входа — Center, IconBadge, Input revealToggle (eye), Stack minH, Box maxW/full (2026-06-14) [`2e6dec6510d2b96907d2e72aee8aa1057e4790e8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2e6dec6510d2b96907d2e72aee8aa1057e4790e8)

- **feat**(ui-kit): направленные паддинги Box (padX/padY/padStart/padEnd/padTop/padBottom) (2026-06-14) [`1509d49144e7cdc29d96615314fe3b8804941b60`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1509d49144e7cdc29d96615314fe3b8804941b60)

- **feat**(ui-kit): добавить layout/typography-примитивы (Stack/Cluster/Grid/Box/Text) + ScrollArea/Separator/Label/Collapsible и раскрываемые строки Table (2026-06-14) [`97247c115d572dc5aa5db84a41a2fd3e979be372`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/97247c115d572dc5aa5db84a41a2fd3e979be372)

### Fixes

- **fix**(content): выровнять структуру инлайн-превью на уровне вариантов ответа (2026-06-17) [`d006d355b716818d133054de0b4c65ae05793c18`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d006d355b716818d133054de0b4c65ae05793c18)
  Карточка превью индентируется на ширину ведущих контролов вопроса (слот чекбокса
  \+ твист + зазоры), из-за чего иконки вариантов выступали левее самого вопроса.
  Теперь иконки вариантов стоят под тип-иконкой вопроса, текст вариантов — под его
  текстом, а направляющая родительской темы продолжается сквозь карточку без разрыва.

- **fix**(ds): закрепить высоту .ou-shell (height:100vh) (2026-06-17) [`31161a2af224fb84da777f5981dc1a51d8788aad`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/31161a2af224fb84da777f5981dc1a51d8788aad)
  min-height позволял гриду оболочки расти по контенту: скроллился документ,
  а .ou-shell__main никогда не использовал свой overflow:auto. Фиксированная
  высота возвращает внутренний скролл main — это включает sticky-области
  (заголовок страницы + тулбар + шапка таблицы) на «Темы и вопросы» и «Тесты».
  Правка внесена в обе копии DS-CSS (vendor-источник и client/styles/vendor).

- **fix**(tests): нейтральный цвет пиктограммы папки (DS-стандарт) (2026-06-15) [`94ac16d02da947e6f57584f35e64e8a7875846cd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/94ac16d02da947e6f57584f35e64e8a7875846cd)
  Иконки папок в списке тестов были на warning-токенах (жёлтые); приведены к --ou-fg-muted, как у шеврона и прочих иконок дерева. Индикатор «Требует обновления» (.test-name-warn) не затронут.

- **fix**(learner): просмотр ответов (standard/adaptive) через общий шаблон; удалён React-фолбэк (2026-06-15) [`325663b3be724d9da82b59bc55b8c12310965863`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/325663b3be724d9da82b59bc55b8c12310965863)
  Дефект паритета (feedback_web_must_use_templates): при showCorrectAnswers веб
  рендерил отдельный React-компонент мимо PRD-12 шаблона. Исправление (этап 4-A):
  \- standard-review теперь идёт через TemplateQuestionScreen (2-шаговый футер
    «Принять» -&gt; «Далее»/«Завершить», reviewMode + correctAnswer + feedbackHtml + locked);
  \- adaptive-review тоже получил поэлементную подсветку (reviewMode/correctAnswer);
  \- удалён React-фолбэк (QuestionInput/MatchingQuestion/RankingQuestion + ставшие
    мёртвыми TimerDisplay/SectionTimerDisplay, ~640 строк сырого Tailwind) и
    неиспользуемые импорты иконок;
  \- редкий случай недоступного шаблона (fetch упал) -&gt; аккуратный DS-guard вместо
    второго React-рендера.
  Оба рендера (web + SCORM) рисуют просмотр из общего шаблона. tsc/build/2620 зелёные;
  визуально подтверждён ranking-review через шаблон (footer Принять/Далее, feedback-слот,
  locked, тёмная тема).

- **fix**(ui-kit): развести layout Grid и DataGrid в .ou-lgrid (фон/граница DataGrid протекали в просветы сетки) (2026-06-14) [`e98a425dd95fbf47eee76711358c8300c9786630`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e98a425dd95fbf47eee76711358c8300c9786630)

### Documentation

- **docs**(content-axis): смена типа вопроса сохраняет варианты + дефолт «Не задано» (2026-06-16) [`accacb062e7db0eff2b9710695a2d61168eb5bc0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/accacb062e7db0eff2b9710695a2d61168eb5bc0)
  Решение 2026-06-16: смена типа вопроса переносит введённые варианты между
  типами без подтверждения (пересобирается только структура ответа) — заменяет
  прежнее подтверждение с потерей данных. Обновлены BRC-CA-32, FR-32 и приёмка;
  FR-12 фиксирует дефолт «Не задано» для нового вопроса. Из утверждённого эскиза
  удалён obsolete state подтверждения смены типа (s-q-typechange).

- **docs**(content-axis): BRD + PRD-16 + план модели вопроса; эскиз дополнен (2026-06-16) [`3432e8ab74408ace81e5703e7407f5b255b001f6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3432e8ab74408ace81e5703e7407f5b255b001f6)
  BRD оси контента (BRC-CA-01..44) и PRD-16 (FR-01..50) фиксируют принятый
  объём: сложность число/nullable, медиа только-загрузка, валидация
  конструкторов, порядок и «Случайный порядок» вариантов; переопределение
  выдачи на уровне теста (FR-90/BRC-CA-44) — отложено до подтверждения
  гипотезы. План дополнен блоками C1-C6 (C5 отложен), ROADMAP — строка PRD-16.
  Эскиз content-bank-explorer обновлён под модель сложности/медиа/edge cases
  и терминологию i18n.

- **docs**(ux): план реализации раздела «Темы и вопросы» (2026-06-16) [`54382d1be80d37921d0902d896eeb99b9e157f22`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/54382d1be80d37921d0902d896eeb99b9e157f22)
  Фазовый план объединения страниц Темы+Вопросы в единый table-tree раздел (Папка superset Тема superset Вопрос) по визуальному языку списка тестов. 6 фаз, карта переиспользования, статус серверных API. На согласовании выбран старт с Фазы 0.

- **docs**(ux): аудит авторских разделов + эскиз «Темы и вопросы» (2026-06-15) [`ef22c7b2690e1bba4101ebfa8acf5ab96360b530`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ef22c7b2690e1bba4101ebfa8acf5ab96360b530)
  UX/UI-аудит разделов Темы/Вопросы/Тесты (docs/AUDIT_UX_AUTHOR_CONTENT.md, UX-01..13) и утверждённый эскиз единого раздела «Темы и вопросы»: table-tree по визуальному языку списка «Тесты»; единое дерево Папка superset Тема superset Вопрос, ортогональное оси тестов (тест ссылается на тему и выбирает из пула).

- **docs**: актуализировать README/ROADMAP/.env.example под текущий код (2.2.0-beta) (2026-06-13) [`b718bdce750fe92dda111118c12c0e02aecd26b5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b718bdce750fe92dda111118c12c0e02aecd26b5)
  README приведён в соответствие с PRD-13/14/15 и схемой БД 2.2.0-beta: 5-ролевая
  модель доступа вместо двух ролей, актуальные таблицы (user_roles, *_access_grants,
  test_snapshots, test_question_scoring), оценка как свойство теста (T-40), exceljs
  вместо XLSX, дерево проекта, демо-юзеры, маршруты API и формат Excel-импорта.
  ROADMAP §0 — дата статуса. .env.example — добавлен обязательный DATABASE_URL.

## [2.2.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.1.0-beta...v2.2.0-beta)

### Features

- **feat**(editor): «Вклады вопросов» — группировка по секциям, folding и гейтинг вкладки (2026-06-13) [`245ba572cd8f098d93c8087b834c0cae1b6bfd76`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/245ba572cd8f098d93c8087b834c0cae1b6bfd76)
  Плоский список всех вопросов организован так же, как «Оценка»: карточки
  сгруппированы по секциям (сквозная нумерация), каждая секция сворачивается +
  «Свернуть/Развернуть все» (общий section-fold; в ContributionQuestion добавлен
  topicId для группировки). Пункт рейла «Вклады вопросов» теперь disabled, пока в
  тесте нет ни одной шкалы — матрица без шкал бессмысленна (при удалении последней
  шкалы — возврат на «Список шкал»). Маркер покрытия согласован: внутрь попадаешь
  только со шкалами, поэтому жёлтая точка «не привязан» всегда действенна и
  дублируется бейджем «X не привязано» в заголовке секции. Пустое состояние
  «Список шкал» выровнено по DS (иконка info).

- **feat**(editor): сворачиваемые секции на вкладке «Оценка» + общий примитив folding (2026-06-13) [`e87b613be710d7b2137df7b450d0f778d5388ece`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e87b613be710d7b2137df7b450d0f778d5388ece)
  На длинных тестах таблица оценки по секциям не помещалась на экран. Добавлено
  сворачивание секций (шеврон в заголовке) и глобальные «Свернуть все / Развернуть
  все». Логика вынесена в общий примитив section-fold (useSectionFold +
  FoldAllButtons), чтобы переиспользовать на других вкладках со списками по
  секциям. CSS обобщён в классы tb-fold-* (+ disabled-состояние пункта рейла,
  которого не было в DS). Состояние эфемерное, по умолчанию всё развёрнуто.

- **feat**(topics): «Все» — первая вкладка фильтра и режим по умолчанию для всех ролей (2026-06-13) [`bdf55aae80059da04fe43fef783954a0f39a47c6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bdf55aae80059da04fe43fef783954a0f39a47c6)
  Вкладка «Все» была admin-only и стояла последней, а дефолт был «Мои».
  Сделана первой и режимом по умолчанию для любой роли (снят isAdmin-гейт):
  при заходе на «Темы» пользователь сразу видит весь доступный ему набор.
  Логика фильтра не менялась — сервер по-прежнему отдаёт только видимые темы,
  для не-админа «Все» = объединение «Мои + Доступные + Общие».

- **feat**(deploy): пре-push гейт — падать, если PRD-15 миграции не прогнались (2026-06-13) [`c4a87ed464314bd7203de93541b0f0c524ee65c1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c4a87ed464314bd7203de93541b0f0c524ee65c1)
  drizzle-kit push нельзя давать дойти до промпта create/rename
  test_question_scoring: под --force он дропнул бы topic_courses/events и
  questions.points/scoring_json без backfill. Новый script/verify-prd15-pre-push.sql
  (DO-блок с RAISE) проверяет, что миграции 023/024/026/027/028 уже применились;
  deploy.sh/deploy-test.sh гоняют его ПОСЛЕ цепочки и ДО push — при незавершённой
  миграции деплой падает с понятной ошибкой и push не запускается (тихий дроп
  превращается в громкий безопасный отказ). Dockerfile копирует гейт в образ.

- **feat**(questions): убрать оценку из банка вопросов — API и лист «Вопросы» без «Балл»/«Цена ответа» (PRD-15 T-40) (2026-06-13) [`d6ba4a0e37d7af1c5138b99b7ff073aa5ad40c87`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d6ba4a0e37d7af1c5138b99b7ff073aa5ad40c87)
  POST/PUT /api/questions больше не принимают points/scoringJson (сняты из тела,
  dirty-check и валидации). Standalone-лист «Вопросы» (export/import/шаблон/
  «Справка») без колонок «Балл»/«Цена ответа» — оценка задаётся test-scoped листом
  «Оценка» (модель блока D). Импорт старой колонки молча игнорируется (обратная
  совместимость старых файлов).

- **feat**(db): удалить questions.points/scoring_json — оценка свойство теста (PRD-15 T-40, миграция 028) (2026-06-13) [`64c7958a1fd6407be8cd42f2edf29a3ea2558d22`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/64c7958a1fd6407be8cd42f2edf29a3ea2558d22)
  Миграция 028: идемпотентный re-backfill (повтор 027 — ловит пары тест/вопрос,
  созданные после 027) + дроп колонок points/scoring_json из questions. Из схемы
  убраны оба поля таблицы questions и scoringJson из insertQuestionSchema. Чистый
  резолвер shared/scoring/effective-scoring сохраняет необязательный seam legacy
  (никто больше не подаёт). Прогонять 028 ДО drizzle-kit push (backfill раньше дропа).

- **feat**(editor): вкладка «Оценка» (умолчания + переопределения по вопросам) (PRD-15 блок D) (2026-06-13) [`c17b7106b28fab42d0f230166a5e15b4bd941279`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c17b7106b28fab42d0f230166a5e15b4bd941279)
  Вкладка «Оценка» редактора теста: per-test умолчания цены и модалка переопределений по вопросам (scoring-section + question-scoring-modal + scoring-api). Конструктор градуированной оценки PRD-10 перенесён из карточки вопроса в редактор теста (pages/author/scoring-builder.tsx → features/tests/editor/sections/), questions.tsx очищен.

- **feat**(import): листы «Оценка»/«Структура»/«Квоты» в книге теста (PRD-15 блок D) (2026-06-13) [`3f2c18c28caa20e0633028c719c65bbce00986a1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3f2c18c28caa20e0633028c719c65bbce00986a1)
  Книга теста расширена листами «Оценка» (умолчания цены + переопределения по вопросам), «Структура» (разделы/выборка/порог/режим) и «Квоты» (выдача по тегам). Инспекция файла учитывает эти test-scoped листы (требуется целевой тест); import-new создаёт тест и применяет структуру/квоты; шаблон содержит все листы.

- **feat**(scoring): API умолчаний цены и переопределений по вопросам, аналитика по эффективной оценке (PRD-15 блок D) (2026-06-13) [`40e85f3336a41c152fe012551eb61275f1b2c692`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/40e85f3336a41c152fe012551eb61275f1b2c692)
  Эндпоинты теста для умолчаний цены и переопределений оценки на вопрос; прохождения и аналитика (combined/export/test-details/attempts) считают баллы по эффективной оценке через резолвер, согласованно с вебом и SCORM.

- **feat**(scoring): применять эффективную оценку в вебе, SCORM и снапшотах (PRD-15 блок D) (2026-06-13) [`9a4f4079abec1cd160c3c11d33ca8c4804cac242`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9a4f4079abec1cd160c3c11d33ca8c4804cac242)
  Все потребители оценки идут через резолвер: вебовое выставление баллов (check-answer), запекание TEST_DATA для SCORM (test-json), публикационные снапшоты (test-snapshot) и проверка выполнимости выдачи. Снапшот фиксирует эффективную оценку на момент публикации.

- **feat**(scoring): резолвер эффективной оценки + DAL переопределений (PRD-15 блок D) (2026-06-13) [`b1d4ce6c65b739c265a9292f6fb0553c997ec1ef`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b1d4ce6c65b739c265a9292f6fb0553c997ec1ef)
  Единый источник истины по эффективным баллам/градуированной оценке/сложности вопроса в тесте: умолчание теста/секции → поле вопроса → переопределение test_question_scoring (с пином content-hash). server/services/effective-scoring.ts + методы storage для чтения/записи переопределений.

- **feat**(db): таблица test_question_scoring + умолчания цены (PRD-15 блок D, миграции 026-027) (2026-06-13) [`12d78c2f71920d74cfc25992a87c1253ad5685bf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/12d78c2f71920d74cfc25992a87c1253ad5685bf)
  Оценка становится свойством теста: новая таблица test_question_scoring хранит переопределения баллов/градуированной оценки/сложности на пару (тест, вопрос) с pinned_content_hash для детекта устаревания; tests.default_question_points и test_sections.default_points задают умолчания цены. Миграция 027 — бэкфилл. Колонки nullable/defaulted: отсутствие = legacy-поведение.

- **feat**(topics): единый Drawer темы — Свойства (с богатым фидбэком) + Доступ (PRD-15 T-32, D1) (2026-06-12) [`cc3f78ebc313324db9549dfd0c521e47f44aadb9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cc3f78ebc313324db9549dfd0c521e47f44aadb9)
  Редактирование темы и управление доступом объединены в один DS-Drawer на
  вкладках «Свойства» и «Доступ» (по утверждённому эскизу prd15-topic-access).
  Вкладка «Свойства»: имя с live-проверкой одноимённости (FR-27), папка, описание
  и богатая обратная связь через общий с тестами редактор (FeedbackEditorModal /
  FeedbackPreview) поверх topics.feedback_json (TD-02). Вкладка «Доступ»: владелец,
  видимость, гранты use/manage с мягким/жёстким отзывом. Единое «Сохранить» = PUT
  свойств + PATCH видимости/владельца; гранты/отзыв — немедленные действия.
  
  Удалён старый shadcn-диалог редактирования, отдельная панель доступа
  (topic-access-panel) и диалоги добавления курсов/мероприятий с карточки —
  рекомендованные курсы/мероприятия теперь часть обратной связи; карточка и строка
  показывают только их счётчики из feedback_json.
  
  \- features/topics/topic-drawer.tsx — новый единый компонент
  \- pages/author/topics.tsx — перепроводка на TopicDrawer, чистка мёртвого кода
  \- удалён features/topics/access/topic-access-panel.tsx (логика поглощена)
  \- тесты: topic-drawer (рендёр/сохранение/гранты/отзыв 409) + topics smoke

- **feat**(topics): богатое поле обратной связи topics.feedback_json (TD-02 r.2, аддитивно) (2026-06-12) [`a34bafee00f18776727f9f3e7a040a54e17e2044`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a34bafee00f18776727f9f3e7a040a54e17e2044)
  Тема получила единое богатое поле feedback_json (зеркало feedbackContentSchema:
  формат/текст/документы/курсы/мероприятия), как у тестов и секций. Это
  разблокирует богатый редактор обратной связи темы в едином Drawer (T-32).
  
  Изменение строго аддитивное (вариант A): доставка результатов (веб, SCORM,
  снапшоты) и старый UI карточки тем по-прежнему читают плоский feedback и
  таблицы topic_courses/topic_events. Полное переключение чтений и отказ от
  таблиц вынесены в TD-02 r.3 (вместе с заменой старого экрана на Drawer).
  
  \- schema: topics.feedback_json (jsonb, nullable)
  \- migration 023: ADD COLUMN + идемпотентный бэкфилл из feedback + topic_courses + topic_events
  \- storage: create/updateTopic персистят feedback_json
  \- routes/topics.ts: приём и валидация feedbackJson на POST/PUT (400 invalid_feedback_json),
    пропуск ключа при отсутствии (не перетирает сохранённое)
  \- tests: round-trip create/update, 400 на мусоре, сохранение при отсутствии поля
  \- plan: §7 TD-02 r.1/r.2 отмечены сделанными, r.3 — остаток

- **feat**(results): показывать рекомендуемые мероприятия темы ученику на веб-экране результатов (TD-02) (2026-06-12) [`f335f6e03d16ee3ef1a5cdd79c9c1444020cf7fe`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f335f6e03d16ee3ef1a5cdd79c9c1444020cf7fe)
  Веб-шаблон результатов (TemplateResultPage) раньше не пробрасывал рекомендации:
  курсы и мероприятия проваленных тем рендерил только SCORM-рантайм. Теперь
  buildResultContext агрегирует и дедуплицирует recommendedCourses и
  recommendedEvents из проваленных тем и передаёт их в общий билдер, как это
  делает SCORM. Layout results.html уже умеет рендерить оба блока.
  
  \- schema: topicResultSchema.recommendedEvents (url опционально, default [])
  \- attempts: загрузка getTopicEvents для секции и проброс в topicResults
  \- result-context: агрегация+дедуп курсов/мероприятий по проваленным темам

- **feat**(web): индикатор недоступной темы в составе теста (PRD-15 T-32, E-11) (2026-06-12) [`cc76914f2ce82ccf9c3528042e0c422f92b1b4db`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cc76914f2ce82ccf9c3528042e0c422f92b1b4db)
  Секция, чья тема отсутствует в видимом автору /api/topics (грант отозван/тема приватизирована), помечается тегом «Тема недоступна». Тест продолжает работать и сохраняться; недоступные темы не появляются в пикере (список scoped сервером). +тест.

- **feat**(web): админ-отчёт о дублирующихся темах (PRD-15 T-32, BRC-12) (2026-06-12) [`17e58e8c3945eb7a909b850ba2253b171866f01b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/17e58e8c3945eb7a909b850ba2253b171866f01b)
  Кнопка «Отчёт о дублях» (только админ) открывает диалог с группами одноимённых тем (GET /api/topics/duplicates-report): таблица «Название/Владелец/Видимость», имя владельца резолвится через /api/users.

- **feat**(web): фильтр области тем, скрытие пустых папок, проверка одноимённости (PRD-15 T-32) (2026-06-12) [`22d029e9715cc5713c5a5cdcab90d67847f054c7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/22d029e9715cc5713c5a5cdcab90d67847f054c7)
  Фильтр Мои/Доступные/Общие (+Все для админа) поверх отфильтрованного сервером списка; скрытие папок без видимых в фильтре тем + админ-тоггл «Показывать пустые папки». Живой name-check при создании/переименовании: жёсткая одноимённость у владельца блокирует Save (409), чужая видимая — предупреждение.

- **feat**(web): кнопка «Доступ к теме» в списке тем, гейт владелец/админ (PRD-15 T-32) (2026-06-12) [`596c88607595effb7d3a2b3a4745b0b87a054be0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/596c88607595effb7d3a2b3a4745b0b87a054be0)
  Действие-щит на карточке и в строке открывает панель доступа; видно при topics.access.grant и (владелец темы или администратор).

- **feat**(web): панель доступа к теме — владелец, видимость, гранты, отзыв (PRD-15 T-32) (2026-06-12) [`063f41016782445eb7f72790a118d16d0d0f6595`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/063f41016782445eb7f72790a118d16d0d0f6595)
  DS Drawer по образцу test-access-panel: смена владельца (админ), тумблер видимости приватная/общая, гранты только пользователям (Просмотр/Управление), двухрежимный отзыв (мягкий/жёсткий с 409-зависимостями), возврат отозванного. Владелец+видимость батчатся на «Сохранить»; гранты/отзыв — немедленные действия.

- **feat**(web): обратная связь — раздел «Мероприятия», «Курсы», группированное превью (TD-02) (2026-06-12) [`b3c4df6f3d8ecba2917d6725dfdb98d05a315726`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b3c4df6f3d8ecba2917d6725dfdb98d05a315726)
  Редактор: раздел «Мероприятия» (название + опц. ссылка), переименование «Ссылки»→«Курсы», флаг hideEvents. Новый общий feedback-preview: списки Документы/Курсы/Мероприятия с реальными ссылками и pen-кнопкой редактирования вместо счётчиков. events проходят насквозь типы/мапперы/callers (персист на уровне теста и темы).

- **feat**(shared): рекомендуемые мероприятия в обратной связи (TD-02) (2026-06-12) [`01c8c3e5f0324ababd960973f84c3ffd847c4ccf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/01c8c3e5f0324ababd960973f84c3ffd847c4ccf)
  feedbackContentSchema: новый массив events ({title, url?}) с опциональным URL — курсы остаются ссылками, мероприятия добавляются как отдельный тип. Аддитивно (default []).

- **feat**(server): API владения темами — гранты/видимость/владелец, фильтрация, отзыв, одноимённость (PRD-15 T-27..T-31) (2026-06-12) [`6f72bf941c37fb60262071e858fb2f3cd21c80e0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6f72bf941c37fb60262071e858fb2f3cd21c80e0)
  topics: эндпоинты грантов (GET/POST/DELETE /access), смена владельца/видимости, двухрежимный отзыв (мягкий по умолчанию, жёсткий 409 для админа), /name-check и /duplicates-report, фильтрация списка по видимости. questions: чтение/CRUD по управлению темой, экспорт в видимой области. tests: секции/уровни только из видимых тем с производным чтением при пересохранении.

- **feat**(server): сервисы владения темами — выполнимость отзыва, одноимённость, импорт (PRD-15 T-29..T-31) (2026-06-12) [`d85859b4c19ca60c1daecbccb9ede5277ab91d0d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d85859b4c19ca60c1daecbccb9ede5277ab91d0d)
  topic-access: dependentTestsForGrant (жёсткий отзыв, FR-26), sameOwnerNameClash/visibleSameNameTopics/duplicateNameGroups (FR-27). storage: nameNormalized в create/updateTopic. questions-import: сопоставление тем в видимой импортёру области + владелец-импортёр, гейт обновления по управлению темой (FR-28).

- **feat**(shared): нормализация имени темы для контроля одноимённости (PRD-15 T-30) (2026-06-12) [`c1b183241d5917339e5e20c186e3867418f382b9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c1b183241d5917339e5e20c186e3867418f382b9)
  shared/topics/naming.normalizeTopicName (регистр, пробелы, ё-&gt;е) + колонка topics.nameNormalized с частичным уникальным индексом по владельцу.

- **feat**(db): нормализованное имя темы и частичный индекс по владельцу (PRD-15 T-30) (2026-06-12) [`080ba6f60905ccbb0c7bfe78850d0b4336cb3496`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/080ba6f60905ccbb0c7bfe78850d0b4336cb3496)
  topics.name_normalized + частичный уникальный индекс (owner_id, name_normalized) WHERE owner_id IS NOT NULL: жёсткая уникальность имён только в рамках владельца, legacy-темы (owner NULL) не конфликтуют. Бэкфилл зеркалит JS-нормализацию.

- **feat**(web): «Общий доступ» виден владельцу теста и админу (PRD-15 BRC-27) (2026-06-11) [`de0f8da46a8ef1266f8307f2a4565d91bf5ffd5d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/de0f8da46a8ef1266f8307f2a4565d91bf5ffd5d)
  Пункт меню «Общий доступ» гейтится по владению: автор видит его только для своих
  тестов (canGrantAccessFor), администратор — для всех. Раньше было capability-only
  (admin), теперь capability у автора есть, объектную проверку добавляет клиент.

- **feat**(server): объектный доступ к темам и владельческие гранты тестов (PRD-15 блок C) (2026-06-11) [`9f0f7f94dbe48115d59c67c0f4c2dbeda67e9294`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9f0f7f94dbe48115d59c67c0f4c2dbeda67e9294)
  \- services/topic-access.ts — visibleTopic / canManageTopicContent / canDeleteTopic
    / canGrantTopicAccess (владелец|admin) / canChangeTopicOwner (admin) /
    visibleTopicScope (shared+owned+granted); вопросы наследуют видимость темы;
  \- storage.ts — владелец/видимость темы, DAL грантов (upsert/state/remove,
    активные по user+группам), getSharedTopicIds, createTopic с владельцем-создателем
    и приватностью по умолчанию;
  \- test-access.ts/routes — canGrantAccess = владелец|admin (BRC-27); три
    access-эндпоинта теста получили объектную проверку владения.

- **feat**(access): права тем и делегирование грантов владельцу (PRD-15 блок C, BRC-27) (2026-06-11) [`a5d502246b31062b16160c40e0f6de67f6157f57`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a5d502246b31062b16160c40e0f6de67f6157f57)
  Каталог +4 права (topics.read, questions.read, topics.access.grant,
  topics.owner.change). AUTHOR получает чтение/гранты тем и tests.access.grant
  (BRC-27 — владелец грантит доступ к своим тестам); смена владельца темы —
  только админ. Все новые права scope-aware (объектная проверка по
  владению/видимости в сервисах).

- **feat**(shared): схема владения темами и грантов (PRD-15 блок C) (2026-06-11) [`e001d9e921e4bf15b4a272f45dba62712fbc3648`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e001d9e921e4bf15b4a272f45dba62712fbc3648)
  topics.ownerId + visibility (private|shared) и topicAccessGrants — доступ к теме
  для не-владельца (user/group, use/manage, состояние мягкого отзыва). Вопросы
  наследуют видимость своей темы; отдельных прав на вопрос нет.

- **feat**(db): владение темами и гранты доступа к темам (PRD-15 блок C) (2026-06-11) [`fd6e88258e330123eab1ecdeeca884da9a1bf480`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fd6e88258e330123eab1ecdeeca884da9a1bf480)
  Миграция 021: topics.owner_id (NULL=legacy), topics.visibility (private|shared;
  новые темы приватны, legacy backfill в shared) и topic_access_grants (получатель
  user|group, уровень use|manage, soft-revoke state). Аддитивно: owner NULL +
  visibility shared у существующих тем = поведение не меняется в день один.

- **feat**(web): состояния публикации и экстренная переопубликация (PRD-15 T-23) (2026-06-11) [`20b74204e6439f586baffd248c7fd69ccaaf0f3b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/20b74204e6439f586baffd248c7fd69ccaaf0f3b)
  \- список тестов: бейдж «Есть изменения» рядом со статусом при дрейфе
    (publicationState), пункты меню «Опубликовать изменения» (новый снапшот) и
    «Экстренная переопубликация» с диалогом-подтверждением (аннулирование без
    списания лимита); 409 publish_infeasible открывает диалог влияния;
  \- ученик: на 404 от аннулированной попытки — модал «Тест обновлён, начните
    заново» (попытка не засчитана).

- **feat**(server): доставка из снапшота, переопубликация и аналитика по версиям (PRD-15) (2026-06-11) [`0a658ad1c570280726c93942d1d558558413e9bb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0a658ad1c570280726c93942d1d558558413e9bb)
  \- routes/tests.ts — публикация/переопубликация создают снапшот (FR-10);
    publicationState в списке и деталях (FR-12); POST republish-force с
    аннулированием идущих попыток без списания лимита (FR-14); экспорт SCORM из
    актуального снапшота (FR-16);
  \- routes/attempts.ts — старт/ответ/финиш/resume/таймер во всех путях читают и
    оценивают из пиннингованного снапшота; snapshotId NULL = живой путь (FR-11/13);
  \- analytics/attempts.ts — snapshotVersion по попыткам + сводка распределения по
    версиям публикации (FR-15).

- **feat**(server): сервис снапшотов, фасад источника и DAL (PRD-15 блок B) (2026-06-11) [`1a5b10441e9e6bd40f0d2007c2f5406bfc545040`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1a5b10441e9e6bd40f0d2007c2f5406bfc545040)
  \- services/test-snapshot.ts — сборщик buildSnapshotContent (замораживает сырые
    storage-строки), фасады snapshotDataSource/liveDataSource/dataSourceForAttempt
    и exportSourceForTest (один интерфейс TestDataSource для рантайма), версионный
    createTestSnapshot с pruneSnapshots (удержание, FR-17), getPublicationState
    (дрейф пулов по contentHash, FR-12);
  \- storage.ts — CRUD снапшотов, getReferencedSnapshotIds, annulInProgressAttempts
    (FR-14), snapshotId в createAttempt;
  \- scoring-config.ts — loadScoringConfig принимает источник (снапшот при доставке,
    storage по умолчанию).

- **feat**(shared): схема снапшотов теста и пин попытки (PRD-15 блок B) (2026-06-11) [`e2d87328d9559b56ca9b320127d94070b5a976d0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e2d87328d9559b56ca9b320127d94070b5a976d0)
  testSnapshots — замороженный самодостаточный deliverable теста (content_json),
  создаётся при публикации; attempts.snapshotId — снапшот, из которого попытка
  доставляется и оценивается (FR-10/FR-13).

- **feat**(db): таблица снапшотов публикации и пин attempts.snapshot_id (PRD-15) (2026-06-11) [`efa0f7075da71e5460eef4056ab5abfc2305c7c5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/efa0f7075da71e5460eef4056ab5abfc2305c7c5)
  Миграция 020: test_snapshots (id, test_id, version, content_json, published_at,
  published_by; unique по test_id+version) и nullable attempts.snapshot_id (пин
  попытки на снапшот; NULL = живая доставка/legacy). Аддитивно, поведение в день
  включения не меняется.

- **feat**(import): импорт разделов и квот выдачи из книги Excel (PRD-14) (2026-06-11) [`96d759e6cce27b1ae934bd184399199d5cfade34`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/96d759e6cce27b1ae934bd184399199d5cfade34)
  Расширение мультилистового импорта книги:
  \- workbook-sheets.ts/workbook-import.ts — листы «Структура (разделы)» и квоты
    выдачи по тегам, шаблон книги; scoring-excel.ts — словесные термины ступеней
    «Цена ответа» (correct/wrong/total вместо c/x/T);
  \- import.tsx + i18n — метки разделов/квот в предпросмотре импорта;
  \- tests-workbook/workbook роуты + тесты round-trip и dry-run;
  \- docs: format.md, prd-14, гайд import-template-guide.md, референс-книга.

- **feat**(tests-editor): абсолютный порог прохождения по баллам, не по числу вопросов (PRD-10) (2026-06-11) [`7e8ab20b1e95a26ef8b6fdf293befd47c115c3d4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7e8ab20b1e95a26ef8b6fdf293befd47c115c3d4)
  Градуированная оценка делает основой абсолютного порога заработанные БАЛЛЫ, а не
  число вопросов: редактор ограничивает порог Σ баллов пула секции (maxPoints,
  с откатом к числу вопросов до round-trip). Обновлены mappers/types/validation
  и их тесты.

- **feat**(web): диалог влияния на тесты и dry-run-first защита (PRD-15 T-12) (2026-06-11) [`64fd3e052a2162bd833ea044683dd23ce3601b8a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/64fd3e052a2162bd833ea044683dd23ce3601b8a)
  Модуль client/src/features/content-protection (ModalDialog+Tag+Banner из DS):
  диалог в режимах block/warn/publish, хук useContentGuard с потоком dry-run-first
  (чистая операция → обычное подтверждение, предупреждения → диалог, блокировка →
  диалог с «Удалить принудительно» для админа). Подключено к удалению и правке тем
  и вопросов (одиночное + массовое) и к публикации теста (onError ловит 409
  publish_infeasible). Эскиз: docs/wireframes/prd15-content-in-use.html.

- **feat**(server): применить защиту контента на роутах и scope аналитики (PRD-15) (2026-06-11) [`4e6295700f9fe5e0196d35a02e6481a7ce1ee067`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e6295700f9fe5e0196d35a02e6481a7ce1ee067)
  \- topics/questions/folders/test-folders: создатель в created_by, 409
    content_in_use + ?force + ?dryRun на удалении/правке/массовых операциях;
  \- tests: requireUserContext+scope на /design и /screen-template (FR-09),
    publish-гейт 409 publish_infeasible (E-12);
  \- analytics: фильтрация агрегатов, экспортов и attempt-by-id по области
    видимости актора (F-5, FR-08), общий хелпер analyticsScope.
  
  Попутно в tests.ts/questions.ts едут малые хунки предыдущего трека PRD-10/14
  (maxPoints секции, текст help-листа «Цена ответа») — те же файлы.

- **feat**(server): ссылочная защита контента и dry-run (PRD-15 блок A) (2026-06-11) [`3966ce8a1155bdf267028c5cd08c677e879dc2e3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3966ce8a1155bdf267028c5cd08c677e879dc2e3)
  Серверное ядро защиты общего контента:
  \- services/draw-feasibility.ts — сбор зависимых тестов и оценка мутации
    (удаление темы/вопросов, перетегирование), политика published→409/draft→warn,
    publish-time проверка (E-12) и формулы PRD-2 по тегам (E-6);
  \- services/content-guard.ts — гарды роутов: создатель/админ, 409 content_in_use
    со списком зависимостей, admin ?force=true, ?dryRun=true предпросмотр;
  \- services/test-access.ts — ветка assignedToUser в canReadTest (role-model 6.4);
  \- services/questions-import.ts — те же гарды на пути импорта PRD-14;
  \- middleware/auth.ts — requireUserContext (объектный scope без capability-гейта);
  \- storage.ts — created_by в create-методах, getTestsUsingTopic/Question,
    getTestSectionsByTopic, getMeasurementsForQuestions, getTopicPageRefs,
    isTestAssignedToUser, полный каскад deleteTopic (events/sections/pages).

- **feat**(shared): движки выполнимости выдачи и эффективной цены (PRD-15) (2026-06-11) [`f6063616c3aa8182e039ba36105696871bcec267`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f6063616c3aa8182e039ba36105696871bcec267)
  Чистые, покрытые тестами модули без зависимостей от БД и сети:
  \- shared/draw/feasibility.ts — оценка выполнимости выдачи по каждому
    зависимому тесту (пул/квоты/адаптивные уровни/шкалы/формулы), симулирует
    реальный drawSection для совпадения строгости с рантаймом;
  \- shared/scoring/effective-scoring.ts — цепочка эффективной цены вопроса в
    тесте (переопределение -&gt; секция -&gt; тест -&gt; системное умолчание) и
    переопределение сложности (задел блока D);
  \- shared/schema.ts — колонки created_by и индекс test_sections.topic_id.

- **feat**(db): аудит created_by контента и индекс test_sections.topic_id (PRD-15) (2026-06-11) [`642467be1ceb425e69c099516115126b7e51ae67`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/642467be1ceb425e69c099516115126b7e51ae67)
  Миграция 019: nullable created_by на topics/questions/folders/test_folders
  (legacy-строки NULL = админ-управляемые) и индекс по test_sections.topic_id
  для запроса «где используется». Аддитивно, поведение в день включения не
  меняется.

### Fixes

- **fix**(ds): красное правило required-маркера в загружаемую копию DS-CSS (2026-06-13) [`b4a160b99d7dd8ae713e5b6a6001cc32e5763e4a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b4a160b99d7dd8ae713e5b6a6001cc32e5763e4a)
  В прошлом коммите (c2b3046) правило .ou-field__lbl-req/.ou-combo__lbl-req ушло
  только в «источник» vendor/ui-kit/css, а приложение импортирует отдельную копию
  client/src/styles/vendor/university-rt.css. Поэтому span-маркер рендерился, но
  без своего цвета наследовал цвет метки — звёздочка была не красной. Добавил то же
  правило в загружаемую копию; обе копии теперь синхронны.

- **fix**(ds): красная звёздочка обязательного поля у Input/Combobox (2026-06-13) [`c2b3046befaa5cc38fb2b9e2298d1fe8374c2ce7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c2b3046befaa5cc38fb2b9e2298d1fe8374c2ce7)
  В DS-компонентах Input и Combobox не было пропа required (в отличие от
  Textarea/FormField), поэтому звёздочку фейкали прямо в тексте метки
  (label="Ключ *") — она выходила цветом метки, а не красной. Добавил проп
  required, рендерящий маркер ou-field__lbl-req / ou-combo__lbl-req с цветом
  --ou-error-default (зеркально Textarea), и мигрировал все обязательные поля
  редактора и импорта (Ключ, Название, Имя, Целевой тест, Имя нового теста) на
  label + required. Системный пробел DS, бил по всем обязательным полям, не только
  по ключу шкалы.

- **fix**(scales): метка шкалы — необязательная, fallback на ключ (PRD-5) (2026-06-13) [`c1dffcf6b586a76d39b6cf08540716c6db810128`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c1dffcf6b586a76d39b6cf08540716c6db810128)
  Editor-валидация и бэкенд-схема требовали метку, хотя в утверждённом эскизе у
  поля нет звёздочки (обязателен только ключ). Свежедобавленная шкала без метки
  давала фантомную ошибку: баннер «Поля с ошибками» и блок сохранения, но карточка
  поле не подсвечивала, а «Перейти к ошибкам» вела в несуществующий якорь. Снял
  required-проверку метки в валидации и ослабил insertScaleSchema до пустой строки
  (колонка notNull её принимает, миграция не нужна). Где метка нужна для
  отображения — SCORM-bake — подставляется ключ.

- **fix**(editor): пустое состояние «Показатели» по DS (page-layout + иконка + действие) (2026-06-13) [`33e94b192b6ad0539b83433ea58fd14707875727`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/33e94b192b6ad0539b83433ea58fd14707875727)
  Пустое состояние вкладки использовало layout=inline (узкий вариант «для карточек»)
  без иллюстрации и с кнопкой «Добавить показатель» в шапке над крошечным well.
  Приведено к эскизу s-indicators-empty: ou-empty--page по центру, иконка info,
  первичная кнопка действия внутри состояния, без строки-шапки (зеркалит пустое
  состояние «Список шкал»).

- **fix**(topics): не показывать баннер одноимённости при открытии темы без правки имени (PRD-15) (2026-06-13) [`ff6e46e68974481a6051ebc400355ce49796f7e8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ff6e46e68974481a6051ebc400355ce49796f7e8)
  Предупреждение FR-27 рендерилось в общем для create/edit блоке по
  nameDuplicates.length&gt;0 без учёта режима, поэтому при открытии чужой
  одноимённой темы всплывал текст «Создать можно», хотя ничего не создаётся.
  Теперь баннер показывается только когда имя «новое» для Drawer: всегда при
  создании и при редактировании лишь если тему реально переименовали в
  одноимённую. Текст сделан нейтральным (годится и для создания, и для
  переименования).

- **fix**(deploy): build-test.bat гоняет всю PRD-15 цепочку + гейт до push (2026-06-13) [`57622c0472bd9b5bee20ad37630eb13633951dc3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/57622c0472bd9b5bee20ad37630eb13633951dc3)
  Инлайновый ssh-блок build-test.bat (рекуррентный деплой кода на тест) хардкодил
  прогон только migrations/016, затем drizzle-kit push --force — то же, что было в
  deploy.sh: push упирался в промпт create/rename test_question_scoring и дропнул бы
  topic_courses/events и questions.points/scoring_json без backfill. Теперь
  прогоняет 016+023+024+026+027+028 одним мульти-файловым run-sql, затем пре-push
  гейт verify-prd15-pre-push.sql, затем push (no-op). Прод-путь
  (build-docker.bat/deploy-docker.bat -&gt; run-deploy.sh -&gt; deploy.sh) уже исправлен.

- **fix**(deploy): прогонять PRD-15 data-миграции до drizzle-kit push (2026-06-13) [`198b20ae13ee1fd2550abf7106630c2a399388f4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/198b20ae13ee1fd2550abf7106630c2a399388f4)
  Деплой в pre-push шаге гонял через run-sql.cjs ТОЛЬКО 016, затем push --force.
  Миграции 023/024/026/027/028 не запускались, поэтому push видел деструктивный
  diff: спрашивал create/rename test_question_scoring и дропнул бы topic_courses/
  events и questions.points/scoring_json БЕЗ backfill = потеря рекомендаций и цен.
  \- run-sql.cjs принимает несколько файлов (прогон по порядку в одном соединении);
  \- deploy.sh: цепочка 016,023,024,026,027,028 до push;
  \- deploy-test.sh: цепочка 023,024,026,027,028 до push (роли — инлайн выше).
  Теперь push выходит no-op. Цепочка идемпотентна (см. гаржи миграций), повторный
  деплой безопасен.

- **fix**(migrations): идемпотентность backfill после дропа источников (PRD-15) (2026-06-13) [`4ae2624ad117596b978425fdd38896db6965cea2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4ae2624ad117596b978425fdd38896db6965cea2)
  Backfill-миграции ссылались на колонки/таблицы, которые сами же дропают позже,
  поэтому повторный прогон на уже мигрированной БД падал на этапе планирования
  запроса (relation/column does not exist), хотя WHERE не матчил строк:
  \- 023: UPDATE с подзапросами на topic_courses/topic_events -&gt; guard на
    существование topic_courses;
  \- 027/028: INSERT...SELECT на questions.points/scoring_json -&gt; guard на
    существование колонки points;
  \- 025: DELETE WHERE grantee_type -&gt; guard (push-деплоенная БД создаёт
    topic_access_grants уже без grantee_type).
  plpgsql планирует ветку только при входе, поэтому пост-дроп прогон — чистый
  no-op. Проверено на стенде: повтор всей цепочки exit 0, состояние не изменилось.

### Documentation

- **docs**(prd-15): отложенный персист переопределений оценки в плане и эскизе (2026-06-13) [`2002d319c5820fe0ea4416dccee0baa51d5c429a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2002d319c5820fe0ea4416dccee0baa51d5c429a)
  implementation-plan: датированное уточнение 2026-06-13 — переход с немедленного
  PUT/DELETE на драфт + единое «Сохранить». prd15-test-scoring: аннотации «Сбросить»
  и сохранения приведены к отложенной модели, отмечено, что несохранённое
  переопределение зажигает точку несохранённых изменений. FR-30/31/35 не менялись.

- **docs**(prd-15): RUNBOOK — деплой автоматизирует пре-push цепочку миграций (2026-06-13) [`ba0d2283cdef3154c8192922da0e5fdce5237bd6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ba0d2283cdef3154c8192922da0e5fdce5237bd6)
  run-sql.cjs принимает несколько файлов; deploy.sh/deploy-test.sh гоняют
  016/023/024/026/027/028 до push (раньше только 016 — это был баг); 025 не в
  цепочке на push-деплоенной БД; миграции идемпотентны и защищены гаржами.

- **docs**(prd-15): выверенная прод-процедура миграции — run-sql.cjs, запрет push для дропов (2026-06-13) [`eea901284744588eea19b772ec534e31503182a8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/eea901284744588eea19b772ec534e31503182a8)
  Зафиксирован adversarial-аудит беспотерийности и применение 028 на стенде:
  \- секция «drizzle-kit push НЕДОСТАТОЧЕН»: push пропускает backfill (потеря данных
    при дропе topic_courses/events и questions.points/scoring_json) И не создаёт/
    может удалить рукописные индексы (002/004/007/008/009), которых нет в schema.ts;
  \- канонический механизм — нумерованные SQL через script/run-sql.cjs по порядку,
    023 до 024, 028 до push; push только аддитивно после;
  \- шаг 028 в таблице миграций;
  \- секция «Проверка беспотерийности (до/после)» с pre-flight бэкапом и
    проверочными запросами (колонки удалены, оценка сохранена, нет дублей/сирот).

- **docs**(prd-15): уточнить §9 — гибридная difficulty и теги на вопросе осознанно (2026-06-13) [`093974eeafce7edba6bdbc29e6c07ef831dea4d4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/093974eeafce7edba6bdbc29e6c07ef831dea4d4)
  Пункт «Вне объёма» о полном переносе difficulty/tags переформулирован как
  рассмотрено-и-отклонено: difficulty намеренно гибридная (база на вопросе +
  per-test override, FR-34 — целевая модель), tags остаются контентной
  классификацией (опора квот PRD-11 и формул PRD-2). Чтобы пункт не читался как
  недоделанный хвост трека.

- **docs**(prd-15): закрытие трека — фаза 5 (приёмка/RUNBOOK) и T-40 (удаление колонок) (2026-06-13) [`ed613dea07de04053adef6d803b7e9307b53b389`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ed613dea07de04053adef6d803b7e9307b53b389)
  Фаза 5: новая матрица приёмки (E-1..E-13 + FR-01..FR-36 -&gt; тесты) и RUNBOOK
  развёртывания (порядок миграций 019-028, переходный режим снапшотов, откат).
  T-40: статусы плана 1.5 / content-ownership 1.3 / ROADMAP — трек завершён;
  секция T-40 в RUNBOOK (миграция 028 + откат); format.md — «Балл»/«Цена ответа»
  удалены из листа «Вопросы»; CLAUDE.md и пункт аналитики PRD-13 актуализированы.

- **docs**(scoring): PRD-15 блок D — план, content-ownership, role-model, ROADMAP, эскиз (2026-06-13) [`b53ee3f248b4f0484aeb7676d08b7d1f8d641a9e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b53ee3f248b4f0484aeb7676d08b7d1f8d641a9e)
  Документация трека «оценка — свойство теста»: implementation-plan (T-34..T-44), content-ownership и role-model, запись в ROADMAP, согласованный эскиз prd15-test-scoring (конвенция маркировки переопределений).

- **docs**(wireframes): привести превью обратной связи темы к отгруженному TD-02 (единый Drawer) (2026-06-12) [`c51969e57ac791ab8f851d2de94849e424eaba91`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c51969e57ac791ab8f851d2de94849e424eaba91)
  Эскиз единого Drawer темы (prd15-topic-access.html, вкладка «Свойства») имел
  устаревшее превью обратной связи (мета «N ссылок · N файлов» и сноска
  «мероприятия запланировано») — до реализации TD-02. Привёл к виду отгруженного
  компонента feedback-preview.tsx: шапка со сниппетом и карандашом + сгруппированные
  списки Документы/Курсы/Мероприятия (ссылка мероприятия опциональна), источник —
  topics.feedback_json. Новый файл не создавал (overlap с утверждённым эскизом).
  
  \- prd15-topic-access.html: разметка превью, иконка i-file-text, обновлённая сноска
  \- tb-components.css (зеркало эскизов): синхронизированы TD-02-классы __head/__group/__items/__item

- **docs**(prd15): TD-02 редактор обратной связи сделан, остаток в план §7 (2026-06-12) [`32ed7928ab7ca78ca7988d96157e15f6662b0824`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/32ed7928ab7ca78ca7988d96157e15f6662b0824)
  Раздел «Мероприятия», «Курсы», группированное превью и плумбинг events — DONE. Остаток: показ событий ученику (result-context, SCORM resultsPage), адаптивный триггер, сведение topic_courses/events в фидбэк.

- **docs**(prd15): задачи после PRD-15 — TD-01 гранты user-only, TD-02 редактор ОС (2026-06-12) [`ebf2c0c65088ca47c70b8746aedcd299a2fc603c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ebf2c0c65088ca47c70b8746aedcd299a2fc603c)
  implementation-plan §7: гранты тем только пользователям (убрать группы); расширение редактора обратной связи ссылками на мероприятия и сведение курсов/событий в фидбэк.

- **docs**(prd15): согласованные эскизы T-32 — Drawer темы, список, доступ, одноимённость, дубли (2026-06-12) [`c5d54a11c3283d762c57683f270aaae3fd18dbf4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c5d54a11c3283d762c57683f270aaae3fd18dbf4)
  approved/: единый Drawer темы (вкладки Свойства+Доступ, гранты только пользователям, выбор режима отзыва), список без меток принадлежности, диалог имени, индикатор недоступной темы (E-11), отчёт дублей; обновлены content-in-use и publication-state.

- **docs**(prd15): эскиз состояний публикации и переопубликации (T-23) (2026-06-11) [`6ea26e082e42e8669f6a1fbe3af7f98f22e0eee5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ea26e082e42e8669f6a1fbe3af7f98f22e0eee5)
  4 состояния: бейджи в списке, индикатор в редакторе, диалог экстренной
  переопубликации, уведомление ученику об аннулированной попытке (FR-12/FR-14).

- **docs**(prd15): аудит владения данными, BRD/PRD-15, эскиз и правки role-model (2026-06-11) [`74eae7be98e4e643c1c5cbc082db8d1d78e42fa2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/74eae7be98e4e643c1c5cbc082db8d1d78e42fa2)
  \- AUDIT_DATA_OWNERSHIP.md — аудит парадигмы владения (находки F-1..F-10,
    матрица E-1..E-13);
  \- brd-content-ownership.md (BRC-01..27) + prd-15/ (FR-01..36 + план T-01..44);
  \- role-model.md — владение и видимость тем, гранты use/manage, унификация
    выдачи грантов тестов (BRC-27); brd-access-control.md — перекрёстная ссылка;
  \- wireframes/prd15-content-in-use.html — эскиз диалога влияния (4 состояния).

## [2.1.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.0.1-beta...v2.1.0-beta)

### Features

- **feat**(tests): экспорт теста в Excel из меню действий (PRD-14 FR-15) (2026-06-11) [`e7566f3e2adcf50d184db2f1c89edb96d9ecadfb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e7566f3e2adcf50d184db2f1c89edb96d9ecadfb)
  В меню «...» теста добавлен пункт «Экспорт в Excel» (после «Экспорт SCORM», группа экспорта) → GET /api/tests/:id/workbook/export. Закрывает ветку экспорта теста из PRD-14: тест — из меню теста, вопросы — со страницы «Вопросы».

- **feat**(import): раздел «Импорт» в боковой навигации (PRD-14 FR-15 UI) (2026-06-11) [`95b399893470b5ea8f64d5a6b3779f51cda8d218`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/95b399893470b5ea8f64d5a6b3779f51cda8d218)
  Страница /author/import (import.tsx) на DS-компонентах по согласованному эскизу: инлайн-поток без модалок, автоопределение листов файла, ветка «Целевой тест» (существующий или создание нового), dry-run-предпросмотр плана, состояния пусто→файл→предпросмотр→готово.
  
  Маршрутизация: только вопросы → /api/questions/import; существующий тест → /api/tests/:id/workbook/import; новый → /api/workbook/import-new. Эскиз перенесён в approved/.
  
  Заголовок пока через PageHeader в контенте — переедет в шапку в рамках задачи header-title.

- **feat**(import): мультилистовая книга теста и бэкенд раздела «Импорт» (PRD-14 FR-15) (2026-06-11) [`a5fd6a122c8f9ddcbe077fc7a95d9f294fa8d437`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a5fd6a122c8f9ddcbe077fc7a95d9f294fa8d437)
  Книга = один тест с листами Вопросы/Шкалы/Показатели/«Вклады вопросов»: распознавание по имени, многопроходный импорт (вопросы→шкалы→вклады+показатели), upsert по естественным ключам, ссылка вклада на вопрос по ID или локальному «Ключ строки», симметричный экспорт и round-trip, dry-run без записи (tests-workbook.ts, workbook-import.ts, workbook-sheets.ts).
  
  Раздел «Импорт» получает свои эндпоинты без testId (workbook.ts): inspect (определение листов — нужен ли целевой тест), import-new (создание бессекционного черновика + импорт), template (пустые 4 листа + справка).
  
  validateResultVariableFormula принимает extraScaleKeys/extraVarNames, importWorkbook их прокидывает — формулы на ещё не записанные (dry-run, новый тест) шкалы/показатели не дают ложных ошибок.

- **feat**(import): импорт/экспорт банка вопросов в Excel (PRD-14 Фазы 0-2) (2026-06-11) [`cf280e8c53464446fc4bb6196f67deeb77f17bd6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cf280e8c53464446fc4bb6196f67deeb77f17bd6)
  Round-trip для matching/ranking и сохранение нулевых Балл/Сложность (Ф0); паритет с моделью вопроса — теги, условная ОС, «Цена ответа» (PRD-10), правило «пустая ячейка = сброс / нет колонки = поле не меняется» (Ф1); скачивание шаблона и предпросмотр dry-run без записи (Ф2).
  
  Разбор и сериализация строки вопроса вынесены в общие сервисы (questions-import.ts/questions-export.ts), грамматика «Цены ответа» — в scoring-excel.ts; роут questions.ts стал тонкой обёрткой над ними (переиспользуются импортом книги теста, FR-15).

- **feat**(editor): баннер «Шаблон обновлён», превью из default и инвалидация кэшей шаблонов (2026-06-10) [`b76dccd73678a49df84af478a768f1ded9556bb1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b76dccd73678a49df84af478a768f1ded9556bb1)
  Три правки UI редактора под PRD-3/PRD-7 G21:
  \- design-tab: при дрейфе версии черновика от текущей версии шаблона
    (контент перезалит под тем же id) показывается баннер «Шаблон обновлён»
    с действием «Обновить оформление» — refreshTemplateVersion
    перештамповывает версию и отбрасывает params, которых больше нет в
    манифесте. Флаг считается от черновика, поэтому баннер исчезает сразу
    после реконсиляции, до сохранения.
  \- «Структура»: previewTemplateId рендерит превью из default, когда
    активный шаблон не объявляет вариант вида страницы — иначе «Структура»
    пишет «Из стандартного шаблона», а превью показывает активный шаблон.
  \- админ-реестр: лайфсайкл-мутации инвалидируют не только список реестра,
    но и авторские кэши шаблонов (["templates"]); при staleTime: Infinity
    иначе галерея «Заменить шаблон» и карточка design-tab не подхватывали
    активацию/деактивацию/перезалив.

- **feat**(scorm): паковать default для fallback системных экранов start/results (2026-06-10) [`e0bb586d36ef565f6e32f419bafff0855af4ce9f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e0bb586d36ef565f6e32f419bafff0855af4ce9f)
  В выгрузке системные экраны start/results, которые активный шаблон не
  объявляет в contentTemplates, теперь рендерятся из встроенного default —
  паритет со «Структурой» и веб-превью (PRD-7 G21). Раньше пакет показывал
  собственный (или пустой) экран активного шаблона.
  
  Экспортёр определяет fallbackKinds по манифесту, прокидывает их в
  TEST_DATA, кладёт файлы default под template-default/ и его CSS как
  styles-default.css. Рантайм (templateLoader/mainRender/startPage/
  viewResults) подгружает fallback-лейауты и переключает таблицу стилей на
  default только пока показан fallback-экран — пакет показывает один экран
  за раз, поэтому подмена глобального стиля безопасна.
  
  Манифест default (preview.html) объявляет start/results как
  contentTemplate; попутно поправлен цвет текста карточки роутера (фикс с
  inherit на светлой карточке) и счётчик в подвале превью (5 -&gt; 7).

- **feat**(templates): рендерить системные экраны из default, когда активный шаблон их не объявляет (2026-06-10) [`31c74cda4cd7f60e29f9ed2b1c9b7dcc53da0976`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/31c74cda4cd7f60e29f9ed2b1c9b7dcc53da0976)
  Веб-хост (роуты screen-template и result) и экспорт SCORM теперь резолвят
  каталог шаблона через resolveSystemScreenDir с activeOnly: PRD-7 G21.
  
  Два инварианта для ученических рендеров:
  \- activeOnly: неактивный/черновой/невалидный шаблон не доходит до ученика
    и в выгрузку — резолв проваливается на default (NFR-01).
  \- fallback по виду: если активный шаблон не объявляет contentTemplate
    нужного вида (start/results), системный экран рендерится из default —
    так рантайм совпадает с превью «Структуры» и бейджем «Из стандартного
    шаблона», а не показывает чужой/пустой экран.
  
  Статус активности фильтруется в JS, а не в SQL, чтобы резолвер оставался
  юнит-тестируемым без живой БД.

### Fixes

- **fix**(deps): синхронизировать package-lock (добавить @emnapi) — чинит npm ci (2026-06-11) [`004a63da26c9099a3b3707a7d8089dbe7b1c1c3f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/004a63da26c9099a3b3707a7d8089dbe7b1c1c3f)
  Lockfile был рассинхронизирован с package.json после бампа @tailwindcss/typography: отсутствовали транзитивные @emnapi/core и @emnapi/runtime (optional WASM-runtime для @tailwindcss/oxide-wasm32-wasi). Из-за этого npm ci падал с EUSAGE (Missing ... from lock file) при сборке Docker-образа. Пересинхронизировано через npm install --package-lock-only; package.json не изменён.

- **fix**(tests): список тестов на всю высоту экрана, меню действий не обрезается (2026-06-11) [`4c86457c95bb9d0383508b30d772afefbe55093f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4c86457c95bb9d0383508b30d772afefbe55093f)
  После миграции на DS AppShell .tb-tests-list не заполнял высоту ou-shell__main, из-за чего .tree-area была всего в несколько строк: таблица «обрезалась», а абсолютное .dropdown-menu выходило за короткий overflow-контейнер и резалось. Корень сделан flex-колонкой (flex:1; min-height:0), tree-area получила min-height:0 — таблица тянется на весь экран, меню помещается.

- **fix**(import): стандартная DS-кнопка «Выбрать файл» в загрузчике (2026-06-11) [`8dd4d39d53601ae65b3ad2af28d0f776fda5c25c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8dd4d39d53601ae65b3ad2af28d0f776fda5c25c)
  Серая «таблетка» ou-uploader__cta из FileUploader выглядела как сломанная кнопка (особенно в тёмной теме). Через слот children подставлена настоящая DS-кнопка (Button secondary) с иконкой/подписями; вся зона остаётся кликабельной. Общий DS-CSS не трогаем — правка локальная, чтобы не задеть другие загрузчики.

- **fix**(templates): отбраковывать битый DSL-синтаксис layout-ов при импорте (2026-06-10) [`0a317da11843cd381970da7bb68ac864df89f88f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0a317da11843cd381970da7bb68ac864df89f88f)
  Валидация пакета проверяла наличие layout-ов и слотов, но не компилировала их. Layout с недопустимым mustache (пустой {{}}, {{{ }}}, незакрытый блок) проходил импорт, сохранялся черновиком и падал необработанным исключением в предпросмотре/рантайме (оба хоста рендерят тем же компилятором). Теперь validateTemplatePackage компилирует каждый DSL-источник манифеста (layouts/partials/systemPages/contentTemplates) и блокирует импорт ошибкой LAYOUT_TEMPLATE_SYNTAX, покрывая загрузку, обновление и ре-валидацию.

- **fix**(templates): консистентный ребайнд теста на default при деактивации/удалении (2026-06-10) [`09b047d6c605fc4749a28a622569b8b749ce9812`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/09b047d6c605fc4749a28a622569b8b749ce9812)
  Каскад деактивации и startup-reconcile переключали в designSettingsJson
  только templateId на "default", оставляя params, templateVersion и
  templateApiVersion от удалённого шаблона. В итоге default рендерился с
  чужими параметрами и устаревшим снимком версии, а автору приходилось
  вручную сбрасывать оформление теста до того, как заново загруженный под
  тем же id шаблон подхватывался корректно.
  
  Вынесен чистый хелпер rebindToDefault (PRD-3 §5.3): сохраняет только
  params, чьи ключи всё ещё есть в манифесте default, и перештамповывает
  поля версии под default. Переиспользуется обоими путями (роут деактивации
  и reconcile осиротевшего builtin).

### Documentation

- **docs**(import): PRD-14, спецификация формата, BRD (BR-11) и ROADMAP (2026-06-11) [`a8ee259748e866177c1d132f053ea3fab1aa3dcb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a8ee259748e866177c1d132f053ea3fab1aa3dcb)
  Контракт формата Excel (колонки вопросов, грамматики «Цены ответа» и листов книги), PRD-14 с фазами 0-2 + мультилист FR-15 и реализованным разделом «Импорт» (§5.5), запись BR-11/Этап 12 в BRD, актуализация ROADMAP.

## [2.0.1-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v2.0.0-beta...v2.0.1-beta)

### Features

- **feat**(editor): черновик «Структуры» с полным откатом и новые системные строки (2026-06-09) [`b6bf6a250cb901b0f642a435f9d413958d46cb00`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b6bf6a250cb901b0f642a435f9d413958d46cb00)
  Правки структуры (добавить/изменить/переставить/сменить вариант/удалить) теперь
  копятся локально и применяются на «Сохранить», откатываются на «Отмена» — единый
  цикл с черновиками теста и дизайна. Каталог вариантов следует за выбором шаблона
  в «Оформлении» (до сохранения). Добавлены строки «Старт»/«Итоги теста» и
  per-section «Введение/Итоги раздела»; их per-page предпросмотр рендерит настоящий
  экран через buildStartState/buildResultContext. Подсказка «Доступно вариантов»
  показывается для всех типов строк.

- **feat**(structure): системные start/results и per-section intro/summary (2026-06-09) [`d54837c04f4c5239c6867261bd6c68a9ad9d3c5f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d54837c04f4c5239c6867261bd6c68a9ad9d3c5f)
  Жизненный цикл создаёт «Старт» и «Итоги теста» как тест-уровневые синглтоны, а
  «Введение/Итоги раздела» — по одной строке на тему: intro/summary стали
  per-topic, в linear_flat их нет (у плоского теста нет раздела). Стандартный
  шаблон объявляет start/results; SCORM-рантайм исключает их из потока контентных
  страниц — они рисуются своими экранами.

- **feat**(content-pages): валидировать правки структуры против чернового шаблона (2026-06-09) [`73b81f4e362c5a7c4727db888092146a9293301d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/73b81f4e362c5a7c4727db888092146a9293301d)
  Редактор передаёт выбранный в «Оформлении» шаблон через ?templateId=, чтобы смена
  варианта и добавление страниц работали ещё до сохранения дизайна, а не падали
  404 на проверке ключа против сохранённого шаблона.

- **feat**(schema): ввести виды вариантов start и results (PRD-1 §4.3) (2026-06-09) [`c28841af4cd6549ffacdbf26fbd30397df67c8a3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c28841af4cd6549ffacdbf26fbd30397df67c8a3)
  Стартовый экран и итоги теста становятся template-backed элементами структуры:
  добавлены в variantKindSchema, enum content_pages.kind и в обязательные виды
  стандартного шаблона.

- **feat**(db): расширить content_pages.kind видами start/results (2026-06-09) [`328741b1e1ee1e17b3f051d7516f8e8fe65bd436`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/328741b1e1ee1e17b3f051d7516f8e8fe65bd436)
  Структурная пересборка вводит системные страницы «Старт» и «Итоги теста».
  Миграция 018 добавляет их в CHECK content_pages_kind_check — иначе вставка
  этих строк отвергалась бы на свежей БД.

### Fixes

- **fix**(preview): показывать все объявленные варианты страниц и меню роутера (2026-06-09) [`f6fdb226bfa8c85fdfd787da90560b50bb590dd6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f6fdb226bfa8c85fdfd787da90560b50bb590dd6)
  Предпросмотр перечислял только preview.routes, теряя варианты intro/info/summary,
  которых там нет (учебные страницы, вторые варианты). Теперь buildScreenInputs
  добавляет каждый объявленный контентный вариант с уникальным id, а роутер
  рендерит меню тем на демо-данных. Заодно чинит невидимое название темы в карточке
  стандартного шаблона (текст наследовал светлый цвет на светлом фоне).

### Documentation

- **docs**: задокументировать структурную модель start/results и per-section intro/summary (2026-06-09) [`5b343acb512dda1cd1f3330330e2c52c39415c95`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5b343acb512dda1cd1f3330330e2c52c39415c95)
  Обновлены PRD-1 §4.3 (виды вариантов, таблица гранулярности, жизненный цикл),
  техспецификация платформы (page kinds, требования к стандартному шаблону) и
  руководство разработчика (пример contentTemplates, справочник kind). Зафиксирована
  итоговая модель: start/results — тест-уровневые, intro/summary — по разделам (в
  linear_flat отсутствуют).

## [2.0.0-beta](https://github.com/vvlad1973/Fullstack-MVP-testing/compare/v1.0.0...v2.0.0-beta)

### Features

- **feat**(editor): предпросмотр шаблона и страницы на едином renderScreenInto (2026-06-09) [`cfa34745bbc648c8a12924bcd1fb0d8d1cfcf241`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cfa34745bbc648c8a12924bcd1fb0d8d1cfcf241)
  Обе модалки («Оформление» и «Структура») переведены с легаси preview.html-iframe
  на общий TemplateScreen/renderScreenInto — паритет с разделом «Шаблоны» (легаси
  не удалён, спит). Превью рендерят по ЧЕРНОВИКУ дизайна (шаблон + параметры из
  текущего «Оформления»), поэтому несохранённая смена шаблона видна сразу:
  useDesignSettings следит за draft.templateId, параметры накладываются как
  CSS-переменные на host. Фиксированный холст (1280×720) вписывается по ширине
  (scale-to-fit + ResizeObserver) — без горизонтального скролла. Предпросмотр
  страницы рендерит ОДНУ страницу по её kind: questions → образец вопроса в
  layout вопроса, контентные kind → значения самой страницы.

- **feat**(templates): общий движок предпросмотра — маппинг параметров, билдеры, bundle (2026-06-09) [`365b33bc474a0f4ea5cef16ce26654a462e68fa2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/365b33bc474a0f4ea5cef16ce26654a462e68fa2)
  Фундамент унификации предпросмотра на единый renderScreenInto (без второго
  движка). shared/template/params-css — канонический маппинг параметр→CSS-var
  (фон/текст/карточки согласованно); рантайм пакета templateCore.js делегирует в
  него через TBTemplate-глобал с inline-фолбэком. preview-context получает
  buildContentPageScreen (одна реальная контент-страница). readTemplateBundle
  (manifest+demo+layouts+css, резолв через source_path) + эндпоинт
  GET /api/templates/:id/bundle отдают файлы шаблона клиентским превью.

- **feat**(learner): пер-topic таймер в веб-рантайме (PRD-4 v1.1 §3.2) (2026-06-09) [`a061ee902dab3c6bc6a2f2376fc4495a3d5920f9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a061ee902dab3c6bc6a2f2376fc4495a3d5920f9)
  Веб-плеер получает паритет со SCORM по тайм-боксу темы. Бюджет времени темы
  (test_sections.timeLimitMinutes) переносится в persisted variant и в адаптивные
  topic-state; новый хук use-section-timer ведёт обратный отсчёт, а истечение
  форс-завершает текущую тему через POST /attempts/:id/expire-topic-adaptive
  (идемпотентно при потерянном/повторном ответе — ре-синк на текущий вопрос).
  Поле variant.sections[].timeLimitMinutes добавлено в схему; отсутствие на
  legacy in-progress попытках трактуется как null.

- **feat**(access): UI ролей и прав + диалог доступа к тесту (PRD-13) (2026-06-08) [`47b9cb1ec3e09c6bec9f6668dbcaea2420d2efe7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/47b9cb1ec3e09c6bec9f6668dbcaea2420d2efe7)
  Маршруты защищаются по capability (ProtectedRoute requiredPermission), стартовая страница выбирается по доступной зоне. Если у пользователя нет ни одной зоны — показывается экран «Нет доступа» вместо бесконечного редиректа на /learner (иначе пустой чёрный экран). auth-контекст отдаёт can()/hasRole() из server-permissions.
  
  Панель «Общий доступ» (features/tests/access): владелец + гранты edit/assign, выдача/смена/отзыв с явным сохранением. Меню уровня в строке таблицы рендерится поверх (overflow таблицы ослаблен, иначе DS Table обрезает инлайн-меню Select). Ролевые компоненты: role-picker, lib/roles (formatRoles); колонка владельца и действие «Общий доступ» в списке тестов; редактор пользователей/групп с мультиролями.

- **feat**(access): серверный контроль доступа по правам (PRD-13) (2026-06-08) [`bf6eca2a585f2b577e8723296386d680cf29e1e1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bf6eca2a585f2b577e8723296386d680cf29e1e1)
  Заменяет жёсткие гарды роли (requireAuthor/requireLearner) на requirePermission(capability) во всех доменных роутах. Объектная видимость тестов — владелец + гранты (services/test-access, middleware/test-scope). Суперадмин вычисляется из SUPERADMIN_EMAILS по хешу email (server/config), нигде не хранится; provisionSuperadmins создаёт недостающие аккаунты на старте.
  
  /api/auth/login и /me теперь отдают effective roles + union permissions вместо одиночного role. storage: getUserRoles/addUserRole, seed заводит роли через user_roles. Гранулярная проверка прав на каждом эндпоинте.

- **feat**(access): доменная модель ролей и прав + схема БД (PRD-13) (2026-06-08) [`fcbad9d0e8a9bb11688bc6b777034b228d6ac272`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fcbad9d0e8a9bb11688bc6b777034b228d6ac272)
  Вводит RBAC-фундамент: shared/access (роли SU/AD/AU/MG/US, карта прав, правила назначения, конфиг-суперадмин) как единый источник правды для клиента, сервера и рантайма.
  
  Схема: убрана легаси-колонка users.role, роли вынесены в user_roles (many-to-many); добавлены tests.owner_id и test_access_grants (владелец + гранты edit/assign). Миграция 016 создаёт таблицы и переносит роли (author -&gt; administrator, learner -&gt; learner) под guard по наличию users.role (идемпотентно/безопасно при повторах), 017 дропает колонку.

- **feat**(default-template): кольцевая диаграмма на странице «Итог раздела» (2026-06-07) [`8c4c6d7cdb4d16f2b653c283977c0a3b36406f94`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8c4c6d7cdb4d16f2b653c283977c0a3b36406f94)
  \- manifest: summary.result разрешает core.ringChart (и делает его дефолтным);
  \- demo: итог раздела рендерится кольцом (core.ringChart);
  \- base.css: центрирование значения внутри кольца (.renderer--ring-chart);
  \- preview.html перегенерирован под изменённый шаблон.

- **feat**(template-registry): стартовая валидация шаблонов, отпечаток и чистка осиротевших (2026-06-07) [`a40cdf2725a7b506f6ca815f381d9ea0b64fcf5b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a40cdf2725a7b506f6ca815f381d9ea0b64fcf5b)
  reconcileUploadedTemplates расширен до reconcileTemplates: на старте проверяет
  каждый нешипованный шаблон тем же структурным валидатором, что и при загрузке.
  Чтобы битый/осиротевший шаблон не оказался активным на проде:
  
  \- осиротевшие встроенные (id вне BUILTIN_IDS, без файлов — напр. corporate/minimal)
    удаляются, зависимые тесты транзакционно переводятся на default;
  \- загруженные с пропавшими файлами или непрошедшие валидацию деактивируются
    (status=invalid, не удаляются — это данные автора).
  
  Оптимизация: неизменённые по source_fingerprint шаблоны пропускают повторную
  валидацию (stat без чтения содержимого). server/index.ts вызывает reconcileTemplates.

- **feat**(schema): добавить templates.source_fingerprint для стартовой реконсиляции (2026-06-07) [`c3f0a64927bde4fdcb1ddb84f6d3ef335e2f7a92`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c3f0a64927bde4fdcb1ddb84f6d3ef335e2f7a92)
  Дешёвый отпечаток источника шаблона (хеш путь/размер/mtime файлов) — ключ
  пропуска повторной валидации при старте, когда файлы не менялись. Колонка
  nullable; миграция 015 идемпотентна (в деплое схема применяется drizzle-kit push).

- **feat**(prd-3): админ-UI реестра шаблонов — список, загрузка, предпросмотр+проверка, жизненный цикл (2026-06-07) [`3ee4926203401bb1601f329d596edfc5e4ef8107`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3ee4926203401bb1601f329d596edfc5e4ef8107)
  Видимая часть Фазы 2/3 по утверждённому эскизу, на компонентах @universityrt/ui-kit.
  
  \- Страница /author/templates: карточный список с реальным превью, бейджами
    статуса/комплектности/работоспособности (Tag+tone) и меню действий.
  \- Модалки: загрузка (FileUploader), детали, предпросмотр+проверка
    (3-уровневый рельс Раздел→Тип→Вариант; живой рендер тем же общим
    renderScreenInto, что и оба рантайма — паритет PRD-12; запуск runSmokeChecks
    в браузере → отчёт на сервер → разблокировка активации NFR-01),
    обновление, подтверждения деактивации/удаления с подгрузкой usageCount.
  \- Хелперы статусов и группировщик рельса; роут, пункт навигации «Шаблоны»,
    i18n; CSS-слой tb-templates-admin.css на токенах DS.

- **feat**(prd-3): эндпоинты smoke-bundle и preview-image для клиентской проверки (2026-06-07) [`19f394987e6b5aaba4aeb8d3d7f198b41e3feb5a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/19f394987e6b5aaba4aeb8d3d7f198b41e3feb5a)
  Браузеру для предпросмотра и проверки работоспособности (NFR-03) нужны
  файлы шаблона, которых read-API не отдаёт (preview-page — только встроенные).
  
  \- GET /:id/smoke-bundle: manifest + demo + layouts (без shell) + css +
    template.js/rules из пакета; сервер только читает, не исполняет (NFR-02);
    422 при повреждённых demo-данных.
  \- GET /:id/preview-image: стрим assets.preview для карточек реестра, путь
    строго внутри корня шаблона (защита от обхода).

- **feat**(prd-3): фаза 2 — движок проверки работоспособности + серверный гейт (2026-06-07) [`5ace313024e04b19596efcaa616ea120f8660cc2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5ace313024e04b19596efcaa616ea120f8660cc2)
  Клиентская «проверка работоспособности» шаблона (NFR-03) и гейт активации (NFR-01).
  
  \- shared/template/preview-context.ts: мост PreviewDemoDataset → render-входы по
    каждому экрану из manifest.preview.routes; резолв layout с fallback (§5.3),
    контекст через общие builders (start-state/result-context/transition-context),
    слоты вопроса (интеракция по типу) и скелет content-страниц
  \- shared/template/smoke-runner.ts: обход экранов, рендер каждого через общий
    renderScreenInto в изолированном контейнере, сбор SmokeReport (throw/пустой
    обяз. слот/console-error/пустой рендер = блок; console.warn = предупреждение);
    опц. синтакс-проверка template.js (compile-only, NFR-02) и парс rules JSON.
    Среда-агностичен (browser iframe / jsdom)
  \- сервер: POST /:id/smoke-test принимает клиентский SmokeReport и пишет
    smoke_test_json (заменил заглушку-501); activate ужесточён — для uploaded
    нужно validation.ok И smoke.ok
  \- тесты: движок (guard «default проходит свою проверку» + битые фикстуры);
    обновлён admin API (гейт активации, intake отчёта)
  
  tsc 0; vitest 2089/2089 (+13). Сервер smoke не исполняет (NFR-02).

- **feat**(prd-3): фаза 1 — backend-фундамент жизненного цикла шаблонов (2026-06-06) [`5468a4d2682366bdd054a13decc370a8ef35d4aa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5468a4d2682366bdd054a13decc370a8ef35d4aa)
  Админ-реестр внешних шаблонных ZIP поверх платформы PRD-12 (Фаза 1: без UI и
  без smoke-проверки — она клиентская, Фаза 2).
  
  \- схема `templates` +6 колонок (status/source_type/source_path/validation_json/
    smoke_test_json/installed_at) + zod-енумы; миграция 011 (идемпотентная,
    backfill встроенных, CHECK на status/source_type)
  \- сервис ZIP: распаковка in-memory (jszip) с защитой от zip-slip, strip единого
    корня, хранилище uploads/templates/&lt;id&gt;, упаковка для экспорта
  \- структурная валидация (§4.1/§4.2): манифест, referenced-файлы, запрет
    внешних URL/CDN, лимит размера, контракт слотов по РЕАЛЬНОМУ движку
    renderScreenInto (data-slot="page"; question-text/question-interaction),
    а не по аспирационным маркерам §7 — иначе штатный default не прошёл бы
  \- Admin API /api/admin/templates: list/upload/details/activate/deactivate
    (транзакционный каскад тестов на default)/delete/update/export/validate;
    smoke-test — заглушка 501 (Фаза 2). Гейт author-роли
  \- реестр: новые поля при sync, удалены мёртвые corporate/minimal, реконсиляция
    uploaded на старте (помечает invalid при пропаже файлов)
  \- хелпер версий API перенесён в @shared/schema (валидатор без импорта db)
  \- тесты: валидатор (+ guard «default проходит свой валидатор»), package
    (zip-slip/strip/round-trip), lifecycle API; правка syncBuiltinTemplates
  
  tsc 0 ошибок; vitest 2076/2076 (+37). Миграция к dev-БД ещё не применена.

- **feat**(prd-6): авторский пейн «Повторное прохождение» + маппер retakePolicy (2026-06-04) [`381882f892d5b304482d9ef85fc13911fc8e37e0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/381882f892d5b304482d9ef85fc13911fc8e37e0)
  Новый rail-пункт во вкладке «Настройки» (по согласованному эскизу
  prd6-retake-policy.html): Switch enabled → (если вкл) период охлаждения,
  Select плагина из активного реестра, SegmentedControl failPolicy и
  best-effort warning для suspend_data. Маппер связывает форму с
  tests.retake_policy_json в обе стороны.
  
  \- TestEditorModel.retakePolicy + TestSettingsPayload.retakePolicyJson
  \- мапперы: apiToEditorModel читает (нормализуя legacy cooldownDays + кламп
    1–3650), editorModelToPayload пишет объект при вкл / null при выкл (FR-02);
    defaultRetakePolicy() экспортирован
  \- dirty-tracking retake → вкладка «Настройки»
  \- RetakePane: Switch/NumberInput/Select/SegmentedControl/Banner из @universityrt/ui-kit;
    Select плагинов грузится с /api/tests/:id/available-eligibility-plugins
  \- тесты: 4 round-trip маппера + 5 UI пейна; фикстуры модели дополнены retakePolicy
  
  Проверено вживую: off/webtutor/suspend совпадают с эскизом, реальные имена
  плагинов из реестра, dirty-точка и save-round-trip (PUT→БД→reload) работают.

- **feat**(prd-6): убрать completionReportMode — тест всегда отдаёт статус (2026-06-04) [`c643f64d051aaf0ded4a1efcb5d725ce1138ca23`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c643f64d051aaf0ded4a1efcb5d725ce1138ca23)
  Это конструктор тестов: тест обязан оставлять вердикт passed/failed. Режим
  completed_neutral относится к информационным модулям без оценки, не к
  ретейк-гейту, в рантайме не реализован и к тому же обнуляет cooldown (WebTutor не
  ставит отметку «Пройден», по которой работает гейт). Не бизнес-требование —
  просочился из спеки PRD-6; удалён целиком.
  
  \- schema: убрано поле completionReportMode из retakePolicySchema
  \- export: test-json больше не кладёт его в TEST_DATA.retakePolicy
  \- tests: schema-prd6-retake / scorm-retake-acceptance без completionReportMode
  \- эскиз: снят SegmentedControl «Завершение попытки в LMS» + строки таблиц
  \- спека §4.1: режим завершения удалён; зафиксировано «тест всегда scored»;
    заодно убран устаревший пункт про превью даты (нет «последней попытки» в авторинге)

- **feat**(prd-6): блок-экран retake — системная страница шаблона (system.blocked) (2026-06-04) [`2ed460698721546fa3948e85df2af670a08105f3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2ed460698721546fa3948e85df2af670a08105f3)
  Страница блокировки рендерится из layout system.blocked шаблона оформления,
  а не из встроенного HTML: гейт грузит шаблон (ensureTemplate, без обращения
  к SCORM — NFR-01/02 сохраняются), заполняет retake.* через path-DSL и
  переключает ветки cooldown/error/default. Встроенная стена осталась только
  как фолбэк (нет белого экрана, если у шаблона нет system.blocked).
  
  \- layouts system.blocked (default/corporate/minimal): ветки data-retake-branch
    \+ слоты data-path="retake.cooldownPeriodDays|availableDateHuman"
  \- jsdom-тест: рендер из шаблона, ветки, фолбэк, отсутствие Initialize
  \- эскиз: заметки про источник блок-экрана (шаблон, не встроенный)

- **feat**(prd-6): scorm-player мок под боевой ClientBridge-флоу (2026-06-04) [`17ea57e4085f3b9b0d1f30b1165fece9e36d28c0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/17ea57e4085f3b9b0d1f30b1165fece9e36d28c0)
  /view_doc.html отдаёт SECID, /services/ClientBridgeService — SOAP get_metadata с датой
  прохождения (или без блока «пройден» =&gt; допуск); object_id подсовывается в launch-URL

- **feat**(prd-6): боевой источник webtutor_cooldown — WebTutor ClientBridge get_metadata (2026-06-04) [`012dbf90308ceb60f5698998959293d0b67ca886`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/012dbf90308ceb60f5698998959293d0b67ca886)
  Вскрыто и подтверждено на живом портале РТ (DevTools в работающем SCORM, ≥2 модуля):
  дата прохождения отдаётся НЕ через SCORM и НЕ в HTML, а SOAP-вызовом get_metadata
  карточки курса. Гейт (same-origin, на сессии учащегося):
  \- resolveObjectId из location/referrer/top/parent (object_id виден из вложенного SCO-фрейма)
  \- GET /view_doc.html?object_id=&lt;oid&gt; → выскрести SECID [A-F0-9]{32} (сессионный)
  \- POST /services/ClientBridgeService (SOAP get_metadata, form_url + wsparams) → XAML
  \- парс «Курс был пройден ДД.ММ.ГГГГ» → cooldown-решение
  Подтверждено: same-origin (HTTP 200, не CORS), object_id+form_url общие, дата сходится
  с «Пройти до» самого WebTutor. Реестр-конфиг webtutor_clientbridge_rt; endpoint/marker/
  form_url — админ-тюнинг (NFR-03). Юниты parse + parity TS↔JS-порт.
  Оговорка: маркер — тема РТ (admin-config); «пройден»=passed (completed-not-passed не ловит)

- **feat**(prd-6): WebTutor-мок в scorm-player для локальной проверки retake-гейта (2026-06-04) [`00a1be6754512a4401fb7863fe4ebd978bae161d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/00a1be6754512a4401fb7863fe4ebd978bae161d)
  \- /api/mock-webtutor (GET/POST) + перехват /pp/Ext5/extjs_json_collection_data.html:
    возвращает синтетическую запись по заданной дате последнего прохождения (или пусто =&gt; allowed)
  \- мок-бар в UI: дата/статус/прогресс + «Применить + перезапустить» (перезагружает iframe →
    гейт переоценивается) и «Очистить» (нет прошлой попытки)

- **feat**(prd-6): рантайм-гейт в SCORM-пакете (до Initialize) + блок-экран (2026-06-04) [`f85551b5a2db9330172ffbc0ae560486e688fae1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f85551b5a2db9330172ffbc0ae560486e688fae1)
  \- JS-порты engine.js/plugins.js (parity с shared/eligibility/*); gate.js — рантайм:
    isGated, context, адаптеры webtutor(fetch)/suspend(best-effort), failPolicy+timeout(5s),
    рендер блок-экрана и старт-оболочки «Начать курс»
  \- boot: тело вынесено в runCourse(); gated-тесты сначала проходят гейт ДО SCORM.Initialize
    (NFR-01/02 — blocked =&gt; блок-экран без init/cmi; allowed =&gt; «Начать курс» -&gt; runCourse)
  \- экспорт test.retakePolicy + test.retakePlugin (runtimeEntry+config из реестра) условно (FR-02)
  \- бандлер index.ts подключает eligibility-файлы; стили блок-экрана в styles.css
  \- golden-parity eligibility-engine-port

- **feat**(prd-6): проброс retake_policy_json в API + read-only реестр плагинов (2026-06-04) [`4f944fff47b5c4fc625185005ef04a03e01c0594`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4f944fff47b5c4fc625185005ef04a03e01c0594)
  \- POST/PUT /api/tests принимают и сохраняют retakePolicyJson (валидация retakePolicySchema);
    GET отдаёт поле через loadFullTest; save сохраняет настройку (omitted =&gt; без изменений)
  \- GET /api/tests/:id/available-eligibility-plugins — активные плагины+конфиги для пикера автора

- **feat**(prd-6): ядро retake gate — схема, eligibility-движок, плагины, реестр (2026-06-04) [`0cb7750e3d8640bbe943d3504b724ede185b2469`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0cb7750e3d8640bbe943d3504b724ede185b2469)
  \- tests.retake_policy_json (jsonb) + zod retakePolicySchema (алиас cooldownDays, 1-3650,
    failPolicy/completionReportMode/gateMode); миграция 013 (применена)
  \- shared/eligibility: контракт EligibilityPlugin/Context/Result, движок (календарный
    cooldown, нормализация вердикта, failPolicy, retake-state, оркестрация evaluate)
  \- чистая логика плагинов: webtutor_cooldown (фильтр записей + выбор последней попытки)
    и suspend_data_cooldown (best-effort); сидированный реестр плагинов/конфигов
  \- юниты: eligibility-engine (25), eligibility-plugins, schema-prd6-retake

- **feat**(prd-11): UI квот выдачи в строке темы (вкладка «Состав») (2026-06-04) [`aa06e53762b8c5d70f82c711511b06a9e951557e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/aa06e53762b8c5d70f82c711511b06a9e951557e)
  \- QuotaEditor в tb-topic-row: свич «Квоты по подтемам» + инлайн-таблица страт
    (Select реальных тегов темы, count, режим Ровно/Не менее на тег, удаление/добавление)
  \- Σ-валидация (FR-05): превышение drawCount → error-баннер + блок сохранения (validation.ts)
  \- нехватка по тегу (FR-06): колонка «Доступно» + warning-теги; свич disabled если у темы нет тегов
  \- маппер: (де)сериализация drawBlueprintJson (пустые страты → null = равномерная выдача)
  \- tb-components.css: tb-quota-toggle/block/sum; тесты маппера и валидации
  \- верифицировано вживую: round-trip save/reload, error/warning/off-состояния

- **feat**(prd-11): UI тегирования — chip-инпут тегов в редакторе вопроса (2026-06-04) [`28dd5d19e0c9fd7b1ca8a593ab2b40b31dd765d3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/28dd5d19e0c9fd7b1ca8a593ab2b40b31dd765d3)
  \- TagsInput (shadcn): чипы-removable, ввод+Enter, автодополнение из тегов банка, «Создать тег»
  \- встроен в questions.tsx (state/reset/populate/submit + suggestions); i18n-ключи tags*

- **feat**(prd-11): API тегирования вопросов — приём и нормализация tags (2026-06-04) [`82f25f3e96f81a07a6b5cd319d12aae111fe2ed0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/82f25f3e96f81a07a6b5cd319d12aae111fe2ed0)
  \- POST/PUT /api/questions принимают tags; нормализация через normalizeTags (PUT: omitted =&gt; без изменений)
  \- storage.createQuestion/duplicateQuestion/duplicateTopicWithQuestions сохраняют tags (дубликат больше не теряет теги)

- **feat**(prd-11): упрощение блюпринта квот + нормализация тегов (2026-06-04) [`f01277f0b4db9b303fdc107fa4b74b6e752dc24b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f01277f0b4db9b303fdc107fa4b74b6e752dc24b)
  \- strata:[{tag,count,mode}] без modeGranularity/топик-mode; режим — на тег (effMode = s.mode ?? exact)
  \- shared/tags.ts: normalizeTag/tagKey/normalizeTags (свободные метки, trim+collapse, дедуп регистронезависимо, cap 50)
  \- движок blueprint.ts + JS-порт: сопоставление по нормализованному tagKey (регистронезависимо)
  \- миграция 012 заменяет stale CHECK (валидирует только strata-массив)
  \- тесты: draw-blueprint(+port), schema-prd11, tags; обновлён scorm-builders

- **feat**(prd-11): Стадии 2-3 — стратифицированная выдача (движок + порт) + экспорт (2026-06-04) [`8dcad2f1b3431ebbc2bd3aa265cd4c183d06fd6a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8dcad2f1b3431ebbc2bd3aa265cd4c183d06fd6a)
  Стадия 2: авторитетный shared/draw/blueprint.ts drawSection (страты по порядку, общий used для дедупа FR-04, остаток без exact-тегов FR-03a, warning при нехватке FR-06, shuffle инъектируется). JS-порт в server/scorm/assets/app.js (drawSection, в generateVariant с логом warnings). Сервер routes/attempts.ts использует авторитетный TS напрямую. Без блюпринта = равномерная выдача (FR-02).
  
  Стадия 3: buildTestJson переносит drawBlueprint в рантайм-секцию УСЛОВНО (пакеты без квот бит-идентичны). Тесты: draw-blueprint (unit) + draw-blueprint-port (golden-parity TS&lt;-&gt;порт) + export в scorm-builders. check/build зелёные, vitest 1837.

- **feat**(prd-11): Стадия 1 — схема квот выдачи (draw_blueprint_json) + миграция 011 (2026-06-04) [`3b3db0d7d281d0b44ac932a13d21e9bb1e3d16c1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3b3db0d7d281d0b44ac932a13d21e9bb1e3d16c1)
  Квоты выдачи по тегам (стратифицированная выдача, ось ДОСТАВКИ). Опциональная колонка test_sections.draw_blueprint_json; отсутствие = равномерная выдача, старые тесты не затронуты (FR-02).
  
  migrations/011: колонка + CHECK на modeGranularity/mode (идемпотентно, применена к dev). shared/schema.ts: drawBlueprintSchema/drawStratumSchema (modeGranularity uniform|per_tag, mode exact|min, strata&gt;=1) + колонка drawBlueprintJson + insert-схема. storage (_insertSections test-settings + legacy createTest/updateTest) + SectionPayload: проброс. routes/tests.ts sectionBodySchema: валидация Σ count &lt;= drawCount (FR-05). tests/schema-prd11-blueprint.test.ts. check/build зелёные, vitest 1813.

- **feat**(prd-10): Стадия 6 — golden против эталона РТК (PRD-10 завершён) (2026-06-04) [`f8349413000e0d2cadc3aa9ce8667adf31833f53`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f8349413000e0d2cadc3aa9ce8667adf31833f53)
  Движок shared/scoring/engine с РТК-стандартными конфигами воспроизводит ВСЕ 63 балла внешнего pandas-обработчика (key_NEW_15-08-25.xlsx + report_processed_pandas_*), 0 расхождений, по всем 4 типам (веса/exact/множ./сопост.). Правила взяты из docs/references/main/main.py.
  
  tests/rtk-golden.test.ts: прогон 63 реальных ответов через scoreAnswer, сверка score+sMax. tests/fixtures/rtk-golden.json: фикстура (генератор rtk-golden.gen.py, самовалидируется правилом РТК = 63 балла отчёта, воспроизводима).
  
  PRD-10 ЗАВЕРШЁН (Стадии 0-6): градуированная оценка end-to-end (UI-&gt;экспорт-&gt;рантайм-&gt;Σs-&gt;порог раздела-&gt;сертификация), подтверждено golden. vitest 1803, check/build зелёные.

- **feat**(prd-10): Стадия 5 — порог раздела на Σs + сертификация (2026-06-04) [`84fbca2d55d58ef1ecc893a2680b68d74be32127`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/84fbca2d55d58ef1ecc893a2680b68d74be32127)
  FR-10 (Вариант А): count-правило прохождения раздела/теста переведено на Σ s (earnedPoints) в рантайме checkPassRuleWithPartial — 3 call-site (per-section/per-topic/overall). percent-правило уже считало на Σ s. Для exact+1 балл/вопрос Σ s = число верных → старые тесты не затронуты. Тест tests/scoring-pass-rule.test.ts.
  
  FR-11 (сертификация) уже работала инфраструктурой PRD-2: result-variable controls_status (success/completion) переопределяет cmi.success_status/completion_status; автор задаёт countPassed()==countTopics() (DSL) — кода не потребовалось. check/build зелёные, vitest 1738.

- **feat**(prd-10): Стадия 4 — UI редактора цены ответа (ScoringBuilder) (2026-06-04) [`da0b61e7dca888d92b73a69729bc8afef6b5338c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/da0b61e7dca888d92b73a69729bc8afef6b5338c)
  Секция «Цена ответа» в редакторе вопроса: client/src/pages/author/scoring-builder.tsx (ScoringBuilder + buildScoringJson) + интеграция в questions.tsx (state/init/reset/save). Режимы: single -&gt; exact/weighted (таблица весов); multiple/matching/ranking -&gt; exact/tiered (конструктор ступеней: условия c/x op число|T/P/N, И-условие, балл, Иначе-&gt;0); реактивный sMax. На shadcn — консистентно с формой (которая не на ui-kit); эскиз = спека раскладки/поведения.
  
  Playwright: рендер всех режимов + end-to-end save/round-trip (scoring_json пишется и читается, токен T сохраняется). npm run check/build зелёные, vitest 1733. Отложено: preview-модалка балла, drag-reorder ступеней, серверный check-answer.ts (веб-попытки) остаётся бинарным.

- **feat**(prd-10): Стадия 3 — экспорт цены ответа в SCORM-пакет (2026-06-04) [`b383b3ccc25408db70f53c632916ed21748e9163`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b383b3ccc25408db70f53c632916ed21748e9163)
  buildTestJson (test-json.ts) переносит scoring в рантайм-вопрос — оба блока (секции + адаптив), УСЛОВНО (только когда задано → пакеты без цены ответа бит-идентичны, FR-02). Так q.scoring доходит до checkAnswer в resultsPage.js и градуированный путь активен end-to-end. Тесты в scorm-builders.test.ts (присутствует когда задан / отсутствует когда нет). npm run check чист, vitest 1733 зелёных, build ок.

- **feat**(prd-10): Стадия 2 — рантайм градуированной оценки (движок + порт) (2026-06-04) [`e085d1c605e1c1c527d4c1577b5164bc45b74edf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e085d1c605e1c1c527d4c1577b5164bc45b74edf)
  Авторитетный shared/scoring/engine.ts: scoreAnswer -&gt; {score, sMax, ratio} (exact/weighted/tiered, счётчики c/x/T/P/N, неаддитивная ступенчатая таблица, пол max(0,·), clamp). Рукописный JS-порт server/scorm/template/app/scoring/engine.js, вшит в index.ts перед resultsPage.js. checkAnswer в resultsPage.js делегирует ScoringEngine.scoreAnswer(...).ratio (guard typeof + fallback на старое 0/1). Агрегация Σ s уже готова (earnedPoints += points × ratio).
  
  Тесты: scoring-engine.test.ts (юниты, РТК-неаддитивность А,Б,В-&gt;1) + scoring-engine-port.test.ts (golden-parity TS&lt;-&gt;порт, 29 сценариев). npm run check чист, vitest 1731 зелёный, build ок. Серверный check-answer.ts (веб-попытки) оставлен бинарным — отдельный шаг, не на пути РТК.

- **feat**(prd-10): Стадия 1 — схема цены ответа (scoring_json) + миграция 010 (2026-06-04) [`108e443dced0841a63a960b7563e5f390b872203`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/108e443dced0841a63a960b7563e5f390b872203)
  Градуированная оценка ответа (scoring-model §11). Отдельная nullable-колонка questions.scoring_json (ось правильности); отсутствие = exact, sMax=1 — старые тесты бит-идентичны (FR-02).
  
  migrations/010: колонка scoring_json + CHECK на kind (идемпотентно). shared/schema.ts: questionScoringSchema (union exact/weighted/tiered) + под-схемы condition/predicate/tier; колонка scoringJson; строгая валидация в insertQuestionSchema. storage.ts + routes/questions.ts: проброс + валидация scoringJson (FR-13). tests/schema-prd10-scoring.test.ts: unit-тесты. docs: PRD-10 §4/§10/§12 + статус, HANDOFF — OQ-1/OQ-2/OQ-3 закрыты (OQ-1 отдельная колонка; OQ-3 без durable-id, индексно как PRD-5).

- **feat**(tooling): scorm-player inspector — live scale.*/result.* + LMS traffic (2026-06-03) [`6a6aa773e7773a3d342304dac34ca966b8a58ec2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6a6aa773e7773a3d342304dac34ca966b8a58ec2)
  Extend the local SCORM acceptance player (scripts/scorm-player.mjs) with a
  real-time inspector so a reviewer can watch, during a played attempt:
  
  \- «Шкалы» / «Показатели»: live recompute of scale.* and result.* as the learner
    answers — the parent player reads the same-origin iframe globals (TEST_DATA,
    computeTestScales, computeTestResultVariables, calculateResults, state) and
    calls the package's OWN functions, so values match the runtime exactly; a
    «→ LMS» column shows what was actually published (from cmi.interactions).
  \- «SCORM ↔ LMS»: structured RTE call log (method, args, → LMS response, error
    code) replacing the flat log, plus a cmi.interactions.* table (id /
    learner_response / description) and the latest suspend_data.custom snapshot
    (scale.*/result.* + errors). The RTE shim now records a structured traffic
    log and exposes window.__scorm (getCmi/getTraffic/subscribe/restore/reset).
  
  generate-sample-scorm.ts gains a compact PRD-5/PRD-2 scoring fixture (2 scales
  sym/asym with bands, 24 option measurements, 3 result variables incl. a
  countScales/scaleById formula) so `npm run scorm:sample` exercises the inspector.
  
  Verified end-to-end in the browser: live scales climb on answer, formulas
  resolve live, and at finish the scale_*/scale_*_level/var_* pseudo-interactions
  \+ suspend_data.custom appear with values matching the live recompute. tsc green.

- **feat**(prd-5): B4b — «Вклады вопросов» contribution matrix (2026-06-03) [`a9bbc9a6a85c00352fd5c6f726649d3da257c38f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a9bbc9a6a85c00352fd5c6f726649d3da257c38f)
  Complete the «Шкалы» tab with the per-question contribution matrix (PRD-5 B4b):
  
  \- model/mappers: QuestionMeasurementModel + measurements in the editor model;
    buildMeasurementsFromApi resolves each row's scaleId to the stable scale key
    (the editor references scales by key, never the uuid) and drops orphans.
  \- data layer (scales-api): loadContributionQuestions enumerates each test
    question's answer units with correctness — options (single/multiple), directed
    pairs (matching, "l:r") and item@position placements (ranking, "item:pos");
    saveMeasurements diffs per question and PUTs only the changed ones, resolving
    scaleKey→scaleId from the persisted scales (skips rows whose scale is unsaved).
  \- use-test-editor: measurements reconcile runs after scales on save (re-reading
    scale ids when a scale was just created) and marks the «Шкалы» tab dirty.
  \- UI (scales-section): ContributionsPane — coverage banner, per-question cards
    (status dot ok/warn, type + contributed-scale chips), and the unit×scale grid
    with numeric cells (empty=no row, 0/negatives valid), correct-unit marker and
    per-type hints. Deleting a scale cascades its contributions out of the draft.
    The «Список шкал» card subtitle now shows coverage («N вопросов») + warn dot.
  \- tests: 11 unit tests for the measurements mapper, saveMeasurements diff and
    loadContributionQuestions unit/correctness enumeration.
  
  Verified against docs/wireframes/approved/prd2-prd5-scoring-tabs.html
  (s-contributions) at 1440px: single/multiple/matching grids, live coverage
  (banner/dots/chips/list subtitle), create→save (PUT /measurements with key→id)
  → reload round-trip, and scale-delete cascade — all exercised end-to-end. npm run
  check and npm run build green; full vitest run 1608 passed.

- **feat**(prd-5): B4a — «Шкалы» tab, scale list + bands + preview (2026-06-02) [`4044eb0e40440ad5d9958d841d9db077cd57af43`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4044eb0e40440ad5d9958d841d9db077cd57af43)
  Add the «Шкалы» editor tab (PRD-5 B4a, scale-definitions side):
  
  \- model/mappers: ScaleModel + ScaleBandModel in the editor model;
    buildScalesFromApi lifts bands from config_json; emptyEditorModel seeds [].
  \- validation: scale key grammar/uniqueness + bands numeric/ascending/non-overlap
    (validateScales), routed to the «Шкалы» tab.
  \- data layer: scales-api.ts — saveScales diff-on-save orchestrator (delete-first,
    POST/PUT by index, bands → config_json with empty-label fallback), previewScales
    client and loadScalePreviewContext (demo-answer units derived from measurements).
  \- use-test-editor: «scales» tab key, dirty-diff, tab statuses, and save reconcile
    alongside result variables.
  \- UI: sections/scales-section.tsx — rail (Список шкал / Вклады вопросов), scale
    cards (key/label/aggregation/source/recalc/LMS target), bands editor, show-to-
    learner switch, empty/error states, and the calculation-preview modal wired to
    the shared engine. Source «Другие шкалы» is disabled (composite deferred); the
    contributions matrix is a placeholder pending B4b.
  \- tests: 21 unit tests for the mapper/validation/save/preview logic; existing
    section fixtures get scales: [].
  
  Verified against docs/wireframes/approved/prd2-prd5-scoring-tabs.html (s-scales,
  s-scales-empty, s-preview-calc, s-scale-error) at 1440px in light and dark via
  Playwright; create→reload→delete round-trip exercised end-to-end. npm run check
  and npm run build green; full vitest run 1597 passed.

- **feat**(prd-5): B5 wiring + export + preview — scale.* in runtime/LMS (2026-06-02) [`fbb496a8eb4a7f94ecffef669cce750dd64aa1d6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fbb496a8eb4a7f94ecffef669cce750dd64aa1d6)
  Complete the PRD-5 scale track's runtime/export side:
  
  \- export: test.scales[] (bands lifted from config_json) + flattened
    test.measurements[] with scaleId resolved to the stable key; loadFullTest
    and the SCORM export now load scales + measurements via storage.
  \- runtime (resultsPage.js): compute scale.* once per attempt BEFORE result.*
    so result-variable formulas can read scaleById()/countScales(); fill the
    DSL context.scales; emit scale_{key} (+ scale_{key}_level for banded scales)
    pseudo-interactions in all three finish paths for scorm_target interaction/both.
  \- suspend_data: persist scaleValues + scaleErrors per attempt (deterministic
    recompute on recovery, NFR-04).
  \- preview: POST /api/tests/:id/scales/preview runs the shared engine over demo
    answers and returns { values, errors }.
  \- tests: export scales/measurements assertions, preview endpoint cases, and
    getScales/getQuestionMeasurements added to the loadFullTest/export route mocks.

- **feat**(prd-5): B5 — scale engine runtime port + golden parity + assembly (2026-06-02) [`923db2b48ee30abc189a9a156be64383bbb9f58f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/923db2b48ee30abc189a9a156be64383bbb9f58f)
  Plain-JS twin of shared/scales/engine (server/scorm/template/app/scales/engine.js), joined before resultsPage.js so scale.* is available to the result-variable step. 5-scenario golden parity test (TS ≡ port) covering sum/percent±direction/bands/multiple/matching/ranking/weighted_avg. tsc + build green; the engine is bundled but not yet invoked (wiring into resultsPage.js follows, mirroring the A3→A7 split).

- **feat**(prd-5): B5 core — scale computation engine + fixture tests (2026-06-02) [`cfa436ad329a068f3c5c45453cc3e85dce7d2686`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cfa436ad329a068f3c5c45453cc3e85dce7d2686)
  Pure scale engine (shared/scales/engine.ts): select active contributions per the learner's answer (index-based source_key — option index, matching left:right, ranking item:pos, or whole-question), aggregate (sum/avg/weighted_avg/max/min) into raw, normalize (none/percent with positive|inverse direction over the computed raw range), and apply interpretation bands to the raw value for level/label. Authoritative TS implementation; a runtime JS port follows. 13 fixture tests including the MBI EE (percent) / AD (inverse) / banded scenarios and all four answer types.

- **feat**(prd-5): B3 — scales + measurements API routes (2026-06-02) [`cd94af94f93a18d9d47c55c55478a23faa43a3c1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cd94af94f93a18d9d47c55c55478a23faa43a3c1)
  server/routes/scales.ts (mounted at /api/tests): scales GET/POST/PUT/DELETE/reorder with key-uniqueness (422), and measurements GET (whole-test matrix) + PUT /:questionId (replace one question's rows, test/question id from the path). 16 route integration tests (auth, 404, zod 422, key conflict, CRUD/reorder/upsert lifecycle).

- **feat**(prd-5): B2 — scales + measurements storage + real scale keys in validator (2026-06-02) [`538b595e81c563d3fa6e3c93345fa644ee086db3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/538b595e81c563d3fa6e3c93345fa644ee086db3)
  Storage CRUD/reorder for scales (mirrors result-variable methods) plus getQuestionMeasurements (matrix), getQuestionMeasurementsByQuestion (per-card) and upsertQuestionMeasurements (transactional delete-then-insert replacing one question's contributions). validateResultVariableFormula now resolves scaleById/countScales against the test's real scale keys (was an empty set in Этап A). Refined the measurement zod: valueJson is a number and condition_json stays plain jsonb (reserved) to keep the insert type aligned with the table.

- **feat**(prd-5): B1 — scales + question_measurements schema (migration 009) (2026-06-02) [`29282abc831c6fa36c87cd8ebb52a360c3bbb16a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/29282abc831c6fa36c87cd8ebb52a360c3bbb16a)
  Add the two PRD-5 §9 tables. scales: test-scoped named aggregate (key/label/type/aggregation/normalization/direction/config_json/show_to_learner/scorm_target), UNIQUE(test_id,key). question_measurements: explicit per-unit contribution of a question into a scale (source_type question/option/matching_pair/ranking_position + source_key, value_json scalar, weight, reserved condition_json). FKs are varchar(36) to match the actual tests.id/questions.id primary keys (PRD-5 §9 said uuid — same correction as PRD-2 §8.1). Drizzle tables + zod insert schemas (key regex, label ≤120). Migration applied to the dev DB.

- **feat**(prd-2): A8 UI — Показатели editor tab (2026-06-02) [`3a4b8f418e5d394f8e189b95bb3ca27564fe2176`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3a4b8f418e5d394f8e189b95bb3ca27564fe2176)
  Register the metrics tab (EditorTabKey/TAB_ORDER/TAB_LABELS, tab routing, per-tab dirty diff) and persist result variables in the save mutation (PUT test, reconcile variables, refetch for ids). New result-variables-section.tsx per the approved wireframe: reorderable accordion cards (@dnd-kit), name/label/type/controls_status form, Конструктор/DSL formula editor with a visual builder and debounced live validation, LMS publication + show-to-learner, empty state. Section test fixtures updated for the new required model field.

- **feat**(prd-2): A8 data layer — result variables in the editor model (2026-06-02) [`5af1d4c478839077dd7049289fb3d938dcb74cca`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5af1d4c478839077dd7049289fb3d938dcb74cca)
  Add ResultVariableModel + resultVariables to TestEditorModel; map them in apiToEditorModel/emptyEditorModel; synchronous validation (name grammar/uniqueness, required label/formula, controls_status boolean-only + single controller); and result-variables-api with the diff-on-save orchestrator (deletes, then create/update in display order with index as sortOrder) and the live validate-formula wrapper.

- **feat**(prd-2): A8 — include result variables in GET /api/tests/:id (2026-06-02) [`26153e25da2355b4a1c621f773d42d318d2966c8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/26153e25da2355b4a1c621f773d42d318d2966c8)
  The editor loads result variables into its draft and reconciles them on save against this snapshot, so loadFullTest now returns them alongside sections and adaptive settings.

- **feat**(prd-2): A7 runtime — compute result.*, emit var_ interactions, controls_status (2026-06-02) [`ca7d8cc0915791c728c9aa73b2d5b40536205fc3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ca7d8cc0915791c728c9aa73b2d5b40536205fc3)
  Wire the tested computeResultVariables core into the SCORM runtime finish flow. After scoring, resultsPage.js builds the formula context (percent + per-topic results; tags/scales/sections resolve to neutral defaults for Этап A) and computes result.* once per attempt. A boolean controls_status=success variable overrides cmi.success_status; completion overrides cmi.completion_status after SCORM.finish. All three finish paths (adaptive/standard/lms-only) append var_{name} pseudo-interactions for variables published to the LMS (scorm_target interaction/both). suspendAttempts.js persists resultValues + formulaErrors per attempt so a recovered session restores the same values (NFR-04).

- **feat**(prd-2): A6 — export result variables into the SCORM package (2026-06-02) [`e0465131afdd4bcb9395044df821b9c19c7e9fb4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e0465131afdd4bcb9395044df821b9c19c7e9fb4)
  Load result variables at export and carry them into TEST_DATA: ExportData.resultVariables, a mapped test.resultVariables[] block in buildTestJson (id/name/label/type/formula/show_to_learner/scorm_target/controls_status/sort_order, ordered by sort_order), and the storage load + pass-through in the export route. The runtime (A7) consumes test.resultVariables to compute result.* via FormulaDSL. npm run check clean.

- **feat**(prd-2): A5 — result-variable API routes (2026-06-02) [`a64167d37dfc9f66553fbb160299f4f1c269ef9b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a64167d37dfc9f66553fbb160299f4f1c269ef9b)
  REST CRUD + reorder + live formula validation under /api/tests/:id/result-variables (PRD-2 §9): GET (auth), POST/PUT/DELETE/reorder/validate-formula (author). reorder and validate-formula are registered before /:varId so their literal segments aren't captured as ids. POST/PUT validate the body via insertResultVariableSchema, run the shared DSL validator (422 + validation payload on failure), and reject a second success/completion controller per test. Mounted in routes/index. npm run check clean.

- **feat**(prd-2): A4 — result-variable storage CRUD + reorder + formula validation (2026-06-02) [`208cbfdcfce92ac0a5a49b615dc0b35b0f9dd3a9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/208cbfdcfce92ac0a5a49b615dc0b35b0f9dd3a9)
  Add getResultVariables / create / update / delete / reorderResultVariables to IStorage + DatabaseStorage, modelled on the content_pages methods (test-scoped, uuid id, transactional reorder). validateResultVariableFormula builds the test's reference sets (topicIds from test_sections; priorVarNames = result vars with a smaller sort_order for the DAG rule) and delegates to the shared DSL validator; scaleById resolves to warnings while scales are unimplemented (Этап A). npm run check clean.

- **feat**(prd-2): A3 — runtime DSL port + golden parity (2026-06-02) [`3aced09ce67ddef41c61abcf1299d9c613839bbf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3aced09ce67ddef41c61abcf1299d9c613839bbf)
  Plain-JS twin of shared/formula for the un-bundled SCORM runtime (app/dsl/formula.js, exposes global FormulaDSL.evaluate), wired into joinJsParts before resultsPage.js. A golden corpus (tests/fixtures/formula-cases.json, 27 cases) runs both the authoritative TS evaluator and the port and asserts they agree, so the two cannot silently diverge (PRD-2 §12). 54 tests green, npm run check clean.

- **feat**(prd-2): A2 — result-variable formula DSL (parser/evaluator/validator) (2026-06-02) [`e67ae1f3d93a6da7a49674f62a705a9ddd24385b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e67ae1f3d93a6da7a49674f62a705a9ddd24385b)
  Shared DSL for result-variable formulas (PRD-2 §4.2-4.3), no eval/Function: tokenizer -&gt; recursive-descent parser (precedence OR&lt;AND&lt;comparison&lt;+-&lt;*/&lt;unary&lt;primary) -&gt; tree-walking evaluator (neutral defaults for missing keys, division-by-zero -&gt; 0) -&gt; static validator (return-type inference vs declared type; topic/section ref errors; scaleById warnings while scales are unavailable in Этап A; var() forward-only sort_order for DAG, §10.9; countScales band-level warnings). 27 unit tests green, npm run check clean.

- **feat**(prd-2): A1 — result_variables schema + migration 008 (2026-06-02) [`a6e3e32f644c945dede768f8209f2a6a53975d3e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a6e3e32f644c945dede768f8209f2a6a53975d3e)
  Этап A foundation. Add the test-scoped result_variables table (uuid id, varchar(36) test_id FK to tests — note PRD-2 §8.1 said uuid for test_id, corrected to match tests.id) with name-regex CHECK, type/scorm_target/controls_status enums, and two partial unique indexes (one success / one completion controller per test). Add questions.tags jsonb for aggregate formulas. Drizzle table + insertResultVariableSchema (name regex, label length) + types. npm run check clean.

- **feat**(scorm-templates): expose variant kind in preview manifests (2026-05-30) [`e01af6e037cc683b85c4fc892c16955e494e517a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e01af6e037cc683b85c4fc892c16955e494e517a)
  Tag built-in template variants with their `kind` (intro/info/summary) so the
  editor's variant.kind linking logic recognizes them. Add a `router.menu`
  card-menu variant to the default template for router_by_topics flow and bump
  its manifest version to 1.1.0.

- **feat**(prd-8): close — router lifecycle events (FR-18) + cross-PRD closure (2026-05-29) [`5ba2eb6510825b8e0594c2d5a2d9b20031f9a0d0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5ba2eb6510825b8e0594c2d5a2d9b20031f9a0d0)
  PRD-8 is closed by composition. Most of its acceptance criteria were
  delivered as PRD-7 (UI) and PRD-4 (runtime) sub-phases — see audit
  below. The remaining delta is FR-18 router lifecycle events; this
  commit lands them and marks PRD-8 done in ROADMAP + spec.
  
  Cross-PRD coverage (audit):
    \- FR-01/02/03/04/05/05a/06/06a/b/c — UI in PRD-7 (router_by_topics
      in basic-settings-section, Structure tab tree-branches G45,
      page-row-expand for router params, Settings→Сценарий fields).
    \- FR-07/8/9/10/11/12 — router runtime in PRD-4 phase 4c (router
      state machine, selectRouterTopic, returnFromTopic, completionPolicy
      \+ sectionUnlockRules in routerFlow.js).
    \- FR-13/14 — recovery + SCORM score on final result already covered
      by PRD-4 phase 4f + existing scoring path.
    \- FR-15/16/17 — template router-layout: default ships router.menu
      variant from PRD-7 G48; fallback when template lacks one.
  
  PRD-8 FR-18 (this commit) — router lifecycle events:
    \- router:shown — emitted from renderRouterPage with the current
      sections snapshot (topicId, name, required, status, unlocked) +
      a readyToFinish boolean.
    \- router:sectionSelected — emitted from selectRouterTopic.
    \- router:finalResultUnlocked — first time readyToFinish flips true
      during a render (idempotent via state._routerFinalUnlockedFired).
    \- router:finalResultOpened — emitted when finishRouter is invoked
      by the learner.
    \- Plumbing: thin emitRouterEvent helper around
      TestBuilder.template.emit (existing event bus from templateCore).
      Templates subscribe via TestBuilder.template.on(name, handler).
  
  Tests (tests/prd-4-acceptance.test.ts): +1 covering all four events
  present in the bundled app.js for the router_by_topics combo.
  
  ROADMAP.md:
    \- PRD-8 row in §0 main table → Закрыта 2026-05-29 with cross-PRD
      summary.
    \- PRD-8 row in §0.1 critical path → Закрыта.
    \- Header date kept at 2026-05-29; subtitle updated to «PRD-4 +
      PRD-8 закрыты — Storyline-MVP shippable».
  
  PRD-8 spec (section-router-flow.md):
    \- Status line → **Закрыт 2026-05-29** with cross-PRD pointer and
      acceptance test link.
  
  Verification: type-check clean; full vitest 1423 / 1424 (1 pre-existing
  DB-connectivity failure in tests/migration-prd7.test.ts, unchanged).
  
  Storyline-MVP is now shippable on the code level. Remaining work
  documented for follow-up: live-browser acceptance pass (Playwright +
  axe + LMS smoke) is a separate gate.

- **feat**(prd-4): phase 5 — golden acceptance tests + PRD-4 closure (2026-05-29) [`4d9d03842c70e7c5e2cb718e3e55d5f6849b65e5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4d9d03842c70e7c5e2cb718e3e55d5f6849b65e5)
  Phase 5 closes PRD-4 v1.1 in full. The new acceptance file exercises
  all 5 valid (mode × flowMode) combinations end-to-end through
  generateScormPackage; ROADMAP + PRD-4 spec marked Закрыт 2026-05-29.
  
  Acceptance (tests/prd-4-acceptance.test.ts, 18 tests):
    \- (standard, linear_flat)     — flowPolicy export, content-flow bundle
    \- (standard, linear_by_topics) — section.required default, content
                                      pages with kind
    \- (standard, router_by_topics) — router page export, RouterFlow bundle
    \- (adaptive, linear_by_topics) — adaptiveTopics levels strict gating,
                                      AdaptiveSession + adaptive-session
                                      marker bundle
    \- (adaptive, router_by_topics) — both router + adaptive bundles
    \- Cross-cutting: startSectionTimer / stopSectionTimer (Phase 4e),
                     restoreRouterSession / restore_router (Phase 4f)
                     bundled.
  
    Each combo packs a 2-topic / 2-question / level fixture via the
    REAL generateScormPackage pipeline, reads TEST_DATA out of the
    resulting zip, and asserts the expected runtime surface. Router
    packs include a kind=router content page; adaptive packs include
    per-topic levels with non-empty fields (PRD-4 v1.1 §3.1.2 strict
    gating). The fixture builder handles permutations of router page
    \+ adaptive settings so a single parameterised function covers all
    branches.
  
  ROADMAP.md:
    \- PRD-4 row in §0 main table → Закрыта 2026-05-29 with phase
      summary and cross-link to spec v1.1.
    \- PRD-4 row in §0.1 critical path → Закрыта; PRD-8 still depends.
    \- Header date bumped to 2026-05-29.
  
  PRD-4 spec (course-flow-sections.md):
    \- Status line updated to **Закрыт 2026-05-29** with phase summary
      and cross-link to the acceptance test file.
  
  Verification: type-check clean; full vitest 1422 / 1423 — 18 new
  PRD-4 acceptance tests + 1 pre-existing migration DB-connectivity
  failure unrelated to this change (tests/migration-prd7.test.ts
  needs a local PG).
  
  PRD-4 v1.1 ЗАКРЫТ. Next on the MVP critical path: PRD-8 (router-flow
  extension — extended router UI in editor's «Структура» tab; runtime
  is already in place).

- **feat**(prd-4): phase 4f — sectional state recovery for router_by_topics (2026-05-29) [`4b5438c7038362e1533041ef729767af597a6f52`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4b5438c7038362e1533041ef729767af597a6f52)
  PRD-4 v1.1 §3.2: a learner who closes the SCO mid-test in router mode
  can resume with all previously-completed topics still marked as
  «Пройдена». In-progress section work is dropped (the topic re-enters
  state 'notStarted' so the learner reruns it from scratch).
  
  Persistence contract (utils/scorm/sessionRecovery.js):
    \- saveCurrentSession now persists in router mode regardless of
      timer / adaptive guards (which previously short-circuited save).
      New fields on the stored session blob: flowMode,
      routerTopicStates, sectionResults, routerFinished.
    \- Section timer is intentionally NOT persisted — time passing while
      the SCO was closed is unrecoverable; re-entry to a timed section
      starts the timer fresh.
    \- Adaptive engine in-flight state still skipped (single-topic
      sessions inside router mode are dropped on save; the topic
      re-runs cleanly from the AdaptiveSession entry).
  
  determineRecovery (sessionRecovery.js):
    \- New action: 'restore_router' fires when the active flowPolicy.mode
      is router_by_topics AND a non-stale session exists for that mode.
    \- Non-router modes keep the existing 'restore' / 'show_last_attempt'
      / 'start_fresh' branches unchanged.
  
  restoreRouterSession (sessionRecovery.js, new):
    \- Re-applies routerTopicStates + sectionResults + routerFinished
      from the saved session onto state.
    \- Demotes any 'inProgress' topic back to 'notStarted' — the
      learner enters the router with completed topics marked
      «Пройдена» and the unfinished section ready to re-run.
  
  Bootstrap dispatch (bootstrap/main.js):
    \- New restore_router branch: restoreRouterSession → generateVariant
      → rebuildPageSequence → goToPageSequenceIndex(routerIdx). The
      router page becomes the active item, so render() routes to
      RouterFlow.renderRouterPage with the restored topic-state map.
    \- beforeunload save trigger broadened: any router-mode session
      (regardless of phase) gets saved, not just 'question' phase. This
      captures the routerTopicStates snapshot even when the learner
      closes the SCO while staring at the router page.
  
  Checkpoint on returnFromTopic (routerFlow.js):
    \- Each section completion explicitly invokes saveCurrentSession,
      so a SCO kill that escapes beforeunload (browser crash, process
      termination) still leaves the completed topic in the persistent
      store.
  
  What this does NOT cover:
    \- Linear (linear_by_topics) mid-section recovery — the section's
      answered questions can already be restored via the legacy
      no-timer path; only sectional checkpoints (per-topic completion
      markers) aren't tracked separately for linear mode. Linear
      flows complete linearly anyway — no point letting the learner
      skip topics on resume.
    \- Adaptive session in-flight state inside a topic chunk — the
      AdaptiveSession state is too engine-specific to rehydrate
      cleanly. Re-entering the topic restarts the adaptive session
      from the top.
  
  Verification: type-check clean; full vitest 1404 / 1405 (1 pre-existing
  DB-connectivity failure in tests/migration-prd7.test.ts, unrelated to
  this change — needs PG running locally).
  
  PRD-4 v1.1 Phase 4 complete: 4a-4f all landed. Next is Phase 5
  (golden tests for all 5 valid combos + manifest validation E2E).

- **feat**(prd-4): phase 4e — per-section time limits enforcement (2026-05-29) [`318ad208b789753044be3a4b62fa38ef37209737`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/318ad208b789753044be3a4b62fa38ef37209737)
  PRD-4 v1.1 §3.2: a section can carry its own time limit in addition to
  the test-wide one. Both timers run independently — section expiry
  truncates the current section; test expiry submits the whole attempt
  (legacy behaviour, unchanged).
  
  Exporter (server/scorm/builders/test-json.ts):
    \- sections[].timeLimitMinutes now in TEST_DATA, null when absent
      (= inherit_test: section uses the test timer, no extra timer).
    \- 2 new tests cover the export presence/default.
  
  Runtime — state (state.js):
    \- state.sectionTimer: {topicId, limitMinutes, remainingSeconds, expired,
      onExpire, intervalId} | null. Set by startSectionTimer, cleared by
      stopSectionTimer (or autonomously on expiry).
  
  Runtime — timer (timer/timer.js):
    \- New startSectionTimer(topicId, limitMinutes, onExpire) and
      stopSectionTimer(). Lives next to the existing initTimer/updateTimer
      (test-wide). updateTimerDisplay now also refreshes #section-timer-display
      when present, AND syncs onto TEST_DATA.section.current.timer.{remainingSeconds,
      displayText} so templates using data-path bindings see live values.
    \- Sub-60s threshold flips to the same red/bold style as the main timer.
  
  Runtime — linear flows (contentFlow.js):
    \- syncPhaseToCurrentPage now calls maybeUpdateSectionTimer(item) on
      every navigation step. Detects topic transitions by reading topicId
      from the current item (content.page.topicId, adaptive-session.topicId,
      or flatQuestions[questionIndex].topicId). Stops the current section
      timer when the topic changes, starts a new one when the new section
      has timeLimitMinutes set, no-op when the section inherits the test
      timer or carries no limit.
    \- On expiry, skipSectionFromCurrent(topicId) walks pageSequence past
      every item belonging to the expired topic and re-enters the next
      one via syncPhaseToCurrentPage. If the section was the last in the
      sequence the attempt submits normally.
  
  Runtime — router (routerFlow.js):
    \- selectRouterTopic now starts the section timer when the picked
      topic has timeLimitMinutes. On expiry the handler calls
      returnFromTopic (matches the router's "section is done" semantics —
      learner returns to the hub even if time ran out).
    \- returnFromTopic always calls stopSectionTimer first (safe when no
      timer is active).
  
  Adaptive interaction:
    \- When (adaptive, linear_by_topics) or (adaptive, router_by_topics)
      runs, the section timer wraps the adaptive session lifecycle the
      same way it wraps content+questions in standard mode. The adaptive
      engine itself does not need to know about the timer — it ticks on
      its own interval.
  
  Verification: type-check clean; full vitest 1405/1405 (+2 nets new for
  the section.timeLimitMinutes export tests). Runtime behaviour observable
  only at SCORM playback time — Phase 5 golden tests will exercise the
  timer paths end-to-end.
  
  Remaining: Phase 4f (recovery — persist section timer + currentRouterTopic
  across SCO reload) + Phase 5 (golden tests for all 5 valid combinations).

- **feat**(prd-4): phase 4d-iii — (adaptive, linear_by_topics) integration (2026-05-29) [`ecfaa042c95b188276c308b1bb556f84ef19c28b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ecfaa042c95b188276c308b1bb556f84ef19c28b)
  Closes the last combo of PRD-4 v1.1 §4.6. In adaptive + linear_by_topics
  mode the linear traversal now wraps each topic's adaptive session
  between its before_topic and after_topic content pages, matching the
  spec's «контент → вопросы (adaptive) → результат → контент» chain.
  
  contentFlow.rebuildPageSequence:
    \- New branch for `(adaptive, linear_by_topics)`. For each section in
      TEST_DATA.sections, emits:
        [before_topic content..., {kind: "adaptive-session", topicId},
         after_topic content...]
      Test-scope «after» content (with summary-boundary split into
      pre/post-results) follows the existing logic.
  
  contentFlow.syncPhaseToCurrentPage:
    \- Handles `kind === "adaptive-session"`: sets phase to
      "adaptive-session" and invokes maybeLaunchAdaptiveSession.
  
  contentFlow.maybeLaunchAdaptiveSession (new helper):
    \- Idempotent — re-renders during a running session don't restart it
      (state.adaptiveState non-null guard).
    \- Calls AdaptiveSession.runAdaptiveSession(topicId, onComplete). The
      callback freezes the topic's normalised result in
      state.sectionResults[topicId] (consistent with Phase 4b), then
      advancePageSequence to walk past the marker into after_topic.
    \- Defensive fallback when AdaptiveSession isn't loaded or returns
      false (Phase 1 L2/L3 strict gating should prevent this in
      production): logs a warning and advancePageSequence to skip.
  
  Result normalisation (adaptiveSession.js):
    \- New normalizeAdaptiveTopicResult: maps adaptive topicResults shape
      (achievedLevelIndex, levelsAttempted, levelPercent...) onto the
      standard section-result shape (topicId, percent, correct, total,
      passed) consumed by computeSectionResult / templates bound to
      section.current.result.*. Adaptive-specific fields preserved with
      an `isAdaptive: true` discriminator for templates that branch.
    \- maybeFinishSingleTopic now passes the normalised result to the
      caller's onComplete, ensuring both router and linear adaptive
      flows freeze the same shape into sectionResults.
  
  PRD-4 v1.1 §4.6 «passed» semantics:
    \- Adaptive section is considered «passed» when achievedLevelIndex
      is non-null (i.e. minimum level was confirmed). The «top level»
      variant lives in the preserved achievedLevelIndex/Name field for
      templates that want the stricter signal.
  
  What this closes:
    \- Phase 4d is now complete (i / ii / iii). All five valid (mode,
      flowMode) combinations have runtime support:
        (standard, linear_flat)        legacy, unchanged
        (standard, linear_by_topics)   linear topic chunks (4a/4b)
        (standard, router_by_topics)   router state machine (4c)
        (adaptive, linear_by_topics)   per-topic adaptive in linear flow  ←
        (adaptive, router_by_topics)   per-topic adaptive in router flow
  
  Verification: type-check clean; full vitest 1403/1403 (runtime changes
  observable only at SCORM-package playback time — covered E2E in Phase 5
  golden tests).
  
  Remaining PRD-4 phases: 4e (per-section time limits) + 4f (recovery)
  \+ 5 (golden tests).

- **feat**(prd-4): phase 4d-i/ii — single-topic adaptive + router integration (2026-05-29) [`36faab755dbfe06b5e2bb2028ea213478feba433`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/36faab755dbfe06b5e2bb2028ea213478feba433)
  Closes the (adaptive, router_by_topics) combo end-to-end. The router
  now launches a per-topic adaptive session when a card is clicked, and
  returns to the router page when the topic's adaptive flow signals
  completion (with achievedLevel frozen in sectionResults).
  
  New module (adaptiveSession.js):
    \- runAdaptiveSession(topicId, onComplete): thin wrapper that filters
      TEST_DATA.adaptiveTopics to one topic, calls the existing
      initAdaptiveTest with that single-topic input, and tags
      state.adaptiveState with `_scope: "single-topic"` + `_onComplete`.
      Reuses all level / threshold / transition logic from
      adaptive/adaptive.js.
    \- maybeFinishSingleTopic(): invoked from the adaptive engine when
      the multi-topic loop hits «all topics completed» (which in
      single-topic scope means OUR topic is done). Clears
      state.adaptiveState, extracts the topic result, fires the
      onComplete callback. Returns true to signal the caller (in
      adaptive/adaptive.js) to suppress legacy auto-submit.
  
  Engine hook (adaptive/adaptive.js · submitAdaptiveAnswer):
    \- On «all topics completed», calls AdaptiveSession.maybeFinishSingleTopic
      before falling back to legacy multi-topic finalization.
    \- When handled, sets `result.singleTopicHandled = true` so the caller
      (adaptiveRender) skips renderAdaptiveResults.
  
  Render guard (adaptiveRender.js):
    \- Both `submitAdaptiveAnswerAndShowFeedback` and
      `submitAdaptiveAnswerAndContinue` early-return when
      `result.singleTopicHandled` — state.adaptiveState is null by now
      and the caller's onComplete has already advanced the next phase.
  
  Bootstrap gate (bootstrap/main.js):
    \- Auto-init of the multi-topic adaptive engine now only fires for
      (adaptive, linear_flat) — the legacy mode kept for backward compat
      with packages saved before PRD-4 v1.1 validation. In sectional
      modes (linear_by_topics / router_by_topics) the engine waits for
      per-topic launches from routerFlow / contentFlow.
  
  Router integration (routerFlow.js · selectRouterTopic):
    \- When TEST_DATA.mode === 'adaptive' AND AdaptiveSession is loaded:
      runs the per-topic adaptive session via runAdaptiveSession.
      onComplete callback freezes the topic's adaptive result in
      state.sectionResults (mirrors Phase 4b for templates bound to
      section.current.result.*) and calls returnFromTopic to re-enter
      the router page.
    \- Failure-mode fallback: if runAdaptiveSession returns false
      (strict gating should prevent this — Phase 1 L2/L3 — but defends
      in depth), falls back to the standard topic chunk traversal with a
      console warning.
  
  Bundling (server/scorm/index.ts):
    \- adaptiveSession.js included in the runtime bundle right after
      adaptiveJs / adaptiveRenderJs.
  
  What's NOT in this commit:
    \- Phase 4d-iii: (adaptive, linear_by_topics) integration — needs
      contentFlow.rebuildPageSequence to replace per-topic question
      stretches with adaptive-session markers and advancePageSequence
      to launch the session on entry. Deferred follow-up.
    \- Per-section time limits (Phase 4e).
    \- Recovery for adaptive sessions (Phase 4f).
    \- Golden tests for all 5 valid (mode, flowMode) combos (Phase 5).
  
  Verification: type-check clean; full vitest 1403/1403 (runtime
  changes observable only at SCORM-package playback time).

- **feat**(prd-4): phase 4c-iv — routerCompletionPolicy + sectionUnlockRules (2026-05-29) [`bc94fb49078a82231011b5e7ffec3f5570f39dbf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/bc94fb49078a82231011b5e7ffec3f5570f39dbf)
  Closes Phase 4c (router runtime). The router now enforces
  completion-policy and unlock-rules per PRD-4 v1.1 §4.7.
  
  Exporter (server/scorm/builders/test-json.ts):
    \- flowPolicy.routerCompletionPolicy exported when mode=router_by_topics,
      defaults to 'all_required_completed' (softer rule: any achievedLevel
      counts as «pass» for navigation). Explicit
      'all_required_passed' propagates through.
    \- flowPolicy.sectionUnlockRules propagates when set (router mode only).
      Non-router modes drop these fields to keep TEST_DATA shape predictable.
    \- section.required is now exported (defaults to true when absent on the
      DB row). Optional sections never block the routerCompletionPolicy
      «all_required_*» calculation.
  
  Runtime (server/scorm/template/app/routerFlow.js):
    \- isSectionUnlocked(section): evaluates per-section unlock rule. Modes:
      'always' (default), 'after_sections_completed', 'after_sections_passed'.
      Unknown rules fail-open (don't block the learner).
    \- isRouterReadyToFinish(): evaluates routerCompletionPolicy. Optional
      sections (required=false) never block. Strict 'all_required_passed'
      requires sectionResults[id].passed === true; soft
      'all_required_completed' just requires state=='completed'.
    \- renderRouterPage: locked cards get a router-topic-card--locked
      modifier + 'Недоступна' status label + disabled state. Optional
      sections show a small '(необязательная)' hint. «Завершить» button
      now appears only when isRouterReadyToFinish() returns true.
  
  Styles (server/scorm/assets/styles.css):
    \- .router-topic-card--locked (opacity .5, not-allowed cursor)
    \- .router-topic-card__optional (small grey hint)
  
  Tests (tests/scorm-builders.test.ts): 6 new
    \- Default routerCompletionPolicy when not set
    \- Explicit 'all_required_passed' propagation
    \- sectionUnlockRules propagation in router mode
    \- Non-router modes don't carry router-specific fields
    \- section.required defaults true when absent
    \- section.required=false preserved
  
  Verification: type-check clean; full vitest 1403/1403 (+6 nets new).
  
  Phase 4c is now complete (i + ii/iii + iv). Standard router_by_topics
  flow works end-to-end: test-before → router → topic chunks (with
  unlock gating) → router → «Завершить» (when completionPolicy met) →
  test-after → results.

- **feat**(prd-4): phase 4c-ii/iii — router state machine + topic cards UI (2026-05-29) [`6a8d18f1d926e77a04da1cf37a39f3a405ec133f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6a8d18f1d926e77a04da1cf37a39f3a405ec133f)
  Implements the router_by_topics runtime state machine and minimum
  viable topic-card UI. Together these turn the existing isRouter
  pageSequence marker (4c-i) into a working learner flow:
    test-before → router → topic A → router → topic B → router →
    «Завершить» → test-after → results.
  
  State additions (state.js):
    \- routerTopicStates: { [topicId]: 'notStarted' | 'inProgress' | 'completed' }
    \- currentRouterTopic: topicId | null (null while router is on screen)
    \- routerFinished: bool, set when the learner clicks «Завершить»
  
  New module (routerFlow.js, ~220 lines):
    \- isRouterMode()              — flowPolicy.mode === 'router_by_topics'
    \- buildTopicChunk(topicId)    — [before_topic content, questions, after_topic content]
    \- buildPostRouterSequence()   — test-scope «after» content (summary boundary
                                    splits pre/post-results as in existing logic)
    \- renderRouterPage(page)      — overlays clickable topic cards on top of
                                    the standard renderContentPage output.
                                    Cards carry status class (notStarted/
                                    inProgress/completed) and a disabled state
                                    for completed topics. «Завершить» appears
                                    when all sections are completed (4c-iv adds
                                    proper completionPolicy gating).
    \- selectRouterTopic(topicId)  — picks topic, swaps pageSequence to chunk
    \- returnFromTopic()           — fires from advancePageSequence at chunk end;
                                    marks topic completed; re-renders router
    \- finishRouter()              — switches to the post-router test-end seq
                                    (or submit if there is no test-after content)
  
  Integration:
    \- contentFlow.rebuildPageSequence: when flowMode === 'router_by_topics',
      sequence stops after test-before content (router page becomes the hub).
      Topic chunks built on demand by RouterFlow.
    \- contentFlow.advancePageSequence: at sequence end in router mode, calls
      RouterFlow.returnFromTopic instead of submit() (when on a topic chunk).
    \- contentFlow.syncPhaseToCurrentPage: sets state.phase = 'router' for
      isRouter items so mainRender routes to RouterFlow.renderRouterPage.
    \- mainRender.render: routes 'router' phase + isRouter items to
      RouterFlow.renderRouterPage; falls back to renderContentPage otherwise.
  
  Packaging (server/scorm/index.ts):
    \- routerFlow.js included in the runtime bundle (idempotent: it's a no-op
      when flowPolicy.mode is anything but router_by_topics).
  
  Styles (server/scorm/assets/styles.css):
    \- .router-topic-cards grid layout
    \- .router-topic-card with hover state + completed/inProgress modifiers
    \- .router-finish button hook for the «Завершить» CTA
  
  What's NOT in this commit (still on the Phase 4c-iv todo):
    \- routerCompletionPolicy enforcement (currently «Завершить» requires
      all sections completed; will be gated by all_required_passed vs
      all_required_completed once 4c-iv lands).
    \- sectionUnlockRules (topic-B locked until topic-A passed, etc).
    \- Adaptive sessions inside topic chunks (Phase 4d).
    \- Per-section time limits (Phase 4e).
    \- Recovery / resume (Phase 4f).
  
  Verification: type-check clean; full vitest 1397/1397 (runtime
  changes are observable only at SCORM-package playback time; will be
  covered end-to-end by Phase 5 golden tests).

- **feat**(prd-4): phase 4c-i — export page.kind + mark router items in pageSequence (2026-05-29) [`15be6648777df14371301295d4b812ec87650474`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/15be6648777df14371301295d4b812ec87650474)
  Establishes the contract surface for the router-by-topics runtime work
  that lands across phases 4c-ii / 4c-iii / 4c-iv. This commit does NOT
  add router state-machine behavior — that requires the next slice.
  
  Exporter (server/scorm/builders/test-json.ts):
    \- Each entry in TEST_DATA.contentPages now carries `kind`
      (intro/info/summary/router/questions). Legacy `type` field stays
      for backward compat. Runtime distinguishes router pages by
      `page.kind === "router"`.
  
  Runtime (server/scorm/template/app/contentFlow.js):
    \- rebuildPageSequence reads flowMode from TEST_DATA.flowPolicy.mode.
    \- When mode === "router_by_topics", router content pages (kind ===
      "router") get an `isRouter: true` marker on their pageSequence
      item. Future router state-machine slices (4c-ii) attach to this
      marker: detect, intercept navigation, render topic cards, handle
      topic clicks, return to router after topic completion.
  
  Tests (tests/scorm-export.test.ts): 1 new
    \- Two-page fixture: one router page, one intro page. Confirms the
      exported `kind` field round-trips for both. Existing content-pages
      test updated with a `kind: "intro"` field on its fixture.
  
  Verification: type-check clean; full vitest 1397/1397 (+1 net new).
  
  Next slice (4c-ii): router state machine — topicStates map,
  click → traverse topic chunk, return → router. Estimate ~2-3h.

- **feat**(prd-4): phase 4b — section result frozen before after_topic content (2026-05-29) [`e551d4de916aacff24c9df1363a10123266d3957`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e551d4de916aacff24c9df1363a10123266d3957)
  PRD-4 v1.1 §4.4: «после вопросов раздела Core рассчитывает sectionResult;
  после расчёта Core показывает контентные страницы after_topic, включая
  content.summary». Templates can now bind `data-path="section.current.
  result.percent"` (or `.passed`, `.correct`, etc.) on after_topic content
  pages and see the freshly-computed section result.
  
  Runtime changes:
  
    state.js
      \+ state.sectionResults = {}  // map topicId → result snapshot
  
    render/resultsPage.js
      \+ computeSectionResult(topicId): pure function that scopes the
        existing calculateResults() math to a single topic. Reuses
        checkAnswer + checkPassRuleWithPartial, caches the result in
        state.sectionResults[topicId] on first call, returns cached
        thereafter.
  
    contentFlow.js (syncPhaseToCurrentPage):
      \+ maybeExposeSectionResult(item): when the current pageSequence
        item is a content page with position='after_topic' and a
        topicId, calls computeSectionResult(topicId) and writes the
        result onto TEST_DATA.section.current = {topicId, topicName,
        result}. The renderPathOnlyDsl call inside renderContentPage
        then picks it up via the existing data-path mechanism — no
        template changes needed.
  
  The hook fires for every after_topic content page; computeSectionResult
  short-circuits on the cached result, so re-renders (navigation back,
  re-mount) are cheap.
  
  No unit tests yet — runtime JS isn't exercised under node/vitest;
  will be covered end-to-end by Phase 5 golden SCORM tests for all 5
  valid (mode, flowMode) combinations.
  
  Verification: type-check clean; full vitest 1396/1396 (no new
  test code in this commit; runtime change is observable only at
  SCORM-package playback time).

- **feat**(prd-4): phase 4a — export flowPolicy to TEST_DATA + section-order shuffle (2026-05-29) [`0dca2f2890222b0862e248356c9b8e33840014ce`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0dca2f2890222b0862e248356c9b8e33840014ce)
  First runtime slice. Adds the flowPolicy contract to the SCORM
  package's TEST_DATA payload, and teaches generateVariant() to respect
  the section boundary when flowMode is sectional.
  
  Exporter (server/scorm/builders/test-json.ts):
    \- test.flowPolicy = { mode: 'linear_flat' | 'linear_by_topics' |
      'router_by_topics' } added to TEST_DATA. Missing or unknown
      flowPolicyJson.mode coerces to 'linear_flat' per FR-40, so legacy
      tests without an explicit flowPolicy continue to behave as before.
  
  Runtime (server/scorm/assets/app.js · generateVariant):
    \- Reads TEST_DATA.flowPolicy.mode. For 'linear_flat' (default) the
      pre-existing global shuffle stays. For 'linear_by_topics' /
      'router_by_topics' the global shuffle is skipped so questions stay
      grouped by topic in author order; questions inside a section still
      shuffle at selection time (unchanged).
  
  Tests (tests/scorm-builders.test.ts): 4 new
    \- Default → linear_flat when flowPolicyJson missing.
    \- Explicit linear_by_topics export.
    \- Explicit router_by_topics export.
    \- Unknown / future mode (section_graph) coerced to linear_flat.
  
  Verification: type-check clean; full vitest 1396/1396 (+4 nets new).
  
  Next slice (4b): compute sectionResult BEFORE the after_topic summary
  content page so templates can bind to TEST_DATA.section.current.result.

- **feat**(prd-4): phase 3 — L1 UI guard for adaptive+linear_flat (2026-05-29) [`57745748ea7f0bf2ddc938849e377b6ac215c897`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/57745748ea7f0bf2ddc938849e377b6ac215c897)
  Disables the linear_flat option in the flowMode Select when the test
  is in adaptive mode, and surfaces a warning Banner if the invalid
  (adaptive, linear_flat) state ever slips through (mode-switch race,
  direct model patch, etc).
  
  Changes (basic-settings-section.tsx):
    \- SelectOption.disabled flag set on linear_flat entry when
      model.mode === 'adaptive'. Label augmented with «— недоступно в
      адаптивном режиме» so screen-reader users hear the constraint.
    \- Below the Select, a Banner tone='warning' shows for the precise
      (adaptive, linear_flat) state with a recovery hint pointing at the
      two supported alternatives.
  
  Tests (basic-settings-section.test.tsx): 3 new
    \- Opens the Select dropdown and asserts the augmented «недоступно»
      label appears in the listbox.
    \- Warning Banner renders for invalid (adaptive, linear_flat) combo.
    \- Loops over all 5 valid combos and asserts the warning is absent.
  
  Verification: type-check clean; full vitest 1392/1392 (+3 nets new).

- **feat**(prd-4): phase 2 — L4 mapper auto-fix for legacy (adaptive, linear_flat) (2026-05-29) [`a8039b36da5d70ac6d1ece9c1719c229bbd5eafe`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a8039b36da5d70ac6d1ece9c1719c229bbd5eafe)
  The PRD-4 v1.1 validator (phase 1) blocks (adaptive, linear_flat) on
  both client (L2) and server (L3). DB rows persisted before that
  validation existed may still carry the now-invalid combo. The mapper
  silently coerces flowMode to `linear_by_topics` when reading them so
  the editor can open such tests without immediately failing on the
  first save attempt.
  
  Changes:
    \- apiToEditorModel detects `mode='adaptive' && flowMode='linear_flat'`
      after readFlowModeFromApi and rewrites flowMode to
      `linear_by_topics`. Comment notes the v1.1 rationale.
    \- test-editor.mappers.test.ts: 4 new tests covering the auto-fix
      branch and confirming the other three valid combos pass through
      untouched.
  
  Verification: type-check clean; full vitest 1389/1389 (+4 nets new).

- **feat**(prd-4): phase 1 — L2+L3 flow-policy validation (mode×flowMode) (2026-05-29) [`7fbcabefc0a412f589777d29fc37c91e50ded352`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7fbcabefc0a412f589777d29fc37c91e50ded352)
  PRD-4 v1.1 §3.1.2 compatibility guards. Two contract rules enforced at
  both client-side validation layer and server-side service layer.
  
  Rules:
    1. (adaptive, linear_flat) is blocked — deferred to a future «Flat
       adaptive» PRD. Validation issues 'adaptive_flat_unsupported' on
       flowMode field.
    2. Strict adaptive gating — every section in test.sections[] with
       mode='adaptive' must have a matching adaptive topic with at least
       one configured level. Mixed mode (some topics adaptive, some not)
       is forbidden per PRD-4 v1.1 clarification 2026-05-28. Issues
       'adaptive_section_no_levels'.
  
  Client (L2):
    \- test-editor.validation.ts: rules added next to existing adaptive
      FR-17 validations. The pre-existing FR-17b warning that fired only
      for enabled topics with empty levels is replaced by the stricter
      PRD-4 v1.1 error (every section regardless of enabled flag).
      FR-17b kept as a soft 'recommend ≥2 levels' warning when levels=1.
    \- test-editor.validation.test.ts: 2 existing tests updated to expect
      new error (instead of warning); 2 new tests cover the (adaptive,
      linear_flat) blocked branch and the 5 valid combinations.
  
  Server (L3):
    \- New flow-policy-validator.ts: pure function mirroring client rules,
      operates on TestPayload + sections + adaptiveSettings.
    \- test-settings.ts: validateFlowPolicy() called from create() and
      save() before DB write; FlowPolicyValidationError thrown on
      violations.
    \- routes/tests.ts: catches FlowPolicyValidationError → HTTP 422 with
      {error: 'flow_policy_invalid', violations: [...]}. Mirrors the
      existing 422 shape for required_fields_missing.
    \- tests/services/flow-policy-validator.test.ts: 10 unit tests.
    \- tests/services/test-settings.test.ts: 4 existing tests updated to
      use a valid (adaptive, linear_by_topics) pair (those tests had
      used the now-invalid linear_flat default).
  
  Verification: type-check clean; full vitest 1385/1385 (+12 nets new
  from this phase). Adds 1 test file (flow-policy-validator.test.ts);
  total 52 files. Next phase: L4 mapper auto-fix for legacy DB data.

- **feat**(design): close S12-G4 — all param types (FR-31a) (2026-05-28) [`4ac3db02b1d31c9a9955004f13bdab56ecfcaeba`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4ac3db02b1d31c9a9955004f13bdab56ecfcaeba)
  ParamRow now renders the full set of param types declared in
  DesignParamType; the old «Тип "X" поддерживается в следующем шаге
  (медиатека)» fallback only fires for genuinely-unknown types.
  
  Type system (use-design-settings):
    \- DesignParamType extended: + multiselect, url, file, downloadLink
      (image/asset/number were in the enum but unrendered).
    \- TemplateParam gains optional `accept`/`maxSizeKb` (media validation)
      and `min`/`max`/`step` (number bounds).
    \- New MediaParamValue envelope { url, name, mime?, size?, mediaId?,
      label? } — shared serialisation for the four media-typed kinds.
  
  Controls (design-section ParamRow):
    \- number     → NumberInput with min/max/step pass-through.
    \- url        → Input type="url" + suffix IconButton "Открыть" that
                   launches the URL in a new tab when it parses as http(s).
    \- multiselect → DS Combobox&lt;string&gt; in multiple mode, chips rendered
                   by the component itself.
    \- image/asset/file/downloadLink → shared MediaParamRow:
        DS Button label per kind ("Загрузить изображение" / "Выбрать из
        медиатеки" / "Загрузить файл" / "Добавить файл"),
        hidden &lt;input type="file"&gt; with `accept` filter (image defaults to
        png/jpeg/svg/webp), client-side size guard (image 512 KB, others
        5 MB). Upload via POST /api/media/upload (existing multer disk
        pipeline in server/routes.ts:91), response mapped to a
        MediaParamValue. Chip shows the uploaded filename with a × remove
        button. Errors surface inline as a small Banner.
  
  CSS: .design-media-row + .design-media-chip/__ico/__name added in
  tb-components.css.
  
  Tests: 5 new in design-section.test.tsx (NumberInput row, url with
  type="url", multiselect no-longer-unsupported, image upload-button
  with accept filter, downloadLink descriptor). One existing test
  updated: image params no longer render the «unsupported» banner —
  now the media-row wrapper is asserted instead.
  
  Full suite 1373/1373 (51 files); type-check clean.
  
  S12 status after this slice: G1/G2/G3/G4/G5/G6 ALL CLOSED. S13.8
  (cleanup + acceptance) is the only sub-phase left for PRD-7.

- **feat**(design): close S12-G3 — template gallery modal (FR-33) (2026-05-28) [`fed035e42ca6744b7247504bae150c7d8082c6ac`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fed035e42ca6744b7247504bae150c7d8082c6ac)
  «Заменить шаблон» now opens a real DS gallery modal instead of the
  window.alert placeholder. Lists all active templates, supports search,
  pre-selects the current one, and confirms a switch when the test has
  dirty design params.
  
  Hook (use-design-settings):
    \- New generic setTemplate(id) action: patches the draft to
      { templateId: id, params: {} }. applyDefaultTemplate (S12-G6) is now
      a thin wrapper over it.
  
  UI (template-gallery-modal.tsx, new file):
    \- ModalDialog size=xl, fetches /api/templates lazily via React Query.
    \- Search filter (case-insensitive, label+description). Empty-state.
    \- Grid of cards: thumb sketch + name + description + Tag chips
      (Встроенный, v{version}). Current template gets a tone=accent
      «Текущий» chip with a dedicated data-testid.
    \- Footer info line «Выбран: …» + Cancel + Apply (disabled when
      selected === current).
    \- Dirty-params confirm: when draft.params has entries, surfaces a
      warning Banner and switches Apply to destructive variant labelled
      «Заменить и сбросить параметры».
  
  Wiring (design-section):
    \- «Заменить шаблон» button → setGalleryOpen(true).
    \- TemplateIncompatibleBanner (S12-G6) «Выбрать шаблон» action also
      opens the gallery, replacing the earlier alert placeholder.
  
  CSS: .tpl-gallery-search/-grid/-card/-foot/__* in tb-components.css.
  
  Tests: 3 new in design-section.test (cards render with current chip,
  search filter narrows the list, apply disabled when same-as-current).
  Full suite 1368/1368 (51 files); type-check clean.
  
  S12 status after this slice: G1/G2/G3/G5/G6 closed; G4 (param types
  image/asset/file/...) remains open.

- **feat**(editor): close S13.7 — drawer chrome polish (G1/G2/G5/G16/G30/G31) (2026-05-28) [`cea5bd9a8ed319fa26429a4de61b92b6af766744`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cea5bd9a8ed319fa26429a4de61b92b6af766744)
  Six drawer-level gaps closed inline; G3/G15 (adaptive banner, mode-change
  banner) landed earlier in this PRD-7 cycle.
  
  G1 — Saving overlay:
    \- .ou-drawer__body gets the .tb-saving-host hook unconditionally.
    \- Inner content wrapped in &lt;div inert&gt; when combinedSaving (native attr).
    \- .tb-saving-overlay renders only while saving — backdrop (88% opacity),
      Loader2 spinner with .spin keyframe (new in tb-components.css), text.
    \- role=status + aria-live=polite for screen readers.
  
  G2 — Per-field changes-popover diff:
    \- useTestEditor exposes the previously-private `snapshot`.
    \- ChangesPopover computes diff via collectChangesByTab over 9 top-level
      Settings fields (title/description/mode/flowMode/runtime.*/webhook/
      telemetry). Empty old/maxAttempts/etc rendered as human labels («не
      задан», «без ограничения», «Да/Нет»).
    \- Group title shows the count («N изменение/изменения/изменений»). Each
      item renders «label · old → new». Fallback line when only structural
      changes (sections etc) are dirty.
    \- Total counter in popover header switches to total field-count when
      diffs are computed; falls back to tab-count when no per-field info.
  
  G5 — Conflict diff-table:
    \- New ConflictDiffTable sub-component lazy-fetches /api/tests/:id on
      mount, parses via apiToEditorModel, and renders Поле / На сервере /
      Ваши изменения as a &lt;table&gt; over 8 high-signal fields.
    \- Loading/error states inside the table. ConflictDialog promoted to
      size=l so the table + three actions fit without overflow.
    \- .tb-conflict-diff CSS added in tb-components.css.
  
  G16 — Add-page modal search:
    \- AddPageModal mirrors ReplaceVariantModal: case-insensitive filter
      over label+description, empty-state «Ничего не найдено», hidden
      when there are ≤3 options (small templates don't need filtering).
  
  G30 — Close-confirm chips:
    \- CloseConfirmDialog now lists each dirty editor tab as a neutral
      outline Tag chip below the modal body. Per-tab data-testid for tests.
    \- .tb-close-confirm-chips flex layout added in tb-components.css.
  
  G31 — Close-confirm inline error banner:
    \- When hasErrors && errorFieldCount &gt; 0, the close-confirm renders
      a Banner tone=error with a count line and «Перейти к первой
      ошибке» action that closes the confirm and calls goToError on
      the first validation error. ru pluralisation in the count.
  
  Tests: 4 new (chips visible after dirty + close; error banner appears
  when validation fails; saving overlay absent while idle; per-field diff
  shows up in changes-popover). Full suite 1365/1365 (51 files). Type-
  check clean. Spec S13.7 marked closed with sub-gap status.

- **feat**(design): close S12-G6 — wf-template-incompatible banner + recovery (2026-05-28) [`18cf7a0192f30098c3fd0485a10c93ab2493af43`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/18cf7a0192f30098c3fd0485a10c93ab2493af43)
  When the persisted templateId no longer resolves to an active template
  (404 on /api/templates/:id), the design tab now shows the wireframe's
  incompatible banner with two recovery actions, instead of falling into
  the generic ErrorNotice.
  
  Hook changes (use-design-settings):
    \- New derived flag `templateMissing` — true when the design query has
      settled with data AND the template query has settled with no data
      AND the persisted templateId is non-empty. Distinguishes "template
      deleted" from "design fetch errored" (5xx, network), which still
      surface via the existing `error` field.
    \- New action `applyDefaultTemplate()` — patches the draft to
      `{ templateId: "default", params: {} }`; the Drawer footer's primary
      Save picks it up via the existing isDirty plumbing.
  
  UI changes (design-section):
    \- `templateMissing` takes priority over `error` in the render switch.
    \- When set, the active rail is forced to "template", and the other
      rail buttons (branding/layout/progress) get `disabled` +
      `aria-disabled` + onClick={undefined}. The "Шаблон" rail button
      gets a `.status-dot.error` with aria-label.
    \- New `TemplateIncompatibleBanner` component renders the DS Banner
      tone="error" with two actions: "Выбрать шаблон" (placeholder alert
      pending S12-G3 gallery) and "Применить «Стандартный»" → calls
      `applyDefaultTemplate`. Title "Шаблон недоступен", description
      quotes the failing templateId so the author sees what was missing.
  
  Tests: 3 new (incompatible banner with both actions; status-dot + disabled
  rail; applyDefault click survives). Full suite 1361/1361 (51 files).
  Type-check clean. S12-G6 marked CLOSED in the spec; status-dot at the
  test-editor tab level deferred to S13.7 with a spec note.

- **feat**(structure): close S13.6 — variant-replace search + diff-block (G28/G29) (2026-05-28) [`6fbc3482f3aea2578dc8a06adc09ec3e51ff35d1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6fbc3482f3aea2578dc8a06adc09ec3e51ff35d1)
  ReplaceVariantModal now matches wireframe prd7-variant-replace.html: a
  search input above the variant list, a current-variant chip with
  disabled selection, and a warning diff-block listing the placeholder
  values that will be lost on confirm.
  
  G28 — Variant search + diff-block:
    \- New variant-search input wired to a case-insensitive filter over
      label + description. Empty-state when no match. Hidden when there
      is only one variant for the page kind (no point filtering).
    \- VariantList extended with an optional isCurrent flag — current
      variant gets the wireframe is-current class, aria-disabled,
      tabIndex -1, no onClick, and a meta-tag «Текущий».
    \- Lost-placeholders diff computed by comparing placeholder keys
      (PRD-1 §4.3.3: name = contract between variants of same kind).
      Only placeholders with non-empty current values are listed —
      truly-empty fields don't warn the author about anything.
    \- describePlaceholderValue: strips HTML tags from richText values,
      JSON-stringifies objects/arrays, truncates to 60 chars.
  
  G29 — s-replace-no-fields:
    When the selected variant declares zero placeholders AND it differs
    from current, the diff-block uses the «Текущие настройки страницы
    будут потеряны» heading + «У нового варианта нет редактируемых полей»
    meta, per wireframe lines 263-317.
  
  CSS:
    \- .variant-list__item.is-current + .variant-list__meta(__tag)
    \- .variant-search + .variant-search__icon/__input/__empty
    \- .diff-block + .diff-block__group/__title/__list/__item/__field-*
    All in tb-components.css; ports from the wireframe inline styles.
  
  Tests: 5 new (search filter, current chip, lost-fields diff, no-fields
  diff variant, no diff on initial open). Full suite 1358/1358 (51 files).
  Type-check clean. Spec marks G28/G29 closed.

- **feat**(settings): close S13.2 + S13.3 — feedback section + per-topic limits (G7/G8/G9/G10) (2026-05-28) [`ca4149bde1a20949272192c0f3b4697e4b2d3ec8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ca4149bde1a20949272192c0f3b4697e4b2d3ec8)
  All four critical Settings-tab gaps closed in one slice; both phases live in
  the same file so it makes sense to ship them together.
  
  S13.2-G7 — «Общая обратная связь теста» card in Основное pane:
    \- New TestFeedbackTrigger wraps FeedbackEditorModal with full rich-text +
      links + PDF assets (no hideAssets like adaptive variants). Round-trips
      model.basic.feedback / feedbackLinks / feedbackAssets.
    \- Preview button shows snippet + meta line «N ссылок · M PDF».
    \- Wrapped in DS Card variant=outlined per wireframe 710-839.
  
  S13.2-G8 — Move «Показывать правильные ответы» to Основное:
    Switch relocated from LimitsPane to BasicPane, inside the new feedback
    card after an &lt;hr&gt; separator. data-testid preserved so other call sites
    keep working. The Limits-pane test was deleted; a new test in Основное-
    pane covers the new location.
  
  S13.3-G9 — Per-topic time-limit table:
    New PerTopicLimitsBlock at the bottom of LimitsPane. Derives switch state
    from `sections.some(s =&gt; s.timeLimit.source !== "inherit_test")` — no
    separate boolean in the model. Table renders one row per section with a
    small NumberInput (suffix=минут, placeholder="Без ограничения"). Typing
    N&gt;0 sets `{source:"custom", minutes:N}`, clearing/0 sets `{source:"none"}`
    (per-topic unlimited). Turning the switch OFF mass-resets every section
    to `{source:"inherit_test"}`.
  
  S13.3-G10 — No-topics state (linear_flat):
    When `sections.length === 0` PerTopicLimitsBlock renders a small info
    Banner instead of the switch/table — "Индивидуальные лимиты доступны
    после добавления тем во вкладке Состав". Replaces the standalone
    `s-limits-no-topics` wireframe state with a single conditional.
  
  Tests: 7 new (1 feedback-card + 1 switch + 5 per-topic), 1 deleted (old
  showCorrectAnswers location). Full suite 1353/1353 (51 files), type-check
  clean.

- **feat**(structure): close S13.4 — page-preview iframe + sanitize-warning + row-menu (G17/G18) (2026-05-28) [`f33ea1c0bb07b3cee553779c40bf8ec32f46a7bf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f33ea1c0bb07b3cee553779c40bf8ec32f46a7bf)
  Completes the two remaining S13.4 items by composing existing primitives;
  G17a (replace-variant in author row-menu) landed in e2201f4.
  
  G17b - Per-page preview iframe (FR-44):
    \- New backend route: GET /api/tests/:id/content-pages/:pageId/preview-page.
      Reads the content_page from the DB, loads the test's design-template
      preview.html, and injects an override script that defers one frame past
      the standalone bootstrap, then calls renderContentPage(page, contentTemplates)
      directly with the page's saved values. Design.params from designSettingsJson
      are applied on top of manifest.params[].default and re-init TestBuilder so
      branding (colours/fonts) matches what the learner will see.
    \- New helper: server/scorm/preview-embed.ts factors out the embed CSS and
      encodeJsonForScript / injectIntoPreview mechanics. Both template-preview
      (S12-G2) and the new page-preview consume it - no more duplicated CSS.
    \- New PagePreviewModal mirrors TemplatePreviewModal: size='xl' ModalDialog
      with a sandboxed iframe pointing at the new route. The modal title shows
      the page title; description clarifies that progress is not persisted.
    \- Row-menu now includes Preview on both author rows (next to Replace-Variant
      and Delete) and system rows (next to Replace-Variant). New testIds:
      structure-page-preview-${id} / ${testId}-preview.
  
  G18 - Sanitize-warning banner:
    \- html-sanitizer.ts gains sanitizeHtmlWithDiagnostics and
      sanitizeValuesWithDiagnostics that return both cleaned value and a list
      of SanitizeRemoval records (kind: tag/attribute/uri, label, count).
      on*-handlers reported per concrete attribute name (onclick, onmouseover, ...).
    \- PUT /api/tests/:id/content-pages/:pageId returns sanitizeDiagnostics as a
      sibling field on the response when at least one placeholder was stripped.
    \- useContentPages tracks diagnostics keyed by pageId; a clean re-save with
      no removals clears the entry, dismiss button clears it manually.
    \- PageEditForm renders a .validation-banner--warning at the top of the
      expand listing exactly which tag/attribute/URI was stripped from which
      placeholder (label resolved via variant.placeholders), with a dismiss X.
      CSS modifier --warning added in tb-components.css.
  
  Type-check clean; full vitest 1346/1346 (51 files). S13.4 closed (G17/G18/G23/G25).

- **feat**(structure): partial S13.4 — close G23/G25/G17a (Structure-tab parity) (2026-05-28) [`e2201f44906330a420f5ddbc801c925621b58681`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e2201f44906330a420f5ddbc801c925621b58681)
  Three contained S13.4 items landed; G17 page-preview modal (FR-44) and G18
  sanitize-warning deferred — see spec for scope notes.
  
  G23 — Remove dead `mixed` FlowMode enum:
    \- FlowMode union drops `mixed` (test-editor.types.ts)
    \- FlowSettings drops `mixed` branch (test-editor.types.ts)
    \- isFlowMode guard + buildFlowSettingsFromApi switch (test-editor.mappers.ts)
    \- FLOW_LABEL.mixed removed (start-pages-section.tsx)
    \- TestListFlowMode + isFlowMode guard (tests-list.types.ts, tests-list.tsx)
    No test references "mixed" — clean removal, no test updates needed.
  
  G25 — Template marker for unedited intro/summary rows:
    Heuristic: kind in {intro, summary} AND every valuesJson.values entry is
    null/undefined/empty-string -&gt; render .page-row--template with inline
    tpl-page-marker span instead of .page-row--system. Pure cosmetic — saving
    any placeholder value flips back to system styling. Adds data-from-template
    attr for regression tests. CSS already existed in tb-components.css.
  
  G17a — Replace-variant in author-row menu:
    AuthorPageRow receives onReplaceVariant from ZoneHandlers; row-menu shows
    the new item (above Delete) only when the active template offers more
    than one variant for the page's kind. Reuses the existing
    ReplaceVariantModal — no new modal code.
  
  Type-check clean; full vitest 1346/1346 (51 files). G17 preview split
  to S13.4b: needs a scope decision between full iframe runtime (3-4h, touches
  all 4 built-in preview.html) and a lightweight DS-styled stub (~1.5h).

- **feat**(editor): close S13.1 quick wins — remove window.prompt / window.alert (G32/G39/G40) (2026-05-28) [`87c4858f2d510d28235f9a0ad50ac6164cf43044`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/87c4858f2d510d28235f9a0ad50ac6164cf43044)
  Three production-visible UX violations replaced with DS-native components.
  Business explicitly rejected window-level dialogs in PRD-7 review.
  
  G32 — Move-to-folder picker (tests-list row-menu):
    Replace window.prompt('ID папки для перемещения') with MoveFolderPickModal:
    size='s' ModalDialog, radio-list of folders + 'Корень (без папки)' option,
    current folder pre-selected with Tag 'Текущая', 'Переместить' disabled
    while selected === current (no-op guard).
  
  G39 — Link-insert sub-modal (RTE in FeedbackEditorModal):
    Replace window.prompt('URL ссылки') with a nested size='s' ModalDialog
    rendered alongside the outer modal (React.Fragment wrapper).
      \- On open: captures the current Selection range from the contenteditable
        so the modal can restore it after submit.
      \- Inputs: URL (autofocus, type='url') + 'Текст ссылки' (prefilled with
        selected text if any).
      \- On submit: restores selection; if collapsed, inserts a fresh &lt;a&gt; at
        cursor with the typed text (or URL as fallback); else execCommand
        ('createLink', url) to wrap the existing selection.
      \- 'Вставить' disabled until URL is non-empty.
  
  G40 — Oversize file banner (RTE PDF upload):
    Replace window.alert('Файл(ы) превышают 5 MB…') with an in-modal
    Banner tone='warning' that lists the rejected file names. Dismissible.
  
  Tests:
    \- tests-list.test: new describe 'move-to-folder (S13.1-G32)' asserts
      window.prompt is NOT called and the modal opens with the correct
      pre-selection / disabled-submit state.
    \- feedback-editor-modal.test: 'RTE createLink' describe rewritten as
      'RTE link-insert modal' (3 tests covering open / submit-disabled /
      cancel-no-insert) — old prompt-mocking tests removed. Oversize test
      asserts no window.alert and that the banner contains the file name.
  
  Net diff: +1 test (28 -&gt; 30 in the two affected files; full suite 1346/1346).
  Type-check clean. Spec S13.1 marked closed.

- **feat**(editor): G19 drawer-level read-only footer — single <<Закрыть>> when published (2026-05-28) [`637fa6b512f847ec658981bd9394ba6072e2bf6d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/637fa6b512f847ec658981bd9394ba6072e2bf6d)
  When test.status === 'published' the drawer footer renders a single ghost
  &lt;&lt;Закрыть&gt;&gt; button per wireframe s-readonly (prd7-structure-linear-by-topics.html
  lines 847-849). Save / Cancel / &lt;&lt;Показать изменения&gt;&gt; are hidden; close does
  not require confirm in clean state. requestClose remains as a guard against
  silent edit loss in case Composition/Settings/Design tabs (which do not yet
  propagate readOnly) were dirtied — that broader propagation is a follow-up.
  
  Adds data-testid='test-editor-foot' with data-state in {readonly,dirty,default}
  to anchor regression tests. Two new tests cover the readonly footer markup and
  the no-confirm close path. Spec G19 marked closed.

- **feat**(structure): G19 read-only — Structure-tab visual closeout (2026-05-28) [`dca9ba775b2ab229c054a1ba7cf936258d94ec33`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dca9ba775b2ab229c054a1ba7cf936258d94ec33)
  When test.status === "published" the Structure tab now renders without
  authoring affordances per prd7-structure-linear-by-topics.html s-readonly:
  
    \* @dnd-kit useSortable hooks pass `disabled: true`, so grips become
      no-op; CSS .page-row--readonly / .topic-block--readonly dim the grip
      to opacity 0.2 with cursor:default + pointer-events:none.
    \* AuthorPageGroup / AfterTestZone hide insert-rows entirely.
    \* AuthorPageRow .page-actions menu (Удалить) is omitted.
    \* SystemPageRow «Сменить вариант» becomes disabled (canSwitch flips
      to false), since the active template is locked.
    \* PageEditForm fields wrap in &lt;fieldset disabled&gt; and the action bar
      collapses to a single «Закрыть» button (was Отмена + Сохранить).
  
  readOnly is plumbed from the drawer through StructureSection props
  into ZoneHandlers so every nested row component reads from a single
  source of truth. Drawer-level footer change (Save/Cancel → «Закрыть»)
  is out of scope here — the wireframe puts it on the drawer itself,
  which is a follow-up.

- **feat**(structure): close PRD-7 S13.5 router-mode gaps (G20/G21/G27/G45/G46/G47/G48) (2026-05-28) [`4b5742b779b286934718c02ea4c8b3ea2dcff875`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4b5742b779b286934718c02ea4c8b3ea2dcff875)
  7 of 9 router-mode gaps from S13.5 closed; G19 (read-only) and G22
  (mapping-flow) remain as follow-ups.
  
  G48 — router content_page never materialized for router_by_topics tests
    \* built-in default manifest now declares kind:router variant ("router.menu")
    \* defaultTemplateManifestSchema superRefine now requires all 4 system
      kinds (intro/summary/router/questions) instead of just questions
    \* GET /api/tests/:id invokes idempotent testSettingsService.reconcileExisting()
      on load, healing seed-data tests with missing system rows
    \* migration 006: one-shot SQL backfill for existing tests (intro/summary
      singletons; router for router_by_topics; questions per topic / flat)
      — idempotent NOT EXISTS guards
  
  G47 — topic order in Structure was implementation-defined
    \* test_sections.sort_order column (migration 007 + backfill via
      ROW_NUMBER per test partition); getTestSections ORDER BY sort_order;
      _insertSections writes sortOrder = array index
    \* @dnd-kit topic-drag wired in StructureSection (linear-by-topics and
      router_by_topics), using a dedicated `topic:` id prefix so the shared
      DndContext routes topic vs page drags correctly
    \* .topic-grip rendered when updateModel is plumbed; count format
      «N вопросов» (was «N из M вопросов»)
  
  G45 / G20 — router-row + topics inside one dashed container with connectors
    \* new InsideTestZone component wraps router-row and topics in
      .inside-test (label «Внутри теста») + .tree-branches &gt; .tree-branch
    \* CSS pseudo-elements draw the ├─/└─ tree-connectors via DS tokens
  
  G46 — flow-mode label
    \* FLOW_LABEL.router_by_topics → «Через страницу-маршрутизатор»; basic
      settings select option synced; prd7-editor-settings-tab.html wireframe
      \+ matching test expectations
  
  G21 — fallback indicator on system rows
    \* SystemPageRow shows yellow «Из стандартного шаблона» tag in
      .page-row__meta when active template lacks variants of this kind
  
  G27 — inline validation on system rows
    \* SystemPageRow becomes expandable when its variant has placeholders;
      PageEditForm renders .validation-banner with missing-field labels;
      row gets page-row--error (existing severity scheme); hasStructureErrors
      now covers all kinds so the tab status-dot reflects system-row state

- **feat**(design-tab): close S12-G2 — template preview modal (FR-30) (2026-05-28) [`d1ebe3140fb40d1680c8cda3971307f5ce97791c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d1ebe3140fb40d1680c8cda3971307f5ce97791c)
  Replaces window.alert("Предпросмотр шаблона будет доступен в следующем шаге.")
  on the «Предпросмотр» icon button with a real wireframe-driven ModalDialog
  (prd7-design-tab.html state wf-template-preview, lines 657-730).
  
  Architecture:
  \- Demo fixtures (template-preview-fixtures.ts): hard-coded Russian demo
    content per kind (intro / info / summary) and per question type (single /
    multiple / ranking / matching) + shared course chrome (sidebar topics,
    progress %, page indicator)
  \- CSS (tb-components.css): ported .tpl-preview-{modal,group,stage,shell,
    caption} from wireframe; shell sub-elements (header/progress/body/sidebar/
    content/hero/actions) + question variants (option/marker/matching-row) +
    result block; shell consumes CSS custom properties --tpl-primary / --tpl-fg
    / --tpl-card / --tpl-font for branding application
  \- TemplatePreviewModal component: ModalDialog size="xl"
    className="tpl-preview-modal". Rail derives groups + items from
    template.manifest.contentTemplates (intro → info → 4 question subtypes →
    summary; router kind skipped). Stage renders demo content per selected
    rail item inside a mocked SCORM shell; CSS vars on the shell are built
    from current draft.params via buildShellStyle() (HSL strings wrapped in
    hsl(), hex/rgb/hsl pass-through)
  \- DesignSection wiring: previewOpen state lifted in DesignSection;
    TemplatePane receives onPreview; button onClick = setPreviewOpen(true),
    disabled when template === null (pre-protection for upcoming G6
    incompatible-state). Modal mounted in a React Fragment alongside the
    split container
  
  Edge cases:
  \- template === null → modal returns null (button is also disabled)
  \- contentTemplates empty → modal returns null (no items to show)
  
  DS forwarding note: ModalDialog from @universityrt/ui-kit does NOT forward
  arbitrary HTML attributes, so the data-testid for the modal sits on an
  inner .ou-modal__split wrapper rather than on ModalDialog itself.
  
  Tests: 3 new in design-section.test.tsx (modal opens / closes via «Закрыть» /
  rail items derived from contentTemplates). TEMPLATE fixture extended with a
  minimal contentTemplates[] (intro / info / questions / summary).
  
  Verification:
  \- npm run check — 0 errors
  \- vitest run — 51 files / 1334 tests green (+3 for the new modal tests)
  \- markdownlint-cli2 — s12 spec passes (line-length cleanup pass on
    pre-existing long prose lines)

- **feat**(design-tab): close S12-G1 — params grouped by rail-section (FR-31) (2026-05-28) [`83e2bd3a44e9f31a8dc9540bad2ad43db7249686`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/83e2bd3a44e9f31a8dc9540bad2ad43db7249686)
  Replaces the «Макет» / «Прогресс и шапка» StubPane («реализуются отдельным
  шагом PRD-7») with real param rendering filtered by `param.section`. Brings
  the «Оформление» tab into compliance with FR-31 (acceptance row of
  prd7-design-tab.html: «Параметры шаблона по секциям — Брендирование, Макет,
  Прогресс и шапка. Набор определяется разработчиком шаблона.»).
  
  Contract change (UI-only, no API/DB migration):
  \- TemplateParam.section?: ParamSection ("branding" | "layout" | "progress")
  \- Default fallback "branding" for legacy params without explicit section
  \- templateManifestSchema is .passthrough() so no zod change needed; the
    server unknown-key validator (routes/tests.ts:393) treats `section` as a
    transparent passthrough
  
  Manifests (section assigned per param):
  \- default: 10b / 0l / 2p
  \- corporate: 12b / 0l / 3p
  \- minimal: 6b / 0l / 2p
  \- rtk-storyline: 5b / 4l / 2p (only template with real layout params today)
  
  UI refactor (design-section.tsx):
  \- BrandingPane + StubPane → generic SectionPane + paramsBySection helper
  \- Three sub-rail entries (branding / layout / progress) render their own
    params; empty sections show a tone="info" Banner with section-specific
    copy («У выбранного шаблона …»)
  \- New testids: design-layout-pane, design-layout-pane-empty,
    design-progress-pane, design-progress-pane-empty
  \- Removed: design-stub-layout, design-stub-progress
  
  Test update:
  \- «shows the «следующий шаг» stub» → «renders empty-section info-banner»
    (fixture params have no section → all fall back to branding → layout and
    progress panes show empty-state banners)
  
  Verification:
  \- npm run check — 0 errors
  \- vitest run — 51 files / 1331 tests green (no delta — test count steady)

- **feat**(editor): toast confirms «Структура» Save action (2026-05-28) [`613b15ba147c306087fece6c0cc3ca3bbd65823f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/613b15ba147c306087fece6c0cc3ca3bbd65823f)
  After clicking «Сохранить» (or «Сохранить и закрыть») when content pages were
  mutated, show a toast «Изменения структуры сохранены». Rows themselves were
  persisted at mutation time — the toast tells the user that the Save action
  acknowledged the structure edit cycle, closing the loop opened by the dirty
  indicator.
  
  Skipped when only the test-settings / design draft is dirty (those already
  have their own success path).

- **feat**(structure): drop pages around «Итоги»; Save reflects Структура (2026-05-28) [`9f38b253086f56bbfb6166edd8c675ed7176189d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9f38b253086f56bbfb6166edd8c675ed7176189d)
  «После теста» now renders a single combined list interleaving author after-
  pages with the system summary («Итоги») by sortOrder. Inserts appear in every
  gap, including before «Итоги», so authors can place pre-results pages.
  
  A drop on the «Итоги» row used to vanish: resolveTarget copied position/topicId
  from the hovered page, and the system summary is stored with a best-fit
  `position="after_topic"`+`topicId=null` (see server `positionForKind`). The
  dragged page inherited that pair, which no zone renders — the row disappeared
  from the UI. Now a drop on the summary row is recognised and rewritten to the
  after-zone (`position="after"`, `topicId=null`).
  
  «Сохранить» now lights up after add/drag/reorder on the Структура tab:
  content-page mutations write directly to the API (no draft), so a
  `hasMutated` flag bridges them to the Drawer's combinedDirty signal and is
  reset by `saveAll`/`handleExitWithoutSave`. The structure status split is now
  explicit: required-empty in an author page is an error (red tag, blocks Save);
  templateKeyMissing is a warning (yellow tag, status-dot only).

- **feat**(structure): cross-zone page move between zones/topics + zone drop highlight (2026-05-28) [`18f7d0469abc9af056220b5203217b9d720fe450`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/18f7d0469abc9af056220b5203217b9d720fe450)
  Each author-page group is now a droppable (zoneDroppableId encodes position+topic),
  so a page can be dropped INTO another zone — including an empty one. A shared
  resolveTarget() feeds both the indicator and the drop, deriving the target zone
  from the hovered row or the bare zone droppable. Same-zone drops reorder
  (reorderByDrop); cross-zone drops change the page's position/topicId via
  cp.update then renumber the target zone (cp.reorder) so it lands at the chosen
  index (insertIndexFor). Cross-zone side is rect-based (active not in target list);
  empty/zone targets highlight via .is-zone-drop-target. Unit tests for
  insertIndexFor; npm run check clean; section suite 20/20.

- **feat**(structure): no-shift DnD with correct-side insertion indicator (2026-05-28) [`59a3359adbefd8b6386f05c51b937baa43e56161`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/59a3359adbefd8b6386f05c51b937baa43e56161)
  Rows no longer shift while dragging (that confused the drop direction). The row
  under the pointer shows an insertion line on the side the dragged row will land,
  driven by DropContext. Side is computed by list index (sideByIndex) — the same
  function feeds the indicator and reorderByDrop, so they can never disagree
  (fixes the earlier mismatch where the line showed the wrong edge). DragOverlay
  badge now uses the variant label. npm run check clean; section suite 18/18.

- **feat**(structure): @dnd-kit within-zone page DnD (drag handle, drop indicator) (2026-05-27) [`062b4378afbb28c6ab8d22f4e560c122085ff581`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/062b4378afbb28c6ab8d22f4e560c122085ff581)
  Replace the broken native HTML5 DnD (drag never started without dataTransfer)
  with @dnd-kit: DndContext + per-zone SortableContext, rows are useSortable
  (dragged by the grip handle), DragOverlay for a clean drag preview. Enables
  arbitrary placement within a zone with the sortable insertion shift as the
  drop indicator. Reorder math extracted to computeZoneReorder (unit-tested);
  @dnd-kit drag itself needs real DOM measurements so it is verified in-browser.
  
  Stage A (within-zone) only; cross-zone move (between topics / across the
  questions block) and topic reorder follow separately. npm run check clean;
  editor suite 8 files / 165 green.

- **feat**(scorm): retire legacy start_page_content read; close PRD-7 S10/S11 (2026-05-27) [`ae1bebd1206b563ce4ff67f94fc8f70bbae85304`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ae1bebd1206b563ce4ff67f94fc8f70bbae85304)
  S10 remainder: stop exporting tests.start_page_content into TEST_DATA
  (test-json.ts) and rendering it on the SCORM start overview (startPage.js).
  The content is migrated to a content_pages 'intro' row (migration 003 §4.2)
  and played by the content-flow runtime, so the start screen no longer
  duplicates it. DB column and write-path kept for legacy clients (decisions §1,
  S10 §3.3); in-app web player (take-test.tsx) untouched. Removed an orphaned
  backup file in the template dir. Added a golden guard asserting startPageContent
  is absent from TEST_DATA while an intro page is present.
  
  S11: acceptance pass recorded in docs/prd-7-acceptance-report.md (10/10 criterion
  groups, 0 blockers). Full vitest suite 52 files / 1344 green; npm run check 0.
  ROADMAP and PRD-7 S9-S11 statuses set to Closed. Archive-restore UI remains a
  sanctioned post-MVP deferral (backend POST /restore done + tested).

- **feat**(scripts): SCORM acceptance/debug tooling + golden test (2026-05-27) [`6aef93321bd66c6c6f3114cb44105b42e75e5f62`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6aef93321bd66c6c6f3114cb44105b42e75e5f62)
  scorm:sample (fixed demo), scorm:template (template-aware mock for any id/folder/manifest, with temp staging), scorm:player (local SCORM 2004 player + RTE shim). tests/scorm-package-acceptance.test.ts validates the real export pipeline (manifest/launch/runtime/template/TEST_DATA + content-flow bundle). out/ gitignored.

- **feat**(scorm/runtime): play test-scope content pages + post-results flow (2026-05-27) [`c93e088500912fc3ec53a186984c1bbcf061a64b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c93e088500912fc3ec53a186984c1bbcf061a64b)
  rebuildPageSequence now sequences «До теста» intro/info and pre-results info around the topic stream; the first summary page marks the results boundary (built-in results screen), and pages after it render via a new postResults phase. Backward compatible (no test-scope pages → unchanged). Closes the editor↔runtime gap (PRD-1 §1.9).

- **feat**(test-editor): content-page editor in «Структура» tab; retire ContentPagesDialog (2026-05-27) [`a94b4b7a21cb9c45cf5996603c8d505a6f4621ea`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a94b4b7a21cb9c45cf5996603c8d505a6f4621ea)
  Kind-aware zoned editor: add/edit/reorder/delete author info pages; system rows (intro/summary/questions/router) read-only with «Сменить вариант» (replace-variant, disabled when single variant) and required-field warnings. Content-pages CRUD hook + drawer warning dot; structural classes in tb-components.css. Removes the orphaned legacy ContentPagesDialog (PRD-1 closeout).

- **feat**(content-pages): accept test-scope before/after pages (nullable topicId) (2026-05-27) [`194a813d9d7dd1f2be7294438999050db62b90e7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/194a813d9d7dd1f2be7294438999050db62b90e7)
  POST allows position before/after with topicId=null (zones «До/После теста»); before_topic/after_topic still require a valid topicId. Needed by the «Структура» editor.

- **feat**(schema): add test-scope "after" position for content_pages (2026-05-27) [`4e8a518b8f9a1377430457a26536662a4c0a7b50`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4e8a518b8f9a1377430457a26536662a4c0a7b50)
  Enables author pages in the linear_flat «После теста» zone (mirror of "before"). Widens the position enum and CHECK constraint (migration 005); existing rows untouched.

- **feat**(test-editor): FR-20c error-summary banner + field anchor navigation (2026-05-27) [`78df1a60aebf373b1d6c9ffde0d315b7d0b8144e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/78df1a60aebf373b1d6c9ffde0d315b7d0b8144e)
  Add a DS Banner (error tone) at the top of the drawer body with a field-error
  count and a 'Перейти к ошибкам' action (per approved prd7-editor-drawer.html).
  The action switches to the tab owning the first error (tabForField) and
  focuses/scrolls the offending field via data-field anchors on section
  formfields (basic.title, basic.webhookUrl, passRules.*, sections[i].drawCount,
  sections). Closes the last open S4/4A item; covered by test-editor.test.tsx.

- **feat**(server/utils): exceljs wrapper mimicking the SheetJS subset we used (2026-05-26) [`78b40ea1f02f39683d68ce391ac5040692f395e7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/78b40ea1f02f39683d68ce391ac5040692f395e7)
  Routes previously called XLSX.utils.{json,aoa}_to_sheet / sheet_to_json
  directly. Centralising the API surface here keeps route code terse and
  limits the blast radius if the library is ever swapped again.

- **feat**(prd-7): tests list — triangle-alert indicator for tests saved with warnings (2026-05-26) [`31f88478ffa875c20005e6f1028f03b9c919ee58`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/31f88478ffa875c20005e6f1028f03b9c919ee58)
  After a successful save the editor stamps a boolean into the
  react-query cache under ["test-warnings", testId]. TestRow subscribes
  with enabled:false (queryFn never runs) and rerenders when the cache
  flips. Result: a yellow TriangleAlert appears next to the test title
  on the list when the last save produced any warnings — the visual
  counterpart to the saveError banner added in the drawer.

- **feat**(prd-7): move «Обязательная» to Состав; FR-17 adaptive guards; RTE feedback preview (2026-05-26) [`f7b73993168a5b127b63512de20e9ae30b19320d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f7b73993168a5b127b63512de20e9ae30b19320d)
  «Обязательная» toggle previously lived only in the Settings → Pass-rules
  table; per updated wireframe it belongs in the topic-row header on the
  Состав tab (single point of control). Remove the column from the pass-
  rules table and add a Switch to TopicRow. Pass-rules row signature
  slims down accordingly.
  
  Adaptive mode guards (PRD-7 FR-17):
  \- FR-17a — adaptive mode with no enabled topic is a stop-factor error;
    surfaced via error dot on the rail and a Banner in the Adaptive pane.
  \- FR-17b — every enabled section-topic should have ≥ 2 levels; surfaced
    as a per-topic warning Banner + warning dot on the rail. Disabled
    topics are excluded since they don't participate in test logic.
  \- Switching mode → adaptive now seeds a default level for each section,
    so the new test isn't born with a stop-factor.
  \- Last-level removal is disabled with a tooltip — every topic must
    always carry at least one level slot.
  
  FeedbackPreview rendered RTE content inside a `&lt;button&gt;`, which is
  invalid HTML (block-level inside button). Render through
  `div[role=button]` with keyboard handler so `&lt;p&gt;`/`&lt;ul&gt;` from the RTE
  are valid children; new tb-feedback-preview__rich block clips to ~4
  lines with a fade mask.

- **feat**(prd-7): FeedbackEditorModal — real RTE, PDF assets and links per wireframe s-feedback-edit (2026-05-26) [`57d77c13e18a8c0e68f25c38eb61cf2f601addf4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/57d77c13e18a8c0e68f25c38eb61cf2f601addf4)
  Replaces Textarea + Banner stubs with the actual wireframe implementation:
  \- tb-rte toolbar (Bold / Italic / Link via execCommand) with contenteditable area
  \- PDF assets list with upload button, file-size guard (5 MB), and remove action
  \- «Добавить ссылку» as secondary button (not stretched full-width)
  \- Section headings use tb-feedback-editor__sec-title (uppercase DS class)
  \- DraftAsset strips size/file fields before onSave emission
  \- hideAssets prop to suppress PDF section in test-level feedback context
  \- TopicRow wires FeedbackPreview click → FeedbackEditorModal open/save
  \- basic-settings-section: add assets:[] to FeedbackEditTrigger initial value

- **feat**(prd-7): unified FeedbackEditorModal — single editor for all feedback contexts (2026-05-25) [`0257b5bf84bec4a71b3d9022af8839f51dd89e3f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0257b5bf84bec4a71b3d9022af8839f51dd89e3f)
  Per prd7-editor-drawer.html state s-feedback-edit (FR-36 / FR-37),
  all feedback editing surfaces share one modal: format selector,
  body editor, links list, PDF assets. Previously the editor was
  inlined three different ways (test-level Textarea, topic-level
  failureFeedback Textarea, level-level feedback Textarea +
  AdaptiveLevelLinks composite).
  
  New shared component:
  \- sections/feedback-editor-modal.tsx — ModalDialog composite with:
    \* SegmentedControl format selector (plain / richText / html)
    \* Textarea body editor; richText shows «full RTE — next step»
      Banner stub
    \* Links list (title + url + delete) + «Добавить ссылку»
    \* PDF assets section with a deferred-uploader Banner (FR-37
      integration ships separately); can be hidden via `hideAssets`
  \- CSS: tb-feedback-editor + section helpers ported from wireframe
    to tb-components.css.
  
  Wiring:
  \- AdaptiveTopicAccordion: replace failureFeedback Textarea with
    FailureFeedbackEditor → modal opens on preview-button click.
  \- AdaptiveLevelCard: replace per-level feedback Textarea +
    AdaptiveLevelLinks with LevelFeedbackEditor — single modal
    manages both text and links atomically (onSave applies
    text + links together).
  \- AdaptiveLevelLinks helper deleted (its responsibility moved
    into the modal).
  
  Tests updated: the «adds and removes per-level material links»
  spec now exercises the full modal flow (open trigger → remove
  link inside modal → click Save).
  
  133 vitest tests pass; tsc clean.

- **feat**(prd-7): test editor Drawer + tests-list explorer + settings/design/structure panes (2026-05-24) [`afc5fe5578c10c3a7a784bc4ef8365e0fe40882d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/afc5fe5578c10c3a7a784bc4ef8365e0fe40882d)
  Major PRD-7 landing across editor and tests list:
  
  \- Tests list (client/src/features/tests/list/): explorer-tree per
    prd7-tests-list.html with folder hierarchy, search, more-menu,
    delete confirms; FAB; DS-only `ou-*` / `tb-*` styling.
  \- Editor Drawer (test-editor.tsx + use-test-editor.ts): wide Drawer
    shell with 4 tabs, create/edit mode discriminated union,
    optimistic version save (FR-25k), auto-close on createdId.
  \- Settings tab (basic-settings-section): 5-rail layout with
    Основное / Правила прохождения / Ограничения / Интеграция /
    Адаптивный режим. Pass-rules pane drives decisionPolicy +
    overall rule + per-topic source/required. Adaptive pane has
    master toggle, per-topic accordion, level CRUD (name / min/max
    difficulty / questions / threshold / feedback / links).
  \- Composition tab (topics-structure-section): topic picker modal
    with search, tb-topic-row list with draw-count clamp.
  \- Design tab (design-section + use-design-settings): loads
    current template via /api/templates/:id, renders template card
    (Шаблон pane) and dynamic params form per manifest (Брендирование
    pane) for text / color / boolean / select. Save bar persists via
    PUT /api/tests/:id/design.
  \- Structure tab (start-pages-section + use-content-pages): zoned
    read-only render of content_pages keyed by flowMode (linear_flat
    vs linear_by_topics/router_by_topics), templateKeyMissing
    warning, delete-with-confirm via DELETE /api/tests/:id/content-pages/:pageId.
  \- Backend: tests.ts gains folderId on create + GET /api/tests/:id;
    test-folders.ts DELETE supports folder-only (moveTestsTo) and
    folder-and-tests (confirmName); storage adds
    deleteTestFolderCascade.
  \- DS: vendored university-rt.css to bypass broken upstream
    package exports; tb-components.css extended for topic rows /
    picker modal / adaptive accordion / level cards / design
    template card / structure zones / page rows.
  \- Theme: ThemeProvider syncs Tailwind `.light/.dark` (html) +
    DS `.ou--light/.ou--dark` (body).
  \- Coverage: 134 vitest tests across 9 files
    (editor + tests-list + tree-builder + sections);
    check:wireframes:ds clean over 17 wireframes.
  
  Create/edit/reorder for content_pages, router-page picker,
  gallery for "Заменить шаблон", and image/asset param types
  remain as next-step banners.

- **feat**(routes): migrate POST/PUT /api/tests to TestSettingsService (PRD-7 §1.10) (2026-05-24) [`f18fd1c3afd38a9d8c64b6dd7fb100a821df2f28`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f18fd1c3afd38a9d8c64b6dd7fb100a821df2f28)
  Closes the route-gap: production save path now goes through the service
  so reconciliation (PRD-7 §1.4) and required-fields validation (PRD-1
  §4.3.6) actually fire in one transaction.
  
  \- POST /api/tests calls testSettingsService.create(). Adaptive levels
    are passed through the payload instead of being inserted by the route.
  \- PUT /api/tests/:id calls testSettingsService.save() with sections for
    standard tests and adaptiveSettings for adaptive tests. expectedVersion
    is forwarded from the body when provided.
  \- Error mapping: VersionConflictError → 409 with currentVersion /
    expectedVersion; RequiredFieldsMissingError → 422 with structured
    fields ({ pageId, templateKey, fieldName }); status=404 on Test not
    found errors thrown by the service.
  \- routes.tests.test.ts: TestSettingsService is mocked at the route
    boundary (real error classes preserved). Replaced storage-level
    assertions with service-level ones; added new tests for 409 and 422
    flows.

- **feat**(test-settings): required-fields validation on publish transition (PRD-1 §4.3.6) (2026-05-24) [`d445861262c19e7d5f39e85706b3ca3659fcfa35`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d445861262c19e7d5f39e85706b3ca3659fcfa35)
  New required-fields-validator module exports findMissingRequiredFields()
  plus RequiredFieldsMissingError carrying per-page violations. Pure helper
  treats undefined/null/whitespace/empty arrays as missing; 0/false/objects
  count as filled.
  
  TestSettingsService.save() validates only when target status is
  'published' — draft saves remain permissive so a freshly created test
  (empty intro/summary) can be saved mid-edit. On violation the transaction
  rolls back via thrown RequiredFieldsMissingError. Fail-soft when the
  template manifest cannot be loaded, same as reconciliation.
  
  Findings surfaced in PRD-7 §1.10 todo: the production PUT /api/tests/:id
  route in server/routes/tests.ts still calls storage.updateTest() directly
  and does not yet route through testSettingsService.save(). Until that
  migration lands, the new reconciliation + required-fields validation are
  dormant on the live save path.

- **feat**(api): replace-variant endpoint for content_pages (PRD-7 FR-46) (2026-05-24) [`5101d5c8c3a6cec21efba988c53a452af5185224`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5101d5c8c3a6cec21efba988c53a452af5185224)
  POST /api/tests/:id/content-pages/:pageId/replace-variant switches a
  content page to a different variant of the same kind. Implements the
  'name = contract between variants of the same kind' rule (PRD-1 §4.3.3):
  
  \- Diff: preserved (shared placeholder keys), removed (only in old),
    added (only in new). Returned to the caller.
  \- Apply: filters valuesJson and placeholderStyles down to preserved
    keys, then re-normalizes against the new placeholder set (path /
    renderer revalidation for resultField).
  \- Rejects with 422 on kind mismatch, same-key replacement, or missing
    newTemplateKey. Rejects with 404 on missing test / page / variant.
  
  9 new API tests cover auth, all error paths, and the happy-path diff
  with values preserved/dropped according to the placeholder names.

- **feat**(test-settings): reconcile system content_pages on create/save (PRD-7 §1.4) (2026-05-24) [`efe47cb76146f72c3380177426865cf4902d150f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/efe47cb76146f72c3380177426865cf4902d150f)
  TestSettingsService now keeps system content_pages rows in sync with the
  test's (flowMode, topicIds, templateId) tuple inside the same transaction:
  
  \- create() always reconciles after sections insert.
  \- save() reconciles only when sections, flowPolicyJson, or
    designSettingsJson changed — basic-field saves stay cheap.
  \- Helpers extract flowMode and templateId from their JSON columns with
    documented defaults (linear_flat / 'default' template).
  \- legacyTypeForKind / positionForKind keep the deprecated type column
    and the existing position enum populated until a future migration
    retires them.
  
  Reconciliation loads the test's template manifest plus the default
  template in a single query, plans via planSystemPages(), and applies
  deletes then inserts. Silent no-op if neither manifest is present —
  higher layers handle that as a config error.
  
  9 new integration tests cover create() for all 3 flowModes, all 3
  save() reconciliation triggers, both directions of flowMode change,
  and the perf guard that skips reconciliation on basic-field saves.

- **feat**(services): system content_pages lifecycle planner (PRD-7 §1.4) (2026-05-24) [`caeb4a93c01b550670ca4aa3dc3e179b2208b478`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/caeb4a93c01b550670ca4aa3dc3e179b2208b478)
  Pure planner planSystemPages() reconciles existing system content_pages
  rows (kind: intro/summary/router/questions) against the desired test
  state (flowMode, topicIds, template, defaultTemplate). Computes a diff
  plan with keep / create / delete lists; callers execute it inside a
  transaction.
  
  Handles all four flowMode transitions plus topic add/remove within
  per-topic modes. Carries valuesJson across questions row collapses
  (N→1: takes first row's values) and expansions (1→N: copies flat row's
  values to each new per-topic row), implementing the 'name = contract'
  parameter-transfer rule. User info pages are out of scope and never
  touched. Idempotent: re-running against a steady state produces no
  creates or deletes.
  
  13 unit tests cover all transition directions plus edge cases
  (duplicate cleanup, missing variant fallback).

- **feat**(services): silent variant binding service (PRD-1 §4.3.2) (2026-05-24) [`d227900c0f2958fed8fb3712d8248c24c90157e2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d227900c0f2958fed8fb3712d8248c24c90157e2)
  Pure functions that pick a variant key for a given (template, kind) pair:
  1 variant in template → silent bind; N → bind isDefault (or first) and
  signal hasMultipleChoices for the UI hint; 0 → fall back to the default
  template's variant of the same kind and signal fallbackUsed. Returns
  null only when both template and default lack the kind (hard config
  error — surfaced by caller).
  
  Consumed by upcoming TestSettingsService reconciliation logic.

- **feat**(schema): variant.kind contract for templates and content_pages (PRD-1 §4.3) (2026-05-24) [`9e3606e5b969a95aec5e365e7dd6fc55cddaa04c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9e3606e5b969a95aec5e365e7dd6fc55cddaa04c)
  Introduces the variant-binding contract that drives system-page lifecycle
  across template versions and flowMode changes:
  
  \- VariantKind enum (questions/router/summary/intro/info) + zod schemas
    for manifest entries, full manifest, and default-template refinement
    (must declare at least one kind: questions variant — system fallback).
  \- Migration 004 adds content_pages.kind column (5-value CHECK constraint),
    backfilled from legacy type; type column kept and marked deprecated.
  \- Built-in default/corporate/minimal manifests gain kind on every entry;
    default also declares a question.standard variant for the questions kind.
  \- syncBuiltinTemplates() validates manifests via zod and skips invalid ones;
    default is held to the stricter refined schema.
  \- content-pages route and storage safety-net derive kind on insert.

- **feat**(wireframes): add s-default-adaptive state to editor-drawer (FR-36) (2026-05-21) [`08c244d621ac958f4f7488d852c67e779c9ab5db`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/08c244d621ac958f4f7488d852c67e779c9ab5db)
  Adaptive-вариант вкладки «Состав». В draw-count row значение из input
  заменяется на read-only тег «Адаптивный» + ссылка «Настроить уровни →»
  на одноимённую секцию вкладки Настройки. Сверху body — info-banner
  с пояснением, что количество вопросов на тему определяется уровнем
  сложности учащегося.
  
  В header дополнительно ставится `ou-tag--accent` «Адаптивный» рядом со
  статусом теста, чтобы режим был виден на любой вкладке Drawer без
  открытия секции Адаптивность в Настройках. Кнопка состояния s-default-
  adaptive добавлена в `.wf-nav` рядом с s-default. Иконка `i-info`
  добавлена в sprite.
  
  Сама декорация `--adaptive` уже определена в tb-components.css
  (`tb-draw-count-row--adaptive { flex-wrap: wrap; }`) — задаёт wrap
  поскольку link-tail длиннее number-input.

- **feat**(wireframes): replace s-mobile with s-feedback-edit in editor-drawer (2026-05-21) [`485614af141ee74be0967cc29dc6dff9d8e670ba`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/485614af141ee74be0967cc29dc6dff9d8e670ba)
  Две связанные правки покрытия state'ов вкладки «Состав» editor-drawer:
  
  1. Удалён s-mobile state (343 строки HTML + ~50 строк CSS-блока mobile-*
     \+ 3 строки notes-таблиц + DS-mapping fallback на .mobile-canvas) —
     mobile-адаптивность явно вынесена за scope PRD-7, отдельный PRD
     покроет mobile-эскизы сам.
  
  2. Добавлен s-feedback-edit state — модал `ou-modal--l` поверх drawer
     Состав, покрывающий FR-36 / FR-37 (редактирование обратной связи
     по теме с richtext, ссылками и PDF-assets). Drawer на бэке dimmed
     (opacity 0.55), body aria-hidden, footer disabled.
  
     Структура модала:
     — inline format-selector `ou-seg--s` (Простой / Форматированный / HTML);
     — `tb-rte` rich-text editor с toolbar (B / I / link) и contenteditable
       area с примером (жирный + список);
     — section «Ссылки на материалы» (FR-37): single-row item title+url
       \+ iconbtn-trash, кнопка «Добавить ссылку»;
     — section «Прикреплённые файлы (PDF)» (FR-37): paperclip-row с
       inline-rename, filename·size, iconbtn-trash, upload-button с
       hint про SCORM-пакет.
  
     Дополнения: иконка `i-trash` в sprite, CSS-секция `tb-feedback-editor*`
     / `tb-rte*` (gate-passing DS-токены), DS-mapping fallback для
     feedback-editor / rich-text. FR-аннотации FR-36 / FR-37 на ключевых
     элементах.

- **feat**(tb-components): add topic-row + draw-count + feedback-preview + status-dot--dirty (2026-05-21) [`31cadd496a20d5948806039b5c009a3b828ac2ac`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/31cadd496a20d5948806039b5c009a3b828ac2ac)
  Новые BEM-классы для вкладки «Состав» Drawer-а, экстрагированы из inline
  CSS бывшего prd7-section-basic.html (удалён в следующем коммите как дубликат
  prd7-editor-drawer.html). Остаются как готовая инвестиция для будущего
  рефакторинга editor-drawer на DS + tb-* (см. чек-лист §2 «Gaps»):
  
  \- tb-topic-row + __header / __name / __count / __body — карточка темы
    (название · бейдж · счётчик + body с draw-count input + feedback)
  \- tb-draw-count-row + __label / __max + --adaptive modifier — строка
    «Вопросов в тест [N] из M» (standard) или tag + link (adaptive, FR-36)
  \- tb-feedback-preview + __text / __snippet / __meta / __sep + .is-empty —
    read-only клик-карточка обратной связи по теме
  \- tb-status-dot--dirty — accent variant для индикатора dirty-вкладки
  
  Синхронизировано в обе копии: client/src/styles/ (canonical, попадает
  в prod-bundle через index.css) и docs/wireframes/ (для эскизов).

- **feat**(wireframes): simplify variant-replace modal + relocate to approved/ (2026-05-21) [`e3af4daa8a148bfd36397fcd702fe1fc36bc7ab0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e3af4daa8a148bfd36397fcd702fe1fc36bc7ab0)
  Дизайнер согласовал 2026-05-21 после серии упрощений:
  \- Счётчики «N параметров» под не-текущими вариантами удалены (избыточны —
    пользователь увидит поля при редактировании)
  \- Зелёный блок «Будут сохранены N параметров» удалён во всех state'ах —
    positive feedback не нужен, штатное поведение пользователь увидит позже
    в page-row-expand
  \- Технические термины schema / kind убраны из user-facing текста и заменены
    на человеческие формулировки («У нового варианта таких полей нет»,
    «Текущие настройки страницы будут потеряны»)
  \- Иконка warning-блока заменена i-trash → i-warn (предупреждение, а не действие)
  \- Заголовок модала обёрнут в ou-modal__head-text для корректной позиции
    close-кнопки по DS-канону
  \- s-replace-empty-diff теперь полностью спокойный (только список без блоков)
  \- Final Sign-off pending снят, файл считается approved

- **feat**(wireframes): add «Требует обновления» status tag on tests-list (2026-05-21) [`837b1e1605f0425457b172f1e9d1b90c4636be1e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/837b1e1605f0425457b172f1e9d1b90c4636be1e)
  Закрывает §3.1 чек-листа: при дрейфе версии шаблона тест в общем списке
  получает дополнительный ou-tag--warning рядом со стандартным статусом.
  Реализовано на примере «Аудит логов SCORM» через flex-direction: column
  в ячейке статуса (точечный inline-стиль с DS-токенами).

- **feat**(wireframes): wrap «Внутри теста» under dashed contour in linear modes (2026-05-21) [`fa2ef8b693a4b64723d150671fd051cb3f5a6217`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fa2ef8b693a4b64723d150671fd051cb3f5a6217)
  Extend the zone-unification pattern (router_by_topics already done) to
  linear_flat and linear_by_topics so all three flowModes show the inner test
  content inside the same dashed-border zone-block as «До теста» / «После теста».
  
  linear_flat (1 state — s-main): the system questions-row is now wrapped in a
  `.zone-block` with «Внутри теста» header. Hand-edited because the state has a
  single fixed structure.
  
  linear_by_topics (10+ states): runtime JS wrapper. A small `wrapTopicsInZone`
  IIFE iterates every `.wf-state`, finds direct-child `.topic-block` siblings
  inside the drawer body, and moves them into a freshly created
  `.zone-block.zone-block--topics` titled «Внутри теста». This keeps the static
  markup of all 13 states (s-main, s-empty-pages, s-readonly, s-mapping,
  s-dirty-form, s-validation, s-sanitize, s-delete, s-dnd, s-dnd-topics,
  s-mode-change, etc.) untouched while applying the same visual contour
  everywhere. Idempotent via a `data-topics-wrapped` flag.
  
  The zone-block--topics modifier is reserved for potential per-zone tweaks
  later (e.g. different spacing for nested topics).

- **feat**(wireframes): unify «Внутри теста» under same dashed-zone contour as До/После (2026-05-21) [`0e55bee9596809b4dc5a27c8ab4e6c2cd12e8bfa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0e55bee9596809b4dc5a27c8ab4e6c2cd12e8bfa)
  Make the three structural zones (До теста / Внутри теста / После теста) visually
  consistent in the router_by_topics wireframe. Previously «Внутри теста» was a
  plain section with just a small label — now it's wrapped in the same dashed
  border-radius contour as the other two zones, including a CAPS header with a
  chevron icon.
  
  CSS changes:
  \- .inside-test gets dashed border + border-radius (matching .zone-block);
  \- .inside-test__label gets border-bottom dashed (matching .zone-header);
  \- new .inside-test__body provides the internal padding for the router-row +
    tree-branches content.
  
  Header icon switched from i-router (semantic-specific) to i-chevron-down so all
  three zone labels look identical. The router visual marker remains on the
  router page-row itself.
  
  Content of every state (s-main, s-main-multi-variant, s-main-fallback,
  s-empty-topics, s-validation, s-row-menu-open) wrapped in .inside-test__body.
  The tree-branches hierarchy now sits inside the same kind of contour as the
  sibling zones.

- **feat**(wireframes): finalize questions-row migration + demo row-menu dropdown (2026-05-21) [`1f9af95dcc095d4bc3db8e088348fe36634a104b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1f9af95dcc095d4bc3db8e088348fe36634a104b)
  Closes two outstanding debts from the variant.kind rewrite:
  
  1. Full markup re-render of every .questions-row in linear-by-topics
     (9 occurrences across s-main, s-empty-pages, s-readonly, s-dirty-form,
     s-validation, s-sanitize, s-mapping, s-dnd, s-dnd-topics). Each now
     uses the unified page-row--system page-row--questions structure with
     variant badge, page-icon, and `…` row-menu iconbtn — matching the
     contract from the router wireframe and PRD-1 §4.3. Legacy
     .questions-row / .questions-icon CSS rules dropped from both
     linear-flat and linear-by-topics (and the deprecated insert-btn
     selectors that referenced them).
  
  2. New s-row-menu-open state on prd7-structure-router.html demonstrates
     the open `…` dropdown for two cases:
     \- system kind (router): «Сменить вариант…» / «Предпросмотр» /
       «Удалить» (disabled with tooltip — system page is non-removable);
     \- user kind (info): destructive «Удалить» enabled in the row-menu.
     CSS for .row-menu / .row-menu__item / .row-menu__sep added inline;
     positioned absolute relative to .page-actions. JS wiring deferred to
     frontend implementation.
  
  Checklist updated with new s-row-menu-open acceptance item under §11.

- **feat**(wireframes): rewrite router structure under variant.kind + add variant-replace modal (2026-05-21) [`18b5669ab0cf443a56439556371337531808076f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/18b5669ab0cf443a56439556371337531808076f)
  Aligns the Структура tab wireframes with the new variant.kind model
  (PRD-1 §4.3) and the redefined router_by_topics architecture
  (PRD-7 §2.3b).
  
  prd7-structure-router.html — rewritten end-to-end. The legacy «scenario map»
  (router-block + section-card + connector-wrap + final-result-block) is
  removed. New model:
  \- linear_flat-style zones «До теста» / «После теста»;
  \- system page-row with kind: router in the «Внутри теста» zone;
  \- topics as branches under the router-row via tree-connectors
    (thin DS lines, ├─ and └─).
  States: s-main, s-main-multi-variant, s-main-fallback, s-empty-topics,
  s-mode-change (now flagging «Маршрутизатор будет удалён»),
  s-validation (required-params not filled — page-row--warn +
  validation-banner + .status-dot--error on the tab + Save disabled).
  
  prd7-variant-replace.html — new wireframe for the «Сменить вариант» modal
  (triggered from `…` row-menu, applicable to all kinds). Three states:
  s-replace-modal (with diff of lost / kept params), s-replace-empty-diff
  (symmetric schema, no losses), s-replace-no-fields (empty schema on the
  new variant).
  
  linear_flat / linear_by_topics — `.questions-row` visually unified with
  `page-row--system page-row--questions` (accent-border, page-row padding);
  full markup re-render of all 9 instances is deferred to a follow-up
  commit. Sprite `i-dots` and CSS classes `page-row--system`, `page-icon`
  added in linear_flat.
  
  wireframes-acceptance-checklist.md — §11 rewritten under the new router
  model, new §11a covers variant-replace modal, §8 / §9 extended with
  variant.kind addendums (`…` row-menu, multi-variant hint, fallback
  warning, required-params validation).

- **feat**(wireframes): add FR-code annotation badges to all drawer states (2026-05-18) [`e130f6d6816d9ce2a864e4c87c895a2e1402bf77`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e130f6d6816d9ce2a864e4c87c895a2e1402bf77)
  Added wf-annot-wrap / wf-annot badges to s-default, s-dirty, s-error,
  s-saving, s-changes, s-settings. Toggle via «Аннотации» button — shows
  FR-04, FR-05, FR-05a, FR-25a/b/c, NFR-19/20/21 codes on the actual
  elements they describe, replacing the need to cross-reference the notes table.

- **feat**(check-wireframes-ds): extend gate coverage (stage 2) (2026-05-17) [`d93bf58bda4e155438b850b27b26e5e770987a06`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d93bf58bda4e155438b850b27b26e5e770987a06)
  Extend the wireframes DS lint gate per docs/wireframes-ds-fix-plan.md
  stage 2: broader legacy-class list, more raw-color patterns, direct-unit
  detection inside &lt;style&gt; blocks, CSS selector scanning, optional
  --strict-inline flag, and legacy/ directory ignore (forward-looking).
  
  Categories detected by gate v2:
  \- legacy class tokens in HTML class="..."
  \- legacy class selectors in CSS files
  \- raw color literals (#hex, hsl/hsla/rgb/rgba/oklch/lab/lch/hwb, white/black)
    inside &lt;style&gt; blocks and CSS files
  \- direct length units (px, rem, em, non-zero) inside &lt;style&gt; and CSS files
  \- optional inline style="..." scanning under --strict-inline,
    whitelist via data-wf-demo on the carrying element
  
  Gate result: 60 -&gt; 2611 violations. Run time: ~1.9s.
  This becomes the roadmap for stages 3-4.
  
  Co-Authored-By: Claude Opus 4.7 &lt;noreply@anthropic.com&gt;

- **feat**(client): PRD-7 test settings editor with drawer, sections and validation (2026-05-11) [`20e6bcbfce4e3d8a6e61d55299e31fdc9785d8f5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/20e6bcbfce4e3d8a6e61d55299e31fdc9785d8f5)
  \- New test-editor feature module: mappers, validation, types, hook, section components
  \- ContentPagesDialog and DesignSettingsDialog components with full test coverage
  \- Updated page-header and tests list page to wire up the new editor drawer

- **feat**(scorm): content pages, design settings and external template system (2026-05-11) [`f573f938b1229aadf5e01ac8122bd642b9303f14`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f573f938b1229aadf5e01ac8122bd642b9303f14)
  Builders:
  \- test-json.ts: include contentPages and designSettings in SCORM export JSON;
    sanitize richText/html feedback fields via html-sanitizer before embedding
  \- media-assets.ts: bundle feedback PDF assets into SCORM package
  \- template-copy.ts: new builder that copies external template files into the
    SCORM ZIP, resolving EJS layouts and partials
  
  Runtime (template/app/):
  \- templateCore.js + templateLoader.js: engine for rendering external templates
    using Mustache-style layout resolution
  \- contentFlow.js: sequencing layer for intro/outro content_pages within a course
  \- render/contentPage.js: renderer for content_page slides
  \- render/renderers.js: dispatcher routing question types to typed render functions
  \- main.js + mainRender.js + startPage.js + answers.js + state.js: adapt existing
    runtime to carry designSettings and content-flow state
  
  Built-in templates (server/scorm/templates/):
  \- default, corporate, minimal: three baseline themes with EJS shells, layout
    partials, CSS and demo course.json for preview
  \- rtk-storyline: Storyline-style theme with hero artwork, per-question-type layouts,
    progress bar partial and adaptive review layout

- **feat**(api): PRD-7 test settings endpoints, content pages and template registry routes (2026-05-11) [`7191d0f4850d04d924e9259995a81769ed8f4fc7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7191d0f4850d04d924e9259995a81769ed8f4fc7)
  \- tests.ts: Zod validation for all PRD-7 body fields (status, feedbackJson,
    flowPolicyJson, designSettingsJson, telemetryEnabled); PATCH /api/tests/:id/status
    for status transitions without version bump; GET ?status=archived filter
  \- content-pages.ts: full CRUD for /api/tests/:id/content-pages (PRD-1 §5);
    sort_order auto-assignment, type/topic_id immutability guard
  \- templates.ts: GET /api/templates registry endpoint with API version filtering
  \- questions.ts: minor cleanup (unused import removal)
  \- analytics/combined.ts: exclude archived tests from analytics aggregation
  \- routes/index.ts + server/index.ts: mount new routers

- **feat**(server): storage layer for PRD-7 settings, content pages and template registry (2026-05-11) [`0b269fd1b4f6142cdb8b5c879b5d27c9e08c7773`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0b269fd1b4f6142cdb8b5c879b5d27c9e08c7773)
  \- storage.ts: contentPages CRUD, patchTestStatus (no version bump), getMigrationHealth,
    mapLegacyTest() backward-compat helper (status derived from published for old rows)
  \- services/test-settings.ts: business-logic service isolating PRD-7 field validation
    (feedbackJson, flowPolicyJson, designSettingsJson) from route handlers
  \- template-registry.ts: in-process registry for external SCORM templates;
    isSupportedTemplateApiVersion guard
  \- utils/html-sanitizer.ts: DOMPurify-based sanitizer for richText/html feedback fields
  \- utils/crypto.ts: minor type-safety fix

- **feat**(schema): add PRD-7 test settings fields and PRD-1 content pages schema (2026-05-11) [`be8fad9e608c7576683d9155894c6f43006e19a0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/be8fad9e608c7576683d9155894c6f43006e19a0)
  \- tests table: status enum (draft/published/archived), feedbackJson, flowPolicyJson,
    telemetryEnabled, designSettingsJson; mark published/startPageContent as @deprecated
  \- testSections table: required, timeLimitMinutes, feedbackJson per-section fields
  \- new contentPages table for intro/outro pages (PRD-1 §5)
  \- new templates table for external template registry (PRD-3)
  \- Zod schemas: feedbackContentSchema, feedbackLinkSchema, feedbackAssetSchema,
    feedbackFormatSchema, flowPolicySchema, designSettingsSchema
  \- migrations 002 (content_pages) and 003 (prd7 test settings columns)
  \- drizzle.config.ts: point migrations dir to /migrations

- **feat**: Мероприятия для тем, доработка назначений, первый вход, SCORM, переименование в СкилУм (2026-04-17) [`6f170539bfbb60dd7b57f995881ceb7b48a35da1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6f170539bfbb60dd7b57f995881ceb7b48a35da1)
  ## Рекомендуемые мероприятия для тем
  
  \- shared/schema.ts: добавлена таблица `topic_events` (id, topicId, title — без URL, только название)
  \- server/storage.ts: добавлены методы getTopicEvents, createTopicEvent, deleteTopicEvent
  \- server/routes/topics.ts: добавлены POST /:topicId/events и DELETE /events/:id
    \- DELETE /events/:id и DELETE /courses/:id перемещены выше /:id чтобы Express не перехватывал маршрут
    \- GET /api/topics теперь возвращает поле events[] вместе с courses[]
  \- server/routes/tests.ts: при SCORM-экспорте теперь загружаются events для каждой секции
  \- server/scorm/builders/test-json.ts: добавлено поле recommendedEvents в JSON секций
  \- server/scorm/assets/app.js: результаты SCORM — раздел "Рекомендуемые мероприятия" с иконкой
    календаря, дедупликация курсов и мероприятий по title across проваленных тем
  \- server/scorm/template/app/render/viewResults.js: аналогичная дедупликация и блок мероприятий
  \- server/scorm/template/app/render/resultsPage.js: recommendedEvents добавлены в calculateResults
  \- server/scorm/template/app/utils/pdfExport.js: блок мероприятий в PDF без URL, только текст
  \- client/src/pages/author/topics.tsx: кнопка "Мероприятие" (CalendarDays) в карточке и в
    строке списка, диалог с полем только "Название", отображение событий в карточке с удалением
  
  ## Группы и назначения — доработка UI
  
  \- client/src/components/assign-test-dialog.tsx: expandable строки для групп с участниками,
    кнопки resend/revoke для каждого пользователя, фильтрация участников групп из вкладки Users,
    исправлен баг testId is not defined в AssignmentRow
  \- server/routes/assignments.ts: GET /api/assignments/:id/group-users,
    POST resend-group, POST resend-user/:userId, PATCH revoke-user/:userId
  \- server/routes/groups.ts: notifyNewGroupMember — при добавлении в группу автоматически
    отправляется письмо со ссылкой на тест по всем активным назначениям группы
  
  ## Первый вход
  
  \- client/src/pages/first-login.tsx: убраны поле имени и чекбокс GDPR, gdprConsent=true хардкодом
  \- server/email.ts: добавлена sendInviteEmail с текстом приглашения вместо сброса пароля,
    инвайт-токен живёт 7 дней (был 30 минут)
  \- server/routes/users.ts: bulk-import использует sendInviteEmail
  
  ## Продакшн-фиксы (cookie, сессия, Docker)
  
  \- server/index.ts: app.set("trust proxy", 1) для корректной работы за Keenetic/nginx
  \- server/routes.ts: cookie.secure теперь управляется через COOKIE_SECURE env (не NODE_ENV)
    чтобы избежать проблемы с esbuild инлайнингом process.env.NODE_ENV="production"
  \- server/routes/auth.ts: явный session.save() перед ответом на логин
  \- docker/scripts/deploy.sh: правильный порядок stop→rmi→build --no-cache, drizzle-kit push --force
  \- docker/templates/.env.example: добавлены NODE_ENV, PORT, API_BASE_URL, COOKIE_SECURE
  \- script/build.ts: копирование server/scorm/template/ в dist/scorm/template/ при сборке
  
  ## SCORM — дедупликация вопросов
  
  \- server/scorm/assets/app.js: исправлена выборка вопросов через usedIds чтобы один вопрос
    не попадал в несколько секций одного теста
  
  ## Переименование приложения: СкилУм
  
  \- client/index.html: title и meta description
  \- client/src/lib/i18n.ts: t.auth.appName = "СкилУм" (сайдбар, страница входа)
  \- server/email.ts: fallback APP_NAME = "СкилУм" (все письма)
  \- docker/templates/.env.example, .env.example: APP_NAME=СкилУм
  \- package.json: name = "skilum"
  
  ## Тесты
  
  \- tests/routes.topics-folders-groups.test.ts: добавлены моки getTopicEvents/createTopicEvent/
    deleteTopicEvent, новые тесты для POST /:topicId/events и DELETE /events/:id включая
    проверку что deleteTopic не вызывается при удалении события
  \- tests/scorm-builders.test.ts: dbSection дополнен events[], тесты на recommendedEvents
    в JSON (наличие, отсутствие url, пустой массив)
  
  Co-Authored-By: Claude Sonnet 4.6 &lt;noreply@anthropic.com&gt;

- **feat**(docker): add Docker deployment system (2026-02-07) [`5cbb2ecad0fdbc358c0fd268dfe0109b0818e348`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5cbb2ecad0fdbc358c0fd268dfe0109b0818e348)
  \- Dockerfile for Node.js 20 Alpine
  \- docker-compose template with healthcheck
  \- prepare-deploy.bat: local build and archive creation
  \- upload-deploy.bat: SCP upload to server
  \- deploy.sh: server-side deployment script
  \- rollback.sh: full rollback script
  \- deploy.env configuration for server paths and Docker settings

- **feat**(db): add database resilience and graceful shutdown (2026-02-07) [`a5ce57bf256c57dd1fc4bdb5fc1e1b6116043121`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a5ce57bf256c57dd1fc4bdb5fc1e1b6116043121)
  \- Add connection pool error handling to prevent crashes
  \- Add health check function and /api/health endpoint
  \- Add withRetry wrapper for database operations
  \- Add waitForDatabase for startup connection waiting
  \- Add graceful shutdown on SIGTERM/SIGINT

### Fixes

- **fix**(templates): резолвить каталог шаблона через БД source_path (2026-06-09) [`36f33de772e98382a632401aa9626d39db23a2e3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/36f33de772e98382a632401aa9626d39db23a2e3)
  Подключённый к тесту шаблон рендерился как `default`: резолверы искали файлы
  по конвенции встроенных (server/scorm/templates/&lt;id&gt;), а загруженные (PRD-3)
  лежат в uploads/templates/&lt;id&gt;. Новый resolveTemplateDir резолвит каталог по
  templates.source_path — единому источнику и для встроенных, и для загруженных,
  включая `default` (его __dirname-путь в собранном dist не существует, поэтому
  тоже идёт через БД). Применён в веб-экранах ученика, экране результатов и
  экспорте SCORM (ExportData.templateDir). Сервисы рендера остаются db-free —
  каталог приходит готовым из роут-слоя.

- **fix**(scripts): починить set-password/create-admin под модель ролей user_roles (PRD-13) (2026-06-08) [`706ad89ab2b319c8ff946f27b98560226ae44752`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/706ad89ab2b319c8ff946f27b98560226ae44752)
  Обе тулзы обращались к удалённой колонке users.role и падали на мигрированной БД. set-password.mjs: убран role из RETURNING (он только меняет пароль). create-admin.ts: пользователь апсертится без role, роль administrator выдаётся отдельно через user_roles (idempotent ON CONFLICT) — иначе скрипт и падал, и не давал прав в новой модели.

- **fix**(templates-ui): не предлагать активацию уже активного шаблона (2026-06-07) [`e7ffd1837a957a2aeab155edc0b60fe8ab6b2e85`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e7ffd1837a957a2aeab155edc0b60fe8ab6b2e85)
  В окне предпросмотра для шаблона со status=active кнопка «Активировать»
  становится неактивной с подписью «Активирован», вердикт — «Шаблон активен»
  (canActivateNow=false при isAlreadyActive). Раньше для активного дефолта кнопка
  оставалась доступной и повторная активация была бессмысленной.

- **fix**(templates-ui): двухуровневый рейл предпросмотра (убрать лишний средний уровень) (2026-06-07) [`2014e8186acb94ae819c85c0da4b862a268df371`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2014e8186acb94ae819c85c0da4b862a268df371)
  Тип с одним вариантом отрисовки рендерится как сам экран (Раздел → экран), без
  сворачиваемого среднего уровня; группа типа показывается только при 2+ вариантах
  отрисовки, где она оправдана. Для шаблонов с одним вариантом на тип (как default)
  рейл становится плоским 2-уровневым. Заодно aria-expanded приведён к строковому
  значению (lint).

- **fix**(templates-ui): структура рейла предпросмотра по спеке + высота на всё окно (2026-06-07) [`134efa807e859197c4aae756f37527213335749d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/134efa807e859197c4aae756f37527213335749d)
  \- preview-rail: разделы по видам страниц PRD-1 §4.3 (Введение раздела / Учебные
    страницы / Вопросы / Итог раздела) + системные (Результаты теста §8.3, Системные
    экраны §5.4); intro/summary подписаны симметрично как закладки раздела.
  \- CSS: окно предпросмотра фиксированной высоты, рейл и сцена делят полную высоту
    и скроллятся внутри — рейл больше не обрезается коротким.

- **fix**(template-preview): реальная разметка интерактива + результат раздела для summary (2026-06-07) [`0b4b2590f8ebbf1956a383e3f7b1f6eafd3e4c16`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0b4b2590f8ebbf1956a383e3f7b1f6eafd3e4c16)
  \- buildInteraction приведён к боевой разметке веб-хоста (.option / .ranking-board
    с ручкой / .matching-board), которую стилизует CSS шаблона — в предпросмотре
    ранжирование/сопоставление больше не голый список; пары сопоставления смещены,
    чтобы не раскрывать ответ.
  \- content.summary («Итог раздела») получает result.* из sectionResult (результат
    РАЗДЕЛА, §8.2 платформы), а не из итога теста — runtime-safe, без правок манифеста.

- **fix**(boot): не прерывать запуск при сбое синка реестра шаблонов (2026-06-07) [`8bba7ab6c2d24009c91fdb68f2222b69a72d5362`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8bba7ab6c2d24009c91fdb68f2222b69a72d5362)
  syncBuiltinTemplates/reconcileUploadedTemplates были жёсткими await перед
  httpServer.listen(), поэтому любой их сбой (например, ещё не накатанная схема)
  ронял весь boot — приложение не слушало порт целиком из-за проблемы только в
  реестре шаблонов. Оборачиваем оба вызова в try/catch: ошибка логируется, boot
  продолжается, реестр пере-синкается на следующем рестарте. waitForDatabase и
  seedDatabase остаются fail-fast.

- **fix**(deploy): применять схему БД (drizzle-kit push) до старта приложения (2026-06-07) [`6474c396878bddeb901a710aa877091918c177d7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6474c396878bddeb901a710aa877091918c177d7)
  Новый образ крашился на старте: server/index.ts делает await syncBuiltinTemplates()
  до httpServer.listen(), поэтому на устаревшей схеме сервер не поднимается. Оба
  скрипта запускали приложение раньше схемы — deploy-test.sh push не вызывал вовсе,
  deploy.sh делал push после up -d.
  
  Теперь оба применяют схему ДО старта одноразовым контейнером с переопределённым
  entrypoint (`docker compose run --rm --no-deps --entrypoint sh &lt;svc&gt; -c
  "npx drizzle-kit push --force"`), затем поднимают приложение уже на актуальной схеме.

- **fix**(prd-11): экспортировать q.tags в рантайм TEST_DATA — квоты не работали в пакете (2026-06-04) [`dbbc2aea70c7ed9a42f4727a629b768d5e8fe59d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dbbc2aea70c7ed9a42f4727a629b768d5e8fe59d)
  Маппинг рантайм-вопроса (test-json.ts) включал scoring (PRD-10), но не tags — в
  выгруженном пакете у вопросов не было тегов, drawSection не находил совпадений и
  квоты молча вырождались в равномерную выдачу. Теперь tags пробрасываются условно
  (только при наличии, как scoring → пакеты без тегов бит-идентичны, FR-02).
  
  Проверка: новый scorm-quota-acceptance.test.ts собирает РЕАЛЬНЫЙ пакет и гоняет
  его собственный drawSection над встроенным TEST_DATA (200 случайных выдач) —
  инварианты квот соблюдены (exact=ровно, min=не менее, Σ=drawCount, без дублей).
  Дополнительно верифицировано вживую в scorm-player (реальный браузер-рантайм
  generateVariant, 100/100 выдач). Юнит-тест экспорта tags в scorm-builders.

- **fix**(prd-5): weight as real + explicit optional measurement zod fields (2026-06-02) [`d8953a63d48b40f3246a863183bd8a811e14f899`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d8953a63d48b40f3246a863183bd8a811e14f899)
  numeric mapped to a string in Drizzle and made weight required in the insert schema, breaking measurement payloads. Use real (a number multiplier) and mark weight/sourceKey/sortOrder/conditionJson optional in the zod schema so a contribution row only needs scaleId/sourceType/valueJson. Migration 009 column type updated (the dev DB column was altered in place).

- **fix**(prd-2): mock getResultVariables in tests/attempts route mocks (2026-06-02) [`008b6294eb5cca72a0c9e66b9a81b156084e8a3d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/008b6294eb5cca72a0c9e66b9a81b156084e8a3d)
  loadFullTest (GET/POST/PUT /api/tests) now loads result variables (A8), so the storage mocks in these route tests need getResultVariables or loadFullTest throws. Same regression class as the scorm-export mock fix; surfaced by the full-suite run during A9.

- **fix**(prd-2): A8 visual — DS-native layout, leadingIcon, stable row key (2026-06-02) [`68495c1ba3743386bdfcbc8a7923951171486d24`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/68495c1ba3743386bdfcbc8a7923951171486d24)
  Three issues caught by the Playwright pass against the approved wireframe: (1) wf-list-head/wf-grid-2 are wireframe-only classes absent from the app CSS — replaced the header and the 2-col form/builder grids with Tailwind utilities (flex/justify-between, grid grid-cols-2); (2) the add button stacked its icon above the text — moved Plus to the Button leadingIcon prop, matching sibling sections; (3) the per-row key included the editable name, so typing remounted the card and lost focus/expansion — added a stable clientKey assigned at creation (never sent to the API, not part of the save diff). Verified end-to-end: empty/list/expanded/builder states render, live validate-formula works, and the save orchestrator round-trips (PUT test → POST variable → reload).

- **fix**(prd-2): mock getResultVariables in scorm-export test (2026-06-02) [`f2382a6d0a93d40c107b89eb5bc6873d14355d82`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f2382a6d0a93d40c107b89eb5bc6873d14355d82)
  A6 added a storage.getResultVariables call to the SCORM export route but the export test's storage mock lacked it, so the route threw before generateScormPackage and three assertions failed. Add the mock (resolves []). Regression surfaced by the full-suite run while wiring A7.

- **fix**(prd-7): name structure page drag handles for screen readers (2026-05-31) [`b7313af87ec04ae925417d0e861ea076e7490196`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b7313af87ec04ae925417d0e861ea076e7490196)
  The content-page grip in the Structure tab carries @dnd-kit role=button but had no accessible name, so assistive tech announced a nameless button (axe aria-command-name, WCAG 4.1.2). Surfaced by the S11 live-browser acceptance pass. Mirror the existing topic-grip pattern: aria-label with the page title and aria-hidden on the decorative icon.

- **fix**(wireframes): drawer body scroll contract for long tabs and split layouts (2026-05-30) [`a95018826b5621a1090f6b5979b239c80c339ef9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a95018826b5621a1090f6b5979b239c80c339ef9)
  Long Drawer tabs expanded under the footer instead of becoming scrollable
  because the flex-column body was not allowed to shrink below its content
  height. Add `min-height: 0` to `.ou-drawer__body` and `.tb-settings-content`,
  and introduce `.ou-drawer__body--flush` so split (rail + content) tabs use the
  inner content panel as the single scroll container rather than a body-level
  scroll wrapping both rail and content.

- **fix**(editor): drawer body scroll regression — tabs without internal scroll container (2026-05-29) [`f4ebace8fe450aa94a63765f03541591e9357c8c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f4ebace8fe450aa94a63765f03541591e9357c8c)
  Regression: commit cea5bd9 (S13.7-G1 saving overlay) wrapped the body
  content in `&lt;div inert&gt;` and added the .tb-saving-host class to
  .ou-drawer__body. Two layout bugs:
  
  1. .tb-saving-host carried `overflow: hidden`, killing the body's
     own overflow-y:auto.
  2. The unstyled inert wrapper div became a flex child of the body
     (DS uses flex column for ou-drawer__body). Without `min-height: 0`
     the flex item collapsed and the inner content overflowed the
     visible area without producing a scrollbar.
  
  Result: Состав / Настройки / Оформление tabs were unscrollable —
  parts of long content were unreachable. Структура appeared fine
  because StructureSection carries its own internal scroll container.
  
  Fix:
    \- .tb-saving-host loses `overflow: hidden` (kept only
      `position: relative` for absolute-positioned overlay anchoring).
    \- The inert wrapper now uses className "tb-saving-inert-wrap" with
      `display: contents` so it is removed from the layout tree but the
      `inert` attribute still propagates to its descendants. Direct
      children (Section components) become flex children of the body
      again, restoring scroll behaviour.
  
  CSS comments explain the constraint so future overlays don't re-break
  this. Type-check clean; full vitest 1403/1403 (no test added — visual
  fix, observable only in the browser).

- **fix**(editor): close S13 parity quick-wins + reopen PRD-7 as S12+S13 (2026-05-28) [`4f589048e87b88981da930b93350e504c59df3e3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4f589048e87b88981da930b93350e504c59df3e3)
  Audit 2026-05-28 against approved wireframes found 36 discrepancies between
  the test editor and the wireframes signed off on 2026-05-21. PRD-7 reopened
  as two parallel phases — S12 (Design tab) and S13 (Editor parity). This
  commit closes the low-risk quick-wins from S13 and opens the docs for the
  remainder.
  
  S13 closures (12 gaps):
  \- G3 adaptive info-banner on «Состав» (composition-adaptive-banner)
  \- G6 ou-drawer__body--flush — verified DS-canonical (no gap)
  \- G11 webhook ou-formfield__desc «Оставьте пустым, если webhook не нужен»
  \- G13 misleading «stubs for deferred work» section header removed
  \- G14 stale «ships separately» JSDoc rewritten to current state
  \- G15 mode-change info-banner on «Структура» (savedFlowMode plumbed
    through useTestEditor); structure-mode-change-banner
  \- G26 router empty-topics CTA «Перейти к Составу» — onGoToComposition
    callback wired to setActiveTab("composition")
  \- G36 sort widget: native &lt;select&gt; → DS Select&lt;SortKey&gt;
  \- G38 delete-confirm name-block (extracted from label for copy-paste) +
    hint «Регистр символов учитывается»
  \- G41 orphan pass-rules-section.tsx removed
  \- G42 orphan adaptive-settings-section.tsx removed
  \- G44 DS Tabs hidePanel — verified equivalent (no gap)
  \- S12-G5 orphan design-settings-dialog.tsx + test removed (S10 cleanup
    was incomplete)
  
  Out of batch (require deeper work, prioritization table follows):
  \- G8 showCorrectAnswers placement — touches tests
  \- G21 router fallback tag — needs template manifest domain knowledge
  \- G23 mixed in FLOW_LABEL — multi-file refactor (type + mappers + list)
  \- G12, G24 — investigations still pending
  
  Verification:
  \- npm run check — 0 errors
  \- vitest run (full suite) — 51 files / 1331 tests green
    (delta -13 tests matches exactly the deleted design-settings-dialog suite)
  
  Docs:
  \- docs/specs/prd-7/s13-editor-parity.md — new phase spec (31 gaps, 8 sub-
    phases, DoD); closed items marked with strikethrough + date
  \- docs/ROADMAP.md — S13 added alongside S12 in §0 and §0.1 critical path
  \- docs/prd-7-acceptance-report.md — STATUS UPDATE references both phases
  \- docs/specs/prd-7/s12-design-closeout.md — G5 marked closed

- **fix**(editor): generalize save toast — Save covers all three sources (2026-05-28) [`f3d8730ae8ef0a5e3f6f4e5410985b0a32ba6e46`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f3d8730ae8ef0a5e3f6f4e5410985b0a32ba6e46)
  «Сохранить» persists the test-settings draft, the design draft AND
  acknowledges content-page mutations in one click, so the toast must reflect
  all three. Snapshot dirty flags before save, then show a single
  «Изменения сохранены» if any of them was dirty.
  
  Previous wording «Изменения структуры сохранены» was misleading when the
  user also edited Настройки / Оформление in the same session.

- **fix**(structure): swallow cross-zone mutation rejection (surface via banner) (2026-05-28) [`9b9f525d25d2f5f639b87b6483f7d029ec312ab5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9b9f525d25d2f5f639b87b6483f7d029ec312ab5)

- **fix**(structure): precise cross-zone drop (pointerWithin) + allow null topicId on PUT (2026-05-28) [`6ad267b2a8d51620eafa8417af7dca258d85be83`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ad267b2a8d51620eafa8417af7dca258d85be83)
  Two cross-zone bugs found in live testing:
  1. closestCenter compared the dragged item's center to droppable centers, so a
     drop could land in an adjacent topic. Switch to a pointer-based collision
     (structureCollision): the zone/row actually under the cursor wins, preferring
     a row hit over the wrapping zone.
  2. The content-pages PUT route rejected topicId=null with 422 'topicId does not
     belong to this test' (topicId !== undefined caught null too), so moving a page
     OUT of a topic into a test-scope zone («До/После теста») failed. Validate
     membership only for a non-null topicId, mirroring POST. Adds a route test.
  
  npm run check clean; content-pages routes 45/45.

- **fix**(structure): sortable unit = row+insert; arbitrary placement + drop indicator (2026-05-28) [`d1a3150df327743d14e7f52d7cb8c74dff82d592`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d1a3150df327743d14e7f52d7cb8c74dff82d592)
  Stage A reorder only swapped reliably: the InsertRow separators were interleaved
  between sortable items and the measured @dnd-kit node was the inner .page-row,
  so the reorder shift could not open a clean gap and 3+ row placement misbehaved.
  
  Make each sortable unit the whole wrapper (row + its trailing «+ Добавить» line)
  via a SortablePageItem that owns useSortable; AuthorPageRow now takes
  dragHandleProps + isDragging. Add a drop-target insertion line + dragging dim in
  tb-components.css. Verified in-browser: moving the first of three rows to the end
  reorders to [A,C,B] and persists via PUT /reorder; the insertion indicator shows.

- **fix**(test-editor): widen close-confirm to size l so actions don't overflow (2026-05-28) [`8f220019c6b95e58ede9e0cd446e104dd8cee9a5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8f220019c6b95e58ede9e0cd446e104dd8cee9a5)
  The «Продолжить редактирование» / «Выйти без сохранения» / «Сохранить» trio
  overflowed the medium modal (the first button was clipped past the left edge).
  Bump the dialog to size l; verified in-browser the buttons fit within the card.

- **fix**(test-editor): footer «Отменить» discards and closes without a save prompt (2026-05-27) [`748e860f5d1762be7e5fbca5240ea28c38dba768`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/748e860f5d1762be7e5fbca5240ea28c38dba768)
  Footer «Отменить» (dirty) routed to the same requestClose as the header «×»,
  so an explicit cancel opened a 'save before closing?' dialog — contradictory.
  Wire it to discard+close directly (handleExitWithoutSave); the save-confirm now
  belongs only to the ambiguous «×»/backdrop. The confirm's stay button is renamed
  «Отмена» → «Продолжить редактирование» to drop the «Отменить»→«Отмена» collision.
  
  Wireframes updated to match (re-approval 2026-05-27): «Отменить» = discard+close;
  prd7-editor-close-confirm stay button label. Adds a regression test.

- **fix**(test-editor): mark Drawer body as active tab's panel (a11y) (2026-05-27) [`c6750b272a9b36b0e5d07deb16efe59e33fe8b1c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c6750b272a9b36b0e5d07deb16efe59e33fe8b1c)
  S11 live audit (Playwright + axe-core) found a critical aria-valid-attr-value
  on the editor tablist: tabs carry aria-controls=panel-&lt;key&gt; (ui-kit Tabs with
  hidePanel) but no matching panel existed. Mark the content container as
  role=tabpanel + id=panel-&lt;key&gt; + aria-labelledby=tab-&lt;key&gt; so the active tab's
  aria-controls resolves. axe re-run: 0 violations across all four tabs.
  
  Adds a regression test and records the full live-audit results (axe, NFR-17
  108ms, dirty/close-confirm smoke) in docs/prd-7-acceptance-report.md §7.

- **fix**(scorm/media): stop double-processing mediaUrl during asset extraction (2026-05-27) [`9a6632aa69d3ca2c7c988f1f9dd67bcfbfc9ec9c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9a6632aa69d3ca2c7c988f1f9dd67bcfbfc9ec9c)
  mediaUrl was handled by the explicit block and again by the generic field
  loop, so a missing/blocked file was reported twice in `missing`. Skip the
  mediaUrl key in the loop. Other data:/uploads string fields are still embedded
  (SCORM self-containment); update the stale 'non-mediaUrl' test accordingly.

- **fix**(ds): border-box control heights + align level-grid threshold row (2026-05-27) [`7fec46cb17b60d380858a11df03eaf2b8fb2382c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7fec46cb17b60d380858a11df03eaf2b8fb2382c)
  The «Тип порога» (select) and «Порог» (number) controls in the adaptive level
  card rendered at different heights and the select dropdown overflowed into the
  number field.
  
  vendor/university-rt.css: give .ou-field__box / .ou-number__box /
  .ou-select__trigger `box-sizing: border-box` so their height matches the
  --ou-size-control-* token (they were content-box, rendering 2px taller than the
  token and than .ou-textarea). Mirrors the upstream DS source fix.
  
  tb-components.css (.tb-level-grid):
  \- align-items end -&gt; start so label+control pairs line up across a row (the
    select cell did not reach the row track bottom under `end`);
  \- stretch .ou-select to its cell and let its trigger shrink (min-width:0) so the
    180px-min-width dropdown no longer overflows the neighbouring field;
  \- drop the now-redundant box-sizing workaround (handled by the DS).

- **fix**(test-settings): gate required-fields validation on the publish transition only (2026-05-27) [`62ccafb1622ea6cdaee6fca9e680f6895fbb5356`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/62ccafb1622ea6cdaee6fca9e680f6895fbb5356)
  PUT /api/tests/:id ran the required-fields validation whenever the resulting
  status was "published", so editing an already-published test re-tripped the gate
  on every save and rolled back the whole transaction (422). A published test left
  in an incomplete state could never be saved or fixed.
  
  Read the pre-update status and run _validateAllRequiredFields only on an actual
  draft/archived -&gt; published transition, matching the documented intent
  (PRD-1 §4.3.6 "publish boundary"). Draft saves and re-saves of a published test
  now succeed; the gate still fires when publishing.
  
  Adds a regression test covering the already-published re-save case.

- **fix**(server/utils/excel): bypass exceljs Buffer/ArrayBuffer type clash (2026-05-26) [`c564fbcec902fae6c95b8a60d413ef1a66f73a56`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c564fbcec902fae6c95b8a60d413ef1a66f73a56)
  exceljs/index.d.ts declares a global `interface Buffer extends ArrayBuffer`
  that merges with Node's generic `Buffer&lt;TArrayBuffer&gt;`, making `load`'s
  parameter unmatchable from a real Node Buffer under `@types/node` with
  the resizable-ArrayBuffer shape. Cast to `any` at the call site so tsc
  stops failing; runtime behaviour is unchanged.

- **fix**(prd-7): drawer save — full adaptive payload, error surfacing, close on save (2026-05-26) [`c4daaabee0b4aab62560e707672c0da28e2c0578`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c4daaabee0b4aab62560e707672c0da28e2c0578)
  Several latent issues meant a failed PUT looked successful while a
  successful save left partial data behind:
  
  \- POST/PUT only sent test-level scalars + sections; adaptive settings
    and showDifficultyLevel were dropped, so adaptive edits never
    persisted. buildSavePayload() now attaches both via
    mapEditorAdaptiveToPayload().
  \- Server-side feedback column is string-typed; the structured payload
    has to travel under feedbackJson, not feedback. Rename the typed
    field accordingly.
  \- save() swallowed unexpected HTTP errors (400/5xx). It now returns a
    boolean and exposes saveError so the Drawer can show an inline
    Banner instead of closing silently.
  \- Drawer now closes on successful Save and stamps a warnings flag
    into the react-query cache so the list view can show a triangle-
    alert indicator (consumed in the next commit).
  \- useDesignSettings dirty-check normalizes missing params to {} so a
    freshly-loaded test no longer reports as dirty.

- **fix**(prd-7): add tb-rte/feedback-editor CSS classes + --wf-border-w global fallback (2026-05-26) [`2e3b0bdd8198462e3fe6721832fc73d18e1d409e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2e3b0bdd8198462e3fe6721832fc73d18e1d409e)
  tb-rte, tb-rte__toolbar, tb-rte__area, tb-rte__sep and tb-feedback-editor__*
  classes were missing from tb-components.css, preventing RTE and PDF-asset
  section from rendering. Also promotes --wf-border-w to :root so portal-rendered
  modals (ModalDialog renders outside .tb-tests-list scope) inherit the border
  width variable correctly.
  
  Mirrored to docs/wireframes/tb-components.css.

- **fix**(prd-7): adaptive topic accordion — chev goes LAST inside trigger (after toggle) (2026-05-25) [`b68b0d3757b78439ed6a46832b15c66d55b505ce`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b68b0d3757b78439ed6a46832b15c66d55b505ce)
  Per prd7-editor-settings-tab.html line 4757-4801, the
  `.ou-acc__trigger` &lt;button&gt; contains, in DOM order:
  
    1. tb-status-dot (lead)
    2. ou-acc__trigger-text (title + subtitle)
    3. tb-adaptive-topics__toggle (Switch + «Включено» label)
    4. ou-acc__chev (SVG, last)
  
  Previous impl placed the toggle as a SIBLING of the button (not
  inside) so the chev — which was the last child of the button —
  visually rendered BETWEEN text and toggle, not at the right edge.
  
  Fix: move the toggle inside the trigger button as per wireframe,
  with `onClick={(e) =&gt; e.stopPropagation()}` so flipping the switch
  doesn't expand/collapse the accordion. Chev now sits at the right
  edge as the last element.
  
  Drop the now-unused `.tb-adaptive-topics__head` CSS rule.
  
  133 vitest tests pass; tsc clean.

- **fix**(prd-7): Adaptive topic accordion — status-dot lead + chev + Включено/Выключено label (2026-05-25) [`2a77fd6a5fd9270f070032607d2aa62502160e32`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2a77fd6a5fd9270f070032607d2aa62502160e32)
  Per prd7-editor-settings-tab.html (wf-adaptive state, lines 4756-4801)
  the per-topic accordion row contains:
  1. Leading `tb-status-dot` reflecting validity (FR-17: ≥ 1 level).
  2. Trigger text with title + subtitle in DS wireframe format
     («N вопросов · M уровней · валидно / невалидно»).
  3. Toggle group: Switch + small «Включено» / «Выключено» label.
  4. Trailing chev SVG that rotates when the item is `.is-open`
     (DS .ou-acc__chev styles already provide the rotation).
  
  Before: accordion row only had title + a shorter «X уровней ·
  включено» subtitle + Switch — no status indicator, no chev,
  no label next to switch.
  
  AdaptivePane now passes `questionCount` (= section.maxQuestions)
  to each accordion so the subtitle can show the total per-topic
  question pool.
  
  133 vitest tests pass; tsc clean.

- **fix**(prd-7): hide «Адаптивный режим» rail item when test mode is standard (2026-05-25) [`d5f3699a39bf529b226f18981ff099771251d25d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d5f3699a39bf529b226f18981ff099771251d25d)
  Per requirements: the «Адаптивный режим» sub-section configures
  behaviour that only runs when the test itself is in adaptive mode.
  Showing it in standard mode is meaningless — the warning banner
  that previously occupied that pane («Сейчас режим теста —
  стандартный...») was a UX workaround, not the right design.
  
  Changes:
  \- SettingsSection: filter «adaptive» out of the rail when
    `model.mode === "standard"`. If it was the active pane (e.g.,
    the user just toggled the mode in Основное), fall back to
    «Основное» automatically.
  \- AdaptivePane: drop the «warning when mode!=adaptive» Banner —
    it's no longer reachable because the parent guards the render.
  \- Tests: replace the 5-rail assertion with a 4-rail standard
    assertion plus a 5-rail adaptive assertion; rewrite the
    Адаптивный режим pane test block to render with `mode: "adaptive"`
    (otherwise the pane no longer mounts). Drop the two banner tests.
  
  133 vitest tests pass; tsc clean.

- **fix**(prd-7): Branding color params use ui-kit ColorPicker, not raw text input (2026-05-25) [`86ab7f0b06507cd56ad5bbe4da6345f27c458b5b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/86ab7f0b06507cd56ad5bbe4da6345f27c458b5b)
  Per prd7-design-tab.html (wf-branding state, lines 446-456):
  
  ```
  &lt;button class="ou-color-trigger" aria-expanded="false" ...&gt;
    &lt;span class="ou-color-trigger__swatch" style="--swatch: #0066CC;"&gt;&lt;/span&gt;
    &lt;span class="ou-color-trigger__value"&gt;#0066CC&lt;/span&gt;
  &lt;/button&gt;
  ```
  
  i.e. color params are a DS-styled trigger button (swatch + value
  label) that opens a palette, NOT a plain text input. The
  `@universityrt/ui-kit` `ColorPicker` component implements this.
  
  Migration:
  \- design-section.tsx ParamRow color branch: replace `Input type=text`
    with `ColorPicker` (HEX value, opens a palette on click).
  \- Add color-format.ts utility for round-tripping between the
    picker's HEX format and the template manifest's mixed formats:
    most built-in templates store HSL `"H S% L%"` (default/corporate/
    minimal); rtk-storyline stores HEX `"#rrggbb"`. Detect format on
    read, convert to HEX for the picker, convert back on save —
    preserving the template author's chosen serialization.
  
  134 vitest tests pass; tsc clean.

- **fix**(prd-7): tab status dot — DS-styled 8×8 inline, not inside ou-tabs__badge pill (2026-05-25) [`40ed0b92f8b05f64b761378edcc4044024a7e56b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/40ed0b92f8b05f64b761378edcc4044024a7e56b)
  Per prd7-editor-drawer.html / prd7-structure-*.html, the
  dirty/warn/error indicator on an editor tab is a small inline dot
  rendered NEXT to the tab text (8×8, var(--ou-space-2)), NOT inside
  the `.ou-tabs__badge` pill (which is 18×18 with bg-page background
  and is intended for counts/labels).
  
  Two issues fixed:
  \- The `.status-dot` class wasn't defined in our app CSS — only in
    wireframe-local &lt;style&gt;. Port the wireframe definition into
    tb-components.css using DS tokens (--ou-accent-default for dirty,
    --ou-warning-default for warn, --ou-error-default for error).
  \- The ui-kit Tabs `badge` prop wraps its child in `.ou-tabs__badge`.
    Switch to composing the dot directly into the tab `label` so it
    renders inline next to the text without the pill background.
  
  134 vitest tests pass; tsc clean.

- **fix**(prd-7): hide tab badge pill on clean tabs (2026-05-25) [`ae4221daaebe6b50434b766e1421abd19f32750e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ae4221daaebe6b50434b766e1421abd19f32750e)
  ui-kit Tabs renders &lt;span class="ou-tabs__badge"&gt; whenever the
  `badge` prop is non-null. The badge pill has min-width:18px /
  height:18px / bg-page background, so it appears as a visible
  circle even when its child node is null — which produced an
  empty dot on every editor tab regardless of dirty state.
  
  Fix: only set the `badge` prop when the tab actually has
  non-clean state (error / warning / dirty); pass `undefined` for
  clean tabs so the pill is not rendered at all.
  
  134 vitest tests pass; tsc clean.

- **fix**(prd-7): unify Design save into drawer footer (remove per-pane DesignSaveBar) (2026-05-25) [`6ddd2bf39a7881a17df73c16a0a2ebafa70ff9ad`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ddd2bf39a7881a17df73c16a0a2ebafa70ff9ad)
  Per wireframe prd7-design-tab.html — design tab footer shows the same
  single «Сохранить» button as other tabs, NOT a separate per-pane
  save bar. Refactor so the drawer footer drives both test-settings
  and design-settings save:
  
  \- Hoist `useDesignSettings` from DesignSection up to TestEditorView.
    DesignSection accepts an optional `design` prop; falls back to its
    own hook call when used standalone (component tests, future
    embeds).
  \- Combine isDirty / isSaving across editor + design; drawer footer
    «Сохранить» enables on either-dirty and persists both drafts
    sequentially (test settings first for `expectedVersion` ordering,
    design after via its independent endpoint).
  \- Combined «Отменить»/«Закрыть» triggers also revert design draft.
  \- DesignSaveBar removed; replaced with a minimal inline
    `DesignSaveError` banner that only surfaces failures (action
    controls now live in drawer footer).
  
  Tests adapted: per-pane `design-save` button assertions dropped;
  draft mutation verified through input value / switch checked
  state / select trigger label. The save-flow test wraps the
  section in a small `Harness` component that mirrors the parent
  pattern (hoisted hook + hidden test-only save trigger).
  
  134 vitest tests pass; tsc clean.

- **fix**(prd-7): Design Template pane — tpl-block + preview-sketch thumb per wireframe (2026-05-25) [`88d3435ba3bf988cf5e9717b82743209e372fdc6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/88d3435ba3bf988cf5e9717b82743209e372fdc6)
  Port wireframe-local CSS (tpl-block, tpl-thumb, tpl-info, tpl-name,
  tpl-desc, tpl-actions, tpl-preview-btn + preview-sketch and its
  ps-* schematic sub-classes) from prd7-design-tab.html to
  tb-components.css; rewrite TemplatePane in design-section.tsx to
  use the wireframe markup:
  
  \- Replace ui-kit Card with `.tpl-block` row layout (thumb left,
    info right, preview-icon top-right).
  \- Render a 160×90 schematic `preview-sketch` (header + progress +
    sidebar + question + 3 options) with the template's first 3 chars
    as a faux logo title.
  \- Eye-icon preview button (lucide Eye) wired to a stub alert until
    the full preview modal ships.
  \- Tags (Встроенный / v…) now live in `.tpl-info__head` next to the
    name instead of CardHeader's subtitle slot.
  
  134 vitest tests pass; tsc + check:wireframes:ds clean.
  
  Browser baseline note: `color-mix(in srgb, …)` requires Chrome ≥ 111
  and Safari iOS ≥ 16.2 — identical to upstream wireframe CSS.

- **fix**(prd-7): AdaptiveLevelCard — status-dot lead + collapse chev per wireframe (2026-05-25) [`9331adf14e2471cf7b525cd120c475d1c026ce2d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9331adf14e2471cf7b525cd120c475d1c026ce2d)
  Per prd7-editor-settings-tab.html (wf-adaptive state) level card has:
  1. Status-dot leading element (tb-status-dot--ok/err) reflecting
     validity of the level configuration (min ≤ max, questions ≥ 1,
     threshold in bounds; richer rules deferred to FR-17 follow-up).
  2. Trail action group containing both the trash button AND a chev
     button (tb-level-card__chev) that collapses the card body.
  3. Subtitle ends with «валидно»/«невалидно» mirroring the dot state.
  
  Card adds is-collapsed modifier; CSS already in tb-components.css
  (tb-status-dot, tb-level-card__chev, .is-collapsed → rotate -90°).
  
  134 vitest tests pass; tsc clean.

- **fix**(prd-7): align Structure tab markup with wireframe (flow-mode-bar, zone-block, page-row) (2026-05-25) [`07d293bbca6090a1d78690738f0e116318095c03`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/07d293bbca6090a1d78690738f0e116318095c03)
  Port wireframe-local CSS from prd7-structure-linear-flat.html to
  tb-components.css as DS-extensions per wf-only-skeleton-frame rule,
  and rewrite start-pages-section.tsx to use them:
  
  \- Replace heavyweight `Banner` flow-mode notice with a thin
    `.flow-mode-bar` (label + hint + small Layout icon).
  \- Replace custom `tb-structure-zone` with wireframe's `zone-block`
    (dashed border + uppercase-caption zone-header).
  \- Replace per-topic `tb-structure-zone--topic` with `topic-block`
    (elevated card with topic-header showing name + question count).
  \- Replace `tb-page-row` with `page-row`/`page-variant-badge`/
    `page-title`/`page-actions`; per-row meta tags use `.page-row__meta`
    with auto-derived severity stripe (info/warn/error).
  \- Replace inline «Удалить» button with a dots-icon `MenuTrigger`
    from ui-kit; «Удалить» moves into the menu, confirm prompt stays
    inline. Menu portals so its popup escapes overflow:hidden zones.
  \- Carry `page-row--questions` + page-icon (lucide HelpCircle) for
    the system questions stream row.
  
  Tests adapted for dots-menu flow: open via
  `structure-page-actions-{id}`, then click `structure-page-delete-{id}`.
  
  134 vitest tests pass; tsc + check:wireframes:ds clean.

- **fix**(prd-7): wireframe-correspondence pass for editor — footer / required toggle / limits / level remove (2026-05-25) [`979d224ba9fb19da83ef0306201f9c3a2f219ad6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/979d224ba9fb19da83ef0306201f9c3a2f219ad6)
  Per HARD wireframes-first rule, align editor markup with approved
  wireframes (prd7-editor-drawer.html, prd7-editor-settings-tab.html,
  prd7-design-tab.html):
  
  \- test-editor.tsx footer: clean state shows ghost «Закрыть» +
    primary «Сохранить»; dirty state shows ghost «Показать изменения»
    \+ secondary «Отменить» + primary «Сохранить» (was: always
    secondary «Закрыть»).
  \- basic-settings-section.tsx PassRules per-topic «Обязательная»
    column: use Switch from ui-kit (per wireframe role="switch")
    instead of bare checkbox.
  \- basic-settings-section.tsx Limits pane: reorder per wireframe
    (Максимум попыток before Лимит времени теста); rename time-limit
    label to «Лимит времени теста» + suffix «минут».
  \- basic-settings-section.tsx AdaptiveLevelCard remove button: use
    Trash2 icon (per wireframe i-trash) instead of «×» literal.
  \- design-section.tsx «Заменить шаблон» button: add Layout leading
    icon (per wireframe i-layout).
  
  134 vitest tests pass; tsc clean.
  
  Deferred (functional/structural, not addressed in this pass):
  \- Adaptive level «valid/invalid» status dot + per-level collapse
    toggle (requires validation logic).
  \- Adaptive topic distribution histogram (requires question-difficulty
    data aggregation).
  \- Structure tab full rewrite: flow-mode-bar, zone-block, page-row
    with drag-handle/expand-toggle/dots-menu (large CSS+JSX port from
    wireframe — separate ticket).
  \- Design Template pane preview-sketch thumbnail (separate ticket).

- **fix**(wireframes): close DS-gate violations across approved files (2026-05-21) [`c39ca43bc08a941c972777b34186d1a2ecb74ba3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c39ca43bc08a941c972777b34186d1a2ecb74ba3)
  Зачищаем 21 direct-unit / named-color violation, найденную линтером
  `check:wireframes:ds`. Источники нарушений — мостовые fallback'и
  `var(--token, 360px)`, hardcoded `10px` font-size аннотаций, `8px`
  status-dot размеры, литералы в комментариях и фиксированные ширины
  формы в tb-components.
  
  Подход: ввели недостающие wireframe-токены `--wf-size-360` и
  `--wf-border-w-2` в `prd7-shared.css`; добавили локальные `--tb-*`
  токены (`--tb-input-w-md`, `--tb-input-w-sm`, `--tb-accent-rail-w`,
  `--tb-space-half`) в обе копии `tb-components.css` под gate-skip
  блоком; `@container (max-width: 720px)` обёрнут отдельным gate-skip
  с пояснением, что var() в container-queries не работает в
  production-браузерах.
  
  Линтер: 0 violations, 17 файлов passed.

- **fix**(wireframes): extend severity rail over mapping-warn block + unify save button (2026-05-21) [`b9c551743363299659338b00b334b2c2bee2ceeb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b9c551743363299659338b00b334b2c2bee2ceeb)
  s-mapping in linear_by_topics had two issues visible in the latest review:
  
  1. The yellow severity stripe stopped at the page-row, leaving the
     `.mapping-warn` block below it (with the variant-replacement select) with
     no stripe. The mapping-warn block is the row's continuation — it asks the
     user to choose a replacement for the missing variant — so the stripe must
     cover both. Added `.page-row + .mapping-warn` rules mirroring the existing
     `.page-row + .page-row-expand` ones. Also reset mapping-warn's own
     horizontal margin when it follows a severity row, so the stripe sits flush
     with the row above (single rail, no offset).
  
  2. The footer primary button read «Применить шаблон» — wireframe-specific
     text that broke the convention used by every other Drawer state. Renamed
     to «Сохранить» (matches FR-25a) and secondary «Отмена» → «Отменить» for
     tense consistency with the rest of the file.
  
  Verified in Playwright: yellow rail now runs continuously from the page-row
  through the mapping-warn block; footer shows «Отменить» / «Сохранить».

- **fix**(wireframes): extend severity stripe over expanded page-row in linear modes (2026-05-21) [`ed4b81b1f0110ef3495941cd38c3ddd5c81a2840`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ed4b81b1f0110ef3495941cd38c3ddd5c81a2840)
  The severity border-left was only painted on the page-row itself in linear_flat
  and linear_by_topics — when the row was expanded, the .page-row-expand block
  below it had no stripe, breaking the visual continuity of the signal. Worst
  case: a validation row showed a red stripe at the top, then a wide colourless
  expand body with red banners and error-fields inside it — the row looked
  disconnected from the issue it was about.
  
  Add severity-stripe rules for .page-row + .page-row-expand combinations in
  both linear files (router already had this). Cascade picks up the same colour
  as the row itself: red for --error / ou-tag--error, yellow for --warn /
  ou-tag--warning, blue for ou-tag--info. Without a severity trigger on the row
  above, expand stays plain (no stripe).
  
  Verified in Playwright: s-validation in linear_by_topics now shows a
  continuous red stripe from the page-row through the error banner, name field
  and content field down to the «Содержимое не может быть пустым» error message.

- **fix**(wireframes): hide «Удалить» from row-menu of system pages (not disabled) (2026-05-21) [`5d180a7e6e41d792f2664c45ecfd685f03641534`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5d180a7e6e41d792f2664c45ecfd685f03641534)
  System page-rows (kind: router / summary / intro / questions) cannot be
  removed manually — the previous «show as disabled» pattern was visual noise.
  Drop the menu item (and its preceding separator) entirely for system kinds,
  keep it only for user-created info-rows.
  
  s-row-menu-open router-row dropdown now shows just two items:
    \- Сменить вариант…
    \- Предпросмотр
  The info-row in «После теста» still shows the full menu including a
  destructive «Удалить» — that's the active demonstration of the contrast.
  
  Notes table + checklist §11 updated to match the new behaviour.

- **fix**(wireframes): align severity-border colors with DS info/warning/error tokens (2026-05-21) [`d99c78da9f269c503d6a69600c056d518c13495a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d99c78da9f269c503d6a69600c056d518c13495a)
  Three corrections to the severity-border system introduced in the previous
  commit:
  
  1. Info border colour: switched from `--ou-accent-default` (purple, the brand
     accent) to `--ou-info-default` (blue, matches the DS info palette and
     `ou-tag--info` content colour). Border and tag now read as one signal.
  
  2. Page-row-expand continuity: the expand body now inherits its left-border
     colour from the row above's severity, so an info row + its expand share a
     blue stripe, a warning row gets yellow continuation, a validation row red.
     Without a severity trigger the expand stays plain.
  
  3. Split `.page-row--warn` (yellow, warning — used for mapping prompts) from
     `.page-row--error` (red, validation/blocking errors):
     \- `s-mapping` row in linear_by_topics keeps `--warn` → yellow stripe;
     \- `s-validation` row in router renamed `--warn` → `--error` → red stripe;
     \- `s-validation` row in linear_by_topics now carries `--error` (was bare,
       so the row above the validation banner had no signal — fixed).
  
  Verified across:
  \- linear_flat s-main: questions-row → blue border + blue info tag ✓
  \- linear_by_topics s-mapping: page-row → yellow border ✓
  \- linear_by_topics s-validation: page-row → red border ✓
  \- router s-main-multi-variant / s-main-fallback / s-validation: blue / yellow / red ✓
  \- router default states: no borders (signal-only attention) ✓

- **fix**(wireframes): replace «system» accent border with severity-based border (2026-05-21) [`fdd3c2b56874090b8a991984b1d68b41787da24d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fdd3c2b56874090b8a991984b1d68b41787da24d)
  The accent (blue) border-left on every `.page-row--system` was visual noise:
  it appeared on rows that didn't need attention (system pages in their default
  state) just because they were attached by the system, not because there was
  anything to draw the user to.
  
  New semantics — border-color reflects severity of the row's state, not its
  ownership:
  
  | Border colour | Trigger                                                |
  |---------------|--------------------------------------------------------|
  | (none)        | normal — no meta tags, nothing to attend to            |
  | Blue          | ou-tag--info in .page-row__meta (info available)       |
  | Yellow        | ou-tag--warning in meta (fallback, conflict)           |
  | Red           | ou-tag--error in meta OR .page-row--warn (validation)  |
  
  Implementation via CSS :has() — border colour is auto-derived from the
  highest-severity meta tag present, with priority error &gt; warning &gt; info.
  No per-row modifier needed; markup stays clean.
  
  Side changes:
  \- `.page-row--system` becomes a documentation-only class (no CSS rule);
    it stays in the markup as a semantic marker that the row is system-bound.
  \- `.page-row--warn` keeps the name for back-compat with s-validation but now
    produces a red border + error-on-soft title (matches «error» semantics).
  \- The old per-row warning border-color rule in router (was using
    --ou-warning-default) is dropped — severity rule handles it now.
  
  Verified in Playwright across all router states:
  \- s-main / s-row-menu-open: no borders (default state) ✓
  \- s-main-multi-variant: blue borders on rows with «Доступно N вариантов» ✓
  \- s-main-fallback: yellow borders on rows with «Из стандартного шаблона» ✓
  \- s-validation: red border on the router-row, red status-dot on the tab ✓
  \- linear_flat / linear_by_topics: only questions-row with info tag in s-main
    gets a blue border; default page-rows stay unmarked.

- **fix**(wireframes): unify page-row actions to single «…» menu in linear modes (2026-05-21) [`342957f47ddb31a4f75d6612e9ebcf0040cdf567`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/342957f47ddb31a4f75d6612e9ebcf0040cdf567)
  Bring linear_flat and linear_by_topics in line with the variant.kind row-menu
  contract (PRD-1 §4.3): every editable page-row now exposes a single
  `ou-iconbtn` with `i-dots` instead of separate Eye + Trash buttons. The
  dropdown content (with «Сменить вариант» / «Предпросмотр» / «Удалить») is
  demonstrated in `prd7-structure-router.html#s-row-menu-open`.
  
  Touched ~10 page-row instances across both linear files:
  \- linear_flat: the «Правила прохождения теста» row in s-main;
  \- linear_by_topics: «Что такое информационная безопасность», «Итоги раздела»
    and equivalents across s-main / s-mapping / s-dirty-form / s-validation /
    s-sanitize / s-delete / s-dnd / s-dnd-topics. Replace_all handled both
    14-space and 16-space indent variants and the Trash-only variant.
  
  Intentionally left untouched:
  \- s-readonly: keeps the single Eye iconbtn (read-only preview, no actions);
  \- s-page-preview / -aa modal frame label icons;
  \- delete-confirm modal danger icon.
  
  Checklist housekeeping:
  \- §8 (linear_by_topics): 5 «variant.kind» dependencies marked [x];
  \- §9 (linear_flat): 2 dependencies marked [x];
  \- Final Sign-off: «mobile (&lt;960px)» moved out of scope — covered by a future
    PRD; only desktop (1440px) acceptance applies to §8/§9/§11.
  
  Verified in Playwright at 1600×1100 for both linear files: editable rows show
  a single `…` button, no Eye/Trash artefacts remain.

- **fix**(wireframes): unify zone padding across all flowModes + allow insert after summary (2026-05-21) [`214ba8b00a590124d5acea08350d471724a216d0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/214ba8b00a590124d5acea08350d471724a216d0)
  Two related visual fixes across linear_flat / linear_by_topics / router:
  
  1. Unified padding for «Внутри теста» between flowModes — router was using
     var(--wf-space-14) (14px) while linear modes were on var(--ou-space-4)
     (16px). Aligned router .inside-test__body to var(--ou-space-4); also
     added the same .zone-block &gt; .topic-body rule that linear files use so
     the «До теста» / «После теста» zones in router get the same inset.
  
  2. Added a trailing insert-row after the system summary page in every
     editable «После теста» zone:
     \- linear_flat / linear_by_topics: one «+ Добавить страницу» row right
       after the «Итоговая страница» summary in s-main / s-empty-pages /
       s-mode-change (all editable states; s-readonly intentionally skipped);
     \- router: same after «Итоговая страница теста» in s-main and
       s-row-menu-open.
  
  Visually verified in Playwright at 1600×1100:
  \- All three modes show identical dashed zones with consistent inner card
    widths (cards no longer touch the dashed border).
  \- Authors can now add a custom page below the summary in every flowMode.

- **fix**(wireframes): inset child cards from zone-block dashed border in linear modes (2026-05-21) [`d42a7793c25f70c5851fce319a11fb6d4333103a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d42a7793c25f70c5851fce319a11fb6d4333103a)
  The page-row / topic-block cards inside .zone-block (До/Внутри/После теста)
  had their borders touching the dashed border of the surrounding zone. With
  the .topic-body wrapper having zero horizontal padding, child cards spanned
  the full inner width of the zone — visually the two borders fused.
  
  Add `.zone-block &gt; .topic-body { padding: var(--ou-space-2) var(--ou-space-4); }`
  to both linear_flat and linear_by_topics. The `&gt;` combinator only targets the
  zone's direct body wrapper, so per-topic `.topic-body` (inside individual
  `.topic-block` cards) keeps its existing padding.
  
  Also drop a previous over-engineered attempt that turned topics into borderless
  "sections" inside .zone-block--topics — that changed too much. The simple
  inset padding is enough; topic cards keep their normal card look but now sit
  inside the zone with breathing room from the dashed border.
  
  Verified in Playwright: questions-row in linear_flat and topic-block cards in
  linear_by_topics both show a clear visual gap to the zone border on left/right.

- **fix**(wireframes): move multi-variant / fallback hints to dedicated meta row (2026-05-21) [`8fdefd4e5da7aed0cb39c38bcd9af6967ff0ff5e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8fdefd4e5da7aed0cb39c38bcd9af6967ff0ff5e)
  The inline «Доступно N вариантов» and «Из стандартного шаблона» badges were
  crammed into the same flex line as the variant badge + title + actions,
  making the row visually noisy and pushing the title sideways.
  
  Restructure each row into a header line + an optional second line carrying
  the meta hint as a proper DS ou-tag:
  \- info-coloured tag for «Доступно N вариантов» (i-info icon);
  \- warning-coloured tag for «Из стандартного шаблона» (i-warn icon).
  
  The new `.page-row__meta` block uses `flex-basis: 100%` so it wraps onto its
  own line within the flex-wrap'd `.page-row`. Left margin aligns the tag with
  the variant badge above; on `.page-row--system` the offset compensates for
  the accent border.
  
  Removed the now-unused `.multi-variant-hint` / `.fallback-warn` custom
  classes — DS tags replace them across all states (s-main-multi-variant,
  s-main-fallback, s-row-menu-open).

- **fix**(wireframes): wire up showState + toggles for router and variant-replace (2026-05-21) [`ac7902e39d78f4d3f950a1a16bf9b3bb4d72830c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ac7902e39d78f4d3f950a1a16bf9b3bb4d72830c)
  The two wireframes added in this session referenced `showState`,
  `toggleTheme`, `toggleDensity`, `toggleAnnots` from onclick handlers but
  never defined them — the state-switcher buttons were inert. Every other
  wireframe ships these helpers inline; the router rewrite (created from
  scratch) and the new variant-replace modal both inherited the gap.
  
  Add the canonical inline `&lt;script&gt;` (mirroring approved/prd7-tests-list.html):
  \- resolveState() + showState() with .wf-state / .shell class toggling
  \- toggleTheme / toggleDensity / toggleAnnots
  \- page-row expand chevron handler (router only — variant-replace has no rows)
  \- updateWireframeNavOffset on resize/ResizeObserver
  
  Side cleanup in variant-replace: extract the three inline
  `style="margin-bottom: var(--ou-space-3);"` modal intros into a
  reusable .modal-intro class.

- **fix**(wireframes): remove tag-group chips from mobile footer, sync with desktop (2026-05-18) [`cedb24ce8018aeba068a629a6dbc6e5a5117935b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cedb24ce8018aeba068a629a6dbc6e5a5117935b)

- **fix**(wireframes): add Изменено status tag before tab chip in mobile footer (2026-05-18) [`7d69e93668f3fcf54ba57998cb54e9f9c3bc7fc7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7d69e93668f3fcf54ba57998cb54e9f9c3bc7fc7)

- **fix**(wireframes): raise ou-tabs z-index above saving-overlay to prevent tab strip clipping (2026-05-18) [`cc9e6235732c9ae85c620ffa5093e1df58b1659a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cc9e6235732c9ae85c620ffa5093e1df58b1659a)

- **fix**(wireframes): restore saving-overlay coverage broken by wf-annot-wrap--block position:relative (2026-05-18) [`52d57c6bcaa5cf02e4820ef1fd7ee021d89824b9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/52d57c6bcaa5cf02e4820ef1fd7ee021d89824b9)

- **fix**(wireframes): apply bg-page template pattern and fix missing shared CSS links (2026-05-18) [`d68702736c3dd3f09a5ac60e0eb840b2f9f1d5ed`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d68702736c3dd3f09a5ac60e0eb840b2f9f1d5ed)
  \- prd7-editor-conflict.html, prd7-editor-settings-tab.html: add missing
    prd7-shared.css link (audit blocker)
  \- prd7-editor-drawer.html: extract repeated bg-page HTML into &lt;template
    id="tpl-bg-page"&gt; injected via injectBgPage() IIFE; add .wf-tpl-slot
    CSS class to avoid inline display:contents; consistent with pattern
    already used in prd7-design-tab.html and prd7-structure-linear-by-topics.html

- **fix**(wireframes): resolve CSS/HTML linter warnings in prd7-editor-drawer (2026-05-18) [`2f7342deb2f490687832b7a1e9aace01459c3510`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2f7342deb2f490687832b7a1e9aace01459c3510)
  \- Add standard line-clamp alongside -webkit-line-clamp (vendor prefix warning)
  \- Replace style="display:none" on SVG sprite with .wf-svg-sprite class
  \- Remove redundant style="position:relative" from .changes-anchor (already in CSS)

- **fix**(wireframes): include FR/NFR codes table in Пояснения modal (2026-05-18) [`92071af765cb2b102d35f5432a016e0094140419`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/92071af765cb2b102d35f5432a016e0094140419)
  openNotesModal() now appends a third section «Коды требований FR / NFR»
  by cloning the first notes-box from #s-notes. Users can see an annotation
  badge on an element (e.g. FR-25a) and immediately look up its meaning
  without switching to the notes state.

- **fix**(wireframes): switch all asset refs to root-relative paths (2026-05-18) [`dba7c234c027825e19aa932ac5c413c9464b0b77`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dba7c234c027825e19aa932ac5c413c9464b0b77)
  Wireframes previously used relative paths (ds/university-rt.css,
  prd7-shared.css, wf-links.js) that broke when files were moved into
  docs/wireframes/approved/. Changed to /docs/wireframes/… so any file
  can be placed in any subdirectory and serve correctly without code edits.

- **fix**(wireframes): remove duplicate Сохранение tag from s-saving footer (2026-05-17) [`814b4c9e0a5fcae008e92f7afb14803873c6d352`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/814b4c9e0a5fcae008e92f7afb14803873c6d352)

- **fix**(wireframes): add horizontal padding to drawer tabs nav to align with head/body (2026-05-17) [`6d83c88527b7c6e830ad67d804e39a1c3d309406`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6d83c88527b7c6e830ad67d804e39a1c3d309406)

- **fix**(wireframes): add missing feedback section to Сетевая безопасность in s-dirty, s-error, s-changes states (2026-05-17) [`1209a5de2ef3cb31188709c760db8acb4251e2cc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/1209a5de2ef3cb31188709c760db8acb4251e2cc)

- **fix**(wireframes): remove redundant changed-section tags from drawer footers (2026-05-17) [`85347302157143ea15de9cb7cf22399b210a23af`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/85347302157143ea15de9cb7cf22399b210a23af)

- **fix**(wireframes): remove redundant form-hint from drawer topic section (2026-05-17) [`2a5dd1ca7d10c71e6c7b7daa856bc5dca6140356`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2a5dd1ca7d10c71e6c7b7daa856bc5dca6140356)

- **fix**(wireframes): improve prd7-editor-drawer topic cards and header status tag (2026-05-17) [`92c05a2ce9ee0a39363204abd4187ca676bbdba4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/92c05a2ce9ee0a39363204abd4187ca676bbdba4)
  \- topic-row migrated to DS tokens (ou-border-default, ou-radius-l, ou-shadow-sm)
    replacing wf-* variables; topics now visually separate as cards
  \- remove 'Изменений нет' ou-tag from s-default footer — nothing shown when clean
  \- Опубликован: add ou-tag--outline for visible green border vs barely-tinted soft bg

- **fix**(wireframes): add missing prd7-shared.css link to 11 wireframe files (2026-05-17) [`b0b094990775436f68e2e66ab7f7b5e0231566a3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b0b094990775436f68e2e66ab7f7b5e0231566a3)
  Without prd7-shared.css, --wf-space-* tokens were undefined causing
  SVG icons (e.g. wf-feedback-meta) to render at natural unconstrained size.

- **fix**(wireframes): adopt new DS ou-choice-card (4a-gap closed) (2026-05-17) [`057bee7e4db37c033b38ab8ee51ca16a9e6b0926`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/057bee7e4db37c033b38ab8ee51ca16a9e6b0926)
  Pick up the new DS primitive ChoiceCard from ui-kit (handbook commit
  adds .ou-choice-card to css/university-rt.css and React component
  ChoiceCard / ChoiceCardGroup / ChoiceCardInset) and use it in place of
  the previous wireframe-local modal-option-* / modal-inset pattern.
  
  Local DS copy:
  \- docs/wireframes/ds/university-rt.css — sync .ou-choice-card-* rules.
  
  Wireframe migration (prd7-tests-list.html):
  \- s-folder-delete-a / s-folder-delete-b modals replaced
    &lt;div class="modal-option ..."&gt;…&lt;/div&gt; with the canonical
    &lt;label class="ou-choice-card …"&gt;&lt;input … class="ou-choice-card__input"&gt;…
    &lt;/label&gt; structure inside an &lt;fieldset class="ou-choice-card-group"&gt;.
  \- Destructive variant uses ou-choice-card--destructive (red border / soft
    background on selected); inset content (move-to select, confirmation
    input) wrapped in &lt;span class="ou-choice-card__inset"&gt;.
  
  CSS cleanup:
  \- Drop dead local rules .modal-option / -icon / -title / -sub / -danger
    and .modal-inset / .modal-warn from &lt;style&gt; block.
  
  Gate:
  \- modal-option-* and modal-inset re-added to legacyClassTokens as a
    regression guard now that DS provides the replacement.
  
  Audit:
  \- 4a-gap entry marked closed with the migration path recorded.
  
  Gate (base + strict-inline): still passes 0/22.

- **fix**(wireframes): migrate wf-* product controls to ou-field / ou-textarea (stage 3.3d) (2026-05-17) [`c02291c5be7ee093f19234867b4ed82487c39405`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c02291c5be7ee093f19234867b4ed82487c39405)
  The wf-* prefix is reserved for wireframe meta (state switcher, notes,
  DS Mapping). Nine files used it to disguise product input/select/textarea
  styling. Migrate all of them to the DS BEM structure and lock the gate
  against regressions.
  
  Class migrations (input / select / textarea wrapped in ou-field/__box or
  ou-textarea/__box; control gets ou-field__input or ou-textarea__input):
  \- wf-text-input [--m / --error]   -&gt; ou-field--m/s [ou-field--error]
  \- wf-textarea  [wf-code-textarea] -&gt; ou-textarea--m
  \- wf-select   [wf-select--full]   -&gt; ou-field--m [ou-field--full]
  \- wf-num-input [--error]           -&gt; ou-field--s [ou-field--error]
  \- wf-level-name-input              -&gt; ou-field--m
  \- wf-failure-input                 -&gt; ou-textarea--s
  \- wf-json-textarea                 -&gt; ou-textarea--m
  \- wf-link-input                    -&gt; ou-field--m
  \- wf-format-select                 -&gt; ou-field--m
  \- wf-flow-select                   -&gt; ou-field--m
  \- wf-type-select                   -&gt; ou-field--s
  \- wf-field-input [--wide/--narrow/--error]
                                     -&gt; ou-field--m/s [ou-field--error]
  \- wf-tbl-input-{s,m,l,num}         -&gt; ou-field--s/m
  \- wf-tbl-textarea                  -&gt; ou-textarea--s
  \- wf-tbl-select [--w]              -&gt; ou-field--s
  \- wf-mob-input [--narrow]          -&gt; ou-field--m/s
  \- wf-mob-settings-select           -&gt; ou-field--m
  \- wf-mob-settings-selector         -&gt; ou-formfield (grouper wrapper)
  \- wf-draw-count-input              -&gt; ou-field--s
  \- draw-count-input                 -&gt; ou-field--s
  
  Files changed:
  \- docs/wireframes/prd7-editor-drawer.html        (11 controls)
  \- docs/wireframes/prd7-editor-mobile.html        (9)
  \- docs/wireframes/prd7-editor-settings-tab.html  (38)
  \- docs/wireframes/prd7-editor-status-indicators.html (3)
  \- docs/wireframes/prd7-mode-switch-warning.html  (1)
  \- docs/wireframes/prd7-section-adaptive.html     (33)
  \- docs/wireframes/prd7-section-basic-feedback-editor.html (11)
  \- docs/wireframes/prd7-section-basic-states.html (2)
  \- docs/wireframes/prd7-section-basic.html        (3)
  \- scripts/check-wireframes-ds.mjs (add wf-*-input/select/textarea names
    to legacyClassTokens with explicit comment)
  \- docs/wireframes-ds-audit.md (close 3.3c-deferred, add 3.3d entry)
  
  Note: local &lt;style&gt; rules for the migrated wf-* classes are now dead
  code but remain in the files; deletion is a separate cosmetic cleanup
  and does not affect the gate.
  
  Gate (base + strict-inline): still 0 violations on all 22 files.

- **fix**(wireframes): finish inline + style-block raw values cleanup (stage 4b/4c/4d) (2026-05-17) [`13d95686710d54e25284542e5f90976a68482c06`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/13d95686710d54e25284542e5f90976a68482c06)
  Bring the strict-inline gate down to 0 across all 22 wireframe files.
  
  Stage 4b — inline raw colors (98 -&gt; 0):
  \- Tag color swatches, preview sketches, gallery previews, color preset
    palettes and preview-flow steps with data-wf-demo.
  \- Extend the gate to honour data-wf-demo as an ANCESTOR whitelist
    (stream-scan tags into a stack, mark open-to-close intervals).
  \- Replace recurring chrome hsl() values with --ou-* tokens.
  
  Stage 4c — spacing tokens (1842 style + 715 inline direct units -&gt; few):
  \- Bulk rewrite the spacing scale 4/8/12/16/20/24/32/40/48/64/80/96 px
    to var(--ou-space-N) inside both inline style="..." and &lt;style&gt; blocks
    across every prd7-*.html, plus prd7-shared.css and ds/wf-utils.css.
  
  Stage 4d — residual values (~120 -&gt; 0):
  \- Add a WIREFRAME-ONLY token layer in prd7-shared.css (--wf-border-w,
    --wf-border-w-1-5, --wf-space-{half,1q,tiny,6,7,9-15}, --wf-size-{...}
    and --wf-radius-pill) inside a /* gate-skip-start ... gate-skip-end */
    pragma, then sweep every Npx in the source files to var(--wf-...).
  \- Replace standalone `white`/`black` with var(--ou-neutral-0)/
    var(--ou-neutral-950); fix the false-positive named-color regex
    (was matching `white-space`).
  \- Exclude `em` from the direct-unit scan (typographic only, no tokens).
  \- Introduce /* gate-skip-start ... gate-skip-end */ pragma support in
    the gate; let the strict-inline scan honour an opt-out region for
    the token definitions themselves.
  
  Files changed:
  \- docs/wireframes/prd7-*.html (16 files)
  \- docs/wireframes/prd7-shared.css (+wireframe-token layer, pragma)
  \- docs/wireframes/ds/wf-utils.css (px -&gt; --ou-space/--wf-* sweep)
  \- scripts/check-wireframes-ds.mjs (demoRanges, skip-pragma, em removal,
    improved named-color lookbehind)
  \- docs/wireframes-ds-audit.md (mark 4b/4c/4d closed)
  
  Gate (base + strict-inline): 1929 -&gt; 0 violations on all 22 files.

- **fix**(wireframes): replace spacing px with --ou-space tokens (stage 4c bulk) (2026-05-17) [`0d453910e86d4f7b5ced57fa820f8ab733c8dc97`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0d453910e86d4f7b5ced57fa820f8ab733c8dc97)
  Bulk replace canonical spacing px values inside both inline style="..."
  attributes and &lt;style&gt; blocks across all prd7-*.html files. Sed targets
  exact word-boundary tokens, so non-spacing values (font-size: 13px,
  custom max-width: 220px, 1px borders, etc.) are left untouched for the
  follow-up pass.
  
  Token map:
     4px -&gt; var(--ou-space-1)
     8px -&gt; var(--ou-space-2)
    12px -&gt; var(--ou-space-3)
    16px -&gt; var(--ou-space-4)
    20px -&gt; var(--ou-space-5)
    24px -&gt; var(--ou-space-6)
    32px -&gt; var(--ou-space-7)
    40px -&gt; var(--ou-space-8)
    48px -&gt; var(--ou-space-9)
    64px -&gt; var(--ou-space-10)
    80px -&gt; var(--ou-space-11)
    96px -&gt; var(--ou-space-12)
  
  Files: 16 prd7-*.html
  
  Gate (strict-inline): 1830 -&gt; 1775 violations.
  Inline direct units: 490 -&gt; 277. Style-block direct units: 1842 -&gt; 1411.
  Inline raw colors: 0 (unchanged). Legacy class: 0 (unchanged).

- **fix**(wireframes): clear inline raw colors via data-wf-demo + tokens (stage 4b) (2026-05-17) [`27d0822934149892559ea2aca64c54ee5d06fdc5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/27d0822934149892559ea2aca64c54ee5d06fdc5)
  Mark demo subtrees (color swatches, template/gallery preview sketches,
  mock SCORM preview drawers, color preset palette, preview flow steps)
  with data-wf-demo and extend the gate to treat the attribute as an
  ancestor-level whitelist (build interval map of demo ranges, skip any
  inline style inside them).
  
  Replace recurring hsl() tokens used in product chrome with --ou-* tokens:
  \- hsl(0 72% 97%)  -&gt; var(--ou-error-soft)        (delete-confirm tint)
  \- hsl(0 72% 80%)  -&gt; var(--ou-error-default)     (delete-confirm border)
  \- hsl(220 15% 90%) -&gt; var(--ou-bg-surface-3)     (disabled badge bg)
  \- hsl(220 15% 45%) -&gt; var(--ou-fg-muted)          (disabled badge fg)
  \- hsl(38 80% 45%)  -&gt; var(--ou-warning-default)  (folder icon color)
  
  Files changed:
  \- docs/wireframes/prd7-design-tab.html        (data-wf-demo + token swaps)
  \- docs/wireframes/prd7-structure-linear-flat.html
  \- docs/wireframes/prd7-structure-linear-by-topics.html
  \- docs/wireframes/prd7-structure-mixed.html
  \- docs/wireframes/prd7-structure-router.html
  \- docs/wireframes/prd7-tests-list.html
  \- scripts/check-wireframes-ds.mjs (demoRanges/inDemoRange, ancestor scan
    with simple HTML tag-stack honoring void elements)
  
  Gate: inline raw colors 98 -&gt; 0. Total: 1929 -&gt; 1830 (-99).

- **fix**(wireframes): clean up legacy_class leftovers across 6 files (stage 4a) (2026-05-17) [`b782bc13fdf09e9ae78301e73ebe2a1df1f7eb19`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b782bc13fdf09e9ae78301e73ebe2a1df1f7eb19)
  Apply the sidebar / topbar / nav / skel / empty / modal / toggle / btn-link
  renames from stages 3.4, 3.9 and 3.3b to all product wireframes (not just
  prd7-design-tab.html). Drop modal-option-* and modal-inset from the gate
  legacy list — DS has no radio-card-in-modal primitive, tracked as a gap.
  
  Files changed:
  \- docs/wireframes/prd7-design-tab.html (.tpl-thumb .skel -&gt; ou-skel)
  \- docs/wireframes/prd7-structure-linear-flat.html (2 toggle blocks)
  \- docs/wireframes/prd7-structure-linear-by-topics.html (4 toggle blocks)
  \- docs/wireframes/prd7-structure-mixed.html (btn-link composite)
  \- docs/wireframes/prd7-structure-router.html (btn-link composite)
  \- docs/wireframes/prd7-tests-list.html (sidebar/nav/topbar/test-title/
    modal-overlay/dialog/title/desc/actions/warn, btn-destructive composite)
  \- scripts/check-wireframes-ds.mjs (drop modal-option-* / modal-inset
    from legacyClassTokens with comment)
  \- docs/wireframes-ds-audit.md (record 4a gap for modal-option pattern)
  
  Class replacements applied:
  \- sidebar/-header/-nav/-footer/-title -&gt; ou-side[/__brand/__nav/__foot] +
    brand-text
  \- nav-item / nav-item active / nav-icon / nav-group-label -&gt;
    ou-side__item [is-active] / ou-side__ico / ou-side__group-title
  \- topbar -&gt; ou-shell__header; test-title -&gt; class dropped
  \- skel / skel-block -&gt; ou-skel
  \- empty-block / empty-title / empty-desc -&gt; ou-empty[/__title/__desc]
  \- modal-overlay/dialog/title/desc/actions -&gt; ou-modal__backdrop / ou-modal
    / ou-modal__title/__desc/__foot
  \- modal-warn -&gt; ou-banner ou-banner--warning
  \- btn-link (in ou-btn composite) -&gt; ou-btn--ghost
  \- btn-destructive (in ou-btn composite) -&gt; ou-btn--destructive
  \- toggle-row / toggle / toggle-thumb blocks -&gt; ou-formfield__inline +
    ou-switch[.is-on] + __input/__track/__thumb
  
  Gate (base): 2177 -&gt; 1929 violations. legacy_class: 248 -&gt; 0.

- **fix**(wireframes): wrap inputs/select/textarea in ou-field/ou-textarea (stage 3.3c) (2026-05-17) [`6ca6aa127853a3b327180f9ad78a24c7ed6ab8e6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6ca6aa127853a3b327180f9ad78a24c7ed6ab8e6)
  Wrap raw &lt;input type="text|number|email|password|url|date|time"&gt;, &lt;select&gt;
  and &lt;textarea&gt; elements in the DS BEM structure:
  \- &lt;input&gt;    -&gt; &lt;div class="ou-field ou-field--m"&gt;&lt;div class="ou-field__box"&gt;&lt;input class="ou-field__input" ...&gt;&lt;/div&gt;&lt;/div&gt;
  \- &lt;textarea&gt; -&gt; &lt;div class="ou-textarea ou-textarea--m"&gt;&lt;div class="ou-textarea__box"&gt;&lt;textarea class="ou-textarea__input" ...&gt;&lt;/textarea&gt;&lt;/div&gt;&lt;/div&gt;
  \- &lt;select&gt;   -&gt; hybrid (native select inside ou-field__box with ou-field__input)
  Error states use ou-field--error / ou-textarea--error and pair with
  ou-formfield__msg ou-formfield__msg--error.
  
  Files changed:
  \- docs/wireframes/prd7-design-tab.html (12 inputs + 2 selects)
  \- docs/wireframes/prd7-structure-linear-flat.html (8 inputs + 2 selects + 7 textareas, plus form-error variant)
  \- docs/wireframes/prd7-structure-linear-by-topics.html (~10 inputs + 2 selects + 8 textareas, plus form-error variants)
  \- docs/wireframes/prd7-structure-mixed.html (5 inputs + 4 textareas + 4 toggle/checkbox blocks, plus form-error variant; toggle blocks rewritten to ou-switch + __input/__track/__thumb)
  \- docs/wireframes/prd7-section-basic-states.html (1 input)
  \- docs/wireframes/prd7-section-basic-feedback-editor.html (2 textareas)
  \- docs/wireframes/prd7-section-start-pages.html (3 inputs + 1 select)
  \- docs/wireframes/prd7-tests-list.html (1 input + 1 select)
  \- docs/wireframes/prd7-shared.css (-20 lines, drop input/select/textarea scaffold)
  \- docs/wireframes-ds-audit.md (mark 3.3b/3.3c closed, log wf-*-input gap)
  
  Class renames also applied in this pass:
  \- form-control / form-control-sm -&gt; ou-field__input (in ou-field--m / --s)
  \- form-input / form-input--error -&gt; ou-field__input (with ou-field--error)
  \- form-textarea -&gt; ou-textarea__input
  \- form-field -&gt; ou-formfield
  \- form-error-msg -&gt; ou-formfield__msg ou-formfield__msg--error
  \- toggle-row / toggle / toggle-slider / toggle-label -&gt; ou-formfield__inline + ou-switch.is-on + __input/__track/__thumb + plain text span
  
  Hybrid &lt;select&gt; approach (native select inside ou-field__box) documented
  as a gap in audit; DS strict pattern uses ou-select__trigger/__menu/__opt
  which is JS-driven and impractical for static wireframes.
  
  Wireframe-only controls (wf-text-input, wf-textarea, wf-num-input,
  wf-tbl-*, wf-mob-*, wf-flow-select, wf-failure-input, wf-link-input,
  wf-format-select, wf-type-select, wf-field-input, wf-draw-count-input,
  draw-count-input) left untouched: they fall outside the gate's legacy
  list and are tracked as a deferred gap in audit.
  
  Gate: 2194 -&gt; 2177 violations. prd7-shared.css: 191 -&gt; 171 lines.

- **fix**(wireframes): migrate empty/skel/toast/dialog blocks (stage 3.9) (2026-05-17) [`ef9e9965d2c594aa28c663474d8b87cf11890621`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ef9e9965d2c594aa28c663474d8b87cf11890621)
  Rename empty-block/empty-title/empty-desc/skel to ou-empty/__title/
  __desc/ou-skel in prd7-design-tab.html. Drop Empty / Skeleton /
  Dialog / overlay / Toast CSS blocks from prd7-shared.css (DS already
  provides ou-empty/ou-skel/ou-modal/ou-toast via ds/university-rt.css).
  
  Files changed:
  \- docs/wireframes/prd7-design-tab.html (17 violations cleared)
  \- docs/wireframes/prd7-shared.css (-58 lines, Separator kept as
    WIREFRAME-ONLY glue under .sep)
  
  Class replacements:
  \- empty-block  -&gt; ou-empty
  \- empty-title  -&gt; ou-empty__title
  \- empty-desc   -&gt; ou-empty__desc
  \- skel         -&gt; ou-skel
  
  Note: modal/dialog/overlay legacy classes were already absent from HTML.
  
  Gate: 2260 -&gt; 2221 violations.

- **fix**(wireframes): migrate banner classes to ou-banner (stage 3.8) (2026-05-17) [`ab199def3c61e5b99ce5e0e9fc7f5e4e04eb2b95`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ab199def3c61e5b99ce5e0e9fc7f5e4e04eb2b95)
  Replace legacy .banner.{warn,error,readonly} and .banner.banner-{warn,error}
  markup with DS ou-banner/--warning/--error in 3 files; drop the matching
  CSS block from prd7-shared.css.
  
  Files changed:
  \- docs/wireframes/prd7-design-tab.html (4 banner blocks + banner-icon)
  \- docs/wireframes/prd7-structure-linear-flat.html (5 banner blocks)
  \- docs/wireframes/prd7-structure-linear-by-topics.html (7 banner blocks)
  \- docs/wireframes/prd7-shared.css (-12 lines)
  
  Class replacements:
  \- banner error               -&gt; ou-banner ou-banner--error
  \- banner warn                -&gt; ou-banner ou-banner--warning
  \- banner readonly            -&gt; ou-banner (neutral default)
  \- banner-icon                -&gt; ou-banner__ico
  \- banner banner-error        -&gt; ou-banner ou-banner--error
  \- banner banner-warn         -&gt; ou-banner ou-banner--warning
  
  Gate: 2282 -&gt; 2260 violations.

- **fix**(wireframes): drop legacy card CSS (stage 3.7) (2026-05-17) [`017ac0f986ea7fbaa179b1c286880290d12027b1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/017ac0f986ea7fbaa179b1c286880290d12027b1)
  HTML already uses ou-card / ou-card__head / __body / __title. Remove
  the .card / .card-row block from prd7-shared.css.
  
  Files changed:
  \- docs/wireframes/prd7-shared.css (-6 lines)
  
  Gate: 2284 -&gt; 2282 violations.

- **fix**(wireframes): drop legacy badge CSS (stage 3.6) (2026-05-17) [`62d4ec6ca804d73f6f53244ae4572311673257e3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/62d4ec6ca804d73f6f53244ae4572311673257e3)
  HTML already uses ou-tag / ou-tag--success / ou-tag--neutral. Remove
  the .badge / .badge-* block from prd7-shared.css.
  
  Files changed:
  \- docs/wireframes/prd7-shared.css (-13 lines)
  
  Gate: 2297 -&gt; 2284 violations.

- **fix**(wireframes): drop legacy editor-tabs CSS (stage 3.5) (2026-05-17) [`acd262b1b5e876abfe2fa16462211d0a15e8d7aa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/acd262b1b5e876abfe2fa16462211d0a15e8d7aa)
  HTML already uses ou-tabs / ou-tabs__list / ou-tabs__tab. Remove the
  .editor-tabs / .etab block from prd7-shared.css.
  
  Files changed:
  \- docs/wireframes/prd7-shared.css (-15 lines)
  
  Note: DS uses .ou-tabs__tab.is-active, not --active. Existing HTML
  markup already follows the DS convention; no class changes needed.
  
  Gate: 2303 -&gt; 2297 violations.

- **fix**(wireframes): migrate AppShell + Sidebar to ou-side / ou-shell (stage 3.4) (2026-05-17) [`98983c93e117274cbee47e0e0b1dd78c91dd1498`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/98983c93e117274cbee47e0e0b1dd78c91dd1498)
  Rename sidebar/topbar/main/nav-* in prd7-design-tab.html background
  template to DS BEM. Drop the matching Sidebar + Main blocks from
  prd7-shared.css.
  
  Files changed:
  \- docs/wireframes/prd7-design-tab.html (18 legacy class violations -&gt; 0)
  \- docs/wireframes/prd7-shared.css (-59 lines)
  
  Class renames in HTML:
  \- sidebar          -&gt; ou-side
  \- sidebar-header   -&gt; ou-side__brand
  \- sidebar-title    -&gt; brand-text (DS hides-on-collapse helper)
  \- logo-box         -&gt; ou-shell__brand-mark
  \- sidebar-nav      -&gt; ou-side__nav
  \- nav-group-label  -&gt; ou-side__group-title
  \- nav-item         -&gt; ou-side__item
  \- nav-item active  -&gt; ou-side__item is-active (DS uses .is-active, not --active)
  \- nav-icon         -&gt; ou-side__ico
  \- sidebar-footer   -&gt; ou-side__foot
  \- user-row/avatar/user-name/user-role
    -&gt; ou-shell__user/__avatar/__user-meta/__user-name/__user-role
  \- main             -&gt; ou-shell__main
  \- topbar           -&gt; ou-shell__header (DS uses __header, not __top)
  \- test-title       -&gt; class dropped (no DS equivalent for inline page title)
  
  CSS removed:
  \- .sidebar/__header/__nav/__footer/.logo-box/.sidebar-title
  \- .nav-group-label/.nav-item/.nav-item.active/.nav-icon
  \- .user-row/.avatar/.user-name/.user-role
  \- .main/.topbar/.back-btn/.test-title/.topbar-actions
  
  Gate: 2353 -&gt; 2303 violations.
  
  Co-Authored-By: Claude Opus 4.7 &lt;noreply@anthropic.com&gt;

- **fix**(wireframes): migrate toggle/radio to ou-switch/ou-radio (stage 3.3b) (2026-05-17) [`730000db6d720271e8646a696cc5dd7c54514a15`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/730000db6d720271e8646a696cc5dd7c54514a15)
  Rewrite legacy .radio-row/.radio-opt/.toggle/.toggle-row blocks in
  prd7-design-tab.html to the DS BEM structure (ou-radio with __input/
  __ring/__dot, ou-switch--m.is-on with __input/__track/__thumb). Drop
  the matching CSS rules from prd7-shared.css.
  
  Files changed:
  \- docs/wireframes/prd7-design-tab.html (1 radio-row + 2 toggle-row blocks)
  \- docs/wireframes/prd7-shared.css (-16 lines)
  
  Structures:
  \- radio-row/radio-opt -&gt; ou-formfield__inline &gt; label.ou-formfield__inline
    &gt; span.ou-radio.ou-radio--m + __input/__ring/__dot + text
  \- toggle/toggle-row -&gt; ou-formfield__inline &gt; label.ou-switch.ou-switch--m
    .is-on + __input/__track/__thumb + label text
  
  Gate: 2377 -&gt; 2353 violations.
  
  Co-Authored-By: Claude Opus 4.7 &lt;noreply@anthropic.com&gt;

- **fix**(wireframes): migrate form-* class names to ou-formfield (stage 3.3a) (2026-05-17) [`9f4acfaf09105971de85ee98cfe5e8331a417f14`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9f4acfaf09105971de85ee98cfe5e8331a417f14)
  Rename product form classes to DS formfield BEM in 4 files; drop legacy
  .form-* CSS rules from prd7-shared.css. Direct input/select/textarea/
  radio/toggle CSS stays in place as WIREFRAME-INFRA glue until stages
  3.3b (radio/toggle structural refactor) and 3.3c (input/select/textarea
  ou-field wrap).
  
  Files changed:
  \- docs/wireframes/prd7-design-tab.html (form-group/label/hint/error/req)
  \- docs/wireframes/prd7-structure-linear-flat.html (form-group/label/hint)
  \- docs/wireframes/prd7-structure-linear-by-topics.html (form-group/label/hint)
  \- docs/wireframes/prd7-structure-mixed.html (form-label)
  \- docs/wireframes/prd7-shared.css (-9 lines, drop .form-* rules)
  \- docs/wireframes-ds-audit.md (status log + open gaps for 3.3b/3.3c)
  
  Class renames:
  \- form-group  -&gt; ou-formfield
  \- form-label  -&gt; ou-formfield__lbl
  \- req         -&gt; ou-formfield__lbl-req
  \- form-hint   -&gt; ou-formfield__desc
  \- form-error  -&gt; ou-formfield__msg ou-formfield__msg--error
  
  Gate: 2558 -&gt; 2377 violations.
  
  Co-Authored-By: Claude Opus 4.7 &lt;noreply@anthropic.com&gt;

- **fix**(wireframes): drop legacy drawer CSS (stage 3.2) (2026-05-17) [`582509647973cc700227a7f3da5ef7e692f9074b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/582509647973cc700227a7f3da5ef7e692f9074b)
  HTML drawer markup already migrated to ou-drawer__*; remove
  .drawer-* rules from prd7-shared.css and legacy positioning
  selectors from wf-utils.css.
  
  Files changed:
  \- docs/wireframes/prd7-shared.css (-72 lines)
  \- docs/wireframes/ds/wf-utils.css (drop .drawer-overlay / .drawer / .overlay
    from the wf-state positioning selectors)
  
  Removed selectors:
  \- .drawer-overlay, .drawer, .drawer-header, .drawer-title, .drawer-close
  \- .drawer-tabs, .drawer-tab, .drawer-tab.active, .drawer-body
  \- .drawer-footer, .drawer-footer-meta, .drawer-footer-actions
  \- legacy participants in .wf-state &gt; .{drawer-overlay,overlay,drawer}
  
  Gate: 2587 -&gt; 2558 violations. Legacy CSS drawer selectors: 4 -&gt; 0.
  
  Co-Authored-By: Claude Opus 4.7 &lt;noreply@anthropic.com&gt;

- **fix**(wireframes): drop legacy buttons CSS block (stage 3.1) (2026-05-17) [`91097c6e0c36d804091c50bf5694a3e069d9e138`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/91097c6e0c36d804091c50bf5694a3e069d9e138)
  HTML buttons already migrated to ou-btn/ou-iconbtn; remove orphaned
  .btn/.btn-* rules from prd7-shared.css.
  
  Files changed:
  \- docs/wireframes/prd7-shared.css (-20 lines)
  
  Buttons block removed: .btn, .btn-primary, .btn-secondary, .btn-outline,
  .btn-ghost, .btn-danger, .btn-sm, .btn-xs, .btn-icon.
  
  Gate: 2611 -&gt; 2587 violations.
  
  Co-Authored-By: Claude Opus 4.7 &lt;noreply@anthropic.com&gt;

- **fix**(wireframes): migrate drawer-footer to ou-drawer__foot (stage 1) (2026-05-17) [`cc5753d044f5191353f03bff1baf51c86c17f549`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/cc5753d044f5191353f03bff1baf51c86c17f549)
  Replace legacy drawer-footer classes with DS BEM equivalents.
  
  Files changed:
  \- docs/wireframes/prd7-structure-linear-by-topics.html
  \- docs/wireframes/prd7-structure-linear-flat.html
  \- docs/wireframes/prd7-design-tab.html
  
  Replacements:
  \- drawer-footer-actions -&gt; ou-drawer__foot-actions
  \- drawer-footer-meta -&gt; ou-drawer__foot-meta
  \- drawer-footer -&gt; ou-drawer__foot
  
  Gate: 60 -&gt; 0 violations. DS check passed for 22 wireframe files.
  
  Co-Authored-By: Claude Opus 4.7 &lt;noreply@anthropic.com&gt;

- **fix**: Правки по коду для отработки (2026-04-11) [`39c48dadca48bfc438309e77fb494e006519a564`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/39c48dadca48bfc438309e77fb494e006519a564)
  1. поправили выгрузку скормо
  2. Добавили уведомления для типов вопросов (мини инструкции как действовать)
  3. Для скорма:
  \- поправили кнопку завершитть теперь она всегда появляется в конце
  \- результат можно скачать теперь после каждой попытки
  \- Ссылка при переходе теперь открывается в другом окне
  \- При повторном входе в курс и переходе мои результаты открывается полная страница с рекомендациями

- **fix**(adaptive-test): исправить адаптивный тест и улучшить экспорт результатов в SCORM (2026-03-12) [`80599cd1e7423fd73a6eff8b6f27f6878c7c2a9d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/80599cd1e7423fd73a6eff8b6f27f6878c7c2a9d)
  \- добавлено перемешивание вариантов ответов в адаптивном режиме
  \- обновлен финальный экран результатов адаптивного теста
  \- улучшена передача уровней и рекомендаций в SCORM
  \- для архива SCORM используется название теста
  \- добавлен поиск в админ панели блоков Вопрос, темы, тесты

- **fix**(types): resolve TypeScript errors across client and server (2026-02-08) [`daddcaeb9d209dab8e13bd7e66ba9fca88c3de1e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/daddcaeb9d209dab8e13bd7e66ba9fca88c3de1e)
  \- Fix type casting for matching question answers in analytics
  \- Replace null with undefined for optional user fields
  \- Use correct property name (name instead of username) in learner layout
  \- Fix dataJson access and sections type in take-test page
  \- Add null checks for testId in SCORM export routes
  \- Add forceConsistentCasingInFileNames to tsconfig

- **fix**(docker): use dynamic PORT and IPv4 address in healthcheck (2026-02-08) [`7d62cbe9a9c28d62f5a1d689bc682d2836feec6d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7d62cbe9a9c28d62f5a1d689bc682d2836feec6d)
  Replace hardcoded port 5000 with ${PORT} environment variable and
  use 127.0.0.1 instead of localhost to avoid IPv6 resolution issues
  in Alpine containers.

- **fix**(EMS/CJS): Some fixes with type of modules (2026-02-05) [`0bb4cd3ca3cc79c0a6acff781ad80256262c0c7e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0bb4cd3ca3cc79c0a6acff781ad80256262c0c7e)

- **fix**(http server): Listen port of http server has been fixed (2026-02-05) [`d907b715a5adbe80841f2b042fe257cf71fc45ce`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d907b715a5adbe80841f2b042fe257cf71fc45ce)

- **fix**(scripts): The start script has been fixed (2026-02-05) [`fd46c14951a0f107ce326a565c187e010304e392`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fd46c14951a0f107ce326a565c187e010304e392)
  Calling of the cross-env has been added

- **fix**(.env): .env has been removed (2026-02-05) [`c44ea215636c19ba644b0bc74c8d45e39d3242d1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c44ea215636c19ba644b0bc74c8d45e39d3242d1)

- **fix**(dialog.tsx): Definition of the aria-describedby prop has been added (2026-02-05) [`400110eb25c4bb748a0e366bd8c159ee03847474`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/400110eb25c4bb748a0e366bd8c159ee03847474)
  We explicitly indicate that the component description may be missing via definition of aria-describedby prop

### Documentation

- **docs**(access): спецификации и RUNBOOK PRD-13, эскизы в approved/ (2026-06-08) [`3d7f8f41725bd6cb14c763437c4af05d03d065f1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3d7f8f41725bd6cb14c763437c4af05d03d065f1)
  BRD/PRD-13, role-model, implementation-plan, acceptance-matrix и RUNBOOK по RBAC. Эскизы prd13-test-access/user-editor добавлены; согласованные эскизы prd6/7/11 перенесены в wireframes/approved/. ROADMAP синхронизирован.

- **docs**: синхронизировать предпросмотр и формат шаблона с реализацией (2026-06-07) [`908296519dffd7efe38380eaaf911b7411a10670`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/908296519dffd7efe38380eaaf911b7411a10670)
  \- PRD-3 §3.4: адаптивный рейл (разделы по видам PRD-1 §4.3 + системные; тип с
    одним вариантом = 2 уровня, сворачиваемый «тип→варианты» только при 2+);
    content.summary показывает результат раздела (кольцо core.ringChart); активация
    недоступна для уже активного шаблона.
  \- guide: kind-таблица с симметрией intro↔summary (введение/итог раздела) и
    результатом раздела; полный список рендереров resultField (+core.ringChart);
    пример summary на кольце; sectionResult в демо-наборе.
  \- approved wireframe: помечено расхождение рейла с эскизом (адаптивный 2-уровневый),
    источник истины — PRD-3 §3.4.

- **docs**: симметрия intro/summary, результат раздела и source_fingerprint (2026-06-07) [`0481bf2b694e9479a8a6046c0500548def3fb9db`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0481bf2b694e9479a8a6046c0500548def3fb9db)
  \- PRD-1 §4.3: intro/summary приведены к двойной гранулярности (одна на тест в
    linear_flat / по одной на тему в by_topics), как у questions; добавлена заметка
    про симметричные закладки раздела (before_topic/after_topic) и что content.summary
    показывает результат РАЗДЕЛА (итог теста — экран results); снято противоречие с PRD-4.
  \- spec-template-platform §8.2: симметрия content.intro/content.summary + result.* у
    summary = результат раздела.
  \- PRD-3: §5.7 стартовая реконсиляция и source_fingerprint, §6 — колонка в схеме.

- **docs**(prd-3): обновить ссылки на миграцию (014) и механизм применения схемы (2026-06-07) [`700ea4ca405f31353cc4f42f5a207d76f6c48721`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/700ea4ca405f31353cc4f42f5a207d76f6c48721)
  Миграция переименована 011 -&gt; 014 (номер 011 был занят 011_prd11_draw_blueprint);
  поправлены ссылки в §6 и описании Фазы 1. Добавлено примечание, что схему в деплое
  приводит drizzle-kit push (нумерованные SQL — ручной/исторический след).

- **docs**(spec-template-platform): сослаться на практическое руководство по шаблонам (2026-06-07) [`2c3569c69f8b3a34c0e6796754d796c5bf8245ff`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2c3569c69f8b3a34c0e6796754d796c5bf8245ff)
  Делаем гайд обнаружимым из источника истины платформы: добавлена ссылка на
  docs/guides/template-development.md в §1.

- **docs**(guides): добавить руководство по разработке шаблонов оформления (2026-06-07) [`9a33d19a3e9f8dddaa1c73d2c8ba4ded432385d2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9a33d19a3e9f8dddaa1c73d2c8ba4ded432385d2)
  Пошаговый гайд для разработчика внешнего шаблона: структура ZIP, справочник
  manifest.json, контракт макетов/слотов, синтаксис DSL, публичный контекст
  рендера, контентные шаблоны/placeholders, темизация (HSL-токены), рантайм-API
  template.js, демонстрационный набор данных, коды ошибок валидации и проверки
  работоспособности, чек-лист и типичные ошибки. Все примеры выверены по
  реальному коду движка/валидатора и эталонному шаблону default.

- **docs**(spec-template-platform): сверить контракт оболочки/слотов с валидатором (2026-06-07) [`c3a0150dc631b04a3d9ba54003fb944cd8d20777`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c3a0150dc631b04a3d9ba54003fb944cd8d20777)
  Закрываем давно помеченный разрыв §7/§8 между спекой и реализованным
  валидатором/рантаймом (PRD-3/PRD-12).
  
  \- §7: валидатор требует от оболочки только data-slot="page"; маркеры
    data-nav/data-action не обязательны (встроенный default их не объявляет,
    действия Core навешивает делегированием) — жёсткая привязка отнесена к
    будущей фазе расширенных интерактивов.
  \- §8.1/§9: обязательный слот вопроса — question-text (не устаревшее
    question-prompt), это имя проверяет валидатор и заполняет рендерер.

- **docs**(prd-3): отметить трек реализованным, актуализировать спеку и ROADMAP (2026-06-07) [`e99ebc62d47ec2bf4609cb1051aaaf8c7b67192b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e99ebc62d47ec2bf4609cb1051aaaf8c7b67192b)
  PRD-3 закрыт 2026-06-07 — приводим документы к реализованному коду.
  
  \- external-templates.md v2.0→v2.1: статус «реализовано» с разбивкой по фазам;
    маршрут /author/templates (не /admin/); §3.4 — живой предпросмотр через общий
    renderScreenInto + smoke-bundle; §4.3 — фактические проверки движенка
    (исключение/пустой рендер/незаполненный слот/console.error; template.js только
    компиляция; rules — JSON.parse); §6 SQL под реальную схему; §7 +smoke-bundle/
    preview-image; §10 — статус приёмки с отметками о покрытии.
  \- ROADMAP: PRD-3 в таблице статусов закрыт, обновлена дата шапки.

- **docs**(prd-3): закрыть DS Mapping gate 3.5 эскиза админ-шаблонов (2026-06-07) [`b171318d9ca9c77a5ba8e67c5c5c1807d2185d92`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b171318d9ca9c77a5ba8e67c5c5c1807d2185d92)
  Оба ранее открытых пункта таблицы DS Mapping разрешены готовыми
  компонентами @universityrt/ui-kit (новых не создаём, custom-строк нет):
  
  \- зона загрузки файла -&gt; FileUploader (onFiles/accept=".zip"/maxSizeMb=20/
    error/disabled) + строка принятого архива FileItem (status idle|uploading|
    success|error);
  \- индикатор статуса варианта -&gt; Tag с dot + tone (pass-&gt;success, warn-&gt;
    warning, fail-&gt;error, не проверено-&gt;neutral).
  
  Подзаголовок таблицы помечен как пройденный gate; блокеров для старта
  frontend нет.

- **docs**(prd-3): согласованный эскиз админ-страницы шаблонов (Фаза 2/3 UI) (2026-06-07) [`529deeb695b484cb418d12f37471c00adb9f1057`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/529deeb695b484cb418d12f37471c00adb9f1057)
  Полный набор состояний админ-интерфейса шаблонов: список карточек с реальным
  превью, пусто, меню карточки, загрузка (выбор/обработка/принят/отклонён/
  предупреждения), детали, предпросмотр+проверка работоспособности (трёхуровневый
  сворачиваемый список Раздел→Тип→Вариант, статус на варианте), деактивация с
  каскадом, удаление (+нельзя), обновление с отчётом миграции параметров, экспорт.
  DS-компоненты ou-*, чип-счётчики, DS-шеврон фолда; фон модалок — реальный
  список под scrim. Acceptance + wf-notes + DS Mapping.

- **docs**: вычистить устаревшую документацию, актуализировать спеки, подключить markdownlint (2026-06-06) [`192d39c079f83f3feaa48cf402e63266fef09f67`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/192d39c079f83f3feaa48cf402e63266fef09f67)
  \- удалены завершённые/устаревшие файлы (legacy, plans, фазовые отчёты и
    todo PRD-1/PRD-7, reports, phase0-renderer-inventory) — история фаз в git
  \- 21 живой документ ужат под фактический код (README, ROADMAP, спеки PRD-1..12,
    архитектура, гайдлайны, wireframes-нотации)
  \- добавлен docs/architecture/test-editor-contracts.md (контракты редактора PRD-7)
  \- package.json: markdownlint-cli2 + скрипты lint:md / lint:md:fix; 26 md без ошибок

- **docs**(prd-6): closeout Phase 1 — acceptance-чеклист, ROADMAP, HANDOFF (2026-06-04) [`71fffb22fbcd62e4761c882d36203af2f3a64124`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/71fffb22fbcd62e4761c882d36203af2f3a64124)
  \- спека §10: пункты Phase 1 отмечены [x] (включение/период/плагин, FR-02,
    NFR-01/02, блок-экран из шаблона, ClientBridge-источник, failPolicy);
    отложенное вынесено в блок Phase 2 (админ-реестр, UI-выбор конфига,
    диагностика плагина); убран obsolete-пункт превью даты
  \- ROADMAP: статус PRD-6 → «Phase 1 закрыта 2026-06-04» с резюме
  \- HANDOFF.md переписан под закрытый трек PRD-6 (предыдущий PRD-10/11 — в git)

- **docs**(prd-6): эскиз retake — убран баннер «превью даты» (псевдо-данные в настройках) (2026-06-04) [`8182957b845216522d5cb43de71acf3eeb10303b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8182957b845216522d5cb43de71acf3eeb10303b)
  «Последняя полноценная попытка» — рантайм-значение на учащегося (тянется из
  WebTutor перед стартом), на этапе авторинга его не существует. Захардкоженная
  дата в info-баннере читалась как реальные данные → удалена.
  
  \- холст: снят info-Banner с превью даты; подчищены осиротевшие .wf-calc CSS и
    символ #i-clock
  \- таблицы: убрана строка «Превью даты», DS Mapping → «Warning / ошибка»
  \- заметка состояния s-on-webtutor без «+ превью даты»

- **docs**(prd-6): эскиз retake — техкомментарии из холста в сопроводительные таблицы (2026-06-04) [`3ddca7e2dc5fa608e6331ac23415bf5151186385`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3ddca7e2dc5fa608e6331ac23415bf5151186385)
  По канону wireframe (холст = только реальный продуктовый UI): из холста убраны
  технические аннотации, продуктовая микрокопия де-жаргонизирована, а техника
  перенесена в «Пояснения».
  
  \- холст: снят info-баннер «layout system.blocked / версия плагина», убран msg
    про авто-подстановку конфига; подписи без «до SCORM Initialize»,
    «fail-open/fail-closed», «passed/failed/completed/unknown»; warning suspend без
    «suspend_data/SCORM-регистрация»
  \- «Пояснения»: новая строка «Момент проверки» (до SCORM Initialize, NFR-01/02);
    обогащены строки плагина (версия 1.0.0), завершения (маппинг статусов LMS),
    warning suspend (технические детали)
  \- скаффолд: убраны px-фолбэки var(--wf-size-*, Npx) → чистый DS-чек (0 нарушений)

- **docs**(prd-6): убрать избыточное поле «Конфигурация» из пейна (Фаза 1 — один конфиг на плагин) (2026-06-04) [`615bf83f1e40233d8e198b20e82b2befd5d48796`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/615bf83f1e40233d8e198b20e82b2befd5d48796)
  Автор выбирает только плагин; единственный активный конфиг подставляется неявно
  (findEligibilityConfig fallback). Видимый Select конфига вернётся в Фазе 2 при &gt;1 конфиге.
  Заодно убраны стейл-плейсхолдеры «Основной каталог/Сертификации»; синхронизированы Пояснения/Mapping.

- **docs**(prd-6): фикс эскиза — двойная рамка Select (нейтрализован legacy .ou-select фолбэк wf-utils) (2026-06-04) [`d2f2d49b83060e0c22883d77e7ddcf16690933d7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d2f2d49b83060e0c22883d77e7ddcf16690933d7)

- **docs**(prd-6): эскиз «Повторное прохождение» (настройки) + блок-экран (2026-06-04) [`149a274fdd936993720079221649ea84f6950cfc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/149a274fdd936993720079221649ea84f6950cfc)
  Состояния: выкл / вкл·WebTutor (превью даты) / вкл·suspend_data (warning) / ошибка периода;
  учащийся: блок-экран (cooldown) + блок-экран при ошибке (failClosed). Канон wf-nav/wf-state +
  wf-notes/wf-mapping; только DS ou-* (Card/Switch/NumberInput/Select/SegmentedControl/Banner) и
  токены. DRAFT — не в approved/ до согласования.

- **docs**: актуализация под закрытие PRD-10/11 (RTK-трек завершён) (2026-06-04) [`410acc9bfb79b612563b59f709f4d87821cd11d9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/410acc9bfb79b612563b59f709f4d87821cd11d9)
  \- ROADMAP §0.2 + дата: PRD-10/11 → ЗАКРЫТ, трек завершён end-to-end
  \- PRD-10/PRD-11 specs: статус «План» → «Реализован» (Стадии/закрытие, рантайм-фикс tags)
  \- BRD: BR-09/BR-10 реализованы; BR-10-06 упрощён (режим квоты на тег, без гранулярности)
  \- scoring-model §10.7: пререквизит «градуированный checkAnswer» отмечен реализованным
  \- HANDOFF: верхний статус → оба трека завершены и запушены
  \- README: возможности автора — «Цена ответа», «Квоты выдачи по тегам», теги (подтемы)

- **docs**(handoff): PRD-11 завершён — упрощение A1-A3, тегирование (B), UI квот (A4) (2026-06-04) [`e4e7881925b55328c218e198f93421972754abe9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e4e7881925b55328c218e198f93421972754abe9)

- **docs**(prd-11): эскиз тегирования + правила именования тега (§3a) + статус §10 (2026-06-04) [`b9ea0952b8225e49993c69e12cb662bdceba2f06`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b9ea0952b8225e49993c69e12cb662bdceba2f06)

- **docs**(prd-11): редизайн квота-эскиза (свич+инлайн, режим на тег) + тегирование вопросов в охват (2026-06-04) [`b2dd1a8f30d237b86f20e9b9bf5d1df9e3762569`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b2dd1a8f30d237b86f20e9b9bf5d1df9e3762569)
  Эскиз квота-редактора согласован: свич + инлайн-блок в реальной строке темы (tb-topic-row, ui-kit), режим Ровно/Не менее НА КАЖДЫЙ тег, реальные теги, выверен Playwright light/dark. Модель финализирована: strata:[{tag,count,mode}] без modeGranularity/топик-mode.
  
  Тегирование вопросов внесено в охват PRD-11 (§3a, под-трек B): поле questions.tags есть, но задать тег нельзя (нет UI/API) — пререквизит квот. Спека: §3a + FR-09 + стадии A/B + OQ-4; §4/§5/§7 под новую модель. HANDOFF обновлён.

- **docs**(prd-11): эскиз квота-редактора выдачи по тегам (Стадия 0b, на согласовании) (2026-06-04) [`7c9c53b6db765463344d892f9585073b947912e6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7c9c53b6db765463344d892f9585073b947912e6)
  docs/wireframes/prd11-draw-quotas.html — квота-редактор в настройках темы (вкладка «Структура»): 5 состояний (общий/по-тегам/нехватка/ошибка/не-задано) + Пояснения + DS Mapping. DS-компоненты ou-* (Card/SegmentedControl/Select/NumberInput/Table/Banner/Tag/EmptyState), выверен Playwright light/dark. DRAFT — не в approved/ до согласования (GATE для UI-стадии 4).

- **docs**(prd-10): согласование эскиза цены ответа — стабильный фрейм модалки (2026-06-04) [`6732a3d788f69cb6d4f95adb632382f1d7fd1f2c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6732a3d788f69cb6d4f95adb632382f1d7fd1f2c)
  Перенос в approved/ (согласовано 2026-06-04). Устранён скачок размера модалки при смене режима цены ответа: единая ширина ou-modal--xl + фиксированная высота, скролл только в body, прижатие к верху; empty поджат под рамку (ou-empty вместо --page).

- **docs**(prd-10): эскиз цены ответа — убрать дубль заголовка и пояснительные баннеры (2026-06-04) [`3b5948819ba5fbabfbfcfde25f3c7dd91394c59c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3b5948819ba5fbabfbfcfde25f3c7dd91394c59c)
  Один заголовок «Цена ответа» вместо дубля «Оценивание ответа» + «Цена ответа»;
  seg идёт сразу под ним без отдельного лейбла. Пояснительные info-баннеры
  вынесены из холста в wf-notes (правило «в холсте только реальный UI»); скрытые
  wf-annot-бейджи убраны (FR-коды — в DS Mapping). Реальный UI (ошибка/пусто/
  валидация) сохранён. Выверено Playwright.

- **docs**: актуализация BRD (BR-09/BR-10) + ROADMAP §0.2 (PRD-10/PRD-11) (2026-06-04) [`4b35469b9d7a81c68dc8089c697486581e3ac066`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/4b35469b9d7a81c68dc8089c697486581e3ac066)
  BR-09 -&gt; «Цена ответа» (ступенчатая таблица); новый BR-10 «Квоты выдачи по
  тегам» + модель draw_blueprint_json + Этап 11. ROADMAP §0.2: PRD-10/PRD-11
  подняты перед релизом РТК; источник PRD-1...PRD-11.

- **docs**(prd-11): «Квоты выдачи по тегам» — стратифицированная выдача (2026-06-04) [`3b3e5e08fa2eee743335ee372a372a84d0e63e84`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3b3e5e08fa2eee743335ee372a372a84d0e63e84)
  Опциональная квота [тег -&gt; count, mode] на теме (test_sections.draw_blueprint_json):
  exact/min, гранулярность uniform/per_tag (дефолт uniform). Гарантирует объём
  подтемы внутри общей выборки drawCount; stateless. retry-different снят — на
  WebTutor нет cross-attempt стора (adl.data 401, проверено).

- **docs**(prd-10): «Цена ответа» — план реализации + эскиз редактора (2026-06-04) [`dcb2c34d50b63b8e4a64d6eb5fa6309b6a315237`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dcb2c34d50b63b8e4a64d6eb5fa6309b6a315237)
  PRD-10 (graded-answer-scoring): question.scoring (exact|weighted|tiered),
  13 FR, 6 стадий, golden vs РТК key_NEW. Эскиз редактора цены ответа в форме
  вопроса (9 состояний, только DS ou-*, выверен Playwright в light/dark).

- **docs**(scoring-model): §11 «Цена ответа» — градуированный балл за ответ (2026-06-04) [`db7027c9ee47a466e66153250b2be34ff3ffeb62`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/db7027c9ee47a466e66153250b2be34ff3ffeb62)
  Нормирует частичный балл за вопрос: single — веса опций; multiple/matching/
  ranking — ступенчатая таблица «условие → балл» над счётчиками c/x/T; дефолт —
  точное совпадение (старые тесты бит-идентичны). Терминология «рубрика» →
  «Цена ответа»; согласованы ссылки §2.1/§5/§7.3/§10.7/§10.10. Эталон — РТК-2026.

- **docs**(prd-10): scoring-model §11 (rubrics) + PRD-10 plan; actualize ROADMAP/HANDOFF/BRD (2026-06-03) [`04c2d6d82ebf8dab4e7a63e9818aeab400983e9d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/04c2d6d82ebf8dab4e7a63e9818aeab400983e9d)
  scoring-model.md v1.9: new §11 graded answer scoring — tier-table rubrics for multiple/matching, weighted options for single, scoreRatio/partial; cross-refs §2.1/§7.3/§10.7/§10.10 aligned (were marked deferred).
  
  PRD-10 (new spec): graded-answer-scoring.md — implementation plan grounded in verified RTK-2026 data (key_NEW_15-08-25.xlsx + pandas result); 13 FR, question.rubric model, 6 stages, 4 open questions.
  
  ROADMAP §0.2: PRD-N -&gt; PRD-10, elevated to priority 2 (drives RTK certification = 2nd external postprocessor, pre-release). BRD BR-09 now links PRD-10. HANDOFF §9 = next-track handoff.

- **docs**: actualize PRD-2/PRD-5 track status (plan done, HANDOFF acceptance-tooling + issues status) (2026-06-03) [`fdcdef11293416881b3650d8a5dd447d5adb06a3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fdcdef11293416881b3650d8a5dd447d5adb06a3)

- **docs**: HANDOFF — PRD-5 Stage B complete (B4b done), Stage C next (2026-06-03) [`59a27b045434399cc136a6ea104c2b2f47d7bbfb`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/59a27b045434399cc136a6ea104c2b2f47d7bbfb)

- **docs**: HANDOFF — B5 + B4a done, B4b remaining (2026-06-02) [`2450b79dba2260e7bafd0c9884761d12d71dceba`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2450b79dba2260e7bafd0c9884761d12d71dceba)

- **docs**: HANDOFF for PRD-2/PRD-5 continuation (2026-06-02) [`5f2dc13dbfedb1dd01e38934ea5cbb206da59f39`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5f2dc13dbfedb1dd01e38934ea5cbb206da59f39)
  Captures status (Этап A done; PRD-5 B0-B3 + B5 core/port done; B5 wiring/export/preview + B4 remaining), key decisions (index-based source_key, varchar(36) FKs, B5-before-B4, B0 wireframe reuse), the repeatable patterns (parity port, editor section diff-on-save, route-test harness), dev setup (DB container, admin@test.com/admin123) and the gotchas that bit during the work (loadFullTest mock breakage, drizzle-zod uuid/numeric/jsonb quirks, wf-* classes absent in app CSS).

- **docs**(prd-5): scales transfer both value and band level to LMS (2026-06-02) [`9c4d79f69f2c6b47aedc112fc3d3f235bff67486`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9c4d79f69f2c6b47aedc112fc3d3f235bff67486)
  Amendment per request: when a scale has bands, the LMS export must carry both the numeric value AND the band it falls into — not just the band label as before. §8.2 now emits two pseudo-interactions for a banded scale: scale_{key} (raw value) and scale_{key}_level (band.label); a band-less scale emits only scale_{key}. Updates the §8.3 id/recommendation tables, the MBI example (§4.1 dual emission per scale), the implementation plan (Этап B export), and scoring-model §2.2. PRD-5 1.6 -&gt; 1.7; example-mbi 1.1 -&gt; 1.2.

- **docs**(wireframes): resolve DS-mapping custom rows (этап 3.5) (2026-06-02) [`746bde7dbfb3e4ea3cdd8277e3e7411d5d1fead1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/746bde7dbfb3e4ea3cdd8277e3e7411d5d1fead1)
  Resolve the two custom DS-mapping blockers in the scoring wireframe. DnD ordering -&gt; DS Ranking (drag-to-reorder primitive: grip + up/down + numbering), reused for the indicators list (row = name + summary). Visual formula constructor -&gt; composition of DS primitives (Textarea/Combobox/Select/Tag/Button/Banner) with no own rendering; the composed widget is escalated as a DS candidate. No 'custom' remains in the DS-component column.

- **docs**(prd-2,prd-5): design closeout — fix inconsistencies, approve specs + plan (2026-06-02) [`7120542e0846505ca41402633a3a8ae4e0b28e4f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7120542e0846505ca41402633a3a8ae4e0b28e4f)
  Review pass found no core open questions (no «Открытые вопросы» sections). Fix the genuine inconsistencies it surfaced: unify the result-variable name pattern (DB CHECK aligned to ^[a-z][a-z0-9_]{0,63}$), add the missing source (questions/other-scales) and bands rows to the PRD-5 scale field table. Promote PRD-2 (2.1-&gt;2.2) and PRD-5 (1.5-&gt;1.6) from Backlog to Утверждено and the implementation plan from Черновик to Согласован. ROADMAP notes specs approved while implementation is still not started. Deferred items (condition_json, graded checkAnswer + durable-ID prerequisites) remain out of scope by design.

- **docs**(wireframes): promote scoring-tabs to approved/ after sign-off (2026-06-02) [`60d148a8a1bc02addeecb29850929d27aab01500`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/60d148a8a1bc02addeecb29850929d27aab01500)
  Wireframes approved. Move prd2-prd5-scoring-tabs.html into docs/wireframes/approved/ per the DS process; CSS links are absolute so they survive. Fix the internal back-link (sibling now) and update external references in the implementation plan and the UX note.

- **docs**(wireframes): add matching/ranking contribution grids + composite cycle validation (2026-06-02) [`d11c004a3b609002ab24a93c7a2bc258cb1df858`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/d11c004a3b609002ab24a93c7a2bc258cb1df858)
  Close the wireframe gaps surfaced by the spec review: matching contribution grid (rows = directed pairs leftId-&gt;rightId), ranking grid (rows = item@position placements), and a composite cyclic-dependency validation error with the offending child scale flagged (scoring-model §10.9/§10.11, PRD-5 §11). All on DS ou-* components, cloned from existing card/table/banner patterns. Fix 2 pre-existing rem-unit violations in this file (now 0 DS violations). Update wf-notes (grid rows mention pairs/placements; new composite-acyclicity row). Visually verified at 1440px.

- **docs**(prd-9): resolve open questions + scrypt ADR, approve (2026-06-02) [`02c553f98e7de910ca27cf4b6d0db53c9dd07b7a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/02c553f98e7de910ca27cf4b6d0db53c9dd07b7a)
  Close the four §7 open questions with recorded decisions: top-level functions API (hashPassword/verifyPassword, no class); self-describing hash format (scrypt$... with an argon2id$ extension slot, scrypt only implemented); legacy bcrypt via dynamic import + deprecation warning (externalized transition dep, removed in cleanup) — also fixes the prod-bundle contradiction in §3.2; and a fixed OWASP scrypt profile (N=2^17, r=8, p=1, keyLength=32) with Appendix A as the ADR, including a no-migration resource-reduction lever. Status Backlog -&gt; Утверждено; version 1.0 -&gt; 1.1.

- **docs**(brd,prd-3,prd-6): approve specs — status -> Утверждено (2026-06-02) [`9e21a3b528f1742f4a0504c6770b733cd14ad515`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9e21a3b528f1742f4a0504c6770b733cd14ad515)
  Promote BRD (Черновик -&gt; Утверждено; core validated by shipped PRD-1/4/7/8, BR-09 confirmed as a business requirement with a note that its design is a separate future PRD) and PRD-3 / PRD-6 (Backlog -&gt; Утверждено after a review pass found no blockers: no open questions, dependencies fixed as contracts, refs valid). Also fix a pre-existing MD029 ordered-list error in PRD-6.

- **docs**(brd): add BR-09 graded answer scoring (partial credit) (2026-06-02) [`6948e21cff7548aad92d18685e00b7c2149596d9`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/6948e21cff7548aad92d18685e00b7c2149596d9)
  Register partial/graded credit for multiple/matching/ranking as a full business requirement: BR-09 section (problem + 5 requirements), data-model entry (questions.scoring_json rubric), Stage 10 in the roadmap table, Stage-10 acceptance criteria, and a risk row. Graded score is the correctness axis (scoreRatio in [0,1]), orthogonal to scale contributions; exact-match stays the default. Close traceability: ROADMAP §0.2 and scoring-model §10.10 now cite BR-09. BRD 2.1 -&gt; 2.2.

- **docs**(prd-2,prd-5): align requirements with scoring-model — neutral style, topological order, DAG (2026-06-02) [`ea8ebaadddd5accd1053cee385838beb70ef2da7`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ea8ebaadddd5accd1053cee385838beb70ef2da7)
  PRD-5: rewrite formation-history language (z_score / per-edge reverse / answer_score / threshold-formula, header narrative) as plain model description to match the neutral scoring-model; add topological compute order for composite scales in §4.5 (children before parent, acyclic graph). PRD-2: note that sort_order monotonicity guarantees an acyclic indicator graph by construction (scoring-model §10.9). Versions: PRD-5 1.4 -&gt; 1.5.

- **docs**(prd-5,roadmap): align with scoring-model — cycle detection, matching/ranking, graded-scoring PRD (2026-06-02) [`e681e638f48fce58709fe0b8d46c25b6e2ff4d40`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e681e638f48fce58709fe0b8d46c25b6e2ff4d40)
  PRD-5 §11: specify cyclic-dependency detection (DFS over scale -&gt; child-scale graph; any cycle including self-reference blocks save) and topological compute order. PRD-5 §4.3: make matching/ranking contribution units concrete — directed pair (leftId, rightId) and placement (itemId, position), active by the FACT of the learner's response (correctness orthogonal) — replacing the vague «on pair correctness / depends on position»; add JSON examples. ROADMAP §0.2: register «PRD-N (Градуированная оценка ответа)» as the home for partial-credit scoring (scoring-model §10.10).

- **docs**(scoring-model): neutral model — archetypes, acyclicity, partial-credit, matching/ranking (2026-06-02) [`e05f4a82cf8a125946a00e6e47ab4bb994cc03c8`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e05f4a82cf8a125946a00e6e47ab4bb994cc03c8)
  Make the normative model case-agnostic and self-describing (no formation history). Replace the MBI / executive-cert concrete cases with abstract scoring archetypes in §6 (multi-scale questionnaire, competency certification, weighted exam, rubric grading, skill placement). Drop removal narrative (per-edge reverse, enum narrowing, dead options) and the changelog narrative — describe the model as it is. Add §10.9 (composition graph is acyclic: blocking validation + topological eval for scales, sort_order monotonicity for indicators, runtime re-entry guard), §10.10 (partial correctness for multiple-select: scale = sum rule, standard score = exact-match, graded credit deferred to a separate PRD), and §10.11 (contribution units: matching = directed pair, ranking = item@position). Versions 1.3 -&gt; 1.8.

- **docs**: relabel RTK reference as executive certification test (2026-06-02) [`2a6df695348a96a2d1fc0faa6b3b13ff14c5f8ce`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/2a6df695348a96a2d1fc0faa6b3b13ff14c5f8ce)
  Replace the RTK shorthand with the descriptive «сертификационный тест для руководителей» in the scoring UX note (Сценарий B and surrounding prose) and the PRD-8 reference line. Also fix the PRD-8 reference path, stale after the earlier rename (RTK_..._v2.story -&gt; RTK_Test_commits_last.story); the filename itself stays literal.

- **docs**(prd-2/prd-5): align plan and roadmap with grid model (2026-06-02) [`7ccfb22a84492496703307007e823a49f553d9bc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/7ccfb22a84492496703307007e823a49f553d9bc)
  Update the Stage-B outline to grid terminology (Вклады вопросов / вариант×шкала instead of the «Измерения» matrix) and mark per-edge conditions (condition_json) deferred. Add an explicit prerequisites bullet (graduated checkAnswer + durable answer-unit IDs) ahead of the scale side, and drop a stray orphan code fence at EOF. ROADMAP §0.2 now links the implementation plan and the normative scoring model.

- **docs**(prd-2/prd-5): actualize scoring specs to normative grid model (2026-06-02) [`90659ac5bed77e8ec5db15e529757a47d6dd2df5`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/90659ac5bed77e8ec5db15e529757a47d6dd2df5)
  Sync version headers with their own changelogs (scoring-model 1.2-&gt;1.3, PRD-5 1.3-&gt;1.4). Remove the dead "answer_score" source_type from PRD-5 §9.2 — it contradicted §10.3 (answer_score is the correctness axis, never inherited into scales) and was a selectable-but-dead option. Rewrite the MBI example (§2.4) from the removed answer_score-inheritance to the explicit variant×scale grid (source_type=option, explicit value_json 0..5); fix §2.3 variant count (5-&gt;6). Link the implementation plan from scoring-model related docs.

- **docs**(wireframes): add scoring editor wireframe — Шкалы/Показатели tabs (2026-06-02) [`af59da5dcc6c83dd2c03be60b1e3a8f7cd6a126c`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/af59da5dcc6c83dd2c03be60b1e3a8f7cd6a126c)
  Skeleton wireframe plus UX note for the scoring editor tabs (Шкалы / Показатели), realizing the variant×scale contribution grid from scoring-model.md: list rail (Список шкал / Вклады вопросов), per-question expandable cards with the grid, correctness surfaced in Вопросы, totals in Предпросмотр. Wireframe stage — pending approval before React implementation.

- **docs**(prd-2/prd-5): add paired-track implementation plan (A→B→C) (2026-06-02) [`956aad81e5431b1cc6c4b32573b2c5a03893aa29`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/956aad81e5431b1cc6c4b32573b2c5a03893aa29)
  Draft plan for the PRD-2 + PRD-5 paired track per ROADMAP §0.2: staged delivery A (DSL extension: scaleById/countScales, controls_status) → B (scales + question_measurements tables, runtime, author UI) → C (MBI E2E acceptance replacing process_burnout_export.py). Test-scoped scales; each stage gated by green check + vitest. Pending plan approval — implementation not started.

- **docs**(prd-2/prd-5): add cross-cutting scoring model — variant×scale grid (2026-06-02) [`c89bddca94af088c5bcfa9c9f758ad862d86aad2`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c89bddca94af088c5bcfa9c9f758ad862d86aad2)
  Introduce normative scoring-model.md as the shared basis for PRD-2 and PRD-5, and align both specs to it. Question contributions move to an explicit "вариант × шкала" grid (empty = no edge; 0 and negatives allowed); per-edge reverse is dropped in favor of negative contributions. Records the two delivery prerequisites: graduated checkAnswer (currently strictly 0/1) and stable answer-unit IDs (currently array-index based).

- **docs**(prd-7): record live-browser acceptance pass (S11 gate) (2026-05-31) [`275b5dc804f1e3e9021d5c055cbffb5f5f8b58ef`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/275b5dc804f1e3e9021d5c055cbffb5f5f8b58ef)
  Add report section 9 and update the DoD: NFR-17 cold-cache 516ms, axe across all four Drawer tabs (one real aria-command-name fixed, rest known-minor/needs-review), full E2E smoke per section 4.4, and SCORM export plus scorm:player playthrough. Also documents a stale dev-seed finding (legacy question type single_choice vs schema enum single) normalized in the DB, not a code defect.

- **docs**: reprioritize post-MVP — PRD-2 + PRD-5 paired track (2026-05-30) [`ce76b07187f29d8b9161e389ff4e11210288a435`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/ce76b07187f29d8b9161e389ff4e11210288a435)
  Lift PRD-2 (result variables) and PRD-5 (scales) to the top of the
  post-MVP backlog per business request: ship scale results and final
  category to the LMS without manual Excel postprocessing, replacing the
  external report_build script. The two PRDs run as a paired track because
  the result_variables DSL must expose scaleById()/countScales() before
  PRD-5 can compute final categories. Add MBI worked example as the E2E
  acceptance reference and extend the PRD-2/PRD-5 specs with the shared
  DSL contract.

- **docs**: sync MVP closeout — PRD-1/4/7/8 status sections + acceptance report (2026-05-29) [`5d2c57bcb22f5a066bdcdae7b3a707de36e7317d`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5d2c57bcb22f5a066bdcdae7b3a707de36e7317d)
  Sweep through the spec status sections and the PRD-7 acceptance report
  after the 31-commit MVP push (2026-05-28 / 2026-05-29). No code changes
  — pure doc sync.
  
  PRD-1 (templates-content-pages.md):
    \- Status line → ЗАКРЫТ 2026-05-28 (Backend/Runtime/Frontend = 100%
      MVP-scope). Header date bumped to 2026-05-29.
    \- §«Статус реализации» rewritten: backend/runtime list kept,
      frontend list re-mapped onto the PRD-7 S10/S12/S13 sub-phases
      that actually delivered it. Deferred post-MVP item documented
      (text-overflow preview/diagnostics §1.10).
  
  PRD-4 (course-flow-sections.md):
    \- §«Статус реализации» rewritten as ЗАКРЫТ 2026-05-29 with the
      full phase ladder (1 / 2 / 3 / 4a-4f / 5) and the 6-cell
      compatibility-matrix mapping new pairs back to legacy
      flowPolicy enum names. Cross-link to the 19-test acceptance
      file.
  
  PRD-8 (section-router-flow.md):
    \- §«Статус реализации» rewritten as ЗАКРЫТ 2026-05-29 with
      cross-PRD attribution per FR (FR-01..18). Spotlights the
      PRD-8-specific delta (FR-18 router events landed in 5ba2eb6).
    \- §7 Acceptance Criteria checklist flipped to [x] with cross-refs
      showing where each item was delivered (PRD-7 G45 / PRD-4 4c /
      PRD-4 4f / etc).
  
  PRD-7 acceptance report (prd-7-acceptance-report.md):
    \- Header phase line annotated «закрыта 2026-05-28 после S12 + S13».
    \- New §8 «Итоговый closeout 2026-05-29»: cross-PRD timeline
      (PRD-1 → PRD-7 → PRD-4 → PRD-8), current metrics
      (1423/1424 vitest, type-check clean), deferred items list,
      and a per-commit summary across the whole session for
      traceability.
  
  ROADMAP.md:
    \- §0.1 (Storyline-MVP critical path) annotated with a 🎉
      SHIPPABLE 2026-05-29 banner pointing at PRD-7 acceptance §8.
      The «ускоритель» note tense-shifted to past — work is done.
  
  Verification: type-check clean (no code touched); existing test
  counts unchanged (1423/1424 — 1 pre-existing migration
  DB-connectivity fail in tests/migration-prd7.test.ts).

- **docs**(prd-4): v1.1 — orthogonal mode×flowMode model + adaptive+router (2026-05-28) [`52f956200bd8b222c37e3439532c3d5514e07fdf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/52f956200bd8b222c37e3439532c3d5514e07fdf)
  Pre-implementation amendment for PRD-4. The original spec described
  flowPolicy.mode as a flat 5-value enum (legacy_flat / section_linear /
  section_router / section_graph / adaptive_by_section) — that doesn't match
  the actual code, which uses two orthogonal parameters: test.mode
  (standard|adaptive) and test.flowPolicyJson.mode (linear_flat|
  linear_by_topics|router_by_topics). PRD-4 v1.1 reshapes the spec to
  match reality and explicitly documents the 2×3 compatibility matrix.
  
  Key changes in course-flow-sections.md:
  
    §3.1.0 (new) — Model «mode × flowMode»:
      \- flowPolicy splits into mode + flowMode (2×3 = 6 combinations).
      \- Aliasing table maps old plain-enum values to new (mode, flowMode)
        pairs so existing references keep their meaning.
      \- Allows adaptive+router (new combo), defers adaptive+linear_flat
        to a future «Flat adaptive» PRD.
  
    §3.1.1 (new) — Full flowPolicy field list.
  
    §3.1.2 (new) — Compatibility matrix + 4-layer guards:
      \- 6 combos enumerated; (adaptive, linear_flat) blocked.
      \- Strict gating rule: in adaptive mode every section must have
        a non-empty adaptive.topics[].levels[] entry (mixed mode rejected
        per discussion 2026-05-28).
      \- L1 UI / L2 Zod / L3 API / L4 mapper guards spelled out with
        file references.
  
    §4.1 — Author mode selector reshaped:
      \- Two independent selects (mode + flowMode) instead of a single
        flowPolicy.mode dropdown.
      \- Notes the 6 combos with (adaptive, linear_flat) UI-blocked.
  
    §4.6 — (adaptive, linear_by_topics) section_linear-adaptive alias:
      \- Adaptive termination criterion: session continues while learner
        moves up or down the difficulty scale; ends on stability.
      \- Section completion: «passed» = minimum level confirmed
        (any achievedLevel &gt;= 0 with its own passThreshold); else
        passed=false but section still counts as completed for nav.
      \- Strict gating callout.
  
    §4.7 (new) — (adaptive, router_by_topics):
      \- Learner flow: router → topic adaptive session → topic result →
        router again.
      \- routerCompletionPolicy semantics for adaptive sections:
        all_required_completed (any achievedLevel) vs all_required_passed
        (achievedLevel &gt;= 0).
      \- sectionUnlockRules unchanged from standard+router.
      \- Router page itself is not adaptive — kind:router page renders
        uniformly regardless of test mode.
      \- Strict gating reiterated.
  
  ROADMAP.md:
    \- Post-MVP backlog row added: «PRD-N (Flat adaptive)» for the
      (adaptive, linear_flat) combo deferred from PRD-4 v1.1.
    \- Footnote updated: text-overflow-diagnostics removed «предпросмотр
      шаблона» mention (closed in PRD-7 S12-G2); added flat-adaptive note.
  
  Verification: type-check clean, vitest 1373/1373 (docs-only changes).
  PRD-4 ready for implementation phase. Next: code-level changes start
  with Zod schema (L2 guard) so the model is enforced before runtime
  work begins.

- **docs**(prd-1): close PRD-1 — manifest kinds + closeout doc updates (2026-05-28) [`fec75a1b723e67c753cd7c017000a496f562c1e1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fec75a1b723e67c753cd7c017000a496f562c1e1)
  All MVP-blocking PRD-1 task'и closed; remaining substantial item
  (text-overflow preview/diagnostics §1.10) explicitly deferred post-MVP.
  
  Code change:
    \- rtk-storyline manifest gains `kind` field on all 4 variants:
      start.hero / intro.briefing → intro; info.text → info;
      summary.topicResult → summary. Templates default/corporate/minimal
      already had kind annotations.
  
  Doc updates:
    \- implementation-todo.md status header rewritten — PRD-1 CLOSED
      2026-05-28. §1.10 «предпросмотр шаблона» / «галерея» / both marked
      [x] with PRD-7 S12-G2/G3 cross-refs. §1.12 manifest-validation
      items checked: covered by PRD-7 G48 (defaultTemplateManifestSchema
      superRefine enforcing 4 system kinds) + kind fields now present
      in all 4 built-in manifests.
    \- ROADMAP.md PRD-1 row marked Закрыта 2026-05-28 with PRD-7 cross-ref;
      table column count fixed (status/dependencies split).
  
  Verification: type-check clean; vitest 1373/1373 (51 files). No new
  code changes besides the manifest — PRD-1 closeout is documentation
  of work already completed in PRD-7 S12.
  
  Next MVP step: PRD-4 (Runtime flowPolicy, section.*).

- **docs**(prd-7): close S13.8 — verify pass + PRD-7 closure (2026-05-28) [`0918984fb042dc239723199d665ceb6f8f7e883b`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0918984fb042dc239723199d665ceb6f8f7e883b)
  Final S13 sub-phase. Verify items resolved + PRD-7 marked closed.
  
  S13.8 verify outcomes:
    \- G24 (page-row-expand fallback fields): OK. Default info-kind
      manifests (default/corporate/minimal) declare title + body
      placeholders with required:false; «Текст «Далее»» is a layout
      navigation button, not a placeholder. No code change needed.
    \- G12 (s-basic-warning / s-basic-validation): split.
      \* wf-basic-validation: covered substantively by the existing
        hasErrors drawer-banner + «Перейти к ошибкам» action
        (test-editor.tsx:454-457). Visual deviation from wireframe
        (single action vs anchor link-list) — not material.
      \* wf-basic-warning («Несовместимые настройки сохранены и скрыты»,
        FR-25d/f): NOT covered. Data is already preserved on mode-switch
        (basic-settings-section.test.tsx:201-220) but the UX-notification
        banner listing "hidden settings" is missing. Substantial new
        feature; deferred to S13.8b.
  
  Spec updates:
    \- s13-editor-parity.md: G12 → partial; G24 → ok; S13.8 row → closed;
      Definition of Done filled with current state (1373/1373, no axe pass
      here — acceptance is a separate live-browser run).
    \- ROADMAP.md: S11/S12/S13 all marked Closed 2026-05-28; «Последняя
      актуализация» header updated to «PRD-7 полностью закрыт».
    \- prd-7-acceptance-report.md: header status block rewritten — REVOKED
      then RE-CLOSED 2026-05-28 with before/after metric comparison
      (1344 → 1373 tests). Deferred sub-phases enumerated.
  
  Deferred (don't block PRD-7 MVP):
    \- S13.5b — G22 mapping-flow (cross-tab coupling Design draft ↔
      Structure reader needs architectural decision).
    \- S13.8b — G12 wf-basic-warning UX-notification banner.
  
  PRD-7 is now formally closed at the code level. Live-browser acceptance
  (Playwright + axe) is a separate pass that does not block the code
  closeout.

- **docs**(prd-7): annotate S13.5 status — G22 deferred to S13.5b (2026-05-28) [`b96ff0f526e93160fbe6a4569da887f5e900a66a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b96ff0f526e93160fbe6a4569da887f5e900a66a)
  \* G22 mapping-flow row updated with a deferral note: the cross-tab
    coupling between Design draft template change and Structure-tab
    mapping selections needs architectural design (where the pending
    templateChange lives, how Structure reads the new manifest without
    saving, how mapping-row selections are persisted) before any
    wireframe-level UI can land. Punted to S13.5b.
  \* S13.5 plan row rewritten to reflect actual state and effort.

- **docs**(prd-7): reopen as S12 — Design closeout (2026-05-28) [`b7a1fa06872daa426078f65da0c8f323db51032e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/b7a1fa06872daa426078f65da0c8f323db51032e)
  S11 acceptance was issued prematurely on 2026-05-27: the «Оформление» tab still
  contains four stubs that contradict wireframe prd7-design-tab.html.
  
  \- FR-31: sub-rail «Макет» / «Прогресс и шапка» render StubPane instead of
    manifest.params grouped by section
  \- FR-30: «Предпросмотр шаблона» button shows window.alert() instead of the
    tpl-preview-modal
  \- FR-33: template gallery lost when legacy DesignSettingsDialog was retired;
    «Заменить шаблон» is a no-op placeholder
  \- FR-31a: param types image/asset/file/downloadLink/url/multiselect/number
    fall back to a stub banner
  \- orphan client/src/components/design-settings-dialog.tsx still on disk (S10
    cleanup incomplete)
  
  Acceptance report §2.6 mistakenly delegated FR-30 preview to PRD-1, but FR-31
  and FR-33 are PRD-7 wireframe requirements and cannot be deferred. Stub copy in
  design-section.tsx:402,407 itself names «PRD-7» as the deferred owner.
  
  Changes:
  \- docs/specs/prd-7/s12-design-closeout.md — new phase spec (gaps, contract
    change for param.section, DoD, implementation order, risks)
  \- docs/ROADMAP.md — S11 marked partially closed and reopened as S12; critical
    path of Storyline-MVP gains step 2a
  \- docs/prd-7-acceptance-report.md — STATUS UPDATE block at the top: acceptance
    revoked; PRD-7 closes after S12 acceptance passes
  \- docs/specs/prd-1/implementation-todo.md §1.10 — «галерея шаблонов» and
    «вкладка Оформление» checkboxes downgraded; preview note re-attributed to
    PRD-7 S12

- **docs**: actualize PRD-1/PRD-7 statuses + Storyline-MVP path (2026-05-27) [`e971732e1b5fe1c31cea19e3e1e0b0446c9169b1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e971732e1b5fe1c31cea19e3e1e0b0446c9169b1)
  PRD-1 §4: Структура content-page editor + data model (after position) + runtime content-flow (§4.7); PRD-1 §1.12 row-menu/severity. PRD-7 S9 closed / S10 almost-closed statuses. ROADMAP MVP critical-path step 1 done.

- **docs**: restructure roadmap around Storyline-MVP (fastest-MVP critical path) (2026-05-27) [`18058632cdc55d93d887d5ad8bb833b50b414935`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/18058632cdc55d93d887d5ad8bb833b50b414935)
  Fix the MVP boundary (2026-05-27) and reorganize the plan for the fastest path to
  a shippable Storyline-MVP. ROADMAP gains §0.1 (MVP scope + ordered critical path:
  PRD-7 S10 ⊕ PRD-1 closeout -&gt; S11 -&gt; PRD-4 -&gt; PRD-8) and §0.2 (post-MVP backlog).
  Status table + every not-started PRD spec tagged MVP (PRD-4/8, PRD-1 closeout) or
  post-MVP (PRD-6/2/5/3/9). Execution order is now MVP-first (PRD-8 above PRD-6).

- **docs**(prd-7): actualize statuses — S9 closed (2026-05-27), S10 next (2026-05-27) [`8228c46c21541619c0323a3226070b3e23a6af53`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/8228c46c21541619c0323a3226070b3e23a6af53)
  Mark PRD-7 S9 closed across ROADMAP, execution-strategy, implementation-todo and
  s9-s11-in-progress: component + API + regression tests + FR-20c done; full suite
  52 files / 1375 tests green; npm run check clean. Promote S10 (legacy removal) to
  active/next; S11 now blocked only by S10. Record the one open item (Архив section
  with restore — a feature gap, not a test) and the coverage-gate note. Dates → 2026-05-27.

- **docs**(prd-1): add content-pages editor closeout phase (PRD-7 Structure) (2026-05-27) [`3bbc1bd5712698538a76dd61c788f2bc6b5378fc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3bbc1bd5712698538a76dd61c788f2bc6b5378fc)
  Define the deferred PRD-7&lt;-&gt;PRD-1 integration as an explicit, unnumbered
  closeout phase (PRD-1 todo S4): wiring info-page CRUD (add/edit/reorder/template
  select, rich-text) into the read-only Structure tab and retiring the orphaned
  legacy ContentPagesDialog. Cross-reference from ROADMAP and PRD-7 S9-S11.

- **docs**(prd-7): mark S9 part 2 checklist closed; record full-suite status (2026-05-27) [`aa7df9de609d5deb16a267071fd60729c3106778`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/aa7df9de609d5deb16a267071fd60729c3106778)
  API + regression checklists (S2.3) closed with references to the new cases.
  Note: full vitest run green (52 files, 1363 tests); coverage threshold (50%)
  remains a separate pre-existing gate.

- **docs**: reorganize tree into plans/legacy/references/reports; add guides and audit (2026-05-27) [`a2d401ea21bb5057bb485c38a2d6a70199058016`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a2d401ea21bb5057bb485c38a2d6a70199058016)
  Sort loose top-level docs into purpose folders so the docs root stays navigable:
  \- DOCKER_DEPLOY_PLAN.md, refactoring-plan.md -&gt; docs/plans/
  \- dependency-analysis.md, SCORM_TestConstructor_Documentation.docx -&gt; docs/legacy/
  \- scorm-lms-analysis.md -&gt; docs/reports/
  \- the course-feedback spreadsheet -&gt; docs/references/
  \- drop the duplicate root copy of the call-flow PDF (kept under docs/call-flow/)
  
  Adds docs/guides/LOGGING.md (structured-logging conventions),
  docs/reports/deps-audit-2026-05.md, and a root CHANGELOG.md.

- **docs**: restructure into specs/ and architecture/, fix all cross-links (2026-05-26) [`5fc7653eb0f39a009391cfb4beae5e697aeacfc6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5fc7653eb0f39a009391cfb4beae5e697aeacfc6)
  Reorganize the flat docs/ root into a navigable hierarchy:
  \- docs/specs/: BRD, template-platform spec, and PRD-1..9 (each PRD in its own
    prd-N/ subfolder; prd-N- filename prefix dropped as redundant with the folder)
  \- docs/architecture/: service-architecture, test-settings-parameter-structure,
    object-model-refactoring
  \- docs/issues/: wireframes-drawer-todo.md moved here next to the other issue/TODO files
  \- removed docs/test-settings-review-todo.md (status "closed"; TODO is a pre-Issues
    stage, deleted after merge)
  
  Cross-link integrity:
  \- recomputed every relative markdown link affected by the depth change
    (links to moved targets and to non-moved targets such as ROADMAP.md, issues/,
    reports/) so all 161 in-repo relative links resolve
  \- repointed service-architecture source references (server/*, shared/schema.ts)
    to the real repo paths (were dangling at docs/server/* before the move)
  \- remaining 9 broken links are pre-existing and out of scope (guide code examples,
    the unrelated LOGGING.md, the never-created wireframes-prd1-design-pages.md)
  
  markdownlint: introduced zero new violations; pre-existing issues (deps-audit
  line lengths, wireframes-acceptance underscore emphasis, prd-6 ol-prefix,
  manual-acceptance long command) are deferred to the lint cleanup stage.

- **docs**: remove archived Replit chats, README copies, and stale snapshot reports (2026-05-26) [`16831cf0bd99b2f40242a246c830ef522aab3e6a`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/16831cf0bd99b2f40242a246c830ef522aab3e6a)
  Cleanup leftover artifacts from the original Replit prototype and pre-npm-update snapshots:
  \- call-flow/21-12.md, legacy/21-12.md: raw Replit chat logs with stack traces, no value
  \- call-flow/replit.md: archived Replit-flavored README (ARCHIVED-marker)
  \- call-flow/README (1).md, (2).md: archived copies of root README, filenames with spaces
  \- reports/CODE_REVIEW.md: 2026-02-05 review snapshot, predates the 2026-05 dependency overhaul
  \- reports/ANALYSIS_REPORT.md: ARCHIVED app-state snapshot, predates the 2026-05 dependency overhaul
  
  README.md: drop dead links to reports/ANALYSIS_REPORT.md and CODE_REVIEW.md from the docs tree,
  collapse the dangling tree node to a one-line summary that matches the actual layout.

- **docs**: remove call-flow duplicates of design_guidelines and ANALYSIS_REPORT (2026-05-26) [`0f61c6c7d37bd43f4f90f9c3d312b1bb06b66bb1`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0f61c6c7d37bd43f4f90f9c3d312b1bb06b66bb1)
  Both files were byte-identical copies of the canonical versions in docs/guides/
  and docs/reports/ respectively (verified via diff). Removed to enforce
  "single source of truth" principle from DOCUMENTATION-GUIDELINES.md.

- **docs**: actualize guide and PRD/ROADMAP statuses; split PRD-7 by phase (2026-05-26) [`e3a51cdbcd7a5f0a345f9b9403297639014b693f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/e3a51cdbcd7a5f0a345f9b9403297639014b693f)
  Stage 0 (guide):
  \- DOCUMENTATION-GUIDELINES.md: add project-type applicability matrix
    (lib/backend/frontend/full-stack); add "Актуализация важнее удаления" principle;
    add "ROADMAP.md когда допустим" and "TODO-файлы PRD" subsections; mark TypeDoc
    as library-only with backend/frontend alternatives.
  
  Stage 1 (PRD/ROADMAP actualization):
  \- ROADMAP v1.1 -&gt; v1.2: phase statuses S0-S8 closed, S9 active, S10-S11 pending;
    add PRD-9 as step 8 (tech debt backlog).
  \- PRD-7 split: extract closed phases into prd-7-s0-s8-closed.md (362 lines),
    active phases into prd-7-s9-s11-in-progress.md (360 lines); trim
    prd-7-execution-strategy.md (1217 -&gt; 324) and prd-7-implementation-todo.md
    (807 -&gt; 231); delete prd-7-test-settings-editor-refactor.md.
  \- PRD-1 v2.0 -&gt; v2.1: status "In Implementation (Backend 90%, Runtime 95%,
    Frontend 65%)" with done/pending breakdown.
  \- PRD-2..6, PRD-8, PRD-9: status "Backlog (queued)" with ROADMAP step and blockers.
  \- prd-7-baseline: marked as "Archived snapshot (2026-05-10)".
  \- prd-7-decisions, prd-8, wireframes-drawer-todo, test-settings-review-todo:
    update cross-references after PRD-7 split.

- **docs**: update README versions; mark legacy analysis docs as archived (2026-05-26) [`9f42e7029d0576a644ecd9fe5b4ebce6cef75002`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9f42e7029d0576a644ecd9fe5b4ebce6cef75002)
  README dep tables were a full major behind (zod 3, tailwind 3, express
  4, drizzle 0.39, nodemailer 7, multer 2.0, plus a stale recharts row
  for an unused dep) — refresh to the current set and note that
  html2canvas/jsPDF live in the SCORM runtime via CDN, not npm. Drop the
  "use connect-pg-simple in production" hint, that package was removed.
  
  Old analysis snapshots (dependency-analysis.md, reports/ANALYSIS_REPORT,
  call-flow/{ANALYSIS_REPORT,README (1),replit}) reference deps that no
  longer exist. Rewriting them is out of scope — just slap an ARCHIVED
  blockquote on top pointing readers at package.json / the live code.

- **docs**(wireframes): sync prd-7 drawer — Обязательная moved to Состав; new DS classes (2026-05-26) [`0449bf0b28423dfdce3cd431ae7db1bef5d88fcd`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0449bf0b28423dfdce3cd431ae7db1bef5d88fcd)
  Drawer wireframe (prd7-editor-drawer.html): every TopicRow now carries
  a Switch in the header (tb-topic-row__required) — matches the React
  implementation. Settings wireframe (prd7-editor-settings-tab.html):
  drop the «Обязательная» column from every pass-rules table variant and
  update the cross-reference table to point to «Состав» as the only
  location of this control.
  
  Wireframe DS stylesheet (docs/wireframes/tb-components.css) gains the
  classes that the new UI depends on: wf-sep, adaptive-topics level
  banner, tb-feedback-preview as button-like block, and rich-text RTE
  preview rules — keeps wireframe and DS:ou-kit in lockstep.

- **docs**(prd-7): mark S4/4A as closed — all items implemented except FR-20c (2026-05-26) [`40fa152ef96f829026e0f19b06b316d64f8d2dbf`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/40fa152ef96f829026e0f19b06b316d64f8d2dbf)
  After reading test-editor.tsx and use-test-editor.ts in full:
  FR-05 (CloseConfirmDialog), FR-25c/c1/c2 (ChangesPopover visible when dirty),
  FR-25k (ConflictDialog + 409 branch in SaveHttpError handler),
  NFR-19 (focus first tab on open), NFR-20 (keydown focus trap on drawerRef),
  NFR-21 (StatusBadge aria-label, Tag aria-label, IconButton aria-label)
  are all implemented.
  
  FR-20c (anchor navigation from validation summary to the problematic field)
  is the only genuinely pending item — carried to S9.
  
  ROADMAP and execution-strategy updated accordingly.

- **docs**(prd-7): correct S4/4A status — Drawer scaffold is partial, not closed (2026-05-26) [`9813c6373155302db5ce98ec78624acc4869e99f`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9813c6373155302db5ce98ec78624acc4869e99f)
  §1.7 items FR-05 (close confirmation), FR-25c (changes summary),
  FR-25k (conflict dialog), FR-20c (validation anchors), NFR-19/20/21
  (focus/a11y) are still pending. Updated header of implementation-todo,
  execution-strategy §0 and §3 tables, and ROADMAP S4-S8 row to reflect
  "partial" rather than "closed" for 4A.

- **docs**(prd-7): actualize execution strategy — S4-S8 closed, S9 is next (2026-05-26) [`56e948e85113b4dd053304bcfb0309e59e0bcfdc`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/56e948e85113b4dd053304bcfb0309e59e0bcfdc)
  §0 status table: S4–S8 marked closed with commit refs and remaining
  open items listed (FR-05 close confirmation, FR-25c changes summary,
  FR-25k conflict dialog, FR-39 structure warning, NFR-19/20/21 a11y).
  
  §1.4 session table: S4–S8 marked ✓ 2026-05-25; S9 becomes "следующая".
  
  §3 summary table: S4–S8 rows flipped to ✓; footnote updated to
  "Закрыто 12 сессий, осталось 3 (S9, S10, S11)".
  
  Version bump 1.1 → 1.2.

- **docs**: actualize ROADMAP and PRD status as of 2026-05-25 (2026-05-26) [`c343e9d106475c27f4e49c0be4aca0ce6e5d9d10`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/c343e9d106475c27f4e49c0be4aca0ce6e5d9d10)
  ROADMAP: S4-S8 marked closed; PRD-1 row updated to reflect §4.3 partial completion.
  
  prd-7-implementation-todo: S0-S8 closed in header; §1.4 — VariantKind schema,
  silent binding, lifecycle reconcile, replace-variant endpoint, required-fields
  validation marked done with commit refs; §1.7 — Drawer/layout/footer items
  checked, pending: optimistic conflict (FR-25k), close confirmation (FR-05),
  "Показать изменения" (FR-25c), a11y labels; §1.8 — all six sections checked;
  §1.9 and MVP-срез S4 items checked.
  
  prd-1-implementation-todo: §1.12 — VariantKind enum, silent binding,
  server required-fields validation, replace-variant endpoint and auto-create
  kind:questions marked done with commit refs.

- **docs**(prd-7): align execution strategy with actual progress and DS stack (v1.1) (2026-05-24) [`58a214f7717c706c478e7ebdfed4186534fb4243`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/58a214f7717c706c478e7ebdfed4186534fb4243)
  Brings the document back in sync with reality after 2026-05-21/22:
  
  \- New §0 'Текущий статус' table reflecting what actually happened
    (S0+S1+S2+S2-extensions+S3 closed, 17 files × 435 tests passing).
  \- §1.4 session table gains a Status column; S0-S3 marked closed.
  \- §W.0 rewritten: shadcn/ui guidance replaced by UniversityRT DS
    (ou-* / tb-* classes, ENGINERING_HANDBOOK design-system handbook).
    Local wf-*/wf-dialog* classes explicitly forbidden. Mobile scope
    pushed to a separate PRD.
  \- §W.1 minimal set marked complete with links to approved/* files;
    prd7-editor-mobile.html removed as out-of-scope; structure-router
    description updated to the branch-hierarchy model (PRD-7 decisions
    §2.3b) instead of the legacy scenario-map.
  \- New §'Сессия S2-расширения' documents Block 1A-1E + route-gap
    closure (variant.kind contract, silent binding, content_pages
    lifecycle, replace-variant endpoint, required-fields validation,
    POST/PUT migration to TestSettingsService). This phase did not
    exist in the original strategy.
  \- Фаза 4A prompt: shadcn Sheet/Drawer/Tabs swapped for ou-drawer /
    ou-tabs / ou-btn; references the approved drawer wireframe as the
    visual contract; adds RequiredFieldsMissingError (422) handling
    and FR-25j localStorage ban.
  \- §3 summary table gets a Status column and a new S2+ row; W.3C
    marked snyat (states integrated into the single drawer wireframe).

- **docs**(roadmap): add §0 current status snapshot (2026-05-22) (2026-05-24) [`22c31811469cd84ee480748a79b819647287b603`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/22c31811469cd84ee480748a79b819647287b603)

- **docs**(prd-1): code-walk 2026-05-22; add §1.12 for variant.kind/row-menu/severity-rail contracts (2026-05-24) [`170a12046b848dfa4e2ed78fde9b8a66afd84d50`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/170a12046b848dfa4e2ed78fde9b8a66afd84d50)
  \- Verified 14 implementation files + 254 tests still pass
  \- Split preview/reset item: сброс [x], предпросмотр [ ]
  \- New §1.12: 10 unchecked items for PRD-1 §4.3 contract additions
    (VariantKind enum, manifest validation, silent variant binding, row-menu
    composition, severity-rail, required-params validation, replace-variant API)
  \- §3 rewritten with 2026-05-22 walk + commit refs for 2026-05-11 batch

- **docs**(prd-7): close S1 in implementation-todo; wireframes 153/153 approved (2026-05-24) [`9d09381d7a4131bea06263784c8a056754e5d6b6`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/9d09381d7a4131bea06263784c8a056754e5d6b6)

- **docs**(prd-7): align FR/NFR with approved wireframes; bump to v1.2 (2026-05-24) [`dd05c5028eac857fe55b68eaf48d237952311223`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/dd05c5028eac857fe55b68eaf48d237952311223)
  \- FR-43 + §4.1: mobile (&lt; 960px) declared out-of-scope (separate PRD)
  \- FR-36/37 + new FR-37a: feedback editor modal (3 formats, PDF inline-rename, external links)
  \- New FR-46: variant-replace flow via row-menu + replace-variant endpoint
  \- New FR-47: fixed list of 5 settings sub-tabs
  \- FR-25f/25g: warning + callout reformulated as info-banner (FR-40 model)
  \- Status: Черновик → Утверждено для реализации (S0 + S1 closed)

- **docs**(brd): reformulate BR-08-04 to match FR-40 v2 (content preserved on mode change) (2026-05-24) [`3b29baf13dd66d46eea32fdfa50773e1202758ea`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/3b29baf13dd66d46eea32fdfa50773e1202758ea)

- **docs**(wireframes): sync acceptance-checklist with FR-40/FR-36 reformulation (2026-05-24) [`a340ee5f30fd3e790542bfa5f449aaab8520a576`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/a340ee5f30fd3e790542bfa5f449aaab8520a576)

- **docs**(prd-7): preserve content on flowMode change, keep router params in draft (FR-40) (2026-05-24) [`f1ad83239e75fbaf79472ef8851c4cbc50f6a44e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f1ad83239e75fbaf79472ef8851c4cbc50f6a44e)

- **docs**(wireframes): close §2 adaptive variant item in acceptance checklist (2026-05-21) [`880c5266cd0cd0e836061678d5ba50903753496e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/880c5266cd0cd0e836061678d5ba50903753496e)
  Закрыт последний открытый пункт §2 editor-drawer gaps — Adaptive-вариант
  вкладки «Состав» (FR-36). Все 5 пунктов §2 теперь отмечены [x].

- **docs**(wireframes): close 4 acceptance-checklist items with fact-checks (2026-05-21) [`64431f42698dc2c839c596697521130d78e3f605`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/64431f42698dc2c839c596697521130d78e3f605)
  Отмечены 4 пункта чек-листа:
  
  — §Final Sign-off / DS-gate линтер: 21 violation пофикшены, добавлены
    --wf-size-360, --wf-border-w-2, --tb-input-w-md, --tb-input-w-sm,
    --tb-accent-rail-w, --tb-space-half токены, fallback'и var(--token,
    Npx) заменены на var() без fallback.
  
  — §Final Sign-off / legacy-формат: переформулирован — `wf-state` и
    `showState` исключены из списка legacy (это обязательная wireframe-
    инфраструктура per feedback_wf_only_skeleton_frame). Остались как
    legacy-маркеры только `ou-button` и `wf-page-wrap`; fact-check
    показывает 0 occurrences в HTML-разметке.
  
  — §2 / Feedback edit form (FR-36/37): добавлен state s-feedback-edit
    с модалом `ou-modal--l` поверх drawer Состав; описана структура
    (ou-seg формат-селектор, tb-rte editor, single-row links, PDF-assets
    с upload-кнопкой).
  
  — §2 / s-mobile state: удалён из editor-drawer (mobile вне scope
    PRD-7); инвентарь удалённого (state-button, state-блок, CSS,
    notes-таблицы, DS-mapping fallback).

- **docs**(prd-7): wireframes-first hard rule for UI implementation (2026-05-21) [`5f0169d6ca51f9212faf116e3109e579d894e510`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/5f0169d6ca51f9212faf116e3109e579d894e510)
  Add §0.1a to the implementation guide making approved wireframes the
  single source of truth for visual decisions. Two-layer contract is now
  explicit:
  
  \- PRD-1 / PRD-7 / PRD-8 own business contracts + behavioural rules;
  \- docs/wireframes/approved/* own visual details (DOM structure, DS tokens,
    state coverage, meta tags, severity rail, row-menu composition, padding,
    interactions).
  
  Diverging from either is a blocking defect.
  
  DoD for UI tasks gets six new checkpoints (all states covered, DOM match,
  DS tokens only, severity rail per §4.3.7, row-menu per §4.3.3, screenshot
  matches sketch). PR review must include a visual diff against the approved
  file — code-only review does not satisfy DoD.
  
  If a deviation is discovered mid-implementation:
    1. STOP — do not «finish to taste»;
    2. revert local code that introduced the divergence;
    3. take the gap back to wireframes — designer/PM updates the file;
    4. re-approval, file moved into approved/;
    5. resume implementation against the updated sketch.
  
  «PRD doesn't forbid X» / «the sketch doesn't cover this edge case» are
  not accepted as justifications on review — both are signals that the
  sketch needs to be extended and re-approved, not licence to improvise.

- **docs**(prd-1): formalise row-menu composition + severity-rail convention (2026-05-21) [`147d2807cbfceb6a30894e3fb91799633cfb68e3`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/147d2807cbfceb6a30894e3fb91799633cfb68e3)
  Three gaps surfaced during the post-acceptance audit between approved Structure
  wireframes and the PRD documentation. All closed inside PRD-1 §4.3 to keep the
  variant.kind contract in one place.
  
  §4.3.3 — full row-menu composition
  
  Spell out the contents of the `…` row-menu, which previously documented only
  the «Сменить вариант» trigger. The audit found the wireframes carry three
  items with kind-dependent behaviour:
  
    | item            | info | system kinds |
    |-----------------|------|--------------|
    | Сменить вариант…| ✓    | ✓            |
    | Предпросмотр    | ✓    | ✓            |
    | Удалить         | ✓    | НЕ показывается (not disabled — hidden) |
  
  The hidden-not-disabled rule is the key bit: a disabled delete on system rows
  was visual noise; the only way to «remove» a system page is to switch
  flowMode (§4.3.5 already covers the auto-delete of router-row). If a row ends
  up with zero applicable items the `…` button is hidden as well.
  
  §4.3.7 — page-row__meta + severity-rail visual convention
  
  New subsection covering the two related sketch features that had no PRD
  anchor:
  
  \- `.page-row__meta` — second flex-wrap line under the row carrying DS tags
    (`ou-tag--info` / `--warning` / `--error`) with size `--s`;
  \- Severity-rail — colour of border-left auto-derived from the highest-severity
    meta tag or state modifier, priority error &gt; warning &gt; info.
  
  DS token mapping:
    red    → --ou-error-default   (page-row--error  OR ou-tag--error)
    yellow → --ou-warning-default (page-row--warn   OR ou-tag--warning)
    blue   → --ou-info-default    (ou-tag--info only)
    none   → no border in default state ("не засоряем эфир")
  
  Rail continuity: stripe extends onto the next sibling block (`.page-row-expand`,
  `.mapping-warn`) so a single visual channel covers the row header plus its
  inline form / mapping-prompt below.
  
  These are the visual contracts already realised across the three approved
  Structure wireframes (router, linear_flat, linear_by_topics). Frontend teams
  can now derive the implementation directly from PRD-1 without round-trips to
  the sketches.

- **docs**(prd): introduce variant.kind model with per-topic questions + required-params validation (2026-05-21) [`f7910cd69cf7e8e84bd0f461a8be5aab4e2924a4`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f7910cd69cf7e8e84bd0f461a8be5aab4e2924a4)
  Resolves architectural ambiguity in template variant semantics by introducing
  a functional variant.kind in the template manifest with five values:
  questions / router / summary / intro / info.
  
  Key decisions captured:
  
  \- All four system kinds (questions/router/summary/intro) are stored
    uniformly in content_pages as regular records with special `kind`;
    no separate `questionLayoutCombos[]` collection or
    `design_settings_json.questionLayoutCombo` field. A single ejs variant
    renders all four interactive types via conditional logic — combo
    abstraction was an unnecessary indirection.
  \- `kind: questions` has **per-topic locality**: in linear_flat there is
    one record (topicId: null), in linear_by_topics / router_by_topics one
    per topic. Different topics may use different variants.
  \- Silent binding for system kinds (PRD-1 §4.3.2): 1 variant → silent, N &gt; 1
    → default + hint "Available N variants", 0 → fallback to standard
    template + warning.
  \- Variant swap on existing page-row (PRD-1 §4.3.3): triggered from `…`
    row-menu; field-name contract between variants of the same kind keeps
    matching values, drops the rest with explicit warning (diff block).
  \- Lifecycle of system content_pages records (PRD-1 §4.3.5): auto-create
    on topic add, cascade delete on topic removal, silent rebuild on
    flowMode swap (router record dropped silently when leaving
    router_by_topics).
  \- Required-params validation (PRD-1 §4.3.6): unfilled `required: true`
    fields raise an inline alert in page-row-expand, mark the page-row
    warn, surface an aggregated error-dot on the Структура tab and disable
    Save. Server returns a structured error.
  \- router_by_topics tab is rebuilt around a system page-row with kind: router
    plus topics as branches via tree-connectors (PRD-7 §2.3b). completionPolicy
    and sectionUnlockRules move to Настройки → Сценарий and are no longer
    shown in Структура (PRD-8). Old «scenario map» wording is replaced.

- **docs**(wireframes): add DS migration plan and audit prompt (2026-05-17) [`f33116c45680822f1bb190f865d61edc30a89f02`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/f33116c45680822f1bb190f865d61edc30a89f02)

- **docs**(wireframes): sync audit + checklist + drawer-todo with DS migration (stage 6) (2026-05-17) [`171eb7e22356efac1f2869a54d071ac9173f7e2e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/171eb7e22356efac1f2869a54d071ac9173f7e2e)
  Update wireframes-acceptance-checklist.md (items 1, 2, 8, 10 and Final
  Sign-off) and wireframes-drawer-todo.md to reference DS BEM classes
  (ou-drawer__*, ou-btn--*, ou-iconbtn, ou-tabs__*) instead of legacy
  drawer-*/btn-*/btn-icon names. Reflect the move of design-tab.html and
  pages-tab.html into docs/wireframes/legacy/.
  
  Add a "Покрытие gate v2" section to docs/wireframes-ds-audit.md
  documenting the five gate categories (legacy class tokens, CSS selectors,
  raw colors, direct units, optional --strict-inline).
  
  Files changed:
  \- docs/wireframes-acceptance-checklist.md (items 1, 2; iconbtn rename in 8,
    10; gate check added to Final Sign-off)
  \- docs/wireframes-drawer-todo.md (legacy/ paths; DS migration status header)
  \- docs/wireframes-ds-audit.md (+ gate v2 coverage section)

- **docs**(wireframes): clarify legacy/ README — refs not deprecated (2026-05-17) [`fa64cb5376bf76a851f28a11f4acc4c4f7fe4d31`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fa64cb5376bf76a851f28a11f4acc4c4f7fe4d31)
  design-tab.html and pages-tab.html are isolated from the gate but
  remain the content source-of-truth for the Drawer "Оформление" and
  "Структура" tabs. Their deprecation only follows once approved Drawer
  sketches cover the same content (see memory note).

- **docs**: add PRD-1 through PRD-8 specifications, wireframes and project references (2026-05-11) [`0364e45a3abb85993b723145057750f8979f44be`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/0364e45a3abb85993b723145057750f8979f44be)
  \- PRD documents: PRD-1 templates/content-pages, PRD-2 result-variables, PRD-3 external
    templates, PRD-4 course flow, PRD-5 scales, PRD-6 retake cooldown, PRD-7 test settings
    editor, PRD-8 section router; BRD SCORM enhancements, spec-template-platform
  \- Wireframes: 19 prd7-*.html files with acceptance tables, wf-links.js cross-link resolver,
    updated prd7-shared.css; technical notes moved from canvas into acceptance table
  \- References, issues log, ROADMAP and implementation/review todo lists

- **docs**: add SCORM-LMS data exchange analysis and reliability recommendations (2026-02-08) [`fdc460ade098e5030e93dcb1ee94180215569d26`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/fdc460ade098e5030e93dcb1ee94180215569d26)
  Detailed analysis of SCORM runtime communication with LMS including
  step-by-step data flow, 8 identified reliability issues, and 7 proposed
  solutions prioritized by risk level.

- **docs**: reorganize documentation structure (2026-02-07) [`407eac71118d8ed37f064c358b1e0e97bfe7a9fa`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/407eac71118d8ed37f064c358b1e0e97bfe7a9fa)
  \- Move call-flow diagrams to docs/call-flow/
  \- Move legacy READMEs to docs/legacy/
  \- Move Excel report to docs/
  \- Add Docker deploy plan
  \- Update CODE_REVIEW.md

- **docs**: Strucrure of docs has been refactored (2026-02-05) [`416cca30daa0f5c7dd7eb9a8669717f6c355f4e0`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/416cca30daa0f5c7dd7eb9a8669717f6c355f4e0)

## [v1.0.0](https://github.com/vvlad1973/Fullstack-MVP-testing/tree/v1.0.0)

### Initial

- Initial commit (2025-12-18) [`908911da227c1d83e3b02b2762f5dda1bb26b23e`](https://github.com/vvlad1973/Fullstack-MVP-testing/commit/908911da227c1d83e3b02b2762f5dda1bb26b23e)
